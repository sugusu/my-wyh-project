# Commands and Experiment Plans

This directory collects the numbered command files and the current Stage3.6A-R4 experiment plan/results inside the DeformTransGS project tree.

## Layout

- `all_numbered_commands/`: copied numbered command markdown files from `/data/wyh`.
- `current_stage3_6A_R4/`: Stage3.6A-R4 command, analysis script, protocol lock, report, summary, and final terminal summary.
- `COMMANDS_AND_PLANS_SHA256SUMS.txt`: SHA256 checksum list for files in this directory.

## Current Stage4 Mainline Commands

The current `/data/wyh` command files `第一步命令.md` through `第十步命令.md` have been merged into `all_numbered_commands/`. These are the active Stage4 restart, provenance repair, optical-observable closure, and final audit-freeze command records that led to:

- `CASE REAL-CANONICAL-CARRIER-INSUFFICIENT`
- `ATTRIBUTEDEFORMGS-HYPOTHESIS: UNTESTED`
- `PRIMARY-ATTRIBUTE-DEFORMATION-METHOD-LINE: STOP`

## Current Sealed Experiment

The sealed R4 experiment snapshot is stored at:

`/data/wyh/DeformTransGS/sealed_experiments/stage3_6A_R4_render_scope_transparent_scaffold_closure_20260714T091550Z/`

The compressed archive is:

`/data/wyh/DeformTransGS/sealed_experiments/stage3_6A_R4_render_scope_transparent_scaffold_closure_20260714T091550Z.tar.gz`

Final case:

`CASE SOURCE-OBJ-RENDER-SCOPE-NOT-RECOVERABLE`
