Stage 3.4B-R5：Oracle Semantics Repair and Local Tail Origin Audit

中文：

预言机语义修复与局部重尾来源审计

这一步要做三件事：

重新设计真正严格的 oracle；
证明 image-space oracle 的误差是不是由非线性插值不交换造成；
找出 MAE 重尾到底来自哪些 cell、哪些 camera、哪些 alpha/tau 状态。

下面我把关键代码锁细。

cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式状态
============================================================

Stage 3.4B-R4：

Final CASE = METRIC-FAIL。

当前 Gate：

M0 PASS
M1 FAIL
M2 PASS
M3 FAIL
M4 PASS
M5 PASS
M6 FAIL。

重要结果：

alpha_skip = 0.003922

tau_skip = 0.003929

Algebra：

R =
mean(tau_def) / mean(tau_can)

当：

tau_def_k = q_k * tau_can_k

时：

Q_tau =

sum(tau_can_k*q_k)
/
sum(tau_can_k)

满足：

max abs(R-Q_tau)
=
1.63e-11。

因此：

M2 ALGEBRAIC TARGET = PASS。

------------------------------------------------------------

CURRENT P0 uniform phenotype：

stretch_1.25：

physical q = 0.8
median R = 0.7937

stretch_1.50：

q = 2/3
median R = 0.6527

stretch_2.00：

q = 0.5
median R = 0.4782

state-level Spearman：

rho = 1.0000。

------------------------------------------------------------

Area-preserving controls：

shear_k020：

median R = 0.9991

shear_k040：

median R = 0.9958

twist_60：

median R = 0.9443。

但是 mean absolute error：

shear_k020 = 0.3650

shear_k040 = 1.2217

twist_60 = 6.4213。

因此：

CURRENT P0 exhibits：

PHYSICALLY CORRECT CENTRAL TREND

BUT

HEAVY-TAILED LOCAL RESPONSE ERRORS。

------------------------------------------------------------

Low-support pathology Gate：

NOT SUPPORTED。

Formal tau_skip support retains：

coverage = 0.957。

0.5x vs formal threshold：

median R drift approximately 0。

2x vs formal：

max median R drift = 0.0029。

因此：

禁止继续无证据写：

"large MAE is denominator conditioning"。

============================================================
重要数学问题 1：
IMAGE-SPACE ORACLE IS NOT EXACT
============================================================

Stage 3.4B-R4 constructed oracle images：

tau_pixel =
-log(1-A_pixel)

tau_oracle_pixel =
q * tau_pixel

A_oracle_pixel =
1-exp(-tau_oracle_pixel)。

Then CURRENT metric：

bilinear samples alpha at
subpixel material coordinates。

Therefore：

tau_can_sample =

g(
    B(A_can)
)

where：

g(a) = -log(1-a)

B = bilinear interpolation。

Oracle：

tau_def_sample =

g(
    B(
        f(
            q*g(A_can)
        )
    )
)

where：

f(t)=1-exp(-t)。

In general：

g(B(f(q*g(A))))

IS NOT EQUAL TO：

q*g(B(A))。

Because：

bilinear interpolation
does not commute with
nonlinear alpha↔tau transforms。

Therefore the previous Gate：

pixelwise transformed alpha oracle
must recover q with max error <=1e-5

may be mathematically invalid。

本阶段必须验证。

============================================================
重要数学问题 2：
DENOMINATOR EPSILON
============================================================

Current ratio：

R =
tau_def
/
(tau_can +1e-12)

For identity：

tau_def=tau_can

therefore：

R =
tau
/
(tau+1e-12)

which is NOT exactly 1。

Stage R4 identity oracle：

max error =
2.93e-6。

This may be explained directly by
the +1e-12 denominator epsilon
at very low tau。

Evaluation metric 中：

如果 denominator >0，

禁止用 epsilon 修改 ratio。

正式定义：

R = tau_def / tau_can。

If tau_can <=0：

R = NaN。

Measurable-support validity
is handled separately。

============================================================
本阶段名称
============================================================

Stage 3.4B-R5:

Oracle Semantics Repair
and Local Heavy-Tail Origin Audit

中文：

预言机语义修复
与局部重尾来源审计

============================================================
一、唯一目标
============================================================

回答：

Q1.

M1 Optical Oracle FAIL
是否来自：

A. denominator epsilon

和：

B. alpha↔tau nonlinear transform
与 bilinear interpolation
不可交换？

Q2.

CURRENT metric
在真正 mathematically exact oracle
下是否 PASS？

Q3.

CURRENT P0：

median R physically meaningful
but MAE huge，

这些 heavy-tail cells
来自：

low support

alpha/tau saturation or clipping

boundary

camera disagreement

invalid sampling

or

real accumulated-renderer local response？

禁止：

P1
P2
P3

covariance mechanism

clone
split
training
method design。

============================================================
二、输出目录
============================================================

所有输出：

/data/wyh/DeformTransGS/experiments/
stage3_4B_R5_oracle_tail_audit/

禁止覆盖 R4。

============================================================
三、锁定 CURRENT Protocol
============================================================

必须读取 Stage3.4B-R4：

current_metric_protocol_lock.json

重新验证：

carrier SHA

projection source SHA

metric source SHA

extension SHA

forward.cu SHA

camera tensors。

输出：

r5_protocol_lock.json

如果任何 mismatch：

停止。

============================================================
四、修复 Ratio Primitive
============================================================

在：

analysis/current_optical_metric.py

或 R5 独立模块中定义：

```python
from __future__ import annotations

import numpy as np


def exact_positive_ratio(
    numerator: float,
    denominator: float,
) -> float:
    numerator = float(numerator)
    denominator = float(denominator)

    if not np.isfinite(numerator):
        return np.nan

    if not np.isfinite(denominator):
        return np.nan

    if denominator <= 0.0:
        return np.nan

    return numerator / denominator

禁止：

denominator + 1e-12

max(denominator, eps)

clip denominator。

注意：

这不是 support filtering。

这是数学 ratio 定义。

Support validity
单独处理。

============================================================
五、Identity Epsilon Diagnosis

读取 R4 identity oracle
所有：

tau_cell_can。

对旧 ratio：

R_old =
tau / (tau+1e-12)

计算理论误差：

e_pred =
1 - R_old

=

1e-12
/
(tau+1e-12)。

比较：

observed identity error。

关键：

eps = 1e-12

predicted_error = (
    eps
    /
    (
        tau_cell_can
        + eps
    )
)

逐 cell-camera：

observed_error
vs predicted_error。

统计：

max abs difference

Pearson
Spearman。

Gate：

if：

max abs(
observed_error-predicted_error
)
<=1e-10

则：

IDENTITY FAILURE EXPLAINED BY EPSILON。

然后用：

exact_positive_ratio

重跑 identity oracle。

要求：

median error <=1e-12
p95 <=1e-12
max <=1e-10。

输出：

identity_epsilon_diagnosis.csv
identity_oracle_exact_ratio.csv

============================================================
六、Oracle Level O1：
PURE ALGEBRA CORE

保留 Stage R4 algebra test。

10000 cells
49 samples。

tau_can log-uniform：

1e-4 to 3。

q：

0.3 to 1.2。

tau_def =
q*tau_can。

使用：

exact_positive_ratio。

要求：

max abs(R-Q_tau)
<=1e-12

或 float64 machine-scale
合理范围。

输出：

oracle_O1_algebra.csv

============================================================
七、Oracle Level O2：
SAMPLE-LEVEL ACTUAL-TAU ORACLE

使用 CURRENT canonical alpha maps。

使用真实：

material cells

Q7 quadrature

projection

bilinear sampling

camera validity。

对于每：

cell
camera

首先真实采样：

A_can_k。

转换：

tau_can_k。

正式 q：

1.0
0.8
2/3
0.5
4/9。

直接在 SAMPLE LEVEL 构造：

tau_def_k = (
    float(q)
    * tau_can_k
)

IMPORTANT：

不要生成 alpha image。

不要重新 bilinear。

直接送：

tau_can_k
tau_def_k
q_samples

进入：

aggregate_cell_camera_response。

然后 cross-camera aggregation
必须使用 CURRENT logic。

输出：

oracle_O2_sample_level.csv

Gate：

for every q：

R_camera == q

Q_tau_camera == q

R_cell == q

Q_tau_cell == q。

要求：

median error <=1e-12
p95 <=1e-12
max <=1e-10。

============================================================
八、Oracle Level O3：
CONSTANT-FIELD IMAGE ORACLE

目的：

测试完整：

projection
Q7
bilinear
alpha_to_tau
cell aggregation
cross-camera aggregation。

构造 constant optical-depth images。

正式 canonical tau levels：

0.01
0.05
0.1
0.5
1.0
3.0。

对每 tau0：

A_can =

full image constant：

1-exp(-tau0)。

对于：

q：

1.0
0.8
2/3
0.5
4/9

构造：

A_def =

1-exp(
-q*tau0
)。

image shape：

256x256。

三个 cameras
均使用相同 constant images。

但是：

仍使用真实：

material projection

cell bounds

Q7 samples

validity

cross-camera aggregation。

关键代码：

def constant_tau_alpha(
    height: int,
    width: int,
    tau: float,
) -> np.ndarray:
    alpha = (
        1.0
        -
        np.exp(
            -float(tau)
        )
    )

    return np.full(
        (height, width),
        alpha,
        dtype=np.float64,
    )

为什么 O3 是 exact image oracle：

constant alpha field
under bilinear interpolation
remains constant。

因此不存在：

interpolation / nonlinear transform
non-commutation。

Gate：

all tau0
all q：

median abs(R-q)<=1e-12

p95<=1e-12

max<=1e-9。

输出：

oracle_O3_constant_image.csv

============================================================
九、Oracle Level O4：
PIXELWISE TRANSFORMED-ALPHA DIAGNOSTIC

重新执行旧 R4 image oracle：

A_q =
1-exp(
-q*alpha_to_tau(A_can)
)。

但：

O4 is DIAGNOSTIC ONLY。

NOT exact Gate。

对每 actual quadrature sample：

计算：

Route A：

a_can_s = bilinear_sample(
    A_can,
    x,
    y,
)

tau_can_s = alpha_to_tau(
    a_can_s
)

expected_tau_def = (
    q * tau_can_s
)

Route B：

a_q_s = bilinear_sample(
    A_q,
    x,
    y,
)

actual_tau_def = alpha_to_tau(
    a_q_s
)

定义：

NONCOMMUTATION_GAP =

actual_tau_def

expected_tau_def。

输出：

oracle_O4_noncommutation_samples.csv

字段：

q
state_or_camera
cell_id
camera_id
sample_id

tau_can_sample

expected_tau_def

actual_tau_def

gap

abs_gap。

============================================================
十、O4 Cell-Level Error Reconstruction

使用 O4 actual tau samples。

计算：

observed oracle R error：

E_observed =
R_actual - q。

然后根据：

NONCOMMUTATION_GAP

理论：

tau_def_actual

q*tau_can
+
gap。

因此：

R_actual - q

=

mean(gap)
/
mean(tau_can)。

对每 cell-camera：

计算：

E_predicted =

mean(gap)
/
mean(tau_can)。

要求：

abs(
E_observed

E_predicted
)

max <=1e-10。

如果 PASS：

PIXELWISE ORACLE ERROR
EXACTLY EXPLAINED BY
INTERPOLATION NONCOMMUTATION。

输出：

oracle_O4_error_reconstruction.csv

============================================================
十一、O4 Gap vs Local Alpha Variation

对于每 quadrature sample：

读取 bilinear 4-neighbour pixels。

定义：

alpha_local_range =

max(Ia,Ib,Ic,Id)

min(Ia,Ib,Ic,Id)。

定义：

alpha_local_std。

计算：

Spearman：

abs_gap

vs alpha_local_range

abs_gap

vs alpha_local_std。

按 local range quantile bins：

0-20%
20-40%
40-60%
60-80%
80-95%
95-100%。

报告：

median abs gap
p95。

输出：

oracle_O4_gap_vs_local_variation.csv

目的：

验证 image oracle error
是否集中在 high-gradient alpha regions。

============================================================
十二、Revised Oracle Gate

O0:
IDENTITY EXACT RATIO

PASS。

O1:
ALGEBRA CORE

PASS。

O2:
SAMPLE-LEVEL ACTUAL TAU ORACLE

PASS。

O3:
CONSTANT-FIELD IMAGE ORACLE

PASS。

O4:
PIXELWISE IMAGE ORACLE EXPLANATION

PASS if：

cell-level observed error
is reconstructed by noncommutation gap：

max <=1e-10。

O4 不要求：

R=q。

Formal OPTICAL METRIC ORACLE PASS：

O0-O4 ALL PASS。

============================================================
十三、CURRENT P0 Tail Audit

只有 revised oracle PASS 后执行。

使用 Stage3.4B-R4：

current_p0_cell_camera_trace.csv

如果字段不足：

重新运行 CURRENT P0 trace。

禁止改 metric。

正式 states：

stretch_2.00
cubic_l0333
shear_k040
twist_60。

对每 cell-camera：

定义：

def multiplicative_log_error(
    response: float,
    target: float,
) -> float:
    if (
        not np.isfinite(response)
        or not np.isfinite(target)
        or response <=0
        or target <=0
    ):
        return np.inf

    return abs(
        np.log(
            response / target
        )
    )

定义：

E_abs =
abs(R_camera-Q_tau_camera)

E_log =
abs(
log(
R_camera/Q_tau_camera
)
)

factor_error =

max(
R/Q,
Q/R
)。

说明：

R 是 multiplicative optical response ratio。

因此：

log-ratio error
是 mathematically natural
multiplicative error diagnostic。

它不是为了抑制 outlier
而设计的 Huber/trimming。

============================================================
十四、Cell-Camera Tail Feature Table

输出：

p0_tail_feature_table.csv

字段：

state
cell_id
camera_id

u_center
v_center

R_camera
Q_tau_camera

E_abs
E_log
factor_error

tau_cell_can
tau_cell_def

tau_support_ratio =
tau_cell_can / tau_skip

valid_sample_count
valid_sample_fraction

boundary_distance
boundary_pass

A_can_min
A_can_p01
A_can_p05
A_can_median
A_can_p95
A_can_p99
A_can_max

A_def_min
A_def_p01
A_def_p05
A_def_median
A_def_p95
A_def_p99
A_def_max

tau_can_sample_p01
p05
median
p95
p99
max

tau_def_sample_p01
p05
median
p95
p99
max

alpha_can_upper_clip_fraction

alpha_def_upper_clip_fraction。

Upper clip 定义必须来自：

alpha_to_tau actual clip：

alpha >= 1.0-1e-6。

禁止人为定义 0.95
作为 clip。

同时 secondary diagnostic：

A_def >0.9
A_def >0.99

fractions。

============================================================
十五、Cross-Camera Tail Trace

对于每：

state
cell_id

记录三个 cameras：

R_cam0
R_cam4
R_cam8

Q_cam0
Q_cam4
Q_cam8

E_log_cam0
cam4
cam8。

定义：

camera_log_spread =

max(log R_camera)

min(log R_camera)

over finite positive cameras。

bad_camera_factor2_count：

number cameras：

factor_error >2。

输出：

p0_cross_camera_tail_trace.csv

============================================================
十六、Top-Tail Selection

对于每 state：

分别选择：

TOP_ABS_1PCT：

top 1%
by E_abs。

TOP_LOG_1PCT：

top 1%
by E_log。

另外：

TYPICAL：

E_log
最接近 median。

GOOD：

E_log
最接近 p10。

输出：

tail_cell_manifest.csv

禁止只看 mean MAE。

============================================================
十七、Tail Conditioning Comparison

对于每 state：

ALL

vs TOP_ABS_1PCT

vs TOP_LOG_1PCT

比较：

tau_support_ratio median

tau_cell_can median

tau_cell_def median

A_def_p99 median

alpha_def_upper_clip_fraction

boundary_distance median

valid_sample_fraction

camera_log_spread

bad_camera_factor2_count。

输出：

tail_conditioning_comparison.csv

必须回答：

heavy tails 是否：

LOW SUPPORT DOMINATED

UPPER CLIP DOMINATED

BOUNDARY DOMINATED

CAMERA DISAGREEMENT DOMINATED。

============================================================
十八、No Result-Tuned Filtering

本阶段禁止：

删除 top1%

trim mean

winsorize

Huber

改 tau_skip

增加 tau threshold

根据 E_abs/E_log
过滤 cells。

Tail audit only。

============================================================
十九、Spatial Tail Map

每 state：

stretch2
cubic0333
shear040
twist60

生成：

39x39 material maps：

E_log

factor_error

tau_support_ratio

camera_log_spread。

每张图独立。

matplotlib。

禁止 seaborn。

禁止指定颜色。

输出：

tail_maps/

目的：

判断 heavy-tail
是否 spatially clustered。

计算 Moran-like spatial diagnostic
不必实现复杂统计。

只报告：

top1% tail cells 中：

有至少一个 4-neighbour tail neighbour
的 fraction。

同时随机选择相同数量 cells
1000 次。

计算 random mean neighbour fraction。

如果：

tail neighbour fraction

random mean +3 std

则：

SPATIAL CLUSTERING SUPPORTED。

输出：

tail_spatial_clustering.csv

============================================================
二十、CURRENT P0 Distribution Summary

正式所有 states。

Cell-level：

R_cell
Q_tau_cell。

定义：

E_log_cell =
abs(log(R/Q))。

输出：

current_p0_distribution_summary.csv

字段：

state
n

R_median
Q_median

median_abs_R_error

p90_abs_R_error
p95_abs_R_error
p99_abs_R_error

median_log_error
p90_log_error
p95_log_error
p99_log_error

factor2_failure_fraction

factor5_failure_fraction

factor10_failure_fraction

raw_MAE_R。

factor2：

factor_error >2。

factor5：

5。

factor10：

10。

============================================================
二十一、Central Phenotype Gate

uniform：

stretch1.25
stretch1.50
stretch2。

必须：

median R：

monotonic

rho>=0.99

abs(median R-q)<=0.05。

当前数据预计满足。

Area-preserving：

shear020
shear040
twist60。

要求：

abs(median R-1)<=0.10。

定义：

CENTRAL RESPONSE PHENOTYPE SUPPORTED。

============================================================
二十二、Local Tail Failure Gate

不使用 raw MAE
作为唯一 Gate。

定义：

LOCAL HEAVY-TAIL FAILURE SUPPORTED：

在至少：

2 / 3

area-preserving states：

p95 log error

= log(2)

OR

factor2 failure fraction

=0.05。

解释：

log(2)
表示 optical response
与 target 相差至少 2 倍。

这是 multiplicative response
的直接物理解释。

同时：

必须证明：

revised oracle PASS。

如果 oracle FAIL：

不能称 physical tail。

============================================================
二十三、Tail Origin Classification

CASE TAIL-LOW-SUPPORT：

top log-tail：

tau_support_ratio
strongly concentrated near threshold

且符合：

top1% median tau_can
<=0.1 * all median tau_can。

CASE TAIL-UPPER-CENSORING：

top log-tail 中：

=75%

cell-camera rows

存在：

alpha_def_upper_clip_fraction >0。

CASE TAIL-CAMERA-DISAGREEMENT：

top tail：

median camera_log_spread

=log(5)

且：

bad_camera_factor2_count
主要为 1。

CASE TAIL-SPATIAL-RENDER-RESPONSE：

revised oracle PASS

low support NOT supported

upper censoring NOT supported

camera disagreement
不能解释主要 tails

且：

tail spatial clustering SUPPORTED。

解释：

tails are reproducible
spatially localized accumulated-renderer
response failures。

注意：

这仍不是最终 mechanism。

禁止写 footprint。

CASE TAIL-MIXED：

多个因素同时成立。

CASE TAIL-UNRESOLVED：

以上均不成立。

============================================================
二十四、Strict Gates

R50:
CURRENT PROTOCOL LOCK

PASS。

R51:
REVISED OPTICAL ORACLE

O0-O4 PASS。

R52:
CENTRAL RESPONSE PHENOTYPE

PASS / FAIL。

R53:
LOCAL HEAVY-TAIL FAILURE

SUPPORTED / NOT SUPPORTED。

R54:
TAIL ORIGIN CLASSIFICATION

one classification assigned
with numeric evidence。

============================================================
二十五、Final CASE

CASE METRIC-VALID-CENTRAL-DILUTION-LOCAL-TAIL:

R50 PASS
R51 PASS
R52 PASS
R53 SUPPORTED。

结论：

The CURRENT metric passes
mathematically exact optical oracles。

P0 FIXED_COV shows
physically consistent median area-dilution response，

but a reproducible subset of material cells
exhibits severe multiplicative local optical-response failure。

中文：

当前指标通过严格已知答案测试。

固定协方差 P0
在中位意义上呈现正确面积稀释趋势，

但部分材料单元存在
严重且可重复的局部乘性光学响应失稳。

此时：

允许 P1/P2/P3。

但是 Stage3.4B 科学问题必须改写为：

How does Gaussian covariance transport
change both：

central area-dilution response

and

local optical-response tail stability？

禁止继续假设：

P0 is locally correct。

CASE METRIC-VALID-P0-PHENOTYPE-CLEAN:

R51 PASS
R52 PASS
R53 NOT SUPPORTED。

允许 P1/P2/P3。

原 shape-cancellation hypothesis
可以继续。

CASE ORACLE-FAIL:

R51 FAIL。

metric repair。

CASE TAIL-UNRESOLVED:

R51 PASS
R52 PASS
R53 SUPPORTED

but R54 unresolved。

禁止 P1/P2/P3。

继续 tail source audit。

============================================================
二十六、科学边界

Q_tau remains formal target
because algebra requires it。

Weighted target benefit
does NOT need to be >=10%。

The fact that:

Q_tau vs Q_arithmetic
only improves current cubic MAE by 0.1%

does NOT invalidate Q_tau。

It only means：

within-cell q variation
and tau weighting
have a small numerical effect
for the current cubic states。

禁止恢复：

"weighted target benefit Gate"
as validity requirement。

Raw MAE must remain reported。

But：

raw MAE alone
must not be the only consistency Gate
for multiplicative response ratios
with heavy tails。

必须同时报告：

median

p90/p95/p99

log-ratio error

factor-2 / factor-5 / factor-10 failure fractions。

============================================================
二十七、报告

必须输出：

r5_protocol_lock.json

identity_epsilon_diagnosis.csv
identity_oracle_exact_ratio.csv

oracle_O1_algebra.csv
oracle_O2_sample_level.csv
oracle_O3_constant_image.csv

oracle_O4_noncommutation_samples.csv
oracle_O4_error_reconstruction.csv
oracle_O4_gap_vs_local_variation.csv

p0_tail_feature_table.csv
p0_cross_camera_tail_trace.csv
tail_cell_manifest.csv
tail_conditioning_comparison.csv

tail_spatial_clustering.csv

current_p0_distribution_summary.csv

oracle_tail_audit_report.md
stage3_4B_R5_summary.md
stage3_4B_R5_log.txt

============================================================
二十八、报告必须中文回答

A. R4 M1 为什么不能直接解释成 metric invalid。

B. denominator epsilon
是否完全解释 identity max error。

C. exact-ratio identity result。

D. O1 algebra max error。

E. O2 q=0.8 / 2/3 /0.5 max error。

F. O3 constant image oracle max error。

G. 为什么 old pixelwise alpha oracle
不严格满足 R=q。

H. O4 observed error
是否被 noncommutation gap
精确重建。

I. abs gap vs local alpha range
Spearman。

J. R51 revised oracle。

K. P0 uniform median R。

L. uniform rho。

M. shear/twist median R。

N. R52 central phenotype。

O. 每 state raw MAE。

P. 每 state median log error。

Q. p95 log error。

R. factor2 failure fraction。

S. factor5 fraction。

T. factor10 fraction。

U. top-tail tau support ratio。

V. top-tail upper censor fraction。

W. top-tail boundary distance。

X. top-tail camera log spread。

Y. bad-camera factor2 count。

Z. heavy-tail Gate。

AA. tail spatial clustering。

AB. tail origin classification。

AC. R50。

AD. R51。

AE. R52。

AF. R53。

AG. R54。

AH. Final CASE。

AI. 是否允许 P1/P2/P3。

AJ. 如果允许，
Stage3.4B scientific question
应如何改写。

============================================================
二十九、README

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage3.4B-R4 validated the algebraic
tau-weighted target：

Q_tau =
sum(tau_can*q)
/
sum(tau_can)。

However，
the previous image-space optical oracle
was not an exact oracle：

pixelwise alpha→tau scaling followed by
subpixel bilinear alpha sampling
does not commute with
the nonlinear alpha↔optical-depth transform。

The identity oracle was also affected by
an explicit +1e-12 denominator term。

Stage3.4B-R5 replaces these tests with：

exact positive ratios，

sample-level tau oracle，

constant-field image oracle，

and an explicit noncommutation-error reconstruction
for the old pixelwise oracle。

The current P0 also shows a sharp distinction
between physically correct median response
and very large mean errors，
so R5 audits the origin and multiplicative severity
of the local heavy tail
without trimming or result-tuned filtering。

============================================================
三十、实际执行

必须实际：

lock current protocol

implement exact positive ratio

diagnose identity epsilon

rerun identity exact ratio

O1 algebra oracle

O2 actual-sample tau oracle

O3 constant-field image oracle

O4 nonlinear interpolation diagnostic

reconstruct O4 error analytically

trace current P0 cell-camera tails

compute log-ratio errors

compute factor failure fractions

compare top tails vs all samples

audit support
upper censoring
boundary
camera disagreement

map spatial clustering

R50-R54

Final CASE

不要运行 P1。

不要运行 P2。

不要运行 P3。

不要 covariance mechanism。

不要 clone。

不要 split。

不要 train。

不要修改 TSGS。

============================================================
三十一、最终终端总结

最后只输出：

Identity epsilon explanation PASS/FAIL
Exact-ratio identity max error
O1 algebra max error
O2 q0.8 max error
O2 q2/3 max error
O2 q0.5 max error
O3 constant-image max error
O4 noncommutation reconstruction max error
O4 gap-vs-alpha-range Spearman
Revised oracle PASS/FAIL
Uniform median R values
Uniform rho
shear020/shear040/twist median R
Central phenotype PASS/FAIL
stretch2 median/p95 log error
cubic0333 median/p95 log error
shear040 median/p95 log error
twist60 median/p95 log error
factor2 failure fractions
factor5 failure fractions
factor10 failure fractions
top-tail support ratio
top-tail upper-censor fraction
top-tail boundary distance
top-tail camera log spread
tail spatial clustering PASS/FAIL
Tail origin classification
R50
R51
R52
R53
R54
Final CASE
Can run P1/P2/P3 yes/no
Revised Stage3.4B scientific question
oracle_tail_audit_report.md path
stage3_4B_R5_summary.md path

请直接实际运行。

最高优先级：

DO NOT TREAT THE OLD PIXELWISE ALPHA ORACLE
AS MATHEMATICALLY EXACT。

BILINEAR INTERPOLATION
AND ALPHA↔TAU NONLINEAR TRANSFORMS
DO NOT COMMUTE。

REMOVE +1e-12 FROM THE FORMAL RATIO。

USE SAMPLE-LEVEL TAU ORACLE
AND CONSTANT-FIELD IMAGE ORACLE
AS EXACT TESTS。

DO NOT CALL THE LARGE MAE
"DENOMINATOR CONDITIONING"
WHEN M3 DID NOT SUPPORT LOW-SUPPORT PATHOLOGY。

DO NOT FILTER OUT TAIL CELLS。

MEASURE THEM。

THE KEY QUESTION IS NOW：

IS P0 CENTRALLY PHYSICAL
BUT LOCALLY HEAVY-TAILED？
PROMPT