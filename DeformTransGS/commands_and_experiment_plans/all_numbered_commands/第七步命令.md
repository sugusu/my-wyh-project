这轮结果非常关键。**我接受 `CASE EXISTING-TSGS-RUNTIME-RECOVERED`。**

而且现在可以正式下结论：

[
\boxed{\text{TSGS rasterizer 的 opacity / color backward 没问题}}
]

真实边界测试已经给出：

[
|\nabla_{\text{opacity}}L|_2
=1.407241\times10^{-3}
]

[
|\nabla_{\text{color}}L|_2
=1.257929\times10^{-3}
]

两者：

```text
FINITE_NONZERO
nonzero fraction = 1.0
```

所以之前的：

> `INSTALLED-RASTERIZER-NOT-ATTRIBUTE-DIFFERENTIABLE`

**正式作废。**

---

# 一、现在问题已经缩得非常小

当前完整链条：

```text
Clean GT
    PASS

Rasterizer forward
    PASS

Opacity backward
    PASS

Color backward
    PASS

Stage4 O/C/V adapter
    NOT TESTED IN VERIFIED RUNTIME
```

也就是说：

> **现在只差把 Stage4 adapter 放到这个已经验证的 runtime 里真正运行。**

不是换方向。

不是换 rasterizer。

不是重建 GT。

更不是再研究 KIOT。

---

# 二、这轮很可能还定位出了真正的环境问题

失败时 Python 找到的是：

```text
/home/wyh/.local/lib/python3.10/site-packages/
diff_first_surface_rasterization/__init__.py
```

可工作版本是：

```text
/data/wyh/repos/TSGS/submodules/
diff-first-surface-rasterization/
build/lib.linux-x86_64-cpython-310/
diff_first_surface_rasterization/
```

因此我认为**很可能存在 Python package shadowing（包遮蔽）**：

> `~/.local/site-packages` 中的 source package 被优先加载，但它旁边没有正确可加载的 `_C`；而真正带 `_C.so` 的 build/lib 没有被正确绑定。

这是根据你给出的两个实际路径做出的推断。

Python 官方文档说明，Unix 用户级 site-packages 默认位于 `~/.local/lib/pythonX.Y/site-packages`；设置 `PYTHONNOUSERSITE` 后，Python 不会把用户 site-packages 加入 `sys.path`。 ([Python documentation][1])

所以新的 launcher 应该明确：

```bash
PYTHONNOUSERSITE=1
```

然后只把已验证的 build/lib 放到 `PYTHONPATH` 最前面。

另外，TSGS 官方仓库本身确实将 transparent-GS rasterizer 作为其独立子模块/运行依赖。 ([GitHub][2])

但注意：

> **不要声称“找回了原 TSGS 训练环境”。**

因为：

```text
prior TSGS interpreter identified = NO
```

准确说法是：

> **找到了一个现存、已通过真实 forward/backward Gate 的 rasterizer-capable runtime。**

这已经足够 Stage4 使用。

---

# 三、现在直接跑 F2 + F3

这一次不再做环境审计。

名称：

> **Stage 4.0-R2A-F2F3：真实属性 Oracle 管线最终闭环**

只允许：

1. 固定 verified runtime；
2. O/C/V 计算图测试；
3. 如果断，最小修图；
4. 3 个 canonical；
5. 24 个真实 oracle jobs；
6. fresh test render；
7. 独立算指标。

**24 个真实结果出来后，才第一次看属性规律。**

执行：

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续独立科研项目：

/data/wyh/DeformTransGS

============================================================
最高优先级
============================================================

Resume the REAL AttributeDeformGS
oracle smoke experiment。

The runtime problem is CLOSED。

Do NOT perform another environment search。

Do NOT regenerate GT。

Do NOT run 288 jobs。

============================================================
当前冻结科学状态
============================================================

Stage4.0 original scientific outputs：

RETIRED。

Reason：

synthetic_release_error(...)
generated surrogate release metrics。

The previous pattern：

MAT0 -> NONE
MAT1 -> O
MAT2 -> O+C+V

is provenance-invalid。

AttributeDeformGS hypothesis：

UNTESTED。

KIOT：

CONTROLLED-CARRIER-ONLY。

============================================================
Verified GT
============================================================

VERIFIED_GT_ROOT：

/data/wyh/DeformTransGS/experiments/
stage4_0_R2A_GT1_gt_optical_semantics_closure/
clean_gt

Independent validation：

Js relative p99/max =
0 /0

tau relative p99/max =
5.740e-08 /5.951e-08

RGB absolute p99 =
2.977e-08

alpha absolute p99 =
1.481e-08。

GT CLOSED。

Do NOT regenerate or modify GT。

============================================================
Verified rasterizer runtime
============================================================

Stage4.0-R2A-E1 completed。

Previous incorrect classification：

INSTALLED-RASTERIZER-NOT-ATTRIBUTE-DIFFERENTIABLE

is RETIRED。

A real existing compiled extension was found。

Interpreter：

/usr/bin/python3

Python：

3.10.12

PyTorch：

2.6.0+cu124

torch CUDA：

12.4。

Verified rasterizer package root：

/data/wyh/repos/TSGS/submodules/
diff-first-surface-rasterization/
build/lib.linux-x86_64-cpython-310

Package path：

/data/wyh/repos/TSGS/submodules/
diff-first-surface-rasterization/
build/lib.linux-x86_64-cpython-310/
diff_first_surface_rasterization/__init__.py

Compiled extension：

/data/wyh/repos/TSGS/submodules/
diff-first-surface-rasterization/
build/lib.linux-x86_64-cpython-310/
diff_first_surface_rasterization/
_C.cpython-310-x86_64-linux-gnu.so

Unresolved ldd dependencies：

0。

Real rasterizer forward：

PASS。

Direct opacity leaf gradient：

FINITE_NONZERO

nonzero fraction =
1.000000

L2 =
1.407241e-03。

Direct colors_precomp leaf gradient：

FINITE_NONZERO

nonzero fraction =
1.000000

L2 =
1.257929e-03。

F1 PASS。

Therefore：

the runtime rasterizer formally supports
nonzero image-loss gradients with respect to
opacity and colors_precomp。

Do NOT modify rasterizer source or CUDA。

============================================================
Runtime package shadowing isolation
============================================================

The previous failing import resolved to：

/home/wyh/.local/lib/python3.10/site-packages/
diff_first_surface_rasterization/__init__.py

The verified runtime uses the build/lib package
containing the working compiled `_C` extension。

For every Stage4 real-oracle Python process：

set：

PYTHONNOUSERSITE=1

and prepend exactly：

/data/wyh/repos/TSGS/submodules/
diff-first-surface-rasterization/
build/lib.linux-x86_64-cpython-310

to PYTHONPATH。

Use exact interpreter：

/usr/bin/python3。

Do NOT use：

python

python3 resolved implicitly

conda activate

user-site package।

============================================================
本阶段名称
============================================================

Stage 4.0-R2A-F2F3:

Real Attribute Oracle
Pipeline Final Smoke Closure

中文：

真实动态属性 Oracle
管线最终最小闭环

============================================================
唯一目标
============================================================

1. prove Stage4 O/C/V adapter
is graph-connected under the VERIFIED runtime

2. repair only an exact adapter graph disconnect
if one exists

3. fit three REAL canonical Gaussian models

4. run 24 REAL attribute-release jobs

5. save fresh TEST renders

6. independently reproduce metrics

7. determine whether the pipeline is ready
for the full 288-job scientific experiment。

No attribute necessity claim in this stage。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stage4_0_R2A_F2F3_real_pipeline_closure/

New orchestration/debug code：

/data/wyh/DeformTransGS/attribute_study/
real_oracle/pipeline_closure/

============================================================
一、Verified Runtime Launcher
============================================================

Create：

/data/wyh/DeformTransGS/attribute_study/
real_oracle/pipeline_closure/
verified_stage4_python.sh

Exact content semantics：

#!/usr/bin/env bash
set -euo pipefail

export PYTHONNOUSERSITE=1

RASTER_BUILD_LIB="/data/wyh/repos/TSGS/submodules/diff-first-surface-rasterization/build/lib.linux-x86_64-cpython-310"

export PYTHONPATH="${RASTER_BUILD_LIB}:${PYTHONPATH:-}"

export CUDA_VISIBLE_DEVICES="${CUDA_VISIBLE_DEVICES:-2,3}"

exec /usr/bin/python3 "$@"

Make executable。

Every Python process in this stage：

gradient test

canonical optimization

oracle optimization

fresh rendering

must be launched through this script。

The independent metric evaluator
does not require CUDA rasterizer，
but record its interpreter separately。

============================================================
二、Runtime Identity Gate
============================================================

Through the launcher print：

sys.executable

site.ENABLE_USER_SITE

sys.path first10 entries

torch version

torch CUDA

diff_first_surface_rasterization.__file__

_C.__file__。

Require：

sys.executable =
/usr/bin/python3

site.ENABLE_USER_SITE =
False

package path begins with
the verified build/lib root

_C path equals the verified extension path。

Output：

stage4_verified_runtime_identity.json。

Gate G0。

If FAIL：

STOP。

============================================================
三、Lock Current Real Oracle Source
============================================================

Lock：

VERIFIED_GT_ROOT

verified_gt_root_lock.json

Stage4 O/C/V state implementation

render adapter

release policy

loss implementation

camera split

4096-Gaussian carrier generation

transported geometry implementation

verified rasterizer binary。

Record SHA256。

Output：

f2f3_protocol_lock.json。

Gate G1。

============================================================
四、Release Parameter Semantics
============================================================

Under verified runtime
instantiate every release group。

Assert trainable parameter names exactly：

R0_GEOMETRY_ONLY:
[]

R1_O:
[o_raw]

R2_C:
[sh_coeffs]

R3_V:
[v_raw]

R4_O_C:
[o_raw, sh_coeffs]

R5_O_V:
[o_raw, v_raw]

R6_C_V:
[sh_coeffs, v_raw]

R7_O_C_V_FULL:
[o_raw, sh_coeffs, v_raw]。

Report：

parameter name
shape
dtype
device
requires_grad
is_leaf。

Output：

verified_release_parameter_semantics.csv。

Gate G2a。

============================================================
五、Representative Adapter Case
============================================================

Use：

S0_PLANAR_SHEET

MAT1_NEUTRAL_MASS_CONSERVING

D2_STRETCH_X_1P50。

Use transported geometry。

Use VERIFIED_GT_ROOT。

Use the lowest TRAIN camera ID first。

No optimizer。

============================================================
六、Forward Causality
============================================================

For O/C/V independently：

start from identical state。

Render baseline。

Perturb target tensor only。

O：

IDs mod17 ==0

o_raw +=0.01。

C：

IDs mod17 ==0

DC SH coefficient +=0.01。

V：

IDs mod17 ==0

v_raw[:,0] +=0.01。

For V evaluate at least4 distinct
TRAIN cameras。

Render after perturbation。

Measure：

mean RGB absolute diff

max RGB absolute diff

per-camera diff。

Restore tensor bitwise。

Render again。

Require：

mean diff >1e-9

max diff >1e-8

restore max diff <=1e-7。

Output：

verified_adapter_forward_causality.csv。

Do NOT stop before graph trace
if one attribute fails。

============================================================
七、Exact O Graph Trace
============================================================

Trace：

o_raw
→ opacity_logit
→ sigmoid opacity
→ rasterizer opacities input
→ rendered RGB
→ total loss。

For every tensor record：

object id

data_ptr

shape

dtype

device

is_leaf

requires_grad

grad_fn

contiguous

finite fraction。

retain_grad on non-leaf tensors。

Use torch.autograd.grad
with allow_unused=True。

Report gradient state：

NONE

FINITE_ZERO

FINITE_NONZERO

NONFINITE。

Output：

verified_O_graph_trace.csv。

============================================================
八、Exact C Graph Trace
============================================================

Trace：

sh_coeffs

view direction

SH basis

SH RGB before activation/clamp

colors_precomp

rasterizer colors_precomp input

rendered RGB

total loss。

Important：

Stage4 C is defined as
SH degree2 appearance coefficients
evaluated outside the rasterizer。

The actual colors_precomp tensor
must remain graph-connected
to sh_coeffs。

Do not：

detach

numpy

torch.tensor(existing_tensor)

copy into detached buffer。

Output：

verified_C_graph_trace.csv。

============================================================
九、Exact V Graph Trace
============================================================

Trace：

v_raw

deformed material frame：

n
t1
t2

world view direction

local view coordinate l

v_raw dot l

opacity_logit

sigmoid opacity

rasterizer opacity input

rendered RGB

total loss。

Use at least4 TRAIN cameras。

Output：

verified_V_graph_trace.csv。

============================================================
十、Loss Graph Trace
============================================================

Prediction must remain a torch tensor
from rasterizer forward。

GT may be loaded from NumPy。

Report for：

rendered RGB

RGB L1

tau RGB log L1

alpha tau log L1

DSSIM

total loss：

requires_grad

grad_fn。

Explicitly search the prediction path for：

detach

cpu().numpy

numpy

torch.no_grad

inference_mode

torch.tensor(existing_tensor)

from_numpy(prediction)。

Output：

verified_loss_graph_trace.md。

============================================================
十一、First Broken Edge Classification
============================================================

For O/C/V determine：

FORWARD_NOT_ACTIVE

ATTRIBUTE_TO_ACTIVATION

ACTIVATION_TO_RASTER_INPUT

SH_TO_COLOR

LOCAL_VIEW_TO_OPACITY

RASTER_INPUT_TO_RENDER

RENDER_TO_LOSS

NO_BROKEN_EDGE。

Output：

adapter_first_broken_edge.csv。

If no broken edge：

do not modify source。

============================================================
十二、Minimal Adapter Fix
============================================================

Execute ONLY for an exact identified
Stage4 adapter graph disconnect。

Allowed：

remove accidental detach

remove prediction NumPy roundtrip

remove no_grad around train rendering

use graph-connected tensor directly

replace torch.tensor(existing_tensor)
with direct tensor expression

avoid detached intermediate buffer

pass computed opacity directly
to rasterizer

pass graph-connected colors_precomp directly
to rasterizer。

Forbidden：

modify TSGS source

modify CUDA

custom gradient

manual .grad

straight-through estimator

change O/C/V formula

change loss。

Output：

minimal_adapter_graph_fix.md。

Report exact：

file
function
old lines
new semantic。

============================================================
十三、O/C/V Gradient Gate
============================================================

Run exact original gradient test
under verified runtime。

O：

only o_raw trainable。

C：

only sh_coeffs trainable。

V：

only v_raw trainable，
sum loss over >=4 train cameras。

Require each：

finite fraction =1

nonzero fraction >=0.50

L2 >1e-8。

Output：

verified_single_attribute_gradient_test.csv。

============================================================
十四、Directional Derivative
============================================================

For O/C/V independently：

deterministic random unit direction

seed20260714。

Compute：

autograd g dot d。

Central finite difference：

[L(theta+eps*d)-L(theta-eps*d)]
/
2eps。

eps：

1e-2

3e-3

1e-3。

Require：

at least2 epsilon values
have the same nonzero sign

and best relative error <=0.10

OR absolute difference <=1e-7
when derivative magnitude <=1e-6。

Output：

verified_directional_derivative.csv。

============================================================
十五、F2 Gate
============================================================

F2 PASS iff：

G0 PASS

G1 PASS

G2a PASS

O/C/V forward causality PASS

O/C/V original gradient Gate PASS

O/C/V directional derivative PASS。

If F2 FAIL：

Final CASE =
STAGE4-ADAPTER-GRAPH-NOT-CLOSED。

STOP。

Do not fit canonical models。

============================================================
十六、AST Synthetic Isolation
============================================================

Run AST-based executable dependency audit
over all active Stage4 real-oracle source。

String constants used by audit scripts
do not count。

Require：

executable synthetic dependency count =
0

hardcoded metric-value material/release branch =
0。

Output：

f2f3_ast_source_isolation.csv
f2f3_metric_branch_audit.csv。

C6 PASS。

If C6 FAIL：

STOP。

============================================================
十七、Three REAL Canonical Fits
============================================================

Execute ONLY if F2 and C6 PASS。

Fit exactly：

K0：

S0_PLANAR_SHEET
MAT0_NEUTRAL_FIXED_THICKNESS
D0_IDENTITY。

K1：

S0_PLANAR_SHEET
MAT1_NEUTRAL_MASS_CONSERVING
D0_IDENTITY。

K2：

S1_WAVY_MEMBRANE
MAT2_TINTED_MASS_CONSERVING
D0_IDENTITY。

Use4096 fixed Gaussians。

Geometry fixed。

Trainable：

O+C+V。

Use actual：

torch.optim.Adam。

TRAIN cameras：

16 frozen cameras only。

TEST cameras：

8 frozen cameras never used
in optimization or early stopping。

Loss exactly：

1.0 RGB_L1

+
0.5 TAU_RGB_LOG_L1

+
0.2 ALPHA_TAU_LOG_L1

+
0.1 DSSIM。

Maximum4000 iterations。

Patience500。

Run canonical jobs through
verified_stage4_python.sh。

Record EVERY iteration：

iteration

total loss

RGB loss

tau loss

alpha loss

DSSIM

O grad L2

C grad L2

V grad L2

O parameter delta L2

C parameter delta L2

V parameter delta L2。

Save real checkpoints：

canonical_real_models/
K0.pt
K1.pt
K2.pt。

Each checkpoint contains actual tensors：

o_raw

sh_coeffs

v_raw

geometry SHA

iteration

optimizer state dict。

============================================================
十八、Fresh Canonical TEST Render
============================================================

Reload every canonical checkpoint
into a fresh model instance。

Render all8 TEST cameras。

Save actual：

RGB float32 NPY

alpha float32 NPY。

Compute metrics ONLY after arrays exist。

Use a separate evaluation command。

Report：

PSNR

SSIM

median tau RGB Elog

p95 tau RGB Elog

median alpha-tau Elog。

C4 PASS iff all3：

PSNR >=28 dB

median tau Elog <=0.25

median alpha-tau Elog <=0.25。

If C4 FAIL：

Final CASE =
REAL-CANONICAL-CARRIER-INSUFFICIENT。

STOP。

============================================================
十九、Three Representative Deformed Cases
============================================================

Only if C4 PASS。

Q0：

K0
S0
MAT0
D2_STRETCH_X_1P50。

Q1：

K1
S0
MAT1
D2_STRETCH_X_1P50。

Q2：

K2
S1
MAT2
D3_BIAXIAL_XY_1P50。

Transport geometry exactly。

No geometry optimization。

============================================================
二十、Run 24 REAL Release Jobs
============================================================

For each Q0/Q1/Q2：

run exactly：

R0_GEOMETRY_ONLY
R1_O
R2_C
R3_V
R4_O_C
R5_O_V
R6_C_V
R7_O_C_V_FULL。

Total：

3*8 =
24 jobs。

Every job starts from the SAME
canonical checkpoint for its Q case。

R0：

fresh render only。

R1-R7：

actual torch.optim.Adam。

Only declared tensors trainable。

Maximum2000 iterations。

Patience300。

TRAIN cameras only。

TEST cameras excluded。

Do NOT initialize one release
from another release。

Use GPU2/GPU3 deterministically：

sorted job index modulo2。

Launch both workers through
verified_stage4_python.sh。

============================================================
二十一、Optimizer Causality
============================================================

For every R1-R7 job：

save trainable/frozen tensors
before first optimizer step。

After first optimizer step：

require at least one released tensor
L2 change >1e-10。

Require all frozen optical tensors：

max abs change =
0 exactly。

At final checkpoint report
released tensor delta from initialization。

Output：

real_optimizer_parameter_change_audit.csv。

============================================================
二十二、Checkpoint Integrity
============================================================

Every R1-R7 checkpoint contains：

actual o_raw

actual sh_coeffs

actual v_raw

released tensor names

canonical checkpoint SHA

transported geometry SHA

iteration

optimizer state dict。

Reload into fresh instance。

Require：

max tensor reload error =
0。

Output：

real_checkpoint_integrity.csv。

============================================================
二十三、Fresh TEST Render Manifest
============================================================

For all24 jobs：

reload saved state/checkpoint。

Render all8 TEST cameras。

Save：

RGB NPY

alpha NPY。

Expected：

24*8*2 =
384 arrays。

Create：

real_test_render_manifest.csv。

Fields：

Q case
release
camera
array type
path
SHA256
checkpoint SHA
render timestamp。

No scientific metric
may be populated before
the render arrays exist。

============================================================
二十四、Independent Evaluator
============================================================

Create a separate evaluation entry point。

It accepts only：

VERIFIED_GT_ROOT

real_test_render_manifest.csv

saved render arrays。

It must NOT import：

optimization code

release policy

synthetic Stage4 source。

Compute：

TAU_RGB_ELOG

ALPHA_TAU_ELOG

RGB PSNR

SSIM。

Primary：

E_OPT =

0.70 *
median TAU_RGB_ELOG

+
0.30 *
median ALPHA_TAU_ELOG。

Output：

f2f3_real_release_metrics.csv
f2f3_real_primary_error.csv。

Require exactly24 rows。

============================================================
二十五、Independent Metric Reproduction
============================================================

Select deterministic6 rows
seed20260714。

Recompute from saved NPY arrays
in an independent audit script。

Require：

tau/alpha Elog and E_OPT
max difference <=1e-12

PSNR difference <=1e-10。

Output：

f2f3_metric_reproduction.csv。

============================================================
二十六、C5 Gate
============================================================

C5 PASS iff：

24/24 jobs complete

21/21 R1-R7 optimizer jobs
change released parameters
on first step

frozen tensor max change =0

checkpoint reload max error =0

384 TEST arrays exist

24 finite metric rows exist

metric reproduction PASS。

============================================================
二十七、Diagnostic 24-Job Result
============================================================

Report actual E_OPT for：

Q0 R0-R7

Q1 R0-R7

Q2 R0-R7。

Also report：

best release per Q case

R7 vs R0 improvement。

IMPORTANT：

DO NOT classify：

attribute necessary

minimal sufficient state

regime-level pattern

new transport rule。

These3 cases are only
REAL PIPELINE SMOKE DIAGNOSTICS。

============================================================
二十八、F3 Gate
============================================================

F3 PASS iff：

C4 PASS

C5 PASS

C6 PASS。

============================================================
二十九、Final CASE
============================================================

CASE REAL-ATTRIBUTE-ORACLE-PIPELINE-READY：

F2 PASS

F3 PASS。

Conclusion：

A real differentiable O/C/V
attribute-release pipeline is established。

Evidence includes：

real rasterizer opacity/color gradients

real O/C/V forward causality

real O/C/V autograd gradients

directional derivative agreement

three real canonical fits

24 real Adam release jobs

actual parameter changes

frozen tensor invariance

actual optimized tensor checkpoints

384 fresh TEST render arrays

independent metric reproduction。

ALLOW：

Stage4.0-R2B full real
288-job attribute sufficiency experiment。

------------------------------------------------------------

CASE STAGE4-ADAPTER-GRAPH-NOT-CLOSED：

F2 FAIL。

STOP and report exact edge。

------------------------------------------------------------

CASE REAL-CANONICAL-CARRIER-INSUFFICIENT：

C4 FAIL。

STOP current carrier。

------------------------------------------------------------

CASE REAL-ORACLE-PROVENANCE-FAIL：

C5/C6 FAIL。

STOP。

============================================================
三十、Required Outputs
============================================================

stage4_verified_runtime_identity.json
f2f3_protocol_lock.json

verified_release_parameter_semantics.csv
verified_adapter_forward_causality.csv

verified_O_graph_trace.csv
verified_C_graph_trace.csv
verified_V_graph_trace.csv
verified_loss_graph_trace.md

adapter_first_broken_edge.csv
minimal_adapter_graph_fix.md
if a fix occurs

verified_single_attribute_gradient_test.csv
verified_directional_derivative.csv

f2f3_ast_source_isolation.csv
f2f3_metric_branch_audit.csv

canonical_real_fit_history/*.csv
canonical_real_models/*.pt
canonical_real_fit_metrics.csv

real_oracle_fit_history/**/*.csv
real_optimizer_parameter_change_audit.csv
real_checkpoint_integrity.csv

real_oracle_test_renders/**/*.npy
real_test_render_manifest.csv

f2f3_real_release_metrics.csv
f2f3_real_primary_error.csv
f2f3_metric_reproduction.csv

stage4_0_R2A_F2F3_report.md
stage4_0_R2A_F2F3_summary.md
stage4_0_R2A_F2F3_log.txt

============================================================
三十一、最终终端只输出
============================================================

A. G0

B. exact runtime interpreter

C. user site enabled yes/no

D. rasterizer package path

E. _C path

F. G1

G. R0-R7 trainable names

H. O forward mean/max diff

I. C forward mean/max diff

J. V forward mean/max diff

K. O first broken edge

L. C first broken edge

M. V first broken edge

N. O grad finite/nonzero/L2

O. C grad finite/nonzero/L2

P. V grad finite/nonzero/L2

Q. O directional derivative
autograd / best finite diff / relative error

R. C directional derivative values

S. V directional derivative values

T. F2

U. executable synthetic dependency count

V. hardcoded metric branch count

W. C6

X. K0 canonical
PSNR/tau Elog/alpha Elog

Y. K1 canonical
PSNR/tau Elog/alpha Elog

Z. K2 canonical
PSNR/tau Elog/alpha Elog

AA. C4

AB. real jobs expected/completed

AC. optimizer first-step changed jobs

AD. frozen tensor max change

AE. checkpoint reload max error

AF. TEST render array count

AG. finite metric row count

AH. metric reproduction max
E_OPT / PSNR error

AI. C5

AJ. Q0 R0-R7 actual E_OPT

AK. Q0 best release

AL. Q0 R7 vs R0 improvement

AM. Q1 R0-R7 actual E_OPT

AN. Q1 best release

AO. Q1 R7 vs R0 improvement

AP. Q2 R0-R7 actual E_OPT

AQ. Q2 best release

AR. Q2 R7 vs R0 improvement

AS. F3

AT. Final CASE

AU. real attribute oracle pipeline ready yes/no

AV. allow Stage4.0-R2B full real experiment yes/no

AW. AttributeDeformGS hypothesis status

AX. KIOT status

AY. report path

AZ. summary path

============================================================
三十二、README
============================================================

Update：

/data/wyh/DeformTransGS/README.md

Record：

Stage4.0-R2A-E1 retired the previous
"rasterizer non-differentiable" classification。

The earlier failure occurred before
the compiled `_C` extension
successfully loaded。

A working existing rasterizer binary
was found under the locked TSGS
submodule build/lib tree。

Using the exact working package/binary，
a real rasterizer forward completed。

Direct leaf opacity gradients were：

FINITE_NONZERO

nonzero fraction1.0

L2 approximately1.407e-3。

Direct colors_precomp gradients were：

FINITE_NONZERO

nonzero fraction1.0

L2 approximately1.258e-3。

Therefore the rasterizer runtime
supports attribute gradients。

The previously failing package resolution
entered the user site-package tree，
while the verified package is loaded
from the TSGS build/lib tree containing
the compiled extension。

Stage4 processes are henceforth launched
with user-site packages disabled
and the verified rasterizer build/lib
explicitly locked。

The scientific AttributeDeformGS hypothesis
remains UNTESTED。

Stage4.0-R2A-F2F3 now closes
the Stage4 O/C/V adapter graph，
fits three real canonical carriers，
and runs24 real Adam release jobs
with fresh TEST renders
and independent metric reproduction。

No attribute-necessity claim is permitted
until the full real experiment。

============================================================
三十三、实际执行
============================================================

必须实际：

create verified launcher

disable user site

lock verified build/lib first on PYTHONPATH

verify runtime identity

trace O/C/V forward causality

trace O/C/V graph

trace loss graph

identify first broken edge

apply minimal adapter fix only if necessary

run original gradient Gates

run directional derivatives

run AST C6

if F2/C6 PASS：

fit3 canonical models

fresh TEST render canonical models

C4

if C4 PASS：

run24 actual Adam jobs

audit first optimizer step

audit frozen tensors

reload checkpoints

save384 TEST NPY arrays

run separate evaluator

reproduce6 metric rows

report Q0/Q1/Q2 actual E_OPT

F3

Final CASE

绝对不要：

environment audit again

regenerate GT

run288 jobs

generate synthetic metrics

claim MAT0->NONE

claim MAT1->O

claim MAT2->O+C+V

design predictor

return to KIOT。
PROMPT
```

## 我的真实评价

现在已经跨过了一个很重要的坑：

> **光栅化器是可微的。**

这一点现在不是看源码猜，而是**真实 direct leaf tensor（直接叶子张量）反向传播测试证明了**。

所以后面如果 O/C/V 梯度再是 0：

> 锅就只能在我们的 Stage4 adapter 计算图。

但我还有一个很强的判断：

> **下一轮 O 和 C 大概率能过；V 是否能过才是真正值得看的。**

因为 O 直接生成 opacity，C 直接生成 `colors_precomp`，而这两条 rasterizer 边界已经实际验证非零梯度。V 则多了一层：

[
v_{\rm raw}
\rightarrow
v^\top l
\rightarrow
opacity
]

因此 V 才可能暴露局部材料坐标、view direction（观察方向）或广播维度问题。

**跑 F2F3。下一份报告里的 Q0/Q1/Q2，会是这个新方向第一次真正可信的属性释放结果。**

[1]: https://docs.python.org/es/3/library/site.html?utm_source=chatgpt.com "Site-specific configuration hook — documentación de Python"
[2]: https://github.com/longxiang-ai/TSGS?utm_source=chatgpt.com "TSGS: Improving Gaussian Splatting for Transparent ..."
