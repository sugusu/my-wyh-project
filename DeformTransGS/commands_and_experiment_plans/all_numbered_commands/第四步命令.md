这次我的判断是：

> **C1 拦截是正确的，但 `CASE REAL-ORACLE-GT-INVALID` 这个名字下得太重。**

现在更准确应是：

> **`GT-OPTICAL-REFERENCE-MISMATCH`**
>
> 即：**GT 光学参考语义没有闭环。**

原因很明显：

[
J_s\ error=0
]

但：

[
\tau\ relative\ p99=4.482\times10^{-4}
]

[
max=4.874\times10^{-4}
]

而且 p99 和 max 非常接近。这个形态不像随机数值炸裂，反而像**稳定的系统性偏差**。

常见原因包括：

* 像素射线使用 ((u,v)) 还是 ((u+0.5,v+0.5))；
* ray direction（射线方向）是否归一化；
* camera→surface 还是 surface→camera 方向；
* GT 原实现与独立审计的 `cos_theta` 计算次序不同；
* saved `triangle_id` 对应像素语义不同；
* float32 保存之前存在另一套中间计算；
* 独立审计所谓“matched material sample”实际上不是**同一个像素射线命中的同一个 triangle**。

**不能放宽 C1。**

下一步应该精确找到 (4.5\times10^{-4}) 从哪里来。闭环后直接继续真实 24-job smoke gate，不再重新讨论科学假设。

---

# 新 Codex 命令

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式科学状态
============================================================

Stage4.0 scientific outputs are RETIRED。

Stage4.0-R1 confirmed：

the old oracle used synthetic_release_error(...)
and did not perform real optimization。

AttributeDeformGS hypothesis status：

UNTESTED。

Stage4.0-R2A then stopped at C1。

Observed independent GT audit：

Js relative p99/max：

0 /0。

tau_rgb relative p99/max：

4.482e-4 /4.874e-4。

RGB absolute p99：

2.424e-4。

alpha absolute p99：

1.183e-4。

Therefore：

C1 FAIL was a correct strict stop。

However the Final label：

REAL-ORACLE-GT-INVALID

is too broad。

The current formal state is：

GT-OPTICAL-REFERENCE-MISMATCH。

The mismatch may come from：

pixel-ray semantics

pixel-center convention

ray normalization

view-direction sign

triangle/pixel identity matching

old GT source vs saved-artifact mismatch

or pre-save numerical semantics。

============================================================
最高优先级
============================================================

DO NOT relax C1 thresholds。

DO NOT run full288 jobs。

DO NOT interpret MAT0/MAT1/MAT2 patterns。

DO NOT return to KIOT。

First close the GT optical-reference semantics。

If GT closure passes：

resume the REAL R2A smoke experiment
from C2 through C6。

============================================================
本阶段名称
============================================================

Stage 4.0-R2A-GT1:

GT Optical Semantics
and Pixel-Ray Provenance Closure

中文：

GT 光学语义与像素射线来源闭环

============================================================
唯一科学目标
============================================================

Answer exactly：

1. Which code generated the saved Stage4 GT arrays？

2. What exact pixel coordinates
were used to construct camera rays？

3. Were rays normalized？

4. What exact world ray direction
entered cos_theta？

5. Which triangle normal entered tau？

6. Were normal / Js / h
triangle-constant or interpolated？

7. What dtype was used
during optical computation？

8. What dtype was saved？

9. Does an exact source replay
reproduce the saved GT arrays？

10. Can a new independent per-pixel
reference reproduce the GT
under the exact declared semantics？

Only after this is closed
may C2-C6 execute。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stage4_0_R2A_GT1_gt_optical_semantics_closure/

New source：

/data/wyh/DeformTransGS/attribute_study/
real_oracle/gt_closure/

Create：

gt_closure/
    trace_gt_source.py
    inspect_gt_storage.py
    replay_old_gt.py
    independent_pixel_reference.py
    gt_error_fingerprint.py
    clean_gt_renderer.py
    validate_clean_gt.py
    run_gt1.py

Do not overwrite：

Stage4.0
Stage4.0-R1
Stage4.0-R2A。

============================================================
一、Lock Current Failure
============================================================

Lock：

Stage4.0 protocol

Stage4 GT generation source

Stage4 benchmark source

saved GT arrays

camera lock

deformation matrices

surface definitions

material definitions

Stage4.0-R2A gt_audit.py

R2A report and summary。

Record：

path
size
mtime
SHA256。

Output：

gt1_protocol_lock.json。

Gate D0。

============================================================
二、Trace Exact Old GT Generation Source
============================================================

Find the ACTUAL function that wrote：

*_rgb.npy

*_alpha.npy

*_tau_rgb.npy

*_triangle_id.npy。

Do not infer from README。

Trace executable call chain：

entry point
→ benchmark creation
→ camera ray generation
→ triangle rasterization
→ optical computation
→ np.save。

Output：

old_gt_generation_trace.md。

For every stage record：

source file

function

line range。

Explicitly report：

A. pixel sample coordinate：

u,v

u+0.5,v+0.5

or other。

B. projection convention。

C. ray origin。

D. ray vector before normalization。

E. ray normalization yes/no。

F. ray world direction semantic：

camera_to_surface

surface_to_camera。

G. visible triangle choice。

H. depth quantity used for z-buffer：

camera z

ray t

Euclidean distance

or other。

I. triangle normal source：

canonical face normal

deformed face normal

interpolated vertex normal

other。

J. normal normalization point。

K. Js source。

L. h source。

M. sigma_rgb constants。

N. exact cos_theta expression。

O. exact clamp order。

P. exact tau expression。

Q. exact RGB expression。

R. exact alpha expression。

S. tensor/array dtype
during each optical step。

T. dtype immediately before np.save。

U. saved array dtype。

============================================================
三、Search GT Source for Synthetic Logic
============================================================

Search ONLY GT / benchmark generation code for：

synthetic_release_error

synthetic

surrogate

fake

mock

preset

expected

hardcoded_tau

hardcoded_rgb

hardcoded_alpha。

Material branches defining：

sigma_rgb

h0

mass-conserving vs fixed thickness

are allowed。

Branches directly returning
expected metric or expected release error
are forbidden。

Output：

gt_source_isolation_search.txt。

Gate D1a。

============================================================
四、Saved GT Storage Audit
============================================================

For every GT array type：

rgb

alpha

tau_rgb

triangle_id

report：

dtype

shape

min

max

NaN count

Inf count。

Sample all:

2 surfaces
x3 materials
x7 deformations

and deterministic8 cameras per combination。

Report whether values appear quantized。

For floating arrays compute：

number of unique values

minimum nonzero adjacent unique-value difference

fraction exactly representable
after float16 roundtrip

fraction exactly representable
after float32 roundtrip。

Output：

saved_gt_storage_audit.csv。

============================================================
五、GT Error Fingerprint
============================================================

Use the ORIGINAL R2A
independent audit output。

Do not recompute yet。

For matched samples compute：

tau_saved / tau_independent

tau_saved - tau_independent。

Report by：

surface

material

deformation

camera

RGB channel。

Statistics：

median
p01
p10
p90
p99
std。

Also regress diagnostically：

tau_saved approximately a*tau_ref+b。

Report：

a
b
R2。

Output：

gt_tau_error_fingerprint.csv。

Classification diagnostic：

GLOBAL_MULTIPLICATIVE

CHANNEL_MULTIPLICATIVE

VIEW_DEPENDENT

DEFORMATION_DEPENDENT

PIXEL_DEPENDENT

MIXED。

No threshold changes。

============================================================
六、Exact Old-Source Replay
============================================================

Select deterministic replay set：

surfaces：

S0_PLANAR_SHEET
S1_WAVY_MEMBRANE

materials：

MAT0
MAT1
MAT2

deformations：

D0
D2
D3
D5

cameras：

0
5
12
19。

Total：

2 x3 x4 x4 =
96 GT views。

Create：

replay_old_gt.py。

Reimplement or invoke the ACTUAL
old GT generation function
with the exact old inputs。

Important：

this is a SOURCE REPLAY，
not an independent reference。

Write all replay arrays
to the GT1 output directory。

Do not overwrite old GT。

Compare replay against saved old arrays：

triangle_id exact equality

tau_rgb absolute / relative error

RGB absolute error

alpha absolute error。

Output：

old_gt_source_replay_comparison.csv。

Define:

OLD_SOURCE_REPLAY_MATCH iff：

triangle_id exact equality =100%

tau relative p99 <=1e-7

RGB abs p99 <=1e-7

alpha abs p99 <=1e-7。

Gate D1b。

If source replay does NOT match：

old saved GT artifact provenance is invalid。

Classification：

OLD-GT-ARTIFACT-SOURCE-MISMATCH。

Proceed to clean GT regeneration。

Do NOT try to modify old arrays。

============================================================
七、Independent Exact Per-Pixel Reference
============================================================

Create a NEW implementation：

independent_pixel_reference.py。

It must NOT import：

old GT renderer

Stage4 metrics

Stage4 oracle

synthetic code。

It may load only：

surface mesh definition

F matrices

camera intrinsics/extrinsics

material constants

saved triangle_id。

Critical correction：

DO NOT project arbitrary
material samples back to image。

DO NOT use nearest pixel matching。

DO NOT search for a matching barycentric point。

Instead：

select exact valid PIXELS
from saved triangle_id arrays。

For every sampled pixel：

camera ID

integer pixel x/y

saved visible triangle ID。

Use deterministic sampling：

200000 valid pixel rows

seed20260714

stratified across：

surface
material
deformation
camera。

For exact pixel：

construct the camera ray
using the exact source-declared
pixel-center convention。

Independently compute：

camera center

world ray

ray normalization

deformed triangle vertices

triangle normal

Js_formula

Js_area

h

cos_theta

tau_rgb

RGB

alpha。

The reference formula is：

Js_formula =

abs(detF)
*
norm(inv(F).T @ n_can)。

For MAT0：

h =h0。

For MAT1/MAT2：

h =h0/Js。

cos_theta =

max(
abs(dot(n_def,-d_world)),
0.15
)。

tau_rgb =

sigma_rgb*h/cos_theta。

RGB =exp(-tau_rgb)。

alpha =

1-exp(-mean(tau_rgb))。

But camera ray / direction semantics
must follow the traced actual
camera convention exactly。

Save row-level values：

pixel
triangle
n_can
n_def
Js_area
Js_formula
h
ray
cos_theta
saved tau
reference tau
saved RGB
reference RGB
saved alpha
reference alpha。

Output：

independent_exact_pixel_reference.csv
or parquet if too large。

============================================================
八、Pixel-Ray Convention Diagnostic
============================================================

This section is diagnostic only。

For the SAME sampled pixels
compute candidate ray variants：

H0:
pixel (x,y)

H1:
pixel center (x+0.5,y+0.5)

H2:
source ray without normalization

H3:
source ray normalized

H4:
camera-to-surface direction

H5:
surface-to-camera direction。

Do not combine arbitrary hypotheses。

Report tau error
for each isolated semantic variant。

Output：

pixel_ray_semantic_hypothesis.csv。

The FORMAL reference remains
the source-traced convention。

Do NOT choose semantics
only because they minimize error。

This diagnostic identifies
the R2A audit mismatch cause。

============================================================
九、Exact Independent C1 Re-evaluation
============================================================

Using the formal exact per-pixel reference：

report：

Js relative p99/max

tau relative p99/max

RGB absolute p99/max

alpha absolute p99/max。

Use ORIGINAL C1 thresholds unchanged：

Js relative p99 <=1e-8

Js max <=1e-6

tau relative p99 <=1e-6

tau relative max <=1e-4

RGB absolute p99 <=1e-6

alpha absolute p99 <=1e-6。

Output：

exact_pixel_C1_recheck.json。

Define：

D2 PASS iff all original C1
conditions pass。

============================================================
十、Mismatch Root-Cause Classification
============================================================

Use D1b and D2。

------------------------------------------------------------

CASE AUDIT-MATCHING-BUG:

OLD_SOURCE_REPLAY_MATCH

and

exact per-pixel D2 PASS。

Conclusion：

old saved GT is valid。

Original R2A C1 failure was caused by
material-sample/image-pixel matching semantics
in the first independent audit。

Use old GT arrays as VERIFIED_GT_ROOT。

------------------------------------------------------------

CASE OLD-GT-ARTIFACT-SOURCE-MISMATCH:

old source replay fails。

Conclusion：

saved Stage4 GT arrays cannot be linked
to the current claimed generator source。

Retire old GT arrays。

Proceed to clean GT regeneration。

------------------------------------------------------------

CASE OLD-GT-SEMANTIC-MISMATCH:

old source replay matches

but exact per-pixel D2 FAIL。

Conclusion：

the old GT source itself does not match
the frozen analytical benchmark definition。

Retire old GT arrays and source。

Proceed to clean GT regeneration。

============================================================
十一、Clean GT Renderer
============================================================

Execute ONLY if old GT is retired。

Create：

clean_gt_renderer.py。

This implementation must NOT import：

old benchmark GT renderer

old Stage4 metrics

old oracle

synthetic source。

Use float64 for：

surface vertices

camera matrices

ray construction

triangle normals

F

Js

h

cos_theta

tau_rgb

RGB

alpha。

Triangle rasterization may use
the validated local CPU/CUDA implementation，
but visible triangle semantics must be explicit。

Pixel sample coordinate is LOCKED now as：

pixel center：

x+0.5
y+0.5。

Ray must be normalized。

World ray semantic：

camera_to_surface。

For optical cosine use：

-dot convention only semantically,
but because abs is applied：

cos_theta =
max(abs(dot(n_def,-d_world)),0.15)。

Z-buffer uses positive camera-space z。

GT arrays saved as：

RGB float32

alpha float32

tau_rgb float32

triangle_id int32。

Before saving,
retain optional float64 checksum statistics。

============================================================
十二、Regenerate Complete Clean GT Benchmark
============================================================

Generate ALL:

2 surfaces

x3 materials

x7 deformation states

x24 cameras。

Save under：

/data/wyh/DeformTransGS/experiments/
stage4_0_R2A_GT1_gt_optical_semantics_closure/
clean_gt/

Do NOT overwrite old Stage4 GT。

Create：

clean_gt_manifest.csv。

For every array record：

surface
material
deformation
camera
array type
path
dtype
SHA256。

============================================================
十三、Independent Clean-GT Validation
============================================================

Run：

independent_pixel_reference.py

against CLEAN GT。

The independent implementation
must remain distinct from
clean_gt_renderer.py。

Do not import renderer functions。

Sample：

200000 exact valid pixels。

Recompute from：

saved triangle_id
camera
surface geometry
material constants
F。

Apply ORIGINAL C1 thresholds。

Output：

clean_gt_independent_C1.csv
clean_gt_C1_gate.json。

Gate D3。

If D3 FAIL：

Final CASE：

CLEAN-GT-INDEPENDENT-VALIDATION-FAIL。

STOP AttributeDeformGS benchmark。

Do not run oracle。

============================================================
十四、Verified GT Root Lock
============================================================

If:

AUDIT-MATCHING-BUG

then：

VERIFIED_GT_ROOT =
old Stage4 GT root。

If clean GT D3 PASS：

VERIFIED_GT_ROOT =
GT1 clean_gt root。

Create：

verified_gt_root_lock.json。

Record：

root

reason

manifest SHA

generator source SHA

independent validator SHA。

Gate D4。

============================================================
十五、Resume Real R2A C2-C6
============================================================

Execute ONLY if D4 PASS。

Resume the REAL DIFFERENTIABLE
ORACLE smoke experiment。

Do NOT use any synthetic Stage4 metrics。

Use VERIFIED_GT_ROOT exclusively。

------------------------------------------------------------
C2a release semantics
------------------------------------------------------------

Actual trainable tensors：

R0 []

R1 [o_raw]

R2 [sh_coeffs]

R3 [v_raw]

R4 [o_raw,sh_coeffs]

R5 [o_raw,v_raw]

R6 [sh_coeffs,v_raw]

R7 [o_raw,sh_coeffs,v_raw]。

Assert exact names。

------------------------------------------------------------
C2b autograd
------------------------------------------------------------

Representative：

S0 MAT1 D2。

O gradient：

finite fraction1

nonzero fraction >=0.50

L2 >1e-8。

C same。

V uses >=4 distinct TRAIN cameras。

Same Gate。

------------------------------------------------------------
C3 perturbation causality
------------------------------------------------------------

Perturb deterministic IDs mod17==0。

O:
o_raw +=0.01。

C:
DC SH +=0.01。

V:
v_raw[:,0] +=0.01。

Require：

mean image abs diff >1e-9

and after exact restore：

max render diff <=1e-7。

------------------------------------------------------------
C4 real canonical fits
------------------------------------------------------------

Fit exactly：

K0 S0 MAT0 D0

K1 S0 MAT1 D0

K2 S1 MAT2 D0。

4096 Gaussians。

Geometry fixed。

Train O+C+V。

Actual torch.optim.Adam。

16 TRAIN cameras only。

Maximum4000 iterations。

Patience500。

Loss：

RGB_L1
+0.5 TAU_RGB_LOG_L1
+0.2 ALPHA_TAU_LOG_L1
+0.1 DSSIM。

Save per-iteration real loss/gradient histories。

Reload checkpoint fresh。

Render8 TEST cameras。

Compute metrics from saved arrays。

Smoke PASS:

PSNR >=28 dB

median tau Elog <=0.25

median alpha-tau Elog <=0.25。

Require3/3。

------------------------------------------------------------
C5 real24-job oracle
------------------------------------------------------------

Q0:
K0 / S0 MAT0 D2。

Q1:
K1 / S0 MAT1 D2。

Q2:
K2 / S1 MAT2 D3。

Run R0-R7：

24 jobs。

R1-R7 actual Adam。

Maximum2000 iterations。

Patience300。

Each starts from same canonical checkpoint。

Only declared tensors released。

TEST cameras excluded。

Audit：

first optimizer step parameter changes

frozen tensor max change=0

real checkpoint tensor contents

checkpoint reload max error=0。

------------------------------------------------------------
Fresh TEST renders
------------------------------------------------------------

Reload each job。

Render8 TEST cameras。

Save：

RGB
alpha。

Expected arrays：

24*8*2 =
384。

Create manifest with：

render SHA
checkpoint SHA
camera ID。

------------------------------------------------------------
Independent metrics
------------------------------------------------------------

Separate evaluator process。

Evaluator may load only：

VERIFIED_GT_ROOT

saved TEST render arrays

manifest。

It must not import optimizer
or release policy。

Compute：

tau Elog

alpha-tau Elog

PSNR

SSIM

E_OPT。

Exactly24 metric rows。

Reproduce deterministic6 rows
directly from arrays。

Require：

Elog/E_OPT max difference <=1e-12

PSNR <=1e-10。

------------------------------------------------------------
C6 new-source isolation
------------------------------------------------------------

Search new real_oracle and gt_closure source。

Forbidden metric-generation identifiers：

synthetic_release_error

preset_metric

target_metric

expected_metric

hardcoded_psnr

hardcoded_elog。

No material/release branch may
directly assign a metric value。

============================================================
十六、Strict Gates
============================================================

D0:
GT FAILURE LOCK

PASS。

D1:
OLD GT SOURCE PROVENANCE

D1a source isolation
D1b source replay。

D2:
OLD GT EXACT PIXEL C1。

D3:
CLEAN GT INDEPENDENT C1
if clean regeneration executed。

D4:
VERIFIED GT ROOT。

Then resumed：

C2
C3
C4
C5
C6。

============================================================
十七、Final CASE
============================================================

CASE REAL-ATTRIBUTE-ORACLE-PIPELINE-READY:

D0 PASS

D4 PASS

C2 PASS

C3 PASS

C4 PASS

C5 PASS

C6 PASS。

ALLOW：

Stage4.0-R2B full real
attribute-sufficiency experiment。

------------------------------------------------------------

CASE CLEAN-GT-INDEPENDENT-VALIDATION-FAIL:

D3 FAIL。

STOP AttributeDeformGS benchmark。

------------------------------------------------------------

CASE REAL-ORACLE-GRADIENT-BROKEN:

C2/C3 FAIL。

Fix differentiable renderer graph only。

------------------------------------------------------------

CASE REAL-CANONICAL-CARRIER-INSUFFICIENT:

C4 FAIL。

STOP current attribute study carrier。

------------------------------------------------------------

CASE REAL-ORACLE-PROVENANCE-FAIL:

C5/C6 FAIL。

STOP。

============================================================
十八、Required Outputs
============================================================

gt1_protocol_lock.json

old_gt_generation_trace.md
gt_source_isolation_search.txt
saved_gt_storage_audit.csv
gt_tau_error_fingerprint.csv

old_gt_source_replay_comparison.csv

independent_exact_pixel_reference.csv
or parquet

pixel_ray_semantic_hypothesis.csv
exact_pixel_C1_recheck.json

if clean regeneration：

clean_gt_renderer.py
clean_gt_manifest.csv
clean_gt_independent_C1.csv
clean_gt_C1_gate.json

verified_gt_root_lock.json

release_parameter_semantics_test.csv
single_attribute_gradient_test.csv
attribute_perturbation_causality.csv

canonical_real_fit_history/*.csv
canonical_real_models/*.pt
canonical_real_fit_metrics.csv

real_oracle_fit_history/**/*.csv
optimizer_parameter_change_audit.csv
real_checkpoint_integrity.csv

real_oracle_test_renders/**/*.npy
real_test_render_manifest.csv

r2a_real_release_metrics.csv
r2a_real_primary_error.csv
independent_metric_reproduction.csv

real_oracle_source_isolation.txt

stage4_0_R2A_GT1_report.md
stage4_0_R2A_GT1_summary.md
stage4_0_R2A_GT1_log.txt

============================================================
十九、最终终端只输出
============================================================

A. D0

B. old GT exact generator source/function

C. old GT pixel coordinate convention

D. old GT ray normalized yes/no

E. old GT ray direction semantic

F. old GT optical computation dtype

G. saved RGB/alpha/tau dtype

H. GT synthetic/hardcoded optical array generation yes/no

I. error fingerprint classification

J. source replay triangle-id exact fraction

K. source replay tau/RGB/alpha p99 errors

L. old source replay match yes/no

M. exact pixel independent Js p99/max

N. exact pixel independent tau p99/max

O. exact pixel RGB/alpha p99

P. D2

Q. original C1 mismatch root cause

R. old GT retained or retired

S. clean GT regeneration executed yes/no

T. clean GT total view count

U. clean GT independent Js p99/max

V. clean GT independent tau p99/max

W. clean GT independent RGB/alpha p99

X. D3

Y. VERIFIED_GT_ROOT

Z. D4

AA. R0-R7 declared trainable tensor names

AB. O gradient finite/nonzero/L2

AC. C gradient finite/nonzero/L2

AD. V gradient finite/nonzero/L2

AE. C2

AF. O/C/V perturbation mean image diff

AG. restored render max diff

AH. C3

AI. K0 canonical PSNR/tau/alpha Elog

AJ. K1 canonical PSNR/tau/alpha Elog

AK. K2 canonical PSNR/tau/alpha Elog

AL. C4

AM. real oracle jobs expected/completed

AN. optimizer first-step changed jobs

AO. frozen tensor max change

AP. checkpoint reload max error

AQ. saved TEST array count

AR. independent metric reproduction max error

AS. C5

AT. forbidden synthetic metric source yes/no

AU. hardcoded metric-value branch yes/no

AV. C6

AW. Q0 R0-R7 actual E_OPT

AX. Q1 R0-R7 actual E_OPT

AY. Q2 R0-R7 actual E_OPT

AZ. Final CASE

BA. real attribute oracle pipeline ready yes/no

BB. allow full Stage4.0-R2B real experiment yes/no

BC. AttributeDeformGS hypothesis status

BD. KIOT status

BE. report path

BF. summary path

============================================================
二十、README
============================================================

Update README。

Record：

Stage4.0-R2A stopped correctly at C1。

The first independent GT audit found
exact Js agreement but approximately
4.5e-4 relative tau mismatch，
with corresponding approximately
1e-4 image-domain RGB/alpha differences。

This does not yet prove that
the analytical benchmark model is invalid。

The mismatch may arise from
the old GT pixel-ray semantics，
saved-artifact/source mismatch，
or the first independent audit's
material-sample to pixel matching。

Stage4.0-R2A-GT1 therefore performs
exact source tracing，
source replay，
and exact per-pixel validation using
saved visible triangle IDs。

No C1 threshold is relaxed。

If the old GT cannot be provenance-closed，
it is retired and a clean float64
thin-surface GT renderer is rebuilt
in an isolated module。

Only a GT root that passes
independent exact-pixel C1 validation
may be used by the real differentiable
O/C/V oracle smoke experiment。

============================================================
二十一、实际执行
============================================================

必须实际：

lock failure

trace old GT generator

trace pixel/ray/normal/Js/h semantics

audit saved dtype

fingerprint the tau mismatch

replay96 old GT views

compare replay to saved arrays

build exact per-pixel independent reference

sample200000 exact valid pixels

run pixel-ray semantic diagnostic

rerun original C1 thresholds unchanged

classify root cause

if old GT invalid：
regenerate complete clean GT

independently validate clean GT

lock VERIFIED_GT_ROOT

if D4 PASS：
resume real R2A C2-C6

run real gradients

run perturbation causality

fit3 real canonical models

run24 real oracle jobs

save384 fresh TEST arrays

run independent evaluator

reproduce6 metric rows

audit synthetic isolation

Final CASE

绝对不要：

relax C1

use material-sample nearest pixel matching

generate synthetic release metrics

run288 jobs

claim attribute necessity

return to KIOT。
PROMPT
```

## 我的真实判断

这轮**没有伤到“关键属性探查”这个科学问题**。

它只说明我们现在连实验尺子都必须重新校准。

而且这个误差形态很值得查：

[
4.482\times10^{-4}
\rightarrow4.874\times10^{-4}
]

区间很窄，明显比“GT 完全错误”更像一个固定语义差异。

因此现在别换方向，也别降 Gate。

> **先把同一个 pixel、同一个 triangle、同一根 ray、同一个 normal 的 (\tau) 算到一致。**

R2A-GT1 通过以后，第一次真正有意义的结果将是：

```text
Q0 R0-R7 actual E_OPT
Q1 R0-R7 actual E_OPT
Q2 R0-R7 actual E_OPT
```

**那 24 个真实优化结果，才是 AttributeDeformGS 新主线的第一批科学数据。**
