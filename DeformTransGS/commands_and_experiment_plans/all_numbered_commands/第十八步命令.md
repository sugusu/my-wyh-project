cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前研究状态
==================================================

Stage 3.2.2 已完成：

Fixed-Tau Paradox Audit and Implicit Optical Transport Analysis

当前发现：

1. per-Gaussian tau 固定

2. geometry deformation 后：

   accumulated pixel alpha A 会变化

3. uniaxial stretch：

canonical A≈0.133

stretch_1.10 A≈0.130
stretch_1.25 A≈0.125
stretch_1.50 A≈0.120
stretch_2.00 A≈0.114

4. biaxial_1.50：

A≈0.165

5. twist_60：

A≈0.128

6. position-only ablation 表明：

position spreading
可能是 uniaxial stretch 下
accumulated alpha change 的主要来源。

7. oracle:

tau_oracle = tau_canonical / Js

在当前 carrier 中使 stretch GT error 变差。

==================================================
重要：Stage 3.2.2 存在数学错误
==================================================

Stage 3.2.2 报告中定义：

T_eff = 1 - A

tau_eff = -log(T_eff)

因此正确公式应为：

tau_eff = -log(1 - A)

但是报告数值：

A=0.133
tau_eff=2.019

实际对应：

-log(A)

而不是：

-log(1-A)

因此当前：

effective optical depth
全部需要重新计算。

例如：

A=0.133

正确：

tau_eff =
-log(0.867)
≈0.143

禁止沿用旧 tau_eff 数值。

==================================================
本阶段名称
==================================================

Stage 3.2.3:

Gaussian Surface-Density and Implicit Optical Transport Mechanism Validation

中文：

Gaussian 表面密度与隐式光学传输机制验证

==================================================
一、本阶段唯一目标
==================================================

回答：

在 fixed per-Gaussian tau 条件下，

surface deformation 是否通过：

Gaussian material-point density change

以及：

projected Gaussian footprint change

隐式改变 accumulated alpha / effective optical depth？

重点验证：

对于 area stretch ratio Js，

Gaussian surface number density：

rho_G

是否满足：

rho_G' / rho_G ≈ 1 / Js

以及：

renderer effective optical depth ratio：

tau_eff' / tau_eff

是否接近：

1 / Js。

当前：

不要训练新模型。

不要 E2 refit。

不要设计 tau update method。

不要更换 benchmark。

==================================================
二、项目边界
==================================================

所有新增代码和输出：

/data/wyh/DeformTransGS

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

创建：

/data/wyh/DeformTransGS/experiments/
stage3_2_3_implicit_transport_mechanism/

==================================================
三、输出
==================================================

至少：

effective_tau_formula_correction.md

metric_region_consistency.md
geometry_variant_integrity.csv

surface_density_metrics.csv
projected_density_metrics.csv

alpha_tau_eff_metrics.csv
density_optical_relation.csv

footprint_metrics.csv
position_covariance_balance.csv

sampling_resolution_ablation.csv

implicit_transport_mechanism_report.md
stage3_2_3_summary.md
stage3_2_3_log.txt

以及正式图表。

==================================================
四、修正 Effective Optical Depth
==================================================

正式定义：

A(p) =
accumulated alpha from validated white pass

T_eff(p) =
clamp(1 - A(p), 1e-6, 1.0)

tau_eff(p) =
-log(T_eff(p))

即：

tau_eff =
-log(1-A)

禁止：

-log(A)

建立单元测试：

A=0
tau_eff=0

A=0.1
tau_eff≈0.1053605

A=0.5
tau_eff≈0.693147

A=0.9
tau_eff≈2.302585

要求误差 <1e-6。

输出：

effective_tau_formula_correction.md

明确指出 Stage 3.2.2
旧 tau_eff 数值无效。

==================================================
五、统一 Region / Sampling Protocol
==================================================

当前 Stage 3.2.2 存在：

stretch_2.00 full transport A=0.114

与：

geometry ablation G3 A=0.084

不一致。

首先禁止比较不同统计区域。

建立唯一正式 region：

MATERIAL_INTERIOR

方法：

1. 使用当前 state 的 film optical effect / projected sheet mask

2. binary erosion

优先：

5x5 erosion

3. 排除 image border

4. 排除 projected sheet edge 5 pixels

5. 所有 variant：

G0
G1
G2
G3

在同一个 state-camera 下
必须使用同一个 region mask。

对于 geometry component comparison：

优先使用：

G3 FULL geometry projected interior mask

并固定该 mask
比较 G0/G1/G2/G3。

记录：

pixel count。

重新计算旧：

A=0.114 vs 0.084

差异来源。

输出：

metric_region_consistency.md

==================================================
六、严格 Geometry Variant Integrity
==================================================

对每 state 和：

G0 CANONICAL

G1 POSITION_ONLY

G2 COVARIANCE_ONLY

G3 FULL_TRANSPORT

保存真实 render input checksum：

xyz
scale
rotation
tau
color

字段：

state
variant

xyz_sha256
scale_sha256
rotation_sha256
tau_sha256
color_sha256

以及：

xyz max difference vs canonical
scale max difference vs canonical
rotation max difference vs canonical

严格要求：

G0:
all canonical

G1:
xyz changed
scale exactly canonical
rotation exactly canonical

G2:
xyz exactly canonical
scale and/or rotation changed

G3:
xyz changed
scale and/or rotation changed

tau:
all variants identical canonical

color:
all variants identical canonical

输出：

geometry_variant_integrity.csv

如果 G2 scale/rotation 与 canonical 完全相同：

立即停止后续机制结论。

说明 covariance transport
没有真实进入 renderer。

==================================================
七、Projected Radii / Footprint Validation
==================================================

对于每：

state
variant
camera

从 official renderer 返回：

radii

记录：

visible count
radii mean
median
p10
p90
p95
max

另外对相同 Gaussian identity：

比较：

radii_variant /
(radii_canonical + eps)

统计。

重点：

stretch_2.00

G1 position only

G2 covariance only

G3 full

如果：

G2 covariance 已改变
但 radii 分布完全等于 G0：

必须检查 adapter：

pc.get_scaling
pc.get_rotation

是否读取最新 deformed tensor。

禁止继续宣称 covariance negligible。

输出：

footprint_metrics.csv

==================================================
八、直接验证 Covariance Render Effect
==================================================

选择：

stretch_2.00

固定：

xyz = canonical

tau = canonical

color = white

只比较：

G0 canonical covariance

G2 deformed covariance

white-pass A map。

计算：

A map MAE

A map max difference

tau_eff map MAE

radii difference。

要求根据真实结果回答：

covariance transport
是否进入 rasterizer 并改变 projected footprint。

如果：

radii change > 0

但 A map difference approximately 0：

这是有效现象。

如果：

radii difference = 0：

是 implementation / adapter bug。

==================================================
九、Gaussian Surface Number Density
==================================================

当前 Gaussian identity：

41×41 material grid。

对 uniform sheet deformation：

理论 material-point surface density：

rho_G' / rho_G =
1 / Js

建立两个数值 density estimates。

--------------------------------------------------
D1: Mesh / Material Area Density
--------------------------------------------------

Gaussian count N fixed。

使用 Gaussian material grid triangle connectivity。

canonical total surface area：

A0

deformed total area：

As

定义：

rho_surface =
N / A

ratio：

rho_surface_s /
rho_surface_0

对于 uniform deformation，
理论应接近：

1 / Js。

--------------------------------------------------
D2: Local kNN Surface Density
--------------------------------------------------

对每 Gaussian：

使用 material-grid neighbour topology

优先 4-neighbour
或 8-neighbour。

根据 local tangent neighbourhood
估计 local surface area per Gaussian。

定义 local density：

rho_i =
1 / local_area_i

计算：

rho_i' / rho_i

与：

1 / Js_i

比较。

对于 uniform stretch：

mean absolute error <1e-4
为优先目标。

输出：

surface_density_metrics.csv

==================================================
十、Projected Screen-Space Gaussian Density
==================================================

因为 renderer 在 image space alpha accumulate，

还必须分析 projected density。

对于每：

state
camera

投影 Gaussian centers。

在 MATERIAL_INTERIOR region：

使用至少两种估计：

P1:
projected Gaussian center count
/
projected sheet pixel area

P2:
2D kNN center spacing
或 KDE-free local density estimate

不要使用 Gaussian alpha
定义 density。

输出：

projected_density_metrics.csv

记录：

state
camera
surface_density_ratio
projected_density_ratio
mean_Js

注意：

projected density
还受到 camera foreshortening。

因此主物理分析使用 surface density。

projected density 只解释 renderer。

==================================================
十一、重新计算 Accumulated Alpha 和 Tau_eff
==================================================

统一 MATERIAL_INTERIOR。

对：

canonical

stretch_1.10
stretch_1.25
stretch_1.50
stretch_2.00

biaxial_1.10
biaxial_1.25
biaxial_1.50

twist_30
twist_60

Fresh fixed-tau G3。

计算：

A mean
A median
A p10
A p90

T_eff mean
T_eff median

tau_eff mean
tau_eff median
tau_eff p10
tau_eff p90

注意：

mean tau_eff

和：

-log(1-mean A)

不是同一个量。

必须分别报告。

正式 ratio 优先：

median tau_eff_state
/
median tau_eff_canonical

输出：

alpha_tau_eff_metrics.csv

==================================================
十二、Density vs Optical Relation
==================================================

对每 state：

定义：

R_density =
median local surface density ratio

R_h =
median(1/Js)

R_tau_eff =
median tau_eff state
/
median tau_eff canonical

比较：

R_tau_eff vs R_density

R_tau_eff vs R_h

计算：

absolute error

以及跨 states：

Pearson
Spearman

分别对：

uniaxial

biaxial

all area-changing states

统计。

输出：

density_optical_relation.csv

核心判断：

是否存在：

R_tau_eff ≈ R_density ≈ 1/Js

不要预设成立。

==================================================
十三、Position vs Covariance Balance
==================================================

统一 region。

重新运行：

G0
G1
G2
G3

states：

stretch_1.25
stretch_1.50
stretch_2.00

biaxial_1.50

twist_60

统计：

R_tau_eff

定义：

position_effect =
R_tau_eff_G1 - 1

covariance_effect =
R_tau_eff_G2 - 1

full_effect =
R_tau_eff_G3 - 1

interaction_residual =
full_effect
-
(position_effect + covariance_effect)

注意：

这里只是 effect decomposition diagnostic，
不假设线性因果。

输出：

position_covariance_balance.csv

回答：

1. Position spreading effect 的方向。

2. Covariance footprint effect 的方向。

3. 是否相互抵消。

4. 为什么 uniaxial 与 biaxial
表现不同。

==================================================
十四、Sampling Resolution Ablation
==================================================

这是非常重要的泛化检查。

当前：

41×41
1681 Gaussians

创建相同 canonical sheet
不同固定 Gaussian grid：

21×21
441 Gaussians

41×41
1681 Gaussians

81×81
6561 Gaussians

禁止训练新 appearance model。

为了隔离机制：

使用 uniform canonical tau。

选择 tau 值：

使用 41×41 canonical trained tau median

或：

通过一个 global scalar tau
使 canonical mean accumulated alpha
匹配 41×41 baseline。

必须明确选择。

优先：

对于每 resolution，
仅优化一个 global tau scalar，
使 canonical MATERIAL_INTERIOR
median tau_eff
匹配同一个 target。

禁止 per-Gaussian training。

color：

constant white。

geometry：

规则 grid。

调整 tangent Gaussian scale
保持相同 relative grid coverage policy。

使用 Stage 3.2
coverage multiplier。

对：

canonical

stretch_1.25
stretch_1.50
stretch_2.00

biaxial_1.50

twist_60

使用 fixed global tau。

计算：

R_tau_eff vs 1/Js。

输出：

sampling_resolution_ablation.csv

核心回答：

implicit optical transport
是否依赖 Gaussian sampling resolution。

==================================================
十五、Optional Scale Policy Diagnostic
==================================================

只在主分析完成后执行。

当前 G3：

Sigma'=F Sigma F^T

增加两个 diagnostic。

S0:
FIXED_COVARIANCE

仅 position transport。

S1:
FULL_F_COVARIANCE

Sigma'=F Sigma F^T

S2:
AREA_NORMALIZED_FOOTPRINT

仅作为 diagnostic。

目标：

人为调整 tangent covariance
使 projected / surface footprint
近似保持 canonical material coverage policy。

禁止把 S2 称 proposed method。

具体定义必须基于 local tangent frame
和 Js。

不要随便乘 scalar。

如果无法给出明确 geometry meaning：

跳过 S2。

比较：

R_tau_eff。

目的：

判断 covariance policy
如何控制 implicit optical transport。

==================================================
十六、机制 Gate
==================================================

定义：

IMPLICIT TRANSPORT SUPPORTED

满足：

A.

Surface Gaussian density ratio
数值满足：

R_density ≈ 1/Js

uniform states mean abs error < 1e-3。

B.

Fresh fixed-tau G3：

tau_eff 确实随 area-changing deformation变化。

C.

Uniaxial states：

R_tau_eff 与 R_density：

Spearman >= 0.9

D.

Twist：

R_tau_eff 接近 1。

定义：

abs(R_tau_eff - 1) <= 0.10

E.

Position-only ablation
解释主要 density effect。

如果 A/B 成立，
但 C/D 不成立：

IMPLICIT TRANSPORT PARTIAL。

如果：

tau_eff 不随 density 有系统关系：

NOT SUPPORTED。

另外单独判断：

PHYSICAL CONSISTENCY:

SUPPORTED:
R_tau_eff 与 1/Js
在 area-changing states
mean absolute ratio error <=0.10

PARTIAL:
0.10 < error <=0.25

NOT SUPPORTED:
error >0.25

禁止修改 threshold。

==================================================
十七、研究决策
==================================================

根据结果选择：

CASE A:

Implicit Transport SUPPORTED
+
Physical Consistency SUPPORTED

结论：

For the controlled thin-film Gaussian grid,
fixed-identity geometry transport
implicitly induces optical-depth evolution
consistent with material area dilution.

下一步：

研究这一隐式一致性
在 non-uniform deformation /
sampling adaptation /
densification 下何时失效。

不要设计 tau update。

CASE B:

Implicit Transport SUPPORTED
+
Physical Consistency PARTIAL / NOT SUPPORTED

结论：

Geometry transport induces optical change,
but it is not quantitatively physical。

下一步：

研究 geometry-optics coupling correction。

CASE C:

Implicit Transport PARTIAL

结论：

存在 position-density effect，
但 covariance / projection strongly interfere。

下一步：

建立 deformation-aware optical consistency criterion。

CASE D:

NOT SUPPORTED

停止该机制主线。

==================================================
十八、报告
==================================================

implicit_transport_mechanism_report.md
必须中文回答：

A. Stage 3.2.2 tau_eff 是否计算错误。

B. 正确公式。

C. 修正后的 canonical tau_eff。

D. stretch_2.00 tau_eff。

E. G3 A=0.114 vs 0.084 矛盾原因。

F. G2 covariance 是否真正进入 renderer。

G. G2 radii 是否变化。

H. covariance-only alpha 是否变化。

I. Surface density ratio 是否等于 1/Js。

J. Projected density 如何变化。

K. Fixed tau 下 tau_eff 如何变化。

L. R_tau_eff vs R_density 相关性。

M. R_tau_eff vs 1/Js 误差。

N. position-only effect。

O. covariance-only effect。

P. full effect。

Q. uniaxial / biaxial 差异原因。

R. 21/41/81 grid 是否一致。

S. Implicit Transport Gate。

T. Physical Consistency。

U. Final CASE。

V. 当前新的科学问题。

==================================================
十九、Stage Summary
==================================================

stage3_2_3_summary.md：

# 1. Effective Optical Depth Correction

# 2. Region Consistency

# 3. Geometry Variant Integrity

# 4. Gaussian Surface Density

# 5. Projected Density

# 6. Accumulated Alpha Evolution

# 7. Effective Optical Depth Evolution

# 8. Position / Covariance Balance

# 9. Sampling Resolution Robustness

# 10. Implicit Transport Gate

# 11. Physical Consistency

# 12. Final Research Decision

禁止提出正式方法。

==================================================
二十、README
==================================================

更新 README。

明确修正：

Stage 3.2.2 used an incorrect diagnostic
effective optical depth calculation.

Correct:

tau_eff = -log(1 - A)

not:

-log(A)

The observed accumulated-alpha change remains real,
but its quantitative physical interpretation
must be re-evaluated。

==================================================
二十一、实际执行
==================================================

必须实际：

修正 tau_eff

unit test

统一 region

解释 0.114 / 0.084

geometry input checksum

G2 covariance render-path validation

radii analysis

surface density calculation

projected density calculation

fixed-tau alpha analysis

correct tau_eff analysis

density optical relation

position covariance balance

21/41/81 sampling resolution ablation

机制 Gate

physical consistency

最终 CASE

不要训练新模型。

不要 E2。

不要设计 tau update。

不要改 benchmark。

不要改 TSGS。

==================================================
二十二、最终终端总结
==================================================

最后简洁输出：

1. Old tau_eff formula wrong yes/no
2. Correct formula
3. Canonical corrected tau_eff
4. stretch_2.00 corrected tau_eff
5. A=0.114 vs 0.084 discrepancy root cause
6. G2 covariance input changed yes/no
7. G2 radii changed yes/no
8. G2 alpha map changed yes/no
9. stretch_2.00 surface density ratio
10. expected 1/Js
11. stretch_2.00 R_tau_eff
12. uniaxial R_tau_eff vs density Spearman
13. area-changing physical ratio mean error
14. twist_60 R_tau_eff
15. position-only effect direction
16. covariance-only effect direction
17. full effect direction
18. 21/41/81 resolution consistency
19. Implicit Transport Gate
20. Physical Consistency
21. Final CASE
22. Current scientific question
23. implicit_transport_mechanism_report.md path
24. stage3_2_3_summary.md path

请直接实际运行、检查和验证。

不要换服务器。
不要研究 RT-Splatting runtime。
不要设计 proposed method。
不要修改 TSGS。
PROMPT