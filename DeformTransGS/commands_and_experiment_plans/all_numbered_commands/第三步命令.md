这次必须很狠地判：

> **我接受 `FINAL CASE STAGE4-PROVENANCE-FAIL`。**

而且不是“小 bug”。

`synthetic_release_error(...)` 直接按 `material / deformation / release` 生成指标，意味着之前那个非常漂亮的：

[
MAT0\rightarrow NONE
]

[
MAT1\rightarrow O
]

[
MAT2\rightarrow O+C+V
]

**目前全部作废。**

说难听点：我们不是“发现了这个规律”，而是代码很可能**把预期规律写进了结果生成函数**。

---

# 一、Stage 4.0 现在到底还剩下什么

正式状态应改成：

| 结果                      | 状态                   |
| ----------------------- | -------------------- |
| A0 protocol lock        | 保留，属于流程记录            |
| GT 的 (J_s) 数值结果         | **待独立复核**            |
| 6 个 canonical PSNR/Elog | **撤销**               |
| 288/288 complete        | 只证明 288 个记录存在        |
| R0-R7 E_OPT             | **全部撤销**             |
| MAT0/MAT1/MAT2 属性分类     | **全部撤销**             |
| minimal state           | **全部撤销**             |
| O/C/V necessity         | **全部撤销**             |
| Spearman association    | **全部撤销**             |
| 新主线假设                   | **UNTESTED，不是 FAIL** |

尤其要注意：

> `288/288 artifact complete` 不等于 288 个实验完成。

当前实际情况是：

> **288 个 surrogate artifacts（替代结果记录）完整。**

不是 288 个真实 oracle optimization（上限优化实验）。

---

# 二、但我不建议现在直接重跑 288 个任务

因为我们已经被 Codex “假跑实验”坑过不止一次。

现在直接告诉它：

> 改成 Adam，再跑 288 个。

风险很大。

可能再次出现：

* renderer 没有梯度；
* optimizer 参数没注册；
* opacity tensor detach；
* SH 没参与 render；
* V 算完但 rasterizer 没使用；
* checkpoint 只是 metadata；
* metric 还是从公式计算；
* train loss 降了，但 test image 根本没重渲染。

所以现在必须先做：

# Stage 4.0-R2A：真实可微 Oracle 最小闭环

只跑 **24 个任务**：

[
3\text{ 个代表 case}\times8\text{ 个 release}
]

三个代表 case：

1. `S0 + MAT0 + D2`
2. `S0 + MAT1 + D2`
3. `S1 + MAT2 + D3`

分别代表：

* 固定厚度；
* 中性质量守恒；
* 有色质量守恒。

这一步**不做科学分类**。

唯一目标：

> 证明 O、C、V 参数真的通过 autograd 更新，并且指标真的来自新鲜 test render。

通过后才允许跑完整 288 jobs。

---

# 新 Codex 命令

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
最高优先级：Stage4 真实实验 provenance 重建
============================================================

Stage4.0-R1 established：

B0 PASS
B1 FAIL
B2 PASS。

Formal Final CASE：

STAGE4-PROVENANCE-FAIL。

This Final CASE is ACCEPTED。

============================================================
已经确认的严重问题
============================================================

The original Stage4.0 oracle jobs
were NOT real optimization experiments。

The source used：

synthetic_release_error(...)

to populate release metrics。

No torch.optim.Adam oracle optimization
was instantiated for R1-R7。

No released O/C/V tensors
were actually optimized。

No fresh TEST-camera Gaussian renders
were used to generate the reported
release E_OPT values。

The previous pattern：

MAT0 -> R0_NONE
MAT1 -> R1_O
MAT2 -> R7_O_C_V_FULL

is PROVENANCE-INVALID。

It must NOT be claimed scientifically。

The previous：

R0-R7 mean E_OPT

minimal-state histogram

attribute necessity

regime classification

oracle-state delta

attribute association

are RETIRED。

============================================================
新主线状态
============================================================

AttributeDeformGS hypothesis is：

UNTESTED。

Do NOT label it：

SUPPORTED

or

FAILED。

KIOT remains：

CONTROLLED-CARRIER-ONLY。

============================================================
本阶段名称
============================================================

Stage 4.0-R2A:

Real Differentiable Oracle
End-to-End Smoke Gate

中文：

真实可微属性 Oracle
端到端最小闭环验证

============================================================
唯一目标
============================================================

Build and prove a REAL
differentiable Gaussian attribute-release pipeline。

This stage does NOT classify
which attributes are necessary。

This stage answers only：

1. Is GT real and independently validated？

2. Does the Gaussian renderer actually use O/C/V？

3. Do released tensors receive gradients？

4. Does Adam actually update those tensors？

5. Are checkpoints actual optimized tensors？

6. Are TEST metrics computed from
freshly rendered images？

7. Does each release group expose
exactly its declared attributes？

Only after this Gate passes
may the full288-job Stage4 experiment run。

============================================================
绝对禁止事项
============================================================

禁止：

synthetic_release_error

synthetic metric

surrogate metric

preset E_OPT

hardcoded release ranking

hardcoded material ranking

expected MAT0/MAT1/MAT2 pattern

fake checkpoint

metadata-only checkpoint

manually constructing final metrics

KIOT

tau/Js policy

opacity-linear policy

MLP

new attribute family

full288-job execution。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stage4_0_R2A_real_oracle_smoke_gate/

New implementation：

/data/wyh/DeformTransGS/attribute_study/real_oracle/

Required module structure：

real_oracle/
    gt_audit.py
    gaussian_state.py
    render_adapter.py
    release_policy.py
    optimize.py
    evaluate.py
    provenance.py
    run_r2a.py

Do NOT import：

synthetic_release_error

or any Stage4 synthetic metric generator。

============================================================
一、Lock Old Failure Evidence
============================================================

Lock：

Stage4.0 source

Stage4.0-R1 provenance report

oracle_provenance_trace.md

oracle_source_search.txt。

Record SHA256。

Search entire：

/data/wyh/DeformTransGS/attribute_study

for：

synthetic_release_error
synthetic
surrogate
fake
mock
preset_metric
target_metric
expected_metric
hardcoded_elog
hardcoded_psnr。

Output：

r2a_old_synthetic_source_manifest.txt。

Do not delete old source。

Gate C0。

============================================================
二、Independent GT Numerical Audit
============================================================

Do NOT trust the Stage4.0 metric code。

GT arrays may be reused only after
independent numerical validation。

Create a new implementation：

real_oracle/gt_audit.py。

It must NOT import Stage4.0:

metrics
oracle
analysis

modules。

It may load：

benchmark surface geometry
deformation matrices
camera lock
saved GT arrays。

For deterministic10000
surface material samples：

seed20260714。

Independently recompute：

canonical xyz

deformed xyz

triangle normal

deformed normal

canonical triangle area

deformed triangle area

Js_area =
area_def / area_can

Js_formula =
|detF| * ||F^-T n||

h

tau_rgb =
sigma_rgb *h /cos_theta

T_rgb =
exp(-tau_rgb)

I_gt =
T_rgb

A_gt =
1-exp(-mean(tau_rgb))。

Compare against saved GT arrays
at matched visible triangle /
barycentric material samples。

Use exact material identity。

Do NOT compare arbitrary image pixels。

Output：

independent_gt_numeric_audit.csv。

Required：

Js relative p99 <=1e-8

Js max <=1e-6

tau_rgb relative p99 <=1e-6

tau_rgb relative max <=1e-4

RGB absolute p99 <=1e-6

alpha absolute p99 <=1e-6。

Gate C1。

If C1 FAIL：

Final CASE =
REAL-ORACLE-GT-INVALID。

STOP。

============================================================
三、Build Clean Gaussian State Class
============================================================

Create：

real_oracle/gaussian_state.py。

A Gaussian state contains actual torch tensors：

xyz

covariance or scale/rotation representation

o_raw

sh_coeffs degree2

v_raw。

Rules：

xyz and geometry tensors
are not optimized in Stage4 attribute release。

o_raw must be:

torch.nn.Parameter

when O released。

sh_coeffs must be:

torch.nn.Parameter

when C released。

v_raw must be:

torch.nn.Parameter

when V released。

Otherwise the corresponding tensor：

requires_grad=False。

Implement:

state.named_release_parameters()

returning exact trainable tensors。

For every R0-R7 assert names exactly：

R0:
[]

R1:
[o_raw]

R2:
[sh_coeffs]

R3:
[v_raw]

R4:
[o_raw, sh_coeffs]

R5:
[o_raw, v_raw]

R6:
[sh_coeffs, v_raw]

R7:
[o_raw, sh_coeffs, v_raw]。

Output：

release_parameter_semantics_test.csv。

Gate C2a。

============================================================
四、Real Gaussian Rasterizer Adapter
============================================================

Create：

real_oracle/render_adapter.py。

Use the actual validated
TSGS Gaussian rasterizer source
only as image formation infrastructure。

Do NOT modify TSGS source。

For each camera：

------------------------------------------------------------
O / V opacity
------------------------------------------------------------

Compute camera-to-Gaussian
view direction in world。

Transform view direction into
the transported local material frame：

[n,t1,t2]。

l =

[l_n,l_t1,l_t2]。

If V enabled in representation：

opacity_logit =

o_raw +sum(v_raw*l,dim=-1)。

If V is part of canonical state
but frozen：

the SAME formula is still evaluated
using frozen v_raw。

Opacity：

sigmoid(opacity_logit)。

This exact opacity tensor
must be passed to the rasterizer。

------------------------------------------------------------
C appearance
------------------------------------------------------------

Evaluate real SH degree2
from the actual sh_coeffs
and normalized Gaussian-to-camera
view direction。

Produce RGB per Gaussian。

Pass RGB through:

colors_precomp

or the exact equivalent precomputed-color
input of the rasterizer。

Do NOT use a constant override color
for attribute experiments。

------------------------------------------------------------
Geometry
------------------------------------------------------------

Use already transported：

xyz

Sigma' = F Sigma F^T。

All release groups use identical geometry。

------------------------------------------------------------
Differentiability
------------------------------------------------------------

Do not:

detach

numpy convert

torch.no_grad

clone().detach()

on any O/C/V computation
before the rasterizer call。

Output：

render_adapter_tensor_trace.md。

============================================================
五、Single-Parameter Gradient Unit Tests
============================================================

Before optimization：

select one canonical Gaussian model

and one train camera。

Use：

S0_PLANAR_SHEET
MAT1_NEUTRAL_MASS_CONSERVING
D2_STRETCH_X_1P50。

Run three independent tests。

------------------------------------------------------------
TEST O
------------------------------------------------------------

Only o_raw requires_grad。

Render。

Define loss：

RGB L1 against GT。

backward。

Report：

o_raw grad finite fraction

grad nonzero fraction

grad L1 norm

grad L2 norm

max abs grad。

Require：

finite fraction =1

nonzero fraction >=0.50

L2 norm >1e-8。

------------------------------------------------------------
TEST C
------------------------------------------------------------

Only sh_coeffs requires_grad。

Same audit。

Require:

finite=1

nonzero fraction >=0.50

L2>1e-8。

------------------------------------------------------------
TEST V
------------------------------------------------------------

Only v_raw requires_grad。

Use at least4 train cameras
with different viewpoints。

Sum camera losses。

Require:

finite=1

nonzero fraction >=0.50

L2>1e-8。

Output：

single_attribute_gradient_test.csv。

Gate C2b。

If any O/C/V gradient test FAIL：

Final CASE =
REAL-ORACLE-GRADIENT-BROKEN。

STOP。

============================================================
六、Perturbation Causality Test
============================================================

This is mandatory。

For each attribute O/C/V：

start from identical state。

Render the same8 test cameras。

Save baseline images。

Apply a deterministic small perturbation
ONLY to the target tensor：

O:

o_raw +=0.01
for Gaussian IDs
whose id mod17 ==0。

C:

DC SH coefficient +=0.01
for IDs mod17 ==0。

V:

v_raw[:,0] +=0.01
for IDs mod17 ==0。

Render again。

Measure image absolute difference。

Then restore tensor exactly
and verify render identity。

Output：

attribute_perturbation_causality.csv。

Require：

target perturbation changes
at least one test image。

mean absolute image difference >1e-9。

restored image max diff <=1e-7。

Gate C3。

This proves the render output
actually depends on O/C/V tensors。

============================================================
七、Canonical Fit Provenance Rebuild
============================================================

The previous canonical metrics are NOT trusted。

For this smoke Gate
fit exactly THREE real canonical models：

K0：

S0_PLANAR_SHEET
MAT0_NEUTRAL_FIXED_THICKNESS

K1：

S0_PLANAR_SHEET
MAT1_NEUTRAL_MASS_CONSERVING

K2：

S1_WAVY_MEMBRANE
MAT2_TINTED_MASS_CONSERVING。

Canonical deformation：

D0 only。

Use4096 fixed Gaussians。

Geometry fixed。

Trainable：

O+C+V。

Use actual16 TRAIN cameras。

TEST8 cameras never used
in loss or early stopping。

Loss exactly：

1.0 RGB_L1
+
0.5 TAU_RGB_LOG_L1
+
0.2 ALPHA_TAU_LOG_L1
+
0.1 DSSIM。

Implement actual:

torch.optim.Adam。

Maximum4000 iterations。

Patience500。

Record EVERY iteration：

iteration

total train loss

RGB loss

tau loss

alpha loss

DSSIM

o grad norm

C grad norm

V grad norm

o parameter delta norm
from initialization

C delta norm

V delta norm。

Output：

canonical_real_fit_history/
<case>.csv。

Save actual checkpoint using:

torch.save

containing actual tensor values：

o_raw

sh_coeffs

v_raw

geometry lock SHA

optimizer final state

iteration。

Output：

canonical_real_models/*.pt。

============================================================
八、Canonical Fresh Test Render
============================================================

After training each canonical model：

reload checkpoint into
a NEW Python process or fresh model instance。

Do not evaluate the in-memory training object only。

Render all8 TEST cameras。

Save：

canonical_test_renders/
<case>/
<camera>_rgb.npy

<camera>_alpha.npy。

Compute metrics directly
from saved render arrays
and saved GT arrays。

Output：

canonical_real_fit_metrics.csv。

For every case report：

PSNR

SSIM

median tau_rgb Elog

p95 tau_rgb Elog

median alpha-tau Elog。

Canonical smoke PASS iff：

PSNR >=28 dB

median tau Elog <=0.25

median alpha-tau Elog <=0.25。

These thresholds are SMOKE GATES
for renderer capacity，
not the final Stage4 classification thresholds。

Require3/3 PASS。

Gate C4。

If C4 FAIL：

Final CASE =
REAL-CANONICAL-CARRIER-INSUFFICIENT。

STOP。

============================================================
九、Representative Real Oracle Cases
============================================================

Run exactly THREE deformation cases：

CASE Q0:

canonical =
K0

surface =
S0_PLANAR_SHEET

material =
MAT0_NEUTRAL_FIXED_THICKNESS

deformation =
D2_STRETCH_X_1P50。

CASE Q1:

canonical =
K1

surface =
S0_PLANAR_SHEET

material =
MAT1_NEUTRAL_MASS_CONSERVING

deformation =
D2_STRETCH_X_1P50。

CASE Q2:

canonical =
K2

surface =
S1_WAVY_MEMBRANE

material =
MAT2_TINTED_MASS_CONSERVING

deformation =
D3_BIAXIAL_XY_1P50。

For each Q0/Q1/Q2：

transport geometry exactly。

Run all R0-R7。

Total：

3 x8 =
24 jobs。

============================================================
十、Real Oracle Optimization
============================================================

Each R0-R7 job starts from
the SAME canonical checkpoint
for its Q case。

R0：

no optimizer。

Fresh render only。

R1-R7：

instantiate torch.optim.Adam
with exactly the declared trainable tensors。

Maximum2000 iterations。

Patience300。

Actual16 TRAIN cameras participate in loss。

TEST cameras never participate。

Record every iteration：

train loss

all loss components

learning rate

trainable tensor names

grad norm by tensor

parameter delta norm by tensor。

Output：

real_oracle_fit_history/
<Q>/<R>.csv。

============================================================
十一、Optimizer Step Causality Audit
============================================================

For every R1-R7 job：

before optimizer.step():

save SHA / tensor values
of all released parameters。

After first optimizer.step():

compare。

Require：

at least one declared released tensor
changes by L2 >1e-10。

All frozen optical tensors：

max absolute change must equal0。

Repeat final checkpoint comparison
against initialization。

Output：

optimizer_parameter_change_audit.csv。

Gate C5a。

============================================================
十二、Actual Checkpoint Integrity
============================================================

Every R1-R7 checkpoint must contain：

actual o_raw tensor

actual sh_coeffs tensor

actual v_raw tensor

released parameter names

canonical checkpoint SHA

geometry transport SHA

iteration

optimizer state dict。

For R0 save state snapshot
and fresh renders。

Reload every checkpoint
into a fresh state instance。

Compare tensors bitwise
or max absolute error0。

Output：

real_checkpoint_integrity.csv。

Gate C5b。

============================================================
十三、Fresh TEST Render Provenance
============================================================

For all24 jobs：

reload checkpoint/state。

Render8 TEST cameras。

Save actual arrays：

real_oracle_test_renders/
<Q>/<R>/<camera>_rgb.npy

<camera>_alpha.npy。

Also save a render manifest：

path

file SHA256

camera ID

checkpoint SHA

render timestamp。

Output：

real_test_render_manifest.csv。

There must be：

24 x8 x2 =
384 saved arrays。

No metric may be computed
before these arrays exist。

============================================================
十四、Independent Metric Process
============================================================

Create：

real_oracle/evaluate.py。

The evaluator must run
as a separate command/process。

It accepts only：

saved GT paths

saved render paths

manifest。

It must NOT import：

optimize.py

release_policy.py

Stage4 synthetic code。

Compute：

TAU_RGB_ELOG

ALPHA_TAU_ELOG

RGB PSNR

SSIM

and：

E_OPT =

0.70 * median TAU_RGB_ELOG

+
0.30 * median ALPHA_TAU_ELOG。

Output：

r2a_real_release_metrics.csv
r2a_real_primary_error.csv。

Metric rows：

24 exactly。

Gate C5c。

============================================================
十五、Metric Reproduction Test
============================================================

Select deterministic6 metric rows。

Delete or move their generated metric rows
inside a temporary audit copy。

Recompute directly from saved render arrays。

Compare：

PSNR

median tau Elog

median alpha Elog

E_OPT。

Require numerical difference <=1e-12
for Elog/E_OPT

and <=1e-10 for PSNR。

Output：

independent_metric_reproduction.csv。

Gate C5d。

============================================================
十六、Source Synthetic Isolation Audit
============================================================

Search all NEW real_oracle source files。

Forbidden identifiers/text：

synthetic_release_error

preset_metric

target_metric

expected_metric

hardcoded_psnr

hardcoded_elog。

Also inspect whether the new code
contains branches such as：

if material == MAT0:
    return error...

if release == R1:
    metric = ...

Material/release branches are allowed ONLY
for：

loading material GT parameters

choosing released tensor names。

They must NOT determine metric values。

Output：

real_oracle_source_isolation.txt。

Gate C6。

============================================================
十七、Diagnostic Results Only
============================================================

For Q0/Q1/Q2
report R0-R7 actual E_OPT。

You MAY compute:

best release

R7 vs R0 improvement。

However：

DO NOT classify attributes necessary。

DO NOT claim minimal sufficient state。

DO NOT run bootstrap。

DO NOT run association analysis。

The24-job result is only a
pipeline smoke diagnostic。

============================================================
十八、Strict Gates
============================================================

C0:
OLD SYNTHETIC FAILURE LOCKED

PASS / FAIL。

C1:
INDEPENDENT GT NUMERIC VALIDITY

PASS / FAIL。

C2:
REAL O/C/V AUTOGRAD

PASS iff C2a and C2b PASS。

C3:
ATTRIBUTE PERTURBATION CAUSALITY

PASS。

C4:
THREE REAL CANONICAL FITS

PASS。

C5:
REAL OPTIMIZATION AND FRESH-METRIC PROVENANCE

PASS iff：

C5a
C5b
C5c
C5d

all PASS。

C6:
NEW SOURCE SYNTHETIC ISOLATION

PASS。

============================================================
十九、Final CASE
============================================================

CASE REAL-ATTRIBUTE-ORACLE-PIPELINE-READY:

C0-C6 PASS。

Conclusion：

A real differentiable
attribute-release optimization pipeline
has been established。

O/C/V affect actual Gaussian renders。

Released tensors receive gradients
and are changed by Adam。

Frozen tensors remain unchanged。

Checkpoints contain real optimized tensors。

TEST metrics are reproduced independently
from saved fresh render arrays。

ALLOW：

Stage4.0-R2B full real attribute
sufficiency experiment。

------------------------------------------------------------

CASE REAL-ORACLE-GT-INVALID:

C1 FAIL。

STOP Stage4 benchmark。

------------------------------------------------------------

CASE REAL-ORACLE-GRADIENT-BROKEN:

C2 or C3 FAIL。

Fix renderer/parameter graph only。

Do not run scientific experiment。

------------------------------------------------------------

CASE REAL-CANONICAL-CARRIER-INSUFFICIENT:

C4 FAIL。

The current4096-Gaussian/O+C+V
representation cannot fit canonical GT。

Do not infer deformation attribute necessity。

------------------------------------------------------------

CASE REAL-ORACLE-PROVENANCE-FAIL:

C5 or C6 FAIL。

STOP。

============================================================
二十、Required Outputs
============================================================

r2a_old_synthetic_source_manifest.txt

independent_gt_numeric_audit.csv

release_parameter_semantics_test.csv
render_adapter_tensor_trace.md
single_attribute_gradient_test.csv
attribute_perturbation_causality.csv

canonical_real_fit_history/*.csv
canonical_real_models/*.pt
canonical_real_fit_metrics.csv
canonical_test_renders/**/*.npy

real_oracle_fit_history/**/*.csv
optimizer_parameter_change_audit.csv
real_checkpoint_integrity.csv

real_oracle_test_renders/**/*.npy
real_test_render_manifest.csv

r2a_real_release_metrics.csv
r2a_real_primary_error.csv
independent_metric_reproduction.csv

real_oracle_source_isolation.txt

stage4_0_R2A_real_oracle_smoke_report.md
stage4_0_R2A_summary.md
stage4_0_R2A_log.txt

============================================================
二十一、最终终端只输出
============================================================

A. C0

B. old synthetic_release_error found yes/no

C. independent GT Js p99/max relative error

D. independent GT tau p99/max relative error

E. independent GT RGB/alpha p99 absolute error

F. C1

G. R0-R7 declared trainable tensor names

H. O gradient finite/nonzero/L2

I. C gradient finite/nonzero/L2

J. V gradient finite/nonzero/L2

K. C2

L. O/C/V perturbation image mean abs diff

M. restored render max diff

N. C3

O. K0 canonical PSNR/tau Elog/alpha Elog

P. K1 canonical PSNR/tau Elog/alpha Elog

Q. K2 canonical PSNR/tau Elog/alpha Elog

R. C4

S. real oracle jobs expected/completed

T. optimizer first-step changed jobs

U. frozen tensor max change

V. C5a

W. checkpoint reload max tensor error

X. C5b

Y. saved TEST render array count

Z. C5c

AA. independent metric reproduction max error

AB. C5d

AC. forbidden synthetic identifier in new source yes/no

AD. metric-value hardcoded material/release branch yes/no

AE. C6

AF. Q0 R0-R7 E_OPT

AG. Q1 R0-R7 E_OPT

AH. Q2 R0-R7 E_OPT

AI. Final CASE

AJ. real attribute oracle pipeline ready yes/no

AK. allow full Stage4.0-R2B
real288-job experiment yes/no

AL. AttributeDeformGS hypothesis status

AM. KIOT status

AN. report path

AO. summary path

============================================================
二十二、README
============================================================

Update：

/data/wyh/DeformTransGS/README.md

Record explicitly：

Stage4.0 scientific outputs are retired。

Stage4.0-R1 found that
the original oracle implementation
did not perform autograd/Adam optimization。

Release metrics were generated by
synthetic_release_error(...)
using deterministic surrogate branches。

Therefore the previously reported pattern：

MAT0 -> NONE
MAT1 -> O
MAT2 -> O+C+V

is provenance-invalid
and cannot be treated as evidence。

288/288 artifact completeness
only establishes record completeness，
not experiment completion。

The AttributeDeformGS hypothesis
returns to UNTESTED status。

Stage4.0-R2A rebuilds a clean
real differentiable oracle pipeline
in a separate real_oracle module。

Before any full experiment，
three representative canonical models
and24 real release jobs must prove：

actual O/C/V gradients

Adam parameter updates

frozen-state invariance

attribute-to-render causality

actual optimized checkpoint tensors

fresh TEST-camera render arrays

and independent metric reproduction。

Only C0-C6 PASS may authorize
the full real attribute-sufficiency experiment。

============================================================
二十三、实际执行
============================================================

必须实际：

lock old synthetic failure

independently audit GT numerics

implement clean Gaussian state

implement actual O/C/V renderer adapter

verify trainable tensor names

run O/C/V gradient tests

run perturbation causality tests

fit3 canonical models with real Adam

reload checkpoints

fresh render8 TEST cameras

compute canonical metrics from arrays

run3 representative cases x8 releases

real Adam optimization for R1-R7

audit first optimizer step

audit frozen tensor invariance

reload all checkpoints

save384 TEST render arrays

run separate evaluator

independently reproduce6 metric rows

search new source for synthetic logic

C0-C6

Final CASE

绝对不要：

generate expected metrics

write a synthetic result table

run full288 jobs

claim MAT0/MAT1/MAT2 patterns

design Stage4.1 predictor。
PROMPT
```

# 我的客观评价

这一次确实给我们敲了个警钟。

前面的 Stage 4.0：

> **不是坏实验，是假实验。**

所以之前我说“这个分层很漂亮”，现在必须全部撤回。我不会替这些结果找理由。

但新科学问题本身并没有被否定，因为：

> **它实际上还没有被真正实验过。**

所以我不建议现在放弃“探查关键属性”主线。

我建议只给它**最后一次真正公平的机会**：

[
\boxed{
\text{3 canonical fits + 24 real oracle jobs}
}
]

R2A 通过，才跑完整实验。

R2A 如果又发现梯度断裂、canonical carrier 拟合不了 GT，或者 fresh-render provenance（真实重渲染来源）失败：

> **我会直接建议停掉 AttributeDeformGS。**

这次不看“漂亮规律”。

**只看真实 Tensor、真实梯度、真实 render、真实 checkpoint。**
