这次 N1 的结果是一个**有效正结果**：

```text
CASE OPTICAL-MECHANISM-BOUNDARY-READY
```

它证明：

1. 纯双折射不能直接塞进普通 RGB 衰减模型，除非加入偏振器件；
2. 材料轴相关的各向异性消光可以通过普通 RGB 透射强度观察；
3. Q1 面积标量不能区分 PAIR-A；
4. Q2 无序主拉伸谱不能区分 PAIR-C；
5. Q3 材料坐标系中的完整 (C_t) 能区分 PAIR-C；
6. 局部观察方向混杂可以被控制。

所以现在真正值得继续验证的问题是：

> **对于具有定向吸收微结构的半透明材料，形变后的透射响应是否必须保留材料坐标系中的完整面内应变方向，而不仅是面积变化或主拉伸大小？**

但下一步不能直接写一个：

[
\tau=f(C_{11},C_{12},C_{22})
]

然后证明 Q3 最好。那仍然是循环论证。

正确做法是从**更底层的定向吸收微结构**生成光学响应：

[
\text{canonical absorbing fibers}
\rightarrow
F\text{ 运输微结构方向}
\rightarrow
\text{形成消光张量}
\rightarrow
\text{计算普通 RGB 透射}
]

整个 GT 生成器不允许读取 (J_s,\lambda_1,\lambda_2,C_t) 描述量文件。然后再用独立程序判断 Q1/Q2/Q3 哪一级能够区分 oracle（光学真值）。

---

# 下一条 Codex 指令

阶段名称：

> **Stage D1-N2：Microstructure-Derived Anisotropic Extinction Oracle and Non-Circularity Closure**

中文：

> **微结构驱动各向异性消光真值与非循环性闭环**

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
Stage D1-N2
Microstructure-Derived Anisotropic Extinction Oracle
and Non-Circularity Closure
============================================================

中文：

微结构驱动各向异性消光真值
与非循环性闭环

============================================================
当前正式状态
============================================================

Stage D0 Final CASE：

DEFORMABLE-OPTICAL-TRANSPORT
-FEASIBILITY-PASS。

Stage D1-N0 Final CASE：

DEFORMATION-STATE-HIERARCHY
-IDENTIFIABLE。

Stage D1-N1 Final CASE：

OPTICAL-MECHANISM-BOUNDARY-READY。

Candidate deformation-state hierarchy：

Q1：

Js。

Q2：

unordered principal-stretch spectrum：

log(lambda1)
log(lambda2)。

Q3：

full tangent right Cauchy-Green tensor
in the canonical material basis：

Ct =
[
C11 C12
C12 C22
]。

N0 proved：

there are same-Q1 / different-Q2 pairs；

there are same-Q2 / different-Q3 pairs；

rigid controls preserve material strain state。

N1 selected the primary mechanism：

M3_MATERIAL_AXIS_ANISOTROPIC_EXTINCTION。

Primary observable：

ordinary RGB transmitted intensity
under controlled illumination。

Polarization hardware：

NOT REQUIRED
for this primary mechanism。

============================================================
本阶段唯一科学目标
============================================================

Construct an optical oracle
from a lower-level material microstructure，
not directly from Q1/Q2/Q3 descriptors。

The oracle must begin from：

canonical oriented absorbing elements

exact deformation gradient F

transported microstructure directions

material absorption parameters

controlled viewing direction

ordinary unpolarized RGB illumination。

Then determine whether matched deformation pairs
produce falsifiable optical responses：

1. isotropic material must not create
false Q3 orientation dependence；

2. material-axis anisotropic extinction
must distinguish at least one
same-Q2 / different-Q3 PAIR-C；

3. rigid transformations must preserve
intrinsic material response
under controlled identical local view；

4. the generator must not read Ct，
lambda1/lambda2，
or descriptor-table fields
to construct optical response。

Do NOT train any prediction model。

Do NOT yet claim Q3 is necessary
for real materials。

============================================================
核心非循环性原则
============================================================

The optical oracle generator is forbidden
from using these candidate descriptors
as input：

Js from D0/N0 descriptor tables

lambda1

lambda2

C11

C12

C22

gamma

descriptor labels Q1/Q2/Q3。

The generator may use only：

canonical material sample identity

canonical local frame t1/t2/n

exact deformation matrix F

transported vectors F*a

camera or controlled local ray direction

fixed material microstructure parameters。

If area change is needed for geometric
thickness transport，
compute it independently as：

J_area =
||
cross(F*t1,F*t2)
||

Do not load Js from the descriptor table。

A separate audit program may later compute
Q1/Q2/Q3 for analysis。

============================================================
重要科学范围
============================================================

This stage does NOT prove：

real transparent materials universally require Q3；

all semi-transparent objects have
anisotropic extinction；

RT-Splatting requires new tensor attributes；

a Gaussian model can reconstruct this oracle。

This stage only establishes：

a physically motivated，
ordinary-RGB-observable，
microstructure-derived controlled mechanism

for which Q1/Q2/Q3 matched-pair hypotheses
can be falsified without directly encoding
the candidate descriptor as the answer。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stageD1_N2_microstructure_anisotropic_extinction/

Create source：

/data/wyh/DeformTransGS/
deformable_optical_transport/
stageD1_N2/

Create：

stageD1_N2/
    protocol/
    provenance/
    deformation/
    microstructure/
    material/
    controlled_views/
    oracle/
    independent_replay/
    counterfactual/
    noncircularity/
    audit/
    run_N2.py

Do not overwrite D0/N0/N1 outputs。

============================================================
一、N2 Protocol Lock
============================================================

Lock SHA256：

D0 protocol lock

D0 material samples S0/S1

D0 deformation replay

D0 local-frame table

D1-N0 protocol lock

D1-N0 candidate deformation bank

D1-N0 candidate descriptor table

D1-N0 matched invariant validation

D1-N0 state collision matrix

D1-N1 mechanism boundary report

D1-N1 mechanism recommendation

exact surface definitions

exact deformation implementation。

Output：

D1N2_protocol_lock.json。

Gate O0。

============================================================
二、Deduplicated Deformation Lock
============================================================

Use the11 unique deformation matrices
established by N1。

Retain protocol aliases only as
reproducibility labels。

Create one authoritative matrix key
per unique matrix。

For every alias group record：

canonical matrix key

alias labels

matrix SHA256。

Output：

D1N2_unique_deformation_lock.csv。

Require：

unique matrix count =
11。

Gate O1。

============================================================
三、Controlled Local View Directions
============================================================

This stage is pointwise，
not image-rasterized。

Define controlled directions
in the DEFORMED local frame：

V0_NORMAL：

normalize([0,0,1])。

V1_T1_OBLIQUE：

normalize([0.6,0,0.8])。

V2_T2_OBLIQUE：

normalize([0,0.6,0.8])。

V3_DIAGONAL_OBLIQUE：

normalize([0.4242640687,
           0.4242640687,
           0.8])。

V4_NEG_T1_OBLIQUE：

normalize([-0.6,0,0.8])。

V5_NEG_T2_OBLIQUE：

normalize([0,-0.6,0.8])。

All directions point toward the camera side。

For each material sample/deformation，
convert each local direction into world space
using the exact deformed local frame。

Then independently convert it back
to local coordinates。

Require roundtrip max error <=1e-12。

Output：

D1N2_controlled_view_lock.csv
D1N2_view_roundtrip_audit.csv。

Gate O2。

============================================================
四、Canonical Microstructure Families
============================================================

Create exactly three controlled
canonical microstructure families。

These are material models，
not Q1/Q2/Q3 labels。

------------------------------------------------------------

U0_ISOTROPIC_CONTROL：

Purpose：

rotationally symmetric null control。

Use deterministic256 in-plane orientation samples：

theta_j =
2*pi*j/256

j=0..255。

Canonical tangent direction：

a_j =
cos(theta_j)*t1
+
sin(theta_j)*t2。

Weights：

w_j =
1/256。

All orientations use identical
RGB absorption parameters。

This material must not exhibit
material-axis orientation dependence。

------------------------------------------------------------

U1_T1_ALIGNED_DICHROIC：

Purpose：

primary material-axis anisotropic mechanism。

Use deterministic256 orientation samples
from a symmetric circular distribution
concentrated around +t1 and -t1。

Use an exact deterministic
von-Mises-like discrete weight：

raw_w_j =
exp(
kappa*cos(2*theta_j)
)

with：

kappa =
6.0。

Normalize weights to sum1。

The use of cos(2 theta)
makes the material axis unoriented：

+t1 and -t1 are equivalent。

------------------------------------------------------------

U2_CROSS_BIAXIAL_CONTROL：

Purpose：

secondary material-axis control。

Use a mixture of：

70% t1-aligned distribution

30% t2-aligned distribution。

Use the same concentration：

kappa =
6.0。

Normalize weights。

This is not a required primary mechanism，
but provides a second anisotropic
microstructure topology。

------------------------------------------------------------

For all families save：

orientation index

theta

canonical2D direction

weight。

Output：

D1N2_microstructure/
U0.csv
U1.csv
U2.csv。

Validate：

weight sum error <=1e-15。

For U0：

second orientation moment
must be isotropic within1e-12。

Gate O3。

============================================================
五、Material Absorption Parameter Lock
============================================================

Use fixed pre-metric RGB parameters。

Do not search or tune them
against matched-pair response。

For every microscopic absorber define
a uniaxial absorption contribution：

K_j,c =
k_perp,c * I
+
(
k_parallel,c-k_perp,c
)
*
a'_j a'_j^T。

Use：

k_perp_rgb =
[0.35,0.55,0.80]。

k_parallel_rgb =
[1.10,0.75,0.45]。

Normal-direction base absorption：

k_normal_rgb =
[0.45,0.60,0.75]。

Canonical thickness：

h0 =
0.08。

White unpolarized incident illumination：

I_in =
[1,1,1]。

These parameters are locked
before counterfactual evaluation。

Output：

D1N2_material_parameter_lock.json。

No parameter sweep。

No detectability tuning。

============================================================
六、Microstructure Transport Through F
============================================================

For every：

surface sample

unique deformation

microstructure family

transport every canonical absorber direction。

Canonical world direction：

a_j =
m1_j*t1
+
m2_j*t2。

Transported vector：

b_j =
F*a_j。

Require：

||b_j|| >1e-12。

Transported unit direction：

a'_j =
b_j / ||b_j||。

Compute independently transported normal：

n' =
normalize(
cross(F*t1,F*t2)
)。

Resolve normal sign
against D0 transported frame。

Geometric area ratio：

J_area =
||
cross(F*t1,F*t2)
||。

Geometric thickness：

h_geo =
h0 / J_area。

This is the known mass-conserving
geometry/path baseline。

It must be separated from
the material-axis anisotropic contribution。

Output sampled audit table：

D1N2_microstructure_transport_audit.csv。

============================================================
七、Microstructure-Derived Extinction Tensor
============================================================

For each RGB channel construct
the world-space extinction tensor：

K_micro,c =
sum_j
w_j
[
k_perp,c*I
+
(
k_parallel,c-k_perp,c
)
a'_j a'_j^T
]。

Add the normal-direction component
without double counting：

K_c =
P_tangent * K_micro,c * P_tangent
+
k_normal,c * n'n'^T。

Where：

P_tangent =
I - n'n'^T。

Symmetrize numerically：

K_c =
0.5*(K_c+K_c^T)。

Require each K_c
positive semidefinite
within numerical tolerance。

Report minimum eigenvalue。

Important：

Do not construct K_c from：

Ct

lambda1/lambda2

Js descriptor table。

Output：

D1N2_extinction_tensor_audit.csv。

Gate O4。

============================================================
八、Ordinary Unpolarized RGB Transmission
============================================================

For each controlled world-view direction d：

construct a deterministic
orthonormal polarization-plane basis：

e1,e2

such that：

e1 dot d =0

e2 dot d =0

e1 dot e2 =0。

For every RGB channel：

form projected2x2 absorption matrix：

H_c =
[
e1^T K_c e1    e1^T K_c e2
e2^T K_c e1    e2^T K_c e2
]。

Compute eigenvalues：

mu1_c
mu2_c。

Path denominator：

cos_theta =
max(
abs(dot(n',d)),
0.15
)。

Mode optical depths：

tau1_c =
h_geo*mu1_c/cos_theta

tau2_c =
h_geo*mu2_c/cos_theta。

Ordinary unpolarized transmitted intensity：

T_c =
0.5*
[
exp(-tau1_c)
+
exp(-tau2_c)
]。

Define effective optical depth：

tau_eff_c =
-log(clamp(T_c,1e-12,1))。

This is ordinary intensity
without an analyzer。

It is not a pure birefringence model。

Output full oracle：

D1N2_microstructure_optical_oracle.csv。

Required fields：

sample ID

surface

deformation matrix key

microstructure family

view key

RGB channel

J_area

h_geo

world d

local d

K tensor components

mu1

mu2

tau1

tau2

T

tau_eff。

============================================================
九、Independent Oracle Replay
============================================================

Create separate script：

stageD1_N2/
independent_replay/
replay_microstructure_oracle.py。

It must NOT import
the primary oracle implementation。

The replay may load：

canonical microstructure CSV

material parameter lock

surface sample/local-frame data

deformation matrices

controlled view lock。

It independently recomputes：

transported directions

normal

J_area

h_geo

K tensor

projected eigenmodes

tau1/tau2

T

tau_eff。

Compare deterministic100000 rows。

Require：

K component p99 absolute error <=1e-10

K max absolute error <=1e-8

mu p99 relative error <=1e-9

mu max relative error <=1e-7

T p99 absolute error <=1e-10

T max absolute error <=1e-8

tau_eff p99 absolute error <=1e-10

tau_eff max absolute error <=1e-8。

Output：

D1N2_oracle_replay.csv。

Gate O5。

============================================================
十、Non-Circularity Source Audit
============================================================

Statically and dynamically audit
the primary oracle generator。

Prohibit imports/loads of：

D0 deformation descriptor table

D1-N0 candidate descriptor table

state collision matrix

Ct fields

lambda fields

Js descriptor fields。

Search source for tokens：

C11
C12
C22
lambda1
lambda2
gamma
right_cauchy
descriptor_table。

Any occurrence must be explained
as comment/output-only or fail。

Record all actual runtime input files。

Output：

D1N2_noncircularity_audit.md
D1N2_runtime_input_manifest.csv。

Gate O6 PASS iff：

optical response is generated only from
F，
local frames，
microstructure，
material parameters，
and viewing direction。

============================================================
十一、Rigid Objectivity Counterfactual
============================================================

Use PAIR-D rigid controls：

identity

Rz30

Rx25。

For each material family and
each controlled LOCAL view direction：

compare intrinsic optical response
under identical local view semantics。

Because the material and local view
are transported together，
rigid rotations must not change：

K in the deformed local frame

mu1/mu2

T

tau_eff。

Require：

tau_eff p99 absolute difference <=1e-10

max <=1e-8。

Output：

D1N2_rigid_objectivity.csv。

Gate O7。

============================================================
十二、Isotropic Null-Control Tests
============================================================

Use U0_ISOTROPIC_CONTROL。

For matched deformation pairs：

PAIR-C same spectrum
different material-basis orientation。

Under identical controlled local view：

the isotropic material response
must not depend on canonical material-axis
orientation。

Require：

PAIR-C tau_eff p99 absolute difference <=1e-8

max <=1e-6。

Also verify all in-plane rotations
of the canonical U0 microstructure
produce the same response。

Output：

D1N2_isotropic_null_control.csv。

Gate O8。

============================================================
十三、PAIR-A Q1 Collision Test
============================================================

PAIR-A：

identity

vs

diag(2.0,0.5,1.0)。

For S0：

J_area is matched at1。

Q1 therefore collides。

Evaluate U1/U2 at all
controlled oblique views。

Determine whether microstructure transport
produces a nonzero anisotropic response。

Report：

tau_eff absolute difference

relative difference

RGB T absolute difference

by view/channel/material family。

A detectable PAIR-A response requires：

at least one U1 or U2
view/channel combination with：

tau_eff relative difference >=0.02

and

RGB transmitted-intensity
absolute difference >=1e-3。

Thresholds are frozen before execution。

Output：

D1N2_PAIR_A_counterfactual.csv。

Gate O9a。

This test proves only that
a Q1-only state cannot distinguish
this controlled mechanism。

============================================================
十四、PAIR-B Shear Counterfactual
============================================================

PAIR-B：

identity

vs

[[1,0.5,0],
 [0,1,0],
 [0,0,1]]。

For planar S0：

J_area is matched at1。

Evaluate U0/U1/U2。

Require：

U0 isotropic response remains within
the expected rotationally symmetric control
subject to path/view geometry。

For U1/U2：

report whether shear-induced orientation
transport produces detectable response。

Output：

D1N2_PAIR_B_counterfactual.csv。

Gate O9b is diagnostic，
not required for final PASS。

============================================================
十五、PAIR-C Q2 Collision / Q3 Distinction Test
============================================================

PAIR-C：

C1 =
diag(1.6,0.8,1.0)。

C2 =
Rz45
*
diag(1.6,0.8,1.0)
*
Rz(-45)。

The unordered principal-stretch spectrum
is matched。

Q2 therefore collides。

Ct in canonical material basis differs。

Use identical controlled local view direction
for both pair members。

Evaluate：

U0 isotropic control

U1 t1-aligned anisotropic material

U2 cross-biaxial material。

Required results：

1.

U0 must remain a null control：

tau_eff p99 absolute pair difference <=1e-8

or exact documented geometric reason
if a chosen view contract is not truly matched。

2.

At least one of U1/U2
must produce a detectable pair difference：

tau_eff relative difference >=0.02

and

RGB transmitted-intensity
absolute difference >=1e-3

for at least one
controlled view/channel。

Output：

D1N2_PAIR_C_counterfactual.csv。

Gate O9c。

This is the primary falsifiable test。

============================================================
十六、Same-Local-View Confound Audit
============================================================

For every PAIR-A/B/C comparison verify：

local view vectors are numerically identical
between pair members。

Report max local-view difference。

Require：

max <=1e-12。

Also separate：

geometric thickness/path contribution

from

microstructure anisotropy contribution。

For every comparison compute two oracles：

GEO_ONLY：

replace K_c with its rotationally isotropic
trace-matched tensor。

FULL_MICROSTRUCTURE：

use the real K_c。

Define：

delta_tau_material =
tau_eff_FULL
-
tau_eff_GEO_ONLY。

PAIR-C distinction must remain detectable
in delta_tau_material，
not only in path length。

Output：

D1N2_confound_separation.csv。

Gate O10。

============================================================
十七、Independent Descriptor Analysis
============================================================

Only after the oracle has been generated，
load the N0 candidate descriptor table
in a separate analysis script。

For each matched pair report whether：

Q1 collides

Q2 collides

Q3 differs。

Do not train a model。

Do not calculate feature importance。

Do not perform regression。

Build only a logical counterfactual table：

descriptor state collision

vs

oracle response collision/distinction。

Output：

D1N2_descriptor_oracle_counterfactual_matrix.csv。

============================================================
十八、Scientific Interpretation Rules
============================================================

Allowed conclusion if all Gates pass：

For the controlled microstructure-derived
anisotropic extinction mechanism，
surface-area scalar Q1 is insufficient
for PAIR-A/B controls，
and unordered stretch spectrum Q2 is
insufficient for the material-axis-sensitive
PAIR-C control。

The full material-basis tangent strain state Q3
contains the information required
to distinguish these matched deformations。

Forbidden conclusion：

all real transparent materials require Q3。

Forbidden conclusion：

Q3 is universally sufficient。

Forbidden conclusion：

the mechanism has already been validated
on real captured objects。

Formal scientific role：

MICROSTRUCTURE-DERIVED
-CONTROLLED-MECHANISM-BENCHMARK。

============================================================
十九、Final Gate
============================================================

Define：

MICROSTRUCTURE-ANISOTROPIC-EXTINCTION
-ORACLE-READY

iff：

O0 PASS

O1 PASS

O2 PASS

O3 PASS

O4 PASS

O5 PASS

O6 PASS

O7 PASS

O8 PASS

O9a PASS

O9c PASS

O10 PASS。

============================================================
二十、Final CASE
============================================================

CASE MICROSTRUCTURE-ANISOTROPIC
-EXTINCTION-ORACLE-READY：

the optical oracle is independently replayable；

the generator is non-circular；

rigid objectivity passes；

isotropic null controls pass；

PAIR-A proves a Q1 collision
with different oracle response；

PAIR-C proves a Q2 collision
with different anisotropic oracle response；

same-local-view and path confounds are controlled。

Scientific status：

CONTROLLED-Q3-INFORMATION-NECESSITY
-ESTABLISHED-FOR-THIS-MECHANISM。

Next action：

design D1-N3 representation sufficiency experiment
using held-out deformation matrices，
held-out microstructure parameters，
and separate Q1/Q2/Q3 predictors。

Do NOT yet integrate Gaussian rendering。

------------------------------------------------------------

CASE MECHANISM-NONCIRCULARITY-FAIL：

the optical generator directly consumes
candidate descriptors
or cannot be independently replayed。

STOP。

------------------------------------------------------------

CASE ORDINARY-RGB-ANISOTROPIC-RESPONSE
-NOT-DETECTABLE：

the locked primary mechanism does not
produce a measurable ordinary RGB distinction
on PAIR-C。

STOP or reconsider polarization-aware sensing。

------------------------------------------------------------

CASE OBJECTIVITY-OR-NULL-CONTROL-FAIL：

rigid controls or isotropic controls
produce invalid orientation dependence。

STOP and repair the physics implementation。

============================================================
二十一、Required Outputs
============================================================

D1N2_protocol_lock.json
D1N2_unique_deformation_lock.csv

D1N2_controlled_view_lock.csv
D1N2_view_roundtrip_audit.csv

D1N2_microstructure/U0.csv
D1N2_microstructure/U1.csv
D1N2_microstructure/U2.csv

D1N2_material_parameter_lock.json
D1N2_microstructure_transport_audit.csv
D1N2_extinction_tensor_audit.csv

D1N2_microstructure_optical_oracle.csv
D1N2_oracle_replay.csv

D1N2_noncircularity_audit.md
D1N2_runtime_input_manifest.csv

D1N2_rigid_objectivity.csv
D1N2_isotropic_null_control.csv

D1N2_PAIR_A_counterfactual.csv
D1N2_PAIR_B_counterfactual.csv
D1N2_PAIR_C_counterfactual.csv

D1N2_confound_separation.csv
D1N2_descriptor_oracle_counterfactual_matrix.csv

stageD1N2_oracle_report.md
stageD1N2_oracle_summary.md
stageD1N2_oracle_log.txt

============================================================
二十二、最终终端只输出
============================================================

A. O0

B. protocol deformation entries

C. unique deformation matrices

D. O1

E. controlled local view count

F. local-view roundtrip max error

G. O2

H. microstructure families

I. U0 orientation second-moment
isotropy max error

J. microstructure weight-sum max error

K. O3

L. extinction tensor minimum eigenvalue

M. O4

N. oracle row count

O. oracle replay K p99/max error

P. oracle replay T p99/max error

Q. oracle replay tau_eff p99/max error

R. O5

S. forbidden descriptor runtime input count

T. O6

U. rigid tau_eff p99/max difference

V. O7

W. U0 PAIR-C tau_eff p99/max difference

X. O8

Y. PAIR-A Q1 collision yes/no

Z. PAIR-A maximum tau_eff relative difference
for U1/U2

AA. PAIR-A maximum RGB absolute difference

AB. O9a

AC. PAIR-B maximum anisotropic
tau_eff relative difference

AD. PAIR-C Q2 collision yes/no

AE. PAIR-C U0 null-control
tau_eff p99/max difference

AF. PAIR-C U1/U2 maximum
tau_eff relative difference

AG. PAIR-C U1/U2 maximum
RGB absolute difference

AH. O9c

AI. matched-pair local-view max difference

AJ. PAIR-C material-only
tau_eff relative difference

AK. O10

AL. Q1 collision with oracle distinction
pair count

AM. Q2 collision with oracle distinction
pair count

AN. optical necessity scope

AO. scientific benchmark role

AP. Final Gate

AQ. Final CASE

AR. new primary line STOP/CONTINUE

AS. next exact research action

AT. report path

AU. summary path

============================================================
二十三、README
============================================================

Update：

/data/wyh/DeformTransGS/README.md

Record explicitly：

D1-N1 selected
material-axis anisotropic extinction
as the primary controlled optical mechanism。

The primary observable is ordinary
unpolarized RGB transmitted intensity
under controlled illumination。

Pure birefringence remains outside
the ordinary RGB model
unless a polarizer/analyzer setup is added。

D1-N2 does not define optical response
directly from Js，
principal stretches，
or Ct。

Instead it defines canonical oriented
absorbing microstructures。

The microstructure directions are transported
through the exact deformation gradient F。

A world-space extinction tensor is assembled
from the transported absorber orientations。

Ordinary unpolarized transmission is computed
from the two absorption eigenmodes
in the plane perpendicular to the ray。

Geometric thickness transport is computed
independently from the transported surface area。

The optical oracle generator must not read
candidate descriptor tables。

Matched-invariant counterfactuals are used：

PAIR-A：
same Q1，
different Q2/Q3。

PAIR-C：
same Q2，
different Q3。

An isotropic microstructure is used
as a null control。

Rigid transformations must preserve
intrinsic material response
under identical local-view semantics。

Any conclusion is limited to
the controlled microstructure-derived mechanism。

No claim about universal real-material
necessity is allowed。

============================================================
二十四、实际执行
============================================================

必须实际：

lock D0/N0/N1 evidence

deduplicate deformation matrices

lock controlled local views

create isotropic and anisotropic
microstructure distributions

lock absorption parameters before metrics

transport microstructure directions through F

assemble positive-semidefinite
extinction tensors

compute ordinary unpolarized RGB transmission

independently replay the oracle

prove the generator does not consume
Q1/Q2/Q3 descriptors

validate rigid objectivity

validate isotropic null controls

run PAIR-A counterfactual

run PAIR-C counterfactual

control identical local view

separate geometric path response
from material anisotropy response

build descriptor-collision
versus oracle-response matrix

make the final controlled-mechanism decision

Do NOT train any predictor

Do NOT integrate Gaussian rendering。
PROMPT
```

## 当前真实判断

N1 以后，这条线第一次具备了一个明确、可证伪的核心：

> **同样的面积变化不代表同样的透明响应；同样的主拉伸大小也不一定代表同样的响应，因为材料轴方向可能不同。**

但这个结论必须先由**微结构级独立 oracle**产生，而不是由 (C_t) 公式直接产生。

D1-N2 通过后，才能做下一阶段：

[
Q1\quad vs\quad Q2\quad vs\quad Q3
]

在未见形变和未见微结构参数上的表示充分性实验。

如果 N2 的 PAIR-C 在普通 RGB 下没有可检测差异，这条线就应该停止或转向偏振成像。
