cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前科研主线
==================================================

当前研究问题已经收缩为：

Gaussian geometry deformation transport
会通过：

1. projected Gaussian center distribution

2. individual projected Gaussian footprint

改变 accumulated alpha / transmittance。

需要判断这种：

implicit optical evolution

是否与 thin semi-transparent material
的 area-dilution optical evolution 一致。

当前禁止：

换服务器
RT-Splatting runtime
ASG
新 benchmark
tau update method
MLP

==================================================
已有事实
==================================================

TSGS / 3DGS rasterizer 实际 pixel alpha：

alpha_i(p) =
min(
0.99,
opacity_i * exp(power_i(p))
)

transmittance：

T <- T * (1 - alpha_i)

因此：

T(p) =
product_i (1 - alpha_i(p))

定义：

tau_eff(p) =
-log(T(p))

则：

tau_eff(p) =
sum_i -log(1 - alpha_i(p))

当前 Stage 3.2.3 已确认：

fixed per-Gaussian tau
does not imply fixed accumulated pixel alpha。

但是 Stage 3.2.3 存在三个问题：

1. 使用了重新训练 canonical model：
   loss≈0.045

   而 Stage 3.2 正式 canonical checkpoint：
   NORE≈0
   loss≈1e-6

2. Stage 3.2.2 position-only
   曾表现 alpha decrease

   Stage 3.2.3 却认为
   position effect increases tau_eff

   方向矛盾。

3. “position spreading covers more pixels”
   不是严格机制解释。

Position changes Gaussian center distribution。

Covariance changes individual projected footprint。

必须重新隔离。

==================================================
本阶段名称
==================================================

Stage 3.2.4:

Gaussian Optical-Budget Conservation Audit

中文：

Gaussian 光学贡献预算与形变一致性审计

==================================================
一、唯一目标
==================================================

使用唯一冻结的 Stage 3.2
near-perfect canonical checkpoint，

严格回答：

当 Gaussian geometry 发生 deformation 时，

accumulated effective optical depth 的变化
究竟来自：

A. projected center-density change

B. individual Gaussian footprint change

C. nonlinear alpha accumulation interaction

以及：

这种变化是否稳定依赖：

Gaussian sampling resolution
Gaussian scale policy
canonical opacity/tau level。

当前不要提出 correction method。

==================================================
二、项目边界
==================================================

所有新增代码和结果：

/data/wyh/DeformTransGS

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

创建：

/data/wyh/DeformTransGS/experiments/
stage3_2_4_optical_budget_audit/

==================================================
三、正式 Canonical Checkpoint 唯一化
==================================================

必须定位 Stage 3.2：

5000-iteration canonical fit

mean NORE≈0.0000

对应真实 checkpoint。

禁止重新训练。

记录：

checkpoint path
iteration
training loss
mean NORE

Gaussian count

xyz SHA256
scale SHA256
rotation SHA256
tau SHA256
color SHA256

生成：

canonical_checkpoint_lock.md

后续全部实验：

必须由该 checkpoint fresh load。

如果无法唯一找到该 checkpoint：

停止。

禁止使用 Stage 3.2.3 retrained model。

==================================================
四、Renderer Formula Audit
==================================================

只读检查当前实际使用的：

TSGS FirstSurface rasterizer CUDA forward。

记录真实：

power

alpha

alpha threshold

alpha clamp

T early termination threshold

pixel color accumulation

公式。

必须明确：

alpha_i =
min(0.99, opacity_i * exp(power_i))

alpha < 1/255 是否 skip

T 是否在 <0.0001 时终止。

因此理论：

tau_eff =
sum -log(1-alpha_i)

只对：

实际未被 skip
且实际参与 accumulation 的 contributors

严格成立。

输出：

renderer_optical_budget_formula.md

==================================================
五、统一 Material-Point Sampling
==================================================

停止以整块 region mean
作为主要机制指标。

Gaussian identity：

41×41 grid
1681 material points。

对于每：

state
camera

使用 Gaussian material point index。

canonical xyz_i
deformed xyz_i

投影到对应 camera。

只保留：

positive depth

inside image

距离 deformed projected sheet boundary >= 5 pixels

white-pass A 在有效范围

定义统一 valid material point set。

重要：

比较 G0/G1/G2/G3 时：

同一 state-camera
必须使用完全相同 material indices。

通过 bilinear sampling
读取每个 material point 对应：

A(p_i)

T(p_i)=1-A(p_i)

tau_eff(p_i)=-log(T(p_i))

输出：

material_point_set_validation.csv

==================================================
六、Geometry Variants
==================================================

全部 fresh load canonical checkpoint。

对于：

stretch_1.10
stretch_1.25
stretch_1.50
stretch_2.00

biaxial_1.50

twist_60

建立：

G0 CANONICAL

xyz canonical
Sigma canonical

G1 POSITION_ONLY

xyz = phi(xyz)
Sigma canonical

G2 COVARIANCE_ONLY

xyz canonical
Sigma = F Sigma F^T

G3 FULL_TRANSPORT

xyz = phi(xyz)
Sigma = F Sigma F^T

tau：

canonical fixed

color：

canonical fixed

禁止共享 mutable model。

每 variant：

独立 deepcopy
或 fresh checkpoint load。

保存 tensor checksums。

==================================================
七、Projected Center Density
==================================================

对每：

state
camera

投影 G1/G3 Gaussian centers。

只使用正式 valid material point region。

计算至少：

D1:
2D kNN density

对 projected center coordinates
计算 k=4 / k=8 nearest-neighbour distance。

density proxy：

rho_proj =
1 / mean_knn_distance^2

D2:
center count / projected sheet area

projected sheet area
使用 projected canonical grid connectivity
构成 triangles 后计算。

不要使用 alpha 定义 density。

输出：

projected_center_density.csv

字段：

state
camera
variant
projected_area
center_count
knn4_distance
knn8_distance
density_knn4
density_knn8
count_area_density

计算 relative ratio vs canonical。

==================================================
八、Individual Footprint Measurement
==================================================

不能仅使用 rasterizer integer radii。

对于每 Gaussian：

根据当前 camera
和当前 covariance，

获取或复现 renderer preprocessing 中
projected 2D conic / covariance。

优先使用 TSGS rasterizer
已有 preprocessing output。

如果 API 不返回：

只读审计 CUDA preprocess。

在 DeformTransGS 中实现
与官方 projection math 对齐的 diagnostic。

禁止修改 CUDA。

对每 Gaussian 计算：

projected covariance eigenvalues

sigma_major
sigma_minor

footprint_area_proxy =
pi * sigma_major * sigma_minor

或 determinant-based equivalent。

同时记录 rasterizer radii。

输出：

projected_footprint_metrics.csv

计算 state ratio vs canonical。

==================================================
九、Optical Budget Identity
==================================================

当前 white-pass：

A = accumulated alpha

T = 1-A

tau_eff = -log(T)

首先验证 numerical identity。

对于每 material point：

使用 renderer final A
得到：

tau_eff_render

如果能安全获取实际 contributor list /
per contributor alpha：

计算：

tau_eff_sum =
sum_i -log(1-alpha_i)

比较：

tau_eff_render
vs
tau_eff_sum

要求：

MAE <1e-5

如果 rasterizer 不暴露 contributor alpha：

不要修改 CUDA。

建立 PyTorch reference
仅对：

cam_000

随机最多 256 material points

使用真实 projected means / conics / opacity

按照 CUDA：

alpha clamp
1/255 threshold
T threshold

精确模拟。

比较。

输出：

optical_budget_identity.csv

==================================================
十、Contribution Count / Strength Decomposition
==================================================

对 reference material points：

记录：

actual contributor count

non-skipped contributor count

sum alpha_i

mean alpha_i

median alpha_i

max alpha_i

sum -log(1-alpha_i)

定义：

N_effect =
non-skipped contributor count

S_effect =
mean -log(1-alpha_i)

因此：

tau_eff =
N_effect * S_effect

只是代数统计表达：

mean contribution × count。

比较 G0/G1/G2/G3。

输出：

optical_budget_decomposition.csv

核心回答：

POSITION_ONLY 改变：

N_effect？
S_effect？

COVARIANCE_ONLY 改变：

N_effect？
S_effect？

FULL：

两者如何组合？

==================================================
十一、重新解决 Position Direction 矛盾
==================================================

正式只使用 frozen Stage 3.2 checkpoint。

对：

stretch_2.00
cam_000
cam_004
cam_008

报告：

G0 tau_eff median

G1 tau_eff median

G2 tau_eff median

G3 tau_eff median

以及：

projected center density ratio

footprint area ratio

N_effect ratio

S_effect ratio

明确回答：

position-only：

tau_eff increase
或
decrease

不得使用：

“covers more pixels”

作为解释。

必须具体写：

center density ratio changes by X

contributor count changes by X

mean contributor strength changes by X

==================================================
十二、Canonical Tau-Level Ablation
==================================================

为了判断 implicit optical effect
是否依赖 canonical opacity regime，

不训练模型。

使用 frozen canonical geometry / color。

构造 global tau scaling diagnostic：

T0.5:
tau = 0.5 * canonical tau

T1.0:
tau = canonical tau

T2.0:
tau = 2.0 * canonical tau

T4.0:
tau = 4.0 * canonical tau

注意：

只作为 mechanism diagnostic。

对：

canonical
stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.50
twist_60

运行 G3 fixed tau geometry transport。

计算：

R_tau_eff =
median tau_eff_state /
median tau_eff_canonical

比较：

1/Js

输出：

tau_level_ablation.csv

回答：

implicit transport direction /
magnitude
是否依赖 canonical tau level。

==================================================
十三、Gaussian Scale Policy Ablation
==================================================

不训练。

从 frozen checkpoint 复制。

canonical scale diagnostic：

S0.5:
scale = 0.5 * canonical activated scale

S1.0:
canonical

S1.5:
1.5 * canonical scale

S2.0:
2.0 * canonical scale

tau / color fixed。

对于每 scale policy：

先 canonical render。

记录 canonical tau_eff。

然后 G3 geometry transport：

stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.50
twist_60

计算：

R_tau_eff

projected density

footprint ratio

N_effect
S_effect

输出：

scale_policy_ablation.csv

当前不要强制 canonical alpha matching。

因为目标是观察 mechanism dependence。

==================================================
十四、Sampling Resolution Ablation 修正
==================================================

Stage 3.2.3 的 21/41/81 grid 结论：

“resolution 越高 baseline alpha 越小”

非常可疑。

因为 Gaussian density 增加，
baseline alpha 却降低，
说明不同 resolution 的：

scale
tau

可能没有公平校准。

重新设计。

grid：

21×21
41×41
81×81

使用规则 flat sheet。

对每 resolution：

Gaussian scale relative to grid spacing
使用完全相同 multiplier。

然后只优化一个 global scalar tau：

目标：

canonical cam_000 / 004 / 008

MATERIAL_POINT median tau_eff

匹配固定 target：

使用 frozen Stage 3.2
41×41 canonical checkpoint
的 median tau_eff target。

只优化：

1 scalar tau

禁止 per-Gaussian tau。

color：

white constant。

完成 canonical optical-depth matching 后：

冻结 scalar tau。

对：

stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.50
twist_60

执行 G3。

输出：

sampling_resolution_calibrated.csv

比较：

R_tau_eff。

这是 sampling-resolution
正式机制实验。

==================================================
十五、Physical Target Comparison
==================================================

Benchmark physical target：

R_physical =
1 / Js

对每：

tau-level
scale-policy
sampling-resolution

计算：

R_tau_eff

physical ratio error：

E_phys =
abs(R_tau_eff - 1/Js)

分析：

哪些条件：

direction correct
R_tau_eff < 1 for stretch

哪些：

direction wrong
R_tau_eff > 1

哪些：

接近 physical target。

输出：

physical_target_sweep.csv

==================================================
十六、核心机制结论
==================================================

根据实验严格判断。

M1:
POSITION-DENSITY EFFECT

SUPPORTED：

position-only consistently changes
projected center density
and contributor count，
which explains tau_eff change。

M2:
FOOTPRINT EFFECT

SUPPORTED：

covariance-only changes
projected footprint and contributor statistics。

M3:
IMPLICIT OPTICAL TRANSPORT IS REPRESENTATION-DEPENDENT

SUPPORTED：

R_tau_eff direction or magnitude
changes substantially across：

tau level
scale policy
sampling resolution

定义 substantially：

at least one core stretch state
R_tau_eff range across settings >=0.20

or direction crosses 1.0。

M4:
IMPLICIT TRANSPORT IS PHYSICALLY CONSISTENT

SUPPORTED：

across all tested reasonable settings：

area-changing state mean E_phys <=0.10
and direction correct。

PARTIAL：

only subset of settings meets target。

NOT SUPPORTED：

most settings direction wrong
or E_phys >0.25。

==================================================
十七、研究决策
==================================================

CASE A:

M1/M2 supported

M3 supported

M4 partial/not supported

结论：

Gaussian geometry transport induces
representation-dependent optical drift。

Because accumulated transmittance depends on
center density, footprint, and nonlinear alpha accumulation,
the same physical surface deformation can produce
different optical evolution under different
Gaussian discretizations。

下一步：

设计 deformation-aware optical consistency constraint。

不是：

tau = tau/Js

直接公式。

CASE B:

Implicit transport robustly matches 1/Js
across all settings。

结论：

geometry transport already provides
physical optical evolution。

停止 optical correction 主线。

CASE C:

Observed effect disappears
after frozen-checkpoint and calibrated tests。

结论：

previous mechanism result was experimental artifact。

停止主线。

==================================================
十八、报告
==================================================

生成：

optical_budget_mechanism_report.md

必须中文回答：

A. 使用哪个唯一 canonical checkpoint。

B. canonical NORE。

C. Renderer alpha / T 真实公式。

D. tau_eff exact accumulation identity。

E. position-only tau_eff direction。

F. position-only center density ratio。

G. position-only contributor count ratio。

H. position-only mean contributor strength ratio。

I. covariance-only footprint ratio。

J. covariance-only contributor count ratio。

K. covariance-only tau_eff effect。

L. full transport effect。

M. 为什么 Stage 3.2.2 / 3.2.3 position direction 矛盾。

N. tau level 是否改变 implicit transport。

O. scale policy 是否改变 implicit transport。

P. calibrated 21/41/81 grid 是否一致。

Q. M1 decision。

R. M2 decision。

S. M3 decision。

T. M4 decision。

U. Final CASE。

V. 当前论文级研究问题如何表述。

禁止提出正式 method。

==================================================
十九、Stage Summary
==================================================

stage3_2_4_summary.md：

# 1. Frozen Canonical Checkpoint

# 2. Rasterizer Optical Budget

# 3. Material-Point Sampling

# 4. Position-Density Effect

# 5. Covariance Footprint Effect

# 6. Nonlinear Alpha Accumulation

# 7. Tau-Level Dependence

# 8. Scale-Policy Dependence

# 9. Sampling-Resolution Dependence

# 10. Physical Target Comparison

# 11. Mechanism Decisions

# 12. Final Research Decision

==================================================
二十、README
==================================================

更新 README。

记录：

Stage 3.2.3 confirmed that fixed Gaussian opacity
does not imply fixed rendered transmittance。

However,
the direction of the position effect
was inconsistent across previous audits
because different canonical fits and metric protocols
were used。

Stage 3.2.4 locks a single near-perfect canonical checkpoint
and decomposes accumulated optical depth into：

projected center density
individual Gaussian footprint
and nonlinear alpha accumulation。

==================================================
二十一、实际执行
==================================================

必须实际：

lock canonical checkpoint

audit renderer formula

material point sampling

G0/G1/G2/G3 fresh variants

projected center density

projected footprint

optical budget identity

contributor count / strength decomposition

resolve position direction

tau-level ablation

scale-policy ablation

calibrated 21/41/81 resolution ablation

physical target sweep

M1-M4 decisions

final CASE

不要训练新 per-Gaussian model。

sampling resolution 只允许优化 global scalar tau。

不要 E2 refit。

不要实现 correction。

不要修改 benchmark。

不要修改 TSGS。

==================================================
二十二、最终终端总结
==================================================

最后简洁输出：

1. Frozen canonical checkpoint path
2. Canonical NORE
3. Optical budget identity max error
4. stretch_2.00 G1 tau_eff ratio
5. stretch_2.00 G1 center density ratio
6. stretch_2.00 G1 contributor count ratio
7. stretch_2.00 G1 mean strength ratio
8. stretch_2.00 G2 footprint ratio
9. stretch_2.00 G2 contributor count ratio
10. stretch_2.00 G2 tau_eff ratio
11. stretch_2.00 G3 tau_eff ratio
12. Tau-level R_tau_eff range
13. Scale-policy R_tau_eff range
14. 21-grid calibrated R_tau_eff
15. 41-grid calibrated R_tau_eff
16. 81-grid calibrated R_tau_eff
17. Physical target 1/Js
18. M1
19. M2
20. M3
21. M4
22. Final CASE
23. Current paper-level research question
24. optical_budget_mechanism_report.md path
25. stage3_2_4_summary.md path

请直接实际运行、检查和验证。

不要换服务器。
不要使用 RT-Splatting。
不要研究 ASG。
不要实现 proposed correction。
不要修改 TSGS。
PROMPT