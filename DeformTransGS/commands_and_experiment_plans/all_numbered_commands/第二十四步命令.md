cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前研究状态
==================================================

Stage 3.3:

Local Optical Consistency Boundary Test

已有结果：

Gold Protocol reproduction:
PASS

uniform:

stretch_1.25:
R_tau_eff=0.787
target=0.800

stretch_1.50:
R_tau_eff=0.637
target=0.667

stretch_2.00:
R_tau_eff=0.450
target=0.500

biaxial_1.50:
R_tau_eff=0.400
target=0.444

twist_60:
R_tau_eff=0.877
target≈1

non-uniform cubic:

cubic_l010:
global median R=0.962

cubic_l020:
global median R=0.922

cubic_l0333:
global median R=0.873

area-preserving nonlinear shear:

shear_k020:
mean deviation from 1=0.002

shear_k040:
mean deviation from 1=0.007

==================================================
重要：Stage 3.3 当前结论修正
==================================================

Stage 3.3 当前不能标记：

CASE A LOCAL CONSISTENCY SUPPORTED。

原因：

原 CASE A 核心要求：

1. cubic states point-level Spearman >=0.9

2. cubic_l0333 local MAE <=0.10

3. E_nonuniform <= 2 * E_uniform

尚未计算。

同时：

neighbourhood mixing analysis
尚未完成。

因此：

Stage 3.3 当前状态正式修正为：

PROMISING BUT INCOMPLETE

允许保留的结论：

The tested area-preserving nonlinear shear controls
produce negligible optical response。

中文：

当前测试的面积保持非线性剪切控制
几乎不产生光学响应变化。

禁止写：

nonlinear position deformation alone
does not cause optical drift。

禁止写：

local consistency supported。

==================================================
本阶段名称
==================================================

Stage 3.3.1:

Pointwise Local Optical Correspondence and Neighbourhood Mixing Completion

中文：

点级局部光学对应与邻域混合补全实验

==================================================
一、唯一目标
==================================================

不增加任何 deformation。

不训练。

不重新设计 benchmark。

只使用 Stage 3.3 已有：

canonical

uniform stretch

cubic stretch

shear control

render outputs
和 frozen carrier。

正式回答：

Q1.

对于 spatially varying cubic deformation，

material-point optical response：

R_tau_eff_local(i)

是否对应：

1/Js_i？

Q2.

如果不对应 self Js，

是否更接近：

1-ring

或：

2-ring

neighbourhood-averaged area dilution？

Q3.

Stage 3.3 最终属于：

CASE A
CASE B
CASE C
CASE D

哪一个？

==================================================
二、项目边界
==================================================

所有输出：

/data/wyh/DeformTransGS/experiments/
stage3_3_1_local_correspondence_completion/

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

禁止：

训练

tau update

color update

geometry update

新 deformation

新 camera

新 benchmark

==================================================
三、输入完整性审计
==================================================

定位 Stage 3.3：

frozen carrier manifest

canonical alpha maps

canonical tau_eff maps

cubic_l010 render

cubic_l020 render

cubic_l0333 render

shear_k020 render

shear_k040 render

uniform stretch renders

camera definitions

Gaussian canonical xyz

Gaussian deformed xyz

Js per Gaussian

对所有文件：

记录 path

shape

dtype

SHA256

输出：

input_integrity.md

如果已有 Stage 3.3 render 不完整：

只允许使用完全相同 frozen carrier
重新 render 缺失 state。

禁止重新训练。

==================================================
四、唯一 Gold Point Protocol
==================================================

严格复用 Stage 3.2.6A / Stage 3.3 Gold Protocol。

正式 cameras：

cam_000
cam_004
cam_008

对于 material Gaussian identity i：

canonical xyz_i
投影到 canonical camera：

p_can_i,c

deformed xyz_i
投影到 state camera：

p_def_i,c

分别 bilinear sample：

tau_eff_can(p_can_i,c)

tau_eff_def(p_def_i,c)

定义：

r_i,c =

tau_eff_def(i,c)
/
(
tau_eff_can(i,c)+1e-8
)

valid：

positive depth

canonical projection inside image

deformed projection inside image

距离 deformed sheet boundary >= 8 pixels

tau_eff_can >1e-6

finite

对于 material point i：

至少 2 cameras valid

定义：

R_i =
median_c(r_i,c)

禁止：

ratio of global medians

image pooled median

region mean ratio

输出：

gold_point_protocol_validation.md

==================================================
五、Point-Level Dataset
==================================================

对：

stretch_1.25
stretch_1.50
stretch_2.00

cubic_l010
cubic_l020
cubic_l0333

shear_k020
shear_k040

twist_60

生成：

material_point_response.csv

每行：

state

gaussian_index

grid_u_index
grid_v_index

u
v

Js

q_self =
1/Js

valid_camera_count

tau_eff_can_cam000
tau_eff_def_cam000
r_cam000

tau_eff_can_cam004
tau_eff_def_cam004
r_cam004

tau_eff_can_cam008
tau_eff_def_cam008
r_cam008

R_local

abs_error_self =
abs(R_local-q_self)

==================================================
六、Uniform Point-Level Baseline
==================================================

不要使用旧 global metric
计算 E_uniform。

正式使用：

material_point_response.csv

对于：

stretch_1.25
stretch_1.50
stretch_2.00

每 state：

MAE_self =
mean |R_local - 1/Js|

RMSE

median error

p90 error

p95 error

Spearman：

只有 target 有数值变化时才计算。

uniform target constant，
不计算 Spearman。

定义：

E_uniform =

mean(
MAE_self stretch_1.25,
MAE_self stretch_1.50,
MAE_self stretch_2.00
)

输出：

uniform_pointwise_baseline.csv

==================================================
七、Cubic Point-Level Analysis
==================================================

对于：

cubic_l010
cubic_l020
cubic_l0333

正式计算：

MAE_self

RMSE_self

median absolute error

p90
p95

Pearson(
R_local,
q_self
)

Spearman(
R_local,
q_self
)

同时计算：

linear regression diagnostic：

R_local =
a * q_self + b

输出：

slope a

intercept b

R²

只做 diagnostic。

输出：

cubic_pointwise_consistency.csv

这是 Stage 3.3 最核心结果。

==================================================
八、Spatial Bin Analysis
==================================================

严格按：

abs(u)

bins：

[0.0,0.2)
[0.2,0.4)
[0.4,0.6)
[0.6,0.8)
[0.8,1.0+eps]

对于每 cubic state / bin：

point count

mean Js

median Js

median q_self

median R_local

mean R_local

MAE_self

p10 R
p90 R

bias =

median R_local
-
median q_self

输出：

spatial_bin_consistency.csv

重点报告：

cubic_l0333：

center bin

edge bin。

==================================================
九、建立 Material Grid Neighbourhood
==================================================

Gaussian grid：

41x41

必须根据真实 Gaussian identity / grid index
建立邻域。

禁止使用 3D Euclidean kNN
代替 material topology。

定义：

ring1：

Chebyshev grid distance <=1

排除 self 或包含 self
必须分别报告。

正式主要版本：

包含 self 的 3x3 neighbourhood。

ring2：

Chebyshev grid distance <=2

正式：

包含 self 的 5x5 neighbourhood。

边界自然截断。

==================================================
十、Neighbourhood Targets
==================================================

对于每 material point i：

q_self_i =
1/Js_i

定义：

Q_RING1_UNIFORM：

3x3 material neighbourhood：

mean_j(1/Js_j)

Q_RING2_UNIFORM：

5x5：

mean_j(1/Js_j)

然后定义 Gaussian-distance weighted target。

canonical activated covariance / scale
来自 frozen carrier。

首先建立 canonical tangent-plane
Gaussian distance metric。

对于 neighbour j：

delta_ij
在 local tangent frame。

定义：

w_ij =
exp(
-0.5 *
[
(du/sigma_u_i)^2
+
(dv/sigma_v_i)^2
]
)

其中：

sigma_u_i
sigma_v_i

来自 canonical Gaussian tangent scales。

禁止随便选择 bandwidth。

定义：

Q_RING1_GAUSSIAN

Q_RING2_GAUSSIAN

weighted mean of 1/Js_j。

输出所有 target 到：

material_neighbourhood_targets.csv

==================================================
十一、Neighbourhood Target Comparison
==================================================

对于每 cubic state：

比较：

R_local

vs：

q_self

Q_RING1_UNIFORM

Q_RING2_UNIFORM

Q_RING1_GAUSSIAN

Q_RING2_GAUSSIAN

每种 target：

MAE

RMSE

Pearson

Spearman

输出：

neighbourhood_mixing_analysis.csv

定义：

best_target

=
lowest MAE。

Improvement over self：

Improvement =
(
MAE_self
-
MAE_best
)
/
MAE_self

==================================================
十二、Neighbourhood Mixing Gate
==================================================

NEIGHBOURHOOD CONSISTENCY SUPPORTED：

对于 cubic_l0333：

至少一个 neighbourhood target：

MAE <= 0.70 * MAE_self

即：

相对 self target
MAE 降低 >=30%。

并且：

Spearman >=0.9。

同时：

在 cubic_l020
趋势同方向：

neighbourhood MAE < self MAE。

只有满足才能判：

CASE C candidate。

禁止降低 30% threshold。

==================================================
十三、Area-Preserving Control Point-Level Analysis
==================================================

对：

shear_k020
shear_k040

target：

q_self=1

正式 point-level：

MAE from 1

RMSE

median deviation

p90 absolute deviation

p95 absolute deviation

max deviation

同时计算：

u vs R Pearson/Spearman

v vs R Pearson/Spearman

输出：

area_preserving_pointwise_control.csv

不能只报告 global median。

==================================================
十四、Twist Point-Level Analysis
==================================================

twist_60：

使用实际 per-Gaussian Js。

不要直接假设所有 Js=1。

正式记录：

Js mean
std
min
max

point-level：

MAE vs 1/Js

median error

p90

Spearman if Js nonconstant

输出：

twist_pointwise_control.csv

解释：

为什么旧 global deviation=0.123。

重点判断：

geometry Jacobian Js

还是：

view / projection / neighbourhood mixing。

当前只分析。

==================================================
十五、Uniform vs Non-Uniform
==================================================

定义：

E_uniform

见正式 uniform point baseline。

定义：

E_nonuniform =

mean(
MAE_self cubic_l010,
MAE_self cubic_l020,
MAE_self cubic_l0333
)

计算：

ratio =

E_nonuniform /
(E_uniform + 1e-8)

输出：

uniform_nonuniform_comparison.csv

==================================================
十六、最终 Local Consistency Gate
==================================================

严格重新执行原 Stage 3.3 Gate。

--------------------------------------------------
CASE A:
LOCAL CONSISTENCY SUPPORTED
--------------------------------------------------

全部满足：

1.

cubic_l010 Spearman >=0.9

cubic_l020 Spearman >=0.9

cubic_l0333 Spearman >=0.9

2.

cubic_l0333 MAE_self <=0.10

3.

E_nonuniform <=2*E_uniform

4.

shear_k020 point MAE from 1 <=0.10

shear_k040 point MAE from 1 <=0.10

结论：

implicit area-dilution consistency
extends to spatially varying deformation。

explicit optical correction
currently unnecessary。

--------------------------------------------------
CASE B:
LOCAL CONSISTENCY BREAKS
--------------------------------------------------

uniform E remains good，

且满足任一：

cubic_l0333 MAE_self >0.15

or

cubic_l0333 Spearman <0.7

同时：

neighbourhood gate NOT supported。

结论：

local area-dilution consistency breaks。

--------------------------------------------------
CASE C:
NEIGHBOURHOOD CONSISTENCY
--------------------------------------------------

self-local target 不满足 CASE A，

但是：

Neighbourhood Mixing Gate SUPPORTED。

结论：

implicit optical response
follows footprint-scale neighbourhood deformation
rather than point-local Js。

--------------------------------------------------
CASE D:
CONTROL FAILURE
--------------------------------------------------

任一：

shear_k020 point MAE >0.15

shear_k040 point MAE >0.15

结论：

area-preserving local control fails。

停止 area-dilution claim。

==================================================
十七、重要：CASE 优先级
==================================================

判断顺序：

首先检查 CASE D。

如果不是 D：

检查 CASE A。

如果不是 A：

检查 CASE C。

如果不是 C：

检查 CASE B。

如果：

介于 threshold 之间：

INCONCLUSIVE。

禁止强行选择 CASE。

==================================================
十八、正式科学结论边界
==================================================

CASE A：

允许结论：

Under the controlled fixed-identity Gaussian sheet,
implicit optical-depth evolution approximately follows
point-local surface area dilution
even under spatially varying deformation。

中文：

在当前受控固定身份 Gaussian 薄膜中，
即使面积形变空间变化，
隐式光学深度演化仍近似遵循点局部面积稀释。

此时：

不允许设计 optical correction。

下一步：

irregular sampling / adaptive densification boundary。

--------------------------------------------------

CASE C：

允许结论：

The implicit optical response is better explained by
Gaussian-footprint-scale neighbourhood deformation
than by point-local area stretch。

中文：

Gaussian 隐式光学响应
更符合 footprint 尺度邻域形变，
而非单点局部面积拉伸。

此时：

允许进入：

Gaussian-footprint-aware deformation measure
设计阶段。

--------------------------------------------------

CASE B：

允许结论：

Uniform affine optical consistency
does not extend to spatially varying deformation。

如果 neighbourhood 也不能解释：

下一步寻找其它 geometry-optics coupling。

--------------------------------------------------

CASE D：

撤回 area-dilution consistency 主线。

==================================================
十九、可视化
==================================================

使用 matplotlib。

禁止 seaborn。

不指定颜色。

每图独立。

生成：

1.
cubic_pointwise_scatter.png

每个 cubic state 单独图。

x:
q_self=1/Js

y:
R_local

加入 y=x reference。

2.
cubic_l0333_spatial_response.png

x:
abs(u)

y:
R_local

同时 target 1/Js。

3.
neighbourhood_mae_comparison.png

self
ring1 uniform
ring2 uniform
ring1 Gaussian
ring2 Gaussian

4.
cubic_l0333_response_maps.png

41x41 map：

Js

1/Js

R_local

absolute self error

best neighbourhood target

best neighbourhood error

5.
shear_pointwise_maps.png

k020
k040

R_local-1 map。

==================================================
二十、报告
==================================================

输出：

input_integrity.md
gold_point_protocol_validation.md

material_point_response.csv
uniform_pointwise_baseline.csv
cubic_pointwise_consistency.csv
spatial_bin_consistency.csv

material_neighbourhood_targets.csv
neighbourhood_mixing_analysis.csv

area_preserving_pointwise_control.csv
twist_pointwise_control.csv

uniform_nonuniform_comparison.csv

local_correspondence_report.md
stage3_3_1_summary.md
stage3_3_1_log.txt

==================================================
二十一、local_correspondence_report.md
==================================================

必须中文回答：

A. Stage 3.3 为什么不能直接判 CASE A。

B. Gold point protocol 定义。

C. E_uniform。

D. cubic_l010 MAE / Spearman。

E. cubic_l020 MAE / Spearman。

F. cubic_l0333 MAE / Spearman。

G. cubic_l0333 center-bin：
R / target / MAE。

H. cubic_l0333 edge-bin：
R / target / MAE。

I. self target MAE。

J. ring1 uniform MAE。

K. ring2 uniform MAE。

L. ring1 Gaussian MAE。

M. ring2 Gaussian MAE。

N. best target。

O. best neighbourhood improvement %。

P. shear k020 point MAE / p95。

Q. shear k040 point MAE / p95。

R. twist Js range。

S. twist point MAE。

T. E_nonuniform / E_uniform。

U. Final CASE：
A/B/C/D/INCONCLUSIVE。

V. 当前真正科学问题。

W. 是否允许设计 optical correction /
footprint-aware deformation measure。

==================================================
二十二、README
==================================================

更新：

/data/wyh/DeformTransGS/README.md

明确：

Stage 3.3 global-median results
and area-preserving shear controls
were promising，

but the original Local Consistency Gate
required point-level cubic correlation
and local error analysis。

Therefore Stage 3.3 was not yet CASE A。

Stage 3.3.1 completes
material-point correspondence
and Gaussian-neighbourhood mixing analysis。

==================================================
二十三、实际执行
==================================================

必须实际：

input audit

Gold point protocol

per-material point response

uniform point baseline

cubic point correlation

spatial bins

material topology neighbourhood

uniform neighbourhood targets

Gaussian-weighted neighbourhood targets

neighbourhood comparison

shear point controls

twist point control

uniform vs nonuniform comparison

strict Gate

all figures

all reports

不要训练。

不要重新 render，
除非已有 render 缺失。

不要增加 deformation。

不要设计 method。

不要修改 TSGS。

==================================================
二十四、最终终端总结
==================================================

最后简洁输出：

1. Gold point protocol valid yes/no
2. E_uniform
3. cubic_l010 MAE / Spearman
4. cubic_l020 MAE / Spearman
5. cubic_l0333 MAE / Spearman
6. center-bin R / target
7. edge-bin R / target
8. self MAE
9. ring1 uniform MAE
10. ring2 uniform MAE
11. ring1 Gaussian MAE
12. ring2 Gaussian MAE
13. best target
14. neighbourhood improvement %
15. shear k020 MAE / p95
16. shear k040 MAE / p95
17. twist Js min/max
18. twist point MAE
19. E_nonuniform / E_uniform
20. Final CASE
21. Current scientific question
22. Can design footprint-aware measure yes/no
23. local_correspondence_report.md path
24. stage3_3_1_summary.md path

请直接实际计算和验证。

不要训练。
不要增加实验。
不要设计 method。
不要修改 TSGS。
PROMPT