下面这条是**缩小后的 E0-A 最小验证指令**。它先做 Jacobian（雅可比矩阵）分析，只有明确证明非刚体形变比静态和刚体旋转提供更多独立约束，才允许进行小规模优化；过不了立即停止，不训练任何 Gaussian。

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
你现在继续维护科研项目：

/data/wyh/DeformTransGS

============================================================
Stage E0-A
Nonrigid Incremental Identifiability Gate
============================================================

中文名称：

非刚体形变相对刚体运动的增量可辨识性验证

============================================================
一、候选研究方向
============================================================

暂定名称：

DeformProbe-GS

暂定问题：

Can known nonrigid deformation provide
transparent inverse-rendering constraints
that cannot be obtained from static views
or rigid angular diversity alone?

中文：

已知非刚体形变能否为透明逆渲染提供
静态多视角和刚体旋转无法提供的额外约束？

研究目标不是证明：

“运动有助于逆渲染”。

这个问题已有相关工作。

本阶段必须证明更窄的结论：

在总观测数量、
观察方向分布、
未知参数数量和优化预算完全相同的条件下，

非刚体形变是否通过：

1. 形变后的法线运输；

2. 表面积与厚度变化；

3. 形变矩阵对 canonical normal
   的不同约束；

为透明场景中的：

canonical surface normal

和

shared canonical optical depth

提供刚体运动无法提供的增量信息。

============================================================
二、本阶段科学定位
============================================================

本阶段是：

POINTWISE CONTROLLED IDENTIFIABILITY GATE。

本阶段不是：

Gaussian reconstruction experiment。

禁止：

训练 RT-Splatting

训练 TSGS

训练任何 Gaussian scene

修改 Gaussian 数量

创建 deformation MLP

创建 optical MLP

训练 Transformer

训练 Mamba

训练 neural field

进行 attribute release

继续各向异性微弱信号救援

引入偏振

使用真实图像

设计最终论文方法。

本阶段只回答：

DEFORM4 是否显著优于：

STATIC1

以及

RIGID4。

如果 DEFORM4 不能明显优于 RIGID4：

立即停止新方向。

不得把结果解释为
“非刚体形变监督有效”。

============================================================
三、已有正式状态
============================================================

旧 AttributeDeformGS：

STOP。

原因：

RT-native Perspective-V2 carrier
在复杂 K2 canonical case 上存在
confirmed novel-view generalization gap。

------------------------------------------------------------

Deformable Optical Transport
anisotropic-extinction line：

STOP。

最终结果：

CASE SENSOR-ROBUSTNESS-TRUE-FAIL。

浮点受控 oracle 中存在 Q3 区分信息，
但冻结的有限位深 RGB 传感器协议不稳定。

禁止继续增强该机制。

------------------------------------------------------------

仍可使用的资产：

Stage D0：

CASE DEFORMABLE-OPTICAL-TRANSPORT
-FEASIBILITY-PASS。

D0 已提供：

exact material identity

exact canonical xyz

exact triangle ID

exact barycentric

exact canonical local frame

exact deformation matrix F

exact transported local frame

exact camera and local-view direction

exact pointwise optical oracle。

D0 material samples：

4096 S0

4096 S1。

------------------------------------------------------------

C2-V2：

PERSPECTIVE-THIN-TRANSMISSION-V2。

其科学角色：

KNOWN-LAW-CONTROLLED-BENCHMARK。

MAT1/MAT2 内置：

h' = h0 / Js。

因此本阶段只能验证：

在该已知 forward model 中，
非刚体状态是否改善逆问题可辨识性。

不得声称：

Js 是新发现的真实物理规律。

============================================================
四、核心实验思想
============================================================

采用 matched-local-view protocol：

匹配局部观察方向协议。

所有 STATIC1、RIGID4、DEFORM4
接收完全相同的24个局部观察方向。

这样排除：

相机方向更多

视角覆盖更丰富

刚体旋转产生角度多样性

等解释。

对于刚体运动：

在相同局部观察方向下，

intrinsic material response

surface area

normal-relative viewing geometry

不应提供新的材料约束。

对于非刚体形变：

F^-T normal transport

和

J_area normal dependence

可能产生新的独立约束。

因此核心比较为：

DEFORM4

vs

RIGID4

而不是只比较：

DEFORM4

vs

STATIC1。

============================================================
五、输出路径
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stageE0_A_nonrigid_incremental_identifiability/

Create source：

/data/wyh/DeformTransGS/
deformation_supervision/stageE0_A/

Create：

stageE0_A/
    protocol/
    provenance/
    source_audit/
    samples/
    local_views/
    deformations/
    forward_model/
    jacobian/
    optimization/
    evaluation/
    independent_replay/
    statistics/
    audit/
    run_E0A.py

Do not overwrite any existing experiment。

============================================================
六、E0A-G0：Protocol Lock
============================================================

Locate and SHA256 lock：

D0 protocol lock

D0 material sample files

D0 deformation replay

D0 local-frame table

D0 pointwise optical oracle

D0 deformation equations

D0 material definitions

D0 camera definitions

D0 report

D0 summary

D1-N2-R3 report

D1-N2-R3 summary

C2-V2 benchmark lock。

Actual paths must be traced
from the filesystem and protocol files。

Do not fabricate old paths。

Output：

E0A_protocol_lock.json。

Gate：

E0A-G0 PASS
iff all required sources exist
and SHA256 values are recorded。

============================================================
七、E0A-G1：Source and Equation Audit
============================================================

Trace exact source implementations for：

surface definitions

material definitions

canonical normal

deformation matrices

deformed normal

surface Jacobian

pointwise optical equation。

Record：

source file

function

line range

SHA256。

The expected equations are：

deformed point：

x_F = F x。

deformed normal：

n_F =
normalize(F^-T n)。

surface area ratio：

J_area =
abs(det(F))
*
||F^-T n||。

For a fixed world direction d
pointing toward the camera：

cos_theta =
max(
abs(dot(n_F,d)),
0.15
)。

Canonical optical depth：

tau0_rgb =
sigma_rgb * h0。

For MAT1/MAT2：

tau_rgb =
tau0_rgb
/
(
J_area * cos_theta
)。

Transmission：

T_rgb =
exp(-tau_rgb)。

Use source as truth。

Do not reconstruct equations from memory
when implementation exists。

Output：

E0A_source_inventory.csv
E0A_forward_equations.md。

Gate：

E0A-G1。

============================================================
八、E0A-G2：Independent Forward Replay
============================================================

Create independent script：

stageE0_A/
independent_replay/
replay_D0_forward.py。

It must not import the primary E0-A
forward implementation。

Select deterministic100000 D0 rows。

Seed：

20260714。

Independently recompute：

deformed xyz

deformed normal

J_area

cos_theta

tau_rgb

T_rgb。

Compare against D0 oracle。

Require：

xyz p99/max absolute error：

<=1e-10 / <=1e-8。

normal angular p99/max：

<=1e-5 / <=1e-3 degrees。

J_area p99/max relative error：

<=1e-6 / <=1e-4。

tau p99/max relative error：

<=1e-6 / <=1e-4。

RGB p99/max absolute error：

<=1e-7 / <=1e-5。

Output：

E0A_forward_replay.csv。

If E0A-G2 fails：

STOP。

Do not run Jacobian or optimization。

============================================================
九、Material Sample Subset
============================================================

Use deterministic D0 material samples。

Select：

64 samples from S0

64 samples from S1。

Total：

128 material points。

Seed：

20260714。

Use stratified selection over：

triangle ID

u/v coverage。

Do not select by：

optical value

normal orientation

future metric

expected conditioning。

Reuse the exact same128 sample IDs
in every protocol。

Use materials：

MAT1

MAT2。

Do not use MAT0 in the primary Gate。

Output：

E0A_material_sample_subset.csv。

Required audit：

S0 count =64

S1 count =64

duplicate sample IDs =0

u/v range recorded

triangle coverage recorded。

Gate：

E0A-G3。

============================================================
十、Matched Local-View Direction Lock
============================================================

Create exactly24 controlled directions
in the TRUE deformed local frame。

Use elevation angles from surface normal：

theta：

0
15
30
45
55
65 degrees。

Use azimuths：

phi：

0
90
180
270 degrees。

For every theta/phi：

v_local =
[
sin(theta)*cos(phi),
sin(theta)*sin(phi),
cos(theta)
]。

This gives：

6 x4 =24 directions。

For theta=0，
the four azimuth aliases produce identical
directions。

Keep only one theta=0 direction
and replace the other three with：

theta=20 degrees

phi=45
135
225 degrees。

Final count must remain24 unique directions。

Save exact vectors before execution。

For every sample and deformation：

build the true transported local frame：

B_F_gt =
[t1_F_gt,t2_F_gt,n_F_gt]。

Convert local direction to fixed world direction：

d_world =
B_F_gt * v_local。

During inverse optimization：

d_world is a constant observation ray。

The optimizer must not regenerate d_world
from the predicted normal。

Verify direction roundtrip：

v_replay =
B_F_gt^T d_world。

Require max error <=1e-12。

Output：

E0A_local_view_lock.csv
E0A_local_view_roundtrip.csv。

Gate：

E0A-G4。

============================================================
十一、Deformation Protocols
============================================================

Use exactly three primary protocols：

STATIC1

RIGID4

DEFORM4。

Every protocol receives exactly：

24 RGB observations。

Every observation contains：

3 channels。

Total scalar targets：

72。

No protocol receives more observations。

------------------------------------------------------------

STATIC1：

deformation states：

D0_IDENTITY only。

Use all24 local-view directions。

------------------------------------------------------------

RIGID4：

Use exactly four rigid matrices：

R0_IDENTITY：

I。

R1_RZ30：

Rz(30 degrees)。

R2_RX20：

Rx(20 degrees)。

R3_RY_NEG20：

Ry(-20 degrees)。

Assign exactly6 local-view directions
to each rigid state。

------------------------------------------------------------

DEFORM4：

Use exactly four states：

N0_IDENTITY：

I。

N1_STRETCH_X1P50：

diag(1.5,1.0,1.0)。

N2_SHEAR_XY0P30：

[
[1.0,0.3,0],
[0,1.0,0],
[0,0,1.0]
]。

N3_ANISO_X1P60_Y0P80：

diag(1.6,0.8,1.0)。

Assign exactly6 local-view directions
to each state。

------------------------------------------------------------

Create exactly three schedule variants：

SCHED0

SCHED1

SCHED2。

Schedules cyclically rotate which six
local-view directions are assigned
to each state。

Every local-view direction appears
exactly once per protocol/schedule。

STATIC1 always uses all24 directions。

No duplicate：

(deformation key,local-view key)

inside one protocol schedule。

Output：

E0A_deformation_lock.csv
E0A_observation_schedule.csv。

Gate E0A-G5 iff：

all matrices have positive determinant

condition number <=10

observation count =24

scalar target count =72

local-view direction multiset
is identical across protocols。

============================================================
十二、Held-Out Nonrigid Deformation Bank
============================================================

Create exactly six evaluation-only matrices：

H1：

diag(1.15,0.95,1.0)。

H2：

diag(1.35,0.85,1.0)。

H3：

[
[1.0,0.15,0],
[0,1.0,0],
[0,0,1.0]
]。

H4：

[
[1.0,-0.20,0],
[0,1.0,0],
[0,0,1.0]
]。

H5：

Rz(30deg)
*
diag(1.30,0.90,1.0)
*
Rz(-30deg)。

H6：

Rz(60deg)
*
diag(1.20,0.80,1.0)
*
Rz(-60deg)。

Requirements：

positive determinant

condition number <=10

not present in STATIC1/RIGID4/DEFORM4

no duplicates with each other。

These matrices must never appear
in fitting observations。

Output：

E0A_heldout_deformation_lock.csv
E0A_heldout_deformation_equations.md。

Gate：

E0A-G6。

============================================================
十三、Inverse Problem
============================================================

For every：

material point

material MAT1/MAT2

the shared unknowns are exactly5 parameters。

Normal parameters：

alpha

beta。

Optical parameters：

log_tau0_r

log_tau0_g

log_tau0_b。

No parameter may depend on：

deformation

view direction

camera

protocol observation index。

No per-state optical residual。

No free opacity。

No free transmissivity。

No feature vector。

No neural network。

------------------------------------------------------------

Normal parameterization：

Start from corrupted canonical normal：

n_init。

Construct deterministic tangent basis：

e1_init

e2_init。

Predicted canonical normal：

n_pred =
normalize(
n_init
+
alpha*e1_init
+
beta*e2_init
)。

For each F：

n_F_pred =
normalize(
F^-T n_pred
)。

Predicted area ratio：

J_pred =
abs(det(F))
*
||F^-T n_pred||。

Predicted cosine：

cos_pred =
max(
abs(dot(n_F_pred,d_world)),
0.15
)。

Predicted optical depth：

tau_pred_rgb =
exp(log_tau0_rgb)
/
(
J_pred*cos_pred
)。

Predicted transmission：

T_pred =
exp(-tau_pred)。

------------------------------------------------------------

Only tau0 may be recovered。

Do not separately optimize：

sigma

and

h0。

They are not independently identifiable
in this pointwise model。

Output：

E0A_inverse_problem.md。

============================================================
十四、Initialization Lock
============================================================

Use one normal corruption level：

10 degrees。

Use three deterministic initialization seeds：

20260714

20260715

20260716。

For every sample and seed：

derive a deterministic tangent rotation axis。

Rotate the true canonical normal
by exactly10 degrees。

Initial optical parameters：

log_tau0_init =
log_tau0_gt + epsilon。

epsilon：

deterministic Gaussian

mean0

standard deviation0.25。

Use the same initialization
bitwise across：

STATIC1

RIGID4

DEFORM4。

Use the same initialization across：

SCHED0

SCHED1

SCHED2。

Protocol name must not participate
in the random seed。

Output：

E0A_initialization_lock.csv。

Require：

normal corruption max error
from10 degrees <=1e-10 degrees。

Require：

bitwise identical initial tensors
across protocols and schedules。

Gate：

E0A-G7。

============================================================
十五、Observation Regimes
============================================================

Generate two primary observation regimes。

------------------------------------------------------------

CLEAN64：

float64 clean linear transmission。

------------------------------------------------------------

RAW12：

Start from clean T。

Add deterministic independent Gaussian noise：

sigma =
0.25 / 4095。

Use seeds：

20260714
20260715
20260716。

Clamp to[0,1]。

Quantize：

T_q =
round(T*4095)/4095。

The optimizer receives dequantized T_q
as a fixed constant。

Do not differentiate through quantization。

------------------------------------------------------------

All observations must be generated
using the independent exact forward equations。

Output：

E0A_observation_manifest.csv。

============================================================
十六、Optimization Loss and Optimizer
============================================================

Convert observation to optical depth：

tau_obs =
-log(
clamp(T_obs,1e-8,1.0)
)。

Primary loss：

L =
mean(
(tau_pred-tau_obs)^2
)。

No RGB loss。

No normal supervision。

No tau0 supervision。

No regularizer using ground truth。

A numerical soft barrier is allowed
only when：

tau0_pred <0.01

or

tau0_pred >10.0。

Use real PyTorch autograd。

Use float64。

Optimizer：

Adam。

Learning rates：

alpha/beta：

0.03。

log_tau0 RGB：

0.05。

Maximum steps：

1000。

Evaluate full training loss every10 steps。

Early stopping：

relative improvement threshold：

1e-9。

Patience：

200 steps since last best evaluation。

Checkpoint selection：

lowest full training loss only。

Never select using：

ground-truth parameter error

held-out deformation

future metric。

All protocols share identical settings。

Output：

E0A_loss_definition.md
E0A_optimizer_lock.json。

============================================================
十七、Execution Size
============================================================

Primary combinations：

128 samples

x2 materials

x3 initialization seeds

x3 schedules

x3 protocols

x2 observation regimes。

Expected primary instances：

13824。

Run vectorized batches。

Do not launch one process per instance。

Use only:

CUDA_VISIBLE_DEVICES=2,3。

Preferred mapping：

logical GPU0：

S0。

logical GPU1：

S1。

Record：

physical/logical GPU mapping

batch size

runtime

peak memory

PyTorch version

CUDA version

nonfinite count。

Output：

E0A_runtime_provenance.json。

============================================================
十八、Phase A：Ground-Truth Jacobian Gate
============================================================

Before any optimization，
evaluate Jacobian at the true parameters。

For every：

sample

material

protocol

schedule

compute：

J =
d vec(tau observations)
/
d
[
alpha,
beta,
log_tau0_r,
log_tau0_g,
log_tau0_b
]。

J shape：

72 x5。

Use float64 autograd。

Compute：

numerical rank

singular values

minimum singular value

maximum singular value

condition number

log10 condition number。

Compute Fisher matrix：

H =
J^T J。

Define blocks：

normal block：

alpha,beta。

optical block：

three log_tau0 channels。

Define normalized block coupling：

coupling =
||
H_normal,optical
||_F
/
sqrt(
||
H_normal,normal
||_F
*
||
H_optical,optical
||_F
+
1e-18
)。

Lower coupling means
less geometry-optics ambiguity。

Output：

E0A_Jacobian_rows.csv
E0A_Jacobian_summary.csv。

------------------------------------------------------------

Jacobian Phase Gate J-GATE:

J1：

all Jacobians finite。

J2：

full rank fraction >=99.9%
for DEFORM4。

J3：

DEFORM4 median minimum singular value
must be at least：

2.0x STATIC1

and

1.5x RIGID4。

J4：

DEFORM4 median log10 condition number
must improve by at least：

0.25 relative to STATIC1

and

0.15 relative to RIGID4。

Improvement means numerical value decreases。

J5：

DEFORM4 median coupling
must reduce by at least：

15% vs STATIC1

and

10% vs RIGID4。

J6：

minimum singular-value improvement direction
must hold separately for：

S0

S1

MAT1

MAT2。

Use paired comparison by exact：

sample

material

schedule。

If J-GATE fails：

Final CASE：

NONRIGID-INCREMENTAL-JACOBIAN-NO-BENEFIT。

STOP。

Do not run optimization。

Do not rescue with new matrices
or changed thresholds。

============================================================
十九、Phase B：Small Real Optimization
============================================================

Run Phase B only if J-GATE passes。

For every primary instance：

start from locked corrupted initialization。

Optimize the five parameters。

Save：

sample

surface

material

seed

schedule

protocol

observation regime

steps executed

selected step

stop reason

initial loss

best loss

selected alpha/beta

selected log_tau0

checkpoint SHA256。

Output：

E0A_optimization_runs.csv。

Require：

nonfinite count =0。

Require exact checkpoint reload：

max persistent tensor error =0。

============================================================
二十、Parameter Recovery
============================================================

Compute：

canonical normal angular error。

tau0 log error：

median over RGB of：

abs(
log_tau0_pred-log_tau0_gt
)。

tau0 relative error：

median over RGB。

Report distributions by：

protocol

surface

material

observation regime。

Output：

E0A_parameter_recovery.csv
E0A_parameter_recovery_summary.csv。

============================================================
二十一、Held-Out Deformation Evaluation
============================================================

For each recovered parameter set：

evaluate all：

6 held-out deformation matrices

x24 locked local-view directions。

For every held-out F and local view：

construct fixed world ray using
the TRUE held-out transported frame。

Evaluate prediction using recovered：

n_pred

tau0_pred。

Compare to exact ground truth。

Metrics：

tau RMSE

tau MAE

RGB RMSE

RGB PSNR

median tau relative error。

Output：

E0A_heldout_deformation_metrics.csv
E0A_heldout_deformation_summary.csv。

This is the primary generalization metric。

============================================================
二十二、Paired Statistical Comparison
============================================================

Use exact paired instances：

same sample

same material

same initialization seed

same schedule

same observation regime。

Primary comparison：

DEFORM4 vs RIGID4。

Secondary comparison：

DEFORM4 vs STATIC1。

Use paired bootstrap：

10000 resamples。

Seed：

20260714。

Report：

median paired relative improvement

95% confidence interval

fraction of pairs improved。

Primary metrics：

held-out tau RMSE

normal angular error

tau0 log error

minimum singular value

log10 condition number

geometry-optics coupling。

Output：

E0A_paired_statistics.csv。

============================================================
二十三、Single-Block Diagnostic
============================================================

This diagnostic checks whether benefit
comes specifically from joint ambiguity reduction。

Use only：

32 S0 samples

32 S1 samples

MAT1/MAT2

seed20260714

SCHED0

CLEAN64。

Run three parameter settings：

JOINT：

normal + tau0 unknown。

NORMAL_ONLY：

tau0 fixed to ground truth；
normal unknown。

OPTICS_ONLY：

normal fixed to ground truth；
tau0 unknown。

Compare protocols：

STATIC1

RIGID4

DEFORM4。

Report held-out errors。

Expected useful pattern：

DEFORM4 improvement is largest in JOINT，
while single-block problems show
smaller or no improvement。

Output：

E0A_single_block_diagnostic.csv。

This diagnostic does not control
the primary PASS Gate。

============================================================
二十四、Equal-Budget and Leakage Audit
============================================================

For every paired comparison verify：

same sample

same material

same initialization

same observation count24

same scalar target count72

same local-view direction multiset

same parameter count5

same optimizer

same step cap

same loss

same checkpoint-selection rule。

Verify:

held-out matrices never appear in training。

Verify:

metrics do not participate
in checkpoint selection。

Output：

E0A_equal_budget_audit.csv
E0A_leakage_audit.md。

Gate：

E0A-G8。

Any mismatch：

E0A-PROVENANCE-FAIL。

============================================================
二十五、Independent Metric Reproduction
============================================================

Create independent script：

stageE0_A/
independent_replay/
reproduce_E0A_metrics.py。

It must not import primary metric functions。

Select deterministic5000 optimization instances。

Recompute：

normal error

tau0 error

held-out tau RMSE

held-out RGB RMSE

Jacobian minimum singular value

coupling。

Require：

normal error max difference <=1e-10 degrees

tau0 error max difference <=1e-12

tau RMSE max difference <=1e-10

RGB RMSE max difference <=1e-10

singular-value relative max error <=1e-8

coupling max error <=1e-10。

Output：

E0A_metric_reproduction.csv。

Gate：

E0A-G9。

============================================================
二十六、Frozen Optimization Gates
============================================================

Only evaluate these Gates
after J-GATE has passed。

------------------------------------------------------------

CLEAN64 Gate：

Compare DEFORM4 vs RIGID4。

C1：

median held-out tau RMSE reduction >=10%。

C2：

median normal angular error reduction >=10%。

C3：

median tau0 log error reduction >=8%。

C4：

at least two of C1/C2/C3 pass。

C5：

95% confidence interval for held-out tau
improvement excludes zero。

C6：

held-out tau improvement direction
holds separately for：

S0

S1

MAT1

MAT2。

------------------------------------------------------------

RAW12 Gate：

Compare DEFORM4 vs RIGID4。

R1：

median held-out tau RMSE reduction >=5%。

R2：

median normal angular error reduction >=5%。

R3：

at least one of R1/R2 passes。

R4：

both surfaces show
the same held-out tau improvement direction。

R5：

both materials show
the same held-out tau improvement direction。

------------------------------------------------------------

STATIC benefit check：

DEFORM4 must also outperform STATIC1
on held-out tau RMSE。

Required reduction：

>=10% CLEAN64。

This is secondary because
RIGID4 is the stricter control。

============================================================
二十七、Scientific Classification
============================================================

Classify benefit as：

NONRIGID-DEFORMATION-SPECIFIC-BENEFIT

iff：

J-GATE passes

CLEAN64 Gate passes

RAW12 Gate passes

DEFORM4 beats STATIC1

equal-budget/leakage audit passes

metric reproduction passes。

------------------------------------------------------------

Classify as：

MOTION-DIVERSITY-BENEFIT-ONLY

iff：

DEFORM4 beats STATIC1

but does not beat RIGID4
under frozen Gates。

------------------------------------------------------------

Classify as：

NONRIGID-INCREMENTAL-NO-BENEFIT

iff：

DEFORM4 fails to beat RIGID4。

============================================================
二十八、Allowed Scientific Conclusion
============================================================

If final PASS：

Allowed wording：

Under a controlled pointwise
thin-transmission inverse problem，
with matched local-view directions
and equal observation budgets，
multiple known nonrigid deformation states
provide incremental constraints for joint
surface-normal and shared optical-depth recovery
beyond rigid motion alone。

Forbidden wording：

This already improves RT-Splatting。

This already validates a Gaussian method。

All real transparent objects benefit。

The critical dynamic attributes
have been identified。

Js is a newly discovered law。

The direction is ready for publication。

Formal role：

CONTROLLED-NONRIGID-SUPERVISION
-IDENTIFIABILITY-BENCHMARK。

============================================================
二十九、Final CASE
============================================================

CASE NONRIGID-INCREMENTAL
-IDENTIFIABILITY-PASS：

all provenance Gates pass；

J-GATE passes；

CLEAN64 Gate passes；

RAW12 Gate passes；

DEFORM4 beats STATIC1；

DEFORM4 beats RIGID4。

New primary line：

CONTINUE。

Next exact action：

design Stage E1
Surface-Attached Shared-Optics
Gaussian Carrier Gate。

------------------------------------------------------------

CASE MOTION-DIVERSITY-BENEFIT-ONLY：

DEFORM4 improves over STATIC1
but does not clearly improve over RIGID4。

New primary line：

STOP or REFORMULATE。

Do not claim nonrigid-specific novelty。

------------------------------------------------------------

CASE NONRIGID-INCREMENTAL
-JACOBIAN-NO-BENEFIT：

Jacobian Phase Gate fails。

Do not run optimization。

New primary line：

STOP。

Return to RecycleGS。

------------------------------------------------------------

CASE NONRIGID-INCREMENTAL
-OPTIMIZATION-NO-BENEFIT：

Jacobian Gate passes，
but real optimization fails
the frozen CLEAN64 or RAW12 Gate。

One implementation/convergence audit
is allowed only if training loss
is still clearly decreasing
at the frozen step cap。

No learning-rate tuning。

No deformation-bank tuning。

------------------------------------------------------------

CASE E0A-PROVENANCE-FAIL：

source replay

equal-budget audit

leakage audit

or metric reproduction fails。

Repair provenance only。

============================================================
三十、Required Outputs
============================================================

E0A_protocol_lock.json

E0A_source_inventory.csv
E0A_forward_equations.md

E0A_forward_replay.csv

E0A_material_sample_subset.csv

E0A_local_view_lock.csv
E0A_local_view_roundtrip.csv

E0A_deformation_lock.csv
E0A_observation_schedule.csv

E0A_heldout_deformation_lock.csv
E0A_heldout_deformation_equations.md

E0A_inverse_problem.md
E0A_initialization_lock.csv

E0A_observation_manifest.csv

E0A_loss_definition.md
E0A_optimizer_lock.json
E0A_runtime_provenance.json

E0A_Jacobian_rows.csv
E0A_Jacobian_summary.csv

E0A_optimization_runs.csv

E0A_parameter_recovery.csv
E0A_parameter_recovery_summary.csv

E0A_heldout_deformation_metrics.csv
E0A_heldout_deformation_summary.csv

E0A_paired_statistics.csv
E0A_single_block_diagnostic.csv

E0A_equal_budget_audit.csv
E0A_leakage_audit.md

E0A_metric_reproduction.csv

stageE0A_report.md
stageE0A_summary.md
stageE0A_log.txt

============================================================
三十一、最终终端只输出
============================================================

A. E0A-G0

B. locked source count

C. E0A-G1

D. forward replay sampled rows

E. forward replay tau p99/max error

F. forward replay RGB p99/max error

G. E0A-G2

H. selected S0/S1 samples

I. duplicate sample count

J. E0A-G3

K. local-view count

L. local-view roundtrip max error

M. E0A-G4

N. protocol names

O. observations per protocol

P. scalar targets per protocol

Q. local-view multiset identical yes/no

R. E0A-G5

S. held-out deformation count

T. held-out leakage count

U. E0A-G6

V. initialization normal-angle max error

W. initialization bitwise reuse yes/no

X. E0A-G7

Y. Jacobian finite fraction
STATIC1/RIGID4/DEFORM4

Z. Jacobian full-rank fraction
STATIC1/RIGID4/DEFORM4

AA. median minimum singular value
STATIC1/RIGID4/DEFORM4

AB. DEFORM4 minimum-singular-value ratio
vs STATIC1/RIGID4

AC. median log10 condition number
STATIC1/RIGID4/DEFORM4

AD. DEFORM4 log-condition improvement
vs STATIC1/RIGID4

AE. median coupling
STATIC1/RIGID4/DEFORM4

AF. DEFORM4 coupling reduction
vs STATIC1/RIGID4

AG. S0/S1/MAT1/MAT2
minimum-singular-value direction pass yes/no

AH. J-GATE pass yes/no

AI. optimization executed yes/no

AJ. expected optimization instances

AK. actual optimization instances

AL. optimization nonfinite count

AM. checkpoint reload max error

AN. E0A-G8

AO. CLEAN64 DEFORM4 vs RIGID4
held-out tau RMSE reduction

AP. CLEAN64 DEFORM4 vs RIGID4
normal error reduction

AQ. CLEAN64 DEFORM4 vs RIGID4
tau0 log error reduction

AR. held-out tau improvement95% CI

AS. CLEAN64 Gate pass yes/no

AT. RAW12 DEFORM4 vs RIGID4
held-out tau RMSE reduction

AU. RAW12 DEFORM4 vs RIGID4
normal error reduction

AV. RAW12 both surfaces direction yes/no

AW. RAW12 both materials direction yes/no

AX. RAW12 Gate pass yes/no

AY. CLEAN64 DEFORM4 vs STATIC1
held-out tau improvement

AZ. joint-vs-single-block diagnostic conclusion

BA. equal-budget audit pass yes/no

BB. independent metric reproduction max error

BC. E0A-G9

BD. scientific benefit classification

BE. benefit nonrigid-specific yes/no

BF. Final CASE

BG. new primary line
STOP/CONTINUE/REFORMULATE

BH. allow Stage E1 Gaussian Gate yes/no

BI. old AttributeDeformGS status

BJ. anisotropic transport status

BK. next exact research action

BL. report path

BM. summary path

============================================================
三十二、README
============================================================

Update：

/data/wyh/DeformTransGS/README.md

Record：

The original AttributeDeformGS
attribute-release line remains stopped。

The anisotropic optical-transport line
remains stopped because the additional signal
is not robust under the frozen finite-bit
sensor protocol。

A new low-cost candidate Gate is introduced：

NONRIGID INCREMENTAL IDENTIFIABILITY。

The candidate novelty is not：

motion helps inverse rendering。

The required claim is narrower：

under matched local-view directions，
equal observation budgets，
and identical unknown parameters，
known nonrigid deformation may provide
geometry-optics constraints that cannot be
obtained from rigid motion alone。

Stage E0-A is pointwise。

It does not train a Gaussian model。

STATIC1，
RIGID4，
and DEFORM4 receive the exact same
24 local-view directions。

The primary scientific comparison is：

DEFORM4 vs RIGID4。

The inverse problem contains exactly：

two canonical-normal parameters

and

three shared optical-depth parameters。

A Jacobian Gate is executed first。

Optimization is forbidden if
the Jacobian Gate fails。

Only if DEFORM4 improves：

minimum singular value

condition number

geometry-optics coupling

parameter recovery

and held-out deformation prediction

beyond RIGID4，

may the project proceed to a Gaussian carrier。

============================================================
三十三、实际执行
============================================================

必须实际：

lock D0 evidence

trace source equations

independently replay forward model

select128 deterministic material points

lock24 matched local-view directions

create STATIC1/RIGID4/DEFORM4 protocols

verify equal observation budgets

create held-out nonrigid matrices

lock identical corrupted initialization

generate CLEAN64 and RAW12 observations

compute full float64 Jacobians

apply Jacobian stop Gate

only if Gate passes：
run batched real autograd optimization

evaluate parameter recovery

evaluate held-out deformations

perform paired bootstrap comparison

run single-block diagnostic

audit equal budgets and leakage

independently reproduce metrics

make the frozen scientific decision

Do not perform another rescue
if DEFORM4 fails to beat RIGID4。
PROMPT
```

这条指令最终只允许两种值得继续的结果：

```text
DEFORM4 明显优于 STATIC1
并且
DEFORM4 明显优于 RIGID4
```

只优于静态、不优于刚体，就说明收益主要来自运动或角度多样性，不能支撑“非刚体形变为透明逆渲染提供独特监督”的创新。
