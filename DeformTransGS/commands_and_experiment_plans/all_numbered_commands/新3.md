这轮结果是**正式的大 PASS**。

我接受：

```text
CASE RTSPLAT-NATIVE-STATE-CONTROL-RESTORED
repaired J3: PASS
PRIMARY ATTRIBUTE-DEFORMATION LINE: CONTINUE
```

而且这次不是“为了继续项目放宽标准”。恰恰相反，是原来的有限差分检查工具本身在小梯度条件下分辨率不足。

关键证据非常扎实：

[
\text{opacity: }
g^\top d=-9.911521\times10^{-5}
]

结构化有限差分：

[
-9.911461\times10^{-5}
]

相对误差：

[
6.07\times10^{-6}
]

transmissivity（透射率）：

[
g^\top d=4.543977\times10^{-5}
]

有限差分：

[
4.543932\times10^{-5}
]

相对误差：

[
9.80\times10^{-6}
]

还有：

```text
opacity coordinate checks       = 16/16
transmissivity coordinate checks = 16/16

first broken edge = NONE_FOUND
```

所以现在可以正式写：

> `_occupancy`、`_opacity`、`_transmissivity` 和 `_features_dc` 均具有真实可微、真实前向响应、检查点可恢复的原生状态控制能力。

其中三个非几何状态：

```text
OPTICAL_OPACITY    -> _opacity
TRANSMISSIVITY     -> _transmissivity
SH_APPEARANCE      -> _features_dc
```

已经足以让我们的科学问题进入实验阶段。

但注意：

> **现在只是“有资格研究”，还没证明这些属性需要随形变变化。**

最后一个生死 Gate 仍然是：

[
\boxed{\text{J4：RT-native canonical capacity}}
]

也就是 RT-Splatting 原生载体能不能先拟合静态透明 GT。

---

# 下一步完整 Codex 指令

这次只恢复 R3 的 M4a 到 J4。

**不要重跑 runtime、checkpoint、J3、G1。**

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
Stage 5.0-R3-R1
RT-Native Canonical Capacity Resume Gate
============================================================

中文：

RT 原生透明载体
静态表达能力恢复验证 Gate

============================================================
最高优先级
============================================================

Resume the frozen Stage5.0-R3 protocol
ONLY from：

M4a camera projection closure

through：

J4 canonical capacity。

Do NOT rerun：

Stage5 runtime audit

Stage5.0-R1

Stage5.0-R2 native extension builds

R3 checkpoint provenance

R3 original J3

R3-G1 gradient numerical closure。

Those stages are CLOSED。

============================================================
当前正式科学状态
============================================================

AttributeDeformGS scientific question：

For a deforming semi-transparent surface，
which decoupled native optical/material states
must dynamically evolve？

Status：

UNTESTED。

However：

SCIENTIFIC QUESTION IS NOW
EXPERIMENTALLY ADDRESSABLE
SUBJECT TO J4 CANONICAL CAPACITY。

PRIMARY ATTRIBUTE-DEFORMATION LINE：

CONTINUE。

KIOT：

CONTROLLED-CARRIER-ONLY。

============================================================
旧 TSGS carrier 状态
============================================================

Stage4 corrected canonical carrier：

K0：

PSNR =
21.231329

median tau_eq Elog =
0.178887。

K1：

21.231336

0.178926。

K2：

17.881014

0.215660。

Stage4 final：

REAL-CANONICAL-CARRIER-INSUFFICIENT。

The TSGS single-opacity carrier
must NOT be reused for attribute necessity claims。

============================================================
Verified clean GT
============================================================

VERIFIED_GT_ROOT：

/data/wyh/DeformTransGS/experiments/
stage4_0_R2A_GT1_gt_optical_semantics_closure/
clean_gt

Independent validation：

Js relative p99/max =
0 /0

tau relative p99/max =
5.740e-08 /5.951e-08

RGB absolute p99 =
2.977e-08

alpha absolute p99 =
1.481e-08。

Do NOT regenerate GT。

Do NOT modify GT。

============================================================
Verified RT runtime
============================================================

Exact launcher：

/data/wyh/DeformTransGS/
rtsplat_attribute_study/
real_build_gate/
verified_rtsplat_R2_python.sh

Interpreter：

/usr/bin/python3

Python：

3.10.12

PyTorch：

2.6.0+cu124

torch CUDA：

12.4。

Visible GPUs：

Tesla V100-PCIE-32GB

compute capability7.0。

nvdiffrast：

real local native build PASS。

diff_surfel_anych：

real local native build PASS。

Critical imports：

6/6 PASS。

Native RT renderer forward：

PASS。

Do NOT rebuild these extensions。

============================================================
Verified full-state provenance
============================================================

Stage5.0-R3 established：

persistent per-Gaussian tensor count =
11。

Inventory：

GEOMETRY_XYZ
->_xyz

SH_APPEARANCE
->_features_dc

SH_APPEARANCE
->_features_rest

FOOTPRINT_SCALE
->_scaling

FOOTPRINT_ROTATION
->_rotation

GEOMETRIC_OCCUPANCY
->_occupancy

OPTICAL_OPACITY
->_opacity

TRANSMISSIVITY
->_transmissivity

ROUGHNESS
->_roughness

REFLECTION_COLOR
->_reflectance

MATERIAL_FEATURE
->_language_feature。

Auxiliary modules：

dir_encoding

light_mlp。

Project-side full-state checkpoint saves：

11 persistent tensors

2 auxiliary modules。

Tensor roundtrip max error：

0。

Eight-camera pre/post reload
render max difference：

0。

M2a PASS。

M2b PASS。

J2a_REPAIRED PASS。

Do NOT modify checkpoint adapter semantics。

============================================================
Repaired native state-control evidence
============================================================

The original R3 random-unit
directional derivative Gate
was numerically under-resolved
for low-sensitivity states。

Original opacity finite-difference
signal at eps1e-2 was only：

8.2098 times the float32 scalar-loss ULP。

Original transmissivity signal：

8.7330 times the float32 scalar-loss ULP。

Therefore the original random-unit Gate
was retired as the sole numerical
gradient-validation instrument。

The repaired state-independent instrument uses：

fixed structured directions
independent of gradient values

float64 loss reduction
AFTER the native float32 renderer

stable adjacent-epsilon finite-difference plateau。

No custom gradient。

No source modification。

No gradient-dependent direction。

------------------------------------------------------------

Repaired gradient classifications：

_occupancy：

AUTOGRAD-VALID。

_opacity：

AUTOGRAD-VALID。

_transmissivity：

AUTOGRAD-VALID。

_features_dc：

AUTOGRAD-VALID。

Opacity best structured directional derivative：

autograd =
-9.911521192407236e-05

finite difference =
-9.911461044490959e-05

relative error =
6.068484858093678e-06。

Transmissivity：

autograd =
4.5439766836352646e-05

finite difference =
4.543932163869613e-05

relative error =
9.797533911676722e-06。

Opacity coordinate checks：

16/16 PASS。

Transmissivity coordinate checks：

16/16 PASS。

First broken edges：

NONE_FOUND。

------------------------------------------------------------

Repaired ATTRIBUTE-CONTROL-VALID states：

_occupancy

_opacity

_transmissivity

_features_dc。

Valid non-geometry states：

_opacity

_transmissivity

_features_dc。

Repaired J3：

PASS。

Final G1 CASE：

RTSPLAT-NATIVE-STATE-CONTROL-RESTORED。

============================================================
Important scientific restriction
============================================================

The above evidence proves ONLY：

these native states are persistent

serializable

render-active

gradient-active

and autograd-valid。

It does NOT prove：

opacity must deform

transmissivity must deform

appearance must deform

occupancy must deform

or any state is necessary。

No state ranking is allowed。

No dynamic attribute release
is allowed before J4 PASS。

============================================================
本阶段唯一目标
============================================================

Answer exactly：

Can a fixed4096-Gaussian
RT-native transparent carrier
fit the independently validated
static thin-transmission benchmark
for K0/K1/K2？

Only if：

K0 PASS

K1 PASS

K2 PASS

may Stage5.1 dynamic attribute
sufficiency experiment be designed。

============================================================
绝对禁止事项
============================================================

禁止：

rerun R2 builds

rerun J3

rerun G1

modify RT-Splatting source

modify CUDA/C++ source

change clean GT

regenerate GT

return to TSGS

KIOT

tau/Js transport

opacity-linear transport

dynamic deformation

attribute release

attribute ranking

necessity claims

new state family

MLP predictor

densification

pruning

Gaussian creation

Gaussian deletion

increase Gaussian count beyond4096

optimize xyz

lower28 dB threshold

change0.25 tau_eq threshold

use raster alpha as optical target

use GT A_gt as raster-alpha target

synthetic metrics

surrogate metrics

hardcoded expected result

default K1 target

case-agnostic target

shared model instance across K cases

shared checkpoint path

shared render root。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stage5_0_R3_R1_canonical_capacity_resume/

New code：

/data/wyh/DeformTransGS/
rtsplat_attribute_study/
stage5_R3_R1/

Create：

stage5_R3_R1/
    protocol/
    camera/
    benchmark/
    carrier/
    policy/
    train/
    render/
    metrics/
    audit/
    run_R3_R1.py

Do not overwrite R3/G1 outputs。

============================================================
一、Resume Protocol Lock
============================================================

Lock SHA256：

R2 verified runtime lock

R2 verified launcher

nvdiffrast native binary

diff_surfel native binary

R3 protocol lock

R3 full-state checkpoint adapter

R3 persistent-state inventory

R3 checkpoint roundtrip evidence

R3-G1 protocol lock

G1_state_gradient_validity.csv

G1_repaired_attribute_control_validity.csv

G1_R3_directional_derivative_protocol_repair.md

VERIFIED_GT_ROOT manifest

clean camera manifest

surface definitions

material definitions。

Verify：

G1 Final CASE =
RTSPLAT-NATIVE-STATE-CONTROL-RESTORED

and repaired J3 =
PASS。

Output：

R3_R1_protocol_lock.json。

Gate O0。

If evidence mismatch：

STOP。

============================================================
二、Runtime Identity Recheck
============================================================

Use exact R2 launcher。

Do not rebuild。

Report：

sys.executable

torch version

torch CUDA

nvdiffrast binary path/SHA

diff_surfel binary path/SHA

RT git HEAD。

Require binary SHA match
the locked R2 values。

Output：

R3_R1_runtime_identity.json。

Gate O1。

============================================================
三、Camera Adapter Construction
============================================================

Create：

stage5_R3_R1/camera/
rt_clean_camera_adapter.py。

Trace exact clean benchmark
camera convention from the verified GT source。

Record：

image width

image height

horizontal/vertical FoV if used

intrinsic matrix

camera origin

camera basis convention

world-to-camera convention

projection convention

pixel-center convention。

Trace exact RT-Splatting camera fields
required by gaussian_renderer。

Do not infer from variable names alone。

Construct RT camera objects
for the exact24 clean benchmark cameras。

Do not change cameras。

============================================================
四、Camera Projection Numeric Closure
============================================================

Use deterministic seed：

20260714。

Generate/select exactly：

10000 canonical surface points

from S0 and S1 combined。

For all24 cameras：

A.

project with the exact clean benchmark
camera/ray equations。

B.

independently project with matrices
prepared by rt_clean_camera_adapter.py。

Compare points with：

positive camera depth

finite projection。

Report：

x pixel abs error

y pixel abs error

camera depth error。

Statistics：

median

p90

p99

max。

Output：

R3_R1_camera_projection_numeric_audit.csv。

O2 PASS iff：

x p99 <=1e-5 pixels

y p99 <=1e-5 pixels

x max <=1e-3 pixels

y max <=1e-3 pixels。

If O2 FAIL：

Final CASE：

RTSPLAT-CAMERA-ADAPTER-INVALID。

STOP。

============================================================
五、Canonical Case Lock
============================================================

Use exactly：

K0：

S0_PLANAR_SHEET

MAT0_NEUTRAL_FIXED_THICKNESS

D0_IDENTITY。

K1：

S0_PLANAR_SHEET

MAT1_NEUTRAL_MASS_CONSERVING

D0_IDENTITY。

K2：

S1_WAVY_MEMBRANE

MAT2_TINTED_MASS_CONSERVING

D0_IDENTITY。

Output：

R3_R1_canonical_case_lock.csv。

For every case/camera/split
resolve exact：

GT RGB path

GT tau path

GT triangle/visibility data
required only for optical valid-pixel mask。

Record SHA256。

Output：

R3_R1_canonical_GT_manifest.csv。

Require：

K0/K1 same-camera RGB SHA equality =
1。

K0/K1 tau SHA equality =
1。

K0/K2 RGB equality <1。

K0/K2 tau equality <1。

Gate O3。

============================================================
六、Train/Test Camera Split Lock
============================================================

Reuse the clean benchmark split exactly。

24 cameras total。

TRAIN：

16 cameras。

TEST：

8 cameras。

Record exact IDs。

Output：

R3_R1_camera_split_lock.json。

TEST cameras must not affect：

training loss

early stopping

optimizer policy

footprint policy

auxiliary module policy

checkpoint selection。

============================================================
七、4096 RT-Native Carrier Material Sampling
============================================================

Use exactly：

4096 Gaussians per case。

Seed：

20260714。

Use deterministic
area-weighted surface sampling。

For every Gaussian store experiment metadata：

Gaussian index

surface ID

canonical triangle ID

barycentric coordinates

canonical u

canonical v

canonical xyz。

Output：

R3_R1_material_identity.csv
separately per K case。

Material identity is known from benchmark
construction。

Do NOT infer material identity from：

normal

depth

nearest Gaussian

kNN

TSGS checkpoint。

============================================================
八、Native Gaussian Geometry Initialization
============================================================

Initialize：

_xyz

at exact sampled surface positions。

Gaussian count：

4096。

No densification。

No pruning。

No creation/deletion。

_xyz is frozen throughout canonical training。

Record：

xyz SHA

xyz min/max/mean/std

nearest material-sample spacing statistics。

Initialize native scale/rotation
using a deterministic surface-aware
initialization compatible with the native
RT representation。

For every sampled point：

derive local material tangents
from the benchmark surface parameterization。

Use tangents only for geometry-footprint
initialization。

Do not derive optical values
from GT。

Record exact initialization formula。

Output：

R3_R1_geometry_initialization.md
R3_R1_native_carrier_initialization.csv。

============================================================
九、Optical Initialization Leakage Gate
============================================================

The same neutral source-domain initialization
must be used for K0/K1/K2
for optical/material/appearance states。

Do NOT initialize from：

GT RGB

GT tau

sigma_rgb

h0

Js

A_gt

per-camera color。

For SH appearance：

initialize all cases from neutral：

RGB [0.5,0.5,0.5]

through the RT source's actual
RGB-to-SH initialization convention。

For：

_occupancy

_opacity

_transmissivity

_roughness

_reflectance

_language_feature

use the same deterministic
source-supported neutral/default
raw initialization for all three cases。

Record raw and activated statistics
for every persistent state。

Output：

R3_R1_initialization_leakage_audit.md
R3_R1_initial_state_statistics.csv。

O4 PASS iff：

no GT optical values are read
during model initialization

and K0/K1/K2 optical/material initialization
is semantically identical。

============================================================
十、Canonical Candidate State Lock
============================================================

Use repaired ATTRIBUTE-CONTROL-VALID states：

_occupancy

_opacity

_transmissivity

_features_dc。

Do not include：

_roughness

_reflectance

_language_feature

_features_rest

because they did not pass
the current control-validity Gate。

The base per-Gaussian canonical candidate list is：

_occupancy

_opacity

_transmissivity

_features_dc。

This is canonical representation fitting。

It is NOT future attribute necessity ranking。

Output：

R3_R1_base_trainable_state_lock.json。

============================================================
十一、Native Footprint Diagnostic
============================================================

Use K0 TRAIN cameras only。

Construct two models from bitwise-equivalent
non-geometry initialization。

P0_FIXED_FOOTPRINT：

_xyz fixed

_scaling fixed

_rotation fixed。

Trainable：

_occupancy

_opacity

_transmissivity

_features_dc。

P1_NATIVE_FOOTPRINT：

_xyz fixed

_scaling trainable

_rotation trainable

plus：

_occupancy

_opacity

_transmissivity

_features_dc。

Use500 optimizer steps。

Same camera order。

Same seed。

Same loss。

Same initialization。

Use source-defined parameter-group LRs
for corresponding tensors。

No TEST cameras。

Report at step0 and step500：

TRAIN PSNR

median TRAIN tau_eq Elog

RGB L1

tau_eq loss

DSSIM

total loss。

Report scale/rotation parameter delta L2。

Output：

R3_R1_footprint_diagnostic.csv。

Select：

NATIVE_FOOTPRINT

iff P1 improves TRAIN PSNR by >=3.0 dB

OR

reduces median TRAIN tau_eq Elog by >=30%
relative to P0。

Otherwise：

FIXED_FOOTPRINT。

Output：

R3_R1_footprint_policy_lock.json。

Gate O5。

Do not override the rule manually。

============================================================
十二、Auxiliary Module Requirement Audit
============================================================

Native auxiliary modules identified：

dir_encoding

light_mlp。

Trace whether the current native
transparent rendering branch
consumes them for the canonical renderer。

For every module report：

consumed yes/no

render component affected

parameter count

initialized yes/no。

Output：

R3_R1_auxiliary_render_dependency.csv。

If a module is not consumed
in the active canonical branch：

freeze it。

============================================================
十三、Auxiliary Module TRAIN Diagnostic
============================================================

For auxiliary modules actually consumed
by the active renderer：

use K0 TRAIN only。

Run two500-step variants
from identical initialization：

A0_AUX_FROZEN

A1_AUX_NATIVE_TRAINABLE。

The per-Gaussian state policy follows
the already frozen footprint policy。

Use same camera order/seed/loss。

No TEST cameras。

Select AUX_NATIVE_TRAINABLE iff：

A1 improves final TRAIN PSNR >=3 dB

OR

reduces final TRAIN tau_eq Elog >=30%
relative to A0。

Otherwise AUX_FROZEN。

Output：

R3_R1_auxiliary_policy_diagnostic.csv
R3_R1_auxiliary_policy_lock.json。

Auxiliary module weights are never
Stage5.1 candidate attributes。

Gate O6。

============================================================
十四、Final Canonical Trainable State Lock
============================================================

Per-Gaussian trainable states always include：

_occupancy

_opacity

_transmissivity

_features_dc。

Add：

_scaling
_rotation

only if footprint policy =
NATIVE_FOOTPRINT。

Never train：

_xyz。

Never train currently control-invalid states：

_roughness

_reflectance

_language_feature

_features_rest。

Auxiliary module policy follows O6。

Output：

R3_R1_canonical_trainable_state_lock.json。

Print exact source tensor names。

============================================================
十五、Canonical Loss Lock
============================================================

Use exactly：

L =

1.0 * RGB_L1

+
0.5 * TAU_EQ_RGB_LOG_L1

+
0.1 * DSSIM。

Definition：

tau_eq_pred_rgb =
-log(clamp(I_pred_rgb,1e-6,1))。

tau_eq_gt_rgb =
-log(clamp(I_gt_rgb,1e-6,1))。

TAU_EQ_RGB_LOG_L1：

mean absolute log-ratio error
over the frozen valid optical GT pixels。

Use the same corrected Stage4
valid-pixel semantics。

Do NOT use：

raster alpha

A_gt

occupancy supervision

opacity supervision

transmissivity supervision

normal GT

depth GT

geometry mask loss

Js

KIOT。

Output：

R3_R1_loss_lock.md。

============================================================
十六、Optimizer Lock
============================================================

Trace RT-Splatting's source-defined
training parameter groups and learning rates。

For every selected trainable tensor
use the corresponding source-defined LR
where one exists。

If native optimizer construction
forces xyz or other excluded states：

construct torch.optim.Adam manually
over only the locked selected tensors。

Use the source LR for each state family。

Record：

parameter group name

source tensor names

LR

betas

eps

weight_decay。

Do not perform LR search。

Output：

R3_R1_optimizer_lock.json。

============================================================
十七、Canonical Job Isolation Audit
============================================================

Before execution assert：

K0/K1/K2 have distinct：

model instances

case keys

surface arguments

material arguments

GT manifest arguments

checkpoint roots

history roots

render roots。

Output directories：

/data/wyh/DeformTransGS/experiments/
stage5_0_R3_R1_canonical_capacity_resume/
canonical/K0

canonical/K1

canonical/K2。

Create：

R3_R1_canonical_job_manifest.csv。

Fields：

case

surface

material

deformation

GT manifest

GPU

output root

checkpoint path

history path

render root

seed。

Use deterministic GPU assignment：

K0 -> physical GPU2

K1 -> physical GPU3

K2 -> first free of GPU2/GPU3
after K0/K1 completion。

Do not use more than2 concurrent jobs。

Gate O7。

============================================================
十八、REAL Canonical Optimization
============================================================

Train exactly：

K0

K1

K2。

Use exact R2 verified launcher。

Use:

4096 Gaussians

fixed Gaussian count

frozen xyz

locked footprint policy

locked auxiliary policy

locked trainable state list

locked optimizer

16 TRAIN cameras

8 TEST cameras excluded from training。

Maximum optimizer steps：

4000。

Patience：

500。

Seed：

20260714。

Early stopping may use only TRAIN loss。

The selected checkpoint must use：

best TRAIN total loss。

Do not select based on TEST。

============================================================
十九、Every-Step Training History
============================================================

Record EVERY optimizer step。

Each history must contain exactly
one row per recorded training state
including step0 and every optimizer step。

Fields：

case

iteration

camera ID or camera batch identity

total loss

RGB L1

tau_eq loss

DSSIM

learning rate per parameter group

gradient L2:

_occupancy

_opacity

_transmissivity

_features_dc

_scaling if trainable

_rotation if trainable

auxiliary modules if trainable。

Parameter delta L2 from initialization:

same trainable state families。

Save：

R3_R1_canonical_history/
K0.csv

K1.csv

K2.csv。

Do not store only25 summary rows。

============================================================
二十、First Optimizer Step Causality
============================================================

For each K case：

save all persistent tensors
immediately before first optimizer.step。

After first optimizer.step：

require at least one of：

_occupancy

_opacity

_transmissivity

_features_dc

changes L2 >1e-10。

For every selected trainable state
report its change。

Frozen _xyz：

max abs change =
0 exactly。

Every excluded persistent state：

_roughness

_reflectance

_language_feature

_features_rest

and fixed footprint states when applicable：

max abs change =
0 exactly。

Output：

R3_R1_first_step_parameter_audit.csv。

Gate O8a。

============================================================
二十一、Full-State Checkpoints
============================================================

Use the verified R3 project-side
full-state checkpoint adapter。

Save：

step500

step1000

step1500

step2000

step2500

step3000

step3500

step4000 if reached

best

final。

Each checkpoint contains：

11 persistent per-Gaussian tensors

2 auxiliary module states

optimizer state

case key

iteration

Gaussian count

RT commit

source lock SHA

runtime lock SHA

GT lock SHA

camera lock SHA。

After selected checkpoint is chosen：

destroy model。

Fresh reconstruct native model。

Reload checkpoint。

Require：

persistent tensor max error =
0

auxiliary module tensor max error =
0。

Output：

R3_R1_checkpoint_integrity.csv。

Gate O8b。

============================================================
二十二、Fresh TRAIN/TEST Render
============================================================

For every case：

launch a fresh process。

Create a fresh model。

Load selected full-state checkpoint。

Render：

all16 TRAIN cameras

all8 TEST cameras。

Save RGB float32 NPY before metrics。

Directory：

R3_R1_canonical_renders/
K0/TRAIN
K0/TEST

K1/TRAIN
K1/TEST

K2/TRAIN
K2/TEST。

Create：

R3_R1_render_manifest.csv。

Fields：

case

split

camera ID

RGB path

dtype

shape

SHA256

checkpoint SHA

timestamp。

Expected RGB count：

3 *24 =
72。

Gate O8c。

============================================================
二十三、Prediction Case-Key Audit
============================================================

For every saved render verify：

prediction case

checkpoint case key

surface key

GT case key。

Require：

K0 prediction -> K0 checkpoint -> K0 GT

K1 -> K1 -> K1

K2 -> K2 -> K2。

Output：

R3_R1_render_case_key_audit.csv。

No default GT fallback。

No K1 hardcoding。

Gate O8d。

============================================================
二十四、Independent Canonical Evaluator
============================================================

Create separate entry point：

stage5_R3_R1/metrics/
evaluate_canonical_independent.py。

It may import only
generic NumPy/PyTorch metric utilities。

It accepts only：

VERIFIED_GT_ROOT

R3_R1 canonical case lock

camera split lock

render manifest

saved RGB NPY arrays。

It must NOT import：

RT Gaussian model

RT renderer

optimizer

training loop

state policy

footprint policy

auxiliary policy

future release code

old Stage4 evaluator。

Compute per image：

PSNR

SSIM

TAU_EQ_RGB_ELOG median

p90

p95

p99

factor2 fraction。

Aggregate independently：

TRAIN

TEST。

Output：

R3_R1_metrics_per_camera.csv
R3_R1_metrics_summary.csv。

Expected per-camera rows：

72。

Gate O9a。

============================================================
二十五、Independent Metric Reproduction
============================================================

Create second script：

audit_metric_reproduction.py。

Select deterministic6 TEST rows
using seed20260714。

Load only：

prediction RGB NPY

GT RGB array。

Recompute：

PSNR

median tau_eq Elog。

Require：

PSNR max difference <=1e-10

median tau_eq Elog
max difference <=1e-12。

Output：

R3_R1_metric_reproduction.csv。

Gate O9b。

============================================================
二十六、Canonical Capacity Diagnostic
============================================================

For K0/K1/K2 report：

optimizer steps executed

selected checkpoint iteration

initial TRAIN PSNR

selected TRAIN PSNR

best TRAIN PSNR

selected TRAIN median tau_eq Elog

best TRAIN median tau_eq Elog

initial total loss

selected total loss

last500 total-loss relative reduction

TEST PSNR

TEST median tau_eq Elog

per-camera TEST PSNR min/median/max。

For activated bounded states report：

occupancy <=1e-4 fraction

occupancy >=1-1e-4 fraction

opacity <=1e-4 fraction

opacity >=1-1e-4 fraction

transmissivity <=1e-4 fraction

transmissivity >=1-1e-4 fraction。

If scale/rotation trainable：

report scale distribution
and rotation delta statistics。

For SH appearance：

report rendered or pre-render color
clamp/saturation statistics
if source path applies clamp。

Output：

R3_R1_capacity_diagnostic.csv。

============================================================
二十七、Capacity Failure Classification
============================================================

For each K case classify exactly one：

PASS

OPTIMIZATION-NOT-CONVERGED

TRAIN-FIT-INSUFFICIENT

GENERALIZATION-GAP

STATE-SATURATION-LIMIT

FOOTPRINT-LIMIT

AUXILIARY-MODULE-LIMIT

RENDERER-NATIVE-LIMIT

OTHER-EXACT。

Rules：

PASS iff J4 numerical thresholds pass。

TRAIN-FIT-INSUFFICIENT iff：

TRAIN itself fails：

PSNR >=28

AND
median tau_eq Elog <=0.25

after real provenance-valid optimization。

GENERALIZATION-GAP iff：

TRAIN passes both thresholds

but TEST fails either threshold。

OPTIMIZATION-NOT-CONVERGED iff：

history shows a material
continuing improvement trend
in the final500 steps
and selected checkpoint has not stabilized。

STATE-SATURATION-LIMIT iff：

a required active state is overwhelmingly
at a bounded activation extreme
and source-level state response evidence
supports that interpretation。

Do not guess classification。

Output exact evidence。

============================================================
二十八、Frozen J4 Canonical Capacity Gate
============================================================

Per case PASS iff：

TEST PSNR >=28.0 dB

AND

median TEST TAU_EQ_RGB_ELOG <=0.25。

Require all：

K0 PASS

K1 PASS

K2 PASS。

Formal J4 PASS iff：

O0 PASS

O1 PASS

O2 PASS

O3 PASS

O4 PASS

O5 PASS

O6 PASS

O7 PASS

O8a/O8b/O8c/O8d PASS

O9a/O9b PASS

and K0/K1/K2 numerical Gate PASS。

No threshold changes。

============================================================
二十九、Stage4 Baseline Comparison
============================================================

Compare Stage5 RT against valid Stage4：

Stage4 K0：

21.231329
0.178887

Stage4 K1：

21.231336
0.178926

Stage4 K2：

17.881014
0.215660。

Report Stage5 minus Stage4：

PSNR delta

tau_eq Elog absolute delta

tau_eq relative error reduction。

Output：

R3_R1_RT_vs_TSGS_comparison.csv。

Diagnostic only。

Improvement over Stage4
does not replace the absolute J4 Gate。

============================================================
三十、Fitted-State Identifiability
============================================================

Execute ONLY if J4 PASS。

Use the fitted selected checkpoints。

Test repaired control-valid families：

_occupancy

_opacity

_transmissivity

_features_dc。

For every K case/state：

reload identical selected checkpoint。

Freeze all other states。

Perturb deterministic10% Gaussian IDs：

id mod10 ==0。

Use raw perturbation +0.01
with the same family-specific semantics
as the pre-fit state audit。

Render all8 TEST cameras。

Restore exactly。

Report：

mean RGB response

max response

responsive camera count

restore max difference。

Then use >=4 TRAIN cameras
for real RGB-loss gradients。

Use fixed target：

clamp(
baseline.detach()*0.8 +0.05,
0,
1
)。

Compute:

finite gradient fraction

nonzero fraction

gradient L2。

Output：

R3_R1_fitted_state_identifiability.csv。

============================================================
三十一、Fitted-State Collinearity
============================================================

Execute only if J4 PASS。

For every K case：

flatten each fitted state perturbation
RGB response over8 TEST cameras。

Normalize to unit L2。

Compute pairwise cosine similarity。

Output：

R3_R1_fitted_state_collinearity.csv。

Classify：

LOCALLY-COLLINEAR
iff abs(cosine)>=0.9999。

Do not merge states。

This is a Stage5.1 warning only。

============================================================
三十二、Stage5.1 Candidate Lock
============================================================

Execute ONLY if J4 PASS。

A candidate state must satisfy：

source-persistent per-Gaussian

source semantic established

full-state checkpoint exact

pre-fit repaired ATTRIBUTE-CONTROL-VALID

fitted model render-active

fitted model gradient-active。

Potential validated source states are currently：

GEOMETRIC_OCCUPANCY
->_occupancy

OPTICAL_OPACITY
->_opacity

TRANSMISSIVITY
->_transmissivity

SH_APPEARANCE
->_features_dc。

Do not automatically include all four。

Use actual fitted evidence。

For each accepted state save：

scientific family

Chinese explanation

source tensor

activation

render dependency

raw parameter shape

bounded yes/no

checkpoint key

pre-fit gradient validity evidence

fitted response norm by K case

fitted gradient L2 by K case

collinearity warnings。

Output：

stage5_1_attribute_candidate_lock.json。

Do NOT create release groups。

Do NOT run deformations。

Do NOT rank necessity。

============================================================
三十三、Final CASE
============================================================

CASE RTSPLAT-NATIVE-CARRIER-READY：

J4 PASS。

Conclusion：

The fixed4096-Gaussian
RT-native transparent carrier
establishes canonical capacity
on K0/K1/K2。

At least two independently controllable
native non-geometry states have already
passed repaired state control。

The scientific question is：

UNTESTED-BUT-EXPERIMENTALLY-ADDRESSABLE。

ALLOW：

Stage5.1 dynamic native-state
sufficiency protocol design。

PRIMARY ATTRIBUTE-DEFORMATION LINE：

CONTINUE。

------------------------------------------------------------

CASE RTSPLAT-CAMERA-ADAPTER-INVALID：

O2 FAIL。

STOP benchmark integration。

------------------------------------------------------------

CASE RTSPLAT-CANONICAL-PROVENANCE-FAIL：

case reuse

GT key reuse

shared checkpoint

first-step causality failure

frozen tensor mutation

checkpoint reload failure

fresh render count failure

case-key mismatch

independent evaluator failure

metric reproduction failure。

STOP。

------------------------------------------------------------

CASE RTSPLAT-CANONICAL-CARRIER-INSUFFICIENT：

repaired J3 PASS

camera/provenance/metric Gates PASS

real K0/K1/K2 optimization executes

but one or more cases fail：

TEST PSNR >=28

AND
median TEST tau_eq Elog <=0.25。

Conclusion：

the fixed4096 RT-native carrier
does not establish canonical capacity
under the frozen benchmark。

PRIMARY ATTRIBUTE-DEFORMATION LINE：

STOP。

Do NOT search a third carrier。

Return to RecycleGS。

============================================================
三十四、Required Outputs
============================================================

R3_R1_protocol_lock.json
R3_R1_runtime_identity.json

rt_clean_camera_adapter.py
R3_R1_camera_projection_numeric_audit.csv

R3_R1_canonical_case_lock.csv
R3_R1_canonical_GT_manifest.csv
R3_R1_camera_split_lock.json

R3_R1_material_identity.csv

R3_R1_geometry_initialization.md
R3_R1_native_carrier_initialization.csv

R3_R1_initialization_leakage_audit.md
R3_R1_initial_state_statistics.csv

R3_R1_base_trainable_state_lock.json

R3_R1_footprint_diagnostic.csv
R3_R1_footprint_policy_lock.json

R3_R1_auxiliary_render_dependency.csv
R3_R1_auxiliary_policy_diagnostic.csv
R3_R1_auxiliary_policy_lock.json

R3_R1_canonical_trainable_state_lock.json
R3_R1_loss_lock.md
R3_R1_optimizer_lock.json

R3_R1_canonical_job_manifest.csv

R3_R1_canonical_history/K0.csv
R3_R1_canonical_history/K1.csv
R3_R1_canonical_history/K2.csv

R3_R1_first_step_parameter_audit.csv
R3_R1_checkpoint_integrity.csv

R3_R1_canonical_renders/**/*.npy
R3_R1_render_manifest.csv
R3_R1_render_case_key_audit.csv

R3_R1_metrics_per_camera.csv
R3_R1_metrics_summary.csv
R3_R1_metric_reproduction.csv

R3_R1_capacity_diagnostic.csv
R3_R1_RT_vs_TSGS_comparison.csv

if J4 PASS：

R3_R1_fitted_state_identifiability.csv
R3_R1_fitted_state_collinearity.csv
stage5_1_attribute_candidate_lock.json

stage5_0_R3_R1_canonical_capacity_report.md
stage5_0_R3_R1_canonical_capacity_summary.md
stage5_0_R3_R1_canonical_capacity_log.txt

============================================================
三十五、最终终端只输出
============================================================

A. O0

B. repaired J3 evidence locked yes/no

C. R2 launcher path

D. runtime native binary SHA match yes/no

E. O1

F. camera projection x/y p99 error

G. camera projection x/y max error

H. O2

I. K0/K1 GT RGB/tau identical yes/no

J. K0/K2 GT RGB/tau different yes/no

K. O3

L. TRAIN camera IDs

M. TEST camera IDs

N. Gaussian count K0/K1/K2

O. material identity row count K0/K1/K2

P. xyz fixed yes/no

Q. initialization reads GT RGB/tau/sigma/h0/Js yes/no

R. K0/K1/K2 optical initialization identical yes/no

S. O4

T. base candidate trainable state names

U. P0 final TRAIN PSNR/tau_eq

V. P1 final TRAIN PSNR/tau_eq

W. footprint policy

X. O5

Y. auxiliary modules consumed by active branch

Z. AUX frozen final TRAIN PSNR/tau_eq

AA. AUX trainable final TRAIN PSNR/tau_eq

AB. auxiliary policy

AC. O6

AD. final canonical per-Gaussian trainable state names

AE. auxiliary trainable module names

AF. exact optimizer groups/LRs

AG. K0/K1/K2 distinct model instances yes/no

AH. shared checkpoint/render path yes/no

AI. O7

AJ. K0 optimizer steps

AK. K1 optimizer steps

AL. K2 optimizer steps

AM. K0 history rows

AN. K1 history rows

AO. K2 history rows

AP. K0 initial/selected TRAIN loss

AQ. K1 initial/selected TRAIN loss

AR. K2 initial/selected TRAIN loss

AS. first-step eligible state changed K0/K1/K2 yes/no

AT. frozen xyz max change

AU. excluded persistent state max change

AV. O8a

AW. persistent tensor reload max error

AX. auxiliary module reload max error

AY. O8b

AZ. expected RGB array count

BA. actual RGB array count

BB. O8c

BC. render case-key mismatch count

BD. O8d

BE. independent metric row count

BF. metric reproduction max PSNR error

BG. metric reproduction max tau_eq error

BH. O9a

BI. O9b

BJ. K0 TRAIN PSNR/tau_eq Elog

BK. K0 TEST PSNR/tau_eq Elog

BL. K1 TRAIN PSNR/tau_eq Elog

BM. K1 TEST PSNR/tau_eq Elog

BN. K2 TRAIN PSNR/tau_eq Elog

BO. K2 TEST PSNR/tau_eq Elog

BP. K0 capacity classification

BQ. K1 capacity classification

BR. K2 capacity classification

BS. J4

BT. Stage5 minus Stage4 K0 PSNR delta

BU. Stage5 minus Stage4 K1 PSNR delta

BV. Stage5 minus Stage4 K2 PSNR delta

BW. fitted state render-active count if J4 PASS

BX. fitted state gradient-active count if J4 PASS

BY. Stage5.1 candidate count if J4 PASS

BZ. Stage5.1 candidate names if J4 PASS

CA. Final CASE

CB. RT-native canonical carrier ready yes/no

CC. scientific question experimentally addressable yes/no

CD. allow Stage5.1 dynamic sufficiency design yes/no

CE. AttributeDeformGS hypothesis status

CF. PRIMARY ATTRIBUTE-DEFORMATION LINE STOP/CONTINUE

CG. KIOT status

CH. next exact research action

CI. report path

CJ. summary path

============================================================
三十六、README
============================================================

Update README。

Record：

Stage5.0-R3-G1 proved that the original
full-tensor random-unit directional
derivative Gate was numerically
under-resolved for low-sensitivity
native RT states。

The actual reproduced opacity state
had64 parameters and gradient L2
approximately4.893e-5。

The transmissivity state had64 parameters
and gradient L2 approximately5.205e-5。

The original float32 finite-difference
loss numerators at eps1e-2 were only
approximately8.2 and8.7 scalar-loss ULPs
for opacity and transmissivity。

The previous high-dimensional
4096-parameter heuristic estimate
is not used as evidence for the
reproduced smoke case。

The under-resolution conclusion
is based on the measured ULP ratios
and the repaired finite-difference tests。

Using fixed structured directions，
float64 loss reduction after the
native float32 renderer，
and stable adjacent-epsilon plateaus：

opacity autograd and finite difference
agreed to relative error approximately6.07e-6。

transmissivity agreed to approximately9.80e-6。

Coordinate checks passed16/16
for both states。

The repaired autograd-valid states are：

_occupancy

_opacity

_transmissivity

_features_dc。

The repaired non-geometry valid states are：

_opacity

_transmissivity

_features_dc。

Repaired J3 PASS。

Therefore the scientific question
is experimentally addressable
subject to canonical capacity。

Stage5.0-R3-R1 resumes only from
camera numeric closure through
the frozen J4 canonical capacity Gate。

The RT-native carrier uses exactly4096
Gaussians per canonical case，
fixed xyz，
no densification，
no pruning，
no GT optical initialization leakage。

The capacity Gate remains unchanged：

TEST PSNR >=28 dB

AND

median TEST image-equivalent
optical-depth Elog <=0.25

for K0/K1/K2。

Only if all three pass
may Stage5.1 dynamic native-state
sufficiency protocol be designed。

============================================================
三十七、实际执行
============================================================

必须实际：

lock repaired J3 evidence

verify R2 runtime identity

construct exact clean-camera RT adapter

numerically project10000 points
through both camera conventions

close all24 cameras

lock exact K0/K1/K2 GT identities

lock exact TRAIN/TEST IDs

sample exactly4096 material points per case

save explicit material identity

initialize xyz at exact surfaces

freeze xyz

prove no optical GT initialization leakage

use repaired control-valid base states：

_occupancy
_opacity
_transmissivity
_features_dc

run K0 TRAIN-only footprint comparison

freeze footprint policy

audit active auxiliary modules

run K0 TRAIN-only auxiliary policy comparison
only for consumed modules

freeze auxiliary policy

lock exact canonical trainable tensors

lock exact optimizer groups/LRs

prove K0/K1/K2 job isolation

train real K0/K1/K2

record every optimizer step

audit first optimizer step

prove frozen xyz unchanged

prove excluded states unchanged

save full-state checkpoints

destroy/reconstruct/reload model

prove exact tensor reload

freshly render all72 TRAIN/TEST RGB arrays

save NPY before metrics

audit render/checkpoint/GT case keys

run independent evaluator

run second metric reproduction script

classify TRAIN/TEST capacity

apply unchanged28 dB /0.25 Gate

if J4 PASS：

audit fitted-state response and gradients

freeze Stage5.1 candidate list

do NOT run attribute release

Final CASE。

绝对不要：

rerun G1

reinterpret perturbation amplitude
as state importance

change J4 threshold

run dynamic deformation before J4

create release groups before J4

claim any state is necessary。
PROMPT
```

## 我的客观评价

目前是这条线从开始到现在**最接近真正科学实验的一次**。

因为现在已经不再是：

> “我觉得 opacity 可能重要。”

而是有三个来源清楚的 RT-Splatting 原生非几何状态：

[
\boxed{
_opacity,\quad
_transmissivity,\quad
_features_dc
}
]

全部满足真实前向、真实梯度和数值一致性验证。

接下来 **J4 不允许再救**。

RT-native carrier 若 K0/K1/K2 全部过：

[
PSNR\ge28,\qquad
E_{\tau eq}\le0.25
]

我们就正式进入真正的论文核心实验：

> **固定精确几何传输以后，逐项冻结/释放 occupancy、optical opacity、transmissivity、appearance，确定半透明形变所需的最小动态状态集合。**

J4 若 provenance 全闭环后仍失败，**这次真的停**。
