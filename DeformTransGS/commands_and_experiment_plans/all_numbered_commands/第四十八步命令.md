这次我必须把结论**大幅收紧**。

> **Stage 3.5B-R1 不只是发现“官方 mask 不一致”。你最后贴出的 `Actual local-frame deformation definitions` 暴露了一个更严重的问题：Stage 3.5B 的核心 patch bridge 数值目前不能作为真实渲染证据。**

最关键的一句话是：

> `Stage3.5B did not persist explicit F matrices. It used state names and target q values in STATES plus synthetic policy response generation.`

翻成直白中文：

> **3.5B 没有保存并使用真实形变矩阵 (F) 去传输 xyz/covariance、实际渲染后测响应；它根据状态名和预设 q 合成了 policy response。**

那现在的：

[
0.306120\rightarrow0.023685
]

以及：

[
92.3%
]

**不能再叫“KIOT 在真实 TSGS 重建 patch 上恢复了真实渲染响应”。**

它至多说明：

> 在 TSGS checkpoint 属性分布和选定 patch 上，按一个合成响应模型生成的数据仍呈现 KIOT 理论排序。

这和真实 reconstruction bridge（重建载体桥接）差一整个实际 rasterizer 闭环。

---

# 一、而且当前 (J_s) 定义存在明确数学错误

这是比 mask 更致命的地方。

报告明确说局部基底顺序：

[
[n,t_1,t_2]
]

其中：

* (n)：法向；
* (t_1,t_2)：切向。

表面面积伸长应严格计算：

[
\boxed{
J_s=
|\det F|
\left|F^{-T}n\right|
}
]

在局部基底中：

[
n=
\begin{bmatrix}
1\0\0
\end{bmatrix}
]

---

## D1 `normal_stretch_1p35`

你实际锁定：

[
F=
\begin{bmatrix}
1.35&0&0\
0&1&0\
0&0&1
\end{bmatrix}
]

这是**法向伸长**。

它只拉长 (n) 方向。

表面的两个切向方向：

[
t_1,t_2
]

完全不变。

所以表面积：

[
J_s=1
]

不是：

[
1.35
]

因此：

[
q=1
]

不是：

[
0.7407407
]

我直接按公式算：

[
\det F=1.35
]

[
F^{-T}n=
\begin{bmatrix}
1/1.35\0\0
\end{bmatrix}
]

所以：

[
J_s
===

# 1.35\times\frac1{1.35}

\boxed{1}
]

---

## D2 `normal_compress_0p75`

[
F=
\operatorname{diag}(0.75,1,1)
]

同理：

[
\boxed{J_s=1}
]

而不是：

[
0.75
]

所以：

[
q=1
]

不是：

[
1.333
]

也就是说，你之前所谓：

> compression diagnostic

实际上**根本不是 surface-area compression（表面积压缩）**。

它只是法向缩放。

对于我们当前：

> 面积改变 → 单位面积材料量改变 → 光学状态传输

这个模型来说，它根本不应该作为 (J_s<1) compression 实验。

---

# 二、D4 的名字和矩阵也是直接矛盾的

名字：

> `D4_tangent_stretch_1p55`

但矩阵：

[
F=
\begin{bmatrix}
1.55&0&0\
0&1&0\
0&0&1
\end{bmatrix}
]

而基底顺序是：

[
[n,t_1,t_2]
]

这还是：

> **normal stretch**

不是 tangent stretch。

真正沿 (t_1) 拉伸 1.55 应该是：

[
\boxed{
F_{\rm local}
=============

\begin{bmatrix}
1&0&0\
0&1.55&0\
0&0&1
\end{bmatrix}
}
]

此时：

[
J_s=1.55
]

[
q=\frac1{1.55}=0.645161
]

你现在的 q：

[
0.645161
]

其实对应的是**状态名字想表达的形变**。

但真正矩阵没有实现它。

换句话说：

> **q 是根据 state name 手工指定的，F 却是另一种形变。**

这已经直接证明：

[
\boxed{
F\rightarrow J_s\rightarrow q
}
]

这条物理链在 Stage 3.5B 没真正执行。

---

# 三、D5 更明显：它根本不是 oblique stretch

名字：

> `D5_oblique_stretch_1p80`

实际：

[
F=
\operatorname{diag}(1.8,1,1)
]

这依然只是法向伸长。

没有任何 oblique（斜向）成分。

真正的切平面斜向拉伸，例如沿：

[
d=
\cos\theta,t_1+
\sin\theta,t_2
]

拉伸 (s)，应构造：

[
\boxed{
F_{\rm local}
=============

I+(s-1)dd^T
}
]

其中在 ([n,t_1,t_2]) 坐标：

[
d=
\begin{bmatrix}
0\
\cos\theta\
\sin\theta
\end{bmatrix}
]

例如：

[
\theta=45^\circ
]

[
s=1.8
]

这才叫 oblique tangent stretch（切平面斜向拉伸）。

---

# 四、D3 的 (q=0.812) 同样不是从实际 (F) 得到的

实际矩阵：

[
F=
\begin{bmatrix}
1.231527&0.3&0\
0&1&0\
0&0&1
\end{bmatrix}
]

代入：

[
J_s=
|\det F|
|F^{-T}n|
]

实际：

[
J_s\approx1.04403
]

所以：

[
\boxed{
q\approx0.95783
}
]

不是：

[
0.812
]

那个：

[
0.812
]

明显更接近：

[
\frac1{1.231527}
]

也就是说，又像是：

> 根据人为目标 q 反推一个数字塞进矩阵。

而不是：

> 构造 (F) → 严格计算 (J_s) → 得到 (q=1/J_s)。

---

# 五、所以当前真正的问题是什么？

我们前面整条科学链最重要的原则一直是：

[
\boxed{
F
\rightarrow
J_s
\rightarrow
q=\frac1{J_s}
\rightarrow
\text{KIOT}
}
]

但 Stage 3.5B 实际变成了：

[
\boxed{
\text{state name}
\rightarrow
\text{手工 q}
\rightarrow
\text{synthetic response}
}
]

然后又写了一个“implied F”。

这不能接受。

所以我现在正式撤销：

> `C2 PRIMARY KIOT BRIDGE SUPPORTED`

以及：

> `92.1% reconstructed bridge improvement`

作为**真实 TSGS rasterized bridge evidence（真实 TSGS 光栅化桥接证据）**。

注意我说的是：

> 撤销“真实载体实验证据”的身份。

**不是撤销 KIOT。**

Stage 3.5A 和 3.5A-R1 仍然完整成立：

[
0.279456\rightarrow0.027946
]

controlled carrier 上 KIOT 的解析证据完全没问题。

现在只是发现：

> **真实 TSGS bridge 的执行脚本偷懒做成了 synthetic surrogate（合成替代实验）。**

老实说，这个 bug 幸亏现在发现。

要是拿着“92.3% real TSGS improvement”开始写论文，审稿人只要追问 (F) 和 (J_s) 怎么算，场面会非常尴尬。

---

# 六、现在反而不用再审计了，直接真正重跑 Stage 3.5B

这次必须：

> **官方 `transparent_masks` 重新选 patch。**

以及：

> **实际保存 (F)。**

然后严格：

[
xyz'=F(x-p)+p
]

[
\Sigma'=F\Sigma F^T
]

[
J_s=|\det F||F^{-T}n|
]

[
q=1/J_s
]

[
o'_{\rm KIOT}
=============

\Phi^{-1}(q\Phi(o))
]

实际 TSGS rasterizer fresh render。

**禁止任何 synthetic response generation。**

我建议直接叫：

# Stage 3.5B-R2：Official-Mask Real-Render Reconstruction Bridge

中文：

> **官方透明掩码下的真实渲染重建载体桥接实验**

这才是真正的生死门。

```bash
cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
紧急科学状态修正
============================================================

Stage3.5B-R1 uncovered a FATAL protocol issue
in the previous Stage3.5B reconstructed bridge。

The previous Stage3.5B numerical bridge results：

R0 mean central error =0.306120

R4 KIOT mean central error =0.023685

reported improvement =92.26%

MUST NOT be used as real reconstructed-carrier
rasterized bridge evidence。

Reason：

the actual Stage3.5B deformation report states：

"Stage3.5B did not persist explicit F matrices.
It used state names and target q values in STATES
plus synthetic policy response generation."

Therefore the previous patch policy responses
were not established by a strict chain：

F
→ xyz deformation
→ covariance transport
→ Js from F and surface normal
→ q=1/Js
→ optical policy
→ fresh TSGS rasterizer render
→ material-proxy response metric。

They used synthetic policy response generation。

The previous 92.3% bridge improvement
is therefore RETIRED
as real-render reconstructed-carrier evidence。

It may remain only：

SYNTHETIC BRIDGE EXPLORATORY EVIDENCE。

============================================================
FATAL Js / DEFORMATION SEMANTIC ERROR
============================================================

Stage3.5B-R1 locked local basis order：

[n, t1, t2]

where：

n = patch surface normal

t1,t2 = tangent axes。

Surface area stretch must be：

Js =

abs(det(F))
*
norm(
    inverse(F).T @ n
)。

In local coordinates：

n_local = [1,0,0]^T。

------------------------------------------------------------

Previous D1：

D1_normal_stretch_1p35

F_local =
diag(1.35,1,1)

Previous q =
0.740740741。

This is WRONG for surface area stretch。

Because：

det(F)=1.35

norm(F^-T n)=1/1.35

therefore：

Js=1

q=1。

Normal stretch does NOT change
the tangent surface area。

------------------------------------------------------------

Previous D2：

diag(0.75,1,1)

also：

Js=1

q=1。

It is NOT surface area compression。

------------------------------------------------------------

Previous D4：

named：

tangent_stretch_1p55

but used：

diag(1.55,1,1)

under basis：

[n,t1,t2]。

This is NORMAL stretch，
not tangent stretch。

Correct t1 stretch is：

diag(1,1.55,1)。

------------------------------------------------------------

Previous D5：

named：

oblique_stretch_1p80

but used：

diag(1.8,1,1)。

This is NOT oblique。

------------------------------------------------------------

Previous D3：

F_local =

[[1.231527,0.3,0],
 [0,1,0],
 [0,0,1]]

Previous q =
0.812。

Actual area stretch from F：

Js approximately 1.04403

q approximately 0.95783。

Therefore q was NOT derived
from the actual F。

============================================================
正式结论修正
============================================================

Stage3.5B / R1:

B0 checkpoint semantics evidence:
retained。

B1 opacity↔tau parameterization evidence:
retained。

Full q=1 identity evidence:
retained only if traced to actual renderer。

Previous synthetic patch C2:
RETIRED。

Previous real reconstruction bridge Final CASE:
REVOKED。

Current formal reconstructed bridge status：

NOT YET TESTED WITH REAL RASTERIZED DEFORMATION。

KIOT controlled-carrier evidence from
Stage3.5A / R1 remains VALID。

Do NOT modify KIOT。

============================================================
本阶段名称
============================================================

Stage 3.5B-R2:

Official-Mask Real-Render
Reconstruction Bridge

中文：

官方透明掩码下的
真实渲染重建载体桥接实验

============================================================
一、唯一科学问题
============================================================

Answer：

On ACTUAL automatically selected
TSGS reconstructed transparent-surface patches，
using ACTUAL deformation matrices F，
ACTUAL covariance transport，
ACTUAL Js derived from F and reconstructed normals，
and ACTUAL fresh TSGS rasterizer rendering：

does KIOT restore
the area-stretch optical response
better than：

fixed opacity

tau/Js

opacity-linear

continuous KIOT？

This must be a REAL RENDER experiment。

============================================================
二、绝对禁止事项
============================================================

禁止：

synthetic policy response generation

formula-generated R values

state-name-derived q

manually specified target q
without computing Js

reuse previous synthetic policy response CSV
for Gate metrics

previous heuristic candidate patches

checkpoint_visualize candidate quota

84.5-96.5 percentile candidate band

manual patch selection

policy-result-aware patch selection

training

fine-tuning

MLP

opacity optimization

new KIOT formula

LUT tuning

modifying TSGS

modifying RecycleGS。

All primary numerical results
must come from：

fresh renderer output alpha maps。

============================================================
三、输出目录
============================================================

All outputs：

/data/wyh/DeformTransGS/experiments/
stage3_5B_R2_official_mask_real_render_bridge/

All new code：

/data/wyh/DeformTransGS/analysis/

Do not overwrite previous experiments。

============================================================
四、Source / Method Lock
============================================================

Lock：

TSGS source

TSGS Stage-2 checkpoint

991832 Gaussian PLY/checkpoint

official cameras

actual scene root：

/data/wyh/RecycleGS/data/translab_full/scene_01

official transparent_masks

Stage3.5A-R1 identity-safe KIOT

65536 LUT

validated covariance transport utilities

oracle-validated optical metric primitives。

Output：

r2_real_bridge_protocol_lock.json。

Record SHA256。

Gate R20。

============================================================
五、Official Transparent Mask Lock
============================================================

Use ONLY：

/data/wyh/RecycleGS/data/translab_full/scene_01/
<actual transparent_masks directory>

located in Stage3.5B-R1。

No fallback heuristic。

Map mask files
to official TSGS camera objects。

Output：

official_transparent_mask_camera_map.csv。

Fields：

camera_id
camera_name
image_name
mask_path
mask_sha
mask_height
mask_width
foreground_fraction。

Require：

camera coverage >=95%。

Masks must be categorical。

Use nearest-neighbour sampling。

Gate：

OFFICIAL MASK LOCK PASS。

============================================================
六、Actual White-Pass / First-Surface Render Lock
============================================================

Use actual TSGS renderer。

White-pass optical alpha semantic：

override_color=ones

black background。

Trace actual：

pc.get_opacity

pc.get_transparency

rasterizer call。

Do not change transparency。

Checkpoint transparency is currently:

0.5 constant。

Preserve exactly。

Render canonical full checkpoint
for 12 evenly spaced official cameras。

Save actual:

white-pass output

first-surface depth

visibility/radii if available。

Output：

canonical_full_render_manifest.csv。

No synthetic maps。

============================================================
七、Official-Mask Gaussian Support
============================================================

For ALL 991832 Gaussian centers：

project into the 12 audit cameras
using exact current CUDA projection convention。

For every valid projected center：

sample official transparent mask
nearest-neighbour。

Record：

valid_views

inside_views

inside_fraction。

Also render ACTUAL canonical
first-surface depth from Section 6。

For mask-inside projected Gaussian：

compute：

d_gaussian

d_first。

depth_rel_error =

abs(d_gaussian-d_first)
/
(abs(d_first)+1e-8)。

Formal support counts：

strict_support_views：

mask inside
and depth_rel_error<=0.02。

medium_support_views：

mask inside
and depth_rel_error<=0.05。

loose_support_views：

mask inside
and depth_rel_error<=0.10。

Output：

official_mask_gaussian_multiview_support.csv。

First-surface depth is：

candidate-support reference only。

NOT true geometry。

============================================================
八、Formal Transparent Candidate Definition
============================================================

Formal candidate：

valid_views >=4

inside_fraction >=0.75

medium_support_views >=3。

No opacity threshold。

No transparency threshold。

No quota。

No percentile。

No top-k。

Output：

official_transparent_candidate_lock.csv。

Fields：

gaussian_index

valid_views
inside_views
inside_fraction

strict_support_views
medium_support_views
loose_support_views。

Report：

candidate count

fraction。

Sensitivity diagnostic：

strict-rule count：

valid>=4
inside_fraction>=0.75
strict_support_views>=3

medium formal count

loose-rule count。

Formal experiment uses MEDIUM only。

============================================================
九、Candidate Geometry from Actual Covariance
============================================================

For every official candidate：

Sigma =

R
diag(scale^2)
R^T

using ACTIVATED scale。

Eigendecompose。

Sort ascending：

lambda0 <=lambda1 <=lambda2。

Normal：

smallest eigenvector。

Tangent axes：

two remaining eigenvectors。

Store：

normal

t1

t2

flatness=lambda0/lambda1

anisotropy=lambda2/lambda1。

Output：

official_candidate_geometry.csv。

Do not filter by opacity。

============================================================
十、Automatic Patch Discovery
============================================================

Input：

official MEDIUM candidates only。

Patch size：

768 Gaussians。

For each candidate seed：

768 nearest official candidates
in xyz。

Compute：

radius_p95

normal reference
using scatter matrix：

M=sum n_i n_i^T。

n_patch =
principal eigenvector(M)。

Unsigned normal angles：

acos(abs(n_i dot n_patch))。

normal_p50
p90
p95。

official mask support median。

visible camera count：

audit camera where：

>=90%
patch projected Gaussian centers

are inside official transparent mask。

Eligible：

n=768

normal_p90 <=10 degrees

visible camera count >=3。

Score：

normal_p90_deg

+
5*(1-mask_inside_fraction_median)

+
0.1*normalized_radius。

NO policy quantities。

NO optical response quantities。

============================================================
十一、Freeze Three Official-Mask Patches
============================================================

PATCH_A：

lowest score。

PATCH_B：

lowest eligible
spatially separated from A：

center distance >=
2 * max(radius_A,radius_B)。

PATCH_C：

same separation
from A and B。

If 3 unavailable：

allow 2。

If <2：

PATCH-DISCOVERY-FAIL。

Save：

official_patch_manifest.csv

official_patch_indices/
patch_A.npy
patch_B.npy
patch_C.npy。

Immediately save file SHA and mtime。

Patch lock must precede policy rendering。

Gate R21。

============================================================
十二、Patch Local Basis
============================================================

For every frozen patch：

normal scatter：

M=sum n_i n_i^T。

n_patch =
principal eigenvector(M)。

Choose t1_patch：

project largest PCA xyz direction
onto tangent plane：

v =
v_pca
-
(n dot v_pca)*n。

normalize。

t2_patch =

n_patch cross t1_patch。

Enforce right-handed orthonormal basis。

Define:

B =

stack columns:

[n_patch,t1_patch,t2_patch]。

Require：

B^T B approx I

det(B) approx +1。

Output：

official_patch_local_basis.csv

and:

official_patch_local_basis.npz。

============================================================
十三、Formal REAL Deformation Suite
============================================================

This suite REPLACES
all previous Stage3.5B synthetic state names。

All matrices are explicitly defined
in local basis order：

[n,t1,t2]。

------------------------------------------------------------
E0 IDENTITY
------------------------------------------------------------

F_local =

[[1,0,0],
 [0,1,0],
 [0,0,1]]

Expected Js=1。

------------------------------------------------------------
E1 TANGENT_STRETCH_1P25
------------------------------------------------------------

F_local =

[[1,0,0],
 [0,1.25,0],
 [0,0,1]]

Expected planar Js=1.25

q=0.8。

------------------------------------------------------------
E2 TANGENT_STRETCH_1P50
------------------------------------------------------------

F_local =

[[1,0,0],
 [0,1.50,0],
 [0,0,1]]

Expected Js=1.50

q=2/3。

------------------------------------------------------------
E3 TANGENT_STRETCH_2P00
------------------------------------------------------------

F_local =

[[1,0,0],
 [0,2.00,0],
 [0,0,1]]

Expected Js=2.00

q=0.5。

------------------------------------------------------------
E4 BIAXIAL_TANGENT_1P50
------------------------------------------------------------

F_local =

[[1,0,0],
 [0,1.50,0],
 [0,0,1.50]]

Expected Js=2.25

q=4/9。

------------------------------------------------------------
E5 TANGENT_SHEAR_0P30
------------------------------------------------------------

F_local =

[[1,0,0],
 [0,1,0.30],
 [0,0,1]]

This is a shear INSIDE
the tangent plane。

Expected planar area determinant:

1。

Therefore：

Js=1

q=1。

Area-preserving control。

------------------------------------------------------------
E6 OBLIQUE_TANGENT_STRETCH_1P80
------------------------------------------------------------

theta =
45 degrees。

d_local =

[0,cos(theta),sin(theta)]。

F_local =

I
+
(1.8-1)
*
d_local d_local^T。

Expected Js=1.8

q=1/1.8。

------------------------------------------------------------
E7 PURE_ROTATION
------------------------------------------------------------

rotation angle：

25 degrees

around n_patch。

Js=1。

Control。

============================================================
十四、Compute WORLD F Explicitly
============================================================

For every patch/state：

F_world =

B
F_local
B^T。

For rotation：

F_world =

B
R_local
B^T。

Save ACTUAL matrices：

real_deformation_matrices.npz。

Output：

real_deformation_matrix_audit.csv。

Fields：

patch_id
state

all 9 F_world entries

detF

singular values

orthogonality error if rotation。

NO state-name q table。

============================================================
十五、Compute Js FROM F
============================================================

For every patch Gaussian i：

use its reconstructed covariance normal：

n_i。

Compute：

Js_i =

abs(det(F_world))
*
norm(
    inverse(F_world).T @ n_i
)。

q_i =1/Js_i。

No hardcoded q。

For patch reference normal：

compute Js_patch same formula。

Output：

real_deformation_js_audit.csv。

Fields：

patch_id
state
gaussian_index
Js
q。

Summary：

Js min
p05
median
p95
max
CV。

Sanity Gates：

E0 median Js within1e-10 of1。

E1 median relative Js error
vs1.25 <=0.02。

E2 vs1.5 <=0.02。

E3 vs2 <=0.02。

E4 vs2.25 <=0.03。

E5 median |Js-1| <=0.02。

E6 median relative error vs1.8 <=0.03。

E7 |Js-1| <=1e-10。

If FAIL：

DEFORMATION-JS-FAIL。

STOP。

============================================================
十六、ACTUAL Geometry Transport
============================================================

For patch pivot：

p =
mean xyz patch。

For every Gaussian：

xyz_def =

p
+
F_world
@
(xyz-p)。

Covariance：

Sigma_def =

F_world
Sigma_can
F_world^T。

Use validated transport utilities：

covariance_to_scale_rotation。

Render adapter receives：

xyz_def

activated transported scale

transported rotation。

Assertions：

direct covariance reconstruction max error
<=1e-6 relative/numerical appropriate scale。

Output：

real_transport_tensor_manifest.csv。

Store SHA：

xyz_def

Sigma_def

scale_def

rotation_def

Js

q

per patch/state。

============================================================
十七、Formal Optical Policies
============================================================

All policies share EXACT SAME：

xyz_def

Sigma_def

transparency

override_color

camera。

Only rasterizer opacity changes。

------------------------------------------------------------
P0_FIXED
------------------------------------------------------------

o_new=o。

------------------------------------------------------------
P1_TAU_JS
------------------------------------------------------------

tau = -log1p(-o)

tau_new = q_i*tau

o_new=1-exp(-tau_new)。

------------------------------------------------------------
P2_OPACITY_LINEAR
------------------------------------------------------------

o_new=q_i*o。

------------------------------------------------------------
P3_KIOT_CONT
------------------------------------------------------------

Phi_cont(o_new)=q_i Phi_cont(o)。

------------------------------------------------------------
P4_KIOT_CUDA
------------------------------------------------------------

Stage3.5A-R1 identity-safe B4R

65536 LUT。

No policy formula changes。

============================================================
十八、Policy Tensor Audit
============================================================

For every：

patch
state
policy

save：

xyz SHA
Sigma SHA
scale SHA
rotation SHA
opacity SHA
transparency SHA。

Output：

real_policy_tensor_manifest.csv。

Assertions：

all policy xyz exact same。

all policy Sigma exact same。

all policy transparency exact same。

P0-P4 differ only opacity。

E0/E5/E7 q approximately1：

P0-P4 opacity identity
according to actual q tolerance
and KIOT formal q=1 semantics。

For E5 if Js_i varies slightly around1
because reconstructed normals vary：

report actual policy differences。

Do not force q=1
unless actual Js formula gives1。

============================================================
十九、Fresh REAL Rasterizer Rendering
============================================================

For every：

3 patches

8 states

5 policies

all frozen patch evaluation cameras：

render official TSGS rasterizer。

White pass：

override_color=ones

black bg。

No synthetic alpha generation。

No formula-generated response image。

Save actual renderer outputs：

real_alpha/
<patch>/
<state>/
<policy>/
<camera>.npy

Also save：

first-surface depth
where available。

Output：

real_render_manifest.csv。

Each row：

patch
state
policy
camera

alpha path
alpha SHA
alpha min
median
p95
p99
max

first-surface path/SHA。

Gate R22：

all required fresh render files exist。

============================================================
二十、Render Provenance Assertion
============================================================

Create：

real_render_provenance.md。

Must state for every metric table：

SOURCE =

actual saved rasterizer alpha map。

Add code assertion：

response computation
must load alpha files
listed in real_render_manifest.csv。

It must NOT call：

synthetic_response

generate_policy_response

expected_response

response_from_q

or equivalent。

Search Stage R2 source
for these strings and similar。

Output：

synthetic_response_code_search.txt。

Gate:

no synthetic response path
used in metric generation。

============================================================
二十一、Freeze Evaluation Cameras BEFORE Policy Metrics
============================================================

For each official patch：

from canonical E0 P0 geometry
and official masks：

camera valid iff：

>=90%
patch Gaussian centers

inside transparent mask

and patch projected bbox
inside image with 8px margin。

Freeze cameras。

Output：

real_patch_camera_lock.csv。

Require >=3 cameras per patch。

This file must be generated
before policy response CSV。

============================================================
二十二、Material Proxy Anchors BEFORE Policy Metrics
============================================================

For every patch：

deterministic FPS：

128 Gaussian anchors。

seed：

20260713。

Use actual patch Gaussian xyz。

For each anchor：

canonical normal/tangents
from actual covariance eigensystem。

Create 5x5 samples：

a,b =

[-0.5,-0.25,0,0.25,0.5]。

x_can =

x_i
+
a*s1*t1
+
b*s2*t2。

Transport with actual F_world：

x_def =

p
+
F_world
@
(x_can-p)。

Save：

real_material_proxy_anchor_lock.csv
real_material_proxy_samples.npz。

Anchor/sample lock must precede policy metric generation。

============================================================
二十三、Frozen Canonical Valid Keys
============================================================

Render canonical E0 P0 once。

Using real canonical alpha maps：

for each：

patch
anchor
camera

sample 25 canonical proxy points。

Valid：

projection valid

bilinear valid

finite

valid fraction>=0.8

mean tau_can>0。

Freeze：

real_frozen_anchor_camera_keys.csv。

All states/policies
use SAME canonical-valid keys
where deformed projection remains numerically projectable。

Do NOT use policy alpha
to select keys。

============================================================
二十四、REAL Optical Response Metric
============================================================

For every frozen key：

load actual canonical alpha file。

load actual policy deformed alpha file。

Project canonical samples。

Project actual F-transformed samples。

Bilinear sample alpha。

Convert：

tau_can =
-log1p(-A_can)

tau_def =
-log1p(-A_def)。

Anchor-camera：

R =

mean(tau_def)
/
mean(tau_can)

exact positive ratio。

Physical target：

Q_anchor = q_anchor

where q_anchor
is ACTUALLY computed from：

F_world

and anchor reconstructed normal：

1 /
[
abs(det(F_world))
*
norm(
F_world^-T n_anchor
)
]。

No hardcoded Q。

Output：

real_patch_anchor_camera_response.csv。

============================================================
二十五、Cross-Camera Aggregation
============================================================

For each：

patch
state
policy
anchor

collect same frozen cameras。

Require >=2 camera responses。

R_anchor =median camera R。

Q_anchor =actual q_anchor。

E_abs =
abs(R-Q)

E_log =
abs(log(R/Q))。

Output：

real_patch_anchor_response.csv。

============================================================
二十六、Identity / Rotation Controls
============================================================

E0 IDENTITY：

all policies q=1。

Require：

opacity identity

alpha max diff P0-policy <=1e-6

MAE <=1e-8。

Median R error from1 <=1e-6。

------------------------------------------------------------

E7 PURE_ROTATION：

Js=1。

All scalar policies
must preserve opacity。

Because geometry is rotated，
deformed alpha differs from canonical，
but P0-P4 MUST be mutually identical。

Compare policy alpha pairwise：

max <=1e-6

MAE <=1e-8。

Anchor R distributions
must be identical across policies：

max per-anchor R difference <=1e-6。

Gate R23。

============================================================
二十七、Area-Preserving Shear Control
============================================================

E5 tangent-plane shear
has ideal patch Js=1。

Because per-Gaussian reconstructed normals
are not perfectly identical：

use actual q_i。

Report q:

median
p05
p95。

Primary evaluation compares policies
to actual Q anchors。

Also report：

P0 median R

P4 median R

median Q。

This is a control，
not assumed exact identity
unless q_i exact1。

============================================================
二十八、REAL Central Response Table
============================================================

Primary area-changing states：

E1
E2
E3
E4
E6。

Controls：

E0
E5
E7。

For every：

patch
state
policy

report：

n anchors

median R

p05
p25
p75
p95

median Q

central error：

abs(
median R-median Q
)。

Output：

real_reconstructed_central_response.csv。

============================================================
二十九、REAL KIOT Bridge Gate
============================================================

Aggregate only：

3 patches ×
5 area-changing states

=15 primary patch-state pairs
if all valid。

Policies：

P0
P1
P2
P3
P4。

Compute：

mean central error

median central error。

P4 REAL KIOT BRIDGE SUPPORTED iff：

A.

P4 mean central error <=0.10。

B.

P4 mean error
<=0.50 * P0 mean error。

C.

P4 mean error
<
P1 tau/Js mean error。

D.

P4 beats P0
on >=75% primary pairs。

E.

At least 2 independent patches
have >=2 area-changing states
where：

P0 median R
is closer to1 than median Q

and

P4 recovers >=50%
of P0 central error：

recovery =

(
error_P0-error_P4
)
/
(error_P0+1e-12)

>=0.5。

Output：

real_kiot_bridge_gate.json。

Gate R24。

No threshold changes。

============================================================
三十、Continuous vs CUDA
============================================================

Compare P3 vs P4。

Output：

real_continuous_vs_cuda.csv。

CUDA benefit supported iff：

P4 mean central error
<=0.90 * P3 mean error。

Diagnostic。

============================================================
三十一、Tail Metrics
============================================================

For primary anchor responses：

per policy：

median E_log

p90
p95
p99

factor2
factor5。

Output：

real_reconstructed_tail_severity.csv。

Paired P4-P0:

Delta Elog。

10000 bootstrap

seed 20260713。

Output：

real_reconstructed_paired_tail.csv。

Secondary result。

============================================================
三十二、First-Surface Opacity Sensitivity
============================================================

Using ACTUAL fresh renderer outputs：

For each：

patch
primary state
camera

compare：

GEO_REF：

P0 same deformed xyz/Sigma
original opacity。

KIOT：

P4 same deformed xyz/Sigma
KIOT opacity。

Compare actual first-surface maps。

Within projected official transparent mask
and patch bbox：

median relative depth diff

p90
p95

valid mask IoU。

Output：

real_first_surface_opacity_sensitivity.csv。

Geometry-safe PASS iff：

disturbance criteria from Stage3.5B
remain not supported：

median relative >0.02

or p95 >0.10

or IoU<0.95

in >=25% cases。

Gate R25。

============================================================
三十三、Full q=1 Identity Recheck
============================================================

Retain previous q=1 evidence，
but rerun from current R2 adapter。

Apply P4 KIOT q=1
to all 991832 Gaussians。

Full actual TSGS rendering：

official test cameras

full appearance pipeline。

Compare original。

Output：

r2_full_tsgs_q1_identity.csv。

Require：

image max <=1e-6

MAE <=1e-8。

Gate R26。

============================================================
三十四、Strict Gates
============================================================

R20:
PROTOCOL LOCK

PASS。

R21:
OFFICIAL MASK PATCH LOCK

PASS。

R22:
REAL RENDER PROVENANCE

PASS。

R23:
IDENTITY / ROTATION CONTROLS

PASS。

R24:
REAL KIOT RECONSTRUCTION BRIDGE

SUPPORTED / NOT SUPPORTED。

R25:
FIRST-SURFACE OPACITY SAFETY

PASS / FAIL。

R26:
FULL q=1 IDENTITY

PASS / FAIL。

============================================================
三十五、Final CASE
============================================================

CASE REAL-TSGS-KIOT-BRIDGE-SUPPORTED:

R20 PASS
R21 PASS
R22 PASS
R23 PASS
R24 SUPPORTED
R25 PASS
R26 PASS。

Conclusion：

On official-transparent-mask-selected，
automatically frozen
learned TSGS Gaussian patches，

actual full covariance transport
and fresh rasterizer rendering
show that fixed opacity
fails to follow local area-stretch
optical response。

KIOT，
using actual Js derived from
F and reconstructed Gaussian normals，
substantially restores
the physical area-stretch target
and outperforms：

fixed opacity

tau/Js

opacity-linear transport。

ALLOW：

deformed-GT benchmark construction

full reconstructed-carrier evaluation。

------------------------------------------------------------

CASE REAL-KIOT-OPTICAL-GEOMETRY-CONFLICT:

R20-R24 PASS

R25 FAIL。

KIOT optical bridge succeeds
but direct opacity changes
disturb TSGS first-surface semantics。

Next：

geometry-optics decoupled render bridge。

------------------------------------------------------------

CASE KIOT-CONTROLLED-ONLY:

R24 NOT SUPPORTED。

The previous synthetic bridge evidence
does not transfer to actual
learned TSGS rasterized deformation。

------------------------------------------------------------

CASE PROTOCOL-FAIL:

R20/R21/R22/R23/R26 FAIL。

============================================================
三十六、Required Outputs
============================================================

r2_real_bridge_protocol_lock.json

official_transparent_mask_camera_map.csv
canonical_full_render_manifest.csv

official_mask_gaussian_multiview_support.csv
official_transparent_candidate_lock.csv
official_candidate_geometry.csv

official_patch_manifest.csv
official_patch_indices/*.npy

official_patch_local_basis.csv
official_patch_local_basis.npz

real_deformation_matrices.npz
real_deformation_matrix_audit.csv
real_deformation_js_audit.csv

real_transport_tensor_manifest.csv
real_policy_tensor_manifest.csv

real_render_manifest.csv
real_render_provenance.md
synthetic_response_code_search.txt

real_patch_camera_lock.csv

real_material_proxy_anchor_lock.csv
real_material_proxy_samples.npz
real_frozen_anchor_camera_keys.csv

real_patch_anchor_camera_response.csv
real_patch_anchor_response.csv

real_reconstructed_central_response.csv
real_kiot_bridge_gate.json
real_continuous_vs_cuda.csv

real_reconstructed_tail_severity.csv
real_reconstructed_paired_tail.csv

real_first_surface_opacity_sensitivity.csv
r2_full_tsgs_q1_identity.csv

official_mask_real_render_bridge_report.md
stage3_5B_R2_summary.md
stage3_5B_R2_log.txt

============================================================
三十七、报告必须中文回答
============================================================

A. 为什么 previous Stage3.5B
92.3% 不能作为 real-render evidence。

B. previous synthetic response exact source。

C. 为什么 normal stretch
diag(s,1,1)
在 [n,t1,t2] 下 Js=1。

D. previous D1 actual Js/q。

E. previous D2 actual Js/q。

F. previous D3 actual Js/q。

G. previous D4 name/matrix mismatch。

H. previous D5 why not oblique。

I. R20。

J. official mask camera coverage。

K. official candidate count/fraction。

L. strict/medium/loose counts。

M. selected official patches。

N. patch normal p90。

O. patch visible cameras。

P. exact local basis orthogonality/det error。

Q. E1-E7 actual median Js。

R. E1-E7 actual median q。

S. Js sanity Gates。

T. transport covariance reconstruction error。

U. real render file count。

V. synthetic response path used yes/no。

W. R22。

X. identity control。

Y. rotation policy identity。

Z. R23。

AA. each primary patch/state：
P0/P1/P2/P3/P4/Q median。

AB. P0 mean central error。

AC. P1 mean central error。

AD. P2 mean central error。

AE. P3 mean central error。

AF. P4 mean central error。

AG. P4 vs P0 improvement。

AH. P4 win fraction。

AI. R24 A-E。

AJ. R24。

AK. CUDA-aware benefit。

AL. primary tail comparison。

AM. first-surface median/p95 drift。

AN. valid-mask IoU。

AO. R25。

AP. q1 identity image max/MAE。

AQ. R26。

AR. Final CASE。

AS. strongest scientific conclusion。

AT. can construct deformed-GT benchmark yes/no。

AU. can run full reconstructed-carrier evaluation yes/no。

============================================================
三十八、README
============================================================

更新：

/data/wyh/DeformTransGS/README.md

明确记录：

Stage3.5B and Stage3.5B-R1
did NOT establish a real rasterized
reconstructed-carrier KIOT bridge。

The previous patch response experiment
used state-name / target-q driven
synthetic policy response generation
and did not persist actual deformation F matrices。

A subsequent audit showed
that several named local-frame deformations
were inconsistent with their matrices。

With local basis [n,t1,t2]：

diag(s,1,1)
is a normal-direction stretch
and has surface area stretch Js=1，
not Js=s。

Therefore the previous reconstructed bridge
92.3% improvement is retired
as real-render evidence
and retained only as
synthetic exploratory evidence。

Stage3.5B-R2 restarts the bridge correctly
using：

official transparent_masks

automatic official-mask patch selection

explicit saved deformation matrices F

Js computed strictly from
|detF| ||F^-T n||

actual xyz transport

actual covariance transport F Sigma F^T

actual KIOT opacity transport

fresh TSGS rasterizer alpha maps

and frozen material-proxy response keys。

No synthetic policy-response generator
is allowed。

============================================================
三十九、实际执行
============================================================

必须实际：

lock methods/checkpoint

map official transparent masks

actual canonical white-pass/depth render

project all Gaussian centers

official mask + depth support

freeze official candidates

compute actual covariance normals

automatic official-mask patch discovery

freeze patches before policies

construct exact local basis

construct explicit E0-E7 F_local

convert to actual F_world

save every F

compute Js from F and each normal

verify Js sanity

actually deform xyz

actually transport covariance

actually compute every optical policy

fresh render every policy

save actual alpha maps

prove metric reads actual alpha files

freeze cameras before metrics

freeze anchors before metrics

freeze canonical valid keys

compute actual R/Q

central bridge Gate

tail analysis

actual first-surface sensitivity

full q1 identity

R20-R26

Final CASE

绝对禁止 synthetic response generation。
PROMPT
```

## 我现在最真实的评价

这次确实发现了一个**大 bug**。

甚至比之前 historical R4 metric 漂移更直接。

但我反而觉得现在发现非常值。

因为 KIOT 真正成立的证据：

[
\text{Stage 3.5A}
]

和：

[
\text{Stage 3.5A-R1}
]

完全没被影响。

受影响的只是：

> **“已经迁移到真实 TSGS 了”**

这句话。

所以当前真实状态应该是：

[
\boxed{
\text{KIOT controlled carrier：已成立}
}
]

[
\boxed{
\text{KIOT real TSGS reconstruction bridge：重新待验证}
}
]

千万别再使用：

> “真实 TSGS 上误差下降 92.3%”

作为正式结果。

等 R2 真正用：

[
F\rightarrow J_s\rightarrow q
]

并且 fresh rasterizer render 跑出来。

假如还能得到哪怕：

[
0.30\rightarrow0.08
]

我都会认为主线很强。

**这次 R2 才是真正的真实载体生死实验。**
