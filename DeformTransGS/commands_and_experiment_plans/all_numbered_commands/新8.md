这次结果很关键，我的判断是：

> **原 R4 的“载体容量不足”已经被彻底推翻。**
>
> 但现在也**不能直接宣布 J4 失败、回 RecycleGS**。

目前真正的问题已经缩小到 **K2 的新视角泛化 + 训练尚未收敛**。

## 一、O1 证明我上一轮的质疑完全成立

原 R4 有三个真实协议错误：

```text
HARD-500-STEP-CAP
MISSING-P0-P1-FOOTPRINT-DIAGNOSTIC
HARDCODED-GENERIC-LR
```

尤其学习率错得非常大：

| 状态             | RT 源码 LR | 原 R4 LR |      差距 |
| -------------- | -------: | ------: | ------: |
| occupancy      |     0.05 |    1e-6 | 50,000× |
| opacity        |     0.05 |    1e-6 | 50,000× |
| transmissivity |     0.01 |    1e-6 | 10,000× |
| features_dc    |    0.002 |    1e-6 |  2,000× |

原 R4 基本就是踩着刹车训练了 500 步，然后说车跑不动。这个结论现在正式作废。

---

# 二、修正以后，结果发生了本质变化

K0：

[
\text{TEST PSNR}=28.424
]

[
E_{\tau}=0.0806
]

**PASS。**

K1：

[
\text{TEST PSNR}=28.532
]

[
E_{\tau}=0.0768
]

**PASS。**

这非常重要。

因为它直接证明：

> **4096 个固定数量、固定 xyz 的 RT-native Gaussian，确实能够表达至少平面薄层透明场景。**

因此：

```text
RT-native carrier universally insufficient
```

已经不成立。

载体有真实静态透明表达能力。

---

# 三、K2 也不是“训练集拟合失败”

K2：

```text
TRAIN:
PSNR     = 28.3318
tau_eq   = 0.0557
```

两个阈值：

[
PSNR\ge28
]

[
E_{\tau}\le0.25
]

训练集全部通过。

但 TEST：

```text
PSNR   = 26.1843
tau_eq = 0.3368
```

失败。

所以 Codex 给出的：

```text
GENERALIZATION-GAP
```

我认为是正确的。

这意味着：

> **RT-native carrier 能拟合 wavy tinted transparent surface（波浪形、有颜色的透明表面）的训练视角，但目前没有正确泛化到未见相机。**

这和 carrier capacity insufficient（载体容量不足）完全不同。

---

# 四、现在最关键的数据其实是这两个

```text
selected iteration = 4000
best iteration is final executed = YES
patience elapsed = 0
```

K0/K1/K2 全部如此。

这意味着：

> step 4000 仍然产生了新的 TRAIN best。

因此训练不是：

```text
已经 plateau（稳定）
→ TEST 仍失败
```

而是：

```text
TRAIN still improving
→ 被 max4000 强制截断
```

根据我们自己冻结的规则，这只能分类：

```text
OPTIMIZATION-UNRESOLVED
```

不能分类最终载体失败。

所以：

```text
CASE RTSPLAT-V2-OPTIMIZATION-UNRESOLVED
```

**我接受。**

---

# 五、但现在不能无限继续救

这里我要给自己踩刹车了。

我们不再：

* 改学习率；
* 改 loss；
* 加新状态；
* 增加 Gaussian；
* 加 MLP；
* 改 benchmark；
* 改 train/test；
* 调 scheduler。

只允许做最后一个非常干净的实验：

# Stage 5.0-R4-O2：训练收敛闭环

从 step 4000 的**原 optimizer state（优化器状态）**直接续训。

不 restart。

不改任何超参数。

只使用 TRAIN。

继续到：

[
\boxed{12000\text{ steps maximum}}
]

或者：

> 距离最后一次 TRAIN best 已经 1000 step。

这里我建议 patience 从 500 改成 **1000**。

为什么？

不是因为 K2 TEST 没过。

而是从完整训练历史已经知道：

> 在 4000 step 仍然每隔很短时间刷新 best。

500 step 对当前真实收敛速度可能仍过短。

但为了避免无限续命，直接定死：

```text
max = 12000
patience = 1000
```

且这是**最后一次训练收敛 Gate**。

如果 K2 真正 plateau 后：

```text
TRAIN PASS
TEST FAIL
```

那就是可靠的 generalization gap（泛化差距）。

然后我会接受：

> 当前 carrier 不适合作为 Stage5.1 的实验载体。

不再救。

---

## 可以直接发给 Codex 的完整指令

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
Stage 5.0-R4-O2
Canonical Convergence Closure
============================================================

中文：

RT-native V2 canonical
最终训练收敛闭环

============================================================
最高优先级
============================================================

This is the FINAL optimization-convergence Gate
for the AttributeDeformGS carrier route。

Do NOT redesign the model。

Do NOT change the benchmark。

Do NOT change the optimizer。

Do NOT change learning rates。

Do NOT change the loss。

Do NOT restart from initialization。

Resume the exact corrected O1
K0/K1/K2 selected step4000
full-state checkpoints
with their exact optimizer states。

The only question is：

Were the corrected canonical fits
merely truncated before convergence，
or does K2 retain a genuine
perspective novel-view generalization gap
after TRAIN convergence？

============================================================
正式科学状态
============================================================

Old R4 capacity classification：

RTSPLAT-PERSPECTIVE-V2
-CANONICAL-CARRIER-INSUFFICIENT

is RETIRED。

Reason：

the original R4 optimization protocol contained：

HARD-500-STEP-CAP

invalid footprint-policy handling

HARDCODED-GENERIC-LR。

------------------------------------------------------------

Stage5.0-R4-O1 repaired：

early-stop semantics

real P0/P1 footprint diagnostic

source LR mapping

restart from initialization。

Corrected footprint policy：

NATIVE_FOOTPRINT。

Corrected trainable persistent states：

_scaling

_rotation

_occupancy

_opacity

_transmissivity

_features_dc。

Source-matched learning rates：

_occupancy =
0.05

_opacity =
0.05

_transmissivity =
0.01

_features_dc =
0.002。

Use the exact O1 optimizer lock
for scaling/rotation learning rates。

Do NOT reconstruct their values from memory。

============================================================
Corrected O1 results
============================================================

K0 full-TRAIN：

PSNR =
29.378260490715007

median tau_eq Elog =
0.023837348446249962。

K0 TEST：

PSNR =
28.424359646062456

median tau_eq Elog =
0.08058174699544907。

K0：

PASS。

------------------------------------------------------------

K1 full-TRAIN：

PSNR =
29.480847560616283

median tau_eq Elog =
0.02530415914952755。

K1 TEST：

PSNR =
28.53203048129981

median tau_eq Elog =
0.07684889808297157。

K1：

PASS。

------------------------------------------------------------

K2 full-TRAIN：

PSNR =
28.331837966066587

median tau_eq Elog =
0.05571654625236988。

K2 TEST：

PSNR =
26.184307682898726

median tau_eq Elog =
0.33675524592399597。

K2 TRAIN passes both frozen capacity thresholds。

K2 TEST fails both thresholds。

Current K2 classification：

GENERALIZATION-GAP。

============================================================
Unresolved convergence evidence
============================================================

Corrected optimizer steps：

K0 =
4000

K1 =
4000

K2 =
4000。

Selected checkpoint iterations：

4000
4000
4000。

For all K cases：

best iteration is final executed =
YES。

Final patience elapsed：

0
0
0。

Therefore all corrected canonical jobs
were still producing a new
full16-TRAIN aggregate best
at the maximum4000-step boundary。

The O1 Final CASE：

RTSPLAT-V2-OPTIMIZATION-UNRESOLVED。

This classification is accepted。

============================================================
Scientific restriction
============================================================

Do NOT use the current K2 TEST failure
to tune optimization。

The decision to perform O2 is based ONLY on：

best iteration =
final executed iteration

and

TRAIN patience elapsed =
0。

The TEST metrics are frozen
and must not affect：

training continuation

checkpoint selection

stopping

learning rates

loss

state policy。

============================================================
本阶段唯一目标
============================================================

Resume exact O1 training states
and determine true TRAIN convergence。

For each K case：

resume from iteration4000。

Continue until either：

A.

current_iteration
-
last_full_TRAIN_best_iteration
>=1000

OR：

B.

iteration =
12000。

No other stopping condition。

After selected checkpoints are frozen
using TRAIN only，
render TEST exactly once
for final J4 evaluation。

============================================================
绝对禁止事项
============================================================

禁止：

restart from initialization

change optimizer

change Adam state

reset optimizer state

change learning rates

LR search

scheduler search

change existing scheduler semantics

change loss

change loss weights

change valid optical mask

change V2 benchmark

regenerate V2

change camera

change FoVy

change radius scale

change TRAIN IDs

change TEST IDs

increase Gaussian count

densification

pruning

Gaussian creation/deletion

optimize xyz

add state family

train roughness

train reflectance

train language_feature

train features_rest

add auxiliary modules

MLP predictor

KIOT

attribute release

deformation

look at TEST during continuation

select checkpoint from TEST

change28 dB threshold

change0.25 tau_eq threshold。

============================================================
资源约束
============================================================

Use：

CUDA_VISIBLE_DEVICES=2,3。

Maximum2 concurrent GPU jobs。

GPU assignment：

K0 -> physical GPU2

K1 -> physical GPU3。

After either finishes，
K2 uses the first free allowed GPU。

============================================================
输入证据
============================================================

Resolve exact paths from O1 report/locks。

Required：

O1 protocol lock

O1 corrected optimizer lock
or exact frozen optimizer construction evidence

O1 corrected footprint policy lock

O1 corrected canonical trainable state lock

O1 corrected K0/K1/K2 selected step4000 checkpoints

O1 corrected optimizer state_dicts

O1 corrected camera schedule/history

O1 corrected full-TRAIN selection histories

O1 corrected checkpoint adapter

C2 future J4 benchmark lock。

Do not use the old R4 step500 checkpoints。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stage5_0_R4_O2_convergence_closure/

New code：

/data/wyh/DeformTransGS/
rtsplat_attribute_study/
stage5_R4_O2/

Create：

stage5_R4_O2/
    protocol/
    resume/
    train/
    convergence/
    checkpoint/
    render/
    metrics/
    diagnostics/
    audit/
    run_O2.py

Do not overwrite O1 outputs。

============================================================
一、O2 Protocol Lock
============================================================

Lock SHA256：

O1 protocol lock

O1 final report

O1 final summary

O1 minimal optimization repair

O1 canonical execution decision

O1 repaired footprint diagnostic

O1 repaired footprint policy lock

O1 corrected training source

O1 corrected optimizer construction

O1 corrected canonical histories

O1 corrected full-TRAIN selection histories

O1 selected step4000 checkpoints

O1 corrected checkpoint integrity evidence

O1 metric reproduction evidence

C2 future J4 benchmark lock

C2 camera split lock

R2 verified launcher

R3 checkpoint adapter。

Output：

O2_protocol_lock.json。

Gate T0。

============================================================
二、Exact Step4000 Resume Identity
============================================================

For K0/K1/K2：

load the exact O1 selected checkpoint
at iteration4000。

Require checkpoint metadata：

case key

iteration =
4000

Gaussian count =
4096

benchmark version =
C2-V2

footprint policy =
NATIVE_FOOTPRINT

trainable-state lock matches O1

optimizer lock matches O1。

Load all11 persistent tensors。

Load auxiliary module state。

Load exact optimizer state_dict。

Record optimizer state for every
selected parameter group：

group name

LR

step/count state where applicable

exp_avg tensor SHA

exp_avg_sq tensor SHA。

Output：

O2_resume_identity.csv。

Gate T1a。

============================================================
三、Optimizer-State Roundtrip Audit
============================================================

Before continuation：

save a project-side temporary checkpoint。

Destroy model and optimizer。

Fresh reconstruct model。

Load persistent state。

Fresh construct optimizer
with the exact O1 parameter groups。

Load exact saved optimizer state_dict。

Compare：

parameter-group LR

betas

eps

weight decay

optimizer internal step

exp_avg

exp_avg_sq。

Require：

all optimizer tensor max reload error =
0。

Output：

O2_optimizer_state_roundtrip.csv。

Gate T1b。

No continuation if optimizer state
cannot be reproduced exactly。

============================================================
四、Training Camera Schedule Continuation
============================================================

The corrected O1 deterministic
camera schedule used seeded permutations
of all16 TRAIN cameras。

Resume schedule from iteration4001。

Do NOT restart the seed sequence
at schedule position0。

Reconstruct the deterministic schedule
for iterations1 through12000
from the original scheduling algorithm。

Verify iterations1-4000
exactly match O1 recorded camera IDs。

Require mismatch count =
0。

Then use positions4001 onward。

Output：

O2_camera_schedule_reproduction.csv
O2_training_camera_schedule_4001_12000.csv。

Gate T2。

============================================================
五、Full-TRAIN Best-State Reconstruction
============================================================

From O1 full-TRAIN history
reconstruct the exact state at4000：

best_loss

best_iteration

last_best_iteration。

Require：

best_iteration =
4000。

Require：

patience elapsed =
0。

Record the exact4000 aggregate：

total loss

PSNR

median tau_eq Elog。

Output：

O2_initial_best_state.csv。

============================================================
六、Frozen Convergence Rule
============================================================

Continue from iteration4001。

Every100 optimizer steps：

render all16 TRAIN cameras。

Compute the exact unchanged
canonical aggregate：

RGB_L1

TAU_EQ_RGB_LOG_L1

DSSIM

total loss

PSNR

median tau_eq Elog。

If:

current full-TRAIN total loss
<
best_loss

then：

best_loss =
current loss

best_iteration =
current iteration

save new best checkpoint。

Patience elapsed：

current iteration-best_iteration。

Stop iff：

patience elapsed >=1000。

Hard maximum：

12000 optimizer steps total。

The patience1000 and max12000
are frozen before O2 continuation metrics。

Do not modify them。

============================================================
七、Continuation Optimizer Execution
============================================================

For each case continue exact optimizer state。

Use the exact trainable states：

_scaling

_rotation

_occupancy

_opacity

_transmissivity

_features_dc。

_xyz remains frozen。

Excluded states remain frozen：

_roughness

_reflectance

_language_feature

_features_rest。

No auxiliary module training。

Use exact O1 learning rates
and scheduler semantics。

Do not reset gradient statistics
except normal zero_grad semantics。

============================================================
八、Every-Step Continuation History
============================================================

Record every actual optimizer step
from4001 onward。

Fields：

case

iteration

TRAIN camera ID

single-camera total loss

RGB L1

tau_eq loss

DSSIM

LR by parameter group

gradient L2：

_scaling

_rotation

_occupancy

_opacity

_transmissivity

_features_dc。

Parameter delta L2
relative to the original canonical
initialization
and relative to the step4000 state。

Output：

O2_continuation_history/
K0.csv
K1.csv
K2.csv。

============================================================
九、Full-TRAIN Convergence History
============================================================

At every100-step evaluation save：

case

iteration

full-TRAIN total loss

PSNR

median tau_eq Elog

RGB L1

tau_eq loss

DSSIM

previous best loss

current best loss

best iteration

is_new_best

patience elapsed

stop condition。

Output：

O2_full_train_convergence/
K0.csv
K1.csv
K2.csv。

============================================================
十、TRAIN Trend Diagnostics
============================================================

For every case,
at every full-TRAIN evaluation
compute diagnostic changes over：

last100 steps

last500 steps

last1000 steps
when available。

Report：

absolute loss change

relative loss change

PSNR change

tau_eq Elog change。

Do NOT use these diagnostics
to change stopping rules。

Output：

O2_train_trend_diagnostic.csv。

============================================================
十一、Geometry / State Freeze Audit
============================================================

At：

4000

5000

6000

8000

10000

12000 if reached

selected best

final

record：

Gaussian count

_xyz SHA

_xyz max error from initialization

excluded-state max error

trainable-state finite fraction。

Require：

Gaussian count =
4096。

_xyz change =
0。

Excluded states change =
0。

Output：

O2_geometry_state_freeze_audit.csv。

Gate T3。

============================================================
十二、Continuation Checkpoint Policy
============================================================

Use exact R3/O1 full-state checkpoint adapter。

Save：

every new full-TRAIN best

iteration5000

6000

7000

8000

9000

10000

11000

12000
if reached

final。

Checkpoint metadata must preserve：

original O1 provenance

O2 protocol lock

resume checkpoint SHA

optimizer-state SHA

camera schedule lock

iteration

best iteration

best loss

patience elapsed。

============================================================
十三、Selected Checkpoint Integrity
============================================================

After continuation stops：

select ONLY the minimum
full16-TRAIN aggregate loss checkpoint。

Destroy model and optimizer。

Fresh process。

Fresh reconstruct model。

Load selected checkpoint。

Compare all11 persistent tensors：

max error =
0。

Compare optimizer state where saved：

max tensor error =
0。

Output：

O2_checkpoint_integrity.csv。

Gate T4a。

============================================================
十四、Final TRAIN Convergence Classification
============================================================

Classify every K case exactly one：

TRAIN-CONVERGED-BY-PATIENCE

TRAIN-TRUNCATED-AT-12000

TRAIN-NUMERICALLY-UNSTABLE

TRAIN-PROVENANCE-FAIL。

TRAIN-CONVERGED-BY-PATIENCE iff：

stopped because：

iteration-best_iteration >=1000。

TRAIN-TRUNCATED-AT-12000 iff：

iteration reached12000

and patience elapsed <1000。

If truncated：

do NOT automatically extend beyond12000。

This is the final O2 budget。

============================================================
十五、Freeze Selected Checkpoints Before TEST
============================================================

For all K cases：

freeze selected checkpoint path/SHA

selected iteration

selected TRAIN aggregate loss

TRAIN PSNR

TRAIN tau_eq Elog

convergence classification。

Output：

O2_selected_checkpoint_lock.csv。

Only after this lock exists
may TEST cameras be rendered。

Gate T4b。

============================================================
十六、Fresh Final TRAIN / TEST Render
============================================================

For each K case：

launch a fresh process。

Load selected O2 checkpoint。

Render:

16 TRAIN

8 TEST。

Save RGB float32 NPY
before scientific metric evaluation。

New root：

/data/wyh/DeformTransGS/experiments/
stage5_0_R4_O2_convergence_closure/
final_renders/

Expected arrays：

72。

Create：

O2_final_render_manifest.csv。

Require：

72 arrays。

Audit：

case key

checkpoint case

V2 GT case

camera ID。

Mismatch count：

0。

Gate T5a。

============================================================
十七、Independent Final Metrics
============================================================

Create：

stage5_R4_O2/metrics/
evaluate_O2_final.py。

It may load only：

C2 future J4 benchmark lock

O2 selected checkpoint lock

O2 final render manifest

prediction RGB NPY

V2 GT RGB

V2 valid optical masks。

Do not import training code。

Compute：

PSNR

SSIM

median tau_eq Elog

p90

p95

p99

factor2 fraction。

Output：

O2_metrics_per_camera.csv
O2_metrics_summary.csv。

Expected rows：

72。

============================================================
十八、Independent Metric Reproduction
============================================================

Second standalone script。

Select deterministic6 TEST rows
seed20260714。

At least1 from every K case。

Recompute directly：

PSNR

median tau_eq Elog。

Require：

PSNR max difference <=1e-10

tau_eq median error <=1e-12。

Output：

O2_metric_reproduction.csv。

Gate T5b。

============================================================
十九、Final Capacity Classification
============================================================

Use unchanged thresholds。

Per case PASS iff：

TEST PSNR >=28.0 dB

AND

median TEST tau_eq Elog <=0.25。

For K2：

if TRAIN passes both thresholds

and TEST fails either threshold

after TRAIN-CONVERGED-BY-PATIENCE：

classify：

CONFIRMED-GENERALIZATION-GAP。

If TRAIN passes both
and TEST fails
after TRAIN-TRUNCATED-AT-12000：

classify：

GENERALIZATION-GAP-AT-FINAL-BUDGET。

Do NOT call carrier TRAIN-FIT-INSUFFICIENT。

============================================================
二十、Final J4 Decision
============================================================

J4 PASS iff：

K0 PASS

K1 PASS

K2 PASS

T0-T5 PASS。

------------------------------------------------------------

If K0/K1 PASS

and K2 TEST FAIL

after TRAIN convergence/final budget：

J4 FAIL。

The representation has
static training-view fitting capacity
for K2 but lacks the required
novel-view canonical generalization
under the frozen carrier/protocol。

This is sufficient to block
Stage5.1 dynamic sufficiency study，
because release conclusions could be
confounded by the carrier's existing
undeformed novel-view error。

============================================================
二十一、No Further Carrier Rescue Rule
============================================================

This is the final RT carrier Gate。

After O2：

Do NOT：

increase Gaussian count

unlock xyz

train additional state families

add roughness

add reflectance

add material feature

add features_rest

add light_mlp

change learning rates

change scheduler

change loss

change benchmark

change train/test split

search a third carrier。

If corrected final J4 fails：

PRIMARY ATTRIBUTE-DEFORMATION LINE STOP。

Return to RecycleGS。

============================================================
二十二、Final CASE
============================================================

CASE RTSPLAT-PERSPECTIVE-V2-CARRIER-READY：

final J4 PASS。

AttributeDeformGS hypothesis：

UNTESTED-BUT-EXPERIMENTALLY-ADDRESSABLE。

ALLOW：

Stage5.1 dynamic native-state
sufficiency protocol design。

PRIMARY ATTRIBUTE-DEFORMATION LINE：

CONTINUE。

------------------------------------------------------------

CASE RTSPLAT-PERSPECTIVE-V2
-CONFIRMED-GENERALIZATION-GAP：

K0/K1 PASS。

K2 TRAIN passes。

K2 TEST fails
after final O2 convergence/budget closure。

Conclusion：

the current RT-native carrier
has insufficient canonical novel-view
generalization for the tinted wavy
transparent case。

AttributeDeformGS hypothesis remains：

UNTESTED。

PRIMARY ATTRIBUTE-DEFORMATION LINE：

STOP。

Return to RecycleGS。

------------------------------------------------------------

CASE RTSPLAT-PERSPECTIVE-V2
-TRAIN-FIT-INSUFFICIENT-CONFIRMED：

one or more cases fail TRAIN capacity
after final convergence closure。

STOP。

------------------------------------------------------------

CASE O2-PROVENANCE-FAIL：

optimizer resume mismatch

camera schedule mismatch

frozen geometry mutation

checkpoint reload failure

render case mismatch

metric reproduction failure。

No capacity conclusion。

============================================================
二十三、Required Outputs
============================================================

O2_protocol_lock.json
O2_resume_identity.csv
O2_optimizer_state_roundtrip.csv

O2_camera_schedule_reproduction.csv
O2_training_camera_schedule_4001_12000.csv

O2_initial_best_state.csv

O2_continuation_history/K0.csv
O2_continuation_history/K1.csv
O2_continuation_history/K2.csv

O2_full_train_convergence/K0.csv
O2_full_train_convergence/K1.csv
O2_full_train_convergence/K2.csv

O2_train_trend_diagnostic.csv
O2_geometry_state_freeze_audit.csv

O2_checkpoint_integrity.csv
O2_selected_checkpoint_lock.csv

final_renders/**/*.npy
O2_final_render_manifest.csv

O2_metrics_per_camera.csv
O2_metrics_summary.csv
O2_metric_reproduction.csv

O2_final_capacity_diagnostic.csv

stage5_0_R4_O2_convergence_report.md
stage5_0_R4_O2_convergence_summary.md
stage5_0_R4_O2_convergence_log.txt

============================================================
二十四、最终终端只输出
============================================================

A. T0

B. K0/K1/K2 resume checkpoint iterations

C. K0/K1/K2 resume checkpoint SHAs

D. Gaussian count at resume K0/K1/K2

E. footprint policy at resume

F. trainable state names at resume

G. optimizer group LRs at resume

H. optimizer internal step restored yes/no

I. optimizer exp_avg/exp_avg_sq
roundtrip max error

J. T1a

K. T1b

L. camera schedule iterations1-4000
mismatch count

M. first resumed camera IDs
K0/K1/K2

N. T2

O. initial best iteration K0/K1/K2

P. initial patience elapsed K0/K1/K2

Q. K0 continuation steps executed

R. K1 continuation steps executed

S. K2 continuation steps executed

T. K0 final executed iteration

U. K1 final executed iteration

V. K2 final executed iteration

W. K0 selected best iteration

X. K1 selected best iteration

Y. K2 selected best iteration

Z. K0 final patience elapsed

AA. K1 final patience elapsed

AB. K2 final patience elapsed

AC. K0 convergence classification

AD. K1 convergence classification

AE. K2 convergence classification

AF. K0 last100/500/1000
TRAIN loss relative change

AG. K1 last100/500/1000
TRAIN loss relative change

AH. K2 last100/500/1000
TRAIN loss relative change

AI. frozen xyz max change

AJ. excluded state max change

AK. Gaussian count min/max

AL. T3

AM. checkpoint persistent reload max error

AN. optimizer-state reload max error

AO. T4a

AP. selected checkpoint iterations
K0/K1/K2

AQ. selected TRAIN PSNR/tau_eq
K0

AR. selected TRAIN PSNR/tau_eq
K1

AS. selected TRAIN PSNR/tau_eq
K2

AT. T4b

AU. expected final RGB array count

AV. actual final RGB array count

AW. final render case-key mismatch count

AX. T5a

AY. independent metric row count

AZ. metric reproduction max PSNR error

BA. metric reproduction max tau_eq error

BB. T5b

BC. final K0 TRAIN PSNR/tau_eq

BD. final K0 TEST PSNR/tau_eq

BE. final K1 TRAIN PSNR/tau_eq

BF. final K1 TEST PSNR/tau_eq

BG. final K2 TRAIN PSNR/tau_eq

BH. final K2 TEST PSNR/tau_eq

BI. K0 final capacity classification

BJ. K1 final capacity classification

BK. K2 final capacity classification

BL. final J4

BM. Final CASE

BN. RT-native V2 canonical carrier ready yes/no

BO. scientific question experimentally addressable yes/no

BP. allow Stage5.1 design yes/no

BQ. AttributeDeformGS hypothesis status

BR. PRIMARY ATTRIBUTE-DEFORMATION LINE
STOP/CONTINUE

BS. KIOT status

BT. no-further-carrier-rescue rule active yes/no

BU. next exact research action

BV. report path

BW. summary path

============================================================
二十五、README
============================================================

Update README。

Record explicitly：

Stage5.0-R4-O1 proved that the original
R4 capacity failure was invalid。

The original jobs had a hard500-step cap，
despite iteration500 being a new
full-TRAIN best for every case。

The required P0/P1 footprint diagnostics
were not correctly used to select policy。

The original R4 optimizer assigned
a hardcoded1e-6 LR to all native states。

The locked RT source defines：

occupancy LR0.05

opacity LR0.05

transmissivity LR0.01

features_dc LR0.002。

O1 repaired the exact protocol errors
without LR search。

The valid footprint diagnostic selected：

NATIVE_FOOTPRINT。

The corrected trainable states are：

_scaling

_rotation

_occupancy

_opacity

_transmissivity

_features_dc。

Corrected K0 and K1 pass
the frozen J4 thresholds。

Corrected K2 TRAIN also passes：

PSNR28.3318

tau_eq Elog0.0557。

However K2 TEST remains below Gate：

PSNR26.1843

tau_eq Elog0.3368。

Therefore K2 is currently classified
GENERALIZATION-GAP，
not TRAIN-FIT-INSUFFICIENT。

All three jobs reached the4000-step limit
with the selected best checkpoint
at exactly iteration4000
and patience elapsed0。

The corrected training was therefore
still improving at the frozen maximum budget。

O2 is the FINAL convergence closure。

It resumes exact step4000 model
and optimizer states。

It does not reset the optimizer。

It does not change learning rates，
loss，
benchmark，
state list，
Gaussian count，
or geometry。

Continuation uses TRAIN only。

It stops after1000 steps
without a new full-TRAIN best
or at12000 total steps。

TEST is rendered only after
selected checkpoints are frozen。

If K2 retains a novel-view
generalization gap after O2，
the AttributeDeformGS primary line stops
for the current project
and research returns to RecycleGS。

No further carrier rescue is allowed。

============================================================
二十六、实际执行
============================================================

必须实际：

lock O1 corrected evidence

load exact O1 step4000 checkpoints

verify case/benchmark/policy metadata

verify4096 Gaussians

verify exact trainable state list

load exact optimizer states

prove optimizer exp_avg/exp_avg_sq
roundtrip error0

reconstruct deterministic camera schedule

prove iterations1-4000 schedule match O1

resume from camera schedule iteration4001

reconstruct best iteration4000
and patience0

continue exact optimizer state

do not change LR

do not change loss

do not reset optimizer

evaluate all16 TRAIN cameras every100 steps

select checkpoints from TRAIN only

continue until1000 steps since last best
or iteration12000

record every optimizer step

record every full-TRAIN aggregate evaluation

audit last100/500/1000 TRAIN trends

prove xyz remains frozen

prove excluded states remain frozen

prove Gaussian count4096

save full-state/optimizer checkpoints

freeze selected checkpoint before TEST

fresh reload selected checkpoint

render72 new final RGB arrays

audit case keys

run independent metrics

independently reproduce6 TEST rows

apply unchanged28/0.25 Gate

activate no-further-carrier-rescue rule

Final CASE

绝对不要：

use TEST to continue training

extend beyond12000

change LR

change loss

unlock xyz

increase Gaussian count

add states

run Stage5.1 before final J4。
PROMPT
```

## 我的真实判断

现在这条线其实比之前健康很多。

因为我们已经得到一个非常明确的结果：

> **简单平面透明场景 K0/K1，RT carrier 是能过的。**

甚至 K2 训练视角也过：

[
28.33\text{ dB},\quad0.0557
]

这说明真正的问题不是“透明 Gaussian 完全拟合不了 GT”。

问题集中成：

> **复杂有色波浪透明表面，RT-native 状态在未见观察方向上泛化不够。**

这其实已经开始碰到一个科学问题了。

但是，现在还不能把它直接写成论文创新，因为 K2 仍在 step 4000 更新 best。

**最后跑 O2。**

O2 后 K2 还不过，我建议坚决停 AttributeDeformGS 主线，回 RecycleGS。
