Stage 3.4B-R5A：Oracle Closure and Tail Co-Factor Audit

中文：

预言机闭环与重尾共因子审计

我建议这是最后一次 metric audit。

只解决：

O4 为什么 reconstruction diff=0.5；
low support、upper censor、boundary 三者到底是什么关系；
tail 应该归类 LOW-SUPPORT、UPPER-CENSORING 还是 MIXED。

这一步通过后，我就支持直接跑 P0/P1/P2/P3。

下面是详细指令。

cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式状态
============================================================

Stage 3.4B-R5 completed：

O0 Identity exact ratio:
PASS

O1 Algebra core:
PASS
max error = 2.22e-16

O2 Sample-level tau oracle:
PASS
q=1.0 / 0.8 / 2/3 / 0.5 / 4/9
all machine precision

O3 Constant-field image oracle:
PASS
max error = 0

O4 Pixelwise transformed-alpha
noncommutation reconstruction:
FAIL

reported max reconstruction difference:
0.5

CURRENT P0 central phenotype：

stretch_1.25:
median R = 0.7960
physical q = 0.8

stretch_1.50:
median R = 0.6573
physical q = 2/3

stretch_2.00:
median R = 0.4832
physical q = 0.5

uniform state-level Spearman:
1.0000

area-preserving：

shear_k020:
median R = 0.9991

shear_k040:
median R = 0.9954

twist_60:
median R = 0.9478

Therefore：

CENTRAL RESPONSE PHENOTYPE
is strongly supported。

However：

local response distributions
have severe heavy tails。

Examples：

stretch_2.00:
median log error = 0.198
p95 log error = 3.283
factor2 fraction = 0.255
factor5 fraction = 0.169

shear_k040:
median log error = 0.060
p95 log error = 3.698
factor2 fraction = 0.217
factor5 fraction = 0.125

twist_60:
median log error = 0.385
p95 log error = 5.534
factor2 fraction = 0.451
factor5 fraction = 0.327

Stage R5 reported：

Top1% tail support ratio = 0.0007

Top1% tail upper censor = 1.000

Top1% tail boundary = 1.000

but classified：

TAIL-LOW-SUPPORT。

This single-factor classification
is NOT yet accepted。

============================================================
严格 Gate 修正
============================================================

Stage R5 definition required：

R51 REVISED OPTICAL ORACLE：

O0-O4 ALL PASS。

Because：

O4 FAIL，

formal current status：

R51 = FAIL

not PASS。

However：

O0-O3 prove that
the metric core passes
three mathematically exact oracle levels。

Therefore：

do NOT restart metric design。

Only audit O4 closure。

------------------------------------------------------------

R54 tail origin：

cannot be classified
TAIL-LOW-SUPPORT only

until the meanings of：

upper_censor = 1.000

boundary = 1.000

are explicitly defined and decomposed。

If multiple predefined origin conditions hold：

classification must be：

TAIL-MIXED。

============================================================
本阶段名称
============================================================

Stage 3.4B-R5A:

Oracle Closure and Tail Co-Factor Audit

中文：

预言机闭环
与局部重尾共因子审计

============================================================
一、唯一目标
============================================================

Q1.

Why did O4 error reconstruction
produce max difference = 0.5？

Find the exact first sample/cell/camera
where：

E_observed

differs from：

mean(gap)/mean(tau_can)。

Q2.

Do low canonical support，
deformed alpha upper clipping，
and boundary status
co-occur in the same tail samples？

Q3.

Assign one correct tail classification：

TAIL-LOW-SUPPORT

TAIL-UPPER-CENSORING

TAIL-CAMERA-DISAGREEMENT

TAIL-SPATIAL-RENDER-RESPONSE

TAIL-MIXED

TAIL-UNRESOLVED。

禁止：

P1
P2
P3

covariance mechanism

method design

threshold tuning

tail filtering。

============================================================
二、输出目录
============================================================

所有输出：

/data/wyh/DeformTransGS/experiments/
stage3_4B_R5A_oracle_tail_cofactor/

禁止覆盖 R5。

============================================================
三、Protocol Lock
============================================================

读取：

stage3_4B_R5_oracle_tail_audit/
r5_protocol_lock.json

重新验证：

carrier SHA

metric source SHA

projection SHA

extension SHA

camera SHA。

输出：

r5a_protocol_lock.json

如果 mismatch：

停止。

============================================================
四、重新实现 O4 Cell-Camera Identity Equation
============================================================

必须在同一个函数中同时计算：

tau_can_k

expected_tau_def_k

actual_tau_def_k

gap_k

R_actual

E_observed

E_predicted。

禁止：

E_observed 从旧 CSV 读取。

禁止：

E_predicted 从另一 pipeline 读取。

定义：

```python
from __future__ import annotations

import numpy as np


def audit_o4_cell_camera(
    tau_can: np.ndarray,
    actual_tau_def: np.ndarray,
    q: float,
    valid_mask: np.ndarray,
) -> dict[str, float]:
    tau_can = np.asarray(
        tau_can,
        dtype=np.float64,
    )

    actual_tau_def = np.asarray(
        actual_tau_def,
        dtype=np.float64,
    )

    valid = np.asarray(
        valid_mask,
        dtype=bool,
    )

    valid = (
        valid
        & np.isfinite(tau_can)
        & np.isfinite(actual_tau_def)
    )

    if valid.sum() == 0:
        return {
            "valid_count": 0,
            "mean_tau_can": np.nan,
            "mean_tau_def": np.nan,
            "R_actual": np.nan,
            "E_observed": np.nan,
            "mean_gap": np.nan,
            "E_predicted": np.nan,
            "closure_error": np.nan,
        }

    tc = tau_can[valid]
    td = actual_tau_def[valid]

    expected_td = (
        float(q) * tc
    )

    gap = (
        td - expected_td
    )

    mean_tc = float(
        np.mean(tc)
    )

    mean_td = float(
        np.mean(td)
    )

    if mean_tc <= 0.0:
        return {
            "valid_count": int(valid.sum()),
            "mean_tau_can": mean_tc,
            "mean_tau_def": mean_td,
            "R_actual": np.nan,
            "E_observed": np.nan,
            "mean_gap": float(np.mean(gap)),
            "E_predicted": np.nan,
            "closure_error": np.nan,
        }

    R_actual = (
        mean_td / mean_tc
    )

    E_observed = (
        R_actual - float(q)
    )

    mean_gap = float(
        np.mean(gap)
    )

    E_predicted = (
        mean_gap / mean_tc
    )

    closure_error = (
        E_observed - E_predicted
    )

    return {
        "valid_count": int(valid.sum()),
        "mean_tau_can": mean_tc,
        "mean_tau_def": mean_td,
        "R_actual": float(R_actual),
        "E_observed": float(E_observed),
        "mean_gap": mean_gap,
        "E_predicted": float(E_predicted),
        "closure_error": float(closure_error),
    }

Mathematically：

mean(td)/mean(tc) - q

=

mean(td-q*tc)/mean(tc)。

Therefore closure_error
should be float64 numerical scale。

============================================================
五、O4 Valid-Mask Identity Audit

Current suspicion：

R5 may have computed：

R_actual

with one valid mask

but：

gap / E_predicted

with another valid mask。

For every cell-camera：

record SHA256 or packed-bit hash for：

canonical projection valid

oracle projection valid

canonical bilinear valid

oracle bilinear valid

finite tau valid

R aggregation valid mask

gap aggregation valid mask。

输出：

o4_valid_mask_identity.csv

字段：

q
cell_id
camera_id

r_mask_count
gap_mask_count

mask_equal

mask_mismatch_count

r_mask_sha
gap_mask_sha。

Gate：

all mask_equal = True。

If not：

first mismatch
is O4 root cause candidate。

============================================================
六、O4 First Closure Failure Trace

正式 q：

0.8
2/3
0.5
4/9。

运行全部 cell-camera。

输出：

o4_closure_all_rows.csv

字段：

q
cell_id
camera_id

valid_count

mean_tau_can
mean_tau_def

R_actual
E_observed

mean_gap
E_predicted

closure_error

mask_equal。

定义 failure：

abs(closure_error) >1e-10。

按：

abs closure_error

降序。

输出：

o4_top_closure_failures.csv

取 top 100。

必须报告：

FIRST FAILURE：

q
cell_id
camera_id

tau array shape

valid count

R_actual

E_observed

mean_gap

E_predicted

closure error

mask equality。

============================================================
七、Sample-Level O4 Trace

对于：

top 10 closure failure cell-camera

保存每 sample：

sample_id

u
v

p_x
p_y

A_can_sample
A_q_sample

tau_can

expected_tau_def

actual_tau_def

gap

r_valid
gap_valid。

输出：

o4_failure_sample_trace.csv

禁止只写 summary。

============================================================
八、O4 Closure Gate

修复 confirmed implementation bug
only in R5 diagnostic code。

禁止修改 formal metric。

修复后：

O4 closure：

median abs closure <=1e-13

p95 <=1e-12

max <=1e-10。

如果 PASS：

O4 PASS。

R51 REVISED OPTICAL ORACLE PASS。

如果 FAIL：

R51 FAIL。

禁止 P1/P2/P3。

============================================================
九、Tail Metric Source Lock

读取 R5：

p0_tail_feature_table.csv

p0_cross_camera_tail_trace.csv。

验证：

state/cell/camera unique key。

KEY：

[
"state",
"cell_id",
"camera_id",
]

输出：

tail_input_key_audit.csv

禁止重新计算新 tail
until key lock passes。

============================================================
十、明确 Upper Censor 定义

R5 alpha_to_tau：

alpha clip upper：

1.0 - 1e-6。

Formal upper censor sample：

A >= 1.0 - 1e-6。

Formal cell-camera upper-censored：

upper_clip_fraction > 0。

定义：

UPPER_ALPHA_LIMIT = (
    1.0 - 1e-6
)

sample_upper_censored = (
    alpha_def_samples
    >= UPPER_ALPHA_LIMIT
)

upper_clip_fraction = float(
    sample_upper_censored.mean()
)

cell_camera_upper_censored = (
    upper_clip_fraction > 0.0
)

不要使用：

A>0.9

A>0.99

作为 formal censor。

它们只能 secondary diagnostic。

输出：

upper_censor_definition.md

============================================================
十一、明确 Boundary 定义

必须定位 R5：

Top1% tail boundary = 1.000

到底使用哪个变量。

输出：

boundary_statistic_semantics.md

必须回答：

变量名

数据类型

1 的含义

0 的含义。

如果变量是：

boundary_pass

则：

boundary=1.000

表示：

100% PASS boundary Gate

NOT：

100% boundary-dominated。

Formal boundary-near definition：

必须来自 current boundary distance。

使用 CURRENT protocol
already-defined required margin：

BOUNDARY_MARGIN。

定义：

boundary_near =

boundary_distance
<
BOUNDARY_MARGIN。

定义：

boundary_pass =

boundary_distance

=
BOUNDARY_MARGIN。

禁止新增 5px/10px threshold。

输出：

BOUNDARY_MARGIN exact value
and source line。

============================================================
十二、Tail Sets

正式 audit states：

stretch_2.00

cubic_l0333

shear_k040

twist_60。

使用：

cell-camera E_log。

每 state：

TAIL_LOG_1PCT：

top 1%
by E_log。

TAIL_LOG_5PCT：

top 5%。

CONTROL_MEDIAN：

equal-size samples
closest to median E_log。

CONTROL_LOW：

equal-size samples
closest to p10 E_log。

输出：

tail_cofactor_manifest.csv

============================================================
十三、Cofactor Feature Table

对每 sample group：

ALL

TAIL_LOG_1PCT

TAIL_LOG_5PCT

CONTROL_MEDIAN

CONTROL_LOW

统计：

n

tau_cell_can median

tau_support_ratio median

tau_support_ratio p90

upper_censored fraction

upper_clip_fraction median

A_def >0.9 fraction median

A_def >0.99 fraction median

boundary_near fraction

boundary_pass fraction

boundary_distance median

camera_log_spread median

bad_camera_factor2_count mean

valid_sample_fraction median。

输出：

tail_cofactor_group_statistics.csv

============================================================
十四、Cofactor Co-Occurrence

对每 tail row
定义 binary：

LOW_SUPPORT：

tau_cell_can
<=
0.1 * ALL pooled median tau_cell_can。

UPPER_CENSOR：

upper_clip_fraction >0。

BOUNDARY_NEAR：

boundary_near。

CAMERA_DISAGREE：

camera_log_spread >=log(5)。

输出：

tail_cofactor_cooccurrence.csv

统计 16 种组合：

LOW
UPPER
BOUNDARY
CAMERA。

例如：

1000
1100
0100
1101
etc。

字段：

state
tail_group
low_support
upper_censor
boundary_near
camera_disagree
count
fraction。

重点报告 top1%。

============================================================
十五、Conditional Tail Severity

对于 all rows
按 cofactor group：

LOW only

UPPER only

LOW+UPPER

BOUNDARY only

CAMERA only

LOW+UPPER+BOUNDARY

NONE

OTHER MIXED

计算：

n

median E_log
p90
p95
p99

median factor_error。

输出：

tail_severity_by_cofactor.csv

目的：

判断最大 tail
是否主要来自：

LOW alone

or

LOW + UPPER。

============================================================
十六、Low-Support Necessity/Sufficiency

计算：

P(TAIL1 | LOW)

P(TAIL1 | NOT LOW)

P(LOW | TAIL1)

其中：

TAIL1 =
belongs to pooled top1% E_log。

同样计算：

UPPER。

以及：

LOW+UPPER。

输出：

tail_factor_conditional_probabilities.csv

不要做 causal claim。

只判断 association。

============================================================
十七、Matched-Support Comparison

为了区分：

LOW support 本身

vs

LOW+UPPER interaction，

建立 tau_support_ratio bins：

log10 ratio bins：

[-inf,-3)
[-3,-2)
[-2,-1)
[-1,0)
[0,1)
[1,2)
[2,inf)

每 bin：

UPPER_CENSOR=True

vs False。

计算：

n
median E_log
p90
p95。

输出：

matched_support_upper_censor.csv

如果同 support bin 内：

UPPER group
median E_log

=2x

non-UPPER group

in at least 3 populated bins
(each n>=30)：

UPPER COFACTOR SUPPORTED。

============================================================
十八、Boundary Cofactor Audit

同样：

boundary_near True/False

within matched support bins。

输出：

matched_support_boundary.csv

如果 no boundary-near samples
because current validity requires boundary_pass：

明确：

BOUNDARY CANNOT EXPLAIN TAIL
UNDER CURRENT VALID SET。

============================================================
十九、Camera Disagreement Audit

对于 tail cells：

记录：

exactly 0
1
2
3

bad cameras
with factor_error>2。

如果：

=75% tail cells

have exactly one bad camera

and cross-camera median removes it，

then camera disagreement
does not explain cell-level tail。

如果：

=50% tail cells
have >=2 bad cameras，

camera disagreement can contribute
to R_cell tail。

输出：

tail_bad_camera_count_distribution.csv

============================================================
二十、Spatial Clustering Re-evaluation

R5 spatial clustering：

tail neighbour fraction =0.200

random：

0.0220 +-0.0327

+3std =0.1202。

PASS。

Now recompute separately for：

LOW-only tail cells

LOW+UPPER tail cells

UPPER-only tail cells。

输出：

tail_spatial_clustering_by_cofactor.csv

目的：

判断 spatial clustering
来自哪一 cofactor population。

============================================================
二十一、Correct Tail Classification

TAIL-LOW-SUPPORT：

P(LOW | TAIL1) >=0.75

AND

UPPER COFACTOR NOT SUPPORTED

AND

camera/boundary not explanatory。

TAIL-UPPER-CENSORING：

P(UPPER | TAIL1)>=0.75

AND

LOW support not dominant：

P(LOW | TAIL1)<0.50。

TAIL-MIXED-LOW-UPPER：

P(LOW | TAIL1)>=0.75

AND

P(UPPER | TAIL1)>=0.75

AND

UPPER COFACTOR SUPPORTED
within matched support bins。

TAIL-LOW-SUPPORT-WITH-CENSOR-COOCCURRENCE：

P(LOW | TAIL1)>=0.75

P(UPPER | TAIL1)>=0.75

but matched-support test
does NOT support additional UPPER effect。

解释：

upper censor co-occurs
with low-support tail
but does not explain extra severity
after matching support。

TAIL-CAMERA-DISAGREEMENT：

=50% cell-level tail
has >=2 bad cameras

and median camera spread >=log(5)。

TAIL-SPATIAL-RENDER-RESPONSE：

low/upper/camera/boundary
cannot explain tail

and clustering PASS。

TAIL-MIXED：

multiple other factors supported。

TAIL-UNRESOLVED：

none。

============================================================
二十二、Strict Gates

R5A0:
PROTOCOL LOCK

PASS。

R5A1:
O4 CLOSURE

PASS。

R5A2:
REVISED OPTICAL ORACLE

O0-O4 PASS。

R5A3:
TAIL COFACTOR SEMANTICS

upper censor definition locked

boundary semantic locked

co-occurrence computed。

R5A4:
TAIL ORIGIN CLASSIFICATION

one valid classification
according to Section 21。

============================================================
二十三、Final CASE

CASE READY-FOR-SHAPE-POLICY:

R5A0 PASS
R5A1 PASS
R5A2 PASS
R5A3 PASS
R5A4 PASS。

Conclusion：

The CURRENT optical metric
is mathematically validated
under exact oracle tests。

P0 FIXED_COV exhibits：

physically consistent
central area-dilution response
reproducible local multiplicative heavy tails

with tail origin classified as：

<ACTUAL CLASS>。

Stage3.4B may now compare：

P0 FIXED_COV

P1 RIGID_COV

P2 FULL_AFFINE_COV

P3 FULL_AFFINE_ORACLE。

The scientific question is：

How does Gaussian covariance transport
change：

A. central area-dilution response

and

B. local optical-response tail severity/distribution？

CASE O4-UNRESOLVED:

R5A1 FAIL。

禁止 shape policies。

CASE TAIL-UNRESOLVED:

R5A2 PASS

but R5A4 FAIL。

禁止 shape policies。

============================================================
二十四、报告

必须输出：

r5a_protocol_lock.json

o4_valid_mask_identity.csv
o4_closure_all_rows.csv
o4_top_closure_failures.csv
o4_failure_sample_trace.csv

upper_censor_definition.md
boundary_statistic_semantics.md

tail_input_key_audit.csv
tail_cofactor_manifest.csv
tail_cofactor_group_statistics.csv
tail_cofactor_cooccurrence.csv
tail_severity_by_cofactor.csv

tail_factor_conditional_probabilities.csv
matched_support_upper_censor.csv
matched_support_boundary.csv
tail_bad_camera_count_distribution.csv

tail_spatial_clustering_by_cofactor.csv

oracle_closure_tail_cofactor_report.md
stage3_4B_R5A_summary.md
stage3_4B_R5A_log.txt

============================================================
二十五、报告必须中文回答

A. 为什么 R5 R51
严格来说不能 PASS。

B. O4 closure error=0.5
的 exact implementation root cause。

C. R mask vs gap mask
是否相同。

D. first closure failure。

E. 修复后 O4 median/p95/max closure error。

F. R5A1。

G. R5A2。

H. upper censor exact definition。

I. boundary=1.000
在 R5 中实际是什么意思。

J. BOUNDARY_MARGIN。

K. P(LOW | TAIL1)。

L. P(UPPER | TAIL1)。

M. P(BOUNDARY_NEAR | TAIL1)。

N. P(CAMERA_DISAGREE | TAIL1)。

O. top1% 最常见 cofactor combination。

P. LOW-only median/p95 E_log。

Q. UPPER-only median/p95 E_log。

R. LOW+UPPER median/p95 E_log。

S. P(TAIL1 | LOW)。

T. P(TAIL1 | UPPER)。

U. P(TAIL1 | LOW+UPPER)。

V. matched-support UPPER
是否增加 tail severity。

W. matched-support boundary
是否增加 severity。

X. tail bad-camera count distribution。

Y. spatial clustering
主要来自哪个 cofactor population。

Z. final tail classification。

AA. R5A0。

AB. R5A1。

AC. R5A2。

AD. R5A3。

AE. R5A4。

AF. Final CASE。

AG. 是否允许 P1/P2/P3。

AH. 修正后的 Stage3.4B 科学问题。

============================================================
二十六、README

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage3.4B-R5 passed
three mathematically exact oracle levels：

exact identity ratio，

sample-level optical-depth oracle，

constant-field image oracle。

It also showed that
P0 FIXED_COV has a physically consistent
central area-dilution phenotype
but severe local multiplicative heavy tails。

However，
R5 formally marked R51 PASS
despite O4 closure failure，
which violated the predefined
O0-O4 oracle Gate。

R5 also classified the tail
as LOW-SUPPORT only，
although the reported top1% tail statistics
simultaneously showed：

tau support ratio =0.0007，

upper censor statistic =1.000，

boundary statistic =1.000。

Stage3.4B-R5A closes the O4 identity exactly
and decomposes co-occurring tail factors
before any covariance-transport policy comparison。

============================================================
二十七、实际执行

必须实际：

lock R5 protocol

recompute O4 in one function

audit valid mask identity

trace first closure failure

fix R5 diagnostic-only bug

close O4 equation

lock upper censor semantics

lock boundary statistic semantics

audit tail unique keys

construct tail/control sets

compute cofactor statistics

compute co-occurrence table

conditional probabilities

matched-support upper audit

matched-support boundary audit

camera disagreement

cofactor-specific spatial clustering

assign exact tail class

R5A0-R5A4

Final CASE

不要运行 P1。

不要运行 P2。

不要运行 P3。

不要 covariance mechanism。

不要 tail filtering。

不要 modify thresholds。

不要修改 TSGS。

============================================================
二十八、最终终端总结

最后只输出：

O4 exact root cause
R/gap valid masks identical yes/no
O4 closure median/p95/max after fix
Revised oracle PASS/FAIL
Upper censor exact semantic
R5 boundary=1.000 exact semantic
Boundary margin
P(LOW|TAIL1)
P(UPPER|TAIL1)
P(BOUNDARY_NEAR|TAIL1)
P(CAMERA_DISAGREE|TAIL1)
Most common tail cofactor combination
LOW-only median/p95 Elog
UPPER-only median/p95 Elog
LOW+UPPER median/p95 Elog
P(TAIL1|LOW)
P(TAIL1|UPPER)
P(TAIL1|LOW+UPPER)
Matched-support upper effect supported yes/no
Matched-support boundary effect supported yes/no
Tail bad-camera count distribution
Cofactor spatial clustering result
Final tail classification
R5A0
R5A1
R5A2
R5A3
R5A4
Final CASE
Can run P1/P2/P3 yes/no
Revised scientific question
oracle_closure_tail_cofactor_report.md path
stage3_4B_R5A_summary.md path

请直接实际运行。

最高优先级：

DO NOT OVERRIDE THE O0-O4 GATE。

O4 CLOSURE IS AN ALGEBRAIC IDENTITY。

A 0.5 ERROR MEANS THE DIAGNOSTIC IMPLEMENTATION IS WRONG。

DO NOT CALL ALL TOP-TAIL EFFECTS
LOW-SUPPORT
WHEN UPPER_CENSOR AND BOUNDARY
STATISTICS ARE ALSO 1.000。

FIRST DEFINE WHAT THOSE 1.000 VALUES MEAN。

COMPUTE CO-OCCURRENCE。

MATCH SUPPORT BEFORE ATTRIBUTING
AN ADDITIONAL UPPER-CENSOR EFFECT。

THIS IS THE LAST METRIC AUDIT
BEFORE SHAPE POLICY COMPARISON。
PROMPT