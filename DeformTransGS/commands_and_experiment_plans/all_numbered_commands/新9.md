第一条指令不要直接写模型，也不要急着训练。

先让 Codex 做一个**新主线立项 Gate**：

> 把我们已经做过的 AttributeDeformGS 有效资产全部审计清楚，然后判断“表面附着式形变光学传输”是否能在现有 C2-V2 基准上形成一个独立、可执行的最小实验。

直接复制：

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
你现在继续维护独立科研项目：

/data/wyh/DeformTransGS

============================================================
新研究主线候选
============================================================

Working name：

Deformable Optical Transport GS

中文暂定：

半透明 Gaussian 表面的形变光学传输研究

核心科学问题：

当一个半透明表面发生已知几何形变时，
仅仅传输 Gaussian 的位置、尺度和旋转是否足够？

如果不足：

哪些“与表面形变有关的局部几何状态”
足以解释形变前后光学传输响应的变化？

候选形变量包括但不限于：

surface area ratio Js
表面积变化率

principal stretches lambda1/lambda2
两个主拉伸

in-plane shear
面内剪切

local normal change
局部法线变化

local tangent-frame change
局部切平面坐标系变化

curvature change
曲率变化。

注意：

本项目不再首先询问：

RT-Splatting 的 _opacity 是否必须变化

_transmissivity 是否必须变化

_features_dc 是否必须变化。

新的研究单位是：

DEFORMATION-CONDITIONED OPTICAL TRANSPORT

形变条件光学传输。

目标是研究：

什么最小形变状态 q(F)
足以描述 canonical optical response
到 deformed optical response 的变化。

形式化写为：

T' = G(T0, v_local, q(F))

其中：

T0：
canonical optical response

v_local：
形变后局部材料坐标系中的观察方向

q(F)：
由局部形变梯度 F 导出的形变量。

============================================================
最高优先级
============================================================

本阶段只执行：

Stage D0：
Research Asset and Feasibility Closure

中文：

已有实验资产与新问题可执行性闭环。

不要训练模型。

不要设计最终网络。

不要创建 MLP。

不要运行新的 Gaussian 优化。

不要运行 RT-Splatting canonical training。

不要继续救旧 AttributeDeformGS。

不要执行 RecycleGS。

本阶段只回答：

现有项目是否已经拥有足够的数据和精确对应关系，
可以直接构造：

canonical material point
-> exact deformation
-> deformed material point
-> local deformation gradient F
-> local frame
-> canonical/deformed camera ray
-> canonical/deformed optical GT

这一一对应的分析数据集？

如果 YES：

新的 deformable optical transport
最小验证实验可以继续。

如果 NO：

必须明确缺少什么，
不能凭空补数据。

============================================================
已有正式科学结论
============================================================

AttributeDeformGS old primary line：

STOP。

Final accepted case：

RTSPLAT-PERSPECTIVE-V2
-CONFIRMED-GENERALIZATION-GAP。

原因：

K0/K1 canonical capacity PASS。

K2 TRAIN capacity PASS，
but TEST novel-view capacity FAIL。

K2 final selected iteration：

11900。

K2 TRAIN：

PSNR =
29.051451670419816

median tau_eq Elog =
0.03238577023148537。

K2 TEST：

PSNR =
25.571056657568235

median tau_eq Elog =
0.3729565888643265。

Therefore：

the original per-native-state
necessity/sufficiency experiment
is blocked by undeformed novel-view error。

AttributeDeformGS hypothesis：

UNTESTED。

Do NOT reinterpret this as：

opacity unnecessary

transmissivity unnecessary

appearance unnecessary。

============================================================
可保留资产
============================================================

1. RT native state source audit

Verified source-persistent states include：

_xyz

_features_dc

_features_rest

_scaling

_rotation

_occupancy

_opacity

_transmissivity

_roughness

_reflectance

_language_feature。

Verified control-valid states：

_occupancy

_opacity

_transmissivity

_features_dc。

These states are：

render-active

gradient-active

autograd-valid

checkpoint-restorable。

This evidence may be used only as
representation/source background。

Do not restart the old release experiment。

------------------------------------------------------------

2. Perspective Thin-Transmission Benchmark V2

Benchmark：

PERSPECTIVE-THIN-TRANSMISSION-V2

Version：

C2-V2。

Root：

/data/wyh/DeformTransGS/experiments/
stage5_0_R3_C2_perspective_v2_validity/
perspective_clean_gt_v2

Exact future benchmark lock：

/data/wyh/DeformTransGS/experiments/
stage5_0_R3_C2_perspective_v2_validity/
C2_future_J4_benchmark_lock.json

V2 has：

1008 unique views。

Case grid：

2 surfaces

x

3 materials

x

7 deformations

x

24 cameras。

Surfaces：

S0_PLANAR_SHEET

X(u,v) =
[u,v,0]。

S1_WAVY_MEMBRANE

X(u,v) =
[
u,
v,
0.18*sin(pi*u)*sin(pi*v)
]。

Materials：

MAT0

sigma_rgb =
[1.2,1.2,1.2]

fixed h0。

MAT1

sigma_rgb =
[1.2,1.2,1.2]

h' =
h0/Js。

MAT2

sigma_rgb =
[0.6,1.2,2.0]

h' =
h0/Js。

h0 =
0.08。

Deformations：

D0-D6。

The exact deformation definitions
must be traced from source。

Do not reconstruct them from memory。

------------------------------------------------------------

3. Verified physical equations

Surface Jacobian：

Js =
abs(det(F))
*
||F^-T n||。

Deformed normal：

n' =
normalize(F^-T n)。

Optical path：

cos_theta =
max(
abs(dot(n',-d)),
0.15
)。

tau_rgb =
sigma_rgb * h' / cos_theta。

RGB：

I_gt =
exp(-tau_rgb)。

White background。

A_gt：

diagnostic only。

Primary optical observable：

tau_eq_rgb =
-log(clamp(I_rgb,1e-6,1))。

------------------------------------------------------------

4. Verified V2 camera system

24 perspective cameras。

512x512。

FoVy：

75 degrees。

Camera radial scale：

1.09。

Camera radius：

3.597。

Original orbit directions preserved。

V2 RT ray compatibility：

PASS。

World-hit projection closure：

PASS。

Independent geometry/optical replay：

PASS。

tau_eq closure：

PASS。

Material landmark image motion：

median approximately61.15 pixels

max approximately224.02 pixels。

Therefore V2 is a genuine
perspective multi-view benchmark。

------------------------------------------------------------

5. Old V1 status

Old clean GT V1：

MATERIAL-GRID-OPTICAL-MAP。

V1 is valid only for：

material-grid optical semantics。

It is NOT a perspective image benchmark。

Do not use V1 PSNR
for perspective renderer capacity claims。

============================================================
新主线必须遵循的科学约束
============================================================

新问题不以 novel-view reconstruction
作为第一主实验。

第一主实验研究：

在 exact known material identity
和 exact known deformation correspondence 下，

canonical optical response

与

deformed optical response

之间的变化，

是否可以由局部形变量解释。

Primary comparison必须基于：

same material point identity。

因此核心数据单位必须是：

material sample i

surface

material

deformation

camera

canonical u/v

canonical triangle ID

canonical barycentric

canonical xyz

deformed xyz

canonical local frame

deformed local frame

F

Js

principal stretches

shear diagnostic

canonical normal

deformed normal

camera ray

local view direction

canonical tau_rgb

deformed tau_rgb

canonical RGB

deformed RGB。

本阶段只检查这些量能否可靠获得。

============================================================
绝对禁止事项
============================================================

禁止：

重新训练 RT-Splatting

继续 RT carrier rescue

增加 Gaussian 数量

解锁 xyz

attribute release

opacity oracle

transmissivity oracle

features_dc oracle

设计最终 release groups

训练 deformation MLP

训练 transport MLP

训练 Transformer

训练 Mamba

训练 neural field

使用 TEST PSNR 选择方法

KIOT 作为默认答案

默认 Js 是唯一关键状态

默认 opacity 是物理消光

默认 transmissivity 是真实材料透射率

直接声明论文创新

直接声明 nobody has done this

修改 C2-V2 GT

重新生成 V2

修改物理方程

修改 camera

修改 deformation。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stageD0_deformable_optical_transport_feasibility/

Create source：

/data/wyh/DeformTransGS/
deformable_optical_transport/

Create：

deformable_optical_transport/
    provenance/
    benchmark/
    material_identity/
    deformation/
    local_frame/
    optical_response/
    analysis_table/
    audit/
    run_D0.py

Do not overwrite old experiments。

============================================================
一、D0 Protocol Lock
============================================================

Locate and SHA256 lock：

C2 protocol lock

C2 final report

C2 final summary

C2 future J4 benchmark lock

C2 V2 GT manifest

V2 generator source

V2 independent optical replay source

surface definition source

material definition source

deformation definition source

camera pose lock

camera intrinsic lock

camera split lock

Stage5.0-R4-O2 final report

Stage5.0-R4-O2 final summary

R3-G1 repaired state-control report

RT source-state inventory。

Output：

D0_protocol_lock.json。

Gate D0-G0。

============================================================
二、Trace Exact Benchmark Case Grid
============================================================

From the actual C2-V2 manifest
and generator source：

enumerate every exact：

surface

material

deformation

camera ID。

Require：

1008 unique case/view combinations。

Determine the exact case naming convention。

Output：

D0_case_grid.csv。

Fields：

surface_key

material_key

deformation_key

camera_id

case_root

RGB path

tau path

triangle ID path

barycentric path

world-hit path

normal path

Js path

ray-direction path

valid-mask path。

Do not fabricate a path
if an array was not saved。

Use：

NOT_SAVED。

Gate D0-G1。

============================================================
三、Trace Exact Deformation Definitions
============================================================

Locate exact D0-D6 implementation。

For every deformation record：

deformation key

Chinese explanation

implemented function

source file

source function

parameters

whether globally affine

whether F is spatially constant

whether F depends on x/u/v

whether analytic Jacobian exists

whether autograd Jacobian is available。

For affine deformation：

write exact3x3 F。

For nonlinear deformation：

write the exact implemented mapping：

phi(x)

and identify how the local Jacobian：

F =
d phi / d x

can be evaluated。

Do not use previous report text
when source exists。

Output：

D0_deformation_inventory.csv
D0_deformation_equations.md。

Gate D0-G2。

============================================================
四、Canonical Material Identity Availability
============================================================

Audit whether V2 saved enough information
to recover a stable material identity
for every valid hit。

Check：

triangle ID

barycentric coordinates

canonical surface definition。

For a deterministic sample of：

100000 valid V2 hits

seed20260714

reconstruct canonical material identity：

triangle ID

barycentric

canonical xyz

canonical u/v
where recoverable。

For the same：

surface

material

camera

pixel

compare D0 and D1-D6 records。

Important：

the same pixel does NOT need to hit
the same material point after deformation。

Therefore do NOT establish correspondence
by pixel coordinates。

Instead determine whether a canonical
material point can be replayed forward
through each deformation phi。

Output：

D0_material_identity_recoverability.csv。

Report：

fraction with triangle ID

fraction with valid barycentric

fraction with canonical xyz reconstructable

fraction with canonical u/v reconstructable。

Gate D0-G3 PASS iff：

triangle+barycentric can reconstruct
canonical surface position
for >=99.999% of sampled valid hits。

============================================================
五、Build Canonical Surface Sample Set
============================================================

Create a new ANALYSIS SAMPLE SET。

This is NOT a Gaussian carrier。

Use deterministic area-weighted sampling
on the canonical surfaces。

Seed：

20260714。

Sample exactly：

4096 material points from S0

4096 material points from S1。

For every point save：

sample ID

surface ID

canonical triangle ID

barycentric coordinates

u

v

canonical xyz。

For S0/S1 use exact surface definitions。

Output：

D0_material_samples/
S0.csv

D0_material_samples/
S1.csv。

The same sample IDs must be used
for all materials，
deformations，
and cameras
on the same surface。

Gate D0-G4。

============================================================
六、Exact Deformation Replay
============================================================

For every4096 material sample
and every D0-D6：

compute：

deformed xyz =
phi(canonical xyz)。

Compute local deformation gradient：

F =
d phi / d x

at the canonical sample。

Use analytic Jacobian
when exact source-supported analytic Jacobian exists。

Otherwise use torch autograd
on the exact deformation implementation。

Do not use finite difference
as the primary F source。

For every sample/deformation save：

sample ID

surface

deformation

canonical xyz

deformed xyz

F00-F22

det(F)。

Output：

D0_deformation_replay.csv。

============================================================
七、Independent F Validation
============================================================

For every deformation：

select deterministic1024 samples。

Compare primary F against
an independent central finite-difference Jacobian。

Use multiple epsilon values：

1e-3

3e-4

1e-4

3e-5。

Use float64 deformation evaluation
where implementation permits。

Find a stable finite-difference plateau。

Report：

F absolute error

relative error

det(F) error。

Output：

D0_F_validation.csv。

Gate D0-G5 PASS iff：

for every deformation：

F p99 relative error <=1e-5

and

F max relative error <=1e-3

or exact zero-error analytic equivalence。

If a deformation fails：

STOP and report exact deformation。

============================================================
八、Local Surface Frame Construction
============================================================

For every canonical sample：

derive exact surface partial derivatives：

dX/du

dX/dv。

Construct canonical frame：

t1 =
normalize(dX/du)

n =
normalize(
cross(dX/du,dX/dv)
)

t2 =
normalize(
cross(n,t1)
)。

Frame：

B =
[t1,t2,n]。

For every deformation：

transport tangents through F：

a =
F t1

b =
F t2。

Construct deformed frame：

t1' =
normalize(a)

n' =
normalize(
cross(a,b)
)

t2' =
normalize(
cross(n',t1')
)。

Also independently compute：

n_FinvT =
normalize(F^-T n)。

Resolve sign consistently
using positive dot product
with the transported cross-product normal。

Compare：

n'
vs
n_FinvT。

Output：

D0_local_frame_table.csv
D0_normal_transport_validation.csv。

Gate D0-G6 iff：

normal angular p99 <=1e-5 degrees

max <=1e-3 degrees。

============================================================
九、Compute Deformation Descriptors
============================================================

For every sample/deformation：

compute：

Js =
abs(det(F))
*
||F^-T n||。

Construct tangent-plane deformation map。

Let：

A =
[
dot(F t1,t1')
dot(F t2,t1')

dot(F t1,t2')
dot(F t2,t2')
]。

Compute singular values：

lambda1 >= lambda2 >=0。

Save：

lambda1

lambda2。

Compute：

area_from_stretch =
lambda1*lambda2。

Compare against Js。

Also compute deterministic shear descriptors：

C =
A^T A。

Save：

C11

C12

C22。

Define shear diagnostic：

gamma =
abs(C12)
/
sqrt(max(C11*C22,1e-12))。

This gamma is a diagnostic descriptor，
not yet a physical law。

Compute：

normal rotation angle

t1 rotation angle

t2 rotation angle。

For S1 also compute canonical
and deformed differential-geometry
curvature diagnostics
ONLY if they can be derived reliably
from the exact analytic surface/deformation。

At minimum attempt：

mean curvature H

Gaussian curvature K

delta_H

delta_K。

If curvature implementation
cannot independently validate：

mark curvature：

UNRESOLVED

and do not block the main Gate。

Output：

D0_deformation_descriptor_table.csv。

============================================================
十、Descriptor Internal Closure
============================================================

Verify：

Js

against saved V2 GT Js semantics。

For deterministic100000 valid V2 hits：

reconstruct canonical point
from triangle+barycentric。

Evaluate F and n。

Compute Js independently。

Compare with saved V2 Js。

Require：

p99 relative error <=1e-6

max <=1e-4。

Also verify：

lambda1*lambda2

against Js。

Require：

p99 relative error <=1e-5

max <=1e-3。

Output：

D0_descriptor_closure.csv。

Gate D0-G7。

============================================================
十一、Exact Camera-Ray Replay
============================================================

Load exact C2 camera locks。

For every material sample：

surface S0/S1

every D0-D6

every24 cameras

compute deformed xyz。

Compute observation direction：

d =
normalize(
camera_center
-
deformed_xyz
)。

Define direction toward camera
using exact sign semantics
consistent with V2 optical equation。

Convert to deformed local frame：

v_local =
[
dot(d,t1')
dot(d,t2')
dot(d,n')
]。

Save：

camera ID

world view direction

local view direction。

Verify：

||v_local|| approximately1。

Output：

D0_local_view_direction_table.csv。

Gate D0-G8。

============================================================
十二、Independent Pointwise Optical Oracle
============================================================

This is NOT an image renderer。

For every:

surface sample

material

deformation

camera

compute pointwise thin-transmission oracle
using the exact V2 physical equations。

Use exact source material parameters。

Compute：

h'

cos_theta

tau_rgb

RGB_rgb。

For MAT0：

use exact frozen fixed-thickness semantics。

For MAT1/MAT2：

use exact frozen mass-conserving semantics。

Do not assume all materials use h0/Js。

Trace source semantics。

Save：

sample ID

surface

material

deformation

camera

sigma_r/g/b

h'

cos_theta

tau_r/g/b

RGB_r/g/b。

Output：

D0_pointwise_optical_oracle.csv。

Expected row count：

8192 material samples
x
3 materials
x
7 deformations
x
24 cameras

unless source case compatibility
explicitly excludes invalid
surface/material combinations。

Report exact expected and actual rows。

============================================================
十三、Optical Oracle Replay Validation
============================================================

Use saved V2 valid hits
for deterministic100000 samples。

For every selected hit：

recover canonical material identity
from triangle+barycentric。

Use the exact camera/deformation/material。

Recompute the optical oracle
through the D0 pointwise equations
at the recovered canonical point。

Compare against saved V2：

tau_rgb

RGB。

Because saved V2 ray hits may lie
at arbitrary barycentric positions，
evaluate continuously at that
recovered material point。

Report：

tau relative error

RGB absolute error。

Require：

tau p99 relative <=1e-6

tau max <=1e-4

RGB p99 absolute <=1e-7

RGB max <=1e-5。

Output：

D0_pointwise_oracle_replay.csv。

Gate D0-G9。

============================================================
十四、Canonical-to-Deformed Paired Analysis Table
============================================================

If D0-G0 through D0-G9 PASS：

build the primary paired table。

For every：

surface sample

material

deformation D1-D6

camera

create one row。

Canonical side uses：

same sample ID

same material

D0

same camera。

Deformed side uses：

same sample ID

same material

selected deformation

same camera。

Save：

identity fields：

sample ID

surface

material

deformation

camera

canonical triangle ID

barycentric

u

v。

Canonical geometry：

x0

t10

t20

n0。

Deformed geometry：

x1

t11

t21

n1。

Deformation：

F00-F22

detF

Js

lambda1

lambda2

gamma

normal_rotation

curvature descriptors if validated。

View：

canonical world view direction

deformed world view direction

canonical local view direction

deformed local view direction。

Optics：

canonical h

deformed h

canonical cos_theta

deformed cos_theta

canonical tau_rgb

deformed tau_rgb

canonical RGB

deformed RGB。

Changes：

delta_tau_rgb

tau_ratio_rgb

log_tau_ratio_rgb

delta_RGB。

Output：

D0_paired_optical_transport_table.csv。

Expected conceptual row count：

8192
x3
x6
x24

subject to exact source-supported
surface/material compatibility。

Print actual row count。

============================================================
十五、Basic Identifiability Audit
============================================================

Do NOT train a model。

Use the paired table only。

Answer descriptive questions：

1.

Does D0 identity produce：

Js=1

lambda1=1

lambda2=1

gamma approximately0？

2.

For rigid deformation：

are Js/lambda1/lambda2 invariant？

3.

For pure uniform/in-plane stretch：

do descriptors respond monotonically
where expected？

4.

For shear：

does gamma change
while Js may remain close to1
according to the exact deformation？

5.

Does local view direction change
under camera-relative deformation？

6.

For MAT1/MAT2：

is log tau ratio statistically
associated with Js
under the current synthetic physical law？

Important：

This is only benchmark sanity。

Because V2 MAT1/MAT2 explicitly use h0/Js，
a Js association is BUILT INTO THE GT。

Do NOT claim discovery
that Js controls real transparent materials。

Output：

D0_descriptor_sanity_audit.md
D0_descriptor_summary.csv。

============================================================
十六、New Research Question Feasibility Decision
============================================================

Define：

DEFORMABLE-OPTICAL-TRANSPORT
-EXPERIMENTALLY-ADDRESSABLE

iff：

D0-G0 PASS

D0-G1 PASS

D0-G2 PASS

D0-G3 PASS

D0-G4 PASS

D0-G5 PASS

D0-G6 PASS

D0-G7 PASS

D0-G8 PASS

D0-G9 PASS

and：

a paired canonical/deformed table exists
with exact material identity

exact local deformation gradient

exact local frames

exact local view direction

and exact canonical/deformed optical oracle。

This Gate does NOT require：

RT-Splatting

Gaussian fitting

novel-view generalization

Stage5.1。

============================================================
十七、Scientific Limitation Classification
============================================================

If feasibility PASS：

state explicitly：

The current V2 benchmark optical law
contains a BUILT-IN thickness transport：

h'=h0/Js

for MAT1/MAT2。

Therefore V2 alone CANNOT be used
to discover that Js is the unknown
physical transport law。

It can only be used to validate
the analysis pipeline
and test whether a method recovers
a known transport mechanism。

This distinction is mandatory。

Classify V2 role as：

KNOWN-LAW-CONTROLLED-BENCHMARK。

Then state the next scientific requirement：

design an independent optical-response family
where multiple candidate deformation descriptors
produce distinguishable effects，
or import a more complex physically motivated
transport/material model。

Do NOT solve that next problem in D0。

============================================================
十八、Final CASE
============================================================

CASE DEFORMABLE-OPTICAL-TRANSPORT
-FEASIBILITY-PASS：

All required Gates pass。

Exact material-point correspondence exists。

Exact F exists。

Exact local frames exist。

Exact local view directions exist。

Exact canonical/deformed optical oracle exists。

Paired analysis table exists。

Conclusion：

the new research question is
EXPERIMENTALLY ADDRESSABLE
at the controlled benchmark level。

V2 role：

KNOWN-LAW-CONTROLLED-BENCHMARK。

Next action：

design Stage D1
MULTI-MECHANISM OPTICAL TRANSPORT BENCHMARK
and candidate-descriptor sufficiency protocol。

Do NOT return to RT attribute-release experiments。

Do NOT yet train a transport model。

------------------------------------------------------------

CASE MATERIAL-IDENTITY-NOT-RECOVERABLE：

stable canonical material identity
cannot be reconstructed。

STOP new line。

------------------------------------------------------------

CASE DEFORMATION-GRADIENT-NOT-VALID：

F/Jacobian closure fails。

STOP new line。

------------------------------------------------------------

CASE LOCAL-FRAME-NOT-VALID：

surface-frame transport closure fails。

STOP new line。

------------------------------------------------------------

CASE OPTICAL-ORACLE-NOT-REPLAYABLE：

pointwise optical equations
cannot reproduce saved V2 semantics。

STOP new line。

------------------------------------------------------------

CASE DEFORMABLE-OPTICAL-TRANSPORT
-FEASIBILITY-FAIL：

one or more essential Gates fail。

Return to RecycleGS。

============================================================
十九、Required Outputs
============================================================

D0_protocol_lock.json

D0_case_grid.csv

D0_deformation_inventory.csv
D0_deformation_equations.md

D0_material_identity_recoverability.csv

D0_material_samples/S0.csv
D0_material_samples/S1.csv

D0_deformation_replay.csv
D0_F_validation.csv

D0_local_frame_table.csv
D0_normal_transport_validation.csv

D0_deformation_descriptor_table.csv
D0_descriptor_closure.csv

D0_local_view_direction_table.csv

D0_pointwise_optical_oracle.csv
D0_pointwise_oracle_replay.csv

D0_paired_optical_transport_table.csv

D0_descriptor_sanity_audit.md
D0_descriptor_summary.csv

stageD0_feasibility_report.md
stageD0_feasibility_summary.md
stageD0_feasibility_log.txt

============================================================
二十、最终终端只输出
============================================================

A. D0-G0

B. exact V2 case count

C. D0-G1

D. deformation keys

E. globally affine deformation names

F. nonlinear deformation names

G. analytic Jacobian available names

H. autograd Jacobian required names

I. D0-G2

J. sampled valid hits for identity audit

K. triangle ID available fraction

L. barycentric available fraction

M. canonical xyz reconstructable fraction

N. canonical uv reconstructable fraction

O. D0-G3

P. S0 material sample count

Q. S1 material sample count

R. material sample IDs reused across
materials/deformations/cameras yes/no

S. D0-G4

T. F validation p99/max relative error
by deformation

U. D0-G5

V. normal transport angular p99/max error

W. D0-G6

X. Js closure p99/max relative error

Y. lambda1*lambda2 vs Js
p99/max relative error

Z. D0-G7

AA. local view direction norm
p99/max absolute error

AB. D0-G8

AC. pointwise optical oracle expected rows

AD. pointwise optical oracle actual rows

AE. oracle replay tau p99/max relative error

AF. oracle replay RGB p99/max absolute error

AG. D0-G9

AH. paired analysis table row count

AI. exact same material identity
canonical/deformed yes/no

AJ. exact F available yes/no

AK. exact local frame available yes/no

AL. exact local view direction available yes/no

AM. exact canonical/deformed optical oracle available yes/no

AN. identity descriptor sanity pass yes/no

AO. rigid descriptor invariance pass yes/no

AP. stretch descriptor response pass yes/no

AQ. shear descriptor response pass yes/no

AR. V2 built-in Js law explicitly acknowledged yes/no

AS. V2 scientific role

AT. deformable optical transport
experimentally addressable yes/no

AU. Final CASE

AV. new primary line
STOP/CONTINUE

AW. AttributeDeformGS old line status

AX. KIOT status

AY. next exact research action

AZ. report path

BA. summary path

============================================================
二十一、README
============================================================

Update：

/data/wyh/DeformTransGS/README.md

Record explicitly：

The original AttributeDeformGS
per-native-state release line is stopped。

Its final blocker is a confirmed
canonical novel-view generalization gap
on K2 under the frozen RT-native V2 carrier。

The underlying scientific question
of optical state evolution under deformation
remains untested。

A new candidate research formulation is introduced：

DEFORMATION-CONDITIONED OPTICAL TRANSPORT。

The new question does not begin from
RT-Splatting tensor names。

Instead it asks：

which local deformation descriptors
are sufficient to explain the change
from canonical to deformed
semi-transparent optical response？

The first-stage analysis is material-point based。

Each analysis sample is attached to
a canonical surface using：

triangle ID

barycentric coordinates

u/v

canonical xyz。

The same material identity is replayed
through exact known deformations。

The local deformation gradient F，
surface Jacobian Js，
principal stretches，
shear diagnostics，
local frames，
local viewing direction，
and pointwise optical oracle
are audited before any learned model is introduced。

Perspective Thin-Transmission V2
is used only as a
KNOWN-LAW-CONTROLLED-BENCHMARK。

For MAT1/MAT2，
the law h'=h0/Js is explicitly built into GT。

Therefore a correlation between Js
and optical response is not a discovery。

D0 only validates whether the project
has enough exact correspondence and
differential-geometric information
to study deformable optical transport。

Only if D0 feasibility passes
may Stage D1 design a multi-mechanism
optical transport benchmark
where competing deformation descriptors
have distinguishable explanatory roles。

============================================================
二十二、实际执行
============================================================

必须实际：

lock all relevant C2/O2 evidence

trace exact V2 case grid

trace exact D0-D6 source deformation definitions

audit saved triangle/barycentric identity

sample4096 exact material points per surface

replay exact deformations

compute exact F

independently validate F

construct canonical/deformed local frames

validate normal transport

compute Js

compute principal stretches

compute shear diagnostic

validate lambda1*lambda2 against Js

replay exact C2 camera directions

compute local view directions

construct pointwise optical oracle

independently reproduce saved V2 optical semantics

build canonical/deformed paired table

run only descriptive descriptor sanity checks

explicitly acknowledge the built-in Js law

make the feasibility decision

Do NOT train any learned model。
PROMPT
```

这条指令的意义很简单：

> **先证明我们手里已经有“同一个材料点形变前后的一一对应光学数据”。**

只要 D0 PASS，下一步 D1 才真正值得做：

> 构造多种不同光学传输机制，不再让 GT 只内置 `h'=h0/Js`，然后严格测试 `Js / 主拉伸 / 剪切 / 曲率 / 局部观察方向` 哪些信息是必要的、哪些是冗余的。

**这才是新主线真正的第一步。**
