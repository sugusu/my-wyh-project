cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前研究主线
==================================================

核心研究问题保持不变：

对于薄半透明 Gaussian surface，

当 non-rigid deformation 导致
local surface area stretch ratio Js 改变时，

fixed optical state tau
是否产生随 Js 系统增长的误差？

已有冻结 benchmark：

StretchableFilm-v0

GT rule：

Js =
abs(det(F)) * norm(F^{-T} n)

h_ratio =
1 / Js

tau_gt =
tau0 * h_ratio

opacity_gt =
1 - exp(-tau_gt)

正式 tau0：

1.0

已有 Benchmark Gate：

PASS

uniaxial:

s=1.10
Js=1.10
GT_DYNAMIC vs GT_FIXED masked MAE=0.003

s=1.25
Js=1.25
MAE=0.007

s=1.50
Js=1.50
MAE=0.014

s=2.00
Js=2.00
MAE=0.030

Spearman rho=1.0

biaxial s=1.50:
Js=2.25
MAE=0.038

twist_60:
Js≈1
MAE=0

==================================================
Stage 3.2 当前失败事实
==================================================

已有 controlled carrier：

/data/wyh/DeformTransGS/carriers/thinfilm_gs/

Gaussian count：

1681
41x41 grid

Gaussian identity：

跨 deformation 固定。

无 densification。

无 pruning。

tau：

tau = softplus(_tau_raw)

alpha_opt：

1 - exp(-tau)

但是当前：

TSGS FirstSurfaceGaussianRasterizer
低层直接调用产生全黑输出。

Canonical NORE：

2.482

Loss：

约 2.813

训练基本不下降。

Stage 3.2 当前状态：

RENDER INTEGRATION BLOCKED

不是：

Necessity Gate FAIL。

==================================================
重要代码事实
==================================================

重新审计官方：

/data/wyh/repos/TSGS/gaussian_renderer/__init__.py

/data/wyh/repos/TSGS/scene/cameras.py

必须确认：

官方 gaussian_renderer.render():

1. 创建 screenspace_points
2. 创建 screenspace_points_abs
3. retain_grad
4. 使用 viewpoint_camera.FoVx / FoVy
5. 使用 viewpoint_camera.world_view_transform
6. 使用 viewpoint_camera.full_proj_transform
7. 使用 viewpoint_camera.camera_center

官方 Camera 构造：

world_view_transform =
torch.tensor(
    getWorld2View2(R,T,trans,scale)
).transpose(0,1).cuda()

projection_matrix =
getProjectionMatrix(...).transpose(0,1).cuda()

full_proj_transform =
world_view_transform @ projection_matrix

camera_center =
world_view_transform.inverse()[3,:3]

同时确认：

official render 的 bg_color
是统一 RGB Tensor，

不是 HxW background image。

因此：

仅仅改成 official render()
仍不能直接表达：

transparent film
transmitting spatially varying checkerboard background。

==================================================
本阶段名称
==================================================

Stage 3.2.1:

Renderer Integration Repair and Alpha-Composite Validation

中文：

渲染集成修复与透明背景合成验证

==================================================
一、本阶段唯一目标
==================================================

修复 controlled thin-film Gaussian carrier
与 TSGS official renderer 的集成。

建立一个经过验证的：

Gaussian film contribution
+
per-camera background image transmission

可微渲染 wrapper。

当前：

不要运行完整 E1。

不要运行完整 E2。

不要分析 Js necessity gap。

不要实现 tau transport formula。

先证明：

render 正确

gradient 正确

100 iteration micro-fit 能下降。

==================================================
二、项目边界
==================================================

所有修改只能位于：

/data/wyh/DeformTransGS

禁止修改：

/data/wyh/repos/TSGS

/data/wyh/RecycleGS

/data/wyh/repos/RT-Splatting

允许只读 import：

TSGS scene.cameras.Camera

TSGS gaussian_renderer.render

TSGS rasterizer

TSGS utility functions

==================================================
三、输出目录
==================================================

创建：

/data/wyh/DeformTransGS/experiments/stage3_2_1_renderer_repair/

输出至少：

official_render_sanity.md
camera_adapter_validation.md
carrier_interface_audit.md
visibility_probe.csv

alpha_composite_identity.csv
alpha_composite_report.md

gradient_probe.csv
gradient_probe_report.md

microfit_metrics.csv
microfit_report.md

stage3_2_1_summary.md
stage3_2_1_log.txt

以及必要 PNG。

==================================================
四、首先停止旧 renderer training
==================================================

不要继续使用当前失败的：

5000 iteration canonical fit

结果。

保留旧输出用于 failure record。

不要覆盖：

Stage 3.2 当前报告。

当前所有测试使用新的：

renderer integration repair

目录。

==================================================
五、官方 Render Sanity Test
==================================================

首先证明当前 TSGS official renderer
在当前环境正常。

使用已有已成功 checkpoint：

优先：

/data/wyh/DeformTransGS/baselines/tsgs_synth_canonical_asg/
point_cloud/iteration_30000/point_cloud.ply

或者其它 Stage 2 已确认可渲染 checkpoint。

使用 Stage 2 已验证 camera dataset。

加载：

GaussianModel

official Camera

official gaussian_renderer.render

正常渲染至少：

1 camera

要求：

render finite

render mean > 0

render max > 0

visibility_filter sum > 0

radii positive count > 0

保存：

official_render_sanity.png

如果 official renderer 也全黑：

停止。

说明环境 / checkpoint loading 出现新问题。

禁止继续 carrier repair。

==================================================
六、官方 Camera Adapter
==================================================

删除或弃用当前：

手动构造 world_view_transform

手动构造 projection matrix

的执行路径。

不要删除历史代码，
但正式 renderer 不再使用。

读取 StretchableFilm-v0
已有 camera definitions。

根据此前 Stage 2 已验证的：

NeRF / Mitsuba camera
→ TSGS R,T conversion

复用同一套已通过 round-trip validation 的转换逻辑。

禁止重新猜 camera convention。

为：

cam_000
cam_004
cam_008

构造真实：

scene.cameras.Camera

允许：

preload_img=False

如果官方 Camera API 支持。

必须保存：

FoVx
FoVy
world_view_transform
projection_matrix
full_proj_transform
camera_center

进行 camera center / forward validation。

与 benchmark camera 比较：

camera center L2 error < 1e-5

forward angular error < 1e-3 degree

FOV error < 1e-5 degree

输出：

camera_adapter_validation.md

未通过禁止继续。

==================================================
七、不要强制构造完整 GaussianModel
==================================================

检查 official render 实际读取的 pc interface。

在：

return_plane=False

override_color 非 None

pipe.compute_cov3D_python=False

条件下，

列出真正需要的 interface。

预期至少包括：

pc.get_xyz

pc.get_opacity

pc.get_transparency

pc.get_scaling

pc.get_rotation

pc.active_sh_degree

不要根据预期直接实现。

必须以真实 official render code 为准。

创建：

ThinFilmRenderAdapter

位于：

carriers/thinfilm_gs/renderer_adapter.py

adapter 包装现有：

ThinFilmGaussianModel

只暴露 official render
真实需要的 property / attribute。

不要继承 GaussianModel，
除非源码证明必须。

不要复制 TSGS GaussianModel。

==================================================
八、正式 Render Pipe
==================================================

构造最小 pipe config。

根据 official render 实际使用字段：

compute_cov3D_python

convert_SHs_python

debug

正式设置优先：

compute_cov3D_python = False

convert_SHs_python = False

debug = False

因为：

直接使用 carrier 的：

scale
rotation

禁止 SH evaluation。

==================================================
九、使用 override_color
==================================================

停止使用 SH tensor。

当前 controlled carrier
不研究 view-dependent appearance。

learnable color：

_color_raw

shape：

N x 3

effective color：

color =
sigmoid(_color_raw)

正式调用 official render：

override_color=color

因此：

shs 不应参与颜色计算。

验证：

不同 constant override color：

red = [1,0,0]

green = [0,1,0]

white = [1,1,1]

产生对应可见颜色变化。

输出：

override_color_validation.png

如果颜色不变化：

停止。

==================================================
十、Transparency Auxiliary Parameter
==================================================

official TSGS render 会读取：

pc.get_transparency

当前 controlled carrier
不研究 TSGS transparency supervision。

因此 adapter 提供：

constant transparency tensor

shape：

N x 1

requires_grad=False

值：

0.5

必须确认：

在 return_plane=False
且当前 RGB render path 中，

该参数不会作为 primary RGB alpha。

根据源码 / rasterizer调用语义记录。

当前 primary opacity 必须来自：

pc.get_opacity

即：

alpha_opt =
1 - exp(-tau)

==================================================
十一、Constant Film Visibility Probe
==================================================

暂时不要训练。

固定：

tau such that alpha_opt = 0.5

因此：

tau =
-log(0.5)

固定：

color = white

使用：

black bg_color

对：

cam_000
cam_004
cam_008

official render：

return_plane=False

override_color=white

记录：

render mean

render max

nonzero pixel count

visibility_filter count

positive radii count

radii mean

radii max

输出：

visibility_probe.csv

要求：

所有 3 camera：

nonzero pixels > 0

positive radii > 0

如果：

radii = 0

优先检查：

camera
xyz
scale

不要修改 tau。

如果：

radii > 0
但 RGB=0

检查：

opacity
override_color
rasterizer input。

==================================================
十二、重要：Per-Pixel Background Composite
==================================================

当前 checkerboard background
来自：

StretchableFilm-v0
background_only

每个 camera 有独立：

I_bg

不能使用 TSGS：

bg_color

直接表示 checkerboard。

正式实现：

Differentiable Two-Pass Alpha Composite

中文：

可微双通道 alpha 合成。

--------------------------------------------------
PASS 1: Film Color Contribution
--------------------------------------------------

official render:

bg_color =
[0,0,0]

override_color =
learned color

得到：

C_film

--------------------------------------------------
PASS 2: Accumulated Alpha
--------------------------------------------------

使用完全相同：

xyz
scale
rotation
opacity
camera

official render:

bg_color =
[0,0,0]

override_color =
ones(N,3)

得到：

A_rgb

检查：

三个 channel 最大差异。

要求：

max(
|A_r-A_g|,
|A_r-A_b|,
|A_g-A_b|
)
< 1e-5

定义：

A =
mean_channel(A_rgb)

不要 detach。

A 必须保持 autograd graph。

--------------------------------------------------
FINAL
--------------------------------------------------

读取对应 camera：

background_only image

I_bg

颜色空间必须和 canonical training target
保持一致。

定义：

I_pred =
C_film
+
(1 - clamp(A,0,1)) * I_bg

当前先使用：

A clamp 仅用于数值保护。

必须检查 clamp 是否大面积饱和。

统计：

A < 0
A > 1

pixel count。

如果 A 天然处于 [0,1]：

记录。

==================================================
十三、Alpha Composite Identity Validation
==================================================

必须验证 two-pass composite
确实与 official rasterizer
constant-background alpha compositing语义一致。

选择：

一个 constant background RGB：

B1 =
[0.2, 0.4, 0.7]

B2 =
[0.8, 0.1, 0.3]

固定：

carrier geometry

alpha_opt = 0.5

constant film color：

[0.6, 0.3, 0.2]

对每个 camera：

DIRECT：

official render

bg_color = B

override_color = film color

得到：

I_direct

TWO_PASS：

C_film =
render black background + film color

A =
render black background + white color

I_two =
C_film + (1-A)*B

比较：

MAE
RMSE
max_abs_error

目标：

MAE < 1e-6

优先：

max_abs_error < 1e-5

允许 CUDA floating point small error。

如果误差明显：

禁止继续。

必须检查：

white pass 是否真正等于 accumulated alpha。

输出：

alpha_composite_identity.csv

以及：

alpha_composite_report.md

这是当前 renderer 最关键验证。

==================================================
十四、Checkerboard Background Validation
==================================================

只有 Alpha Composite Identity PASS
才继续。

使用：

I_bg =
真实 background_only image

固定：

alpha_opt = 0.5

测试至少：

color = black

color = gray 0.5

分别合成。

保存：

checkerboard_composite_black.png

checkerboard_composite_gray.png

检查：

sheet region 内：

checkerboard 仍然可见

且相对 background-only：

contrast / brightness 发生变化。

计算：

background-only vs composite

在 optical effect mask：

MAE > 0

要求可观察。

==================================================
十五、Gradient Probe
==================================================

禁止直接跑 5000 iteration。

单 camera：

cam_000

建立当前 canonical model。

分别做三个 probe。

--------------------------------------------------
Probe A: tau gradient
--------------------------------------------------

freeze color

只允许：

_tau_raw.requires_grad=True

forward

使用 canonical GT

计算：

L1 + 0.2 DSSIM

backward

记录：

loss

tau_raw grad finite

grad abs mean

grad abs max

nonzero grad count

要求：

mean grad > 0

nonzero grad count > 0

--------------------------------------------------
Probe B: color gradient
--------------------------------------------------

freeze tau

只允许 color_raw。

同样记录。

--------------------------------------------------
Probe C: joint gradient
--------------------------------------------------

tau + color

记录两组 gradient。

输出：

gradient_probe.csv

如果任何参数：

grad=None

或：

all zero

必须追踪：

tau
→ alpha_opt
→ pc.get_opacity
→ rasterizer
→ A/C_film
→ composite
→ loss

禁止靠调 learning rate解决 zero gradient。

==================================================
十六、Tau Numerical Sensitivity Test
==================================================

选择固定 model。

对所有 Gaussian
统一设置：

tau =

0.3
0.5
0.7
1.0
1.5

color 固定。

渲染：

cam_000

计算：

相邻 tau render difference MAE

要求：

tau 改变产生连续非零 RGB difference。

检查：

tau increases

是否总体导致：

background transmission decrease。

定义 checkerboard transmission diagnostic：

在 sheet mask 内：

corr(
I_pred - C_film,
I_bg
)

或其它清晰 transmission magnitude。

不要预设颜色方向。

只验证：

renderer 对 tau sensitive。

输出：

tau_sensitivity.csv

==================================================
十七、100-Iteration Micro-Fit
==================================================

只有：

official render sanity PASS

camera validation PASS

visibility PASS

alpha composite identity PASS

tau gradient PASS

color gradient PASS

才能运行。

使用：

cam_000

单 camera

canonical tau0=1.0 GT

geometry frozen

优化：

tau
color

100 iterations

Adam

初始：

tau lr=1e-2
color lr=1e-2

记录每 iteration：

loss
L1
DSSIM
tau grad norm
color grad norm
tau mean
alpha mean

要求：

loss_100
<
0.8 * loss_0

即至少下降 20%。

如果未下降：

检查 gradient / optimizer。

不要直接跑 5000。

输出：

microfit_metrics.csv

保存：

iter_000.png
iter_010.png
iter_050.png
iter_100.png

==================================================
十八、3-Camera 500-Iteration Pre-Fit
==================================================

只有 100 iter micro-fit PASS
才继续。

使用：

cam_000
cam_004
cam_008

geometry frozen

优化：

tau
color

500 iterations

保存：

loss curve

iteration 0
100
250
500 render

计算：

Masked PSNR
MAE
NORE

当前不要求：

NORE <= 0.30

本阶段只要求：

NORE 显著低于旧失败结果：

2.482

且：

render 不全黑

loss 明显下降。

如果 NORE 仍 > 0.5：

报告：

renderer integration repaired

but carrier canonical expressiveness insufficient

不要进入完整 Stage 3.2。

==================================================
十九、只有预拟合足够才能恢复 Stage 3.2
==================================================

定义 Renderer Integration Gate：

PASS：

A. official renderer sanity PASS

B. Camera validation PASS

C. 3 cameras visible

D. alpha composite identity MAE < 1e-6

E. tau gradient nonzero

F. color gradient nonzero

G. tau sensitivity nonzero

H. 100 iter loss decrease >= 20%

I. 500 iter three-camera NORE <= 0.50

WEAK：

A-H PASS

但是：

500 iter NORE > 0.50

说明：

renderer 已修复，

但 controlled carrier
无法充分拟合 canonical GT。

此时重新评估：

carrier expressiveness

不要运行 E1/E2。

FAIL：

A-H 任一关键条件失败。

==================================================
二十、不要立即增加 Carrier Capacity
==================================================

如果 Gate WEAK：

禁止自动增加：

SH degree 3

ASG

MLP

densification

Gaussian count

先分析 residual。

至少区分：

background transmission error

film color error

edge / coverage error

view inconsistency

生成：

canonical_residual_decomposition.md

只有确认原因后，
下一阶段再决定是否调整 carrier。

==================================================
二十一、Stage 3.2 恢复规则
==================================================

如果 Renderer Integration Gate PASS：

恢复此前 Stage 3.2：

canonical 5000 iter fit

E1 FIXED TAU

E2 TAU-ONLY REFIT

necessity gap vs Js

tau refit correlation

并生成新的正式 Stage 3.2 report。

允许调用已有 Stage 3.2 code，

但必须统一改用：

new official-Camera
+
official-render
+
two-pass alpha composite renderer

禁止混用旧 low-level renderer result。

如果 Gate WEAK / FAIL：

禁止执行 E1 / E2。

==================================================
二十二、报告
==================================================

stage3_2_1_summary.md 必须回答：

1. Official TSGS renderer 是否正常

2. 旧 low-level renderer 全黑是否属于 integration bug

3. 官方 Camera 是否成功构造

4. Camera center max error

5. Forward angular max error

6. ThinFilm adapter 实际需要哪些 pc interface

7. 是否使用 SH

8. 是否使用 override_color

9. visibility positive radii count

10. white-pass RGB channel max difference

11. Alpha Composite Identity MAE

12. Alpha Composite Identity max error

13. checkerboard background 是否成功透过 film

14. tau gradient mean / max

15. color gradient mean / max

16. tau sensitivity 是否非零

17. 100 iter loss reduction %

18. 500 iter NORE

19. Renderer Integration Gate:
PASS / WEAK / FAIL

20. 是否恢复完整 Stage 3.2

==================================================
二十三、README
==================================================

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage 3.2 initial run was blocked
by a renderer integration failure.

The failure does not evaluate
the fixed-optical-state hypothesis.

Stage 3.2.1 repairs the carrier
using TSGS official Camera/render conventions
and a differentiable two-pass alpha composite
for spatially varying background transmission.

明确：

TSGS bg_color is a constant RGB background,
so a checkerboard background image
must not be passed conceptually as bg_color.

==================================================
二十四、实际执行
==================================================

必须实际完成：

official render sanity

official Camera adapter

carrier interface audit

override_color validation

constant visibility probe

two-pass alpha renderer

alpha composite identity validation

checkerboard composite validation

tau gradient probe

color gradient probe

tau sensitivity test

100 iteration micro-fit

如果通过：
3-camera 500 iteration pre-fit

根据 Gate：

决定是否恢复完整 Stage 3.2。

不要只写代码。

不要继续使用失败的 low-level renderer。

不要运行 E1/E2，
除非 Renderer Integration Gate PASS。

不要实现 tau / Js transport formula。

不要修改 TSGS。

==================================================
二十五、最终终端总结
==================================================

最后简洁输出：

1. Official renderer sanity PASS/FAIL
2. Official Camera validation PASS/FAIL
3. Camera center max error
4. Forward angular max error
5. ThinFilm adapter interface list
6. SH used yes/no
7. override_color used yes/no
8. positive radii count per camera
9. white-pass channel max difference
10. alpha composite identity MAE
11. alpha composite identity max error
12. checkerboard transmission visible yes/no
13. tau grad mean / max
14. color grad mean / max
15. tau sensitivity PASS/FAIL
16. 100 iter loss reduction
17. 500 iter NORE
18. Renderer Integration Gate
19. Resume full Stage 3.2 yes/no
20. alpha_composite_report.md path
21. gradient_probe_report.md path
22. stage3_2_1_summary.md path

请直接实际修复、运行和验证。

不要换服务器。
不要使用 RT-Splatting。
不要继续环境恢复。
不要实现 proposed tau transport formula。
不要修改官方 TSGS。
PROMPT