cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
研究主线纠正
==================================================

此前 Stage 3.0 ~ Stage 3.1.3
尝试使用 RT-Splatting 作为研究载体。

RT-Splatting runtime 因历史 CUDA / PyTorch
环境兼容问题未能在当前服务器运行。

当前明确决定：

不换服务器。

不继续迁移环境。

不继续 Docker / container / CUDA 11.8 调试。

RT-Splatting 仅保留为相关工作和属性解耦参考。

研究主线重新聚焦到：

对于薄半透明 Gaussian 表面，

当 non-rigid deformation 导致
local surface area stretch ratio Js 改变时，

固定 optical state 是否产生
随 Js 系统增加的误差？

==================================================
已有问题存在性证据
==================================================

StretchableFilm-v0 GT benchmark 已正式 PASS。

tau0=1.0：

uniaxial stretch:

s=1.10
Js=1.10
GT_DYNAMIC vs GT_FIXED masked MAE=0.003

s=1.25
Js=1.25
MAE=0.007

s=1.50
Js=1.50
MAE=0.014

s=2.00
Js=2.00
MAE=0.030

Spearman rho=1.0

biaxial stretch:

s=1.50
Js=2.25
MAE=0.038

twist control:

Js≈1
MAE=0

Benchmark 已冻结。

==================================================
本阶段名称
==================================================

Stage 3.2:

Controlled Thin-Film Gaussian Fixed-Optical-State Necessity Test

中文：

受控薄膜 Gaussian 固定光学状态必要性实验

==================================================
一、本阶段唯一目标
==================================================

不使用 RT-Splatting runtime。

在当前已经可运行的 TSGS / Gaussian rasterizer
环境基础上，

构建一个最小受控 Gaussian carrier。

该 carrier 只包含：

geometry
+
per-Gaussian optical depth tau

第一轮只回答：

Geometry 正确形变以后，

保持 canonical tau 不变，

是否会产生随 surface area stretch Js
系统增长的 rendering error？

以及：

保持完全相同 deformed geometry，

仅重新优化 tau，

是否能够显著关闭该误差？

本阶段不提出 tau update formula。

禁止使用：

tau' = tau / Js

作为预测方法。

该公式当前只属于 GT benchmark rule。

==================================================
二、项目边界
==================================================

所有新增代码：

/data/wyh/DeformTransGS

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

允许只读 import：

TSGS Gaussian rasterizer
TSGS camera utilities
TSGS quaternion / covariance utilities

新 carrier 位于：

/data/wyh/DeformTransGS/carriers/thinfilm_gs/

==================================================
三、建立代码结构
==================================================

创建：

carriers/thinfilm_gs/
├── model.py
├── geometry.py
├── renderer.py
├── optical.py
└── io.py

experiments/stage3_2_fixed_optical_necessity/

scripts/run_stage3_2.sh

不要复制整个 TSGS。

==================================================
四、受控 Gaussian 初始化
==================================================

不要运行 3DGS densification。

不要从随机 point cloud 初始化。

使用 StretchableFilm-v0 canonical rectangular sheet
直接建立固定 Gaussian grid。

正式 geometry：

41 x 41 grid

Gaussian count：

1681

Gaussian identity 与 canonical surface material point
一一对应。

要求：

Gaussian index 在所有 deformation state 中保持不变。

xyz：

使用 canonical grid vertex / sample position。

每个 Gaussian 建立 canonical local surface frame：

tangent_u
tangent_v
normal

canonical sheet normal：

根据真实 sheet geometry 计算。

不要硬编码 z normal，
除非审计确认 sheet 位于 xy plane。

==================================================
五、Gaussian Scale / Rotation
==================================================

根据 canonical grid 邻域 spacing
初始化 tangent-plane scale。

Gaussian 使用扁平 2D-like covariance。

scale：

tangent_u direction:
根据局部 grid spacing

tangent_v direction:
根据局部 grid spacing

normal direction:
设置为 tangent scale 的固定小比例。

初始建议比例：

normal_scale =
0.1 * min(tangent_scale_u, tangent_scale_v)

但是必须进行 coverage validation。

rotation：

由：

tangent_u
tangent_v
normal

构造 rotation matrix。

再转换为当前 TSGS rasterizer
真实四元数顺序。

必须复用 TSGS rotation utility。

已知 TSGS quaternion 为：

wxyz

但仍从代码 import utility，
不要自己重新实现不一致版本。

==================================================
六、Optical Attribute
==================================================

每个 Gaussian 仅新增一个显式参数：

tau

shape：

N x 1

activation：

softplus

内部可学习 raw parameter：

_tau_raw

tau =
softplus(_tau_raw)

effective optical alpha：

alpha_opt =
1 - exp(-tau)

renderer 输入 opacity：

alpha_opt

重要：

当前 controlled carrier
不声称这是 RT-Splatting renderer。

不声称 geometric occupancy
与 optical opacity 已完整建模。

当前 geometry 通过：

fixed Gaussian grid
fixed Gaussian identity
fixed covariance transport

受控。

tau 只表示：

synthetic thin-film optical attenuation state。

==================================================
七、颜色模型
==================================================

当前 benchmark material：

blendbsdf(null + diffuse)

因此第一版不要使用：

ASG
SpecularModel
high-order SH

尽量减少 appearance confound。

检查 GT diffuse scattering branch。

使用最小颜色模型：

每 Gaussian learnable RGB color

或：

SH degree 0

优先 SH degree 0 / DC color。

选择依据：

与现有 rasterizer 最容易兼容。

报告真实实现。

禁止使用 SH degree 3
补偿 optical-state error。

==================================================
八、Renderer
==================================================

优先复用当前已编译成功的 TSGS
Gaussian rasterizer。

创建：

carriers/thinfilm_gs/renderer.py

只在 DeformTransGS wrapper 中组织输入。

输入：

xyz
scale
rotation
alpha_opt
DC color
camera

禁止修改 CUDA rasterizer。

禁止修改 TSGS renderer。

如果 TSGS high-level renderer
强依赖 GaussianModel：

允许直接调用低层 GaussianRasterizer。

必须基于真实 API。

==================================================
九、Canonical Coverage Validation
==================================================

在训练 tau 前，

使用固定：

alpha=0.5
constant RGB

渲染 3 cameras。

检查：

sheet projected coverage

holes

edge discontinuity

Gaussian leakage

生成：

coverage_validation.md

以及：

coverage_overview.png

如果存在明显孔洞：

只允许调：

tangent scale multiplier

测试：

0.75
1.0
1.25
1.5

选择最小且无明显孔洞的 multiplier。

Gaussian count 固定 1681。

禁止 densification。

==================================================
十、Canonical Fitting
==================================================

GT：

StretchableFilm-v0

tau0=1.0
canonical GT_DYNAMIC

使用正式 3 cameras：

cam_000
cam_004
cam_008

训练参数只允许：

tau
DC color

geometry：

xyz
scale
rotation

全部冻结。

训练：

5000 iterations

如果明显提前收敛，
仍保存：

1000
3000
5000

optimizer：

根据当前简单参数使用 Adam。

初始 learning rate：

tau:
1e-2

color:
1e-2

允许根据 loss stability
降低到：

1e-3

但必须记录。

Loss：

L1
+
0.2 * DSSIM

与 Gaussian Splatting 常用 image loss
保持一致。

不要加入 deformation loss。

输出：

baselines/thinfilm_gs_canonical_tau1/

==================================================
十一、Canonical Fit Gate
==================================================

使用 GT optical effect mask。

计算：

Masked PSNR
Masked SSIM
MAE
RMSE
NORE

要求：

Canonical Fit PASS：

NORE <= 0.30

WEAK：

0.30 < NORE <= 0.50

FAIL：

NORE > 0.50

如果 FAIL：

禁止进入 deformation necessity test。

先分析：

coverage
color model
tau fit

不要增加 ASG。

==================================================
十二、Geometry Deformation Transport
==================================================

只有 Canonical Fit PASS / WEAK
才能继续。

对 canonical Gaussian：

xyz' =
phi(xyz)

获取：

F_i

covariance：

Sigma' =
F Sigma F^T

重新分解得到：

scale'
rotation'

normal 不作为显式 optical input。

Gaussian identity 保持不变。

实现：

carriers/thinfilm_gs/geometry.py

正式 states：

uniaxial:
1.10
1.25
1.50
2.00

biaxial:
1.10
1.25
1.50

twist:
30
60

必须使用冻结 benchmark
完全相同的 deformation definitions。

==================================================
十三、E1 Fixed Optical State
==================================================

定义：

E1_FIXED_TAU

使用：

deformed xyz
deformed covariance

但是：

tau_i' = canonical tau_i

DC color：

保持 canonical。

禁止优化。

对每个 state
渲染 3 cameras。

与对应：

GT_DYNAMIC tau0=1.0

比较。

计算：

Full image

Optical Effect Mask

指标：

PSNR
SSIM
MAE
RMSE
NORE

额外计算：

E1 Error Increase：

state error
-
canonical error

输出：

e1_fixed_tau_metrics.csv

==================================================
十四、E2 Tau-Only Refit Upper Bound
==================================================

这是本阶段最关键控制变量。

对每一个 deformation state：

使用和 E1 完全相同的：

xyz'
scale'
rotation'
DC color

全部冻结。

唯一允许优化：

tau

初始化：

canonical tau

使用对应：

GT_DYNAMIC

训练：

最多 2000 iterations

optimizer：

Adam

tau lr：

1e-2

如果不稳定：

1e-3

禁止优化 color。

禁止优化 geometry。

禁止 densification。

禁止 pruning。

输出：

baselines/tau_refit/<state>/

渲染：

E2_TAU_REFIT

计算与 E1 完全相同指标。

输出：

e2_tau_refit_metrics.csv

==================================================
十五、最核心的 Necessity Gap
==================================================

定义：

Gap(state) =

NORE_E1_FIXED_TAU
-
NORE_E2_TAU_REFIT

同时报告：

Masked PSNR recovery：

PSNR_E2
-
PSNR_E1

核心分析变量：

Js

不要只按 deformation name 排序。

生成：

necessity_gap_vs_js.csv

字段：

state
deformation_type
strength
mean_Js
median_Js
mean_h_ratio
E1_NORE
E2_NORE
NORE_gap
E1_masked_PSNR
E2_masked_PSNR
PSNR_recovery

==================================================
十六、Necessity Gate
==================================================

PASS：

同时满足：

A.

对于 uniaxial stretch：

E1 NORE 或 MAE
随 Js 总体单调增加。

Spearman rho >= 0.9。

B.

至少：

uniaxial s=1.50
或
s=2.00

E2 tau-only refit：

NORE gap >= 0.10

或：

Masked PSNR recovery >= 1.0 dB。

C.

biaxial s=1.50：

E2 同样具有明显 recovery。

D.

twist_60：

E1 和 E2 差异明显小于
uniaxial s=1.50。

要求：

twist NORE gap
<= 25% of stretch_1.50 gap。

FAIL：

E1 error 不随 Js 增长，

或者：

tau-only refit 无法稳定关闭 gap。

WEAK：

趋势存在但 recovery 小。

禁止调整 threshold。

==================================================
十七、必须检查 Tau Refit 到底学了什么
==================================================

对于每 state：

记录 canonical tau：

mean
std
median

refit tau：

mean
std
median

ratio：

tau_refit / tau_canonical

统计：

mean
median
p10
p90

然后与：

1 / Js

只做相关性分析。

计算：

Pearson

Spearman

重要：

当前禁止直接使用：

tau' = tau / Js

作为方法。

只回答：

tau refit ratio
是否与 thickness ratio
存在统计相关性。

输出：

tau_refit_analysis.csv

==================================================
十八、可视化
==================================================

至少生成：

1. necessity_gap_vs_js.png

x:
mean Js

y:
E1-E2 NORE gap

2. psnr_recovery_vs_js.png

x:
mean Js

y:
E2-E1 masked PSNR

3. tau_ratio_vs_h_ratio.png

x:
1/Js

y:
tau_refit / tau_canonical

4. overview：

canonical

stretch_1.50

stretch_2.00

biaxial_1.50

twist_60

每个展示：

GT
E1 Fixed Tau
E1 Error
E2 Tau Refit
E2 Error

生成：

stage3_2_overview.png

==================================================
十九、报告
==================================================

输出：

experiments/stage3_2_fixed_optical_necessity/

包含：

carrier_implementation_report.md
coverage_validation.md
canonical_fit_metrics.csv
canonical_fit_report.md

e1_fixed_tau_metrics.csv
e2_tau_refit_metrics.csv
necessity_gap_vs_js.csv
tau_refit_analysis.csv

necessity_test_report.md
stage3_2_summary.md
stage3_2_log.txt

==================================================
二十、necessity_test_report.md
==================================================

必须明确回答：

A. Controlled carrier 是否使用 RT-Splatting？

回答：

否。

说明：

它是用于隔离 geometry transport
和 optical-state evolution 的机制实验载体。

B. Gaussian 数量。

C. Gaussian identity 是否跨形变保持。

D. geometry 是否 densify / prune。

E. tau 的定义。

F. alpha_opt 的定义。

G. canonical geometry 是否冻结。

H. canonical fit NORE。

I. E1 是否只更新 geometry。

J. E2 是否只优化 tau。

K. E1 error 是否随 Js 增长。

L. Spearman rho。

M. s=1.50 recovery。

N. s=2.00 recovery。

O. biaxial_1.50 recovery。

P. twist control gap。

Q. Necessity Gate：

PASS
WEAK
FAIL

R. tau refit ratio 与 1/Js 相关性。

S. 当前是否已经证明：

“fixed optical state is insufficient
under area-changing deformation”

只允许：

supported
partially supported
not supported

T. 当前是否可以开始设计
deformation-aware tau transport rule。

回答：

是 / 否

==================================================
二十一、研究边界
==================================================

如果 Gate PASS：

下一阶段才允许：

根据 tau refit analysis

设计：

deformation-aware tau transport。

如果：

tau_refit/tau_canonical
与
1/Js

高度相关，

优先研究解析 transport。

如果相关性弱：

再分析：

view
local geometry
color
或 residual。

如果 Gate FAIL：

停止该主线。

不要继续添加属性。

==================================================
二十二、README
==================================================

更新 README：

明确记录：

RT-Splatting runtime was treated as an engineering blocker,
not the research objective.

Stage 3.2 returns to the core scientific question
using a controlled thin-film Gaussian carrier
on the current server.

==================================================
二十三、实际执行
==================================================

必须实际：

审计当前 Gaussian rasterizer API

实现 controlled carrier

coverage validation

canonical fit

canonical Gate

geometry deformation

E1 fixed tau

E2 tau-only refit

necessity gap analysis

tau refit correlation analysis

所有图表和报告

不要训练 RT-Splatting。

不要继续 container。

不要换服务器。

不要实现 tau update formula。

==================================================
二十四、最终终端总结
==================================================

最后简洁输出：

1. Carrier runnable yes/no
2. Gaussian count
3. Coverage multiplier
4. Canonical NORE
5. Canonical Fit Gate
6. E1 error vs Js Spearman rho
7. stretch_1.50 E1 NORE
8. stretch_1.50 E2 NORE
9. stretch_1.50 PSNR recovery
10. stretch_2.00 E1 NORE
11. stretch_2.00 E2 NORE
12. stretch_2.00 PSNR recovery
13. biaxial_1.50 PSNR recovery
14. twist_60 NORE gap
15. Necessity Gate
16. tau ratio vs 1/Js Spearman
17. Fixed optical state hypothesis
18. Can design tau transport rule yes/no
19. stage3_2_overview.png path
20. necessity_test_report.md path
21. stage3_2_summary.md path

请直接实现、运行、验证。

不要迁移服务器。
不要继续 RT-Splatting runtime recovery。
不要实现 proposed tau formula。
不要修改官方 TSGS。
PROMPT