下一步 Stage 3.4B-R5B

我建议名称：

Formal Valid-Set Tail Factor Closure

中文：

正式有效样本集下的重尾因素闭环

只做四件事：

重建唯一 formal valid set；
查清 boundary contradiction；
真正算 bad-camera distribution；
根据预定义规则给 tail 分类。

不再跑 O0-O4。

不再改 metric。

通过以后直接 P0/P1/P2/P3。

cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式状态
============================================================

Stage 3.4B-R5A 已完成。

已经可靠确认：

R5A0 Protocol Lock:
PASS

R5A1 O4 Closure:
PASS

O4 root cause：

R5 diagnostic used different valid masks
for R and gap aggregation。

After unified mask：

closure median =
4.68e-17

p95 =
1.76e-16

max =
3.37e-16。

Therefore：

O4 algebraic identity closes
at float64 numerical precision。

R5A2 Revised Optical Oracle：

PASS。

Thus：

O0 Identity exact ratio
PASS

O1 Algebra core
PASS

O2 Sample-level tau oracle
PASS

O3 Constant-field image oracle
PASS

O4 Noncommutation closure
PASS。

CURRENT OPTICAL METRIC CORE
IS MATHEMATICALLY VALIDATED。

============================================================
CURRENT P0 CENTRAL PHENOTYPE
============================================================

stretch_1.25：

median R =0.7960
physical q =0.8

stretch_1.50：

median R =0.6573
physical q =2/3

stretch_2.00：

median R =0.4832
physical q =0.5

uniform rho =1.0000。

Area-preserving：

shear_k020：

median R=0.9991

shear_k040：

median R=0.9954

twist_60：

median R=0.9478。

Therefore：

CENTRAL AREA-DILUTION PHENOTYPE
IS SUPPORTED。

============================================================
LOCAL HEAVY TAIL
============================================================

R5 showed severe multiplicative tails。

Examples：

stretch_2.00：

p95 log error=3.28255
factor2 fraction=0.2546
factor5 fraction=0.1693

cubic_l0333：

p95 log error=2.144824
factor2 fraction=0.1647

shear_k040：

p95 log error=3.698196
factor2 fraction=0.2170

twist_60：

p95 log error=5.533819
factor2 fraction=0.4505。

Therefore：

LOCAL HEAVY-TAIL FAILURE
IS SUPPORTED。

============================================================
Stage R5A Tail Classification Problem
============================================================

R5A reported：

P(LOW|TAIL1)=0.6102

P(UPPER|TAIL1)=0.0000

P(BOUNDARY_NEAR|TAIL1)=0.8362

Most common TAIL1 combo：
L0B。

It classified：

TAIL-MIXED。

However this classification
is NOT formally closed。

Reason 1：

Formal boundary semantics：

BOUNDARY_MARGIN=8px。

boundary_near =
boundary_distance <8。

boundary_pass =
boundary_distance >=8。

R5A explicitly reported：

"R5 boundary=1.000 means
100% PASS boundary check
(distance>=8px)."

But also reported：

P(BOUNDARY_NEAR|TAIL1)=0.8362。

If the SAME boundary_distance
and SAME formal valid rows are used，

these statements are contradictory。

------------------------------------------------------------

Reason 2：

R5A reported：

matched-support boundary effect：
N/A
(boundary_near fraction negligible)

but also：

P(BOUNDARY_NEAR|TAIL1)=0.8362。

Contradiction。

------------------------------------------------------------

Reason 3：

Tail bad-camera distribution
was not explicitly reported。

The report says：

"see o4_closure_all_rows.csv"

but that file is an O4 oracle closure table，
not the formal P0 tail camera-disagreement table。

------------------------------------------------------------

Reason 4：

Required cofactor-specific spatial clustering
was not explicitly recomputed。

R5A says：

"see R5"

which only contains overall tail clustering。

------------------------------------------------------------

Reason 5：

Four formal binary factors were defined：

LOW
UPPER
BOUNDARY
CAMERA

but R5A combination labels are：

L0B
00B
000

rather than an explicit four-bit factor tuple。

Therefore R5A4 classification
is INCOMPLETE。

============================================================
Formal Status Correction
============================================================

R5A0:
PASS

R5A1:
PASS

R5A2:
PASS

R5A3:
PARTIAL

R5A4:
FAIL / INCOMPLETE

Current formal Final CASE：

TAIL-CLASSIFICATION-INCOMPLETE。

Do NOT run：

P1
P2
P3

until this stage closes。

============================================================
本阶段名称
============================================================

Stage 3.4B-R5B:

Formal Valid-Set Tail Factor Closure

中文：

正式有效样本集下的重尾因素闭环

============================================================
一、唯一目标
============================================================

Do NOT revalidate metric mathematics。

Metric core is already locked。

Only answer：

Q1.

What is the EXACT formal P0
cell-camera valid set？

Q2.

Why did R5A simultaneously report：

boundary_pass=100%

and

P(boundary_near|tail)=83.6%？

Q3.

After rebuilding all tail factors
on the SAME formal valid rows，

what is the correct tail origin classification？

Q4.

Does camera disagreement
or spatial clustering
contribute to the cell-level tail？

============================================================
二、禁止事项
============================================================

禁止：

rerun O0

rerun O1

rerun O2

rerun O3

rerun O4

change metric

change ratio

change Q_tau

change tau_skip

change boundary margin

filter tail cells

P1

P2

P3

covariance mechanism

method design

clone

split

training

modify TSGS。

============================================================
三、输出目录
============================================================

所有输出：

/data/wyh/DeformTransGS/experiments/
stage3_4B_R5B_formal_tail_factor_closure/

禁止覆盖 R5 / R5A。

============================================================
四、锁定 R5 Formal Inputs
============================================================

读取：

stage3_4B_R5_oracle_tail_audit/
p0_tail_feature_table.csv

stage3_4B_R5_oracle_tail_audit/
p0_cross_camera_tail_trace.csv

stage3_4B_R5_oracle_tail_audit/
current_p0_cell_camera_trace.csv

如果实际路径不同：

实际搜索。

禁止根据猜测生成新 P0 values。

优先使用 R5 已计算数据。

计算 SHA256。

输出：

formal_tail_input_manifest.csv

字段：

artifact
path
sha256
rows
columns。

============================================================
五、Schema Dump
============================================================

对三个 DataFrame：

打印：

shape

all column names

dtype

nunique

NaN fraction。

特别搜索列名：

state

cell_id

camera_id

E_log

factor_error

tau_cell_can

tau_support_ratio

upper_clip_fraction

boundary_distance

boundary_pass

valid_sample_fraction

R_camera

Q_tau_camera

camera_log_spread

bad_camera_factor2_count。

输出：

formal_tail_schema_dump.txt

禁止静默使用近义列。

============================================================
六、唯一 Formal Row Key
============================================================

正式：

KEY = [
    "state",
    "cell_id",
    "camera_id",
]

关键：

```python
KEY = [
    "state",
    "cell_id",
    "camera_id",
]


def assert_unique_key(
    name,
    df,
):
    missing = [
        c
        for c in KEY
        if c not in df.columns
    ]

    if missing:
        raise KeyError(
            f"{name} missing {missing}"
        )

    dup = df.duplicated(
        KEY,
        keep=False,
    )

    if dup.any():
        raise RuntimeError(
            f"{name} duplicate keys:\n"
            + df.loc[
                dup,
                KEY,
            ]
            .head(50)
            .to_string(index=False)
        )

所有 factor analysis
必须基于同一个 unique KEY table。

============================================================
七、Reconstruct ONE Formal Valid Table

新建：

analysis/formal_tail_factor_closure.py

建立：

formal_df。

每 row 必须来自：

CURRENT P0 cell-camera metric。

Formal valid 条件只能来自
CURRENT locked protocol。

必须读取真实字段/代码。

至少记录：

projection_valid_protocol

bilinear_valid_protocol

finite_valid_protocol

valid_sample_fraction_protocol

boundary_pass_protocol

measurable_support_protocol。

输出：

formal_valid_condition_source.md

记录每个 condition：

source file

function

line

exact expression。

禁止重写阈值。

formal_df 只包含：

满足 CURRENT metric
正式 cell-camera validity

并实际进入：

R_camera / cross-camera aggregation

候选集的 rows。

IMPORTANT：

如果 measurable support
是在 cross-camera aggregation 前应用，

按真实顺序执行。

输出：

formal_p0_valid_cell_camera.csv

字段：

state
cell_id
camera_id

R_camera
Q_tau_camera
E_log
factor_error

tau_cell_can
tau_support_ratio

upper_clip_fraction

boundary_distance
boundary_pass

valid_sample_fraction。

另外保存：

formal_valid_row_count.csv

每 state：

raw rows
formal valid rows
unique cells
camera count distribution。

============================================================
八、Formal Validity Self-Consistency Assertions

对 formal_df：

assert formal_df[
    "boundary_pass"
].astype(bool).all()

如果 current formal protocol
确实 requires boundary_pass。

以及：

assert (
    formal_df["boundary_distance"]
    >= BOUNDARY_MARGIN
).all()

如果 boundary_pass definition
is distance >= margin。

如果 assertion FAIL：

必须找到：

FIRST offending KEY。

输出：

formal_validity_contradictions.csv

字段：

state
cell_id
camera_id

boundary_distance
boundary_pass

formal_valid flags

source condition。

此时定义：

BOUNDARY SEMANTIC BUG FOUND。

============================================================
九、Boundary Variable Lineage Audit

定位 R5A 计算：

P(BOUNDARY_NEAR|TAIL1)=0.8362

的确切代码。

输出：

r5a_boundary_lineage.md

必须包含：

source path

function

line range

exact code。

回答：

R5A boundary factor
实际使用哪个 column？

是否：

boundary_distance

boundary_pass

boundary statistic

A>threshold mask

foreground distance

image edge distance

or another field？

记录：

R5A_BOUNDARY_SOURCE_COLUMN。

然后比较：

R5A factor source

vs

CURRENT formal boundary protocol source。

如果不同：

BOUNDARY COLUMN SEMANTIC MIXUP FOUND。

============================================================
十、Formal Boundary Factor

禁止自己创造新 threshold。

首先明确：

如果 formal valid rows
已经全部满足：

boundary_distance >=8

then：

BOUNDARY_NEAR =
boundary_distance <8

must be False for all formal rows。

此时：

boundary_near
cannot be a tail-origin factor
inside the formal valid set。

为了研究：

distance-to-valid-boundary margin
是否与 tail severity 相关，

只能作为 CONTINUOUS DIAGNOSTIC：

boundary_clearance =

boundary_distance

BOUNDARY_MARGIN。

这不是 binary origin factor。

记录：

boundary_clearance。

按 quartile：

Q1
Q2
Q3
Q4

分析 E_log。

输出：

boundary_clearance_tail_diagnostic.csv

计算：

Spearman：

E_log
vs

-boundary_clearance。

只作 diagnostic。

禁止称 boundary-caused。

============================================================
十一、Formal Upper Censor Factor

沿用：

UPPER_ALPHA_LIMIT =
1.0 -1e-6。

UPPER =

upper_clip_fraction >0。

验证：

P(UPPER|TAIL1)。

R5A reported：

0。

重新计算。

输出：

formal_upper_factor_audit.csv

============================================================
十二、Formal LOW Factor

严格沿用 R5A definition：

LOW：

tau_cell_can

<=

0.1 *
POOLED FORMAL ALL-ROW
median tau_cell_can。

IMPORTANT：

pooled median
必须在 formal_df 上重新计算。

输出：

formal_low_factor_definition.json

字段：

pooled_tau_median
low_threshold
n_low
fraction_low。

不要 per-state threshold。

============================================================
十三、Formal CAMERA Factor

不能从 O4 closure table 读取。

从：

formal P0 cell-camera rows

重新建立 cross-camera table。

对于每：

state
cell_id

收集 formal cameras。

每 camera：

factor_error =
max(
R_camera/Q_tau_camera,
Q_tau_camera/R_camera
)

BAD_CAMERA：

factor_error >2。

定义：

bad_camera_count。

定义：

camera_log_spread：

max(log R_camera)

min(log R_camera)

for finite positive R。

CAMERA_DISAGREE：

camera_log_spread >=log(5)。

输出：

formal_camera_disagreement.csv

字段：

state
cell_id

valid_camera_count

bad_camera_count

camera_log_spread

camera_disagree

R_cam0
R_cam4
R_cam8

Q_cam0
Q_cam4
Q_cam8。

============================================================
十四、Important：Cell-Level Tail Set

R5 heavy-tail scientific Gate
was discussed at CELL response level。

Therefore formal TAIL1
must be cell-level。

读取/reconstruct：

R_cell

Q_tau_cell

using same formal measurable cameras。

定义：

E_log_cell =
abs(
log(
R_cell/Q_tau_cell
)
)。

对于每 audit state：

stretch_2.00
cubic_l0333
shear_k040
twist_60

TAIL1：

top 1%
CELL rows
by E_log_cell。

不是：

cell-camera rows top1%。

输出：

formal_cell_tail_manifest.csv

字段：

state
cell_id
E_log_cell
R_cell
Q_tau_cell
tail_rank
is_tail1。

这是最高优先级修正。

============================================================
十五、Project Camera Factors onto Tail Cells

将：

formal_camera_disagreement.csv

merge 到：

formal_cell_tail_manifest.csv

on：

state
cell_id

validate one_to_one。

Tail cell CAMERA factor：

camera_disagree。

bad_camera_count
保留。

============================================================
十六、Project LOW / UPPER onto Tail Cells

对于 cell-level factors：

LOW_CELL：

至少 2 个 formal cameras中：

LOW camera-row fraction >=0.5。

即：

majority formal cameras LOW。

UPPER_CELL：

任一 formal camera
UPPER=True。

同时记录：

low_camera_fraction

upper_camera_fraction。

输出：

formal_cell_factor_table.csv

字段：

state
cell_id

is_tail1

E_log_cell

LOW_CELL
UPPER_CELL
CAMERA_CELL

low_camera_fraction
upper_camera_fraction

bad_camera_count
camera_log_spread

boundary_clearance_min
boundary_clearance_median。

IMPORTANT：

boundary is not a binary factor
if formal validity excludes boundary-near rows。

============================================================
十七、Four-Factor Encoding Correction

正式 factor tuple：

LOW
UPPER
CAMERA

以及：

BOUNDARY_BINARY_AVAILABLE。

如果 formal valid set
makes BOUNDARY_NEAR impossible：

boundary binary factor status：

NOT APPLICABLE。

编码：

L,U,C

three bits：

000
001
010
011
100
101
110
111。

不要写：

L0B。

如果 boundary binary
is actually valid：

use four-bit：

L,U,B,C。

输出：

formal_factor_encoding.md

============================================================
十八、Cell-Level Cofactor Probabilities

基于：

formal_cell_factor_table.csv

计算：

P(LOW_CELL | TAIL1)

P(UPPER_CELL | TAIL1)

P(CAMERA_CELL | TAIL1)

如果 boundary binary applicable：

P(BOUNDARY_CELL | TAIL1)。

以及：

P(TAIL1 | LOW_CELL)

P(TAIL1 | UPPER_CELL)

P(TAIL1 | CAMERA_CELL)。

输出：

formal_tail_conditional_probabilities.csv

============================================================
十九、Cell-Level Cofactor Combination

对 TAIL1 cells：

统计 factor tuple。

输出：

formal_tail_factor_combinations.csv

字段：

state
factor_tuple
count
fraction。

报告：

most common tuple。

============================================================
二十、Matched-Support Camera Audit

因为 UPPER 预计为零，
重点比较 camera disagreement。

按 cell：

canonical support：

tau_support_cell =

median formal camera
tau_support_ratio。

log10 bins：

[-inf,-3)
[-3,-2)
[-2,-1)
[-1,0)
[0,1)
[1,2)
[2,inf)。

每 bin：

CAMERA_CELL=True

vs False。

计算：

n

median E_log_cell

p90
p95。

输出：

matched_support_camera_disagreement.csv

定义：

CAMERA COFACTOR SUPPORTED：

in >=3 populated support bins
(each group n>=20)：

camera-disagree median E_log

=2x

non-disagree median E_log。

============================================================
二十一、Boundary Clearance Diagnostic

按 cell：

boundary_clearance_min。

support bins 内：

lowest clearance quartile

vs highest clearance quartile。

计算 E_log_cell。

输出：

matched_support_boundary_clearance.csv

注意：

只报告 association。

禁止分类：

TAIL-BOUNDARY

unless formal boundary binary
actually exists。

============================================================
二十二、Spatial Clustering by Formal Factor

重新计算 cell-level tail clustering。

分别：

ALL TAIL1

LOW_CELL TAIL1

NOT-LOW TAIL1

CAMERA_CELL TAIL1

NOT-CAMERA TAIL1。

使用 R5 相同：

4-neighbour method

1000 random samples

seed 20260713。

输出：

formal_tail_spatial_clustering.csv

不能引用 R5 old result
代替。

============================================================
二十三、Correct Classification Rules

TAIL-LOW-SUPPORT：

P(LOW_CELL|TAIL1)>=0.75

and

CAMERA cofactor NOT supported

and

UPPER not supported。

TAIL-CAMERA-DISAGREEMENT：

P(CAMERA_CELL|TAIL1)>=0.50

and

CAMERA cofactor supported
in matched-support analysis。

TAIL-MIXED-LOW-CAMERA：

P(LOW_CELL|TAIL1)>=0.50

P(CAMERA_CELL|TAIL1)>=0.50

and

CAMERA cofactor supported。

TAIL-UPPER-CENSORING：

P(UPPER_CELL|TAIL1)>=0.75。

TAIL-SPATIAL-LOCALIZED：

LOW
UPPER
CAMERA

cannot explain tails

and

formal ALL TAIL1 clustering

random mean +3 std。

TAIL-MIXED：

multiple supported factors
not matching above specific class。

TAIL-UNRESOLVED：

none。

IMPORTANT：

boundary clearance association
alone cannot define a boundary-origin class
because formal rows already passed
the 8px boundary gate。

============================================================
二十四、R5A4 Gate

R5A4 TAIL CLASSIFICATION PASS iff：

formal valid set self-consistent

cell-level TAIL1 used

LOW / UPPER / CAMERA definitions locked

camera audit actually computed

spatial clustering recomputed

classification follows Section 23。

============================================================
二十五、Final Gates

R5B0:
INPUT / KEY LOCK

PASS。

R5B1:
FORMAL VALID-SET CONSISTENCY

PASS。

R5B2:
BOUNDARY CONTRADICTION RESOLVED

exact R5A source bug identified。

R5B3:
CELL-LEVEL FACTOR AUDIT

PASS。

R5B4:
CAMERA / SPATIAL AUDIT

PASS。

R5B5:
FORMAL TAIL CLASSIFICATION

PASS。

============================================================
二十六、Final CASE

CASE READY-FOR-SHAPE-POLICY:

R5B0-R5B5 ALL PASS。

结论：

Current optical metric core
is oracle-validated。

P0 FIXED_COV has：

physically consistent central
area-dilution response

and

reproducible cell-level
multiplicative optical tails。

Tail origin classification：

<ACTUAL CLASS>。

允许：

P0
P1 RIGID_COV
P2 FULL_AFFINE_COV
P3 FULL_AFFINE_ORACLE

正式比较。

CASE TAIL-UNRESOLVED:

R5B5 FAIL。

禁止 P1/P2/P3。

============================================================
二十七、报告

必须输出：

formal_tail_input_manifest.csv
formal_tail_schema_dump.txt

formal_valid_condition_source.md
formal_p0_valid_cell_camera.csv
formal_valid_row_count.csv
formal_validity_contradictions.csv

r5a_boundary_lineage.md
boundary_clearance_tail_diagnostic.csv

formal_upper_factor_audit.csv
formal_low_factor_definition.json

formal_camera_disagreement.csv
formal_cell_tail_manifest.csv
formal_cell_factor_table.csv
formal_factor_encoding.md

formal_tail_conditional_probabilities.csv
formal_tail_factor_combinations.csv

matched_support_camera_disagreement.csv
matched_support_boundary_clearance.csv

formal_tail_spatial_clustering.csv

formal_tail_factor_closure_report.md
stage3_4B_R5B_summary.md
stage3_4B_R5B_log.txt

============================================================
二十八、报告必须中文回答

A. 为什么 R5A READY
不能直接接受。

B. formal valid row definition。

C. boundary_pass exact expression。

D. boundary_distance exact semantic。

E. R5A P(BOUNDARY_NEAR|TAIL1)=0.8362
来自哪个 source column。

F. boundary contradiction exact bug。

G. formal rows boundary_distance min。

H. formal boundary_near fraction。

I. formal binary boundary factor
applicable yes/no。

J. pooled tau median。

K. LOW threshold。

L. P(LOW_CELL|TAIL1)。

M. P(UPPER_CELL|TAIL1)。

N. P(CAMERA_CELL|TAIL1)。

O. P(TAIL1|LOW_CELL)。

P. P(TAIL1|CAMERA_CELL)。

Q. most common formal factor tuple。

R. bad-camera count distribution
for tail cells。

S. camera cofactor matched-support
supported yes/no。

T. boundary clearance association。

U. ALL tail clustering。

V. LOW tail clustering。

W. NOT-LOW tail clustering。

X. CAMERA tail clustering。

Y. final tail classification。

Z. R5B0。

AA. R5B1。

AB. R5B2。

AC. R5B3。

AD. R5B4。

AE. R5B5。

AF. Final CASE。

AG. 是否允许 P1/P2/P3。

============================================================
二十九、README

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage3.4B-R5A closed the O4
noncommutation identity at float64 precision，
thereby validating O0-O4。

The CURRENT optical metric core
is now mathematically oracle-validated。

However，
the R5A tail classification
was not formally closed。

It reported：

100% boundary Gate PASS
(distance>=8px)

while also reporting：

P(boundary_near|TAIL1)=0.8362

for boundary_near defined as
distance<8px。

It also did not explicitly compute
the required P0 bad-camera distribution
or cofactor-specific spatial clustering。

Stage3.4B-R5B rebuilds
one unique formal valid cell-camera table，
defines the heavy-tail set at CELL level，
and recomputes LOW / UPPER / CAMERA factors
before assigning the final tail class。

No metric redesign is performed。

============================================================
三十、实际执行

必须实际：

lock R5 tail inputs

dump schemas

assert unique keys

trace formal validity source

build ONE formal valid table

assert boundary semantics

trace R5A boundary source column

resolve contradiction

compute boundary clearance diagnostic

recompute formal upper factor

recompute formal low factor

compute actual camera disagreement

define CELL-level top1% tail

project camera/low/upper factors to cells

use corrected factor encoding

conditional probabilities

factor combinations

matched-support camera audit

boundary-clearance diagnostic

recompute factor-specific spatial clustering

assign formal tail class

R5B0-R5B5

Final CASE

不要 rerun oracle。

不要 modify metric。

不要 P1。

不要 P2。

不要 P3。

不要 threshold tuning。

不要 tail filtering。

不要修改 TSGS。

============================================================
三十一、最终终端总结

最后只输出：

Formal valid key lock PASS/FAIL
Formal valid row count
Boundary pass expression
Formal boundary distance min
Formal boundary-near fraction
R5A boundary contradiction exact bug
Binary boundary factor applicable yes/no
Pooled tau median
LOW threshold
P(LOW_CELL|TAIL1)
P(UPPER_CELL|TAIL1)
P(CAMERA_CELL|TAIL1)
P(TAIL1|LOW_CELL)
P(TAIL1|CAMERA_CELL)
Most common factor tuple
Tail bad-camera count distribution
Camera matched-support effect yes/no
Boundary clearance association
ALL tail clustering PASS/FAIL
LOW tail clustering PASS/FAIL
NOT-LOW tail clustering PASS/FAIL
CAMERA tail clustering PASS/FAIL
Final tail classification
R5B0
R5B1
R5B2
R5B3
R5B4
R5B5
Final CASE
Can run P1/P2/P3 yes/no
formal_tail_factor_closure_report.md path
stage3_4B_R5B_summary.md path

请直接实际执行。

最高优先级：

THE METRIC CORE IS ALREADY VALIDATED。

DO NOT TOUCH O0-O4。

THE CURRENT PROBLEM IS ONLY
TAIL FACTOR SEMANTICS。

BUILD ONE FORMAL VALID TABLE。

DEFINE TAIL AT CELL LEVEL，
NOT CELL-CAMERA LEVEL。

IF BOUNDARY PASS REQUIRES DISTANCE>=8，
THEN BOUNDARY_NEAR<8 CANNOT EXIST
IN THE SAME FORMAL VALID SET。

TRACE THE EXACT R5A BUG。

COMPUTE THE REAL CAMERA DISAGREEMENT TABLE。

DO NOT CALL TAIL-MIXED
UNTIL THE FORMAL CELL-LEVEL FACTORS SUPPORT IT。

THIS IS THE FINAL TAIL-CLOSURE STEP
BEFORE P0/P1/P2/P3。
PROMPT