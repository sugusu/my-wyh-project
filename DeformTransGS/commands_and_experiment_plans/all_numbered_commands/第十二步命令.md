cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前科研状态
==================================================

Stage 3.1.1 已完成。

Repaired Optical-State Necessity Benchmark Gate：

PASS

正式结果：

Uniaxial stretch:

s=1.10
Js=1.10
tau0=1.0 masked MAE=0.003

s=1.25
Js=1.25
tau0=1.0 masked MAE=0.007

s=1.50
Js=1.50
tau0=1.0 masked MAE=0.014

s=2.00
Js=2.00
tau0=1.0 masked MAE=0.030

Spearman rho=1.0

Biaxial stretch:

s=1.50
Js=2.25
tau0=1.0 masked MAE=0.038

Twist control:

Js≈1.0
twist_30 masked MAE=0
twist_60 masked MAE=0

因此：

Optical-State Necessity Benchmark 已正式 PASS。

Benchmark 现在冻结。

禁止修改：

deformation states
Js definition
h_ratio definition
tau0
GT_DYNAMIC rule
GT_FIXED rule
material implementation
Gate threshold

当前唯一阻塞：

RT-Splatting runtime 尚未恢复。

==================================================
本阶段名称
==================================================

Stage 3.1.2:

RT-Splatting Legacy Runtime Recovery and Training Smoke Test

中文：

RT-Splatting 旧版运行环境恢复与训练冒烟测试

==================================================
一、本阶段唯一目标
==================================================

建立一个能够实际运行官方 RT-Splatting 的独立环境。

最终成功标准：

1. official RT-Splatting Python dependencies import success

2. nvdiffrast CUDA rasterization success

3. RT-Splatting 自定义 CUDA / rasterizer extensions 可编译和加载

4. GaussianModel import success

5. gaussian_renderer import success

6. official train.py --help 或等价 argument initialization success

7. 使用一个最小兼容 dataset
实际运行 10 iterations training smoke test

8. 保存有效 checkpoint / point cloud
或确认官方保存 iteration 机制下训练循环正常结束

本阶段：

不要进行正式 canonical training。

不要跑 30K。

不要实现 deformation。

不要修改 optical attributes。

==================================================
二、项目与源码边界
==================================================

项目根目录：

/data/wyh/DeformTransGS

官方 RT-Splatting：

/data/wyh/repos/RT-Splatting

RT-Splatting commit：

3f45b3c

禁止修改：

/data/wyh/repos/RT-Splatting

禁止修改：

/data/wyh/repos/TSGS

/data/wyh/RecycleGS

所有：

环境脚本
兼容 wrapper
日志
报告
smoke dataset
smoke output

保存到：

/data/wyh/DeformTransGS

==================================================
三、输出目录
==================================================

创建：

/data/wyh/DeformTransGS/experiments/stage3_1_2_runtime_recovery/

/data/wyh/DeformTransGS/runtime/rtsplat_legacy/

/data/wyh/DeformTransGS/data/rtsplat_smoke/

/data/wyh/DeformTransGS/scripts/

输出至少包括：

experiments/stage3_1_2_runtime_recovery/
├── environment_resolution.md
├── cuda_toolchain_validation.md
├── nvdiffrast_build_report.md
├── extension_build_report.md
├── import_smoke_report.md
├── training_smoke_report.md
├── runtime_manifest.json
├── stage3_1_2_summary.md
└── stage3_1_2_log.txt

==================================================
四、首先重新确认官方依赖
==================================================

读取：

/data/wyh/repos/RT-Splatting/requirements.txt

README.md

.gitmodules

所有 setup.py

所有 pyproject.toml

所有 submodule requirements。

明确列出：

Python requirement

torch

torchvision

numpy

nvdiffrast

diff-gaussian-rasterization
或其它 Gaussian CUDA rasterizer

simple-knn

nvdiffrast

nvdiffrast 是否为 Git dependency

所有需要编译的 CUDA extensions。

生成依赖图：

RT-Splatting
├── PyTorch
├── Gaussian rasterizer
├── nvdiffrast
├── simple-knn
└── other compiled modules

不要只修 nvdiffrast 后就假设项目可运行。

==================================================
五、环境目标
==================================================

目标环境名：

rtsplat_legacy2

目标首先严格匹配：

Python 3.10

torch 2.0.1

torchvision 0.15.2

torchaudio 2.0.2

PyTorch CUDA 11.8

numpy < 2

原因：

RT-Splatting requirements 固定：

torch==2.0.1
torchvision==0.15.2

PyTorch 官方提供 CUDA 11.8 build。

优先使用官方 PyTorch Conda 安装组合：

pytorch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2
pytorch-cuda=11.8

channels:

pytorch
nvidia

不要使用当前：

torch 2.6.0+cu124

环境。

==================================================
六、创建全新 Conda 环境
==================================================

检查：

conda env list

如果：

rtsplat_legacy2

存在但状态不完整：

审计后删除并重新创建。

只删除该 env。

禁止删除其它 env。

创建：

conda create -n rtsplat_legacy2 python=3.10 -y

激活后记录：

which python
python --version
sys.executable
sys.prefix

检查：

sysconfig.get_paths()["include"]

检查：

Python.h

必须确认 Conda environment 自己的 Python header 是否存在。

输出完整路径。

==================================================
七、安装官方 PyTorch 2.0.1 CUDA 11.8
==================================================

优先使用 Conda 官方组合。

安装：

pytorch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2
pytorch-cuda=11.8

根据 PyTorch 官方 channel：

-c pytorch
-c nvidia

安装后验证：

import torch

print(torch.__version__)
print(torch.version.cuda)
print(torch.cuda.is_available())
print(torch.cuda.device_count())

对每张 GPU：

device name
compute capability

运行：

torch.zeros(1, device="cuda")

要求成功。

如果 Conda solver 因当前 channel metadata
无法解析该历史组合：

记录完整 solver error。

然后使用 PyTorch 官方 CUDA 11.8 wheel：

torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2

index-url:
https://download.pytorch.org/whl/cu118

不要安装其它 torch version。

==================================================
八、建立 Environment-Local CUDA 11.8 Toolchain
==================================================

关键要求：

不要使用：

/usr/local/cuda-12.4/bin/nvcc

编译 PyTorch 2.0.1 cu118 extensions，
除非最终验证明确证明兼容。

目标：

environment-local CUDA/NVCC 11.8。

首先通过 Conda metadata 查询：

cuda-nvcc=11.8
cuda-toolkit=11.8

在 nvidia channel 是否可用。

实际执行：

conda search

记录结果。

如果 cuda-nvcc 11.8 可用：

优先安装满足 extension compilation 的
最小 CUDA development toolchain。

至少需要：

nvcc
CUDA headers
CUDA runtime development headers

不要为了图省事安装无关 GUI / profiling tools。

如果无法可靠判断最小集合：

允许安装：

cuda-toolkit=11.8

到 rtsplat_legacy2 environment。

禁止 sudo。

安装后验证：

which nvcc

nvcc --version

要求 nvcc 版本：

11.8.x

检查：

CUDA_HOME

设置：

CUDA_HOME

为 environment-local CUDA root。

不要把：

/usr/local/cuda-12.4

设为 CUDA_HOME。

记录：

CUDA_HOME
PATH
LD_LIBRARY_PATH

==================================================
九、验证 PyTorch CUDA 与 NVCC 匹配
==================================================

要求记录：

torch.version.cuda

nvcc release

目标：

torch CUDA = 11.8

nvcc = 11.8

如果 major/minor 不一致：

禁止开始 extension compilation。

必须先修环境。

运行最小：

torch.utils.cpp_extension.CUDA_HOME

记录结果。

必须指向 environment-local CUDA 11.8。

如果仍指向：

/usr/local/cuda-12.4

修正当前 shell environment。

禁止修改系统 symlink。

==================================================
十、Compiler Audit
==================================================

记录：

gcc --version
g++ --version

检查 PyTorch 2.0.1 / CUDA 11.8 extension build
是否对当前 GCC 给出 incompatible compiler warning。

不要根据记忆猜。

实际执行一个最小 PyTorch CUDA extension build test。

在：

runtime/rtsplat_legacy/

建立：

minimal_cuda_extension/

使用：

torch.utils.cpp_extension.load

编译一个最简单 CUDA add kernel。

输入：

CUDA tensor

输出：

tensor + 1

验证：

max error = 0

如果失败：

记录第一条 compiler error。

在此之前不要编译 RT-Splatting extensions。

==================================================
十一、nvdiffrast Source Selection
==================================================

官方 nvdiffrast repository：

/data/wyh/repos/nvdiffrast

如果不存在：

clone official:

NVlabs/nvdiffrast

禁止修改源码。

记录：

remote
current latest commit
latest commit date
tags

RT-Splatting commit：

3f45b3c

根据：

RT-Splatting commit date

README history

git submodule / install instruction history

nvdiffrast git log

寻找时间接近 RT-Splatting 开发时期的
official nvdiffrast commit。

重要：

当前 nvdiffrast documentation
推荐较新 PyTorch。

因此不要默认 latest main
与 torch 2.0.1 匹配。

选择 historical commit 时：

必须给出：

commit hash
commit date
选择依据

禁止随机试多个 hash。

最多：

historical candidate 1 次

latest main 1 次

==================================================
十二、编译 nvdiffrast
==================================================

在：

rtsplat_legacy2

安装：

setuptools
wheel
ninja

使用：

pip install . --no-build-isolation -v

保存完整 build log。

明确设置：

CUDA_HOME=environment-local CUDA 11.8

设置适合当前 GPU 的：

TORCH_CUDA_ARCH_LIST

必须根据实际：

torch.cuda.get_device_capability()

设置。

例如 RTX 3090 是 compute capability 8.6，
但必须从实际设备查询结果决定。

禁止凭经验硬编码。

如果服务器存在多种 GPU：

选择实际用于 smoke test 的 GPU capability。

编译完成后：

python -c "import nvdiffrast.torch as dr; print('IMPORT_OK')"

==================================================
十三、nvdiffrast CUDA Smoke Test
==================================================

不要只测试 import。

创建：

runtime/rtsplat_legacy/test_nvdiffrast_triangle.py

使用官方 API：

RasterizeCudaContext

构造一个最小三角形。

运行 CUDA rasterization。

验证：

output tensor device = cuda

output shape 正确

finite

non-zero covered pixels > 0

保存：

triangle_raster.npy

如果官方 repository 中存在对应 triangle sample：

优先同时运行官方 sample。

禁止因为 GUI / display unavailable
将 image display failure 误判为 rasterization failure。

只要求 headless numerical success。

==================================================
十四、安装 RT-Splatting Python Dependencies
==================================================

读取 requirements.txt。

因为 torch 已经固定安装：

安装其它依赖时避免 pip 自动升级 torch。

不要直接无脑：

pip install -r requirements.txt

导致 torch 被替换。

创建：

runtime/rtsplat_legacy/requirements_runtime.txt

从官方 requirements 派生。

保留所有非 torch dependencies。

排除：

torch
torchvision

并记录差异。

安装后验证：

pip check

记录 dependency conflicts。

禁止修改官方 requirements.txt。

==================================================
十五、编译 RT-Splatting 所有 CUDA Extensions
==================================================

完整检查：

.gitmodules

submodules/

所有 setup.py。

逐个编译官方 extension。

可能包括：

diff-gaussian-rasterization
simple-knn
或仓库真实模块。

必须根据仓库实际内容决定。

每个 extension：

记录：

module name
source path
build command
CUDA_HOME
TORCH_CUDA_ARCH_LIST
build success
import success

如果失败：

记录第一条真正 compiler error。

禁止修改 extension source。

输出：

extension_build_report.md

==================================================
十六、RT-Splatting Import Smoke
==================================================

使用官方仓库作为工作目录：

cd /data/wyh/repos/RT-Splatting

按 train.py 的真实 import 方式测试。

至少验证：

import torch

import nvdiffrast.torch

from scene import GaussianModel
或真实 GaussianModel import

import gaussian_renderer

import train dependencies

如果 train.py import 时会直接 parse args 或启动：

使用：

python train.py --help

或安全的等价入口。

记录所有 imports。

==================================================
十七、准备最小 RT-Splatting Smoke Dataset
==================================================

不要直接使用 StretchableFilm-v0 正式数据训练。

当前只是运行环境测试。

首先检查 RT-Splatting dataset loader。

使用其最容易支持的数据格式：

COLMAP
或
NeRF transforms

必须根据真实 loader 决定。

优先复用：

/data/wyh/DeformTransGS

已有 synthetic canonical images 和 cameras。

创建一个最小 dataset：

/data/wyh/DeformTransGS/data/rtsplat_smoke

最多：

3 cameras

分辨率：

允许降到 128 x 128

必须满足 RT-Splatting loader。

如果 RT-Splatting 要求：

transparent_masks

生成 smoke-only mask。

mask 只能根据：

GT object-present image
vs
background-only image

构建。

禁止根据 opacity 构造。

当前 mask 仅用于：

loader / training smoke。

不要用于科研指标。

==================================================
十八、10-Iteration Training Smoke Test
==================================================

检查 train.py 真实参数。

使用官方训练逻辑。

运行：

10 iterations

或者官方最小可配置 iteration 数。

禁止自行修改 train loop。

输出：

/data/wyh/DeformTransGS/baselines/rtsplat_smoke_10iter

必须保存：

完整 command

stdout
stderr

记录：

iteration 1 loss

final iteration loss

是否 backward success

是否 optimizer.step success

是否进入 rasterizer

是否进入 nvdiffrast path

是否 CUDA OOM

是否 NaN / Inf

如果 10 iteration 内官方不触发：

checkpoint save

则不要求 point_cloud.ply。

但必须证明完整：

forward
loss
backward
optimizer step

循环运行至少 10 次。

==================================================
十九、不得修改官方源码的严格检查
==================================================

本阶段结束前：

cd /data/wyh/repos/RT-Splatting

运行：

git status --short

要求：

为空

或仅存在官方 submodule build-generated ignored files。

如果存在 tracked source modification：

必须报告。

禁止偷偷修源码。

同样检查：

/data/wyh/repos/nvdiffrast

git status --short

==================================================
二十、Runtime Manifest
==================================================

生成：

runtime_manifest.json

至少包含：

conda_env

python_version

python_executable

python_include_path

python_h_path

torch_version

torch_cuda_version

torchvision_version

nvcc_path

nvcc_version

cuda_home

gpu_name

gpu_compute_capability

gcc_version

gxx_version

nvdiffrast_commit

nvdiffrast_commit_date

nvdiffrast_import

nvdiffrast_cuda_smoke

compiled_extensions

rtsplat_import_success

training_smoke_success

==================================================
二十一、Stage 3.1.2 最终判断
==================================================

stage3_1_2_summary.md 必须选择：

CASE A:

nvdiffrast CUDA smoke PASS
+
all required RT-Splatting extensions PASS
+
RT-Splatting imports PASS
+
10-iteration training smoke PASS

Decision:

RUNTIME RECOVERED

Next:

Stage 3.2 canonical RT-Splatting training
on StretchableFilm-v0.

CASE B:

nvdiffrast PASS
but RT-Splatting extension/import fails

Decision:

PARTIAL RUNTIME

Report exact blocker.

Do not train canonical model.

CASE C:

environment-local CUDA 11.8 cannot be established
or nvdiffrast CUDA smoke fails

Decision:

RUNTIME BLOCKED

Recommend container / different server environment.

Do not train canonical model.

==================================================
二十二、README 更新
==================================================

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage 3.1.1:

Optical-State Necessity Benchmark Gate
was confirmed PASS after fixing
a deformation-state parsing bug.

The repaired stretch signal is monotonic:

s=1.10 → 0.003
s=1.25 → 0.007
s=1.50 → 0.014
s=2.00 → 0.030

with Spearman rho=1.0.

Twist area-preserving controls remain zero.

Stage 3.1.2:

RT-Splatting legacy runtime recovery.

==================================================
二十三、实际执行
==================================================

必须实际：

创建 rtsplat_legacy2

安装 official PyTorch 2.0.1 CUDA 11.8

建立 env-local CUDA/NVCC 11.8

验证 torch CUDA / nvcc matching

编译最小 CUDA extension

选择 nvdiffrast source

编译 nvdiffrast

运行 CUDA triangle smoke

安装 non-torch RT-Splatting requirements

编译所有 required extensions

运行 import smoke

创建 3-view smoke dataset

运行 10-iteration training smoke

生成所有报告

不要只生成 shell script。

不要训练正式 RT-Splatting。

不要实现 deformation。

不要修改 optical parameters。

不要修改官方源码。

==================================================
二十四、最终终端总结
==================================================

最后简洁输出：

1. Conda env
2. Python version
3. Python.h path
4. torch version
5. torch CUDA version
6. nvcc path
7. nvcc version
8. CUDA_HOME
9. GPU / compute capability
10. minimal CUDA extension PASS/FAIL
11. selected nvdiffrast commit
12. nvdiffrast build PASS/FAIL
13. nvdiffrast triangle CUDA smoke PASS/FAIL
14. RT-Splatting compiled extensions
15. extension build PASS/FAIL
16. RT-Splatting imports PASS/FAIL
17. 10-iteration training smoke PASS/FAIL
18. first loss
19. final loss
20. NaN / Inf yes/no
21. git source modified yes/no
22. Stage 3.1.2 CASE
23. Runtime recovered yes/no
24. Can enter Stage 3.2 yes/no
25. runtime_manifest.json path
26. stage3_1_2_summary.md path

请直接实际执行和验证。

禁止修改任何官方 baseline source。
禁止正式训练。
PROMPT