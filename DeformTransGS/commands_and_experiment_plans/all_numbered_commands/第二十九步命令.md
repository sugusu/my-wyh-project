cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前正式状态
==================================================

Stage 3.3.R2 当前结论修正：

PROTOCOL INVALID AFTER C0 FAILURE

原因：

Projection Gate 要求：

projected Gaussian centers
inside 3-pixel-dilated alpha foreground fraction
>= 0.95

实际结果：

cam_000:
0.5284

cam_004:
0.7876

cam_008:
0.6982

全部低于 0.95。

因此：

C0 = FAIL

不是：

PASS with caveat。

按照 Stage 3.3.R2 原协议：

Projection FAIL 后
应立即停止。

因此 Stage 3.3.R2 后续：

material-cell metric

E_uniform_cell

cubic cell MAE

shear cell MAE

twist cell MAE

matched-Js cell analysis

只能保留为 invalid-protocol diagnostics。

禁止用于 scientific conclusion。

==================================================
重要机制修正
==================================================

禁止继续写：

alpha peak is displaced from projected Gaussian center
because of covariance projection。

实际 rasterizer：

power =
-0.5 * d^T C d

d =
projected mean - pixel

因此单 Gaussian kernel
在 d=0 时 power=0，
exponential falloff 最大。

Covariance controls：

shape
orientation
footprint

not projected mean location。

==================================================
本阶段名称
==================================================

Stage 3.3.R3:

Rasterizer Projection and Material Parameterization Integrity Audit

中文：

光栅器投影与材料参数坐标完整性审计

==================================================
一、唯一目标
==================================================

只回答：

1.

当前 Python material-point projection
是否与 TSGS CUDA rasterizer
内部 points_xy_image 完全一致？

2.

当前 material parameterization：

x = 0.75*u
y = 0.75*v
z = 0

是否真实对应 frozen carrier xyz？

3.

Projection foreground fraction 0.53-0.79
究竟来自：

A. projection convention mismatch

B. material world mapping mismatch

C. inactive low-opacity Gaussians

D. alpha map / geometry input mismatch

禁止：

material cell metric

point ratio

local optical consistency

screen footprint mechanism

new deformation

training

method design

==================================================
二、项目边界
==================================================

所有输出：

/data/wyh/DeformTransGS/experiments/
stage3_3R3_projection_parameterization_audit/

禁止修改：

/data/wyh/repos/TSGS

/data/wyh/RecycleGS

/data/wyh/repos/RT-Splatting

==================================================
三、锁定 Frozen Carrier 和 Alpha Maps
==================================================

定位 Stage 3.2.6A / Gold protocol
正式使用的 frozen carrier。

记录真实 tensor：

xyz
activated scale
raw scale if available
rotation
tau
opacity = 1-exp(-tau)
color

SHA256。

记录：

Gaussian count。

对 canonical：

cam_000
cam_004
cam_008

重新使用 validated Stage 3.2.1 path：

official Camera

official gaussian_renderer.render

override_color = ones

black background

two-pass white-pass alpha path

实际 fresh render：

A_can。

保存：

fresh_alpha_cam000.npy/png
fresh_alpha_cam004.npy/png
fresh_alpha_cam008.npy/png

记录 alpha map SHA256。

不要读取旧 Stage 3.3.R2 alpha map
作为唯一来源。

同时比较：

fresh alpha
vs
Stage 3.3.R2 alpha

MAE
max error
SHA256。

输出：

frozen_render_identity.csv

如果 alpha maps 不一致：

标记：

ALPHA INPUT MISMATCH

并定位 source。

==================================================
四、只读审计本地实际 Rasterizer Projection
==================================================

不要只参考上游 graphdeco。

必须检查当前本地 TSGS
实际使用的 FirstSurface rasterizer CUDA source。

定位真实 extension source。

记录：

projection input matrix

transformPoint4x4 / equivalent

homogeneous divide

NDC coordinates

ndc2Pix definition

image width / height usage

point_image storage

pixel coordinate convention

row / column convention

是否 y flip

是否 +0.5 / -0.5 offset。

输出：

local_rasterizer_projection_formula.md

必须给出实际 source path
和相关代码行 / function name。

==================================================
五、实现 Exact CUDA-Matched Projection
==================================================

新建：

analysis/exact_rasterizer_projection.py

逐字对应当前本地 CUDA projection logic。

输入：

xyz

viewmatrix / projmatrix
或实际 CUDA 使用 matrix。

输出：

p_view

p_hom

p_proj

point_image_x

point_image_y

inside image

positive depth。

禁止：

使用 OpenGL 常规经验重新推导。

禁止：

使用 cv2.projectPoints。

禁止：

使用旧 Stage 3.3 projection function。

实现必须根据本地 CUDA。

==================================================
六、旧 Projection vs Exact Projection
==================================================

对：

cam_000
cam_004
cam_008

全部 1681 Gaussians。

比较：

old_x
old_y

vs

exact_x
exact_y。

记录：

dx
dy
euclidean error。

统计：

mean
median
p90
p95
p99
max。

检查：

systematic x offset

systematic y offset

y sign / flip

x-y swap。

输出：

old_vs_exact_projection.csv

生成：

projection_error_vector_cam000.png
projection_error_vector_cam004.png
projection_error_vector_cam008.png

如果：

median pixel error >0.1

或：

p95 >0.5

则：

PROJECTION FORMULA MISMATCH FOUND。

==================================================
七、Material Grid Identity Audit
==================================================

当前 Stage 3.3.R2 假设：

41x41 grid

u,v in [-1,1]

x = 0.75*u
y = 0.75*v
z = 0。

现在禁止假设其正确。

根据真实 frozen xyz
和真实 grid_u_index / grid_v_index：

建立：

X_grid[41,41,3]。

首先检查 ordering：

每个 grid index
是否唯一。

相邻：

(i+1,j)
(i,j+1)

的 xyz distance。

记录：

u-neighbour distance
v-neighbour distance

mean/std/min/max。

然后构造 assumed mapping：

x_assumed(u,v) =
[0.75u, 0.75v, 0]

与真实 xyz 比较：

max Euclidean error
mean error
p95。

输出：

material_grid_identity.csv

要求：

如果使用 assumed mapping
进行 quadrature：

max error <=1e-6。

如果 >1e-6：

STAGE 3.3.R2 MATERIAL PARAMETERIZATION INVALID。

==================================================
八、Fit Exact Canonical Grid Parameterization
==================================================

无论 assumed mapping 是否 PASS，

从真实 X_grid 构建正式 parameterization diagnostic。

优先测试 affine plane：

x(u,v) =
c + a*u + b*v

least squares fit。

报告：

max residual
mean residual
p95。

如果：

max residual <=1e-6

则：

AFFINE GRID。

正式 mapping 使用：

c + a*u + b*v。

如果失败：

使用 material-grid bilinear interpolation：

对每 grid quad：

四个真实 xyz corners

bilinear map from local cell coordinates。

禁止再使用：

[0.75u,0.75v,0]

输出：

material_parameterization_report.md

==================================================
九、Active Gaussian Definition
==================================================

Rasterizer actual alpha：

alpha =
min(
0.99,
opacity * exp(power)
)

center：

power=0。

实际 rasterizer skip：

alpha < 1/255。

因此定义：

CENTER_ACTIVE:

opacity >=1/255。

同时：

VISIBLE:

official render radii >0。

IN_FRAME:

exact projected mean inside image。

正式 active-visible set：

CENTER_ACTIVE
AND
VISIBLE
AND
IN_FRAME。

对每 camera：

记录：

total Gaussian count

center-active count

visible count

in-frame count

active-visible-in-frame count。

输出：

active_gaussian_statistics.csv

==================================================
十、Foreground Support Validation
==================================================

alpha foreground：

A_can >1e-4。

由于 rasterizer
alpha <1/255 会 skip，

同时报告：

A>1e-4
A>1e-3
A>1/255

支持 mask。

对每 camera：

计算 projected centers
inside 3-pixel-dilated foreground fraction。

分别对：

ALL IN-FRAME GAUSSIANS

VISIBLE IN-FRAME

CENTER-ACTIVE IN-FRAME

ACTIVE-VISIBLE-IN-FRAME

四组统计。

输出：

foreground_support_validation.csv

核心 Gate 只使用：

ACTIVE-VISIBLE-IN-FRAME。

要求：

fraction >=0.95。

如果：

ALL fraction low

但 ACTIVE-VISIBLE fraction >=0.95

结论：

LOW FRACTION CAUSED BY INACTIVE GAUSSIANS。

不是 projection bug。

==================================================
十一、Single-Gaussian Projection Spot Check
==================================================

这是最关键直接验证。

每 camera：

从：

ACTIVE-VISIBLE-IN-FRAME

选择：

64 Gaussians。

优先 interior grid。

固定 random seed。

对于每 selected Gaussian j：

fresh load carrier。

只保留 Gaussian j optical contribution：

其它 Gaussian opacity
设置为 renderer-inactive。

禁止修改 TSGS。

只在 DeformTransGS adapter copy 中设置。

white override_color。

black bg。

render single-Gaussian alpha map。

要求：

single alpha map finite。

记录：

argmax pixel：

x_max
y_max

predicted exact projected mean：

x_proj
y_proj

center alpha bilinear sample：

A_at_proj

max alpha：

A_max

distance:

sqrt(
(x_max-x_proj)^2
+
(y_max-y_proj)^2
)

同时使用 projected conic
计算：

Mahalanobis distance
of argmax from projected mean。

输出：

single_gaussian_projection_spotcheck.csv

注意：

由于 discrete pixel lattice
和 anisotropic covariance，

不要要求 argmax Euclidean distance exactly zero。

Gate：

median argmax distance <=1.0 pixel

p95 <=2.0 pixels

A_at_proj / (A_max+eps) >=0.90 median。

如果 FAIL：

projection convention mismatch
或 alpha map coordinate mismatch。

==================================================
十二、Alpha Support Hole Audit
==================================================

只有 exact projection PASS 后执行。

对 fresh canonical alpha maps：

在 projected sheet interior：

统计：

A distribution：

min
p001
p01
p05
p10
median
p90。

fraction：

A=0

A<1e-6

A<1e-5

A<1e-4

A<1e-3

A<1/255。

计算：

distance transform
of low-alpha regions。

同时沿真实 material grid cells：

使用 exact material parameterization

仅做 support diagnostic：

Q9 sample canonical A。

记录每 cell：

fraction samples A<1e-6

A<1e-4

A<1/255

不计算 tau ratio。

输出：

canonical_alpha_support_holes.csv

目的：

判断 rendered alpha field
是否本身存在 sub-cell holes。

==================================================
十三、正式分类
==================================================

只能选择：

CASE P:
PROJECTION MISMATCH

old vs exact projection error
超过 Gate

或：

single Gaussian spot check FAIL。

结论：

Stage 3.3.R / R2 local metrics invalid
because material-to-pixel correspondence was wrong。

修 projection 后
重新执行 point/cell metric。

--------------------------------------------------

CASE G:
MATERIAL PARAMETERIZATION MISMATCH

assumed world mapping：

[0.75u,0.75v,0]

不匹配 frozen xyz。

结论：

Stage 3.3.R2 quadrature sampled
the wrong material surface。

使用 exact grid parameterization
重跑 cell metric。

--------------------------------------------------

CASE I:
INACTIVE GAUSSIAN SUPPORT EFFECT

projection exact。

material parameterization exact。

ALL-center foreground fraction low，

但：

ACTIVE-VISIBLE-IN-FRAME fraction >=0.95。

结论：

旧 Projection Gate
错误地将 rasterizer-inactive Gaussians
计入 support validation。

修正 Projection Gate。

然后评估 canonical alpha support holes。

--------------------------------------------------

CASE H:
CANONICAL ALPHA FIELD HAS HOLES

projection exact。

parameterization exact。

active projection support PASS。

但是：

material cell Q9
存在大量 low-alpha samples。

定义：

interior Q9 sample
fraction A<1e-4 >=0.10。

结论：

current controlled carrier
does not define a spatially continuous local optical field
at material-cell scale。

此时：

禁止继续 point/cell local consistency line。

不要通过 blur / median 强行修复。

需要重新判断：

carrier suitability。

--------------------------------------------------

CASE D:
ALPHA / GEOMETRY INPUT MISMATCH

fresh alpha vs old alpha
不一致，

或 render geometry checksum
与 projected geometry 不一致。

结论：

修复 data-flow protocol。

--------------------------------------------------

CASE CLEAN:

projection exact

parameterization exact

active support fraction >=0.95

low-alpha interior sample fraction <0.10。

此时：

Stage 3.3.R2 instability
来自 metric definition，
允许继续 metric redesign。

==================================================
十四、报告
==================================================

输出：

frozen_render_identity.csv

local_rasterizer_projection_formula.md

old_vs_exact_projection.csv

material_grid_identity.csv
material_parameterization_report.md

active_gaussian_statistics.csv
foreground_support_validation.csv

single_gaussian_projection_spotcheck.csv

canonical_alpha_support_holes.csv

projection_parameterization_audit_report.md
stage3_3R3_summary.md
stage3_3R3_log.txt

==================================================
十五、报告必须回答
==================================================

A. Stage 3.3.R2 Projection Gate
为什么实际 FAIL。

B. old vs exact projection median/p95/max error。

C. y flip / xy swap / pixel offset 是否存在。

D. assumed [0.75u,0.75v,0]
mapping max error。

E. exact material parameterization 类型：
AFFINE / BILINEAR GRID。

F. center-active Gaussian fraction。

G. active-visible-in-frame count per camera。

H. ALL center foreground fraction。

I. ACTIVE-VISIBLE foreground fraction。

J. single Gaussian argmax median/p95 distance。

K. A_at_proj/A_max median。

L. fresh alpha vs old alpha MAE。

M. canonical interior A<1e-4 fraction。

N. Q9 material samples A<1e-4 fraction。

O. low-alpha holes 是否真实存在。

P. Stage 3.3.R2 instability
主要属于：

projection

parameterization

inactive support

canonical holes

data-flow mismatch

metric definition。

Q. Final CASE：
P/G/I/H/D/CLEAN。

R. 当前真实研究状态。

S. 是否允许重跑 material-cell metric。

==================================================
十六、README
==================================================

更新：

/data/wyh/DeformTransGS/README.md

明确：

Stage 3.3.R2 incorrectly reported
Projection PASS despite center-support fractions
of 0.528 / 0.788 / 0.698,
below the predefined 0.95 threshold。

Therefore downstream cell-level scientific metrics
are not accepted。

Stage 3.3.R3 audits
the exact CUDA projection convention,
the frozen material-grid parameterization,
rasterizer-active Gaussian support,
and canonical alpha-field holes
before any further local optical analysis。

==================================================
十七、实际执行
==================================================

必须实际：

fresh canonical white-pass render

alpha checksum comparison

local CUDA projection audit

exact projection implementation

old vs exact comparison

material grid identity audit

exact parameterization fit

active Gaussian statistics

conditional foreground support validation

single Gaussian projection renders

canonical alpha hole audit

Final CASE

不要计算 cell optical ratio。

不要计算 cubic MAE。

不要 matched Js。

不要 mechanism。

不要训练。

不要设计 method。

不要修改 TSGS。

==================================================
十八、最终终端总结
==================================================

最后输出：

1. Old Projection Gate true PASS/FAIL
2. Exact projection median/p95/max error
3. Projection mismatch type
4. Assumed grid mapping max error
5. Exact parameterization type
6. Center-active fraction
7. Active-visible counts per camera
8. ALL foreground fractions
9. ACTIVE-VISIBLE foreground fractions
10. Single Gaussian argmax median/p95 distance
11. A_at_proj/A_max median
12. Fresh-old alpha MAE
13. Interior A<1e-4 fraction
14. Q9 A<1e-4 fraction
15. Canonical alpha holes yes/no
16. Primary root cause
17. Final CASE
18. Can rerun material-cell metric yes/no
19. projection_parameterization_audit_report.md path
20. stage3_3R3_summary.md path

请直接实际检查和验证。

不要训练。
不要研究 optical mechanism。
不要重新设计 metric。
不要修改 TSGS。
PROMPT