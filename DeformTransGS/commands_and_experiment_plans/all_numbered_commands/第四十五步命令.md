

## 一、Stage 3.5A 的结果非常强

最核心的一张表其实是：

| 方法              |    六状态平均中心误差 |
| --------------- | -----------: |
| P2 固定光学状态       |     0.279456 |
| (\tau/J_s)      | **1.183019** |
| opacity 线性缩放    |     0.125755 |
| KIOT-Continuous | **0.039124** |
| KIOT-CUDA       | **0.027946** |

KIOT-CUDA 相对 P2：

[
\frac{0.279456-0.027946}{0.279456}
=90.0%
]

也就是：

> **中心物理响应误差降低 90%。**

而且 stretch2：

[
Q=0.5
]

P2：

[
0.955487
]

传统物理直觉 (\tau/J_s)：

[
2.734181
]

KIOT-CUDA：

[
\boxed{0.545549}
]

这组结果已经非常像论文里的核心结果了。

---

## 二、现在真正成立的论文逻辑

我建议你以后不要再讲：

> “形变后透明度应该除以面积变化。”

这个故事已经被实验杀死了。

现在的故事应该是：

### 第一层：几何运动学正确以后，光学状态反而错了

PhysGaussian 将 Gaussian kernel 与连续介质运动学结合，使核随物理形变演化。([arXiv][1])

你的实验进一步发现：

[
\Sigma'=F\Sigma F^T
]

之后：

[
R:0.463\rightarrow0.955
]

即原来固定协方差产生的隐式面积稀释几乎被抵消。

---

### 第二层：直接缩放光学深度也不对

因为 rasterizer 的实际参数语义是：

[
o=1-e^{-\tau}
]

然后：

[
\alpha=o,g
]

其中 (g) 是 Gaussian 核幅值。

所以：

[
\tau'=\frac{\tau}{J_s}
]

只在：

[
g=1
]

也就是 Gaussian 中心严格正确。

离开中心后错误迅速增加。

你已经用 CUDA 单 Gaussian 实验验证解析模型和实际 renderer：

[
median=0,\qquad p95=0
]

不是理论脑补。

---

### 第三层：问题不是标量状态容量不足

这是 K6、K7 最重要的地方。

best scalar oracle 相对 (\tau/J_s)：

[
97.59%
]

RMSE 改善。

Free-(\tau) patch：

> `CAPACITY-SUPPORTED`

所以：

[
\boxed{
\text{scalar state 有容量，scalar transport rule 错了}
}
]

这句话我觉得可以成为论文 Introduction 中非常核心的发现。

---

### 第四层：KIOT 传输的是“核积分光学贡献”

不是中心光学深度：

[
\tau
]

而是：

[
\Phi(o)
=======

\int_{\text{Gaussian kernel}}
-\log(1-og),dA
]

然后：

[
\boxed{
\Phi(o')
========

\frac1{J_s}\Phi(o)
}
]

实验结果说明，这个思路确实能恢复物理响应。

最新的一手文献边界也对你有利：PhysGaussian关注 Gaussian kernel 的物理运动学；OMG研究 opacity 与材料截面属性的依赖；RT-Splatting将几何占据与光学 opacity 解耦；GLINT则显式分解透明场景中的界面、反射和透射辐射。([arXiv][1])

**基于我对这些一手来源的核查，我没有发现它们明确提出“用局部面积伸长 (J_s) 传输 kernel-integrated optical contribution（核积分光学贡献）”这一规则。**这是当前检索下的推断，不是“绝对没人做过”的最终新颖性证明。([arXiv][1])

所以这个方向现在**确实值得继续**。

---

# 三、但是我发现 KIOT-CUDA 还有一个必须修的小洞

看你的 limiting case：

```text
q=1 tau=1e-6:
max opacity diff = 1e-6

q=1 tau=1e-4:
max opacity diff = 1e-4
```

等等。

当：

[
q=1
]

任何 transport 都必须：

[
o'=o
]

严格 identity（恒等）。

为什么 B4 没做到？

因为：

[
o\le\alpha_{\rm skip}
]

时：

[
\Phi_{\rm CUDA}(o)=0
]

所以 inverse：

[
\Phi^{-1}(0)
]

返回了：

[
o'=0
]

但实际上：

[
[0,\alpha_{\rm skip}]
]

整个区间在 CUDA skip 语义下都对应：

[
\Phi_{\rm CUDA}=0
]

这是一个**多对一映射**。

因此 inverse 不唯一。

Codex 当前默认选了 0。

这在当前 controlled carrier 上可能影响极小，但到了真实 TSGS 百万 Gaussian：

> **大量低 opacity Gaussian 很可能被无意义清零。**

所以：

> **现在还不能直接把当前 B4 原样塞进 TSGS。**

这是方法实现问题，不是方法假设失败。

---

# 四、还有一个必须明确的研究边界：目前只证明 (J_s\ge1)

你的 states：

* stretch；
* biaxial stretch；
* positive cubic stretch；
* area-preserving shear；
* twist。

本质上：

[
J_s\ge1
]

所以：

[
q=\frac1{J_s}\le1
]

研究的是：

> **面积扩张 → 薄膜变薄。**

还没有测试：

[
J_s<1
]

即：

> 面积压缩 → 薄膜变厚。

这对 KIOT 很重要。

因为 (\Phi(o)) 存在最大值。

当：

[
q>1
]

可能出现：

[
q\Phi(o)>\Phi_{\max}
]

此时不存在：

[
o'\in[0,1]
]

满足目标。

所以现在论文不能写：

> arbitrary non-rigid deformation（任意非刚性形变）。

当前严谨表述应该是：

> **stretch-dominated thin transparent deformation**

中文：

> **以面积拉伸为主的薄透明材料形变。**

这个范围我觉得完全可以接受。

---

# 五、下一步我不建议马上训练 TSGS

还差一个很小的：

# Stage 3.5A-R1：KIOT Method Closure and Fast Inverse Audit

中文：

> **KIOT 方法闭环与高效逆映射审计**

这不是再查机制。

只处理三个工程/方法问题：

1. 修复 (q=1) identity；
2. 明确 (J_s\ge1) 的方法范围；
3. 把每 Gaussian 100 次二分逆函数变成 GPU 高效版本。

毕竟你的 TSGS full checkpoint：

[
991,832\text{ Gaussians}
]

不能给每个 Gaussian 用 Python 循环二分 100 次。那服务器会当场开始思考人生。

直接执行：

````bash
cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式科学状态
============================================================

Stage3.5A completed：

T0 PASS
T1 PASS
T2 SUPPORTED
T3 SUPPORTED
T4 SUPPORTED
T5 MIXED/WEAK
T6 SUPPORTED。

Final CASE：

ANALYTIC-KIOT-SUPPORTED。

This Final CASE is ACCEPTED。

============================================================
已验证核心结果
============================================================

Six area-changing state
mean central errors：

B0 P2 fixed optical state：
0.279456

B1 tau/Js：
1.183019

B2 opacity linear：
0.125755

B3 KIOT continuous：
0.039124

B4 KIOT CUDA-aware：
0.027946。

Therefore：

B3 reduces P2 central error by：
86%

B4 reduces P2 central error by：
90%。

B4 also improves over B3。

------------------------------------------------------------

stretch_2.00：

physical Q：
0.500000

B0 P2：
0.955487

B1 tau/Js：
2.734181

B2 opacity linear：
0.704969

B3 KIOT continuous：
0.563768

B4 KIOT CUDA-aware：
0.545549。

------------------------------------------------------------

Isolated scalar RMSE：

P3 tau/Js：
0.00109687

opacity linear：
0.000388821

KIOT continuous：
0.000174260

KIOT CUDA：
0.000131347

best scalar oracle：
0.0000258005。

Median oracle gain recovery：

KIOT continuous：
0.86

KIOT CUDA：
0.90。

------------------------------------------------------------

Stage3.4C-R1 established：

SCALAR-RULE-WRONG

and：

PATCH FREE-TAU CAPACITY-SUPPORTED。

Therefore：

one scalar optical state per Gaussian
has sufficient local capacity
in the current controlled carrier。

The supported problem is：

the optical-state TRANSPORT RULE
is wrong。

============================================================
正式 candidate method
============================================================

KIOT：

Kernel-Integrated Opacity Transport。

中文：

核积分不透明度传输。

Core rule：

Phi(o_new)
=
q Phi(o_old)

where：

q =1/Js。

B3：

Phi_cont(o)
=
2*pi*Li2(o)。

B4：

Phi_cuda(o)
uses the actual local rasterizer
alpha skip and alpha clamp semantics。

Do NOT claim：

Li2 itself is novel。

Candidate contribution under test：

deformation-aware transport
of kernel-integrated optical state。

============================================================
当前必须修复的问题 1：
SUB-SKIP INVERSE NON-UNIQUENESS
============================================================

Stage3.5A limiting tests reported：

q=1
tau=1e-6

max opacity diff =
1e-6

q=1
tau=1e-4

max opacity diff =
1e-4。

But for q=1：

any valid optical-state transport
must satisfy：

o_new = o_old

exactly。

Root cause hypothesis：

for：

o <= ALPHA_SKIP

Phi_cuda(o)=0。

Therefore：

Phi_cuda inverse at target=0
is non-unique。

Current B4 selects：

o_new=0。

But every：

o in [0,ALPHA_SKIP]

is renderer-equivalent
under the isolated CUDA skip semantics。

The inverse requires
a principled tie-break rule。

============================================================
当前必须明确的问题 2：
DEFORMATION DOMAIN
============================================================

Current benchmark only validates：

Js >=1

therefore：

q=1/Js <=1。

This corresponds to：

area-preserving

or

area-expanding / thinning deformation。

No area-compression states：

Js<1

q>1

have been validated。

For q>1：

q Phi(o)

may exceed：

max representable Phi。

Therefore：

KIOT may have no scalar solution
for high-opacity Gaussians。

Do NOT claim：

arbitrary non-rigid deformation。

============================================================
当前必须解决的问题 3：
INVERSE PERFORMANCE
============================================================

Current KIOT implementation
uses scalar monotonic bisection。

The full TSGS Stage-2 checkpoint has：

991832 Gaussians。

A Python per-Gaussian
100-iteration inverse loop
is not an acceptable integration path。

Need：

GPU-vectorized

or

lookup-table-based inverse。

============================================================
本阶段名称
============================================================

Stage 3.5A-R1:

KIOT Method Closure,
Domain,
and Fast Inverse Audit

中文：

KIOT 方法闭环、
适用域与高效逆映射审计

============================================================
一、唯一目标
============================================================

Answer：

Q1.

How should Phi_cuda^{-1}(0)
be defined so that KIOT
is exact identity when q=1？

Q2.

What is the formal feasible domain
of continuous KIOT and CUDA-aware KIOT
for q>1？

Q3.

Can a GPU/LUT inverse reproduce
the Stage3.5A B4 results
without expensive scalar bisection？

Q4.

After identity repair，
does KIOT-CUDA preserve
the Stage3.5A 90% central-error reduction？

禁止：

real TSGS training

real carrier integration

MLP

residual model

new optical representation

changing Phi definition

changing q

changing covariance

changing metric

changing frozen keys。

============================================================
二、输出目录
============================================================

/data/wyh/DeformTransGS/experiments/
stage3_5A_R1_kiot_method_closure/

新增代码：

/data/wyh/DeformTransGS/analysis/

禁止覆盖 Stage3.5A。

============================================================
三、Protocol Lock
============================================================

Lock：

Stage3.5A protocol

renderer constants

carrier

deformation inputs

P2 geometry

frozen camera keys

frozen cell keys

optical metric

Q_tau。

Output：

kiot_r1_protocol_lock.json。

All SHA values
must match Stage3.5A。

Gate D0：

PASS / FAIL。

============================================================
四、Audit Phi_cuda Plateau
============================================================

Use actual：

ALPHA_SKIP

ALPHA_MAX。

Generate opacity grid：

0
1e-8
1e-7
1e-6
1e-5
1e-4

0.25*ALPHA_SKIP
0.5*ALPHA_SKIP
0.9*ALPHA_SKIP
ALPHA_SKIP
1.1*ALPHA_SKIP

0.01
0.1
0.5
0.9
0.99
0.999。

Compute：

Phi_cuda(o)。

Output：

phi_cuda_plateau_audit.csv。

Must explicitly verify：

for 0<=o<=ALPHA_SKIP：

Phi_cuda(o)=0。

Explain：

Phi_cuda is monotonic
but NOT strictly monotonic
on [0,1)。

Therefore：

its inverse is set-valued
at target=0。

============================================================
五、Identity-Preserving Tie Break
============================================================

Define repaired CUDA KIOT：

B4R_KIOT_CUDA_IDENTITY_SAFE。

Formal rule：

1.

If abs(q-1)<=1e-12：

return o_old EXACTLY。

2.

Compute：

phi_old =
Phi_cuda(o_old)。

3.

If：

phi_old ==0
and q<1：

use the CONTINUOUS KIOT solution：

Phi_cont(o_new)
=
q Phi_cont(o_old)。

Reason：

all sub-skip opacities
are equivalent under current CUDA contribution semantics，

but continuous KIOT provides
a unique latent optical-state transport
and avoids arbitrary collapse to zero。

This is a tie-break rule
only on the Phi_cuda zero plateau。

4.

Otherwise：

solve：

Phi_cuda(o_new)
=
q Phi_cuda(o_old)。

Implement：

```python
def kiot_cuda_identity_safe(
    opacity,
    q,
):
    opacity = np.asarray(
        opacity,
        dtype=np.float64,
    )

    q = np.asarray(
        q,
        dtype=np.float64,
    )

    if opacity.shape != q.shape:
        opacity, q = np.broadcast_arrays(
            opacity,
            q,
        )

    out = np.empty_like(opacity)

    identity = np.abs(
        q -1.0
    ) <=1e-12

    out[identity] = opacity[identity]

    active = ~identity

    phi_old = phi_cuda(
        opacity[active]
    )

    plateau = (
        phi_old ==0.0
    )

    sub_opacity = opacity[active]
    sub_q = q[active]

    active_out = np.empty_like(
        sub_opacity
    )

    if np.any(plateau):
        active_out[plateau] = (
            kiot_continuous(
                sub_opacity[plateau],
                sub_q[plateau],
            )
        )

    normal = ~plateau

    if np.any(normal):
        target = (
            sub_q[normal]
            * phi_old[normal]
        )

        active_out[normal] = (
            invert_phi_cuda(
                target
            )
        )

    out[active] = active_out

    return out
````

Use robust floating comparison
for plateau according to actual implementation。

Do NOT silently use exact `==`
if numerical Phi implementation
does not produce exact zero。

Output：

identity_safe_definition.md。

============================================================
六、Identity Tests
================

Opacity grid：

100000 log/linear mixed samples
from：

1e-10 to 1-1e-10。

q=1。

Compare：

B1
B2
B3
B4 original
B4R repaired。

Output：

q1_identity_test.csv。

B4R Gate：

max opacity diff <=1e-15。

Also test complete carrier：

all 1681 Gaussians

shear_k020

shear_k040

twist_60

where Js=1。

Require：

tau transported
vs canonical

max diff <=1e-12。

Output：

area_preserving_policy_identity.csv。

Gate D1：

PASS only if all identity tests pass。

============================================================
七、Sub-Skip Carrier Audit
========================

For current 1681 carrier：

report opacity:

min
p001
p01
p05
p10
median
p90
max。

Count：

o < ALPHA_SKIP

o == ALPHA_SKIP within 1e-12

o > ALPHA_SKIP。

Output：

carrier_subskip_opacity_audit.csv。

Also report：

what fraction of Stage3.5A B4
Gaussians changed only because
the old inverse selected zero
on the plateau。

============================================================
八、Expansion-Domain Tests
========================

Formal supported domain under test：

0 < q <=1。

Test q：

1
0.95
0.8
2/3
0.5
0.25
0.1。

Opacity grid：

same 100000 values。

For B3 and B4R：

verify：

opacity_new finite

0<=opacity_new<1

q decreases
→ opacity_new non-increasing。

Verify transport identity：

B3：

# Phi_cont(o_new)

q Phi_cont(o_old)。

B4R：

for phi_old>0：

# Phi_cuda(o_new)

q Phi_cuda(o_old)。

For plateau samples：

verify continuous tie-break identity。

Output：

kiot_expansion_domain_test.csv。

Gate D2：

PASS。

============================================================
九、Compression Feasibility Diagnostic
====================================

DIAGNOSTIC ONLY。

q：

1.1
1.25
1.5
2.0
4.0。

For continuous：

Phi_cont_max =

limit o→1-
Phi_cont(o)。

For CUDA-aware：

Phi_cuda_max =

limit o→1-
Phi_cuda(o)

under actual ALPHA_MAX。

For every：

q
carrier Gaussian

compute target：

q Phi(o_old)。

Feasible iff：

target <=Phi_max。

Output：

kiot_compression_feasibility.csv。

Fields：

policy_integral
q
n
feasible_fraction
infeasible_fraction

opacity median among infeasible
opacity p10/p90。

No method modification。

Formal scope decision：

If any tested q>1
has nonzero infeasible fraction：

CURRENT KIOT SCOPE =

AREA-PRESERVING OR AREA-EXPANDING
THIN-SURFACE DEFORMATION

Js>=1。

Do NOT claim compression support。

Output：

kiot_domain_scope.md。

============================================================
十、Reference Bisection Implementation
====================================

Freeze current Stage3.5A
100-iteration float64 bisection
as：

REFERENCE_INVERSE。

Do not modify。

Generate：

1,000,000 random pairs：

opacity：

mixture：

50% uniform [0,1)

25% log-uniform [1e-10,1e-2]

25% concentrated [0.9,1)

q：

uniform [0.1,1]。

seed：

20260713。

Compute B4R reference opacity。

Save only summary/hash
unless memory permits。

============================================================
十一、GPU LUT Inverse
==================

Implement：

analysis/kiot_fast_inverse.py

Build monotonic LUT
for the STRICTLY MONOTONIC
Phi_cuda branch：

o in：

[ALPHA_SKIP, 1-1e-8]。

Initial LUT size：

65536。

Store：

opacity_lut

phi_lut。

Use torch tensors
on CUDA。

For target phi：

torch.searchsorted(
phi_lut,
target,
)

then linear interpolation
between adjacent entries。

Handle：

identity branch

zero plateau continuous tie-break

outside range error。

Do NOT clamp infeasible q>1 target。

Current formal q<=1
must always be feasible。

============================================================
十二、LUT Accuracy
===============

Compare：

GPU LUT B4R

vs

REFERENCE_INVERSE

for 1,000,000 random test pairs。

Report opacity error：

median
p95
p99
max。

Report transported Phi error：

abs(
Phi_cuda(o_lut)
---------------

target
)

and relative error
for target>1e-10。

Output：

fast_inverse_accuracy.csv。

Gate：

opacity：

p99 <=1e-6

max <=1e-5。

Phi：

p99 relative <=1e-5。

If FAIL：

increase LUT：

131072

then：

262144。

Use smallest LUT that passes。

Record selected LUT size。

Gate D3。

============================================================
十三、GPU Performance Benchmark
============================

Benchmark：

N=1681
N=100000
N=991832
N=2000000。

Methods：

CPU scalar 100-iter bisection
only for N<=100000

GPU vectorized bisection
if implemented

GPU LUT。

Warmup：

20。

Measured iterations：

100
for N<=100000

20
for larger N。

torch.cuda.synchronize
before/after timing。

Output：

kiot_inverse_runtime_benchmark.csv。

Report：

milliseconds

Gaussians / second

speedup vs reference
where reference measured。

No hard performance Gate。

============================================================
十四、Stage3.5A B4R Regression
===========================

Using EXACT Stage3.5A:

P2 geometry

10 states

3 cameras

frozen keys

metric。

Replace only：

B4 old

with：

B4R identity-safe GPU LUT。

Fresh render。

Output：

b4r_cell_response.csv
b4r_central_response.csv
b4r_tail_severity.csv。

Compare B4R vs old B4：

six area-changing states：

median R diff

central error diff。

Area-preserving：

alpha max diff vs B0

median R diff vs1。

Output：

b4_vs_b4r_regression.csv。

Gate D4：

six-state B4R mean central error <=0.035。

P2→B4R improvement >=0.85。

stretch2 central error <=0.08。

area-preserving：

tau identity max <=1e-12。

============================================================
十五、Method Equation Lock
=======================

If D0-D4 PASS，

write：

kiot_method_equations.md。

Formal CURRENT KIOT scope：

Js>=1。

q=1/Js。

Canonical opacity：

o_i =
1-exp(-tau_i)。

Define renderer-aware integral：

Phi_cuda(o)。

Transport：

if q=1：

o_i' = o_i。

elif Phi_cuda(o_i)>0：

o_i' =

Phi_cuda^{-1}(
q_i Phi_cuda(o_i)
)。

else：

o_i' =

Phi_cont^{-1}(
q_i Phi_cont(o_i)
)。

Then：

tau_i' =
-log(1-o_i')。

Explicitly label the last branch：

zero-plateau tie-break。

Do NOT claim it changes visible
current-renderer contribution
while both states remain sub-skip。

============================================================
十六、Literature / Claim Boundary
==============================

Do NOT perform broad web scraping。

Only update project claim wording：

"We investigate deformation-aware transport
of kernel-integrated Gaussian optical contribution."

Do NOT write：

first Li2 opacity correction

first physically based opacity

first transparent dynamic Gaussian。

Do NOT claim：

arbitrary deformation。

Current scope：

stretch-dominated thin semi-transparent surfaces
with local Js>=1。

============================================================
十七、Strict Gates
===============

D0:
PROTOCOL LOCK

PASS。

D1:
IDENTITY SAFE

PASS。

D2:
EXPANSION DOMAIN

PASS。

D3:
FAST INVERSE ACCURACY

PASS。

D4:
STAGE3.5A METHOD REGRESSION

PASS。

============================================================
十八、Final CASE
=============

CASE KIOT-READY-FOR-RECONSTRUCTION-BRIDGE:

D0-D4 ALL PASS。

Conclusion：

KIOT is a numerically closed，
identity-preserving，
efficient scalar optical-state transport
for the currently validated domain：

Js>=1。

The method retains the Stage3.5A
central optical restoration result
while avoiding arbitrary collapse
of sub-skip latent opacity states。

ALLOW：

integration into a learned/reconstructed
transparent Gaussian carrier。

---

CASE KIOT-INVERSE-IMPLEMENTATION-FAIL:

D1 or D3 FAIL。

Fix inverse only。

---

CASE KIOT-REGRESSION:

D4 FAIL。

Return to method semantics。

============================================================
十九、Required Outputs
===================

kiot_r1_protocol_lock.json

phi_cuda_plateau_audit.csv
identity_safe_definition.md

q1_identity_test.csv
area_preserving_policy_identity.csv
carrier_subskip_opacity_audit.csv

kiot_expansion_domain_test.csv
kiot_compression_feasibility.csv
kiot_domain_scope.md

fast_inverse_accuracy.csv
kiot_inverse_runtime_benchmark.csv

b4r_cell_response.csv
b4r_central_response.csv
b4r_tail_severity.csv
b4_vs_b4r_regression.csv

kiot_method_equations.md

kiot_method_closure_report.md
stage3_5A_R1_summary.md
stage3_5A_R1_log.txt

============================================================
二十、最终报告必须中文回答
=============

A. Stage3.5A 是否真正支持 KIOT candidate method。

B. 为什么 B4 old 在 q=1 sub-skip 区域不是 identity。

C. Phi_cuda zero plateau 范围。

D. B4R tie-break rule。

E. q=1 opacity max diff。

F. area-preserving full-carrier tau max diff。

G. carrier sub-skip Gaussian fraction。

H. q<=1 domain test。

I. continuous q>1 infeasible fractions。

J. CUDA q>1 infeasible fractions。

K. formal method scope。

L. selected LUT size。

M. LUT opacity p99/max error。

N. LUT Phi p99 relative error。

O. 991832 Gaussian LUT runtime。

P. B4 old mean central error。

Q. B4R mean central error。

R. stretch2 B4R median R / Q。

S. P2→B4R improvement。

T. area-preserving B4R responses。

U. D0。

V. D1。

W. D2。

X. D3。

Y. D4。

Z. Final CASE。

AA. 是否允许 reconstructed transparent carrier integration。

============================================================
二十一、README
==========

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage3.5A established KIOT
as the first candidate optical-state transport method
in the project。

CUDA-aware KIOT reduced
the six-state central response error
from 0.279456 to 0.027946，
a 90% improvement over fixed optical state，
while tau/Js increased error to 1.183019。

However，
the CUDA-aware kernel integral contains
a zero plateau for opacity below
the rasterizer alpha skip threshold。

The original inverse selected zero
on this plateau，
so it was not exact identity for q=1
at sub-skip opacity values。

Stage3.5A-R1 introduces
an identity-preserving zero-plateau tie-break，
audits the validated deformation domain，
and replaces scalar bisection
with an accurate GPU LUT inverse
before reconstructed-carrier integration。

============================================================
二十二、实际执行
========

必须实际：

lock protocol

audit Phi_cuda plateau

implement identity-safe B4R

run q=1 identity tests

run area-preserving carrier identity

audit sub-skip carrier opacity

test q<=1 domain

audit q>1 feasibility

freeze reference bisection

implement GPU LUT inverse

test one million random pairs

select minimum passing LUT size

benchmark 991832 Gaussians

rerun exact Stage3.5A B4R

compare B4 old vs B4R

D0-D4

Final CASE

不要：

train TSGS

integrate TSGS

MLP

change metric

change covariance

change q

claim arbitrary deformation。
PROMPT

```

## 我的真实评价

**现在这条线已经值得认真保了。**

但论文主线目前必须写得窄而准：

> **针对以面积拉伸为主的薄半透明材料，在 Gaussian kernel 随连续介质形变完整传输后，传统固定 opacity 或直接厚度缩放无法保持正确光学演化。我们传输核积分有效光学贡献，从而得到一个渲染器感知的标量 opacity 更新。**

我刚做的最新一手文献核查里，PhysGaussian解决物理运动学与 Gaussian kernel 演化，OMG研究 opacity 与材料属性依赖，RT-Splatting和 GLINT解决透明 Gaussian 的表示与辐射分解；这些相邻问题都已经有人做，但我没有在这些工作中看到你现在这条“**局部面积伸长驱动核积分光学状态传输**”的明确规则。这个判断是基于当前一手来源的检索推断，还需要投稿前做一次系统新颖性检索。:contentReference[oaicite:3]{index=3}

**R1 一过，下一步不要再做解析机制审计。直接进入 reconstructed transparent carrier（重建得到的透明 Gaussian 载体）桥接实验。**
```

[1]: https://arxiv.org/abs/2311.12198?utm_source=chatgpt.com "PhysGaussian: Physics-Integrated 3D Gaussians for Generative Dynamics"
