cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前研究主线
==================================================

核心研究问题：

透明 / 半透明 Gaussian 在 non-rigid deformation 后，
几何传输与光学状态之间存在什么关系？

当前禁止换服务器。

禁止重新研究 RT-Splatting runtime。

禁止重新研究 ASG。

禁止更换 benchmark。

禁止增加 volumetric path tracing method。

==================================================
已有冻结事实
==================================================

StretchableFilm-v0 benchmark：

Js =
abs(det(F)) * norm(F^{-T} n)

h_ratio =
1 / Js

GT synthetic optical rule：

tau_gt =
tau0 * h_ratio

opacity_gt =
1 - exp(-tau_gt)

tau0=1.0

GT_DYNAMIC vs GT_FIXED：

stretch_1.10:
Js=1.10
MAE=0.003

stretch_1.25:
Js=1.25
MAE=0.007

stretch_1.50:
Js=1.50
MAE=0.014

stretch_2.00:
Js=2.00
MAE=0.030

biaxial_1.50:
Js=2.25
MAE=0.038

twist_60:
Js≈1
MAE=0

Benchmark Gate PASS。

==================================================
Stage 3.2 当前反常结果
==================================================

Controlled ThinFilm Gaussian carrier：

1681 Gaussians
41x41 grid

Gaussian identity fixed

no densification
no pruning

canonical fit：

NORE=0.0000

E1：

geometry transported：

xyz' = phi(xyz)

Sigma' = F Sigma F^T

tau fixed：

tau' = canonical tau

color fixed。

但是：

all states E1 NORE < 0.0002

包括：

stretch_2.00
biaxial_1.50

E1 fixed tau
几乎完美匹配 GT_DYNAMIC。

因此出现逻辑问题：

GT_DYNAMIC vs GT_FIXED
存在 MAE 0.030~0.038

但：

E1_FIXED_TAU vs GT_DYNAMIC
近乎 0 error。

当前报告声称：

“Mitsuba path-traced signal
cannot be reproduced by alpha blending”

该结论当前不成立。

因为：

E1 alpha-composite renderer
实际上几乎完美匹配 GT_DYNAMIC。

==================================================
本阶段名称
==================================================

Stage 3.2.2:

Fixed-Tau Paradox Audit and Implicit Optical Transport Analysis

中文：

固定 Tau 反常现象审计与隐式光学传输分析

==================================================
一、本阶段唯一目标
==================================================

回答：

为什么：

per-Gaussian tau 固定

但：

geometry covariance transport 后

最终 render
几乎完全匹配 deformation-dependent GT_DYNAMIC？

必须严格区分：

A. evaluation / path / state mutation bug

B. Gaussian projected kernel change

C. Gaussian overlap / density change

D. covariance transport induced accumulated-alpha change

E. benchmark-carrier optical semantic mismatch

当前不要提出方法。

禁止实现：

tau' = tau / Js

作为正式方法。

允许将 GT oracle tau
用于 diagnostic only。

==================================================
二、项目边界
==================================================

所有新增代码和结果：

/data/wyh/DeformTransGS

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

禁止覆盖：

Stage 3.1.1
Stage 3.2
Stage 3.2.1

已有正式结果。

创建：

/data/wyh/DeformTransGS/experiments/
stage3_2_2_fixed_tau_paradox/

==================================================
三、输出
==================================================

至少生成：

path_and_file_integrity.md
parameter_integrity.md
independent_metric_recheck.csv

three_way_reference_metrics.csv

alpha_field_metrics.csv
material_point_alpha_metrics.csv

geometry_component_ablation.csv
oracle_tau_diagnostic.csv

implicit_transport_report.md
stage3_2_2_summary.md
stage3_2_2_log.txt

以及必要可视化。

==================================================
四、第一步：文件路径和 GT 完整性审计
==================================================

不要使用现有 metric pipeline。

首先定位真实文件：

每个 state：

GT_DYNAMIC EXR / array path

GT_FIXED EXR / array path

background-only path

E1_FIXED_TAU render path

E2_TAU_REFIT render path

E2_COLOR_REFIT render path

正式 states：

canonical
stretch_1.10
stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.10
biaxial_1.25
biaxial_1.50
twist_30
twist_60

对每个文件：

absolute path
file size
SHA256

记录。

检查：

GT_DYNAMIC path != GT_FIXED path

E1 path != GT_DYNAMIC path

E1 path != E2 path

不得只比较字符串。

读取实际 array 后计算：

array shape

dtype

min
max
mean

array byte checksum。

重点检查：

stretch_1.10
stretch_1.25
stretch_1.50
stretch_2.00

是否再次存在：

underscore decimal parsing bug。

禁止使用：

float(name.split("_")[1])

建立唯一正式 state parser。

例如显式 mapping。

==================================================
五、独立重新计算 GT Gate
==================================================

新建完全独立脚本：

analysis/recheck_stage3_2_paradox.py

禁止 import：

Stage 3.1.1 metrics code

Stage 3.2 metrics code

只允许：

numpy
OpenEXR/imageio
scipy

以及简单 mask operations。

独立读取：

GT_DYNAMIC
GT_FIXED
background

重新定义 optical effect mask。

严格使用 frozen benchmark
相同定义。

独立计算：

GT_DYNAMIC vs GT_FIXED

Masked MAE
RMSE
PSNR

必须重新得到接近：

stretch_1.10 0.003
stretch_1.25 0.007
stretch_1.50 0.014
stretch_2.00 0.030
biaxial_1.50 0.038
twist_60 0

如果无法复现：

停止后续分析。

结论：

benchmark metric pipeline bug
或文件 mapping bug。

==================================================
六、独立重新计算 E1 Metrics
==================================================

使用同一个独立脚本。

计算：

E1 vs GT_DYNAMIC

E1 vs GT_FIXED

GT_DYNAMIC vs GT_FIXED

必须形成 three-way comparison。

输出：

three_way_reference_metrics.csv

字段：

state
camera_id

E1_dynamic_MAE
E1_fixed_MAE
dynamic_fixed_MAE

E1_dynamic_NORE
E1_fixed_NORE

重点判断：

CASE 1:

E1_dynamic ≈ 0
E1_fixed ≈ dynamic_fixed

说明：

E1 真实匹配 GT_DYNAMIC。

CASE 2:

E1_fixed ≈ 0
E1_dynamic ≈ dynamic_fixed

说明：

Stage 3.2 metric target/path 错误。

CASE 3:

E1_dynamic ≈ 0
E1_fixed ≈ 0
但 dynamic_fixed > 0

数学上矛盾。

说明：

metric / mask / array bug。

CASE 4:

独立结果与 Stage 3.2 不一致

说明旧 metrics pipeline bug。

必须明确分类。

==================================================
七、严格 Tau / Color Parameter Integrity
==================================================

加载：

canonical checkpoint

以及每个 E1 state 实际 render input。

逐 state 比较：

canonical tau
vs
E1 tau

canonical _tau_raw
vs
E1 _tau_raw

canonical color
vs
E1 color

canonical _color_raw
vs
E1 _color_raw

记录：

max_abs_difference
mean_abs_difference
SHA256 tensor checksum

要求 E1：

tau difference = 0

tau_raw difference = 0

color difference = 0

color_raw difference = 0

检查：

E1 model

是否与：

E2 tau-refit model

共享 tensor storage。

使用：

data_ptr

storage pointer

Python object id

检查。

重点审计：

是否：

E1 render 后
E2 in-place optimizer
修改了同一个 model object

然后重新保存 / 重新评价 E1。

如果存在：

aliasing
shallow copy
shared Parameter

必须明确。

使用：

copy.deepcopy

重新从 frozen canonical checkpoint
建立 fresh E1。

重新 render。

==================================================
八、重新生成 Fresh E1
==================================================

不读取旧 E1 rendered images。

对于每 state：

重新加载 frozen canonical checkpoint。

创建全新 model。

验证：

tau checksum
color checksum。

然后只执行：

geometry deformation transport。

立即 render。

在 E2 优化前保存。

目录：

fresh_e1/

再次独立计算：

Fresh E1 vs GT_DYNAMIC

Fresh E1 vs GT_FIXED。

如果 old E1 与 fresh E1 不一致：

定位 state mutation bug。

==================================================
九、核心概念审计：Pixel Alpha
==================================================

当前 carrier：

per-Gaussian:

tau_i fixed

alpha_opt_i =
1 - exp(-tau_i)

但是 rasterizer 的实际 per-pixel contribution
还依赖 projected Gaussian kernel。

因此：

fixed tau_i

不等于：

fixed accumulated pixel alpha A(p)。

使用 Stage 3.2.1 validated white pass：

black background
+
white override color

得到：

A_rgb

验证 RGB channel identity。

定义：

A(p) =
mean(A_rgb)

定义 effective pixel transmittance：

T_eff(p) =
clamp(1 - A(p), 1e-6, 1)

定义 diagnostic effective optical depth：

tau_eff(p) =
-log(T_eff(p))

注意：

tau_eff 只是 renderer-level diagnostic。

不是 per-Gaussian tau。

禁止混淆。

==================================================
十、直接分析 Fixed-Tau Alpha Field
==================================================

对：

canonical

stretch_1.10
stretch_1.25
stretch_1.50
stretch_2.00

biaxial_1.50

twist_60

全部使用：

Fresh E1 fixed tau。

渲染：

A(p)

tau_eff(p)

保存：

alpha_maps/
effective_tau_maps/

对每 camera：

在 film interior region：

统计：

A mean
A median
A p10
A p90

tau_eff mean
tau_eff median
tau_eff p10
tau_eff p90

输出：

alpha_field_metrics.csv

回答：

per-Gaussian tau 固定时，

pixel accumulated alpha
是否随 Js 系统变化？

==================================================
十一、Material-Point Correspondence Analysis
==================================================

不能只在 image coordinates
直接比较 canonical / deformed alpha。

因为 sheet geometry 发生变化。

当前 Gaussian grid：

41 x 41

Gaussian identity 与 material point 一一对应。

利用：

Gaussian index

作为 material correspondence。

对于每 Gaussian i：

canonical xyz_i

deformed xyz_i

分别投影到同一 camera。

必须使用 validated official Camera。

得到：

p_i_canonical
p_i_deformed

从对应 alpha map
使用 bilinear sampling：

A_canonical(p_i_canonical)

A_deformed(p_i_deformed)

只保留：

projection inside image

positive depth

距离 sheet projected edge
至少 3 pixels

的 material points。

由于 single sheet 无复杂 self-occlusion，
记录该假设。

计算 per material point：

alpha_ratio =
A_deformed /
(A_canonical + 1e-8)

effective_tau_ratio =
tau_eff_deformed /
(tau_eff_canonical + 1e-8)

输出：

material_point_alpha_metrics.csv

对每 state：

mean
median
p10
p90

==================================================
十二、与 Js / h_ratio 比较
==================================================

对于 uniform stretch：

h_ratio =
1 / Js

比较：

material-point effective_tau_ratio

与：

1.0

以及：

h_ratio

计算：

absolute distance to 1

absolute distance to h_ratio

例如：

D_fixed =
abs(median_tau_eff_ratio - 1)

D_thickness =
abs(median_tau_eff_ratio - h_ratio)

回答：

Fixed per-Gaussian tau
经过 geometry transport 后，

renderer-level effective optical depth
更接近：

unchanged

还是：

1/Js scaling。

不要预设答案。

==================================================
十三、Geometry Component Ablation
==================================================

这是本阶段最关键机制实验。

使用完全相同：

canonical tau
canonical color

建立四种 geometry variant。

G0:
CANONICAL

positions:
canonical

covariance:
canonical

G1:
POSITION_ONLY

positions:
phi(xyz)

covariance:
canonical Sigma

G2:
COVARIANCE_ONLY

positions:
canonical xyz

covariance:
F Sigma F^T

G3:
FULL_GEOMETRY_TRANSPORT

positions:
phi(xyz)

covariance:
F Sigma F^T

即当前 E1。

对：

stretch_1.25
stretch_1.50
stretch_2.00

biaxial_1.50

twist_60

渲染 white-pass alpha。

计算 material-point：

effective_tau_ratio

并与：

1
1/Js

比较。

输出：

geometry_component_ablation.csv

核心回答：

A. position spreading 是否降低 accumulated alpha？

B. covariance expansion 是否增加 accumulated alpha？

C. 两者结合是否抵消？

D. Full geometry transport 是否产生
接近 1/Js 的 effective optical depth change？

==================================================
十四、Oracle Tau Diagnostic
==================================================

仅作为 diagnostic。

禁止称为 proposed method。

定义：

ORACLE_TAU_GT_RULE

对于每 Gaussian：

tau_oracle =
tau_canonical * h_ratio

其中：

h_ratio =
1 / Js

这是冻结 GT benchmark rule
映射到 carrier tau 的诊断。

使用：

与 Fresh E1 完全相同 geometry。

只将：

tau

替换为 oracle tau。

color fixed。

渲染：

ORACLE_TAU。

比较：

Fresh E1 FIXED TAU

ORACLE TAU

GT_DYNAMIC

GT_FIXED

计算：

Masked MAE
NORE
PSNR

输出：

oracle_tau_diagnostic.csv

重点判断：

A.

FIXED TAU ≈ GT_DYNAMIC

ORACLE TAU worse

说明：

geometry transport 本身
已经隐式产生 optical-state evolution，
直接再乘 1/Js 会 double count。

B.

ORACLE TAU ≈ GT_DYNAMIC

FIXED TAU worse

说明：

旧 Stage 3.2 metric / E1 pipeline 有错误。

C.

两者都差

说明 carrier optical semantics
与 benchmark rule 不匹配。

D.

两者都好

检查 signal / mask / metric bug。

==================================================
十五、Renderer Formula Audit
==================================================

基于：

TSGS rasterizer wrapper

以及实际 CUDA forward semantics，

只读检查：

opacity

projected conic / Gaussian kernel

alpha

transmittance accumulation

找到真实公式或代码逻辑。

报告：

per-Gaussian scalar opacity
如何与 projected Gaussian kernel value
共同形成 pixel alpha。

明确回答：

covariance change
是否会改变：

Gaussian footprint

kernel value distribution

pixel alpha

accumulated transmittance。

不要修改 CUDA。

输出到：

implicit_transport_report.md

==================================================
十六、当前研究问题重新判定
==================================================

根据真实结果选择：

CASE A:

Stage 3.2 evaluation / path / mutation bug

Decision:

修复 bug。

重新运行 Necessity Test。

研究问题保持：

fixed optical state necessity。

CASE B:

Fresh fixed-tau E1
确实匹配 GT_DYNAMIC

且：

material-space effective tau
随 Js 接近 1/Js 变化

Decision:

standard covariance + position transport
already induces implicit optical-state evolution。

原假设：

“fixed per-Gaussian tau means fixed optical state”

错误。

新的科学问题：

Does Gaussian geometry transport
implicitly produce physically consistent
optical-state evolution under deformation?

下一步：

测试该 implicit transport
在哪些 deformation / sampling / Gaussian scale
条件下成立或失效。

当前不要设计 explicit tau rule。

CASE C:

Fresh E1 matches GT_DYNAMIC

但 effective alpha change
不与 1/Js 对齐。

Decision:

carrier matches GT through another compensation mechanism。

必须定位：

color
coverage
mask
projection

不能设计 tau rule。

CASE D:

Oracle tau matches GT_DYNAMIC
and fresh fixed tau does not。

Decision:

necessity hypothesis重新获得支持。

重新执行 Stage 3.2 Gate。

==================================================
十七、报告
==================================================

stage3_2_2_summary.md
必须严格分：

# 1. Paradox Definition

# 2. File and Path Integrity

# 3. Independent Metric Recheck

# 4. Parameter Integrity

# 5. Fresh E1 Re-render

# 6. Pixel Alpha / Effective Optical Depth

# 7. Material-Point Correspondence Results

# 8. Position vs Covariance Ablation

# 9. Oracle Tau Diagnostic

# 10. Renderer Formula Audit

# 11. Root Cause

# 12. Research Decision

必须明确回答：

1. Stage 3.1.1 GT Gate 是否独立复现。

2. Stage 3.2 E1 near-zero error 是否独立复现。

3. E1 是否真实使用 fixed canonical tau。

4. 是否存在 tensor alias / mutation。

5. Fresh E1 是否仍匹配 GT_DYNAMIC。

6. Fresh E1 vs GT_FIXED MAE。

7. Fixed tau 下 accumulated alpha 是否变化。

8. effective tau ratio 是否接近 1/Js。

9. position-only effect。

10. covariance-only effect。

11. full geometry transport effect。

12. oracle tau 是否改善。

13. Stage 3.2 原结论是否正确。

14. 最终 CASE A/B/C/D。

==================================================
十八、README
==================================================

更新 README。

不要写：

Mitsuba path tracing signal
cannot be captured by alpha blending

除非本阶段获得直接证据。

改为：

Stage 3.2 revealed a paradox:

fixed per-Gaussian tau
nearly matches deformation-dependent GT
after covariance transport.

Stage 3.2.2 audits whether
Gaussian geometry transport
implicitly changes accumulated optical attenuation.

==================================================
十九、实际执行
==================================================

必须实际：

path audit

SHA256

independent GT metric recheck

independent E1 metric recheck

parameter checksum

tensor alias audit

fresh E1 rerender

white-pass alpha analysis

effective tau analysis

material-point projection sampling

position-only ablation

covariance-only ablation

full geometry ablation

oracle tau diagnostic

renderer formula audit

最终 CASE 判断

不要训练新模型。

不要 E2 refit。

不要实现新方法。

不要改 benchmark。

不要改 TSGS。

==================================================
二十、最终终端总结
==================================================

最后简洁输出：

1. GT Gate independently reproduced yes/no
2. Old E1 near-zero error reproduced yes/no
3. Path/file bug yes/no
4. Tau fixed checksum identical yes/no
5. Color fixed checksum identical yes/no
6. Tensor alias/mutation found yes/no
7. Fresh E1 vs GT_DYNAMIC stretch_2.00 MAE
8. Fresh E1 vs GT_FIXED stretch_2.00 MAE
9. Fixed-tau accumulated alpha changes with Js yes/no
10. stretch_2.00 material effective tau ratio
11. expected h_ratio
12. position-only tau ratio
13. covariance-only tau ratio
14. full transport tau ratio
15. Oracle tau vs GT_DYNAMIC MAE
16. Fixed tau vs GT_DYNAMIC MAE
17. Oracle improves yes/no
18. Original Stage 3.2 conclusion correct yes/no
19. Final CASE
20. Current scientific question
21. implicit_transport_report.md path
22. stage3_2_2_summary.md path

请直接实际检查、运行和验证。

不要换服务器。
不要研究 RT-Splatting runtime。
不要重新设计 benchmark。
不要训练新模型。
不要实现 tau transport method。
PROMPT