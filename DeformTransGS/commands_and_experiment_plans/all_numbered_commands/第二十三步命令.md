cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
研究主线正式收缩
==================================================

Stage 3.2.6A 已完成协议矛盾审计。

Stage 3.2.5：

R_tau_eff=1.461

结论撤回。

原因：

旧 metric region / material correspondence /
aggregation protocol 不稳定。

正式统一使用：

Gold Material-Point Optical Response Protocol。

Gold Protocol 定义：

A =
validated white-pass accumulated alpha

T =
clamp(1-A, 1e-6, 1)

tau_eff =
-log(T)

对于 Gaussian material point i：

canonical xyz_i 投影到 canonical image。

deformed xyz_i 投影到 deformed image。

分别 bilinear sample：

tau_eff_can(i)

tau_eff_def(i)

定义：

r_i =
tau_eff_def(i)
/
(tau_eff_can(i) + eps)

per-camera response：

median_i(r_i)

overall response：

median across cameras。

正式 3 cameras：

cam_000
cam_004
cam_008

==================================================
已有 Gold 结果
==================================================

使用固定身份 41x41 Gaussian thin-film carrier。

Gold results：

stretch_1.25:

R_tau_eff=0.778
physical target 1/Js=0.800
error=0.022

stretch_1.50:

R_tau_eff=0.638
target=0.667
error=0.029

stretch_2.00:

R_tau_eff=0.467
target=0.500
error=0.033

biaxial_1.50:

R_tau_eff=0.423
target=0.444
error=0.021

twist_60:

R_tau_eff=0.905
target≈1.0
deviation=0.095

area-changing states
mean absolute ratio error≈0.026。

当前正式结论：

For the controlled regular fixed-identity
thin-film Gaussian representation,

geometry transport under uniform affine
area-changing deformation
implicitly induces effective optical-depth evolution
approximately consistent with 1/Js area dilution。

中文：

在当前规则、固定身份薄膜 Gaussian 表示中，
均匀仿射面积形变后的有效光学深度变化，
近似遵循 1/Js 面积稀释规律。

==================================================
重要结论边界
==================================================

禁止写：

Gaussian deformation is physically correct。

禁止写：

all transparent Gaussians naturally satisfy 1/Js。

当前只证明：

regular Gaussian sheet

fixed identity

uniform affine area deformation

controlled thin-film alpha carrier

Gold material-point protocol。

==================================================
Stage 3.2.6A CASE 修正
==================================================

此前 report 标记：

CASE A REPRESENTATION INPUT MISMATCH。

但审计结果：

xyz same
scale same
rotation same
tau same
color same
camera same
deformation phi same

因此不应标记 representation input mismatch。

正式修正为：

METRIC PROTOCOL MISMATCH / LEGACY METRIC ARTIFACT。

Stage 3.2.5 conclusion INVALID。

Gold Protocol 作为后续唯一 metric protocol。

更新相关报告和 README。

==================================================
本阶段名称
==================================================

Stage 3.3:

Local Optical Consistency Boundary Test

中文：

局部光学一致性边界实验

==================================================
一、本阶段唯一科学问题
==================================================

Uniform deformation 下：

R_tau_eff ≈ 1/Js

已经获得支持。

当前只回答：

当 deformation 是 spatially non-uniform，

即：

Js = Js(x)

在 surface 上空间变化时，

fixed-identity Gaussian geometry transport
是否仍满足局部：

tau_eff_def(i)
/
tau_eff_can(i)

≈

1 / Js_i

？

换句话说：

均匀面积拉伸下观察到的
implicit optical consistency

是否是真正的 local property，

还是：

regular grid + global affine deformation
产生的特殊现象？

当前禁止设计 correction method。

禁止 tau update。

禁止 MLP。

禁止重新研究 representation divergence。

==================================================
二、项目边界
==================================================

所有新增代码和结果：

/data/wyh/DeformTransGS

创建：

/data/wyh/DeformTransGS/experiments/
stage3_3_local_optical_consistency/

禁止修改：

/data/wyh/repos/TSGS

/data/wyh/RecycleGS

/data/wyh/repos/RT-Splatting

==================================================
三、唯一 Frozen Carrier
==================================================

使用 Stage 3.2.6A Gold Protocol
实际验证的完整 Gaussian representation。

必须记录：

checkpoint / tensor source path

Gaussian count

xyz SHA256

scale SHA256

rotation SHA256

tau SHA256

color SHA256

禁止重新训练。

禁止重建不同 scale policy。

禁止重新优化 tau。

生成：

frozen_carrier_manifest.json

==================================================
四、Gold Protocol 唯一化
==================================================

将：

Stage 3.2.6A

analysis/gold_optical_response_protocol.py

作为正式 metric definition。

如果当前脚本位于其它目录：

复制到：

analysis/gold_local_optical_protocol.py

只允许增加：

per-material-point Js input

local statistics

spatial bin analysis。

禁止改变：

camera set

material point projection

boundary margin

bilinear sampling

tau_eff formula

camera aggregation。

首先重新运行：

stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.50
twist_60

必须复现 Gold results。

允许误差：

absolute R difference <=0.01。

输出：

gold_protocol_reproduction.csv

如果无法复现：

立即停止。

==================================================
五、新增 Non-Uniform Stretch
==================================================

使用 canonical sheet local coordinate。

首先根据真实 canonical geometry
计算：

sheet center c

principal tangent directions
tangent_u
tangent_v

以及沿 tangent_u 的 half extent：

L

对每 material point：

x_local =
dot(x-c, tangent_u)

y_local =
dot(x-c, tangent_v)

u =
x_local / L

定义：

NONUNIFORM_CUBIC_STRETCH

x_local' =

x_local
+
lambda * x_local^3 / L^2

y_local' =
y_local

normal coordinate unchanged。

转换回 world coordinates。

lambda：

0.10
0.20
1/3

state names：

cubic_stretch_l010
cubic_stretch_l020
cubic_stretch_l0333

解析 strength 必须使用显式 mapping。

禁止从 underscore split 猜 float。

==================================================
六、Analytic F and Js
==================================================

对于：

x' =
x + lambda*x^3/L^2

局部：

dx'/dx =
1 + 3*lambda*x^2/L^2

定义：

s_local =
1 + 3*lambda*u^2

local deformation gradient
in canonical tangent frame：

F_local =

diag(
s_local,
1,
1
)

转换到 world frame：

Q =
[tangent_u, tangent_v, normal]

F_world =
Q F_local Q^T

计算：

Js_i =
abs(det(F_world))
*
norm(F_world^{-T} n_i)

对于 planar canonical sheet：

analytic expected：

Js_i =
1 + 3*lambda*u_i^2

必须验证：

computed Js
vs
analytic Js

mean absolute error <1e-5

max error <1e-4。

边缘理论：

lambda=0.10:
max Js≈1.30

lambda=0.20:
max Js≈1.60

lambda=1/3:
max Js≈2.00

输出：

nonuniform_js_validation.csv

如果失败：

禁止 render。

==================================================
七、新增 Non-Uniform Area-Preserving Control
==================================================

定义：

NONUNIFORM_SHEAR_CONTROL

canonical local coordinates：

x_local' =
x_local
+
k * y_local^2 / L

y_local' =
y_local

normal coordinate unchanged。

选择：

k =
0.20
0.40

state names：

shear_control_k020
shear_control_k040

local F：

[[1, 2*k*y/L, 0],
 [0, 1,         0],
 [0, 0,         1]]

转换 world frame。

对于 planar sheet：

Js_i = 1

所有 material points。

验证：

mean |Js-1| <1e-5

max |Js-1| <1e-4。

目的：

产生明显 spatially varying position deformation，

但：

local surface area preserved。

==================================================
八、Geometry Transport
==================================================

对于所有新 states：

Gaussian identity 固定。

xyz：

使用真实 nonlinear phi(x)。

每 Gaussian：

使用 analytic local F_i。

canonical covariance：

Sigma_i。

传输：

Sigma_i' =
F_i Sigma_i F_i^T

恢复：

scale_i'
rotation_i'

使用已有 validated covariance decomposition。

tau：

canonical fixed。

color：

canonical fixed。

禁止：

densification
pruning
split
clone
tau update
color update。

==================================================
九、Geometry Validation
==================================================

输出：

local_geometry_validation.csv

每 state：

Gaussian count

Js mean
Js std
Js min
Js max

xyz finite

scale finite

rotation finite

quaternion norm max error

covariance reconstruction mean error
p95
max

同时：

Gaussian index ordering checksum。

==================================================
十、Local Gold Optical Response
==================================================

对每：

new state
camera

使用 Gold material point correspondence。

对于 material point i：

r_i,c =

tau_eff_def,c(i)
/
(
tau_eff_can,c(i)+1e-8
)

physical target：

q_i =
1 / Js_i

只保留：

Gold valid points。

对于每 state-camera：

记录：

valid count

median r

median q

MAE_local =
mean |r_i - q_i|

RMSE_local

Pearson(r,q)

Spearman(r,q)

输出：

local_pointwise_consistency.csv

==================================================
十一、跨 Camera Material-Point Aggregation
==================================================

对于每 Gaussian material identity i：

收集所有 valid cameras：

r_i,c

如果至少：

2 cameras valid

定义：

r_i =
median_c(r_i,c)

对应：

q_i =
1/Js_i

输出：

material_local_response.csv

字段：

state
gaussian_index
u
v
Js
h_ratio
valid_camera_count
R_tau_eff_local
absolute_error

这是本阶段主数据。

==================================================
十二、Spatial Bin Analysis
==================================================

对于 cubic stretch：

按：

|u|

划分 bins：

[0.0,0.2)
[0.2,0.4)
[0.4,0.6)
[0.6,0.8)
[0.8,1.0]

每 bin：

mean Js
median 1/Js

median R_tau_eff_local

MAE

p10
p90

输出：

spatial_bin_consistency.csv

理论：

中心：

Js≈1
R≈1

边缘：

Js larger
R smaller。

检查是否存在：

local response lag

overshoot

cross-region mixing。

==================================================
十三、Neighbourhood Mixing Diagnostic
==================================================

Gaussian splatting 的 pixel alpha
由邻近多个 Gaussian 累积。

因此 local optical response
可能不是严格 point-local。

对每 material point：

计算 canonical material-grid neighbourhood：

1-ring
2-ring

定义：

q_self =
1/Js_i

q_ring1 =
weighted mean_j(1/Js_j)

q_ring2 =
weighted mean_j(1/Js_j)

权重首先使用：

uniform

再使用：

canonical Gaussian-space distance Gaussian weight。

不要使用 learned model。

分别比较：

R_tau_eff_local

与：

q_self
q_ring1
q_ring2

计算：

MAE
Pearson
Spearman

输出：

neighbourhood_mixing_analysis.csv

目的：

判断 renderer optical response
更接近：

严格 local 1/Js

还是：

neighbourhood-averaged area dilution。

禁止将 neighbourhood averaging
称为 method。

仅 diagnostic。

==================================================
十四、Uniform vs Non-Uniform Comparison
==================================================

统一 Gold Protocol。

构建：

uniform benchmark states：

stretch_1.25
stretch_1.50
stretch_2.00

non-uniform：

cubic l010
l020
l0333

比较：

uniform local MAE

non-uniform local MAE

注意：

uniform state 中：

q constant。

仍使用 material point r_i。

输出：

uniform_nonuniform_comparison.csv

核心回答：

非均匀 deformation
是否显著降低 local physical consistency。

==================================================
十五、Area-Preserving Control
==================================================

对于：

shear_control_k020
shear_control_k040

target：

q_i=1

计算：

median R_tau_eff_local

mean absolute deviation from 1

p95 absolute deviation

spatial correlation：

u vs R
v vs R

要求报告：

虽然 geometry spatially non-uniform，

如果 Js=1，

optical response 是否保持 near 1。

同时继续保留：

twist_60

作为已有 control。

==================================================
十六、Local Consistency Gate
==================================================

首先定义 uniform baseline：

E_uniform =

mean local MAE
across：

stretch_1.25
stretch_1.50
stretch_2.00

--------------------------------------------------
CASE A:
LOCAL CONSISTENCY SUPPORTED
--------------------------------------------------

满足：

1.

所有 cubic states：

Spearman(
R_tau_eff_local,
1/Js
)
>=0.9

2.

strongest：

cubic_l0333

local MAE <=0.10

3.

E_nonuniform
<=
2 * E_uniform

4.

area-preserving shear：

mean |R-1| <=0.10

结论：

uniform affine result
extends to spatially varying local deformation。

当前 explicit optical correction
没有必要。

下一步：

测试真实 irregular Gaussian sampling /
adaptive densification。

--------------------------------------------------
CASE B:
LOCAL CONSISTENCY BREAKS
--------------------------------------------------

满足任一：

cubic strongest local MAE >0.15

或：

Spearman <0.7

同时 uniform baseline remains good。

结论：

implicit optical consistency
is a global-affine / locally-uniform special case。

新的问题：

Gaussian splatting mixes neighbouring material points,
causing optical transport to deviate from local area dilution
under non-uniform deformation。

下一步允许设计：

local optical consistency correction。

但仍禁止直接 tau/Js。

--------------------------------------------------
CASE C:
NEIGHBOURHOOD CONSISTENCY
--------------------------------------------------

self 1/Js local error较大，

但：

ring1 或 ring2 averaged 1/Js

MAE 比 self target
至少降低 30%

且 Spearman >=0.9。

结论：

implicit optical response
follows a spatially filtered deformation signal，
rather than point-local Js。

下一步研究：

Gaussian-footprint-aware deformation measure。

这可能是新的核心创新点。

--------------------------------------------------
CASE D:
CONTROL FAILURE
--------------------------------------------------

area-preserving shear：

mean |R-1| >0.15

说明：

position/projection deformation
本身产生强 optical drift，
与 Js 无关。

停止 area-dilution consistency claim。

重新研究 renderer geometry-optics coupling。

==================================================
十七、正式科学问题边界
==================================================

CASE A：

论文问题尚不足。

因为 geometry transport
已经隐式保持 local optical consistency。

继续找失效条件。

CASE B：

允许表述：

Uniform area dilution consistency
does not extend to spatially varying deformation。

中文：

均匀面积稀释下的隐式光学一致性
无法自然推广到空间变化形变。

CASE C：

允许表述：

The implicit optical response is governed by
footprint-scale neighbourhood deformation
rather than point-local area stretch。

中文：

Gaussian 的隐式光学响应
更受 footprint 尺度邻域形变影响，
而非单点局部面积拉伸。

这是非常值得继续的方法方向。

CASE D：

当前 area-dilution hypothesis
不成立。

==================================================
十八、可视化
==================================================

使用 matplotlib。

禁止 seaborn。

不要指定颜色。

每张独立。

1.

local_response_vs_target.png

x:
1/Js_i

y:
R_tau_eff_local

分别：

cubic l010
l020
l0333

2.

local_error_vs_u.png

x:
|u|

y:
absolute local error

3.

spatial_bin_response.png

x:
bin mean Js

y:
median R

同时显示 target 1/Js。

4.

neighbourhood_target_comparison.png

self
ring1
ring2

MAE comparison。

5.

local_response_maps.png

对于 cubic_l0333：

Js map
1/Js map
R_tau_eff map
absolute error map

使用 material-grid 41x41 reshape。

6.

control_response_maps.png

shear k040：

Js map
R_tau_eff map
deviation from 1。

==================================================
十九、报告
==================================================

输出：

gold_protocol_reproduction.csv
nonuniform_deformation_report.md
nonuniform_js_validation.csv
local_geometry_validation.csv

local_pointwise_consistency.csv
material_local_response.csv
spatial_bin_consistency.csv
neighbourhood_mixing_analysis.csv

uniform_nonuniform_comparison.csv
area_preserving_control.csv

local_optical_consistency_report.md
stage3_3_summary.md
stage3_3_log.txt

==================================================
二十、local_optical_consistency_report.md
==================================================

必须中文回答：

A. Stage 3.2.6A CASE 修正为什么是 metric protocol mismatch。

B. Gold Protocol 是否复现。

C. Uniform baseline local MAE。

D. cubic deformation 定义。

E. cubic Js analytic validation error。

F. cubic l010 local MAE / Spearman。

G. cubic l020 local MAE / Spearman。

H. cubic l0333 local MAE / Spearman。

I. center region response。

J. edge high-stretch response。

K. self 1/Js target MAE。

L. ring1 target MAE。

M. ring2 target MAE。

N. 最佳 deformation target 是 self / ring1 / ring2。

O. shear k020 mean |R-1|。

P. shear k040 mean |R-1|。

Q. twist control。

R. uniform vs non-uniform consistency drop。

S. Final CASE A/B/C/D。

T. 当前真正科学问题。

U. 是否允许设计 correction。

只有：

CASE B
或
CASE C

回答：

是。

==================================================
二十一、README
==================================================

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage 3.2.6A unified optical response evaluation
with a material-point Gold Protocol。

Under uniform affine area-changing deformation,
the controlled fixed-identity Gaussian sheet
shows effective optical-depth response close to 1/Js。

Stage 3.3 tests whether this consistency
is point-local and extends to spatially varying deformation。

==================================================
二十二、实际执行
==================================================

必须实际：

修正 Stage 3.2.6A CASE 文本

freeze Gold carrier

Gold protocol reproduction

implement cubic nonuniform stretch

analytic F / Js validation

implement area-preserving nonlinear shear

geometry transport

local material-point optical response

spatial bins

neighbourhood mixing diagnostic

uniform local baseline

area-preserving controls

Local Consistency Gate

所有可视化和报告

不要训练。

不要重新拟合 tau。

不要设计 correction。

不要改 TSGS。

==================================================
二十三、最终终端总结
==================================================

最后简洁输出：

1. Gold protocol reproduced yes/no
2. Uniform baseline local MAE
3. cubic l010 Js validation max error
4. cubic l010 local MAE / Spearman
5. cubic l020 local MAE / Spearman
6. cubic l0333 local MAE / Spearman
7. center-bin R / target
8. edge-bin R / target
9. self target MAE
10. ring1 target MAE
11. ring2 target MAE
12. best target self/ring1/ring2
13. shear k020 mean deviation
14. shear k040 mean deviation
15. twist deviation
16. uniform vs nonuniform error ratio
17. Final CASE
18. Current scientific question
19. Can design correction yes/no
20. local_response_maps.png path
21. local_optical_consistency_report.md path
22. stage3_3_summary.md path

请直接实际运行和验证。

不要训练。
不要实现 optical correction。
不要修改 TSGS。
PROMPT