cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前状态
==================================================

Stage 3.3.2 当前标记：

MECHANISM AUDIT INCOMPLETE

禁止继续写：

CASE B2 confirmed

禁止写：

footprint heterogeneity does not explain error

禁止设计 method。

已有正式事实：

E_uniform = 0.036

E_nonuniform = 0.112

E_nonuniform / E_uniform = 3.1

cubic_l0333 self MAE = 0.156

simple neighbourhood averaging improvement <3%

area-preserving controls：

shear_k020 MAE=0.002
shear_k040 MAE=0.008

matched-Js coarse-bin analysis：

q 0.75-0.85:
cubic error / uniform error = 9.3x

q 0.60-0.70:
8.8x

q 0.45-0.55:
7.2x

当前 strongest observation：

At similar local Js magnitude,
spatially varying cubic deformation
has much larger local optical-consistency error
than uniform deformation。

==================================================
重要统计错误
==================================================

Stage 3.3.2 声称：

Spearman returned NaN because
cubic q_self values are symmetric duplicates。

该解释错误。

scipy.stats.spearmanr
允许 tied ranks。

NaN 必须审计：

constant input

NaN / Inf propagation

wrong array shape / axis

incorrect filtering

wrong column

当前必须修复。

==================================================
本阶段名称
==================================================

Stage 3.3.3:

Screen-Space Footprint Heterogeneity and Matched-Js Mechanism Audit

中文：

屏幕空间 Gaussian 覆盖尺度形变异质性
与同 Js 匹配机制审计

==================================================
一、唯一目标
==================================================

回答：

在控制 point-local Js magnitude 后，

为什么 cubic deformation
仍具有远高于 uniform deformation
的 optical consistency error？

重点验证：

screen-space Gaussian footprint
所覆盖的 deformation field variation

是否解释额外误差。

禁止：

训练

新 deformation

tau update

correction

MLP

修改 renderer

==================================================
二、输入
==================================================

严格使用 Stage 3.3.1 / 3.3.2：

material_point_response.csv

frozen carrier

canonical / deformed xyz

scale

rotation

tau

per-Gaussian Js

official Camera definitions

states：

canonical

stretch_1.25
stretch_1.50
stretch_2.00

cubic_l010
cubic_l020
cubic_l0333

shear_k020
shear_k040

twist_60

禁止重新训练。

==================================================
三、Spearman Bug Audit
==================================================

对每 cubic state：

读取：

R_local
q_self

明确输出：

raw row count

finite row count

NaN count R

NaN count q

Inf count R

Inf count q

R dtype

q dtype

R shape

q shape

R std

q std

unique R count

unique q count

使用：

r = np.asarray(R_local, dtype=np.float64).reshape(-1)

q = np.asarray(q_self, dtype=np.float64).reshape(-1)

valid =
np.isfinite(r) & np.isfinite(q)

使用：

scipy.stats.spearmanr(
    r[valid],
    q[valid],
    nan_policy="raise"
)

同时：

Pearson。

禁止按 |u| 分组后替代正式 Spearman。

如果仍然 NaN：

必须明确：

which input is constant

或具体 exception。

输出：

spearman_bug_audit.csv
corrected_cubic_correlation.csv

==================================================
四、正式复现 TSGS Screen-Space Covariance
==================================================

只读检查实际 TSGS rasterizer
所使用 CUDA forward。

在 DeformTransGS analysis 中复现：

3D covariance

camera view transform

projection Jacobian J

projected 2D covariance

including actual low-pass term：

cov_2d[0,0] += 0.3
cov_2d[1,1] += 0.3

inverse conic。

必须使用当前实际 TSGS rasterizer
代码语义。

禁止使用：

material-space Euclidean Gaussian weight

作为正式 screen target。

==================================================
五、Projection Diagnostic Validation
==================================================

对于：

cam_000
cam_004
cam_008

states：

canonical
cubic_l0333

从 official renderer 获取：

radii。

使用 diagnostic projected covariance
计算：

3-sigma major radius。

比较：

diagnostic radius
vs renderer radii。

记录：

Pearson
Spearman

median absolute error

p95 error。

允许 integer ceil 差异。

要求：

radius Spearman >=0.99

否则：

停止 screen-space mechanism analysis。

输出：

projected_covariance_validation.csv

==================================================
六、真实 Screen-Kernel Deformation Field
==================================================

对于每：

state
camera
valid material point i

使用其 deformed projected coordinate：

p_i

对于每 Gaussian j：

使用 actual deformed：

projected mean_j
projected conic_j

计算：

d =
mean_j - p_i

power_j =
-0.5 *
(
conic_x * dx^2
+
2 * conic_xy * dx*dy
+
conic_y * dy^2
)

要求：

power_j <= 0

定义：

w_kernel_j =
exp(power_j)

保留：

w_kernel_j >= exp(-9/2)

约 3-sigma support。

不乘 opacity。

定义：

q_j = 1/Js_j

Q_KERNEL_i,c =
sum(w_kernel_j*q_j) /
sum(w_kernel_j)

H_KERNEL_STD_i,c =
sqrt(
sum(w_kernel_j*(q_j-Q_KERNEL)^2)
/
sum(w_kernel_j)
)

H_KERNEL_REL_i,c =
H_KERNEL_STD /
(abs(Q_KERNEL)+1e-8)

H_KERNEL_MAD_i,c =
sum(
w_kernel_j*abs(q_j-Q_KERNEL)
)
/
sum(w_kernel_j)

H_KERNEL_RANGE_i,c =
weighted p90(q_j)
-
weighted p10(q_j)

同时记录：

kernel support Gaussian count。

输出：

screen_kernel_heterogeneity_per_camera.csv

==================================================
七、Cross-Camera Aggregation
==================================================

必须与 Gold Point Protocol 对齐。

对于 material point i：

使用与 R_local 完全相同 valid cameras。

定义：

Q_KERNEL_i =
median_c(Q_KERNEL_i,c)

H_REL_i =
median_c(H_KERNEL_REL_i,c)

H_MAD_i =
median_c(H_KERNEL_MAD_i,c)

H_RANGE_i =
median_c(H_KERNEL_RANGE_i,c)

support_count_i =
median camera support count。

输出：

screen_kernel_heterogeneity_material.csv

==================================================
八、Optical-Budget Weighted Diagnostic
==================================================

只作为 diagnostic。

对于相同 p_i：

opacity_j =
1-exp(-tau_j)

alpha_j =
min(
0.99,
opacity_j*exp(power_j)
)

过滤：

alpha_j >= 1/255

定义：

w_budget_j =
-log(1-alpha_j)

Q_BUDGET =
sum(w_budget*q_j)/sum(w_budget)

H_BUDGET_REL =
weighted std(q_j)/Q_BUDGET

注意：

不要 early-termination reorder
用于正式 target。

这是 diagnostic。

输出：

optical_budget_heterogeneity.csv

==================================================
九、Same-Point Uniform Response Baseline
==================================================

不要继续使用 coarse q bins
作为主 matched-Js evidence。

使用：

canonical q=1

stretch_1.25 q=0.8

stretch_1.50 q=0.6666667

stretch_2.00 q=0.5

对于每：

material point i
camera c

已有 uniform response：

R_uniform(i,c,q_k)

在 q 轴建立：

piecewise linear interpolation。

禁止跨 material point。

禁止跨 camera。

因此得到函数：

R_uniform_expected(
    i,
    c,
    q
)

对于 cubic point：

q_cubic_i

在相同：

material identity i
camera c

插值得到：

R_expected_uniform_i,c

定义：

uniform-conditioned expected error：

E_expected =
abs(
R_expected_uniform_i,c
-
q_cubic_i
)

cubic actual error：

E_cubic =
abs(
R_cubic_i,c
-
q_cubic_i
)

extra_error：

Delta_E =
E_cubic - E_expected

这是正式 matched-Js control。

输出：

same_point_same_camera_matched_js.csv

==================================================
十、Matched-Js Gate
==================================================

对：

cubic_l010
cubic_l020
cubic_l0333

统计：

Delta_E mean
median
p90

fraction Delta_E > 0

paired test：

Wilcoxon signed-rank
或 permutation test。

优先：

paired permutation test。

对 cubic_l0333：

MATCHED-JS EXTRA ERROR SUPPORTED：

median Delta_E >=0.05

fraction Delta_E >0 >=0.75

paired p <0.01

禁止修改 threshold。

==================================================
十一、Heterogeneity vs Extra Error
==================================================

主响应变量不再使用：

E=abs(R-q)

而使用：

Delta_E

即：

超过“相同 material point /
相同 camera /
相同 q 下 uniform response”
的额外误差。

predictors：

P1 =
abs(log(Js))

P2 =
H_KERNEL_REL

P3 =
H_KERNEL_MAD

P4 =
H_KERNEL_RANGE

P5 =
H_BUDGET_REL diagnostic

分别计算：

Pearson
Spearman

对：

每个 cubic state

以及 ALL_CUBIC pooled。

输出：

heterogeneity_extra_error_correlation.csv

==================================================
十二、State-Stratified Correlation
==================================================

禁止只使用 pooled correlation。

因为：

lambda severity
可能同时增加：

Js magnitude
heterogeneity
error。

可能产生 state confounding。

因此必须分别报告：

within cubic_l010

within cubic_l020

within cubic_l0333

以及 pooled。

如果 pooled high
但 within-state low：

禁止声称 heterogeneity law。

==================================================
十三、Regression Diagnostic
==================================================

使用 ALL_CUBIC：

Model A:

Delta_E ~ abs(log Js)

Model B:

Delta_E ~ H_KERNEL_REL

Model C:

Delta_E ~ abs(log Js) + H_KERNEL_REL

Model D:

Delta_E ~ abs(log Js)
        + H_KERNEL_REL
        + interaction

interaction =
abs(log Js)*H_KERNEL_REL

标准化 predictors。

报告：

R2
adjusted R2

coefficients

bootstrap 95% CI

VIF

仅 diagnostic。

输出：

matched_error_regression.csv

==================================================
十四、Strict Mechanism Gate
==================================================

M1:
CORRELATION PIPELINE VALID

Spearman bug resolved

且所有 cubic
得到 finite statistic。

--------------------------------------------------

M2:
MATCHED-JS EXTRA ERROR

cubic_l0333
通过 Matched-Js Gate。

--------------------------------------------------

M3:
SCREEN-FOOTPRINT HETEROGENEITY EXPLAINS EXTRA ERROR

必须同时满足：

A.

ALL_CUBIC：

Spearman(
Delta_E,
H_KERNEL_REL
)
>=0.7

B.

cubic_l0333 within-state：

Spearman >=0.6

C.

Model C adjusted R2

>=

Model A adjusted R2 + 0.10

D.

H_KERNEL_REL coefficient
bootstrap 95% CI
不跨 0。

如果满足：

SUPPORTED。

--------------------------------------------------

M4:
OPTICAL-BUDGET HETEROGENEITY

只作 diagnostic。

如果比 geometric H_KERNEL_REL
显著更强：

记录。

不能作为正式 Gate。

==================================================
十五、最终 CASE
==================================================

CASE B1:

M1 PASS
M2 PASS
M3 PASS

结论：

Point-local area-dilution consistency
breaks when deformation becomes heterogeneous
within the screen-space Gaussian footprint。

允许进入：

heterogeneity-aware optical consistency
method design。

注意：

此时仍不要直接使用
H_KERNEL_REL 作为最终方法。

--------------------------------------------------

CASE B2:

M1 PASS
M2 PASS
M3 FAIL

结论：

At matched local Js,
spatially varying deformation produces
significant extra optical error，

but Gaussian-footprint heterogeneity
does not yet explain the mechanism。

现象成立，
机制 unresolved。

禁止 method。

--------------------------------------------------

CASE C:

如果 screen-kernel weighted target
相对 self target MAE 降低 >=30%
且 Spearman >=0.9

则：

footprint-averaged deformation target supported。

--------------------------------------------------

CASE ARTIFACT:

M2 FAIL。

此前 coarse-bin 7-9x result
属于 bin / location / camera confounding。

撤回 heterogeneity line。

==================================================
十六、报告
==================================================

输出：

spearman_bug_audit.csv
corrected_cubic_correlation.csv

projected_covariance_validation.csv

screen_kernel_heterogeneity_per_camera.csv
screen_kernel_heterogeneity_material.csv
optical_budget_heterogeneity.csv

same_point_same_camera_matched_js.csv

heterogeneity_extra_error_correlation.csv
matched_error_regression.csv

screen_footprint_mechanism_report.md
stage3_3_3_summary.md
stage3_3_3_log.txt

==================================================
十七、报告必须回答
==================================================

A. Spearman NaN 根因。

B. 修复后 cubic l010 Spearman。

C. l020 Spearman。

D. l0333 Spearman。

E. projected covariance/radius validation。

F. cubic_l0333 matched Delta_E median。

G. Delta_E >0 fraction。

H. paired test p。

I. coarse-bin 7-9x conclusion
是否仍有效。

J. ALL_CUBIC Delta_E vs H_REL Spearman。

K. l0333 within-state Spearman。

L. Delta_E vs |log Js| Spearman。

M. Model A adjusted R2。

N. Model B adjusted R2。

O. Model C adjusted R2。

P. H_REL coefficient CI。

Q. optical-budget H 是否更强。

R. M1。

S. M2。

T. M3。

U. Final CASE。

V. 当前科学问题。

W. 是否允许开始设计 method。

==================================================
十八、实际执行
==================================================

必须实际：

修复 correlation bug

复现 actual projected covariance

验证 radii

screen kernel heterogeneity

same material point
same camera
matched q interpolation

paired matched analysis

state-stratified correlations

regression diagnostic

strict Gate

不要训练。

不要新 deformation。

不要设计 method。

不要修改 TSGS。

==================================================
十九、最终终端总结
==================================================

输出：

1. Spearman NaN root cause
2. cubic l010/l020/l0333 corrected Spearman
3. radius validation Spearman
4. l0333 matched Delta_E median
5. Delta_E positive fraction
6. paired p
7. coarse matched-Js finding valid yes/no
8. ALL_CUBIC Delta_E-H Spearman
9. l0333 within-state Delta_E-H Spearman
10. Delta_E-logJs Spearman
11. Model A adj R2
12. Model B adj R2
13. Model C adj R2
14. H coefficient CI
15. optical-budget H stronger yes/no
16. M1
17. M2
18. M3
19. Final CASE
20. Can design method yes/no
21. screen_footprint_mechanism_report.md path
22. stage3_3_3_summary.md path

请直接计算和验证。

不要训练。
不要新增 deformation。
不要提出 correction。
不要修改 TSGS。
PROMPT