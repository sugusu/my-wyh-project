cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
研究主线
==================================================

当前核心科学问题：

对于 transparent / semi-transparent Gaussian surface，

不同 Gaussian representations
如果在 canonical state 下具有近似相同的
accumulated alpha / transmittance，

那么施加完全相同的 geometry deformation transport 后，

这些 representations 是否仍保持
optical equivalence？

或者：

canonical-equivalent Gaussian representations
是否在 deformation 后产生
representation-dependent optical divergence？

中文：

形变前光学等价的透明 Gaussian 表示，
经过相同几何形变后，
是否产生表示依赖的光学分化？

==================================================
已有实验结论
==================================================

Stage 3.2.5 已确认：

对于同一个 frozen Gaussian geometry，

直接缩放 per-Gaussian tau：

0.25x
0.5x
1.0x
2.0x
4.0x
8.0x

stretch_2.00 的：

R_tau_eff =

1.664
1.518
1.461
1.571
1.627
1.627

因此：

opacity operating regime
会影响 deformation optical response magnitude。

但是：

这些 tau-scaled models
在 canonical state 下
本身具有不同 optical appearance。

因此：

Stage 3.2.5 只能证明：

OPACITY-REGIME DEPENDENCE

不能正式证明：

REPRESENTATION-DEPENDENT OPTICAL DRIFT。

Stage 3.2.5 Final CASE A
撤回。

当前状态：

INCOMPLETE。

另外：

R2 scale-policy 未完成。

R3 sampling-resolution 未完成。

R4 contributor mechanism
没有提供要求的 R_N_effect / R_S_effect
定量结果。

当前不要继续进行 contributor mechanism analysis。

先证明 representation divergence phenomenon。

==================================================
本阶段名称
==================================================

Stage 3.2.6:

Canonical-Equivalent Representation Divergence Gate

中文：

Canonical 光学等价表示的形变后光学分化验证

==================================================
一、唯一目标
==================================================

构建多个 Gaussian representations。

要求：

这些 representations
在 canonical state：

accumulated alpha field
和
effective optical depth field

近似相同。

然后：

施加完全相同的 deformation。

保持各自 canonical tau 固定。

比较：

deformed accumulated alpha

deformed effective optical depth。

回答：

Canonical-equivalent representations
是否在 deformation 后产生
systematic optical divergence？

当前：

不要提出 correction。

不要设计 optical constraint。

不要研究 contributor mechanism。

不要使用 GT_DYNAMIC 作为训练目标。

本阶段是 renderer mechanism experiment。

==================================================
二、项目边界
==================================================

所有新增代码和结果：

/data/wyh/DeformTransGS

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

创建：

/data/wyh/DeformTransGS/experiments/
stage3_2_6_canonical_equivalent_divergence/

==================================================
三、Renderer
==================================================

统一使用：

Stage 3.2.1 validated renderer path

TSGS official Camera

TSGS official render

override_color

two-pass white-pass alpha render

定义：

A(p) =
white-pass accumulated alpha

T(p) =
clamp(1-A(p), 1e-6, 1)

tau_eff(p) =
-log(T(p))

禁止：

-log(A)

==================================================
四、Canonical Reference Representation
==================================================

建立唯一 reference representation：

REF_41

41x41 Gaussian grid

1681 Gaussians

使用当前 Stage 3.2.5
frozen checkpoint geometry：

xyz
scale
rotation

以及：

tau

注意：

本阶段不要求该 checkpoint
near-perfect fit Mitsuba GT。

因为当前目标不是评价 GT fitting。

当前目标：

将 REF_41 的 canonical alpha field
定义为 representation-equivalence target。

使用正式 12-camera rig。

如果当前 benchmark camera rig 为 12 cameras：

全部使用。

如果代码只加载 3 cameras：

恢复冻结 benchmark camera definition
中的完整 12 cameras。

禁止创建新 camera。

渲染 canonical：

white-pass A_ref

tau_eff_ref

保存：

reference_alpha/
reference_tau_eff/

==================================================
五、Camera Split
==================================================

使用 12 cameras。

Canonical equivalence fitting cameras：

cam_000
cam_004
cam_008

Canonical holdout validation cameras：

其余 9 cameras。

目的：

避免 alternative representation
只匹配三个 training views。

==================================================
六、Alternative Representation Families
==================================================

建立两类 representations。

--------------------------------------------------
FAMILY S: Scale Policy
--------------------------------------------------

Gaussian count：

41x41
1681

xyz：

与 REF_41 完全相同。

canonical local frames：

相同。

scale multiplier：

S05 = 0.5
S075 = 0.75
S10 = 1.0 reference
S15 = 1.5
S20 = 2.0

对 activated scale
统一乘 multiplier。

rotation：

与 reference 相同。

--------------------------------------------------
FAMILY R: Sampling Resolution
--------------------------------------------------

规则 sheet：

R21:
21x21
441 Gaussians

R41:
41x41
1681
reference

R81:
81x81
6561 Gaussians

sheet physical dimensions：

与 reference 完全一致。

Gaussian tangent scale：

grid spacing
×
reference coverage multiplier。

normal scale ratio：

与 REF_41 相同。

local frame：

相同定义。

==================================================
七、Canonical Optical Equivalence Fitting
==================================================

这是本阶段最关键步骤。

对于每 alternative representation：

S05
S075
S15
S20
R21
R81

geometry：

全部冻结。

color：

constant white。

只允许优化：

per-Gaussian tau。

允许 per-Gaussian tau fitting。

原因：

当前目标是构造不同 Gaussian discretization /
footprint policy
对同一 canonical optical field
的等价表示。

禁止优化：

xyz
scale
rotation

禁止：

densification
pruning
split
clone

训练目标：

在 fitting cameras：

cam_000
cam_004
cam_008

最小化：

L_alpha =
mean abs(A_alt - A_ref)

+

0.2 *
mean abs(tau_eff_alt - tau_eff_ref)

只比较：

projected film valid region。

background 不参与。

optimizer：

Adam。

tau lr：

1e-2

最大：

5000 iterations。

保存：

1000
3000
5000

使用 final checkpoint。

禁止优化 color。

==================================================
八、Canonical Equivalence Gate
==================================================

对每 representation：

在：

3 fitting cameras

和：

9 holdout cameras

计算：

Alpha MAE

Alpha RMSE

Tau_eff MAE

Tau_eff RMSE

Alpha SSIM

定义：

FIT_EQUIVALENT：

3 fitting cameras mean：

Alpha MAE <= 0.002

Tau_eff MAE <= 0.003

HOLDOUT_EQUIVALENT：

9 holdout cameras mean：

Alpha MAE <= 0.005

Tau_eff MAE <= 0.0075

同时：

Alpha SSIM >= 0.98

只有同时满足：

FIT_EQUIVALENT
+
HOLDOUT_EQUIVALENT

才标记：

CANONICAL_EQUIVALENT。

不允许为了增加 representation 数量
修改 threshold。

输出：

canonical_equivalence_metrics.csv

canonical_equivalence_report.md

==================================================
九、Equivalent Representation Selection
==================================================

正式进入 deformation comparison
至少要求：

REF_41

以及：

至少 3 个 alternative representations
通过 CANONICAL_EQUIVALENT。

其中必须包括：

至少一个 FAMILY S

至少一个 FAMILY R。

如果：

少于 3 个 alternative PASS

或者：

没有 scale family PASS

或者：

没有 resolution family PASS

则：

EQUIVALENT REPRESENTATION CONSTRUCTION FAIL。

停止。

不要进行 deformation divergence。

分析为什么 canonical alpha field
无法由 alternative representation 表达。

==================================================
十、Canonical Pairwise Consistency
==================================================

对于所有 PASS representations：

在 12 cameras：

计算任意 pair：

A_i vs A_j MAE

tau_eff_i vs tau_eff_j MAE

输出：

canonical_pairwise_consistency.csv

记录：

max canonical pairwise alpha MAE

max canonical pairwise tau_eff MAE

这是 deformation 后 divergence
的 baseline。

==================================================
十一、统一 Geometry Deformation
==================================================

正式 states：

stretch_1.25
stretch_1.50
stretch_2.00

biaxial_1.50

twist_60

对每 representation：

使用完全相同 physical deformation definition。

xyz：

phi(xyz)

covariance：

Sigma' =
F Sigma F^T

恢复：

scale'
rotation'

tau：

保持该 representation
canonical fitted tau。

禁止 tau update。

Gaussian count：

保持不变。

禁止 densification。

禁止 pruning。

==================================================
十二、Deformed Optical Response
==================================================

对每：

representation
state
12 cameras

white-pass render：

A

tau_eff

计算：

material/projected interior：

median tau_eff

定义：

R_tau_eff =

median_tau_eff_deformed
/
median_tau_eff_canonical

注意：

每个 representation
使用自己的 canonical denominator。

输出：

deformed_response_metrics.csv

==================================================
十三、Representation Divergence
==================================================

对于每 state：

收集所有 canonical-equivalent representations：

R_tau_eff。

计算：

min

max

range

std

CV

定义：

D_R(state) =
max(R_tau_eff)
-
min(R_tau_eff)

输出：

representation_response_spread.csv

另外：

对每 camera：

计算 deformed alpha map
representation pairwise MAE。

输出：

deformed_pairwise_consistency.csv

==================================================
十四、Canonical-to-Deformed Divergence Amplification
==================================================

定义：

Canonical pairwise alpha disagreement：

D0_alpha

=
pairwise alpha MAE。

Deformed pairwise disagreement：

Ds_alpha

定义：

Amplification_alpha =
Ds_alpha /
(D0_alpha + 1e-8)

同理：

tau_eff。

输出：

divergence_amplification.csv

目的：

证明：

representations 在 canonical state
基本等价，

但 deformation 后失去等价性。

==================================================
十五、Physical Target
==================================================

只作为 diagnostic。

不可压缩 thin film target：

R_physical =
1 / Js

正式：

stretch_1.25:
0.8

stretch_1.50:
0.666667

stretch_2.00:
0.5

biaxial_1.50:
1/2.25
≈0.444444

twist_60:
≈1.0

对每 representation：

E_phys =
abs(
R_tau_eff
-
R_physical
)

输出：

physical_consistency_by_representation.csv

当前不要修改 tau。

==================================================
十六、Core Visualization
==================================================

使用 matplotlib。

禁止 seaborn。

不要指定颜色。

每张图独立。

1.

canonical_equivalence.png

显示：

REF_41
+
所有 PASS representation

cam_000 white-pass alpha。

2.

stretch2_response_by_representation.png

x：

representation

y：

R_tau_eff

画 physical target=0.5 horizontal line。

3.

response_spread_vs_js.png

x：

Js

y：

D_R(state)

4.

canonical_vs_deformed_pairwise.png

x：

state

y：

pairwise alpha MAE

包含 canonical baseline。

5.

representation_divergence_overview.png

states：

canonical
stretch_1.50
stretch_2.00
biaxial_1.50
twist_60

选择：

REF_41
最小 R_tau_eff representation
最大 R_tau_eff representation

展示 alpha maps。

==================================================
十七、Representation Divergence Gate
==================================================

PASS 必须同时满足：

A. CANONICAL EQUIVALENCE

REF + 至少 3 个 alternative representations
通过 CANONICAL_EQUIVALENT。

并包含：

scale family >=1

resolution family >=1。

B. DEFORMATION RESPONSE SPREAD

对于：

stretch_2.00

D_R >= 0.15

或者：

至少一个 representation：

R_tau_eff < 0.95

且另一个：

R_tau_eff > 1.05。

C. DIVERGENCE AMPLIFICATION

stretch_2.00：

deformed pairwise alpha MAE mean

>=

5 × canonical pairwise alpha MAE mean。

D. AREA-PRESERVING CONTROL

twist_60：

D_R_twist

<=

25% of D_R_stretch_2.00

或者：

twist max R_tau_eff deviation from 1
<=0.10。

如果：

A PASS

但 B/C 不满足：

NOT SUPPORTED。

如果：

A 无法满足：

INCONCLUSIVE。

禁止修改 threshold。

==================================================
十八、正式科学结论边界
==================================================

如果 Gate PASS：

允许结论：

Canonical-equivalent transparent Gaussian representations
can lose optical equivalence after identical geometry transport。

中文：

形变前具有近似相同累计透射的透明 Gaussian 表示，
在相同几何形变后可能失去光学等价性。

允许命名：

Representation-Dependent Optical Drift

表示依赖光学漂移。

允许表述：

The deformation-induced optical response
is not representation invariant。

中文：

形变诱导的光学响应不具有表示不变性。

此时下一阶段允许：

设计 deformation-aware optical consistency constraint。

但是仍禁止直接：

tau = tau / Js

作为最终方法。

--------------------------------------------------

如果 Gate NOT SUPPORTED：

说明：

在 canonical-equivalent control 下，
representation dependence 不成立。

Stage 3.2.5 仅证明
opacity-regime dependence。

停止 representation-drift 主线。

--------------------------------------------------

如果 INCONCLUSIVE：

说明：

无法构造 canonical-equivalent alternatives。

重新分析 carrier capacity。

==================================================
十九、报告
==================================================

输出：

reference_representation_report.md

alternative_representation_report.md

canonical_equivalence_metrics.csv
canonical_equivalence_report.md

canonical_pairwise_consistency.csv

deformed_response_metrics.csv
representation_response_spread.csv

deformed_pairwise_consistency.csv
divergence_amplification.csv

physical_consistency_by_representation.csv

representation_divergence_report.md
stage3_2_6_summary.md
stage3_2_6_log.txt

==================================================
二十、representation_divergence_report.md
==================================================

必须中文回答：

A. REF_41 canonical alpha 定义。

B. 使用多少 cameras。

C. fitting / holdout cameras。

D. 每个 representation：
   Gaussian count
   scale policy
   tau 是否重新拟合。

E. 哪些 representations
通过 canonical equivalence。

F. max canonical pairwise alpha MAE。

G. max canonical pairwise tau_eff MAE。

H. stretch_2.00
每个 representation 的 R_tau_eff。

I. stretch_2.00 D_R。

J. stretch_2.00 pairwise alpha amplification。

K. biaxial_1.50 D_R。

L. twist_60 D_R。

M. physical target comparison。

N. Representation Divergence Gate。

O. 是否正式支持：

representation-dependent optical drift。

只允许：

SUPPORTED
NOT SUPPORTED
INCONCLUSIVE

P. 是否允许进入
optical consistency constraint method design。

==================================================
二十一、Stage 3.2.5 结论修正
==================================================

在 README 和新 report 中明确：

Stage 3.2.5 did not prove
representation-dependent optical drift。

It proved:

opacity-operating-regime dependence
of deformation-induced accumulated transmittance。

The stronger representation-dependence hypothesis
requires canonical-equivalent representations。

==================================================
二十二、README
==================================================

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage 3.2.5 confirmed that
the magnitude of deformation-induced optical drift
depends on the opacity operating regime。

However,
direct tau scaling changes the canonical optical appearance
and therefore does not establish representation dependence。

Stage 3.2.6 constructs canonical-equivalent
Gaussian representations and tests whether
identical geometry transport breaks optical equivalence。

==================================================
二十三、实际执行
==================================================

必须实际：

建立 REF canonical alpha field

12-camera reference render

建立 scale family

建立 resolution family

tau-only canonical equivalence fitting

3-view fitting

9-view holdout validation

canonical equivalence Gate

pairwise canonical comparison

identical deformation transport

12-camera deformed response

R_tau_eff spread

pairwise divergence

divergence amplification

twist control

physical target diagnostic

Representation Divergence Gate

所有图表和报告

不要设计 correction。

不要分析 contributor count。

不要修改 TSGS。

不要使用 RT-Splatting。

==================================================
二十四、最终终端总结
==================================================

最后简洁输出：

1. Reference representation
2. Camera count
3. Canonical-equivalent representations
4. Number of equivalent alternatives
5. Scale-family equivalent yes/no
6. Resolution-family equivalent yes/no
7. Max canonical pairwise alpha MAE
8. Max canonical pairwise tau_eff MAE
9. stretch_2.00 R_tau_eff per representation
10. stretch_2.00 D_R
11. stretch_2.00 alpha disagreement amplification
12. biaxial_1.50 D_R
13. twist_60 D_R
14. Representation Divergence Gate
15. Representation-dependent optical drift
16. Can design optical consistency constraint yes/no
17. representation_divergence_report.md path
18. stage3_2_6_summary.md path

请直接实际运行和验证。

不要训练 geometry。

不要修改 benchmark。

不要提出 method。

不要修改 TSGS。
PROMPT