这次我要给你一个**有点残酷但很重要的判断**：

> **数值结果非常强，KIOT 在真实 TSGS Gaussian 上的迁移证据明显成立。**
>
> 但我**暂时不接受**
>
> `CASE TSGS-RECONSTRUCTED-KIOT-BRIDGE-SUPPORTED`
>
> 作为正式最终 Gate。

不是 KIOT 又出问题了。

而是 **Stage 3.5B 实际执行偏离了我们预先锁死的 bridge protocol，而且偏离发生在两个最关键的位置：透明 Gaussian 选择和形变适用域。**

我现在会把“科学结果”和“Gate 合规性”彻底分开讲。

---

# 一、先说好消息：这组结果真的很强

核心误差：

| 规则            |       平均中心误差 |
| ------------- | -----------: |
| 固定 opacity    |     0.306120 |
| (\tau/J_s)    |     0.216136 |
| opacity 线性    |     0.101056 |
| KIOT 连续版      |     0.037896 |
| **KIOT CUDA** | **0.023685** |

相对固定 opacity：

[
\frac{0.306120-0.023685}{0.306120}
==================================

92.26%
]

也就是说：

> **KIOT-CUDA 在 learned irregular Gaussians（学习得到的不规则 Gaussian）上，平均中心响应误差下降约 92.3%。**

而且：

[
\text{win fraction}=1.0
]

也就是报告统计的所有 patch-state pair（局部块—形变状态组合）上，R4 都赢 R0。

从你贴出的状态看，趋势也非常漂亮。

例如 Patch 0：

### 法向拉伸

[
Q=0.741
]

固定：

[
R_0=0.985
]

KIOT：

[
R_4=0.759
]

### 切向拉伸

[
Q=0.645
]

固定：

[
R_0=0.985
]

KIOT：

[
R_4=0.670
]

### 斜向拉伸

[
Q=0.556
]

固定：

[
R_0=0.982
]

KIOT：

[
R_4=0.588
]

这已经不是 41×41 人工 carrier 上的现象。

这是：

> **99 万 Gaussian TSGS checkpoint 中自动抽出的真实 learned Gaussian patches。**

而且 q=1 identity：

[
\max=0,\quad MAE=0
]

first-surface 输出漂移：

[
median=0.52%
]

[
p95=2.775%
]

valid-mask IoU：

[
0.9856
]

所以我对 KIOT 方法本身的判断是：

> **非常乐观。**

TSGS 本身明确采用两阶段方案，第二阶段固定 opacity 以维护已经建立的几何精度，因此检查 opacity 修改是否影响 first-surface 输出，确实是必要的语义安全测试。([arXiv][1])

---

# 二、但是第一个严重问题：你没有使用官方 transparent mask

我们原来的指令明确写：

优先级：

1. `transparent_masks`
2. object masks
3. RGBA alpha

如果不存在：

> `MASK-SOURCE-FAIL`

结果实际输出：

```text
transparent mask source:
checkpoint_visualize_nearest_depth_multiview_support
```

这个名字一看就不是官方 mask。

更关键的是，TSGS 官方 TransLab 数据管线明确支持生成 `transparent_masks/`，官方仓库文档列出的 TransLab 数据结构本身也包含 `transparent_masks/`。([GitHub][2])

所以这里有两种可能。

### 情况 A：你完整 scene_01 数据里实际有 `transparent_masks`

那 Codex 没用。

这是明确 protocol violation（协议违规）。

### 情况 B：你本地下载的数据确实没有

那么按照我们预定义规则：

> 应该 STOP `MASK-SOURCE-FAIL`

而不是自己创造：

```text
checkpoint_visualize_nearest_depth_multiview_support
```

替代 transparent mask。

---

## 为什么这件事很重要？

因为现在 candidate：

```text
119019 / 991832
fraction = 0.119999
```

这个比例甚至精确到大约：

[
12%
]

我会非常警惕。

不一定有 bug。

但是必须确认：

> 它是不是某个 top-12% / quota / percentile selection（按比例选取）产生的？

如果你的 candidate 是：

> 根据 checkpoint 自己的 nearest-depth / visualization support 选出来的

然后再测试：

> checkpoint Gaussian opacity transport

那么可能存在 representation-conditioned selection bias（表示条件选择偏差）。

说简单点：

> **用 TSGS 自己的几何表现来挑“最像透明表面”的 Gaussian，再证明 KIOT 在这些 Gaussian 上有效。**

它不一定错。

但论文审稿人完全可以问：

> 为什么不是官方透明物体 mask？

所以这一条必须补。

---

# 三、第二个严重问题：实验加入了 (J_s<1) 的 compression

Stage 3.5A-R1 明确锁定：

[
J_s\ge1
]

也就是：

> 面积保持或面积扩张。

正式 scope（适用范围）：

> `AREA-PRESERVING OR AREA-EXPANDING THIN-SURFACE DEFORMATION`

但 Stage 3.5B 实际出现：

```text
D2_normal_compress_0p75
```

而目标：

[
Q=1.333
]

显然：

[
q=\frac1{J_s}=1.333
]

所以：

[
J_s=0.75<1
]

这已经进入 compression（面积压缩）。

---

## 更关键的是：你的 aggregate B3 指标似乎把 compression 也算进去了

总平均：

```text
R0 = 0.306120
R1 = 0.216136
R2 = 0.101056
R3 = 0.037896
R4 = 0.023685
```

如果这里包含：

```text
D2_normal_compress_0p75
```

那么：

> **B3 Gate 使用了 Stage 3.5A-R1 尚未正式验证的方法域。**

虽然非常有意思的是，KIOT 在 compression 上居然也表现得很好：

Patch 0：

[
Q=1.333
]

[
R_4=1.309
]

Patch 1：

[
R_4=1.308
]

这结果其实相当惊喜。

但是科学纪律上：

> **不能因为 out-of-domain（适用域外）结果漂亮，就偷偷把它计入主 Gate。**

必须拆成：

### Primary scope

[
J_s\ge1
]

### Compression diagnostic

[
J_s<1
]

后者单独报告。

如果压缩真的稳定，我们再专门扩展 KIOT domain。

---

# 四、第三个问题：形变定义也发生了改变

原 Stage 3.5B prompt 写的是：

```text
D1 diag(1.25,1,1)
D2 diag(1.50,1,1)
D3 diag(2.00,1,1)
D4 diag(1.50,1.50,1)
D5 identity
D6 pure rotation
```

结果实际变成：

```text
D1_normal_stretch_1p35
D2_normal_compress_0p75
D3_tangent_shear_0p30
D4_tangent_stretch_1p55
D5_oblique_stretch_1p80
```

这套 deformation suite（形变集合）说实话**可能比我原来写的更合理**。

因为它明显是在 patch local frame（局部坐标系）里定义：

* normal stretch（法向拉伸）
* tangent shear（切向剪切）
* tangent stretch（切向拉伸）
* oblique stretch（斜向拉伸）

对于真实表面 patch，其实更有物理解释。

所以我不是说要删。

我的意见是：

> **必须承认 Stage 3.5B 实际跑的是“新 protocol”，而不是声称 B2 按原 protocol PASS。**

也就是说：

```text
B2 reconstructed patch protocol: PASS
```

这个标签目前太草率。

应该重新正式锁定这套 local-frame deformation protocol。

---

# 五、所以我对当前结果的正式评价

我不会判：

> `KIOT-CONTROLLED-ONLY`

因为明显已经不是 controlled-only。

KIOT 已经迁移到真实 learned TSGS Gaussian patch。

但我也暂时不判：

> `TSGS-RECONSTRUCTED-KIOT-BRIDGE-SUPPORTED`

我的正式标签是：

> **`KIOT-RECONSTRUCTED-BRIDGE-EVIDENCE-STRONG-BUT-PROTOCOL-DRIFT`**

中文：

> **KIOT 真实重建载体桥接证据很强，但实验协议发生漂移。**

注意：

**这是实验管理问题，不是方法失败。**

甚至我现在对方法比上一轮更乐观。

---

# 六、我特别建议：不要马上造 deformed-GT

只做一个很短的 Stage 3.5B-R1。

这是**最后的 bridge protocol closure（桥接协议闭环）**。

不重新渲染全部实验都可以。

优先利用现有 CSV。

解决三件事：

1. 查本地真实 `transparent_masks` 到底有没有；
2. 把 (J_s\ge1) 与 (J_s<1) 结果拆开；
3. 正式锁定实际使用的 local-frame deformation suite。

然后重新计算 B3。

**不改 KIOT。**

**不重新挑 patch，除非官方 transparent mask 证明当前 patch selection 无效。**

---

# 直接执行 Stage 3.5B-R1

```bash
cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前状态
============================================================

Stage3.5B reported：

B0 PASS
B1 PASS
B2 PASS
B3 SUPPORTED
B4 PASS
B5 PASS

Final：

TSGS-RECONSTRUCTED-KIOT-BRIDGE-SUPPORTED。

Numerically，
the result is strong。

Aggregate central errors：

R0 FIXED =
0.306120

R1 TAU_JS =
0.216136

R2 OPACITY_LINEAR =
0.101056

R3 KIOT_CONT =
0.037896

R4 KIOT_CUDA =
0.023685。

Reported：

R4 vs R0 improvement =
0.922628

R4 win fraction =
1.000000。

First-surface opacity sensitivity：

median relative depth drift =
0.005200

p95 relative depth drift =
0.027750

valid-mask IoU =
0.985600。

Full TSGS q=1 identity：

image max =
0

MAE =
0。

Therefore：

KIOT reconstructed-carrier evidence
is STRONG。

============================================================
但是 Stage3.5B 存在协议漂移
============================================================

PROTOCOL ISSUE 1：

Original Stage3.5B required
transparent Gaussian support
to use：

1. official transparent_masks

2. official object masks

3. RGBA alpha mask

in that priority。

If no valid mask exists：

MASK-SOURCE-FAIL。

Actual reported source：

checkpoint_visualize_nearest_depth_multiview_support。

This is NOT obviously
an official transparent mask。

The official TSGS TransLab pipeline
documents transparent_masks
as an optional/generated transparent-object mask source。

Therefore current candidate selection provenance
must be audited。

------------------------------------------------------------

PROTOCOL ISSUE 2：

Stage3.5A-R1 formal KIOT scope：

Js >=1

q=1/Js <=1。

AREA-PRESERVING OR AREA-EXPANDING
THIN-SURFACE DEFORMATION。

But Stage3.5B included：

D2_normal_compress_0p75

with：

Q approximately 1.333

therefore：

Js approximately 0.75 <1

q approximately 1.333 >1。

This is COMPRESSION，
outside the formally validated Stage3.5A-R1 domain。

It must NOT be included
in the primary Js>=1 KIOT bridge Gate
without an explicit compression-domain extension。

------------------------------------------------------------

PROTOCOL ISSUE 3：

Original deformation suite：

global affine diagonal stretches。

Actual Stage3.5B used：

D1_normal_stretch_1p35

D2_normal_compress_0p75

D3_tangent_shear_0p30

D4_tangent_stretch_1p55

D5_oblique_stretch_1p80

plus identity/rotation controls。

This local-frame deformation suite
may be scientifically reasonable，
but it is a DIFFERENT protocol。

It must be formally locked
and documented before final Gate acceptance。

============================================================
本阶段名称
============================================================

Stage 3.5B-R1:

Reconstructed Bridge Protocol Closure

中文：

真实重建载体桥接协议闭环

============================================================
一、唯一目标
============================================================

Do NOT redesign KIOT。

Answer：

Q1.

Did the local TransLab scene_01 data
contain an official transparent mask source？

Q2.

Was the Stage3.5B
119019-Gaussian candidate set
selected independently of policy/render outcomes，
and was the approximately 12% fraction
caused by a fixed quota/percentile？

Q3.

Does KIOT reconstructed-patch restoration
remain supported when the PRIMARY Gate
uses ONLY Js>=1 states？

Q4.

What exact local-frame deformation matrices
were actually used in Stage3.5B？

Q5.

Can the actual Stage3.5B local-frame protocol
be frozen as a reproducible bridge benchmark？

============================================================
二、禁止事项
============================================================

禁止：

change KIOT

change LUT

new optical method

MLP

train

fine-tune TSGS

new patch selection after seeing policy results

manual patch selection

change central metric

change anchor metric

change frozen policy responses

deformed-GT construction

full multi-scene evaluation。

This is only：

PROTOCOL CLOSURE。

============================================================
三、输出目录
============================================================

/data/wyh/DeformTransGS/experiments/
stage3_5B_R1_bridge_protocol_closure/

Do not overwrite Stage3.5B。

============================================================
四、Lock Stage3.5B Artifacts
============================================================

Locate and SHA256：

tsgs_bridge_protocol_lock.json

transparent_mask_source_audit.md

gaussian_transparent_multiview_support.csv

transparent_surface_candidate_lock.csv

patch_manifest.csv

patch Gaussian index npy files

patch_evaluation_camera_lock.csv

material_proxy_anchor_lock.csv

material_proxy_samples.npz

frozen_patch_anchor_camera_keys.csv

patch_policy_anchor_camera_response.csv

patch_policy_anchor_response.csv

reconstructed_patch_central_response.csv

reconstructed_patch_policy_comparison.csv

reconstructed_patch_tail_severity.csv

tsgs_first_surface_opacity_sensitivity.csv

full_tsgs_q1_identity.csv。

Output：

stage3_5B_artifact_lock.csv。

Gate C0：

all required artifacts lock。

============================================================
五、Actual Dataset Root Audit
============================================================

Read actual Stage3.5B protocol lock
and actual TSGS camera/dataset config。

Determine exact scene_01 dataset root。

Do NOT assume path。

Output：

actual_scene01_dataset_root.txt。

Recursively inspect only that scene root
for directories/files matching：

transparent_masks

transparent_mask

masks

mask

images

raw_images。

Output：

scene01_mask_file_manifest.csv。

Fields：

source_type
absolute_path
is_dir
file_count
extensions
first_10_filenames。

============================================================
六、Official Transparent Mask Audit
============================================================

For candidate sources：

transparent_masks

masks

RGBA images alpha。

For each source：

number images

resolution distribution

dtype

unique/min/max values

camera filename coverage

mapping to official TSGS camera names。

For transparent_masks：

verify actual files
and non-empty foreground fractions。

Output：

official_mask_semantic_audit.csv。

Formal priority：

P1 =
transparent_masks
if usable and camera coverage >=95%。

Else：

P2 =
masks
ONLY if dataset/source metadata
proves mask corresponds to the transparent object
or transparent object is the intended foreground object。

Else：

P3 =
RGBA alpha
only if alpha semantics are documented/verified。

If none：

OFFICIAL_MASK_AVAILABLE = NO。

Do not invent a replacement mask
inside this audit。

============================================================
七、Trace checkpoint_visualize_nearest_depth_multiview_support
============================================================

Locate exact Stage3.5B code
that produced：

checkpoint_visualize_nearest_depth_multiview_support。

Output：

checkpoint_support_source_trace.md。

Must include：

source path

function

line range

exact algorithm。

Answer：

Does it use：

checkpoint xyz

checkpoint opacity

checkpoint scale

checkpoint first-surface depth

white-pass alpha

policy result

KIOT result

central response result

or rendered appearance？

Explicitly mark each YES/NO。

============================================================
八、Candidate 12% Fraction Audit
============================================================

Current candidate：

119019 /991832
=
approximately 0.119999。

Trace candidate selection code。

Output：

candidate_fraction_source_trace.md。

Must answer：

Was candidate selection based on：

fixed top-k

top 12%

quantile

percentile

score threshold

view-count threshold

depth threshold

mask threshold

connected component

other？

If any fixed quota/percentile
was used：

record exact value。

Output candidate score distribution：

min
p001
p01
p05
p10
p12
median
p90
p95
p99
max。

Output：

candidate_selection_score_audit.csv。

Formal label：

CANDIDATE-QUOTA-SELECTED

or

CANDIDATE-THRESHOLD-SELECTED。

============================================================
九、Selection Independence Audit
============================================================

Prove candidate and patch selection
were completed BEFORE policy response evaluation。

Use：

file mtime_ns

script control flow

logs。

Compare timestamps：

candidate lock

patch manifest

policy render manifest

policy response CSV。

Output：

selection_independence_audit.csv。

Also search selection code
for references to：

R0

R1

R2

R3

R4

KIOT

central_error

E_log

policy_response

win_fraction。

Output：

selection_code_policy_keyword_search.txt。

Gate C1：

SELECTION INDEPENDENT
iff：

no policy response is used in candidate/patch score
and candidate/patch locks precede policy metric generation。

============================================================
十、Official-Mask Cross-Check
============================================================

Execute ONLY if：

OFFICIAL_MASK_AVAILABLE = YES。

Do NOT replace frozen candidates yet。

For each of the 119019
existing frozen candidate Gaussians：

project into the same 12 audit cameras。

Sample official transparent mask
with nearest-neighbour。

Compute：

official_mask_valid_views

official_mask_inside_views

official_mask_inside_fraction。

Output：

frozen_candidate_official_mask_crosscheck.csv。

For all 991832 Gaussians：

compute the SAME official-mask support summary
if computationally feasible。

Output：

all_gaussian_official_mask_support.csv。

Formal cross-check metrics：

candidate official-mask precision proxy：

fraction frozen candidates
with：

valid_views>=4

inside_fraction>=0.75。

candidate vs official-support Jaccard：

using official candidate definition：

valid_views>=4

inside_fraction>=0.75。

Report：

precision proxy

recall proxy

Jaccard。

IMPORTANT：

Do NOT call these true precision/recall
unless GT Gaussian labels exist。

============================================================
十一、Patch Official-Mask Cross-Check
============================================================

If official mask available：

for each frozen patch：

report fraction patch Gaussians
satisfying official candidate support。

Also patch projected center
inside-mask fraction
for each frozen evaluation camera。

Output：

frozen_patch_official_mask_crosscheck.csv。

Define：

FROZEN PATCH MASK CONSISTENCY PASS iff：

for every patch：

>=90% patch Gaussians
satisfy official support

and

median camera inside-mask fraction >=0.90。

If FAIL：

current Stage3.5B patch result
remains heuristic-support evidence only。

Do NOT automatically repick patches
in this stage。

============================================================
十二、Formal Lock Actual Local-Frame Deformations
============================================================

Trace actual Stage3.5B deformation code。

For every state：

IDENTITY

PURE_ROTATION

D1_normal_stretch_1p35

D2_normal_compress_0p75

D3_tangent_shear_0p30

D4_tangent_stretch_1p55

D5_oblique_stretch_1p80

record the exact matrix construction
in patch local basis：

[n, t1, t2]
or actual basis ordering。

Output：

actual_local_frame_deformation_definitions.md。

For every patch/state：

save actual world-space F。

Output：

actual_patch_deformation_matrices.npz

and：

actual_patch_deformation_audit.csv。

Fields：

patch_id
state

detF

singular_value_1
singular_value_2
singular_value_3

Js_patch

q_patch

Js_gaussian_median
p05
p95
CV

in_formal_kiot_domain。

Define：

in_formal_kiot_domain =
Js_i >=1
for ALL primary evaluated Gaussians/anchors
or the actual locked primary criterion。

Do not infer from state name only。

============================================================
十三、Primary vs Compression State Split
============================================================

PRIMARY：

patch-state pair where：

Js anchor/gaussian target
satisfies formal Stage3.5A-R1 scope：

Js>=1

q<=1。

COMPRESSION DIAGNOSTIC：

any patch-state with：

Js<1

q>1。

Output：

bridge_state_domain_classification.csv。

Fields：

patch_id
state
Js_min
Js_median
Js_max
q_median
domain_class。

No compression state
may enter PRIMARY B3 aggregation。

============================================================
十四、Recompute PRIMARY Bridge Metrics
============================================================

Use existing：

reconstructed_patch_central_response.csv
or exact response artifact。

NO rerender unless required。

Filter only PRIMARY domain pairs。

For policies：

R0_FIXED_OPTICAL

R1_TAU_JS

R2_OPACITY_LINEAR

R3_KIOT_CONT

R4_KIOT_CUDA。

Compute：

mean central error

median central error

win fraction R4 vs R0

R4 improvement from R0。

Output：

primary_domain_bridge_metrics.csv。

============================================================
十五、Primary B3 Gate
============================================================

Use original Stage3.5B B3 thresholds
UNCHANGED：

A.

R4 mean central error <=0.10。

B.

R4 mean central error
<=0.50 * R0 mean central error。

C.

R4 mean central error
<
R1 tau/Js mean central error。

D.

R4 beats R0
on >=75%
PRIMARY patch-state pairs。

E.

At least 2 independent patches
contain one PRIMARY state where：

R0 median response
is closer to1 than Q

and

R4 shifts toward Q
by >=50%
of R0 central error。

Output：

primary_b3_gate_recalculation.json。

Report A-E separately。

Gate C2：

PRIMARY KIOT BRIDGE
SUPPORTED / NOT SUPPORTED。

============================================================
十六、Compression Results Are Diagnostic Only
============================================================

For COMPRESSION DIAGNOSTIC pairs：

report：

R0/R1/R2/R3/R4/Q

central errors。

Output：

compression_diagnostic_results.csv。

Do NOT merge into C2。

Report：

number compression pairs

R4 mean error

R4 win fraction

whether any target
exceeded Stage3.5A-R1
formal inverse feasibility。

If compression results are strong：

state only：

COMPRESSION EXTENSION EVIDENCE OBSERVED。

Do NOT expand method scope yet。

============================================================
十七、Recompute Tail Summary on PRIMARY Domain
============================================================

Using existing patch anchor responses：

PRIMARY pairs only。

For R0 and R4：

median E_log

p95 E_log

factor2 fraction。

Paired R4-R0 Delta Elog。

Output：

primary_domain_tail_summary.csv。

Tail remains secondary。

============================================================
十八、Reclassify B2 Protocol Status
============================================================

B2 original label cannot remain PASS
without qualification
if original mask protocol was not followed。

Define：

B2A LOCAL-FRAME PATCH PROTOCOL LOCK：

PASS iff：

actual deformations recovered

patch/anchor/camera selection reproducible

identity/rotation controls previously PASS

selection independent。

Define：

B2B TRANSPARENT SUPPORT PROVENANCE：

PASS iff：

official mask source was used originally

OR

official-mask cross-check validates
all frozen patches under Section 11。

If no official mask available：

B2B =
HEURISTIC-SUPPORT-ONLY

not FAIL。

============================================================
十九、Reclassify Final Bridge Case
============================================================

CASE KIOT-RECONSTRUCTION-BRIDGE-SUPPORTED:

C0 PASS
C1 PASS
C2 SUPPORTED
B2A PASS

and B2B PASS。

Conclusion：

KIOT reconstruction bridge
is formally supported
on official transparent-mask-consistent
learned TSGS patches
within Js>=1 scope。

ALLOW：

deformed-GT benchmark construction

full reconstructed-carrier evaluation。

------------------------------------------------------------

CASE KIOT-RECONSTRUCTION-BRIDGE-HEURISTIC-SUPPORT:

C0 PASS
C1 PASS
C2 SUPPORTED
B2A PASS

but B2B =
HEURISTIC-SUPPORT-ONLY。

Conclusion：

KIOT transfers to automatically selected
learned TSGS surface patches，

but transparent-object membership
is currently based on heuristic
checkpoint support rather than official mask provenance。

ALLOW：

protocol repair with official masks。

Do NOT yet call
transparent-object reconstruction bridge
formally closed。

------------------------------------------------------------

CASE FROZEN-PATCH-MASK-INCONSISTENT:

official mask available

but frozen patch consistency FAIL。

Do NOT repick in this stage。

Current results remain
learned-surface patch evidence。

Next stage must rerun patch selection
using official transparent masks。

------------------------------------------------------------

CASE PRIMARY-DOMAIN-BRIDGE-NOT-SUPPORTED:

C2 NOT SUPPORTED。

KIOT bridge fails
within its formal Js>=1 scope。

============================================================
二十、Strict Gates
============================================================

C0:
ARTIFACT LOCK

PASS。

C1:
SELECTION INDEPENDENCE

PASS / FAIL。

C2:
PRIMARY Js>=1 KIOT BRIDGE

SUPPORTED / NOT SUPPORTED。

C3:
LOCAL-FRAME DEFORMATION PROTOCOL

LOCKED / FAIL。

C4:
TRANSPARENT SUPPORT PROVENANCE

OFFICIAL-MASK-PASS

FROZEN-PATCH-MASK-INCONSISTENT

HEURISTIC-SUPPORT-ONLY。

============================================================
二十一、Required Outputs
============================================================

stage3_5B_artifact_lock.csv

actual_scene01_dataset_root.txt
scene01_mask_file_manifest.csv
official_mask_semantic_audit.csv

checkpoint_support_source_trace.md
candidate_fraction_source_trace.md
candidate_selection_score_audit.csv

selection_independence_audit.csv
selection_code_policy_keyword_search.txt

if official mask available：

frozen_candidate_official_mask_crosscheck.csv
all_gaussian_official_mask_support.csv
frozen_patch_official_mask_crosscheck.csv

actual_local_frame_deformation_definitions.md
actual_patch_deformation_matrices.npz
actual_patch_deformation_audit.csv

bridge_state_domain_classification.csv

primary_domain_bridge_metrics.csv
primary_b3_gate_recalculation.json

compression_diagnostic_results.csv
primary_domain_tail_summary.csv

bridge_protocol_closure_report.md
stage3_5B_R1_summary.md
stage3_5B_R1_log.txt

============================================================
二十二、报告必须中文回答
============================================================

A. 为什么 Stage3.5B 数值强
但 Final CASE 不能直接接受。

B. actual dataset root。

C. transparent_masks exists yes/no。

D. transparent_masks usable yes/no。

E. masks exists yes/no and semantic。

F. RGBA alpha usable yes/no。

G. official mask selected source。

H. checkpoint_visualize support exact algorithm。

I. support algorithm uses opacity yes/no。

J. uses first-surface depth yes/no。

K. uses policy/KIOT result yes/no。

L. candidate 11.9999% 是 quota 还是 threshold。

M. candidate/patch selection independent yes/no。

N. official candidate cross-check metrics。

O. frozen patch official-mask consistency。

P. actual local basis definition。

Q. each deformation exact local matrix。

R. each state primary/compression classification。

S. PRIMARY pair count。

T. COMPRESSION pair count。

U. PRIMARY R0 mean central error。

V. PRIMARY R1 error。

W. PRIMARY R2 error。

X. PRIMARY R3 error。

Y. PRIMARY R4 error。

Z. PRIMARY R4 improvement。

AA. PRIMARY R4 win fraction。

AB. B3 A-E。

AC. C2。

AD. compression R4 mean error。

AE. compression extension evidence yes/no。

AF. primary tail result。

AG. B2A。

AH. B2B/C4。

AI. C0。

AJ. C1。

AK. C2。

AL. C3。

AM. C4。

AN. Final CASE。

AO. 是否允许 deformed-GT benchmark。

AP. 是否允许 full reconstructed-carrier evaluation。

============================================================
二十三、README
============================================================

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage3.5B produced strong numerical evidence
that KIOT transfers to learned TSGS Gaussian patches：

the reported CUDA-aware KIOT
mean central error was 0.023685，
a 92.3% reduction relative to fixed opacity，
with a 100% reported win fraction。

The q=1 full-checkpoint identity
was exact，
and direct KIOT opacity mutation
caused limited TSGS first-surface drift
under the tested patch protocol。

However，
the Stage3.5B execution differed
from the predefined bridge protocol
in two important ways。

First，
the reported transparent support source was
`checkpoint_visualize_nearest_depth_multiview_support`
rather than an explicitly verified official
TransLab transparent mask。

Second，
the evaluated deformation suite included
`normal_compress_0p75`,
which lies outside the formally validated
KIOT scope Js>=1。

Stage3.5B-R1 therefore audits
official mask provenance，
selection independence，
the exact local-frame deformation matrices，
and recomputes the bridge Gate
using only PRIMARY Js>=1 patch-state pairs。

No KIOT method changes are made。

============================================================
二十四、实际执行
============================================================

必须实际：

lock Stage3.5B artifacts

find actual scene root

audit transparent_masks/masks/RGBA

trace checkpoint support source

trace exact 12% candidate rule

prove selection independence

cross-check frozen candidates/patches
against official masks if available

recover exact local-frame deformation matrices

compute actual Js per patch/state

split PRIMARY Js>=1
from COMPRESSION diagnostic

recompute B3 A-E on PRIMARY only

report compression separately

recompute primary tail summary

C0-C4

Final CASE

不要：

change KIOT

rerender policies unless artifact missing

repick patches

construct deformed GT

full-scene evaluation

train
fine-tune
modify TSGS。
PROMPT
```

## 我现在最真实的评价

你现在**绝对不要因为我指出 protocol 漂移就觉得方向又出问题了**。

恰恰相反。

这次结果让我对 KIOT 更有信心：

[
0.3061\rightarrow0.0237
]

92.3% 改善。

并且真实 TSGS Gaussian 上：

[
R_0\approx0.98
]

几乎完全丢失面积稀释。

KIOT 能拉到：

[
0.759\quad(Q=0.741)
]

[
0.670\quad(Q=0.645)
]

[
0.588\quad(Q=0.556)
]

**这已经非常像真实方法结果。**

现在的问题只是：

> 你得保证审稿人不能一句“你这个 transparent patch 到底怎么选的？为什么还把未验证的 compression 算进主结果？”把整个表打回来。

先跑 3.5B-R1。

只要 **PRIMARY (J_s\ge1)** 重新统计后 KIOT 仍然保持明显优势，并且官方 `transparent_masks` 能验证这三个 frozen patches，我下一步就支持你**直接构建 deformed-GT benchmark，不再做 bridge 审计**。

[1]: https://arxiv.org/abs/2504.12799?utm_source=chatgpt.com "TSGS: Improving Gaussian Splatting for Transparent Surface Reconstruction via Normal and De-lighting Priors"
[2]: https://github.com/longxiang-ai/TSGS/blob/main/translab/README.md?utm_source=chatgpt.com "TSGS/translab/README.md at main · longxiang-ai/TSGS"
