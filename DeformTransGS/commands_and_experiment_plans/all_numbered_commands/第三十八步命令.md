cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式状态
============================================================

Stage 3.4B-R3 已完成：

Historical Alpha Metric Bridge。

结果：

33 historical alpha files：

ALL SHA MATCH。

但是：

使用 RECOVERED HISTORICAL ALPHA
通过 CURRENT locked metric path

无法复现 historical：

material_cell_response_exact_Q7.csv。

结果：

key coverage：

both = 14945 / 15040
left_only = 115
right_only = 80

R_cell：

median diff = 7.35e-02
p95 diff = 3.72
max diff = 3.34e4

Q_cell：

max diff = 3.67e-02

valid camera count：

exact。

因此：

H0 HISTORICAL ALPHA METRIC BRIDGE = FAIL。

Final：

HISTORICAL-METRIC-PROVENANCE-DIVERGENCE。

============================================================
正式研究决策
============================================================

停止继续追溯 historical Stage3.3.R4
exact metric implementation。

原因：

historical alpha 已恢复，

但 historical CSV
无法通过 current metric path 重建。

尤其：

Q_cell max diff = 0.0367。

Q_cell does NOT depend on rendered alpha。

Therefore：

the historical pipeline differed in：

cell definition

quadrature

Js sampling

target aggregation

validity filtering

or historical metric source。

Historical Stage3.3.R4 metrics：

0.0496
0.0668
0.0832
0.0519
0.945
0.0921
0.0360

正式降级为：

HISTORICAL EXPLORATORY EVIDENCE。

禁止继续作为：

hard Gate reference。

不要删除历史数据。

README 中保留 provenance warning。

============================================================
当前新的问题
============================================================

CURRENT metric pipeline：

is reproducible。

CURRENT renderer：

same-process bit-exact repeatable。

cross-process repeatability PASS。

CURRENT P0：

three-run metric ranges = 0。

但是 CURRENT P0 raw MAE：

stretch_1.25 = 79.0108
stretch_1.50 = 105.0934
stretch_2.00 = 121.5494
biaxial_1.50 = 153.8497

cubic_l0333 = 18.9505
shear_k040 = 125.9601
twist_60 = 128.6408。

A reproducible metric
is not automatically a scientifically valid metric。

Before：

P1 RIGID_COV
P2 FULL_AFFINE_COV
P3 ORACLE

the CURRENT metric itself
must pass known-answer oracle tests。

============================================================
重要数学修正：CELL TARGET
============================================================

CURRENT response definition：

tau_cell_can =
mean_k(tau_can_k)

tau_cell_def =
mean_k(tau_def_k)

R_cell =
tau_cell_def
/
tau_cell_can。

Suppose pointwise physical oracle：

tau_def_k =
q_k * tau_can_k

where：

q_k = 1 / Js_k。

Then：

R_cell

=

sum_k(
q_k * tau_can_k
)
/
sum_k(
tau_can_k
)。

Therefore the mathematically matched target is：

Q_tau_cell

=

sum_k(
tau_can_k * q_k
)
/
sum_k(
tau_can_k
)。

NOT：

mean_k(q_k)。

The old：

Q_arithmetic =
mean(1/Js)

is only exact when
tau_can is constant within the cell。

Furthermore：

tau_can is camera-dependent
because it is measured from rendered white-pass accumulation。

Therefore：

for each cell-camera：

Q_tau_camera

=

sum_valid(
tau_can_k * q_k
)
/
sum_valid(
tau_can_k
)。

Then：

R_cell =
median_c(
R_camera
)

must be compared with：

Q_tau_cell =
median over THE SAME VALID CAMERA SET
of Q_tau_camera。

This is an evaluation-target correction。

It is NOT：

a method

an optical update

a learned correction。

============================================================
本阶段名称
============================================================

Stage 3.4B-R4:

Current Optical Metric Oracle Validation
and Measurable-Support Gate

中文：

当前光学指标预言机验证
与可测信号支持门

============================================================
一、唯一目标
============================================================

回答：

Q1.

CURRENT metric
在已知：

tau_def = q * tau_can

的 synthetic oracle
下能否精确恢复 q？

Q2.

CURRENT ratio explosions
是否集中于 canonical optical signal
低于 renderer-defined measurable scale
的 cells？

Q3.

使用由本地 CUDA rasterizer
实际 alpha skip threshold
定义的 measurable-support filter 后，

CURRENT P0 是否形成：

stable

monotonic

physically interpretable

optical response？

只有全部通过：

允许 P1/P2/P3。

============================================================
二、禁止事项
============================================================

禁止：

P1 RIGID_COV

P2 FULL_AFFINE_COV

P3 tau/Js oracle geometry experiment

clone

split

training

tau fitting

method design

修改 TSGS。

本阶段只：

validate CURRENT metric。

============================================================
三、输出目录
============================================================

所有输出：

/data/wyh/DeformTransGS/experiments/
stage3_4B_R4_current_metric_oracle_validation/

禁止覆盖历史实验。

============================================================
四、锁定 CURRENT Pipeline
============================================================

锁定：

CURRENT carrier

CURRENT exact CUDA projection

CURRENT bilinear sampler

CURRENT Q7 midpoint quadrature

CURRENT material parameterization

CURRENT camera objects：

cam_000
cam_004
cam_008

CURRENT renderer

CURRENT extension binary

CURRENT alpha semantic。

记录 SHA256：

source
extension .so
forward.cu
carrier tensors
camera tensors。

输出：

current_metric_protocol_lock.json

以及：

current_metric_source_manifest.md

禁止读取 historical R4 CSV
作为 Gate target。

============================================================
五、读取本地 Rasterizer Alpha Skip Threshold
============================================================

必须检查当前实际 local source：

/data/wyh/repos/TSGS/submodules/
diff-first-surface-rasterization/
cuda_rasterizer/forward.cu

或 CURRENT extension 对应真实 source。

搜索 actual code：

alpha < ...

例如：

alpha < 1.0f / 255.0f

禁止根据 summary 硬编码。

输出：

rasterizer_alpha_skip_audit.md

必须记录：

source path
function
line
exact code。

解析：

alpha_skip。

定义：

tau_skip =

-log(
1 - alpha_skip
)。

Python：

```python
import math


def alpha_to_tau_scalar(
    alpha: float,
) -> float:
    if not (
        0.0 <= alpha < 1.0
    ):
        raise ValueError(
            f"invalid alpha={alpha}"
        )

    return -math.log(
        1.0 - alpha
    )


tau_skip = alpha_to_tau_scalar(
    alpha_skip
)
输出：

alpha_skip
tau_skip。

如果 source threshold
不是 1/255：

使用 actual source value。

============================================================
六、统一 Alpha → Tau

新建：

analysis/current_optical_metric.py

关键代码：

from __future__ import annotations

import numpy as np


def alpha_to_tau(
    alpha: np.ndarray,
) -> np.ndarray:
    alpha = np.asarray(
        alpha,
        dtype=np.float64,
    )

    if np.any(
        ~np.isfinite(alpha)
    ):
        raise ValueError(
            "alpha contains non-finite values"
        )

    if np.min(alpha) < -1e-7:
        raise ValueError(
            f"alpha min={alpha.min()}"
        )

    if np.max(alpha) >1.0 +1e-7:
        raise ValueError(
            f"alpha max={alpha.max()}"
        )

    alpha = np.clip(
        alpha,
        0.0,
        1.0 -1e-6,
    )

    return -np.log1p(
        -alpha
    )

必须 unit test：

A=0
→ tau=0

A=1-exp(-0.5)
→ tau=0.5

A=1-exp(-1)
→ tau=1

max error <1e-12。

输出：

alpha_tau_unit_test.md

============================================================
七、拆分 Cell-Camera Core Aggregation

建立纯 NumPy core。

不含：

projection
renderer
deformation。

def aggregate_cell_camera_response(
    tau_can: np.ndarray,
    tau_def: np.ndarray,
    q_samples: np.ndarray,
    sample_valid: np.ndarray,
) -> dict[str, float]:
    tau_can = np.asarray(
        tau_can,
        dtype=np.float64,
    )

    tau_def = np.asarray(
        tau_def,
        dtype=np.float64,
    )

    q_samples = np.asarray(
        q_samples,
        dtype=np.float64,
    )

    sample_valid = np.asarray(
        sample_valid,
        dtype=bool,
    )

    if not (
        tau_can.shape
        ==
        tau_def.shape
        ==
        q_samples.shape
        ==
        sample_valid.shape
    ):
        raise ValueError(
            "sample arrays shape mismatch"
        )

    valid = (
        sample_valid
        & np.isfinite(tau_can)
        & np.isfinite(tau_def)
        & np.isfinite(q_samples)
    )

    if valid.sum() == 0:
        return {
            "valid_sample_count": 0,
            "tau_cell_can": np.nan,
            "tau_cell_def": np.nan,
            "R_camera": np.nan,
            "Q_arithmetic_camera": np.nan,
            "Q_tau_camera": np.nan,
        }

    tc = tau_can[valid]
    td = tau_def[valid]
    q = q_samples[valid]

    tau_cell_can = float(
        np.mean(tc)
    )

    tau_cell_def = float(
        np.mean(td)
    )

    R_camera = float(
        tau_cell_def
        /
        (
            tau_cell_can
            + 1e-12
        )
    )

    Q_arithmetic_camera = float(
        np.mean(q)
    )

    tau_weight_sum = float(
        np.sum(tc)
    )

    if tau_weight_sum <=0:
        Q_tau_camera = np.nan
    else:
        Q_tau_camera = float(
            np.sum(
                tc * q
            )
            /
            tau_weight_sum
        )

    return {
        "valid_sample_count": int(
            valid.sum()
        ),
        "tau_cell_can": tau_cell_can,
        "tau_cell_def": tau_cell_def,
        "R_camera": R_camera,
        "Q_arithmetic_camera":
            Q_arithmetic_camera,
        "Q_tau_camera":
            Q_tau_camera,
    }
============================================================
八、Algebraic Target Unit Test

这是最高优先级数学测试。

固定 seed：

20260713。

生成：

10000 random cells。

每 cell：

49 samples。

tau_can：

log-uniform：

1e-4 到 3。

q：

uniform：

0.3 到 1.2。

定义：

tau_def =
q * tau_can。

对每 cell：

调用：

aggregate_cell_camera_response。

理论：

R_camera

必须精确等于：

Q_tau_camera。

不要求等于：

Q_arithmetic_camera。

计算：

abs(
R - Q_tau
)

要求：

max <=1e-10。

同时计算：

abs(
R - Q_arithmetic
)

报告：

mean
median
p95
max。

输出：

weighted_target_algebra_test.csv

和：

weighted_target_algebra_report.md

必须明确回答：

为什么 ratio-of-mean tau
对应 tau-weighted q target。

============================================================
九、Uniform Optical-Depth Oracle Images

使用 CURRENT canonical alpha：

cam000
cam004
cam008。

正式 q：

1.0
0.8
2/3
0.5
4/9。

对于每 q：

pixelwise：

tau_can_image =
alpha_to_tau(A_can)

tau_oracle_image =
q * tau_can_image

A_oracle =

1 - exp(
-tau_oracle_image
)

推荐：

def make_uniform_tau_oracle_alpha(
    alpha_can: np.ndarray,
    q: float,
) -> np.ndarray:
    tau = alpha_to_tau(
        alpha_can
    )

    tau_def = (
        float(q)
        * tau
    )

    return (
        1.0
        -
        np.exp(
            -tau_def
        )
    )

关键：

oracle uses SAME geometry。

canonical projected samples
和 deformed projected samples：

IDENTICAL。

使用 identity material mapping。

不要使用 stretch deformation。

这是 metric known-answer test。

============================================================
十、Oracle End-to-End Cell Metric

对于每：

q
camera
cell

使用：

CURRENT projection

CURRENT Q7 quadrature

CURRENT bilinear sampler

A_can

A_oracle。

同一个 sample coordinate：

p_can == p_def。

q_samples：

全部等于 q。

运行 full current cell-camera core。

cross-camera：

必须使用 current median logic。

定义：

R_cell =
median valid R_camera。

Q_tau_cell =
median over SAME valid cameras
Q_tau_camera。

Q_arithmetic_cell
同样保存。

输出：

uniform_tau_oracle_cell_response.csv

每 q 统计：

n

R median

MAE：
abs(R-q)

p95 error

max error。

Gate：

for ALL q：

median abs error <=1e-8

p95 <=1e-7

max <=1e-5。

如果 FAIL：

CURRENT METRIC FAIL。

立即停止。

============================================================
十一、Identity Oracle

A_def = A_can。

q =1。

必须：

R_camera approximately 1

R_cell approximately 1。

Gate：

median error <=1e-10

p95 <=1e-9

max <=1e-7。

输出：

identity_oracle_test.csv

============================================================
十二、Current P0 Raw Cell-Camera Trace

只有 oracle tests PASS 后执行。

正式 current P0 states：

canonical
stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.50
cubic_l010
cubic_l020
cubic_l0333
shear_k020
shear_k040
twist_60。

使用：

CURRENT deterministic fresh alpha。

对每：

state
cell_id
camera

输出：

current_p0_cell_camera_trace.csv

字段：

state
cell_id
camera_id

tau_cell_can
tau_cell_def

R_camera

Q_arithmetic_camera
Q_tau_camera

valid_sample_count
valid_sample_fraction

boundary_pass

measurable_support

其中：

measurable_support =

tau_cell_can >= tau_skip。

禁止用：

0.001
0.01

人为 threshold。

============================================================
十三、Support Conditioning Audit

对于每 state：

对 raw cell-camera：

统计 tau_cell_can：

min
p001
p01
p05
p10
median
p90
p99
max。

R_camera：

min
p001
p01
p05
median
p95
p99
p999
max。

定义 error：

abs(
R_camera

Q_tau_camera
)。

top 1% error samples：

tau_cell_can median。

all samples：

tau_cell_can median。

ratio：

tail_support_ratio。

输出：

current_p0_support_conditioning.csv

计算：

fraction：

tau_cell_can <0.5*tau_skip

<1.0*tau_skip

<2.0*tau_skip。

============================================================
十四、Error vs Support Relationship

pooled all states。

计算：

Spearman：

abs(R_camera-Q_tau_camera)

vs

-log10(
tau_cell_can +1e-12
)。

同时按：

tau_cell_can / tau_skip

bins：

[0,0.25)
[.25,.5)
[.5,1)
[1,2)
[2,4)
[4,8)
[8,inf)

每 bin：

n
median tau
median error
mean error
p90
p95
p99。

输出：

error_vs_optical_support.csv

定义：

LOW-SUPPORT PATHOLOGY SUPPORTED：

top 1% error
median tau_cell_can

<=

0.1 *
all median tau_cell_can

AND

median error in：

tau/tau_skip <1

=

5x median error
for tau/tau_skip >=2。

不用 Spearman
作为单独 hard Gate。

============================================================
十五、Formal Measurable Cell-Camera Definition

正式定义：

MEASURABLE camera-cell：

current existing projection/sample validity PASS

current boundary protocol PASS

valid sample fraction
满足 current protocol

tau_cell_can >= tau_skip。

只增加第 4 条。

原因：

tau_skip
来自 actual rasterizer
alpha contribution threshold。

不是调参 threshold。

输出：

measurable_support_definition.md

============================================================
十六、Cross-Camera Aggregation with Measurable Support

对于：

cell
state

仅聚合：

MEASURABLE camera-cell。

至少：

2 cameras measurable。

正式：

R_cell =
median(
R_camera
)

Q_tau_cell =
median over EXACT SAME CAMERA IDS
of：

Q_tau_camera。

Q_arithmetic_cell：

secondary diagnostic。

关键代码：

def aggregate_cross_camera_measurable(
    rows,
) -> dict:
    rows = [
        row
        for row in rows
        if row["measurable_support"]
    ]

    if len(rows) <2:
        return {
            "valid_camera_count": len(rows),
            "R_cell": np.nan,
            "Q_tau_cell": np.nan,
            "Q_arithmetic_cell": np.nan,
        }

    r = np.asarray(
        [
            row["R_camera"]
            for row in rows
        ],
        dtype=np.float64,
    )

    q_tau = np.asarray(
        [
            row["Q_tau_camera"]
            for row in rows
        ],
        dtype=np.float64,
    )

    q_arith = np.asarray(
        [
            row["Q_arithmetic_camera"]
            for row in rows
        ],
        dtype=np.float64,
    )

    return {
        "valid_camera_count":
            len(rows),
        "R_cell":
            float(np.median(r)),
        "Q_tau_cell":
            float(np.median(q_tau)),
        "Q_arithmetic_cell":
            float(np.median(q_arith)),
    }

IMPORTANT：

Q_tau
必须使用与 R 相同 cameras。

============================================================
十七、Rebaseline CURRENT P0

输出：

current_p0_measurable_cell_response.csv

每：

state
cell_id

记录：

R_cell
Q_tau_cell
Q_arithmetic_cell

valid_camera_count

error_tau =
abs(
R_cell-Q_tau_cell
)

error_arithmetic =
abs(
R_cell-Q_arithmetic_cell
)。

============================================================
十八、CURRENT P0 Metric Summary

每 state：

n total cells

n measurable cells

coverage fraction

R：

min
p01
p05
median
p95
p99
max

Q_tau median

Q_arithmetic median

MAE_tau

median_error_tau

p90_tau
p95_tau

MAE_arithmetic

median_error_arithmetic

Spearman(
R,
Q_tau
)
if Q_tau nonconstant。

输出：

current_p0_metric_rebaseline.csv

============================================================
十九、Target Semantics Comparison

对于：

cubic_l010
cubic_l020
cubic_l0333

计算：

abs(
Q_tau-Q_arithmetic
)

mean
median
p90
p95
max。

计算：

MAE(R,Q_tau)

vs

MAE(R,Q_arithmetic)。

输出：

weighted_vs_arithmetic_target.csv

定义：

WEIGHTED TARGET BENEFIT SUPPORTED：

for at least 2/3 cubic states：

MAE(R,Q_tau)
<
MAE(R,Q_arithmetic)

and relative reduction >=10%。

注意：

这只是 evaluation semantics。

不是 method。

============================================================
二十、Uniform Response Phenotype

uniform states：

stretch_1.25
stretch_1.50
stretch_2.00。

physical q：

0.8
2/3
0.5。

记录：

median R。

要求：

MONOTONIC：

R_1.25

R_1.50

R_2.00。

state-level Spearman：

median R
vs
q

必须：

rho >=0.99。

同时：

每 state：

abs(
median R - q
)
<=0.15。

定义：

FIXED-COV DILUTION PHENOTYPE SUPPORTED。

这是 NEW CURRENT baseline Gate。

禁止使用 historical R4 MAE。

============================================================
二十一、Area-Preserving Controls

states：

shear_k020
shear_k040
twist_60。

正式 target：

Q_tau approximately 1。

每 state：

median R

MAE_tau
median_error
p90
p95。

Gate：

shear_k020：

abs(median R-1)<=0.10

shear_k040：

abs(median R-1)<=0.10

twist_60：

abs(median R-1)<=0.10。

同时：

median error <=0.10。

定义：

AREA-PRESERVING PHENOTYPE SUPPORTED。

============================================================
二十二、Threshold Sensitivity Diagnostic

Formal threshold：

1.0 * tau_skip。

Secondary diagnostic only：

0.5 * tau_skip

2.0 * tau_skip。

分别重新聚合。

不要改变其他 metric。

每 state 比较：

coverage
median R
median error。

输出：

support_threshold_sensitivity.csv

定义：

SUPPORT GATE ROBUST：

for uniform
and area-preserving states：

median R difference：

formal vs 0.5x <=0.03

formal vs 2x <=0.03。

如果 FAIL：

measurable-support definition
仍过度敏感。

禁止 P1/P2/P3。

============================================================
二十三、Strict Gates

M0:
CURRENT PROTOCOL LOCK

PASS：

all SHA/input locks valid。

M1:
OPTICAL METRIC ORACLE

identity PASS

uniform tau oracle
all q PASS。

M2:
ALGEBRAIC TARGET SEMANTICS

weighted target algebra test：

max abs(R-Q_tau)<=1e-10。

M3:
MEASURABLE SUPPORT CONDITIONING

LOW-SUPPORT PATHOLOGY SUPPORTED

and

after formal support conditioning：

all state cell R
finite fraction >=0.99。

M4:
SUPPORT GATE ROBUSTNESS

threshold sensitivity PASS。

M5:
CURRENT FIXED-COV DILUTION PHENOTYPE

uniform monotonic Gate PASS

rho >=0.99

abs median R-q <=0.15
for all three states。

M6:
AREA-PRESERVING CURRENT CONTROL

shear/twist Gate PASS。

============================================================
二十四、Final CASE

CASE METRIC-LOCKED-P0-PHENOTYPE-SUPPORTED:

M0 PASS
M1 PASS
M2 PASS
M3 PASS
M4 PASS
M5 PASS
M6 PASS。

结论：

The historical R4 metric is retired
as a hard reference。

A new CURRENT optical metric
has passed known-answer oracle tests，
uses a mathematically matched
tau-weighted physical target，
and excludes only renderer-scale
unmeasurable optical cells。

Under this CURRENT locked protocol，
P0 FIXED_COV shows
a reproducible area-dilution phenotype
and area-preserving controls。

此时：

允许 Stage3.4B
P1/P2/P3 comparison。

后续所有 policies
必须使用：

same CURRENT metric

same measurable-support rule

same Q_tau target

same camera set

same renderer。

CASE METRIC-LOCKED-P0-PHENOTYPE-NOT-SUPPORTED:

M0-M4 PASS

M5 or M6 FAIL。

结论：

CURRENT metric is technically valid，

but the fixed-covariance carrier
does not reproduce the hypothesized
area-dilution phenotype
under the current reproducible pipeline。

此时：

不要 P1/P2/P3。

重新判断 shape-transport hypothesis。

CASE METRIC-FAIL:

M1 or M2 FAIL。

当前 metric 定义本身错误。

CASE SUPPORT-FAIL:

M3 or M4 FAIL。

ratio metric remains support-sensitive。

禁止 shape policy comparison。

============================================================
二十五、重要科学边界

禁止：

使用 historical R4
0.0832 等数值作为当前 Gate。

禁止：

因为 current P0
three-run identical
就称 metric valid。

禁止：

根据 MAE 被 threshold 改善
选择 threshold。

Formal threshold
必须来自 actual local rasterizer
alpha skip code。

禁止：

把 Q_tau 称为 method。

Q_tau 是：

metric-consistent physical target。

禁止：

运行 P1/P2/P3
unless Final CASE exactly：

METRIC-LOCKED-P0-PHENOTYPE-SUPPORTED。

============================================================
二十六、报告

必须输出：

current_metric_protocol_lock.json
current_metric_source_manifest.md

rasterizer_alpha_skip_audit.md
alpha_tau_unit_test.md

weighted_target_algebra_test.csv
weighted_target_algebra_report.md

identity_oracle_test.csv
uniform_tau_oracle_cell_response.csv

current_p0_cell_camera_trace.csv
current_p0_support_conditioning.csv
error_vs_optical_support.csv

measurable_support_definition.md
current_p0_measurable_cell_response.csv
current_p0_metric_rebaseline.csv

weighted_vs_arithmetic_target.csv
support_threshold_sensitivity.csv

current_metric_oracle_validation_report.md
stage3_4B_R4_summary.md
stage3_4B_R4_log.txt

============================================================
二十七、报告必须中文回答

A. 为什么停止追 historical R4 metric。

B. 为什么 historical R4
降级为 exploratory evidence。

C. CURRENT metric 是否 reproducible。

D. reproducible 为什么不等于 scientifically valid。

E. local rasterizer alpha_skip exact value。

F. tau_skip value。

G. 为什么 ratio-of-mean tau
对应 tau-weighted q。

H. algebra test max R-Q_tau error。

I. algebra test R-Q_arithmetic error。

J. identity oracle result。

K. q=0.8 oracle MAE/max。

L. q=2/3 oracle MAE/max。

M. q=0.5 oracle MAE/max。

N. M1。

O. current raw P0
top-tail tau support。

P. low-support pathology supported yes/no。

Q. measurable support retained coverage。

R. raw vs conditioned
R p99/max reduction。

S. cubic l010
Q_tau vs Q_arithmetic difference。

T. cubic l020 difference。

U. cubic l0333 difference。

V. weighted target benefit。

W. stretch1.25 median R/q。

X. stretch1.50 median R/q。

Y. stretch2 median R/q。

Z. uniform monotonic rho。

AA. shear k020 median R/error。

AB. shear k040 median R/error。

AC. twist60 median R/error。

AD. threshold 0.5x sensitivity。

AE. threshold 2x sensitivity。

AF. M0。

AG. M1。

AH. M2。

AI. M3。

AJ. M4。

AK. M5。

AL. M6。

AM. Final CASE。

AN. 是否允许 P1/P2/P3。

============================================================
二十八、README

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage3.4B-R3 proved that
the recovered historical Stage3.3.R4 alpha maps
do not reproduce the historical Q7 CSV
through the CURRENT metric path。

The divergence includes：

R_cell differences

cell-key differences

and Q_cell differences。

Because Q_cell does not depend on alpha，
the historical metric/target implementation
itself differed from the current pipeline。

The historical R4 metrics are therefore retained
only as historical exploratory evidence
and are no longer used as hard Gate references。

Stage3.4B-R4 validates
the CURRENT optical metric
using known-answer optical-depth oracle tests。

It also corrects the material-cell physical target：

for a response defined as

mean(tau_def) / mean(tau_can),

the matched pointwise law

tau_def = q * tau_can

implies a tau-weighted target

sum(tau_can*q) / sum(tau_can),

not the arithmetic mean of q。

A measurable-support Gate
is derived from the actual local rasterizer
alpha contribution skip threshold，
rather than an empirical denominator cutoff。

============================================================
二十九、实际执行

必须实际：

lock current pipeline

read actual CUDA alpha skip threshold

alpha-to-tau unit tests

implement cell-camera core

weighted-target algebra test

identity alpha oracle

uniform q optical-depth oracle images

end-to-end oracle metric

trace current P0 per camera/cell

audit low-support conditioning

define renderer-grounded measurable support

reaggregate with same camera sets
for R and Q_tau

rebaseline current P0

compare weighted vs arithmetic target

uniform phenotype

area-preserving controls

threshold sensitivity

M0-M6

Final CASE

不要运行 P1。

不要运行 P2。

不要运行 P3。

不要 covariance mechanism。

不要 clone。

不要 split。

不要 train。

不要修改 TSGS。

============================================================
三十、最终终端总结

最后只输出：

Historical R4 hard reference retired yes/no
Current protocol lock PASS/FAIL
alpha_skip
tau_skip
Algebra R-Q_tau max error
Algebra R-Q_arithmetic median error
Identity oracle PASS/FAIL
q0.8 oracle MAE/max
q2/3 oracle MAE/max
q0.5 oracle MAE/max
Optical oracle Gate PASS/FAIL
Low-support pathology supported yes/no
Formal support retained coverage
Raw→conditioned R p99 reduction
Weighted target benefit supported yes/no
stretch1.25 median R/q
stretch1.50 median R/q
stretch2 median R/q
Uniform monotonic rho
shear k020 R/error
shear k040 R/error
twist60 R/error
0.5x threshold max median-R drift
2x threshold max median-R drift
M0
M1
M2
M3
M4
M5
M6
Final CASE
Can run P1/P2/P3 yes/no
current_metric_oracle_validation_report.md path
stage3_4B_R4_summary.md path

请直接实际运行和验证。

最高优先级：

STOP HISTORICAL METRIC ARCHAEOLOGY。

DO NOT USE HISTORICAL R4 NUMBERS AS HARD GATES。

REPRODUCIBLE DOES NOT MEAN VALID。

FIRST TEST THE METRIC ON KNOWN-ANSWER OPTICAL ORACLES。

FOR R = MEAN(TAU_DEF) / MEAN(TAU_CAN)，

THE MATCHED TARGET IS：

SUM(TAU_CAN * Q) / SUM(TAU_CAN)。

NOT MEAN(Q)。

USE THE SAME CAMERA SET
FOR R_CELL AND Q_TAU_CELL。

DERIVE SUPPORT THRESHOLD
FROM ACTUAL LOCAL CUDA ALPHA SKIP。

DO NOT RUN P1/P2/P3 UNTIL M0-M6 PASS。
PROMPT