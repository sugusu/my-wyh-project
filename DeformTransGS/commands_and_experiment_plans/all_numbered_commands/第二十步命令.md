cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前研究状态
==================================================

当前主线：

透明 / 半透明 Gaussian 在几何形变后，
accumulated transmittance 会发生隐式变化。

Stage 3.2.4 初步发现：

对于相同 stretch_2.00 deformation，

tau scaling diagnostic：

0.5 × canonical tau:
R_tau_eff = 2.37

1.0 × canonical tau:
R_tau_eff = 1.46

2.0 × canonical tau:
R_tau_eff = 1.02

4.0 × canonical tau:
R_tau_eff = 0.93

说明：

implicit optical response
可能依赖 canonical tau operating regime，
甚至出现方向翻转。

Scale-policy ablation
也显示 response 对 Gaussian scale 敏感。

但是 Stage 3.2.4 存在三个未完成问题：

1. 未使用协议要求的 Stage 3.2 near-perfect checkpoint。

2. 未实际测量 contributor count / contributor strength，
却提前将 position effect 解释为 contributor count mechanism。

3. calibrated 21/41/81 sampling-resolution experiment 未完成。

因此：

Stage 3.2.4 状态：

PROMISING BUT INCOMPLETE

禁止开始 proposed method。

==================================================
本阶段名称
==================================================

Stage 3.2.5:

Representation-Dependent Optical Drift Confirmation

中文：

透明 Gaussian 表示依赖光学漂移确认实验

==================================================
一、唯一目标
==================================================

只验证三个问题：

Q1.

在同一个 frozen canonical checkpoint 下，

改变 tau operating regime，

是否能够稳定使同一个 deformation 的：

R_tau_eff

发生显著 magnitude change
甚至 direction flip？

Q2.

position-only transport 改变 tau_eff
究竟来自：

contributor count N_effect

还是：

per-contributor strength S_effect

或两者共同作用？

Q3.

在 canonical optical depth 公平校准后，

21×21
41×41
81×81

不同 Gaussian sampling resolution
是否产生不同 deformation optical response？

禁止：

训练新的 per-Gaussian model

E2 refit

设计 correction

tau/Js method

MLP

修改 benchmark

修改 TSGS

==================================================
二、项目边界
==================================================

所有输出：

/data/wyh/DeformTransGS/experiments/
stage3_2_5_representation_drift_confirmation/

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

==================================================
三、唯一 Frozen Checkpoint
==================================================

首先审计已有 Stage 3.2 文件。

搜索：

canonical_checkpoint_manifest.json

canonical fit reports

iteration 5000 checkpoint

tau/color tensor dumps

检查此前声明：

Canonical NORE = 0.0000

对应 checkpoint 是否真实存在。

不要根据 filename 猜。

对于所有候选 checkpoint：

独立加载。

使用 Stage 3.2.1 validated renderer：

official TSGS Camera
official render
override_color
two-pass alpha composite

渲染：

cam_000
cam_004
cam_008

canonical tau0=1.0 GT。

独立计算：

L1
Masked MAE
ORE
NORE

输出：

checkpoint_reproduction.csv

选择规则：

如果存在：

mean NORE <= 0.01

选择最低 NORE checkpoint。

标记：

FROZEN_CANONICAL。

如果不存在：

明确写：

Stage 3.2 near-zero canonical checkpoint
cannot be reproduced。

此时：

不要使用 Stage 3.2.4 loss=0.045 checkpoint
冒充 near-perfect checkpoint。

选择当前可复现的最低 NORE checkpoint，

但报告必须标记：

BEST_REPRODUCIBLE_CHECKPOINT

并记录 NORE。

禁止重新训练。

后续所有 Q1/Q2：

只使用这一唯一 checkpoint。

保存：

frozen_checkpoint_lock.json

包含：

path
NORE
loss
xyz SHA256
scale SHA256
rotation SHA256
tau SHA256
color SHA256

==================================================
四、统一 Tau_eff 定义
==================================================

正式：

A =
validated white-pass accumulated alpha

T =
clamp(1-A, 1e-6, 1)

tau_eff =
-log(T)

禁止 -log(A)。

正式主指标：

material-point median tau_eff。

R_tau_eff(state) =

median_tau_eff(state)
/
median_tau_eff(canonical)

同一个 setting：

canonical 与 deformation
必须使用完全一致：

tau scaling
scale policy
camera
material-point filter。

==================================================
五、Q1：同 Checkpoint Tau-Regime Direction Flip
==================================================

从唯一 frozen checkpoint fresh load。

geometry：

完全相同。

color：

完全相同。

只进行 diagnostic tau scaling：

0.25×
0.5×
1.0×
2.0×
4.0×
8.0×

禁止训练。

对于每 tau scale：

canonical

stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.50
twist_60

使用：

G3 FULL geometry transport。

计算：

canonical median tau_eff

state median tau_eff

R_tau_eff

输出：

tau_regime_confirmation.csv

对 stretch_2.00：

必须报告完整 response curve。

定义：

DIRECTION_FLIP_CONFIRMED：

至少存在两个 tau scale：

一个：

R_tau_eff > 1.05

另一个：

R_tau_eff < 0.95

如果：

只有 magnitude variation，
没有 crossing：

DIRECTION_DEPENDENCE_SUPPORTED

但：

DIRECTION_FLIP_NOT_CONFIRMED。

同时检查：

结果是否与 Stage 3.2.4
0.5/1/2/4 trend 一致。

==================================================
六、Tau Saturation Diagnostics
==================================================

对于每 tau scale：

计算 canonical 和 stretch_2.00：

per-Gaussian scalar opacity：

opacity_i =
1-exp(-tau_i)

统计：

mean
median
p10
p90

fraction opacity > 0.5
fraction opacity > 0.8
fraction opacity > 0.95

white-pass：

A median

tau_eff median

输出：

tau_saturation_diagnostics.csv

目的：

不要笼统使用：

low opacity
high opacity
saturation

必须给出真实 opacity distribution。

==================================================
七、Q2：Contributor Exact Reference
==================================================

只使用：

cam_000

states：

canonical
stretch_1.25
stretch_1.50
stretch_2.00

variants：

G0 canonical

G1 position only

G2 covariance only

G3 full transport

tau scales：

0.5×
1.0×
4.0×

从唯一 frozen checkpoint fresh load。

==================================================
八、Reference Pixel Selection
==================================================

每个 state：

使用 G3 projected sheet interior。

选择最多：

128 material points。

要求：

positive depth
inside image
距离 boundary >= 8 pixels

使用 Gaussian identity
投影得到 material-point pixel。

同 state：

G0/G1/G2/G3

使用同一 material indices。

保存：

reference_material_points.csv

==================================================
九、精确复现 Rasterizer Contributor Logic
==================================================

只读检查实际 TSGS rasterizer CUDA forward。

在 DeformTransGS 中建立：

analysis/reference_alpha_accumulator.py

使用真实：

projected mean

conic / projected covariance

per-Gaussian opacity

depth order

对于每 reference pixel：

严格按照 CUDA 实际规则：

power

exp(power)

alpha = opacity * exp(power)

alpha clamp upper 0.99

alpha skip threshold

transmittance update

early termination

模拟 accumulation。

不得修改 CUDA。

先验证：

reference tau_eff

vs

official white-pass tau_eff。

要求：

median absolute error <1e-4

max error <1e-3

如果不能达到：

禁止做 contributor mechanism claim。

输出：

reference_accumulator_validation.csv

==================================================
十、Contributor Decomposition
==================================================

reference accumulator 验证通过后。

对于每：

state
variant
tau_scale
material point

记录：

N_candidate

N_non_skipped

N_until_termination

sum_alpha

mean_alpha_non_skipped

median_alpha_non_skipped

sum_log_contribution =
sum[-log(1-alpha_i)]

mean_log_contribution

max_log_contribution

定义：

N_effect =
N_non_skipped

S_effect =
mean_log_contribution

检查：

tau_eff =
N_effect * S_effect

数值误差。

输出：

contributor_decomposition.csv

==================================================
十一、正式 Position Mechanism 判断
==================================================

对于：

stretch_2.00
G1 position-only

分别比较：

tau scale 0.5
1.0
4.0

relative to G0：

R_center_density

R_N_effect

R_S_effect

R_tau_eff

必须回答：

tau_eff increase/decrease

主要对应：

N_effect change

S_effect change

还是 nonlinear interaction / early termination。

禁止写：

“more Gaussians contribute”

除非：

R_N_effect > 1

且实际统计支持。

禁止写：

“fewer Gaussians contribute”

除非：

R_N_effect < 1。

生成：

position_mechanism_report.md

==================================================
十二、Q3：Calibrated Sampling Resolution
==================================================

grid：

21×21
41×41
81×81

规则 canonical sheet。

Gaussian count：

441
1681
6561

geometry：

同一 1.5×1.5 sheet。

Gaussian identity fixed。

scale：

tangent scale =
grid spacing × same coverage multiplier

normal scale policy 相同。

color：

constant white。

每 resolution 只允许一个：

global scalar tau。

==================================================
十三、Canonical Optical Calibration
==================================================

目标：

三个 resolution canonical
具有相同 optical operating point。

Target：

使用唯一 frozen checkpoint：

cam_000
cam_004
cam_008

material-point median tau_eff
的三相机 mean。

记录：

TARGET_TAU_EFF。

对于：

21
41
81

只优化：

global scalar tau_raw

一个参数。

geometry frozen。

color white。

目标 loss：

abs(
mean_three_camera_median_tau_eff
-
TARGET_TAU_EFF
)

允许使用 Adam
或 binary search。

优先 binary search，
因为只有一个 monotonic scalar。

要求：

canonical calibration relative error <=1%。

记录：

resolution
grid spacing
Gaussian count
scale
global tau
global opacity
canonical tau_eff
relative error

输出：

sampling_calibration.csv

如果某 resolution 无法达到 1%：

禁止进行 sampling comparison。

==================================================
十四、Calibrated Resolution Deformation Test
==================================================

校准后冻结 global tau。

对每 resolution：

canonical

stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.50
twist_60

G3 full geometry transport。

计算：

R_tau_eff。

输出：

sampling_resolution_confirmation.csv

对于 stretch_2.00：

记录：

R21
R41
R81

定义：

SAMPLING_DEPENDENCE_SUPPORTED：

max(R21,R41,R81)
-
min(R21,R41,R81)
>= 0.20

或：

至少一个 <0.95
且至少一个 >1.05。

否则：

NOT SUPPORTED。

==================================================
十五、Scale Policy 复现
==================================================

使用唯一 frozen checkpoint。

只对：

stretch_2.00

scale multipliers：

0.5
1.0
1.5
2.0

tau canonical fixed。

重新计算：

R_tau_eff。

输出：

scale_policy_confirmation.csv

确认 Stage 3.2.4 scale dependence
是否能在唯一 checkpoint 下复现。

Scale dependence supported：

max R - min R >=0.20
或 direction crossing 1。

==================================================
十六、Representation Dependence Gate
==================================================

R1 TAU-REGIME DEPENDENCE

SUPPORTED：

stretch_2.00 R_tau_eff range
across tau scale >=0.20。

STRONG：

同时 DIRECTION_FLIP_CONFIRMED。

R2 SCALE-POLICY DEPENDENCE

SUPPORTED：

stretch_2.00 R range
across scale multipliers >=0.20
or crosses 1。

R3 SAMPLING-RESOLUTION DEPENDENCE

严格根据 calibrated 21/41/81。

SUPPORTED / NOT SUPPORTED。

R4 CONTRIBUTOR MECHANISM RESOLVED

SUPPORTED：

reference accumulator validation PASS

且：

position G1 tau_eff change
可以通过 measured N_effect / S_effect
给出量化解释。

==================================================
十七、最终科研决策
==================================================

CASE A:

R1 supported
+
R2 supported
+
R4 supported

结论：

Gaussian geometry transport produces
representation-dependent optical drift。

Opacity operating regime and Gaussian footprint policy
change the deformation-induced accumulated transmittance。

如果 R3 同时 supported：

可以进一步写：

Gaussian discretization density
also affects the drift。

只有此 CASE：

下一阶段允许设计
deformation-aware optical consistency constraint。

注意：

仍然不要直接设计 tau/Js formula。

CASE B:

R1/R2 effect
在唯一 checkpoint 下无法复现。

结论：

Stage 3.2.4 result
was checkpoint-specific artifact。

停止 representation-dependent drift 主线。

CASE C:

R1/R2 supported
但 contributor mechanism 无法解析。

结论：

phenomenon supported，
mechanism unresolved。

继续机制分析。

不设计 method。

==================================================
十八、论文级表述边界
==================================================

如果 CASE A：

允许表述：

“相同几何形变下，
Gaussian 表示的 opacity 工作区间与 footprint policy
会改变累计透射的演化幅度，
甚至可能改变变化方向。”

只有 R3 supported：

才允许加入：

“sampling resolution / discretization density”。

禁止写：

“物理不一致”
作为绝对结论。

更准确：

“does not guarantee representation-invariant
or physically consistent optical evolution”

中文：

“不保证表示无关或物理一致的光学演化”。

==================================================
十九、报告
==================================================

输出：

checkpoint_reproduction_report.md

tau_regime_report.md
tau_saturation_report.md

reference_accumulator_report.md
position_mechanism_report.md

sampling_resolution_report.md
scale_policy_report.md

representation_dependence_report.md
stage3_2_5_summary.md
stage3_2_5_log.txt

==================================================
二十、stage3_2_5_summary.md
==================================================

必须回答：

1. Stage 3.2 near-zero checkpoint 是否真实找到。

2. Frozen checkpoint path。

3. Frozen checkpoint NORE。

4. stretch_2.00 tau-scale response curve。

5. Tau direction flip confirmed yes/no。

6. Actual opacity distribution at 0.5× / 1× / 4×。

7. Reference accumulator validation error。

8. stretch_2.00 G1 0.5×：
   R_N_effect
   R_S_effect
   R_tau_eff

9. stretch_2.00 G1 1×：
   同上。

10. stretch_2.00 G1 4×：
    同上。

11. Position mechanism final explanation。

12. Sampling TARGET_TAU_EFF。

13. 21 calibration error。

14. 41 calibration error。

15. 81 calibration error。

16. stretch_2.00 R21/R41/R81。

17. Sampling dependence。

18. Scale-policy response curve。

19. R1。

20. R2。

21. R3。

22. R4。

23. Final CASE。

24. 是否允许开始设计 optical consistency constraint。

25. 当前论文级研究问题。

==================================================
二十一、README
==================================================

更新 README。

明确：

Stage 3.2.4 revealed strong tau-level
and scale-policy dependence，
but used a non-locked canonical checkpoint
and did not complete exact contributor
or calibrated sampling analysis。

Stage 3.2.5 performs confirmation
under one frozen checkpoint。

==================================================
二十二、实际执行
==================================================

必须实际：

checkpoint search and reproduction

freeze one checkpoint

tau-scale sweep

opacity distribution statistics

reference accumulator implementation

CUDA logic validation

exact contributor decomposition

position mechanism analysis

21/41/81 optical calibration

calibrated sampling deformation

scale policy confirmation

R1-R4 decisions

Final CASE

不要训练 per-Gaussian model。

不要实现 correction。

不要修改 TSGS。

==================================================
二十三、最终终端总结
==================================================

最后简洁输出：

1. Near-zero checkpoint found yes/no
2. Frozen checkpoint
3. Frozen NORE
4. stretch_2.00 tau response curve
5. Direction flip confirmed yes/no
6. opacity median 0.5x / 1x / 4x
7. reference accumulator max error
8. G1 0.5x R_N / R_S / R_tau_eff
9. G1 1x R_N / R_S / R_tau_eff
10. G1 4x R_N / R_S / R_tau_eff
11. position mechanism
12. calibrated R21
13. calibrated R41
14. calibrated R81
15. sampling dependence
16. scale response curve
17. R1
18. R2
19. R3
20. R4
21. Final CASE
22. Can design optical consistency constraint yes/no
23. representation_dependence_report.md path
24. stage3_2_5_summary.md path

请直接运行和验证。

不要换服务器。
不要使用 RT-Splatting。
不要设计 proposed correction。
不要修改 TSGS。
PROMPT