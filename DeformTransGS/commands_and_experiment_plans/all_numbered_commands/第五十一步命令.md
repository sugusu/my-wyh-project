这次结果其实比报告写的**好很多**。

我直接给结论：

> **我不接受 `CASE NORMAL-SEMANTIC-BUG`。**
>
> 当前更准确的状态应该是：
>
> [
> \boxed{\text{DEPTH-NORMAL-BRIDGE-PROVISIONAL}}
> ]
>
> 中文：**第一表面深度法向桥接基本成立，但还差层纯度与数值等价性收口。**

而且，这次不是我“想救 KIOT”。

是报告自己的数字和 Final CASE **直接矛盾**。

---

# 一、这里根本没有证据证明 normal implementation bug

R4 得到：

[
\text{median angle}=1.145903\times10^{-4}\ ^\circ
]

[
p99=1.145998\times10^{-4}\ ^\circ
]

TSGS normal 来源又已经实际追踪为：

> `get_smallest_axis / get_normal_axis`

即：

> 激活后的最小 scale 轴，经 wxyz 四元数旋转得到 normal。

你的 covariance normal：

[
\Sigma=R\operatorname{diag}(s^2)R^T
]

取最小特征值对应特征向量。

这两者数学上本来就是同一个轴。

实际测量也只有：

[
0.0001146^\circ
]

差异。

这不是：

> `NORMAL_BUG`

这几乎就是**数值等价**。

TSGS 官方方法本身将几何学习与外观精修分阶段处理，并通过 first-surface depth（第一表面深度）进行透明表面几何推断；官方仓库也明确描述 Stage 2 固定 opacity、推理阶段使用 first-surface depth 做 mesh reconstruction（网格重建）。([arXiv][1])

---

# 二、L1 是一个“数值 Gate 设计问题”，不是 normal bug

我们预先写死：

[
median\le10^{-4}\ ^\circ
]

实际：

[
1.145903\times10^{-4}\ ^\circ
]

只超：

[
1.45903\times10^{-5}\ ^\circ
]

也就是：

[
0.0000146^\circ
]

更关键的是，在：

[
n_1\cdot n_2\rightarrow1
]

时使用：

[
\theta=\arccos(|n_1^Tn_2|)
]

数值条件非常差。

因为：

[
\frac{d\arccos x}{dx}
=====================

-\frac1{\sqrt{1-x^2}}
]

当：

[
x\rightarrow1
]

导数趋于无穷。

你现在：

[
0.0001146^\circ
\approx2.0\times10^{-6}\ {\rm rad}
]

对应：

[
1-\cos\theta
\approx2\times10^{-12}
]

也就是说：

> **两个 normal 的 dot product 只差 (10^{-12}) 量级，就会被 acos 放大成当前这个角度。**

所以现在正确做法不是：

> 降低 1e-4 threshold 救结果。

而是：

> **在近零夹角区域不要用 acos 作为 semantic equivalence（语义等价）主 Gate。**

应该使用 sign-invariant vector residual（符号无关向量残差）：

[
e_v
===

\min(
|n_a-n_b|_2,
|n_a+n_b|_2
)
]

或者 projector residual（投影矩阵残差）：

[
e_P
===

|n_an_a^T-n_bn_b^T|_F
]

因为 normal：

[
n
]

和：

[
-n
]

表示同一个轴。

**这里不是 threshold tuning（调阈值）。**

是原 Gate 在零角度附近使用了数值不稳定的量。

---

# 三、报告最大的内部矛盾其实在 L4

看你自己的结果。

H-N1：

> FAIL

即：

> Gaussian normal 不能作为材料表面法向。

这很合理：

[
median\ angle(n_{\rm Gaussian},n_{\rm depth})
=============================================

67.07^\circ
]

[
p90=86.73^\circ
]

说明：

[
\boxed{
\text{Gaussian 最小轴}
\neq
\text{first-surface 材料表面法向}
}
]

这是一个非常强的结果。

---

然后 H-N2：

> **SUPPORTED**

H-N2 的定义是：

> Gaussian normal 不可靠，但 first-surface depth normal（第一表面深度法向）是 coherent（局部一致）的。

而你的结果：

[
7019/7019
]

候选 Gaussian 都有至少 3 个 depth-normal views。

[
DEPTH_NORMAL_RELIABLE
=====================

7019/7019
]

即：

[
100%
]

深度法向局部：

[
p90\le10^\circ
]

的像素比例：

[
100%
]

然后 patch sweep：

```text
K=64   → 7019
K=128  → 7019
K=256  → 7019
K=512  → 7019
K=768  → 7019
```

也就是说，按照输出：

> **每一个 fresh candidate seed，在所有正式 patch scale 上都能形成 depth-normal coherent patch。**

结果 Final CASE 居然写：

> `NORMAL-SEMANTIC-BUG`

然后：

> `valid material-normal proxy: none`

这明显不对。

按照我们预定义的 H-N2 逻辑：

[
\boxed{
H\text{-}N2\ SUPPORTED
}
]

就意味着候选 proxy（代理法向）应该是：

> **multiview first-surface depth normal**
>
> 多视角第一表面深度法向。

TSGS 官方设计本来就专门通过 first-surface extraction（第一表面提取）从透明 alpha-blending 权重中寻找最可能的表面位置，而不是宣称每个 Gaussian 的最小 scale 轴天然就是独立材料点法向。([arXiv][1])

所以：

> **Gaussian covariance normal 和 TSGS normal 语义是一致的。**
>
> 但：
>
> **TSGS Gaussian normal 本身不是当前 checkpoint 中可靠的材料表面法向代理。**

这两个结论完全可以同时成立。

---

# 四、其实 R4 已经发现了真正的 bridge 答案

当前证据链是：

[
n_{\rm TSGS}
\approx
n_{\rm covariance}
]

误差约：

[
10^{-4}\ ^\circ
]

所以：

> normal implementation 没有明显 bug。

但是：

[
\angle(
n_{\rm Gaussian},
n_{\rm depth}
)
]

median：

[
67.1^\circ
]

说明：

> Gaussian primitive orientation（Gaussian 图元方向）和 reconstructed first surface orientation（重建第一表面方向）严重不一致。

与此同时：

[
100%
]

fresh candidates 获得 reliable multiview depth normals。

所以现在最合理的 material kinematics bridge（材料运动学桥）其实已经出来：

### Gaussian kernel 运动学

继续：

[
\Sigma'=F\Sigma F^T
]

Gaussian covariance 还是按照实际形变梯度传输。

### 材料面积运动学

计算：

[
J_s=
|\det F|
|F^{-T}n_{\rm material}|
]

但：

[
n_{\rm material}
]

不再取：

[
n_{\rm Gaussian}
]

而取：

[
\boxed{
n_{\rm material}
================

n_{\rm first-surface-depth}
}
]

即：

> 多视角 TSGS 第一表面深度法向。

这不是改 KIOT。

KIOT 依然：

[
\Phi(o')
========

\frac1{J_s}\Phi(o)
]

变的是：

> **(J_s) 中 material normal（材料法向）的来源。**

---

# 五、这件事其实有理论依据

PhysGaussian 把 Gaussian 与材料点运动学联系起来，利用 deformation gradient（形变梯度）更新 Gaussian kernel；这类物理解释依赖一个能表达材料运动学的离散载体。([CVF开放访问][2])

但 TSGS 的目标是透明表面重建。它使用 normal/de-lighting priors（法向和去光照先验）学习几何，并通过 first-surface depth extraction（第一表面深度提取）恢复透明表面；Stage 2 还固定 opacity 维护几何。([arXiv][1])

所以我的推断是：

> **把 TSGS 每个 Gaussian 的最小轴直接提升成“材料点法向”，是我们 bridge 的错误假设。**

这是推断，但你的数据非常支持：

[
67^\circ
]

median mismatch。

而 first-surface depth normal：

[
100%
]

局部 coherent。

---

# 六、不过我还不允许直接跑 KIOT vs opacity-linear

因为 R4 还有一个地方必须收口。

这个结果太完美了：

```text
depth-normal coherent patches

K64   7019
K128  7019
K256  7019
K512  7019
K768  7019
```

**每个 candidate、每个 K 都 coherent。**

我会警惕。

可能是真的，因为 first-surface depth 非常平滑。

也可能存在：

1. 每个 candidate 被错误赋成同一个 normal；
2. camera/world normal transform 漏了旋转；
3. multiview scatter 输出高度退化；
4. patch sweep 错误复用了 seed normal；
5. layer spread 根本没执行。

报告明确说：

> `layer spread not used`

所以目前还差：

> **depth-normal non-degeneracy（深度法向非退化性）**
>
> *
>
> **single-layer purity（单一表面层纯度）**

两个小 Gate。

不用再做大审计。

---

# 下一步：Stage 3.5B-R4A

名称：

> **Depth-Normal Bridge Closure**

中文：

> **第一表面深度法向桥接闭环**

这是一个很小的收口。

通过后直接重跑 KIOT vs opacity-linear Kill Gate。

```bash
cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式状态修正
============================================================

Stage3.5B-R4 reported：

L0 PASS

L1 FAIL

L2 PASS

L3 PASS

L4 NORMAL-SEMANTIC-BUG。

This Final CASE is NOT ACCEPTED。

Reason：

the reported data do NOT support
a normal implementation bug。

============================================================
关键事实 1：
TSGS NORMAL 与 COVARIANCE NORMAL 基本等价
============================================================

Actual TSGS normal source：

GaussianModel.get_smallest_axis /
get_normal_axis。

It uses：

minimum activated scale axis

after quaternion_to_matrix。

Quaternion convention：

wxyz。

Measured unsigned angle：

median =
1.145903e-04 deg

p99 =
1.145998e-04 deg。

The predefined angular median Gate：

1e-4 deg

was exceeded only by：

1.45903e-05 deg。

Near dot(n1,n2)=1，
acos is numerically ill-conditioned。

Therefore：

L1 angular FAIL
does NOT establish NORMAL_BUG。

Do NOT change the angular threshold
to force PASS。

Instead audit semantic equivalence
using numerically stable
sign-invariant axis metrics。

============================================================
关键事实 2：
GAUSSIAN NORMAL 与 DEPTH NORMAL 不一致
============================================================

Gaussian vs multiview depth normal：

median angle =
67.065703 deg

p90 =
86.728436 deg。

H-N1：

FAIL。

Thus：

TSGS Gaussian minimum-scale axis
is NOT currently supported
as the material-surface normal proxy
for this checkpoint。

============================================================
关键事实 3：
DEPTH NORMAL BRIDGE EVIDENCE 很强
============================================================

Fresh depth support：

STRICT =
5523

MEDIUM =
7019

LOOSE =
7170。

Old5972 vs fresh MEDIUM Jaccard：

0.849779。

L2：

PASS。

Depth-normal local p90<=10deg fraction：

1.000000。

Fresh MEDIUM candidates with
>=3 depth-normal views：

7019。

DEPTH_NORMAL_RELIABLE：

7019 /7019
=
1.000000。

H-N2：

SUPPORTED。

H-N3：

FAIL。

Depth-normal patch sweep reported：

K64:
7019 coherent seeds

K128:
7019

K256:
7019

K512:
7019

K768:
7019。

Therefore：

the report's statement：

"valid material-normal proxy: none"

contradicts H-N2 and the depth-normal
coherence results。

However：

the perfect 7019/7019 coherence
at every K requires a
NON-DEGENERACY AUDIT。

Layer purity was also not completed。

============================================================
本阶段名称
============================================================

Stage 3.5B-R4A:

Depth-Normal Bridge Closure

中文：

第一表面深度法向桥接闭环

============================================================
一、唯一目标
============================================================

Answer：

Q1.

Are TSGS normal and covariance normal
semantically equivalent despite
the unstable near-zero acos Gate？

Q2.

Are the multiview first-surface depth normals
non-degenerate and spatially varying？

Q3.

Why did all 7019 seeds pass
depth-normal coherence
for every K>=64？

Q4.

Do at least two spatially separated
depth-normal coherent patches
also satisfy first-surface layer purity？

If YES：

formal material-normal proxy =

MULTIVIEW FIRST-SURFACE DEPTH NORMAL。

Then allow the KIOT vs opacity-linear
real-carrier Kill Gate。

Do NOT run optical policies here。

============================================================
二、禁止事项
============================================================

禁止：

KIOT render

opacity-linear render

policy comparison

new optical rule

change KIOT

change Phi

MLP

training

normal threshold tuning

manual patch selection

fallback selection。

============================================================
三、输出目录
============================================================

/data/wyh/DeformTransGS/experiments/
stage3_5B_R4A_depth_normal_bridge_closure/

============================================================
四、Protocol Lock
============================================================

Lock Stage3.5B-R4：

normal semantic trace

fresh depth support samples

fresh MEDIUM candidate lock

depth normal maps

depth normal local reliability

candidate multiview depth normals

Gaussian-vs-depth normal table

Gaussian-normal sweep

depth-normal sweep。

Output：

r4a_protocol_lock.json。

Gate M0。

============================================================
五、Stable Axis Equivalence Metric
============================================================

For ALL 991832 Gaussians：

n_tsgs

n_cov。

Use float64。

Normalize both explicitly。

Compute：

sign_invariant_vector_error =

min(
    ||n_tsgs-n_cov||_2,
    ||n_tsgs+n_cov||_2
)。

Also compute projector error：

P_tsgs =
n_tsgs n_tsgs^T

P_cov =
n_cov n_cov^T

projector_fro_error =

||P_tsgs-P_cov||_F。

Output：

stable_normal_semantic_equivalence.csv。

Report：

vector error：
median
p90
p99
max

projector error：
median
p90
p99
max。

Also report float64 dot：

1-abs(dot)

median
p99
max。

Formal semantic equivalence PASS iff：

vector p99 <=1e-5

AND

projector p99 <=2e-5。

These thresholds are NOT a replacement
for the previous angular Gate。

They test direct axis equality
at the implementation precision scale。

Record the old acos Gate as：

NUMERICALLY ILL-CONDITIONED NEAR ZERO。

Gate M1。

If M1 FAIL：

NORMAL IMPLEMENTATION ISSUE。

STOP。

============================================================
六、Depth-Normal Diversity Audit
============================================================

Load all valid multiview depth normals。

Report component distributions：

nx
ny
nz

mean
std
p01
p25
median
p75
p99。

Compute global sign-invariant
normal scatter eigenvalues：

lambda1>=lambda2>=lambda3。

Normalize eigenvalues by sum。

Output：

depth_normal_diversity.csv。

Also compute pairwise unsigned angles
for deterministic 100000 random
candidate-normal pairs。

seed20260713。

Report：

median
p10
p90
p95。

If：

global scatter rank approximately1

or pairwise p90 <=1 deg，

mark：

DEPTH-NORMAL-GLOBAL-DEGENERACY。

Do NOT continue。

============================================================
七、Camera Transform Audit
============================================================

For 1000 deterministic
depth-normal pixels per audit camera：

record：

camera-space reconstructed normal

world-space transformed normal

camera rotation matrix。

Output：

depth_normal_camera_world_trace.csv。

Verify：

world normal =
R_c2w @ camera_normal

or the exact current convention。

Then for matched nearby surface locations
seen in >=3 cameras：

compare world-space normals
sign-invariantly。

Output：

cross_view_world_normal_consistency.csv。

Report median/p90 angle。

Gate：

p90 <=15 deg
for the reliable-depth-normal set。

Gate M2。

============================================================
八、Trace Perfect 7019-Coherence Result
============================================================

Locate exact Stage R4 code that generated：

K64/K128/K256/K512/K768
all =7019。

Output：

perfect_coherence_source_trace.md。

Must show：

source path

function

line range

normal array used

KNN indices

patch scatter computation

p90 computation

eligible counter。

Explicitly test for bugs：

seed normal broadcast

one normal reused for patch

global n_depth_mv reused

KNN indices ignored

candidate IDs shifted

angles computed against each own normal
instead of patch reference

degrees/radians mismatch。

Create a 10-seed manual numeric trace：

for each seed and K64/K768：

first 10 neighbour IDs

first 10 normals

normal unique rows

patch scatter eigenvalues

patch reference normal

angle p50/p90/p95。

Output：

depth_patch_manual_trace.csv。

Gate M3：

PASS iff no implementation bug。

============================================================
九、Recompute Depth-Normal Patch Sweep Independently
============================================================

Create a NEW independent implementation：

analysis/independent_depth_normal_patch_sweep.py

Do not import the old sweep function。

Input：

fresh MEDIUM candidates
with DEPTH_NORMAL_RELIABLE。

Use xyz float64。

Use n_depth_mv float64。

K：

64
128
256
512
768。

For each seed：

exact KNN。

Patch normal：

principal eigenvector of

sum n_i n_i^T。

Angles：

acos(
clip(
abs(n_i dot n_patch),
0,
1
)
)

in degrees。

Compute p90。

Output：

independent_depth_normal_patch_sweep.csv。

Compare old vs independent
for matched seed/K：

p90 median abs diff

p95

max。

Output：

old_vs_independent_depth_sweep.csv。

Require：

max p90 diff <=1e-6 deg。

Gate M4。

============================================================
十、Freeze Coherent Depth-Normal Patches
============================================================

Eligible：

K>=64

depth-normal p90 <=10 deg

visible official-mask cameras >=3。

For every seed：

choose LARGEST passing K。

Rank：

larger K

lower p90

higher mask support

smaller radius。

Select A/B/C spatially separated：

center distance >=
2*max(radius_i,radius_j)。

No fallback。

Require >=2。

Save：

depth_normal_patch_manifest.csv

depth_normal_patch_indices/*.npy。

Patch selection occurs BEFORE
layer-purity result classification
and before any optical policy。

============================================================
十一、First-Surface Layer Purity
============================================================

For every frozen patch
and official audit camera：

use fresh_depth_support_samples.csv。

For patch Gaussian projections
that are：

inside transparent mask

and have valid d_first：

delta_d =
d_gaussian-d_first。

Do NOT use relative absolute error only。

Compute：

median delta_d

MAD

p05
p25
p75
p95。

Layer spread：

(
p95-p05
)
/
(
median(abs(d_first))+1e-8
)。

Also count multimodality proxy：

fraction delta_d < -0.05*|d_first|

fraction abs(delta_d)<=0.05*|d_first|

fraction delta_d > +0.05*|d_first|。

Output：

depth_normal_patch_layer_purity.csv。

Per-patch summary：

median camera layer_spread。

Formal layer-pure patch：

median camera layer_spread <=0.05

AND

median camera center-band fraction >=0.75。

Do not alter thresholds。

============================================================
十二、Final Surface-Layer Gate
============================================================

MATERIAL NORMAL PROXY
= FIRST-SURFACE DEPTH NORMAL

SUPPORTED iff：

M0 PASS

M1 PASS

M2 PASS

M3 PASS

M4 PASS

>=2 spatially separated
depth-normal coherent patches

AND

>=2 frozen patches are layer-pure。

Gate M5。

============================================================
十三、Final CASE
============================================================

CASE DEPTH-NORMAL-BRIDGE-CARRIER:

M0-M5 PASS。

Conclusion：

TSGS Gaussian minimum-scale normals
are semantically implemented correctly
and numerically equivalent
to covariance minimum axes。

However，
they are not valid material-surface normals
for the current transparent reconstruction。

The reconstructed first-surface geometry
provides a coherent multiview surface-normal proxy。

Formal material normal for deformation bridge：

n_material =
multiview first-surface depth normal。

Gaussian covariance still transports：

Sigma' =
F Sigma F^T。

Js is computed using：

Js =
|detF|
||F^-T n_material||。

ALLOW：

one final real-render
KIOT vs opacity-linear Kill Gate
using depth normals。

------------------------------------------------------------

CASE DEPTH-NORMAL-DEGENERATE:

M2/M3/M4 FAIL。

The apparent 100% depth-normal coherence
was implementation/degeneracy artifact。

STOP TSGS bridge。

------------------------------------------------------------

CASE NO-LAYER-PURE-PATCH:

normal Gates PASS
but fewer than2 layer-pure patches。

STOP TSGS bridge。

------------------------------------------------------------

CASE NORMAL-IMPLEMENTATION-ISSUE:

M1 FAIL。

============================================================
十四、Required Outputs
============================================================

r4a_protocol_lock.json

stable_normal_semantic_equivalence.csv
depth_normal_diversity.csv

depth_normal_camera_world_trace.csv
cross_view_world_normal_consistency.csv

perfect_coherence_source_trace.md
depth_patch_manual_trace.csv

independent_depth_normal_patch_sweep.csv
old_vs_independent_depth_sweep.csv

depth_normal_patch_manifest.csv
depth_normal_patch_indices/*.npy

depth_normal_patch_layer_purity.csv

depth_normal_bridge_closure_report.md
stage3_5B_R4A_summary.md
stage3_5B_R4A_log.txt

============================================================
十五、最终报告必须中文回答
============================================================

A. 为什么 R4 NORMAL-SEMANTIC-BUG
不能直接接受。

B. old acos median/p99。

C. sign-invariant vector p99/max。

D. projector p99/max。

E. 1-|dot| p99/max。

F. M1。

G. depth-normal component std。

H. global scatter normalized eigenvalues。

I. random pair unsigned-angle median/p90。

J. depth-normal globally degenerate yes/no。

K. camera→world transform exact semantic。

L. cross-view world-normal median/p90。

M. M2。

N. 7019 perfect coherence exact source。

O. broadcast/reuse/index/radian bug yes/no。

P. M3。

Q. independent sweep coherent counts by K。

R. old-vs-independent max p90 diff。

S. M4。

T. frozen depth-normal patches count。

U. patch sizes。

V. patch depth-normal p90。

W. layer spread per patch。

X. center-band fraction per patch。

Y. layer-pure patch count。

Z. M5。

AA. Final CASE。

AB. valid material-normal proxy。

AC. allow final KIOT vs opacity-linear Kill Gate yes/no。

AD. allow deformed-GT yes/no。

AE. allow multi-scene yes/no。

============================================================
十六、README
============================================================

更新 README：

Stage3.5B-R4 did not establish
a normal implementation bug。

The actual TSGS normal path
and covariance minimum-eigenvector path
agree to approximately1.15e-4 degrees，
while the old angular Gate used acos
in a numerically ill-conditioned
near-parallel regime。

At the same time，
TSGS Gaussian normals differ strongly
from multiview first-surface depth normals
(median67.1 degrees，
p9086.7 degrees)。

Fresh first-surface support rebuilt
5523/7019/7170 strict/medium/loose candidates，
confirming the earlier depth-support bug。

All7019 MEDIUM candidates obtained
reliable multiview depth normals，
and the previous sweep reported
strong depth-normal coherence
at all tested scales。

Stage3.5B-R4A therefore performs
a numerically stable TSGS-normal equivalence test，
audits depth-normal diversity and camera transforms，
independently reproduces the depth-normal patch sweep，
and tests first-surface layer purity。

No optical policy is evaluated。

============================================================
十七、实际执行
============================================================

必须实际：

lock R4

stable TSGS-vs-cov axis equivalence

depth-normal diversity audit

camera/world normal transform audit

cross-view normal consistency

trace perfect 7019 sweep

manual 10-seed trace

independent patch sweep

compare old/new sweep

freeze spatially separated
depth-normal coherent patches

layer-purity audit

M0-M5

Final CASE

绝对不要：

run KIOT

run opacity-linear

change normal thresholds

use acos Gate alone
for near-zero semantic equivalence

fallback patch selection。
PROMPT
```

# 我的真实评价

**现在别因为 `CASE NORMAL-SEMANTIC-BUG` 又悲观。**

我反而认为 R4 给出的最重要结果是：

[
\boxed{
n_{\rm TSGS}\approx n_{\rm covariance}
}
]

但是：

[
\boxed{
n_{\rm Gaussian}\not\approx n_{\rm surface}
}
]

以及：

[
\boxed{
n_{\rm first-surface-depth}
\text{高度一致}
}
]

这意味着前面的问题很可能不是 TSGS “没有表面”。

而是我们犯了一个 bridge 假设错误：

> **把 splat kernel orientation（泼溅核方向）当成 material surface orientation（材料表面方向）。**

TSGS 官方设计本身更强调通过 first-surface depth 提取透明表面几何，而 Stage 2 固定 opacity 来保护这一几何。([arXiv][1])

所以，R4A 只要确认深度法向不是代码广播出来的假平滑，并且有至少两个 layer-pure（单层纯净）patch：

> **下一步就用 depth normal 算 (J_s)，真正重跑 KIOT vs opacity-linear。**

而且这次我会把它当**最后一次 TSGS bridge 生死实验**。

KIOT 输，就杀 real-carrier claim。

KIOT 赢，就正式进入 deformed-GT。

[1]: https://arxiv.org/abs/2504.12799?utm_source=chatgpt.com "TSGS: Improving Gaussian Splatting for Transparent Surface Reconstruction via Normal and De-lighting Priors"
[2]: https://openaccess.thecvf.com/content/CVPR2025/papers/Shen_EnliveningGS_Active_Locomotion_of_3DGS_CVPR_2025_paper.pdf?utm_source=chatgpt.com "EnliveningGS: Active Locomotion of 3DGS - CVF Open Access"
