cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前正式研究状态
==================================================

Stage 3.3.3 发现严重 metric implementation bug。

真实根因：

当前所谓：

R_local per material point

实际上没有进行：

per-material-point bilinear sampling。

代码使用：

mask-level median tau_eff ratio

并将同一个响应值
用于 mask 内全部 material points。

直接证据：

对于所有 cubic states：

R_unique = 1

因此：

Spearman NaN。

根据当前审计：

Stage 3.3.1 至 Stage 3.3.3
所有依赖 point-level R_local 的结论
必须撤回或标记 invalid。

包括：

cubic point-level MAE

center / edge response lag

neighbourhood target comparison

H_REL correlation

coarse matched-Js 7-9x

same-point matched-Js Delta_E

M1 / M2 / M3 decisions

CASE B / B1 / B2。

当前正式状态：

LOCAL METRIC PIPELINE INVALID

禁止写：

CASE B2 confirmed。

禁止写：

heterogeneity effect proven。

禁止写：

response lag due to footprint mixing。

==================================================
仍然保留的证据
==================================================

只保留 global / camera-level evidence。

Uniform deformation Gold/global response：

stretch_1.25:
R approximately 0.787
target 0.800

stretch_1.50:
R approximately 0.637
target 0.667

stretch_2.00:
R approximately 0.450
target 0.500

Area-preserving nonlinear shear global response：

shear_k020:
R approximately 0.998

shear_k040:
R approximately 0.993

允许结论：

The controlled Gaussian sheet exhibits
approximately area-dilution-consistent
GLOBAL optical response
under uniform area deformation。

The tested area-preserving shear controls
show negligible GLOBAL optical response change。

禁止称：

point-local consistency。

==================================================
本阶段名称
==================================================

Stage 3.3.R:

Per-Material-Point Optical Response Pipeline Repair

中文：

材料点级光学响应指标管线修复

==================================================
一、唯一目标
==================================================

真正实现：

same material point
before / after deformation

的 point-level optical response。

正式定义：

for Gaussian material identity i
and camera c：

canonical xyz_i
→ canonical projected coordinate p_can_i,c

deformed xyz_i
→ deformed projected coordinate p_def_i,c

从真实 white-pass alpha maps：

A_can_c
A_def_state,c

分别 bilinear sample：

A_can_i,c =
sample(A_can_c, p_can_i,c)

A_def_i,c =
sample(A_def_c, p_def_i,c)

tau_can_i,c =
-log(
clamp(1-A_can_i,c, 1e-6, 1)
)

tau_def_i,c =
-log(
clamp(1-A_def_i,c, 1e-6, 1)
)

r_i,c =
tau_def_i,c
/
(tau_can_i,c + 1e-8)

对于 material identity i：

至少 2 valid cameras：

R_local_i =
median_c(r_i,c)

本阶段只修复和重新验证 metric。

禁止研究 mechanism。

==================================================
二、项目边界
==================================================

所有新增输出：

/data/wyh/DeformTransGS/experiments/
stage3_3R_point_metric_repair/

禁止修改：

/data/wyh/repos/TSGS

/data/wyh/RecycleGS

/data/wyh/repos/RT-Splatting

禁止：

训练

新 deformation

tau update

optical correction

screen-space heterogeneity mechanism

neighbourhood mechanism conclusion

==================================================
三、输入完整性
==================================================

定位现有 frozen carrier。

定位现有：

canonical white-pass alpha maps

stretch_1.25 alpha maps

stretch_1.50 alpha maps

stretch_2.00 alpha maps

biaxial_1.50 alpha maps

cubic_l010 alpha maps

cubic_l020 alpha maps

cubic_l0333 alpha maps

shear_k020 alpha maps

shear_k040 alpha maps

twist_60 alpha maps

formal cameras：

cam_000
cam_004
cam_008

canonical xyz

per-state deformed xyz

per-state Js。

对所有 alpha maps：

记录：

absolute path
shape
dtype
min
max
mean
SHA256

确认 alpha map
来自 validated Stage 3.2.1 white pass。

如果缺失：

允许使用完全相同 frozen carrier
重新 render 缺失 map。

禁止重新训练。

输出：

point_metric_input_audit.md

==================================================
四、审计旧 Bug
==================================================

找到当前生成：

material_point_response.csv

R_local

r_cam000
r_cam004
r_cam008

的真实代码。

记录：

script path
function
line / relevant code block。

必须证明：

旧实现在哪一步使用：

mask median

region median

camera-level median

并广播为：

per-point R。

输出：

old_point_metric_bug_report.md

必须明确回答：

为什么 R_unique=1。

==================================================
五、实现独立 Bilinear Sampler
==================================================

新建：

analysis/per_material_point_response.py

建立独立 NumPy bilinear sampler。

输入：

image H x W

floating pixel coordinate：

x
y

注意：

x = horizontal / column
y = vertical / row

定义：

x0=floor(x)
x1=x0+1
y0=floor(y)
y1=y0+1

按标准双线性插值。

禁止使用 mask median。

禁止 nearest neighbour。

==================================================
六、Bilinear Sampler Unit Tests
==================================================

建立 synthetic image：

I[y,x] =
3*x + 5*y + 7

因为这是线性平面，

bilinear interpolation
在任意内部 floating coordinate
应精确恢复：

3*x + 5*y + 7。

随机：

1000 coordinates

要求：

max absolute error <1e-10
使用 float64。

测试 integer coordinates：

sample(I,x_int,y_int)
必须等于：

I[y_int,x_int]

max error = 0。

测试：

x/y ordering swap diagnostic。

故意交换 x/y 后：

必须产生明显 error，
证明测试能发现 coordinate swap。

输出：

bilinear_sampler_unit_test.md

如果 FAIL：

停止。

==================================================
七、Camera Projection Validation
==================================================

禁止重新猜相机 convention。

严格使用：

Stage 3.2.1 validated official Camera convention。

对于：

cam_000
cam_004
cam_008

canonical Gaussian xyz：

投影到 pixel coordinates。

验证：

projected sheet bbox
与 white-pass alpha foreground bbox
空间重叠。

记录：

projected point bbox：

xmin
xmax
ymin
ymax

alpha foreground bbox。

计算：

bbox IoU diagnostic

以及：

projected points falling inside alpha foreground fraction。

要求：

inside foreground / 3-pixel dilation
fraction >=0.95。

输出：

projection_validation.csv

如果任一 camera <0.95：

停止。

说明 projection/pixel convention 有问题。

==================================================
八、Direct Pixel Spot Check
==================================================

每 camera 随机选择：

20 material points

要求其 projection：

距离 integer pixel center <0.05 pixels。

对于这些点：

bilinear sample alpha

vs

direct nearest integer alpha value。

计算：

MAE
max difference。

因为坐标接近整数：

要求：

MAE <1e-3

优先 max <5e-3。

输出：

direct_pixel_spotcheck.csv

==================================================
九、Boundary / Valid Point Protocol
==================================================

正式 valid criteria：

canonical positive camera depth

deformed positive camera depth

canonical projected coordinate：

x in [0,W-1)
y in [0,H-1)

deformed projected coordinate：

same

使用 deformed projected sheet binary mask。

boundary distance：

distance_transform_edt(mask)

在：

round / nearest pixel of p_def

要求：

distance >=8 pixels。

canonical tau_eff：

>1e-6

all finite。

重要：

boundary mask
只用于 valid-point filtering。

禁止使用 mask 内 median
定义 point response。

==================================================
十、正式 Point-Level Response
==================================================

states：

stretch_1.25
stretch_1.50
stretch_2.00

biaxial_1.50

cubic_l010
cubic_l020
cubic_l0333

shear_k020
shear_k040

twist_60

对每：

state
camera
material Gaussian identity i

保存：

gaussian_index

grid_u_index
grid_v_index

u
v

Js
q_self=1/Js

camera_id

p_can_x
p_can_y

p_def_x
p_def_y

A_can

A_def

tau_eff_can

tau_eff_def

r_camera

valid

invalid_reason

输出：

per_camera_material_response.csv

==================================================
十一、Cross-Camera Aggregation
==================================================

对于每：

state
gaussian_index

仅使用 valid cameras。

要求：

valid camera count >=2。

定义：

R_local =
median(r_camera)

同时记录：

R_mean_camera

R_min_camera
R_max_camera
R_camera_std

输出：

material_point_response_repaired.csv

==================================================
十二、Point-Resolution Validation Gate
==================================================

对每 state：

报告：

valid material point count

R finite count

R unique count
rounded 1e-8

R std

R min
median
max

要求：

cubic_l010
cubic_l020
cubic_l0333

全部：

R_unique_count >=50

且：

R_std >1e-4。

如果不满足：

PIPELINE FAIL。

禁止继续科学分析。

注意：

uniform deformation
也报告 R variation，
但不设置 unique threshold。

输出：

point_resolution_validation.csv

==================================================
十三、Legacy vs Repaired Comparison
==================================================

对于每 state：

旧：

legacy mask-median R

新：

repaired R_local median

比较：

legacy global R

median repaired R

mean repaired R

repaired R std

MAE between：

constant legacy R

and repaired per-point R。

输出：

legacy_repaired_metric_comparison.csv

明确回答：

旧 metric 是否只捕捉 global response。

==================================================
十四、重新计算 Uniform Point-Level Baseline
==================================================

仅 pipeline PASS 后执行。

对于：

stretch_1.25
stretch_1.50
stretch_2.00

计算：

MAE_self =
mean |R_local-1/Js|

RMSE

median abs error

p90
p95

定义：

E_uniform =
mean three MAE。

输出：

uniform_pointwise_baseline_repaired.csv

==================================================
十五、重新计算 Cubic Point-Level Metrics
==================================================

对于：

cubic_l010
cubic_l020
cubic_l0333

使用：

R_local repaired

q_self=1/Js

计算：

MAE
RMSE
median error
p90
p95

Pearson

Spearman：

r =
np.asarray(R_local,dtype=float64).reshape(-1)

q =
np.asarray(q_self,dtype=float64).reshape(-1)

finite filter

scipy.stats.spearmanr(
r,
q,
nan_policy="raise"
)

记录：

R unique
q unique
R std
q std。

输出：

cubic_pointwise_metrics_repaired.csv

==================================================
十六、重新计算 Spatial Bins
==================================================

cubic states：

abs(u) bins：

[0,.2)
[.2,.4)
[.4,.6)
[.6,.8)
[.8,1.0+eps]

每 bin：

count

median Js
median q

median R_local

mean R_local

MAE_self

bias =
median R - median q

输出：

spatial_bins_repaired.csv

重点：

cubic_l0333 center
和 edge。

==================================================
十七、重新计算 Area-Preserving Controls
==================================================

shear_k020
shear_k040

target=1。

使用 repaired R_local：

MAE
RMSE
median abs error
p90
p95
max

输出：

shear_pointwise_controls_repaired.csv

==================================================
十八、Twist Recheck
==================================================

twist_60：

使用真实 Js。

计算：

Js min
max
mean
std

point MAE vs 1/Js

Spearman if nonconstant

camera response std。

输出：

twist_pointwise_repaired.csv

==================================================
十九、Same-Point Same-Camera Matched-Js Recheck
==================================================

只有 repaired
per-camera r_camera
允许用于该实验。

Uniform states：

canonical q=1
R=1

stretch_1.25 q=0.8

stretch_1.50 q=0.6666667

stretch_2.00 q=0.5

对于每：

material identity i
camera c

建立自己的：

q → r_uniform(i,c,q)

piecewise linear interpolation。

禁止：

cross-point interpolation

cross-camera interpolation

mask median。

对于 cubic sample：

same i
same c

q_cubic

得到：

r_uniform_expected。

定义：

E_expected =
abs(r_uniform_expected-q_cubic)

E_cubic =
abs(r_cubic-q_cubic)

Delta_E =
E_cubic-E_expected。

输出：

matched_js_repaired.csv

==================================================
二十、Matched-Js Statistical Gate
==================================================

对每 cubic state：

只使用 valid matched samples。

报告：

sample count

Delta_E median
mean
p90

positive fraction。

paired permutation test：

H0：

median / mean paired Delta_E
无系统正偏。

使用至少：

10000 permutations

固定 random seed。

同时：

Wilcoxon signed-rank
作为 secondary diagnostic。

输出：

matched_js_statistics_repaired.csv

M2 MATCHED-JS EXTRA ERROR：

cubic_l0333 必须同时：

median Delta_E >=0.05

positive fraction >=0.75

permutation p <0.01。

禁止缺任何一项。

==================================================
二十一、重新执行 Local Phenomenon Gate
==================================================

此阶段不研究 footprint mechanism。

只判断现象。

P0:
POINT METRIC PIPELINE

PASS：

bilinear unit test PASS

projection PASS

point resolution PASS。

--------------------------------------------------

P1:
UNIFORM LOCAL CONSISTENCY

SUPPORTED：

E_uniform <=0.075。

--------------------------------------------------

P2:
NONUNIFORM LOCAL BREAK

SUPPORTED：

uniform P1 supported

且 cubic_l0333：

MAE >0.15

或：

Spearman <0.7。

--------------------------------------------------

P3:
MATCHED-JS EXTRA ERROR

严格使用 M2 Gate：

SUPPORTED / NOT SUPPORTED。

--------------------------------------------------

P4:
AREA-PRESERVING CONTROL

SUPPORTED：

shear k020 MAE <=0.10

shear k040 MAE <=0.10。

==================================================
二十二、研究决策
==================================================

CASE LOCAL-A:

P0 PASS
P1 PASS

所有 cubic：

Spearman >=0.9

cubic_l0333 MAE <=0.10

E_nonuniform <=2*E_uniform

P4 PASS。

结论：

point-local consistency supported。

停止 optical-correction line。

--------------------------------------------------

CASE LOCAL-B:

P0 PASS
P1 PASS
P2 PASS
P3 PASS
P4 PASS。

结论：

At matched local Js,
spatially varying deformation produces
significant extra point-level optical error。

中文：

在控制材料点局部 Js 后，
空间变化形变仍产生显著额外点级光学误差。

此时：

现象正式成立。

下一阶段才允许进行
screen-space footprint mechanism audit。

仍禁止设计 method。

--------------------------------------------------

CASE LOCAL-C:

P0 PASS
P1 PASS
P2 PASS
P3 NOT SUPPORTED
P4 PASS。

结论：

non-uniform error increases，
但 matched-Js evidence 不成立。

需要重新解释。

--------------------------------------------------

CASE CONTROL-FAIL:

P4 FAIL。

停止 area-dilution line。

--------------------------------------------------

CASE METRIC-FAIL:

P0 FAIL。

继续修 metric。

禁止任何机制结论。

==================================================
二十三、报告
==================================================

输出：

point_metric_input_audit.md

old_point_metric_bug_report.md

bilinear_sampler_unit_test.md

projection_validation.csv
direct_pixel_spotcheck.csv

per_camera_material_response.csv
material_point_response_repaired.csv

point_resolution_validation.csv
legacy_repaired_metric_comparison.csv

uniform_pointwise_baseline_repaired.csv
cubic_pointwise_metrics_repaired.csv
spatial_bins_repaired.csv

shear_pointwise_controls_repaired.csv
twist_pointwise_repaired.csv

matched_js_repaired.csv
matched_js_statistics_repaired.csv

point_metric_repair_report.md
stage3_3R_summary.md
stage3_3R_log.txt

==================================================
二十四、point_metric_repair_report.md
==================================================

必须中文回答：

A. 旧 R_unique=1 的真实代码原因。

B. 旧 point metric 是否使用 mask median。

C. bilinear sampler unit test。

D. projection validation。

E. cubic l010 repaired R unique / std。

F. cubic l020 repaired R unique / std。

G. cubic l0333 repaired R unique / std。

H. legacy vs repaired difference。

I. repaired E_uniform。

J. cubic l010 MAE / Spearman。

K. cubic l020 MAE / Spearman。

L. cubic l0333 MAE / Spearman。

M. l0333 center R / target。

N. l0333 edge R / target。

O. shear k020 point MAE。

P. shear k040 point MAE。

Q. twist point MAE。

R. l0333 matched Delta_E median。

S. positive fraction。

T. permutation p。

U. P0。

V. P1。

W. P2。

X. P3。

Y. P4。

Z. Final CASE。

AA. Stage 3.3.1/3.3.2/3.3.3
哪些结论 invalid。

AB. 当前真实科学问题。

AC. 是否允许进入 screen-footprint mechanism audit。

只有：

CASE LOCAL-B

回答：

是。

==================================================
二十五、README
==================================================

更新：

/data/wyh/DeformTransGS/README.md

明确记录：

Stage 3.3.3 identified that the previous
per-material-point optical response
was not actually sampled per material point。

The implementation broadcast a mask-level median response,
causing R_unique=1 and invalidating point-level correlation,
local-error, neighbourhood, and matched-Js analyses。

Stage 3.3.R repairs
same-material-point before/after bilinear sampling
before any further mechanism claim。

==================================================
二十六、实际执行
==================================================

必须实际：

audit old code

implement independent bilinear sampler

unit tests

projection validation

direct pixel spot checks

real per-material-point sampling

cross-camera aggregation

point resolution validation

uniform re-evaluation

cubic re-evaluation

spatial bin re-evaluation

shear re-evaluation

twist re-evaluation

same-point same-camera matched-Js

paired permutation test

P0-P4

Final CASE

不要分析 screen kernel。

不要计算 H_REL。

不要设计 method。

不要新增 deformation。

不要训练。

不要修改 TSGS。

==================================================
二十七、最终终端总结
==================================================

最后输出：

1. Old R_unique=1 root cause
2. Bilinear sampler PASS/FAIL
3. Projection validation PASS/FAIL
4. cubic l010 R unique/std
5. cubic l020 R unique/std
6. cubic l0333 R unique/std
7. E_uniform repaired
8. cubic l010 MAE/Spearman
9. cubic l020 MAE/Spearman
10. cubic l0333 MAE/Spearman
11. center R/target
12. edge R/target
13. shear k020 MAE
14. shear k040 MAE
15. twist point MAE
16. matched Delta_E median
17. positive fraction
18. permutation p
19. P0
20. P1
21. P2
22. P3
23. P4
24. Final CASE
25. Can enter screen-footprint mechanism audit yes/no
26. point_metric_repair_report.md path
27. stage3_3R_summary.md path

请直接实际运行和验证。

不要训练。
不要新增形变。
不要研究机制。
不要设计 method。
不要修改 TSGS。
PROMPT