这次结果**不能说明新方向失败**。

因为 E0-A 实际上只完成了来源审计：

```text
E0A-G0: FAIL
E0A-G1: PASS
forward replay rows: 0
Jacobian: NOT_EXECUTED
optimization: NOT_EXECUTED
```

也就是说：

> **科学实验根本没有开始。**

`PROVENANCE-FAIL` 在这里是“换服务器导致历史证据不完整”，不是“非刚体形变没有增量价值”。

## 当前状态应当修正为

```text
E0-A scientific hypothesis:
UNTESTED

E0-A execution status:
BLOCKED-BY-SERVER-MIGRATION

new primary line:
PAUSED

Stage E1:
NOT ALLOWED YET

Return to RecycleGS:
NOT REQUIRED YET
```

其中 `G1 PASS` 说明核心代码和方程大概率还在；缺失的主要是 D0/C2 的数据文件、锁文件或历史报告。下一步只恢复来源，不修改实验协议，也不重新设计形变。

把下面指令交给 Codex：

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续维护科研项目：

/data/wyh/DeformTransGS

============================================================
Stage E0-A-R0
Server-Migration Provenance Restoration
============================================================

中文：

换服务器后的实验来源恢复与路径重建闭环

============================================================
当前问题
============================================================

Stage E0-A previously returned：

CASE E0A-PROVENANCE-FAIL。

However：

forward replay rows = 0

material samples selected = 0

Jacobian = NOT_EXECUTED

optimization = NOT_EXECUTED。

The failure occurred before any scientific
identifiability experiment was executed。

The user has changed servers，
and some historical files are missing。

Therefore the current classification is：

E0A-BLOCKED-BY-SERVER-MIGRATION。

Do NOT interpret it as：

nonrigid deformation has no benefit。

Do NOT return to RecycleGS yet。

Do NOT modify the E0-A scientific protocol。

============================================================
本阶段唯一目标
============================================================

Restore or deterministically rehydrate
the minimum exact evidence required
to execute E0-A。

This stage must answer：

1. Which exact files are missing？

2. Which missing files already exist
   elsewhere on the new server？

3. Which files have an expected SHA256？

4. Which files can be deterministically
   regenerated from exact locked source？

5. Which files require copying
   from the previous server？

6. Can E0A-G0 and E0A-G2 be restored
   without changing the benchmark？

Do not run Jacobian analysis。

Do not run optimization。

Do not train any Gaussian model。

============================================================
现有失败输出
============================================================

Failed E0-A root：

/data/wyh/DeformTransGS/experiments/
stageE0_A_nonrigid_incremental_identifiability

Read：

stageE0A_report.md

stageE0A_summary.md

stageE0A_log.txt

E0A_protocol_lock.json
if present。

Trace the exact reason for E0A-G0 failure。

Do not rely only on the terminal summary。

============================================================
需要恢复的证据类别
============================================================

Category C0：

C2-V2 benchmark evidence。

Expected historical root：

/data/wyh/DeformTransGS/experiments/
stage5_0_R3_C2_perspective_v2_validity

Important evidence includes：

C2_future_J4_benchmark_lock.json

perspective_clean_gt_v2 manifest

camera intrinsic lock

camera extrinsic/pose lock

surface definitions

material definitions

deformation definitions

C2 report

C2 summary。

------------------------------------------------------------

Category D0：

Stage D0 exact material-point evidence。

Expected historical root：

/data/wyh/DeformTransGS/experiments/
stageD0_deformable_optical_transport_feasibility

Important evidence includes：

D0_protocol_lock.json

D0_material_samples/S0.csv

D0_material_samples/S1.csv

D0_deformation_replay.csv

D0_local_frame_table.csv

D0_deformation_descriptor_table.csv

D0_local_view_direction_table.csv

D0_pointwise_optical_oracle.csv

D0_paired_optical_transport_table.csv

D0_deformation_equations.md

stageD0_feasibility_report.md

stageD0_feasibility_summary.md。

------------------------------------------------------------

Category D0-SOURCE：

Expected source root：

/data/wyh/DeformTransGS/
deformable_optical_transport

Important scripts include：

run_D0.py

benchmark generator

material identity implementation

deformation implementation

local-frame implementation

pointwise optical oracle

independent replay。

------------------------------------------------------------

Category R3：

Final stopped anisotropic line status。

Expected historical root：

/data/wyh/DeformTransGS/experiments/
stageD1_N2_R3_sensor_aggregation_audit

Only the following are required for
historical status provenance：

stageD1N2R3_sensor_aggregation_report.md

stageD1N2R3_sensor_aggregation_summary.md

protocol/source lock if present。

Do not regenerate the anisotropic experiment。

============================================================
一、Failed-Gate Root-Cause Audit
============================================================

Read the failed E0-A source and logs。

For every required E0A-G0 item record：

logical evidence name

expected canonical path

path actually attempted

exists yes/no

file size

expected SHA256 if known

actual SHA256 if present

failure message

whether the item is essential for
forward replay

whether it is only historical documentation。

Output：

E0AR0_failed_gate_root_cause.csv
E0AR0_failed_gate_root_cause.md。

Gate R0-G0。

============================================================
二、Whole-Server Evidence Search
============================================================

Search read-accessible locations including：

/data

/home

/root/shared-nvme
only if readable

current project parent directories。

Search by：

exact filename

historical directory name

SHA256 where available

distinctive report title

manifest keys。

Do not search system pseudo-filesystems：

/proc

/sys

/dev。

Do not copy anything yet。

For every candidate record：

candidate path

filename

size

SHA256

modified time

exact-name match yes/no

exact-SHA match yes/no

content-semantic match yes/no。

Output：

E0AR0_server_search_inventory.csv。

Classify every required item：

FOUND-CANONICAL

FOUND-EXACT-ELSEWHERE

FOUND-NAME-BUT-SHA-MISMATCH

NOT-FOUND。

Gate R0-G1。

============================================================
三、Canonical Path Mapping
============================================================

Create：

E0AR0_path_mapping.csv。

Fields：

evidence name

old expected path

new discovered path

expected SHA256

actual SHA256

mapping type：

UNCHANGED

EXACT-SYMLINK-CANDIDATE

EXACT-COPY-CANDIDATE

REGENERATION-CANDIDATE

OLD-SERVER-COPY-REQUIRED

INVALID-SHA-MISMATCH。

Do not treat same filename as equivalent
when SHA differs。

If expected SHA is unavailable，
compare internal manifest，
row count，
schema，
source provenance
and deterministic summary values。

============================================================
四、Safe Exact-File Restoration
============================================================

For items classified
FOUND-EXACT-ELSEWHERE：

restore the canonical historical path。

Preferred method：

create a relative symbolic link
when the discovered file belongs to
a stable experiment directory。

Otherwise copy with metadata preservation。

Never overwrite an existing different file。

Before writing：

move any conflicting existing file to：

/data/wyh/DeformTransGS/experiments/
stageE0_A_R0_migration_restore/
conflicts/

Preserve its original relative path。

After restoration verify：

source SHA256

canonical-path SHA256

are identical。

Output：

E0AR0_exact_restoration.csv。

Gate R0-G2。

============================================================
五、Deterministic Regeneration Eligibility
============================================================

For every NOT-FOUND item determine whether
it is deterministically regenerable。

A file is REGENERATION-ELIGIBLE only if：

1. exact generating source exists；

2. generating source SHA matches
   a historical protocol/source lock，
   or its contents can be proven identical
   through an old report manifest；

3. all upstream input files exist；

4. fixed random seed is known；

5. exact row schema and expected row count
   are known；

6. no trained checkpoint or stochastic
   optimization result is required。

Expected likely regenerable D0 items：

material sample CSVs

deformation replay

local-frame table

deformation descriptors

local-view table

pointwise optical oracle

paired optical table。

Do not assume eligibility。

Audit each item independently。

Output：

E0AR0_regeneration_eligibility.csv。

Classify：

REGENERATION-ELIGIBLE

SOURCE-HASH-UNRESOLVED

UPSTREAM-INPUT-MISSING

NONREGENERABLE

TRAINED-STATE-REQUIRED。

Gate R0-G3。

============================================================
六、D0 Deterministic Rehydration
============================================================

Run this section only for items classified
REGENERATION-ELIGIBLE。

Do not overwrite recovered historical data。

Create temporary root：

/data/wyh/DeformTransGS/experiments/
stageE0_A_R0_migration_restore/
rehydrated_D0_tmp

Use the exact D0 source and frozen seed：

20260714。

Recompute only missing deterministic artifacts。

Required invariants from the accepted D0 result：

V2 exact case count：

1008。

S0 material sample count：

4096。

S1 material sample count：

4096。

Material identity recoverability：

triangle ID fraction =1.0

barycentric fraction =1.0

canonical xyz fraction =1.0

canonical uv fraction =1.0。

F validation p99/max relative error：

approximately
5.648937021674504e-13
/
5.648937021674504e-13。

Normal transport angular p99/max：

approximately
1.2074182697257333e-06
/
1.4787793334710982e-06 degrees。

Js closure p99/max relative error：

approximately
5.1591316671843886e-08
/
5.953529143649569e-08。

Pointwise optical oracle expected rows：

4128768。

Pointwise optical oracle actual rows：

4128768。

Paired table row count：

3538944。

Oracle replay tau p99/max relative error：

approximately
5.324187658504251e-08
/
9.309925339812295e-07。

Oracle replay RGB p99/max absolute error：

approximately
2.9489197528320688e-08
/
3.122765200869182e-07。

Do not merely write these values。

Actually regenerate and recompute them。

Tolerance for matching historical summaries：

integer counts：
exact。

fractions expected1.0：
absolute error <=1e-12。

floating closure metrics：
relative difference <=1e-6
or absolute difference <=1e-12，
whichever is less strict for the value。

If regenerated outputs fail these checks：

do not install them into canonical paths。

Classify：

REHYDRATION-MISMATCH。

Output：

E0AR0_D0_rehydration_validation.csv。

Gate R0-G4。

============================================================
七、Install Validated Rehydrated Evidence
============================================================

Only after R0-G4 PASS：

install validated regenerated files
into their expected canonical D0 paths。

Record for each file：

generator source SHA

input manifest

seed

row count

output SHA

canonical destination。

Mark the provenance explicitly as：

DETERMINISTICALLY-REHYDRATED
-AFTER-SERVER-MIGRATION。

Do not falsely claim that regenerated SHA
must equal the old missing file SHA
unless byte-identical serialization is guaranteed。

Scientific equivalence must instead be established
through exact schema/count/numerical closure。

Output：

E0AR0_rehydrated_install_manifest.csv。

Gate R0-G5。

============================================================
八、Old-Server Migration Manifest
============================================================

For every essential item that remains missing
and is not safely regenerable，
create：

E0AR0_old_server_copy_manifest.csv。

Fields：

priority

source expected old path

destination new path

filename/pattern

expected SHA if known

estimated size if known

reason required

whether directory-recursive copy is needed。

Also create a shell template：

E0AR0_old_server_rsync_template.sh。

Use placeholders：

OLD_HOST

OLD_USER

OLD_PORT。

Example syntax may use：

rsync -avP -e "ssh -p OLD_PORT" \
OLD_USER@OLD_HOST:<source> \
<destination>

Do not execute network migration。

Do not invent old-server credentials。

============================================================
九、Rebuild Protocol Lock
============================================================

After restoration/rehydration，
create a migration-aware lock：

E0AR0_migration_restored_protocol_lock.json。

For every evidence item record：

canonical path

source type：

ORIGINAL-FOUND

EXACT-RESTORED

DETERMINISTICALLY-REHYDRATED

STILL-MISSING。

SHA256

size

schema

row count where applicable

upstream source SHA

migration timestamp。

Gate R0-G6 PASS iff
all E0-A essential forward-replay evidence exists。

Historical report-only files may be missing
without blocking forward replay，
but must be documented。

============================================================
十、Minimal E0-A Forward Replay Recheck
============================================================

Do not run Jacobian or optimization。

Using the restored evidence，
rerun only：

E0A-G0

E0A-G1

E0A-G2

and deterministic sample selection preview。

Required：

E0A-G0 PASS。

E0A-G1 PASS。

Forward replay sampled rows：

100000。

E0A-G2 PASS。

Forward replay tau p99/max relative error：

<=1e-6 / <=1e-4。

Forward replay RGB p99/max absolute error：

<=1e-7 / <=1e-5。

Sample subset preview：

64 S0

64 S1

duplicates0。

Do not proceed to Jacobian automatically。

Output：

E0AR0_E0A_forward_recheck.csv。

Gate R0-G7。

============================================================
十一、Scientific Status Repair
============================================================

Write：

E0AR0_scientific_status.md。

The old E0-A result must be classified as：

SERVER-MIGRATION-PROVENANCE-BLOCK。

It is not evidence for：

NONRIGID-INCREMENTAL-NO-BENEFIT。

The candidate hypothesis remains：

UNTESTED。

If R0-G7 passes：

new primary line status：

READY-TO-RERUN-E0A-JACOBIAN-GATE。

If essential evidence cannot be restored：

new primary line status：

BLOCKED-PENDING-OLD-SERVER-COPY。

Do not classify scientific STOP
unless the actual E0-A Jacobian Gate executes
and fails。

============================================================
十二、Final CASE
============================================================

CASE E0A-MIGRATION-PROVENANCE-RESTORED：

essential evidence restored or
deterministically rehydrated；

migration-aware lock created；

100000-row forward replay passes；

64/64 sample subset can be selected。

Next action：

rerun the original Stage E0-A
from the Jacobian Phase，
using the unchanged frozen protocol。

Do not redesign the experiment。

------------------------------------------------------------

CASE E0A-OLD-SERVER-COPY-REQUIRED：

one or more essential,
non-regenerable files remain missing。

Next action：

copy only the files listed in
E0AR0_old_server_copy_manifest.csv，
then rerun this restoration Gate。

------------------------------------------------------------

CASE E0A-REHYDRATION-MISMATCH：

regenerated deterministic evidence
does not reproduce accepted D0 invariants。

STOP rehydration。

Do not use regenerated data。

Investigate source/version mismatch
or retrieve original files。

------------------------------------------------------------

CASE E0A-SOURCE-VERSION-MISMATCH：

current source cannot be proven equivalent
to the source that generated D0。

Do not regenerate evidence。

Retrieve the original source or experiment bundle。

============================================================
十三、Required Outputs
============================================================

E0AR0_failed_gate_root_cause.csv
E0AR0_failed_gate_root_cause.md

E0AR0_server_search_inventory.csv
E0AR0_path_mapping.csv

E0AR0_exact_restoration.csv
E0AR0_regeneration_eligibility.csv

E0AR0_D0_rehydration_validation.csv
E0AR0_rehydrated_install_manifest.csv

E0AR0_old_server_copy_manifest.csv
E0AR0_old_server_rsync_template.sh

E0AR0_migration_restored_protocol_lock.json
E0AR0_E0A_forward_recheck.csv
E0AR0_scientific_status.md

stageE0AR0_migration_report.md
stageE0AR0_migration_summary.md
stageE0AR0_migration_log.txt

============================================================
十四、最终终端只输出
============================================================

A. R0-G0

B. exact E0A-G0 failure reason

C. required evidence item count

D. existing canonical count

E. exact files found elsewhere count

F. SHA-mismatch candidate count

G. not-found count

H. R0-G1

I. exact files restored count

J. restoration SHA mismatch count

K. R0-G2

L. regeneration-eligible count

M. source-hash-unresolved count

N. nonregenerable missing count

O. R0-G3

P. D0 files regenerated count

Q. regenerated V2 case count

R. regenerated S0/S1 sample count

S. regenerated oracle row count

T. regenerated paired-table row count

U. regenerated tau replay p99/max error

V. regenerated RGB replay p99/max error

W. accepted D0 invariant mismatch count

X. R0-G4

Y. rehydrated files installed count

Z. R0-G5

AA. essential files still missing count

AB. old-server copy required yes/no

AC. migration manifest path

AD. R0-G6

AE. E0A-G0 recheck

AF. E0A-G1 recheck

AG. forward replay rows

AH. forward tau p99/max error

AI. forward RGB p99/max error

AJ. E0A-G2 recheck

AK. sample preview S0/S1 count

AL. sample preview duplicate count

AM. R0-G7

AN. E0-A hypothesis scientific status

AO. new primary line status

AP. Final CASE

AQ. allow E0-A Jacobian rerun yes/no

AR. next exact research action

AS. report path

AT. summary path

============================================================
十五、实际执行
============================================================

必须实际：

read the failed E0-A logs

enumerate every missing evidence item

search the new server for exact copies

compare SHA256

restore exact files safely

audit deterministic regeneration eligibility

regenerate only eligible missing D0 artifacts

validate all accepted D0 numerical invariants

install only validated artifacts

create an old-server copy manifest
for unresolved nonregenerable files

build a migration-aware protocol lock

rerun only E0A-G0/G1/G2

do not run Jacobian

do not run optimization。
PROMPT
```

## 最终结论

这次不是新方向被否定，而是：

[
\boxed{\text{迁移后的实验环境不完整，科学实验尚未执行}}
]

不要根据这份日志回 RecycleGS，也不要改 E0-A 方法。先恢复 D0/C2 的最小证据闭环。

恢复后，只有真正运行到：

```text
STATIC1 vs RIGID4 vs DEFORM4 Jacobian
```

才能判断这个方向是否值得继续。
