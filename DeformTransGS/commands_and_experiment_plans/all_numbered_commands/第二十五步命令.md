cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前科研状态
==================================================

Stage 3.3.1 已完成 point-level local optical analysis。

正式结果：

Uniform pointwise baseline：

stretch_1.25 MAE = 0.019
stretch_1.50 MAE = 0.035
stretch_2.00 MAE = 0.054

E_uniform = 0.036

Non-uniform cubic：

cubic_l010 MAE = 0.068
cubic_l020 MAE = 0.113
cubic_l0333 MAE = 0.155

E_nonuniform = 0.112

E_nonuniform / E_uniform = 3.1

Area-preserving nonlinear shear：

shear_k020 point MAE = 0.002
p95 = 0.005

shear_k040 point MAE = 0.008
p95 = 0.016

Twist：

point MAE = 0.099

Simple material-grid neighbourhood averaging：

self 1/Js MAE = 0.155

3x3 uniform neighbourhood MAE = 0.153

5x5 uniform neighbourhood MAE = 0.151

best improvement approximately 2.6%

==================================================
重要结论修正
==================================================

Stage 3.3.1 当前不能正式判定：

CASE B confirmed。

当前标记：

CASE B CANDIDATE。

原因一：

报告称：

Spearman 未计算，
因为 q=1/Js 非单调。

该理由错误。

正式 correlation 是：

Spearman(
R_local,
q_self
)

q_self 本身可以直接作为 rank variable。

即使存在 symmetric tied values，
也允许计算 Spearman。

原因二：

Gaussian-weighted neighbourhood targets
尚未计算。

因此：

Neighbourhood Mixing Gate
尚未被完整否定。

原因三：

当前没有直接证据证明：

response lag
由 Gaussian footprint mixing 导致。

禁止在 README / report 中写：

response lags due to Gaussian footprint mixing

作为已证实机制。

==================================================
本阶段名称
==================================================

Stage 3.3.2:

Gaussian-Footprint-Scale Deformation Heterogeneity Audit

中文：

Gaussian 覆盖尺度形变异质性审计

==================================================
一、唯一科学问题
==================================================

回答：

为什么 uniform area stretch 下：

R_local approximately 1/Js

但 spatially varying cubic stretch 下：

local error 显著增加？

核心假设：

Local optical consistency error
is controlled by deformation variation
within the effective Gaussian footprint，

rather than by Js magnitude alone。

中文：

局部光学一致性误差
可能由 Gaussian 有效覆盖范围内的
形变变化程度决定，

而不是只由 Js 数值大小决定。

本阶段只验证该机制。

禁止：

训练

新 deformation

tau update

optical correction

MLP

修改 renderer

修改 benchmark

==================================================
二、项目边界
==================================================

所有输出：

/data/wyh/DeformTransGS/experiments/
stage3_3_2_footprint_deformation_heterogeneity/

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

==================================================
三、输入
==================================================

严格使用 Stage 3.3.1：

material_point_response.csv

以及：

frozen carrier

canonical xyz

canonical scale

canonical rotation

canonical tau

camera definitions

per-state deformed xyz

per-state scale

per-state rotation

per-Gaussian Js

正式 states：

uniform:
stretch_1.25
stretch_1.50
stretch_2.00

non-uniform:
cubic_l010
cubic_l020
cubic_l0333

controls:
shear_k020
shear_k040
twist_60

禁止重新训练。

==================================================
四、首先补算正确 Point-Level Spearman
==================================================

对于：

cubic_l010
cubic_l020
cubic_l0333

直接计算：

scipy.stats.spearmanr(
R_local,
q_self
)

其中：

q_self = 1/Js

不要使用：

u vs R

不要因为 q 对 signed u 非单调
跳过计算。

同时计算：

Pearson。

输出：

corrected_pointwise_correlation.csv

记录：

state
point_count
q_unique_count
Pearson
Spearman
p_value

明确修正 Stage 3.3.1 报告。

==================================================
五、Material-Space Gaussian-Weighted Target
==================================================

Stage 3.3.1 已定义：

41x41 material topology。

此前只计算：

3x3 uniform average

5x5 uniform average。

现在必须完成 Gaussian-weighted targets。

对于 material point i：

canonical local tangent frame：

tangent_u_i
tangent_v_i

canonical activated scales：

sigma_u_i
sigma_v_i

对于 material point j：

delta_ij =
xyz_j - xyz_i

du =
dot(delta_ij, tangent_u_i)

dv =
dot(delta_ij, tangent_v_i)

定义：

w_surface_ij =

exp(
-0.5 *
[
(du / sigma_u_i)^2
+
(dv / sigma_v_i)^2
]
)

只使用 material grid：

ring1:
Chebyshev distance <=1

ring2:
Chebyshev distance <=2

同时增加：

FULL_SUPPORT：

搜索所有 material points，
保留：

w_surface >= exp(-9/2)

即 canonical tangent Gaussian
3-sigma support。

定义：

Q_RING1_GAUSSIAN

Q_RING2_GAUSSIAN

Q_FULL_SURFACE_GAUSSIAN

=

sum_j w*q_j
/
sum_j w

q_j=1/Js_j。

输出：

surface_gaussian_weighted_targets.csv

==================================================
六、Screen-Space Gaussian Footprint Target
==================================================

这是本阶段最重要 diagnostic。

由于实际 splatting 发生在 screen space，

对每：

state
camera
material point i

使用：

deformed geometry。

material point i
投影到：

p_i

对于所有 Gaussian j：

获取 TSGS renderer-compatible：

projected mean_j

projected 2D conic_j

或 projected 2D covariance。

禁止使用简单 Euclidean screen distance
替代 anisotropic Gaussian kernel。

根据实际 rasterizer：

delta =
projected_mean_j - p_i

计算：

power_j(p_i)

定义纯几何 kernel weight：

w_kernel_j = exp(power_j)

要求：

power <= 0

只保留：

w_kernel >= exp(-9/2)

即约 3-sigma support。

重要：

当前纯 geometry weight
禁止乘：

opacity
tau
color。

因为首先测试：

Gaussian footprint geometry
看到的 deformation field。

定义：

Q_SCREEN_KERNEL_i,c

=

sum_j
w_kernel_j * (1/Js_j)

/
sum_j w_kernel_j

同时定义 weighted deformation heterogeneity：

H_SCREEN_i,c

=

sqrt(
sum_j
w_kernel_j *
(q_j - Q_SCREEN_KERNEL_i,c)^2
/
sum_j w_kernel_j
)

定义 normalized heterogeneity：

H_REL_i,c

=
H_SCREEN_i,c
/
(
abs(Q_SCREEN_KERNEL_i,c)+1e-8
)

输出：

screen_kernel_targets_per_camera.csv

==================================================
七、Cross-Camera Footprint Target
==================================================

Stage 3.3.1：

R_local_i

定义为 valid cameras 的：

median R_i,c。

因此 footprint target
也必须采用相同 camera correspondence。

对于 material point i：

至少 2 valid cameras。

定义：

Q_SCREEN_KERNEL_i
=
median_c(
Q_SCREEN_KERNEL_i,c
)

H_SCREEN_i
=
median_c(
H_SCREEN_i,c
)

H_REL_i
=
median_c(
H_REL_i,c
)

输出：

material_footprint_targets.csv

禁止：

pool all camera pixels。

==================================================
八、Optional Optical-Budget Weighted Diagnostic
==================================================

只作为 diagnostic。

不能作为未来 method target。

对相同：

state
camera
material point i

使用当前 fixed canonical tau。

按照真实 rasterizer：

alpha_j(p_i)
=
opacity_j * exp(power_j)

应用：

alpha clamp

alpha skip threshold

但当前不要执行
transmittance early termination
改变 neighbour set。

定义：

w_budget_j
=
-log(
1-clamp(alpha_j,0,0.99)
)

只保留实际：

alpha >= 1/255

定义：

Q_OPTICAL_BUDGET_i,c
=
sum_j w_budget_j*q_j
/
sum_j w_budget_j

目的：

只分析 renderer optical budget
实际“看到”的 Js field。

禁止将 Q_OPTICAL_BUDGET
作为 proposed deformation measure。

输出：

optical_budget_weighted_targets.csv

==================================================
九、Target Comparison
==================================================

对：

cubic_l010
cubic_l020
cubic_l0333

比较：

T0:
Q_SELF = 1/Js_i

T1:
RING1_UNIFORM

T2:
RING2_UNIFORM

T3:
Q_RING1_GAUSSIAN

T4:
Q_RING2_GAUSSIAN

T5:
Q_FULL_SURFACE_GAUSSIAN

T6:
Q_SCREEN_KERNEL

T7:
Q_OPTICAL_BUDGET
diagnostic only

对于每 target：

MAE(
R_local,
Q_target
)

RMSE

Pearson

Spearman

输出：

all_target_comparison.csv

==================================================
十、Neighbourhood Gate 正式补全
==================================================

正式 Neighbourhood Mixing Gate：

对于：

cubic_l0333

至少一个：

T3
T4
T5
T6

满足：

MAE_target
<=
0.70 * MAE_self

即至少降低 30%。

并且：

Spearman >=0.9。

同时：

cubic_l020：

同一 target family

MAE_target
<
MAE_self。

如果满足：

NEIGHBOURHOOD CONSISTENCY SUPPORTED。

注意：

T7 optical budget target
不允许用于通过正式 Gate。

因为它使用 optical parameters，
属于 renderer diagnostic。

==================================================
十一、Footprint-Scale Heterogeneity Hypothesis
==================================================

定义 self-local optical error：

E_i
=
abs(
R_local_i
-
q_self_i
)

使用：

H_SCREEN_i

和：

H_REL_i

计算：

Pearson(E,H_SCREEN)

Spearman(E,H_SCREEN)

Pearson(E,H_REL)

Spearman(E,H_REL)

分别：

cubic_l010

cubic_l020

cubic_l0333

以及：

ALL_CUBIC pooled。

注意：

pooled 时记录 state label。

输出：

heterogeneity_error_correlation.csv

==================================================
十二、Js Magnitude vs Heterogeneity
==================================================

为了证明问题不是简单：

Js 越大误差越大，

比较两个 predictor：

P1:

stretch magnitude:

abs(log(Js_i))

P2:

footprint heterogeneity:

H_REL_i

对：

E_i

分别计算：

Spearman

以及简单 linear regression：

E ~ P1

E ~ P2

E ~ P1 + P2

报告：

R²

adjusted R²

coefficients

当前只做统计 diagnostic。

不要声称因果。

输出：

error_predictor_comparison.csv

==================================================
十三、Matched-Js Analysis
==================================================

这是关键控制。

跨：

uniform stretch
和
cubic states

寻找：

q_self=1/Js

相近的 material points。

建立 bins：

q:

[0.45,0.55)

[0.60,0.70)

[0.75,0.85)

[0.90,1.00+eps]

对于每 q bin：

分别统计：

UNIFORM points

CUBIC points。

记录：

q median

H_REL median

self error median

self error mean

p90 error

输出：

matched_js_comparison.csv

核心判断：

在 Js magnitude 接近时，

高 footprint heterogeneity 的 cubic points
是否误差明显更大。

定义：

至少两个 q bins：

cubic median error
>=
2 * uniform median error

并且：

cubic H_REL
>
uniform H_REL

则：

MATCHED-JS HETEROGENEITY EFFECT SUPPORTED。

==================================================
十四、Spatial Edge Lag Analysis
==================================================

对于：

cubic_l0333

使用：

abs(u) bins。

每 bin：

q_self median

Q_SCREEN_KERNEL median

R_local median

H_REL median

self error

screen-kernel target error

输出：

edge_lag_analysis.csv

重点回答：

edge high-gradient region：

R_local 是：

A. 接近 q_self

B. 接近 Q_SCREEN_KERNEL

C. 两者都不接近。

禁止只写：

response lag。

必须给数值。

==================================================
十五、Uniform / Shear Controls
==================================================

对：

stretch_1.25
stretch_1.50
stretch_2.00

计算：

H_REL。

理论：

Js constant

因此：

q constant

H_REL approximately 0。

验证。

对：

shear_k020
shear_k040

Js≈1 constant

H_REL≈0。

输出：

heterogeneity_control_validation.csv

要求：

uniform / shear median H_REL
<1e-4

允许边界 numerical effects，
使用 interior Gold points。

如果 H_REL 不接近 0：

说明 implementation bug。

==================================================
十六、Mechanism Gate
==================================================

首先修正 Stage 3.3.1 CASE。

--------------------------------------------------
M1:
POINT-LOCAL CORRELATION
--------------------------------------------------

cubic states：

正式报告 Spearman。

如果三个均 >=0.9：

POINT_LOCAL_TREND SUPPORTED。

注意：

这不等于 MAE 足够小。

--------------------------------------------------
M2:
FOOTPRINT TARGET
--------------------------------------------------

严格按正式 Neighbourhood Gate：

SUPPORTED / NOT SUPPORTED。

--------------------------------------------------
M3:
FOOTPRINT HETEROGENEITY ERROR LAW
--------------------------------------------------

SUPPORTED：

同时满足：

A.

ALL_CUBIC：

Spearman(
E_i,
H_REL_i
)
>=0.8

B.

matched-Js heterogeneity effect
SUPPORTED。

C.

cubic MAE：

l010 < l020 < l0333

已观察到，
重新确认。

否则：

NOT SUPPORTED。

--------------------------------------------------
M4:
AREA-PRESERVING CONTROL
--------------------------------------------------

SUPPORTED：

shear point MAE继续：

k020 <=0.10

k040 <=0.10

且：

H_REL≈0。

==================================================
十七、最终 CASE
==================================================

判断顺序：

CASE D:

M4 FAIL。

停止 area-dilution line。

--------------------------------------------------

CASE A:

原 Stage 3.3 CASE A
全部严格满足：

三个 cubic Spearman >=0.9

cubic_l0333 MAE_self <=0.10

E_nonuniform <=2*E_uniform

M4 PASS。

结论：

point-local consistency supported。

--------------------------------------------------

CASE C:

M2 SUPPORTED。

结论：

optical response
更接近 Gaussian-footprint-weighted deformation target。

允许进入：

footprint-aware deformation measure design。

--------------------------------------------------

CASE B1:

M2 NOT SUPPORTED

但：

M3 SUPPORTED。

结论：

local 1/Js consistency breaks
when deformation varies strongly
across the Gaussian footprint，

但 simple footprint averaging
does not directly predict the optical response。

新的研究问题：

How should deformation heterogeneity
within Gaussian support
be incorporated into optical consistency？

允许设计：

heterogeneity-aware optical consistency constraint。

禁止直接将：

Q_SCREEN_KERNEL

作为 target formula。

--------------------------------------------------

CASE B2:

M2 NOT SUPPORTED

M3 NOT SUPPORTED。

结论：

当前“footprint gradient”
解释也不成立。

Stage 3.3 CASE B
机制 unresolved。

禁止设计 method。

==================================================
十八、论文级结论边界
==================================================

CASE C：

允许写：

The deformation-induced optical response
is governed more closely by
Gaussian-footprint-scale deformation
than by point-local area stretch。

--------------------------------------------------

CASE B1：

允许写：

Point-local area-dilution consistency
degrades as deformation becomes heterogeneous
within a Gaussian footprint。

However,
simple neighbourhood averaging
does not explain the resulting optical response。

中文：

当一个 Gaussian 覆盖范围内的形变变得高度不均匀时，
点局部面积稀释一致性逐渐失效；
但简单邻域平均不足以解释实际光学响应。

这是允许进入 method 的结果。

--------------------------------------------------

CASE B2：

不能再写 footprint mixing。

==================================================
十九、可视化
==================================================

使用 matplotlib。

禁止 seaborn。

禁止指定颜色。

每张图独立。

生成：

1.
corrected_cubic_spearman.png

x:
q_self

y:
R_local

每 cubic state 独立。

2.
footprint_target_comparison.png

cubic_l0333：

self
ring uniform
surface Gaussian
screen kernel

MAE comparison。

3.
error_vs_footprint_heterogeneity.png

x:
H_REL

y:
self error

4.
matched_js_error_comparison.png

uniform vs cubic

按 q bin。

5.
cubic_l0333_edge_lag.png

x:
abs(u) bin

显示：

q_self
Q_SCREEN_KERNEL
R_local

6.
heterogeneity_maps.png

cubic_l0333 41x41：

q_self

H_REL

R_local

self error

==================================================
二十、报告
==================================================

输出：

corrected_pointwise_correlation.csv

surface_gaussian_weighted_targets.csv
screen_kernel_targets_per_camera.csv
material_footprint_targets.csv

optical_budget_weighted_targets.csv

all_target_comparison.csv
heterogeneity_error_correlation.csv
error_predictor_comparison.csv
matched_js_comparison.csv
edge_lag_analysis.csv
heterogeneity_control_validation.csv

footprint_heterogeneity_report.md
stage3_3_2_summary.md
stage3_3_2_log.txt

==================================================
二十一、footprint_heterogeneity_report.md
==================================================

必须中文回答：

A. Stage 3.3.1 为什么只能标记 CASE B candidate。

B. cubic_l010 corrected Spearman。

C. cubic_l020 corrected Spearman。

D. cubic_l0333 corrected Spearman。

E. Gaussian-weighted ring1 MAE。

F. Gaussian-weighted ring2 MAE。

G. full surface Gaussian target MAE。

H. screen-kernel target MAE。

I. optical-budget target MAE。

J. 正式 best non-optical target。

K. 相比 self improvement。

L. Neighbourhood Gate。

M. E vs H_REL ALL_CUBIC Spearman。

N. E vs |log Js| Spearman。

O. P1 / P2 / P1+P2 regression R²。

P. matched-Js effect 是否成立。

Q. 至少两个 matched q bin 的具体结果。

R. cubic_l0333 edge：

q_self
Q_SCREEN_KERNEL
R_local
H_REL。

S. uniform H_REL。

T. shear H_REL。

U. M1。

V. M2。

W. M3。

X. M4。

Y. Final CASE：
A/C/B1/B2/D。

Z. 当前真正科学问题。

AA. 是否允许设计 footprint-aware /
heterogeneity-aware constraint。

==================================================
二十二、README
==================================================

更新：

/data/wyh/DeformTransGS/README.md

修正：

Stage 3.3.1 showed that
strong spatially varying stretch
has much larger local optical error
than uniform stretch，

while area-preserving nonlinear shear controls
remain stable。

However,
pointwise Spearman and
Gaussian-footprint-weighted deformation targets
were incomplete。

Stage 3.3.2 tests whether
local optical error is governed by
deformation heterogeneity within Gaussian support。

==================================================
二十三、实际执行
==================================================

必须实际：

correct Spearman computation

Gaussian weighted ring targets

full support surface target

screen-space anisotropic kernel target

optical-budget diagnostic

target comparison

H_SCREEN / H_REL

error correlation

Js magnitude predictor comparison

matched-Js control

edge lag analysis

uniform/shear heterogeneity control

M1-M4

Final CASE

不要训练。

不要新 deformation。

不要设计 constraint。

不要修改 TSGS。

==================================================
二十四、最终终端总结
==================================================

最后简洁输出：

1. cubic l010 Spearman
2. cubic l020 Spearman
3. cubic l0333 Spearman
4. self MAE
5. Gaussian ring1 MAE
6. Gaussian ring2 MAE
7. full surface Gaussian MAE
8. screen kernel MAE
9. optical budget MAE
10. best formal target
11. improvement over self
12. Neighbourhood Gate
13. E vs H_REL Spearman
14. E vs abs(log Js) Spearman
15. P1 R2
16. P2 R2
17. P1+P2 R2
18. matched-Js effect
19. edge q_self / Q_kernel / R / H_REL
20. uniform H_REL
21. shear H_REL
22. M1
23. M2
24. M3
25. M4
26. Final CASE
27. Can design heterogeneity-aware constraint yes/no
28. footprint_heterogeneity_report.md path
29. stage3_3_2_summary.md path

请直接计算和验证。

不要训练。
不要新增 deformation。
不要设计 method。
不要修改 TSGS。
PROMPT