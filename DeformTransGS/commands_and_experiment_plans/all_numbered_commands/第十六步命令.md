cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

==================================================
当前研究状态
==================================================

Stage 3.2.1:

Renderer Integration Repair and Alpha-Composite Validation

已经完成。

正式结果：

Official TSGS renderer sanity:
PASS
render mean=0.83
visibility_filter=23106

Official Camera validation:
PASS
camera center max error < 2.4e-7

Carrier visibility:
PASS
1681 / 1681 Gaussians visible
for all 3 cameras

Two-pass alpha composite identity:
PASS

MAE=0.0
max error=4.2e-7

white-pass RGB channel max difference:
6e-8

Checkerboard transmission:
PASS

Tau gradient:
PASS
1681 / 1681 non-zero

Color gradient:
PASS
5043 / 5043 non-zero

Tau sensitivity:
PASS

100 iteration micro-fit:
PASS
approximately 80% loss reduction

3-camera 500 iteration pre-fit:
loss 0.066 -> 0.005

Stage 3.2 initial all-black failure root cause:

camera convention mismatch。

TSGS world_view_transform 中：

camera forward direction 对应实际
camera -> target forward convention。

修复后：

R_w2c[2,:] = fwd

而不是：

-fwd

==================================================
本阶段名称
==================================================

Stage 3.2 Resumed:

Controlled Thin-Film Gaussian Fixed-Optical-State Necessity Test

中文：

受控薄膜 Gaussian 固定光学状态必要性正式实验

==================================================
一、核心研究问题
==================================================

回答：

对于 thin semi-transparent Gaussian surface，

当 non-rigid deformation 导致
local surface area stretch ratio Js 改变时，

geometry 已按照 deformation gradient F
正确传输，

但是 canonical optical depth tau 保持固定，

是否产生随 Js 系统增长的 rendering error？

以及：

在完全相同 deformed geometry 下，

只重新优化 tau，

是否能够显著关闭该误差？

当前禁止实现：

tau' = tau / Js

或任何 deformation-aware tau transport method。

GT 中的：

tau_gt = tau0 / Js

仍然只是 synthetic benchmark rule。

==================================================
二、项目边界
==================================================

所有新增代码和结果：

/data/wyh/DeformTransGS

禁止修改：

/data/wyh/repos/TSGS
/data/wyh/RecycleGS
/data/wyh/repos/RT-Splatting

正式 renderer 必须统一使用：

Stage 3.2.1 validated official Camera adapter

+
TSGS official gaussian_renderer.render

+
override_color

+
two-pass differentiable alpha composite

禁止重新使用旧 low-level renderer。

==================================================
三、正式输出目录
==================================================

使用：

/data/wyh/DeformTransGS/experiments/stage3_2_fixed_optical_necessity/

正式输出至少包括：

renderer_gate_confirmation.md

canonical_fit_metrics.csv
canonical_fit_report.md

e1_fixed_tau_metrics.csv
e2_tau_refit_metrics.csv
e2_color_refit_metrics.csv

necessity_gap_vs_js.csv
tau_specificity_control.csv
tau_refit_analysis.csv

necessity_test_report.md
stage3_2_summary.md
stage3_2_log.txt

以及正式可视化。

==================================================
四、首先补齐 500 Iter NORE
==================================================

Stage 3.2.1 summary 未明确报告：

3-camera 500 iteration pre-fit NORE。

首先：

加载 Stage 3.2.1 500 iter 最终模型。

重新渲染：

cam_000
cam_004
cam_008

使用正式 two-pass alpha composite。

使用 canonical tau0=1.0 GT。

计算：

Optical Effect Mask

ORE

NORE

输出：

renderer_gate_confirmation.md

必须报告：

per-camera NORE

mean NORE

Renderer Integration Gate 原规则：

PASS:
mean NORE <= 0.50

如果：

mean NORE > 0.50

立即停止。

禁止运行 E1/E2。

最终结论：

renderer integration works
but canonical carrier fit remains insufficient。

如果：

mean NORE <= 0.50

确认：

Renderer Integration Gate = PASS

继续后续实验。

==================================================
五、重新执行正式 Canonical Fit
==================================================

不要直接使用旧失败 Stage 3.2 model。

允许使用 Stage 3.2.1 500 iter model
作为初始化，

但必须明确记录。

正式 geometry：

41 x 41 Gaussian grid

N=1681

Gaussian identity 固定。

无：

densification
pruning
clone
split

geometry：

xyz
scale
rotation

全部冻结。

可学习：

_tau_raw
_color_raw

定义：

tau =
softplus(_tau_raw)

alpha_opt =
1 - exp(-tau)

color =
sigmoid(_color_raw)

renderer：

validated two-pass alpha composite。

正式 canonical GT：

StretchableFilm-v0

tau0=1.0

canonical GT_DYNAMIC

cameras：

cam_000
cam_004
cam_008

训练总 iteration：

5000

保存：

1000
3000
5000

Loss：

L1
+
0.2 * DSSIM

Adam。

学习率：

tau = 1e-2
color = 1e-2

如果 gradient / loss instability：

允许降低到 1e-3。

禁止增加其它 parameter。

记录实际 lr schedule。

输出：

/data/wyh/DeformTransGS/baselines/
thinfilm_gs_canonical_tau1_repaired/

==================================================
六、正式 Canonical Fit Gate
==================================================

iteration 5000：

渲染 3 cameras。

计算：

Full image:

PSNR
SSIM
MAE
RMSE

Optical Effect Mask:

Masked PSNR
Masked SSIM
MAE
RMSE

以及：

ORE
NORE

Gate：

PASS:
mean NORE <= 0.30

WEAK:
0.30 < mean NORE <= 0.50

FAIL:
mean NORE > 0.50

如果 FAIL：

禁止 E1/E2。

如果 PASS / WEAK：

继续。

输出：

canonical_fit_metrics.csv

canonical_fit_report.md

==================================================
七、冻结 Canonical Checkpoint
==================================================

Canonical Fit PASS / WEAK 后：

冻结 iteration 5000。

保存：

xyz
scale
rotation
tau
color

以及 raw parameters。

生成：

canonical_checkpoint_manifest.json

记录：

Gaussian count

xyz checksum

scale checksum

rotation checksum

tau checksum

color checksum

要求后续所有 E1/E2
从完全相同 canonical checkpoint 开始。

==================================================
八、正式 Geometry Deformation Transport
==================================================

使用冻结 benchmark deformation API。

正式 states：

uniaxial:

stretch_1.10
stretch_1.25
stretch_1.50
stretch_2.00

biaxial:

biaxial_1.10
biaxial_1.25
biaxial_1.50

control:

twist_30
twist_60

对每个 canonical Gaussian：

xyz_i' =
phi(xyz_i)

计算真实：

F_i

covariance：

Sigma_i =
R_i diag(scale_i^2) R_i^T

Sigma_i' =
F_i Sigma_i F_i^T

对 Sigma_i'：

进行 symmetric eigendecomposition

或复用项目已有 validated covariance decomposition。

恢复：

scale_i'
rotation_i'

必须：

eigenvalues clamp positive epsilon

检查：

NaN / Inf

rotation quaternion normalized

注意：

Gaussian index 不能改变。

禁止：

densification
pruning
reordering

==================================================
九、Geometry Transport Validation
==================================================

正式 E1 前必须输出：

geometry_transport_validation.csv

每 state：

Gaussian count

mean Js

median Js

min Js

max Js

xyz finite

scale finite

rotation finite

quaternion norm error

covariance reconstruction error：

||Sigma_reconstructed - Sigma'||

mean
p95
max

另外验证：

uniaxial uniform sheet：

mean Js 接近：

1.10
1.25
1.50
2.00

biaxial：

接近：

1.21
1.5625
2.25

twist：

mean Js approximately 1

如果 Js 与 benchmark frozen values
明显不一致：

禁止运行 E1。

==================================================
十、E1 Fixed Optical State
==================================================

定义：

E1_FIXED_TAU

对每 deformation state：

geometry：

使用正式 deformed：

xyz'
scale'
rotation'

optical state：

tau_i' =
canonical tau_i

appearance：

color_i' =
canonical color_i

全部冻结。

禁止任何优化。

渲染：

cam_000
cam_004
cam_008

使用对应：

GT_DYNAMIC
tau0=1.0

background：

对应 deformation state 的
background_only / frozen benchmark background。

使用正式：

two-pass alpha composite。

计算：

Full image:
PSNR
SSIM
MAE
RMSE

Optical Effect Mask:
Masked PSNR
Masked SSIM
MAE
RMSE

ORE
NORE

输出：

e1_fixed_tau_metrics.csv

同时计算：

canonical-normalized error increase：

Delta_NORE_E1(state)
=
NORE_E1(state)
-
NORE_canonical

以及：

Delta_MAE_E1(state)
=
Masked_MAE_E1(state)
-
Masked_MAE_canonical

==================================================
十一、E2 Tau-Only Refit
==================================================

定义：

E2_TAU_REFIT

这是主实验控制变量。

对于每个 deformation state：

使用与 E1 完全相同的：

xyz'
scale'
rotation'
color

全部冻结。

只允许：

_tau_raw

requires_grad=True。

初始化：

canonical _tau_raw

禁止重新初始化。

禁止优化：

xyz
scale
rotation
color

禁止：

densification
pruning

GT：

对应 GT_DYNAMIC tau0=1.0。

3 cameras。

训练：

最多 2000 iterations。

Adam。

tau lr：

1e-2

如果发生明显 instability：

1e-3

必须记录。

保存：

iter 0
500
1000
2000

使用最终或 best-loss checkpoint。

必须明确使用：

final

还是：

best validation/training loss。

由于当前 3 views 全用于 mechanism fitting，

优先 final iteration，
同时报告 best loss。

输出：

/data/wyh/DeformTransGS/baselines/
tau_refit/<state>/

渲染和评价与 E1 完全一致。

输出：

e2_tau_refit_metrics.csv

==================================================
十二、新增必要控制：E2 Color-Only Refit
==================================================

定义：

E2_COLOR_REFIT

目的：

排除普通 RGB appearance parameter
也能同样补偿 optical-state error。

对于每个 deformation state：

geometry：

与 E1 / E2_TAU 完全相同。

tau：

固定 canonical tau。

唯一允许优化：

_color_raw

初始化：

canonical _color_raw

GT：

对应 GT_DYNAMIC。

3 cameras。

训练：

最多 2000 iterations。

Adam。

color lr：

1e-2

必要时：

1e-3

禁止优化 tau。

禁止优化 geometry。

禁止 densification。

输出：

/data/wyh/DeformTransGS/baselines/
color_refit/<state>/

评价：

与 E1 / E2_TAU 完全一致。

输出：

e2_color_refit_metrics.csv

==================================================
十三、严格 Same-Geometry Validation
==================================================

对于每 state：

E1_FIXED_TAU

E2_TAU_REFIT

E2_COLOR_REFIT

必须证明：

xyz max difference = 0

scale max difference = 0

rotation max difference = 0

Gaussian count identical

Gaussian index ordering identical

输出：

same_geometry_validation.csv

任何 geometry difference 非零：

禁止比较该 state。

==================================================
十四、Necessity Gap
==================================================

定义：

Tau Necessity Gap：

Gap_tau(state) =
NORE_E1
-
NORE_E2_TAU

Tau PSNR Recovery：

Recovery_tau =
Masked_PSNR_E2_TAU
-
Masked_PSNR_E1

Color Compensation Gap：

Gap_color(state) =
NORE_E1
-
NORE_E2_COLOR

Color PSNR Recovery：

Recovery_color =
Masked_PSNR_E2_COLOR
-
Masked_PSNR_E1

输出：

necessity_gap_vs_js.csv

字段：

state
deformation_type
strength
mean_Js
median_Js
mean_h_ratio

E1_NORE
E2_tau_NORE
E2_color_NORE

tau_NORE_gap
color_NORE_gap

E1_masked_PSNR
E2_tau_masked_PSNR
E2_color_masked_PSNR

tau_PSNR_recovery
color_PSNR_recovery

==================================================
十五、E1 Error vs Js
==================================================

核心分析：

x =
mean Js

y1 =
Delta_NORE_E1

y2 =
Delta_MAE_E1

对 uniaxial states：

计算：

Spearman rho

Pearson correlation

要求单独报告：

Js vs E1 NORE increase

Js vs E1 masked MAE increase

禁止把 biaxial 和 uniaxial
混在一起计算主单调性。

biaxial 作为额外验证。

==================================================
十六、正式 Necessity Gate
==================================================

PASS 必须同时满足：

A. AREA-CHANGING ERROR TREND

Uniaxial：

Js vs E1 error increase

Spearman rho >= 0.9

对于：

NORE
或
Masked MAE

至少一个成立。

B. TAU REFIT RECOVERY

至少：

stretch_1.50
或
stretch_2.00

满足：

tau NORE gap >= 0.10

或：

tau Masked PSNR recovery >= 1.0 dB

C. BIAXIAL GENERALIZATION

biaxial_1.50：

tau refit 具有：

NORE gap >= 0.10

或：

Masked PSNR recovery >= 1.0 dB

D. AREA-PRESERVING CONTROL

twist_60：

tau NORE gap
<=
25% of stretch_1.50 tau NORE gap

E. TAU-SPECIFICITY DIAGNOSTIC

比较：

Tau-only refit

vs

Color-only refit。

如果：

tau recovery
明显高于 color recovery，

则支持：

optical attenuation state
是主要缺失变量。

定义 diagnostic：

stretch_1.50
stretch_2.00
biaxial_1.50

三个核心 states 中，

至少 2 / 3：

tau NORE gap
>
color NORE gap

或者：

tau PSNR recovery
>
color PSNR recovery
by >= 0.5 dB

则：

TAU-SPECIFICITY SUPPORTED

如果 color-only 与 tau-only
恢复基本相同：

TAU-SPECIFICITY AMBIGUOUS

注意：

Tau-specificity ambiguous
不自动导致整个 Fixed Optical State Gate FAIL。

但禁止直接进入解析 tau transport 方法。

最终 Necessity Gate：

PASS

WEAK

FAIL

严格根据 A-D。

E 单独报告。

==================================================
十七、Tau Refit Analysis
==================================================

对于每 state：

canonical tau：

mean
std
median
p10
p90

refit tau：

mean
std
median
p10
p90

ratio：

tau_refit /
(tau_canonical + 1e-8)

统计：

mean
median
p10
p90

与：

h_ratio =
1 / Js

比较。

对 Gaussian-level：

如果 Js per Gaussian 非 uniform，
计算：

tau ratio vs h_ratio

Pearson

Spearman

对于 uniform stretch：

因为 Js 为常量，

Gaussian-level correlation 没有意义。

此时比较：

state median tau ratio
vs
state h_ratio。

必须区分：

within-state correlation

cross-state correlation。

正式 cross-state：

x =
state mean h_ratio

y =
state median tau_refit/tau_canonical

使用：

uniaxial states

计算：

Pearson
Spearman

输出：

tau_refit_analysis.csv

重要：

此分析只属于 discovery。

禁止实现公式。

==================================================
十八、Residual Decomposition
==================================================

为了判断 E1 error 来源：

至少将 optical mask 分为：

edge region

interior region

使用 mask erosion：

interior =
3x3 erosion
必要时 5x5

edge =
mask - interior

计算：

E1 MAE

E2_tau MAE

E2_color MAE

分别在：

edge
interior

统计。

目的：

如果 error 主要集中在 edge，

可能是 covariance / coverage transport 问题。

如果 interior error
随 Js 增长并被 tau refit关闭，

更支持 optical-state hypothesis。

输出：

residual_region_analysis.csv

==================================================
十九、正式可视化
==================================================

使用 matplotlib。

禁止 seaborn。

不要指定颜色。

每图单独生成。

1.

necessity_gap_vs_js.png

x:
mean Js

y:
E1-E2 tau NORE gap

2.

e1_error_vs_js.png

x:
mean Js

y:
E1 Delta NORE

3.

psnr_recovery_vs_js.png

同时画：

tau recovery
color recovery

4.

tau_ratio_vs_h_ratio.png

state-level：

x:
mean h_ratio

y:
median tau refit ratio

5.

stage3_2_overview.png

使用 PIL contact sheet。

states：

canonical
stretch_1.50
stretch_2.00
biaxial_1.50
twist_60

展示：

GT

E1 Fixed Tau

E1 Error

E2 Tau Refit

E2 Tau Error

E2 Color Refit

E2 Color Error

==================================================
二十、正式报告
==================================================

necessity_test_report.md
必须使用中文回答：

A. Controlled carrier 是否使用 RT-Splatting？

B. 为什么不使用 RT-Splatting？

只允许写：

runtime engineering blocker，
当前 controlled experiment
用于隔离 scientific variable。

不要攻击 RT-Splatting。

C. Gaussian count。

D. Gaussian identity 是否固定。

E. 是否 densify/prune。

F. Renderer 是否使用 Stage 3.2.1 validated path。

G. Canonical Fit NORE。

H. Canonical Fit Gate。

I. E1 是否只 transport geometry。

J. E2 Tau 是否只优化 tau。

K. E2 Color 是否只优化 color。

L. Same geometry validation。

M. E1 error 是否随 Js 增长。

N. Uniaxial Spearman rho。

O. stretch_1.50：

E1 NORE
E2 Tau NORE
E2 Color NORE
Tau PSNR recovery
Color PSNR recovery

P. stretch_2.00：
同上。

Q. biaxial_1.50：
同上。

R. twist_60 tau gap。

S. Necessity Gate：

PASS
WEAK
FAIL

T. Tau-specificity：

SUPPORTED
AMBIGUOUS

U. E1 residual 是否主要位于 edge
还是 interior。

V. tau refit ratio 与 h_ratio
cross-state Spearman。

W. 当前结论：

“fixed optical state is insufficient
under area-changing deformation”

只允许：

SUPPORTED
PARTIALLY SUPPORTED
NOT SUPPORTED

X. 是否允许开始设计 deformation-aware tau transport：

只有：

Necessity Gate PASS
+
Tau-specificity SUPPORTED

才回答：

是。

否则：

否。

==================================================
二十一、Stage 3.2 Summary
==================================================

stage3_2_summary.md

严格分：

# 1. Renderer Gate Confirmation

# 2. Canonical Fit

# 3. Geometry Transport Validation

# 4. E1 Fixed Tau Results

# 5. E2 Tau-Only Refit Results

# 6. Color-Only Compensation Control

# 7. Error vs Surface Stretch Js

# 8. Area-Preserving Twist Control

# 9. Tau Refit / Thickness Correlation

# 10. Necessity Gate

# 11. Tau-Specificity Decision

# 12. Final Research Decision

禁止提出正式方法。

==================================================
二十二、README
==================================================

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage 3.2.1 renderer integration PASS。

The previous all-black render was caused
by an incorrect camera forward-axis convention.

The validated renderer now uses:

official TSGS Camera

official render

override_color

two-pass alpha composite

for per-pixel background transmission。

Stage 3.2 resumes the fixed optical state necessity test。

==================================================
二十三、实际执行
==================================================

必须实际：

计算 500 iter NORE

确认 renderer Gate

正式 canonical 5000 fit

Canonical Fit Gate

冻结 checkpoint

geometry deformation transport

geometry validation

E1 fixed tau

E2 tau-only refit

E2 color-only refit

same geometry validation

metrics

error vs Js correlation

twist control

tau specificity

tau ratio analysis

edge/interior residual analysis

可视化

全部报告

不要只写代码。

如果 canonical Fit FAIL：

停止。

不要执行 E1/E2。

如果 renderer Gate FAIL：

停止。

禁止实现 tau/Js update rule。

==================================================
二十四、最终终端总结
==================================================

最后简洁输出：

1. 500 iter pre-fit NORE
2. Renderer Integration Gate
3. Canonical 5000 NORE
4. Canonical Fit Gate
5. Gaussian count
6. Geometry transport validation PASS/FAIL
7. E1 error-vs-Js Spearman
8. stretch_1.50 E1 NORE
9. stretch_1.50 Tau Refit NORE
10. stretch_1.50 Color Refit NORE
11. stretch_1.50 Tau PSNR recovery
12. stretch_1.50 Color PSNR recovery
13. stretch_2.00 Tau PSNR recovery
14. stretch_2.00 Color PSNR recovery
15. biaxial_1.50 Tau PSNR recovery
16. biaxial_1.50 Color PSNR recovery
17. twist_60 Tau NORE gap
18. Necessity Gate
19. Tau-specificity
20. tau ratio vs h_ratio cross-state Spearman
21. Fixed optical state hypothesis
22. Can design tau transport rule yes/no
23. stage3_2_overview.png path
24. necessity_test_report.md path
25. stage3_2_summary.md path

请直接实际运行和验证。

不要换服务器。
不要使用 RT-Splatting。
不要继续环境恢复。
不要实现 proposed tau transport formula。
不要修改 TSGS。
PROMPT