这轮我先给你一个很明确的判断：

> **F2 是真正的大 PASS。**
>
> 但我暂时不接受 `CASE REAL-CANONICAL-CARRIER-INSUFFICIENT`。

当前更准确的状态应该是：

> **`CANONICAL-FIT-PROVENANCE-UNRESOLVED`**
>
> 中文：**真实 canonical（初始状态）拟合的训练与评估来源尚未闭环。**

不是说 C4 可以无视。**C4 FAIL 必须继续拦住 24 个 oracle 实验。**

但现在还不能说 4096 Gaussian + O/C/V 的表达能力不够。

---

# 一、先说最重要的：F2 已经彻底过了

这次是真结果：

[
O\ forward\ diff=2.027\times10^{-5}
]

[
C\ forward\ diff=1.721\times10^{-5}
]

[
V\ forward\ diff=1.932\times10^{-5}
]

说明：

> 改 O、C、V，真实渲染图都会变化。

梯度：

[
|\nabla_O L|_2=5.313\times10^{-4}
]

[
|\nabla_C L|_2=5.738\times10^{-4}
]

[
|\nabla_V L|_2=5.167\times10^{-4}
]

而且：

```text
finite fraction = 1
nonzero fraction = 1
```

有限差分与自动求导的相对误差：

```text
O = 0.00435
C = 0.000108
V = 0.00151
```

全部远低于 0.10。

因此现在已经正式证明：

[
\boxed{
O,C,V\rightarrow Gaussian\ render\rightarrow loss
}
]

三条真实可微计算图全部成立。

**这一块以后不要再查。**

---

# 二、K0 和 K1 指标完全一样，其实不奇怪

这里我要纠正一个容易误判的地方。

K0：

```text
S0
MAT0_NEUTRAL_FIXED_THICKNESS
D0
```

K1：

```text
S0
MAT1_NEUTRAL_MASS_CONSERVING
D0
```

在 D0 恒等形变下：

[
J_s=1
]

MAT0：

[
h=h_0
]

MAT1：

[
h=\frac{h_0}{J_s}=h_0
]

而且二者：

[
\sigma_{rgb}=[1.2,1.2,1.2]
]

完全一样。

因此在 D0：

[
\boxed{GT_{K0}=GT_{K1}}
]

理论上就是同一个 GT。

所以：

```text
K0 = 18.135546/0.497695/0.497695
K1 = 18.135546/0.497695/0.497695
```

**不构成异常证据。**

---

# 三、真正异常的是 K2 也精确一模一样

K2 是：

```text
S1_WAVY_MEMBRANE
MAT2_TINTED_MASS_CONSERVING
D0
```

它与 K0/K1 有两个根本区别。

第一，表面不同：

[
z=0.18\sin(\pi u)\sin(\pi v)
]

是波浪膜。

第二，光学属性不同：

[
\sigma_{rgb}=[0.6,1.2,2.0]
]

是明显有色吸收。

但它仍然得到：

```text
PSNR             18.135546
tau Elog          0.497695
alpha Elog        0.497695
```

**六位小数完全一致。**

这个概率太低了。

尤其：

[
median\ \tau Elog
=================

# median\ \alpha Elog

0.497695
]

三个 case 全一样。

所以我目前第一怀疑不是“carrier capacity（载体表达能力）”。

而是：

> **canonical evaluator（初始状态评估器）可能没有真正按照 case key 加载对应 GT / render。**

或者：

> K0/K1/K2 训练实际复用了同一个 state、同一个 checkpoint、同一份 render。

或者：

> metric path（指标路径）总是读取第一组 case。

---

# 四、C6 PASS 也不能排除这种 bug

C6 只证明：

```text
synthetic dependency = 0
hardcoded metric branch = 0
```

这很好。

但下面这种 bug 完全可以通过 C6：

```python
gt_path = gt_cases[0]
```

或者：

```python
checkpoint_path = output_dir / "canonical.pt"
```

三个任务覆盖同一个文件。

或者：

```python
for case in cases:
    train(case)

evaluate(last_checkpoint, first_gt)
```

或者：

```python
metric_rows.append(previous_metrics)
```

这些都不是 synthetic metric。

但结果一样是假实验来源问题。

---

# 五、还有一个重要问题：报告没有给训练过程证据

我们之前明确要求记录：

```text
initial train loss
final train loss

O grad norm
C grad norm
V grad norm

O parameter delta
C parameter delta
V parameter delta
```

但最终终端完全没有这些。

现在我不知道：

### 情况 A

Adam 真实训练 4000 iter，但从：

[
loss=1.5
]

降到：

[
loss=0.4
]

最后还是 C4 FAIL。

那可能是真的 carrier 不够。

### 情况 B

训练：

```text
iteration = 0
```

或：

```text
initial loss = final loss
```

那训练根本没跑。

### 情况 C

loss 在降，但：

```text
parameter delta O/C/V = 0
```

optimizer 路径坏了。

### 情况 D

训练正常，但 fresh test render 又读回 initial state。

那是 checkpoint/evaluation bug。

在这些没查清以前：

> **不能做“carrier insufficient（载体能力不足）”结论。**

---

# 六、现在只做 C4 provenance closure

这次不碰 O/C/V graph。

不碰 GT。

不碰 rasterizer。

不跑 24 jobs。

只回答：

> **K0/K1/K2 到底有没有真实训练、真实保存、真实重新加载、真实各自渲染、真实匹配各自 GT。**

阶段名称：

> **Stage 4.0-R2A-C4A：Canonical Fit Training and Evaluation Provenance Closure**

中文：

> **初始 Gaussian 拟合训练与评估来源闭环**

直接执行：

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式科学状态
============================================================

Stage4.0 original scientific outputs：

RETIRED。

AttributeDeformGS hypothesis：

UNTESTED。

KIOT：

CONTROLLED-CARRIER-ONLY。

============================================================
已经正式关闭的问题
============================================================

VERIFIED_GT_ROOT：

/data/wyh/DeformTransGS/experiments/
stage4_0_R2A_GT1_gt_optical_semantics_closure/
clean_gt

Clean GT independent validation：

Js relative p99/max =
0 /0

tau relative p99/max =
5.740e-08 /5.951e-08

RGB absolute p99 =
2.977e-08

alpha absolute p99 =
1.481e-08。

GT CLOSED。

------------------------------------------------------------

Verified rasterizer runtime：

/usr/bin/python3

Python3.10.12

torch2.6.0+cu124

torch CUDA12.4。

Verified package：

/data/wyh/repos/TSGS/submodules/
diff-first-surface-rasterization/
build/lib.linux-x86_64-cpython-310/
diff_first_surface_rasterization/__init__.py

Verified `_C`：

/data/wyh/repos/TSGS/submodules/
diff-first-surface-rasterization/
build/lib.linux-x86_64-cpython-310/
diff_first_surface_rasterization/
_C.cpython-310-x86_64-linux-gnu.so。

Direct opacity gradient：

FINITE_NONZERO

nonzero fraction1

L2 =
1.407241e-03。

Direct colors_precomp gradient：

FINITE_NONZERO

nonzero fraction1

L2 =
1.257929e-03。

Rasterizer gradient boundary CLOSED。

------------------------------------------------------------

Stage4 adapter F2：

PASS。

Real forward perturbations：

O mean/max diff =
2.027006e-05 /1.194954e-03

C =
1.720791e-05 /5.297661e-04

V =
1.931801e-05 /1.420319e-03。

O/C/V first broken edge：

NO_BROKEN_EDGE。

Gradients：

O finite/nonzero/L2 =
1 /1 /5.313005e-04

C =
1 /1 /5.737565e-04

V =
1 /1 /5.166773e-04。

Directional derivative relative errors：

O =
4.346562e-03

C =
1.077400e-04

V =
1.512628e-03。

F2 PASS。

C6 PASS。

Therefore：

DO NOT inspect GT again。

DO NOT inspect runtime again。

DO NOT inspect rasterizer gradients again。

DO NOT inspect O/C/V graph again。

============================================================
当前唯一问题
============================================================

Three real canonical fits reported：

K0：

PSNR =
18.135546

median tau Elog =
0.497695

median alpha Elog =
0.497695。

K1：

18.135546
0.497695
0.497695。

K2：

18.135546
0.497695
0.497695。

C4 FAIL。

K0 and K1 equality is EXPECTED
at D0。

Reason：

K0 =
S0 + MAT0 + D0。

K1 =
S0 + MAT1 + D0。

At D0：

Js =1。

MAT0 h =
h0。

MAT1 h =
h0/Js =
h0。

Both use：

sigma_rgb =
[1.2,1.2,1.2]。

Therefore K0/K1 GT should be identical。

This is NOT a bug。

------------------------------------------------------------

K2 equality is suspicious。

K2：

S1_WAVY_MEMBRANE

MAT2_TINTED_MASS_CONSERVING

D0。

K2 has：

different surface geometry

and

sigma_rgb =
[0.6,1.2,2.0]。

Yet its three reported metrics
are exactly identical to K0/K1
to six decimal places。

Before declaring：

REAL-CANONICAL-CARRIER-INSUFFICIENT

the exact canonical training /
checkpoint /
fresh-render /
GT-key /
metric provenance must be closed。

============================================================
正式撤销当前最终分类
============================================================

RETIRE as established conclusion：

CASE REAL-CANONICAL-CARRIER-INSUFFICIENT。

Current formal state：

CANONICAL-FIT-PROVENANCE-UNRESOLVED。

C4 remains FAIL
and correctly blocks all24 oracle jobs。

Do NOT run24 jobs。

============================================================
本阶段名称
============================================================

Stage 4.0-R2A-C4A:

Canonical Fit Training
and Evaluation Provenance Closure

中文：

初始 Gaussian 拟合训练
与评估来源闭环

============================================================
唯一目标
============================================================

Determine whether K0/K1/K2：

1. loaded the correct case-specific GT

2. used separate model/state instances

3. actually executed Adam optimization

4. changed O/C/V parameters

5. saved distinct real checkpoints

6. reloaded the correct checkpoint

7. rendered fresh TEST images

8. saved case-specific render arrays

9. evaluated against the correct GT case

10. produced genuine metrics。

Only after these are proven
may C4 be interpreted as
carrier capacity evidence。

============================================================
绝对禁止事项
============================================================

禁止：

change GT

change4096 Gaussian count

change covariance

change O/C/V definitions

change loss weights

change Adam

change learning rate

change iteration limit

change patience

change C4 thresholds

run24 oracle jobs

run288 jobs

KIOT

new attribute

new renderer

rerun environment audit

rerun graph audit

manually improve canonical metrics。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stage4_0_R2A_C4A_canonical_provenance_closure/

New source：

/data/wyh/DeformTransGS/attribute_study/
real_oracle/canonical_closure/

Create：

canonical_closure/
    trace_canonical_jobs.py
    audit_case_gt.py
    audit_fit_history.py
    audit_checkpoint.py
    audit_test_render.py
    audit_evaluator_keys.py
    independent_canonical_metrics.py
    rerun_canonical_if_needed.py
    run_c4a.py

Every CUDA Python process must use
the previously verified Stage4 launcher。

Use：

PYTHONNOUSERSITE=1

verified rasterizer build/lib first。

============================================================
一、Protocol Lock
============================================================

Lock：

VERIFIED_GT_ROOT

camera split

4096-Gaussian carrier source

canonical optimization source

loss source

canonical checkpoint source

canonical fresh render source

canonical metric evaluator

K0/K1/K2 existing fit histories

K0/K1/K2 checkpoints

existing canonical TEST renders

existing canonical metric table。

Record：

path
size
mtime
SHA256。

Output：

c4a_protocol_lock.json。

Gate H0。

============================================================
二、Case Identity Definition
============================================================

Freeze exact case keys：

K0：

surface =
S0_PLANAR_SHEET

material =
MAT0_NEUTRAL_FIXED_THICKNESS

deformation =
D0_IDENTITY。

K1：

surface =
S0_PLANAR_SHEET

material =
MAT1_NEUTRAL_MASS_CONSERVING

deformation =
D0_IDENTITY。

K2：

surface =
S1_WAVY_MEMBRANE

material =
MAT2_TINTED_MASS_CONSERVING

deformation =
D0_IDENTITY。

Output：

canonical_case_lock.csv。

============================================================
三、GT Case Identity Audit
============================================================

For all8 TEST cameras
and all16 TRAIN cameras：

resolve exact GT paths
used by K0/K1/K2。

Record：

case
split
camera ID
RGB path
alpha path
tau path
triangle path
SHA256。

Output：

canonical_gt_path_audit.csv。

Require：

K0 and K1：

RGB/tau/alpha SHA
may be identical
camera by camera。

Explicitly report equality fraction。

Require K2 vs K0：

at least one RGB SHA differs

at least one tau SHA differs

at least one alpha SHA differs。

Also compute direct GT differences：

K0 vs K1：

mean/max RGB diff

mean/max tau diff

mean/max alpha diff。

K0 vs K2：

same metrics。

Output：

canonical_gt_case_difference.csv。

Gate H1。

If K2 GT is identical to K0：

CASE CANONICAL-GT-KEY-BUG。

STOP。

============================================================
四、Trace Canonical Job Launches
============================================================

Trace the exact command/process
that executed K0/K1/K2。

For each report：

launcher path

interpreter

CUDA_VISIBLE_DEVICES

case arguments

output directory

checkpoint path

history path

test-render root。

Output：

canonical_job_launch_trace.csv。

Check whether：

same output path reused

same checkpoint filename reused

case argument ignored

loop variable shadowed

last/first case reused。

Search source for：

cases[0]

case_list[0]

surface = fixed value

material = fixed value

checkpoint = common path

history = common path

canonical.pt

model.pt

without case subdirectory。

Output：

canonical_case_reuse_source_search.csv。

============================================================
五、Fit History Integrity
============================================================

For K0/K1/K2 independently
load actual fit-history CSV。

Report：

row count

first iteration

last iteration

unique iteration count

duplicate iteration count

initial total loss

final total loss

best total loss

best iteration

relative loss reduction

last500 relative reduction

early stop reason

learning rate first/final。

Also for O/C/V：

grad L2：

first finite
median
p90
last

parameter delta L2：

first
final
max。

Output：

canonical_fit_history_audit.csv。

Create per-case compact history:

canonical_fit_history_summary.csv。

Formal REAL_TRAINING_EXECUTED iff：

history rows >=2

last iteration >0

at least one O/C/V
median grad L2 >1e-8

at least one O/C/V
final parameter delta L2 >1e-8

relative train loss reduction >0.01。

Gate H2a。

============================================================
六、Independent Optimizer State Audit
============================================================

Load each checkpoint with torch.load
in a CPU-only audit process。

For every K case report：

checkpoint path

SHA256

iteration

contains o_raw yes/no

contains sh_coeffs yes/no

contains v_raw yes/no

contains optimizer state yes/no

optimizer state parameter count

optimizer step min/max

geometry SHA

case metadata if present。

Tensor statistics：

O mean/std/min/max

C mean/std/min/max

V mean/std/min/max。

Output：

canonical_checkpoint_audit.csv。

Compare checkpoint SHAs：

K0 vs K1

K0 vs K2

K1 vs K2。

Because K0/K1 share identical GT
and deterministic initialization，
identical checkpoints are POSSIBLE
if execution is bitwise deterministic。

K2 checkpoint must differ
from K0/K1 in at least one
O/C/V tensor value or checkpoint SHA。

Gate H2b。

============================================================
七、Initialization-to-Checkpoint Delta Audit
============================================================

Reconstruct the exact deterministic
canonical initialization
without optimization。

Use seed20260714。

For K0/K1/K2 compare initial state
with saved final checkpoint：

O L2 delta

O max abs delta

C L2 delta

C max abs delta

V L2 delta

V max abs delta。

Output：

canonical_initial_final_parameter_delta.csv。

Require each K case：

at least one O/C/V final L2 delta >1e-6。

Also report
which attribute tensors changed。

Gate H2c。

============================================================
八、Checkpoint Cross-Load Diagnostic
============================================================

For each TEST camera subset：

camera IDs：

0
3
6
9
12
15
18
21

or the exact frozen TEST IDs。

Render diagnostic matrix：

K0 checkpoint on K0 geometry

K1 checkpoint on K1 geometry

K2 checkpoint on K2 geometry。

Also diagnostic cross-load：

K0 checkpoint on K2 geometry

K2 checkpoint on K0 geometry。

Use correct case-specific
material frame/geometry。

No optimization。

Save hashes only plus aggregate diffs。

Output：

canonical_checkpoint_crossload_render.csv。

This is diagnostic only。

Determine whether K2 state/render
is actually distinct。

============================================================
九、Fresh TEST Render Artifact Audit
============================================================

For every K0/K1/K2 and8 TEST cameras：

list expected existing:

RGB NPY
alpha NPY。

Report：

path

exists

dtype

shape

SHA256

min/max/mean/std。

Output：

canonical_test_render_artifact_audit.csv。

Require：

48 arrays total：

3 cases
x8 cameras
x2 array types。

Check duplicate hashes。

Report：

K0/K1 same-camera RGB hash equality fraction。

K0/K2 same-camera RGB hash equality fraction。

K0/K2 same-camera alpha hash equality fraction。

If K0 and K2 render hashes
are identical on all cameras：

classification diagnostic：

CANONICAL-RENDER-CASE-REUSE。

Gate H3a。

============================================================
十、Fresh Reload-and-Render Reproduction
============================================================

Using verified launcher：

for each K case：

instantiate a NEW Gaussian state。

Load that case's checkpoint。

Verify max tensor load error=0。

Load that case's geometry。

Render8 TEST cameras fresh。

Save under C4A output：

fresh_reproduction/
K0
K1
K2。

Do NOT use old saved renders。

Compare new render vs existing
canonical TEST render arrays：

RGB max diff

alpha max diff。

Output：

canonical_fresh_render_reproduction.csv。

Require：

p99/max differences <=1e-7。

If existing renders do not reproduce：

classification：

CANONICAL-SAVED-RENDER-PROVENANCE-FAIL。

Gate H3b。

============================================================
十一、Evaluator Key Trace
============================================================

Trace exact evaluator code
that produced：

18.135546
0.497695
0.497695。

For each metric row report resolved：

case key

surface

material

deformation

camera IDs

predicted RGB paths

predicted alpha paths

GT RGB paths

GT alpha paths

GT tau paths。

Output：

canonical_evaluator_key_trace.csv。

Require:

K0 metric uses K0 predictions + K0 GT

K1 uses K1 predictions + K1 GT

K2 uses K2 predictions + K2 GT。

Search evaluator source for：

first row reuse

iloc[0]

rows[0]

cases[0]

default surface

default material

same render root

same GT root suffix

stale loop variable

cached metric dictionary
without case key。

Output：

canonical_evaluator_reuse_search.csv。

Gate H4a。

============================================================
十二、Independent Canonical Evaluator
============================================================

Create：

independent_canonical_metrics.py。

It must NOT import：

canonical optimizer

canonical evaluator

release policy

old Stage4 metrics

synthetic code。

Input only：

VERIFIED_GT_ROOT

C4A fresh_reproduction arrays

canonical case lock

camera manifest。

Compute from arrays：

RGB PSNR

SSIM

median TAU_RGB_ELOG

p95 TAU_RGB_ELOG

median ALPHA_TAU_ELOG。

Use exact previously frozen
metric definitions。

Output：

independent_canonical_metrics.csv。

Compare against old reported metric rows。

Output：

canonical_metric_comparison.csv。

Report per metric absolute difference。

Gate H4b。

============================================================
十三、Metric Collision Diagnostic
============================================================

Check whether exact triple：

18.135546
0.497695
0.497695

comes from：

A.
actual K0 arrays

B.
actual K1 arrays

C.
actual K2 arrays

D.
an initialization render

E.
a common stale render

F.
a default metric row。

Evaluate the deterministic initial
unoptimized state for K0/K2
on TEST cameras。

Compute metrics independently。

Output：

canonical_metric_collision_diagnostic.csv。

No scientific interpretation。

============================================================
十四、Root-Cause Classification
============================================================

Classify exactly one primary cause：

CANONICAL-GT-KEY-BUG

CANONICAL-JOB-CASE-REUSE

CANONICAL-OPTIMIZATION-NOT-EXECUTED

CANONICAL-OPTIMIZER-NO-PARAMETER-UPDATE

CANONICAL-CHECKPOINT-CASE-REUSE

CANONICAL-SAVED-RENDER-PROVENANCE-FAIL

CANONICAL-EVALUATOR-KEY-REUSE

CANONICAL-METRIC-STALE-ROW

REAL-CANONICAL-CARRIER-CAPACITY-LIMIT

OTHER-EXACT-CODE-CAUSE。

Output：

canonical_c4_root_cause.md。

Must identify exact：

file
function
line range
evidence。

============================================================
十五、Minimal Provenance Fix
============================================================

Execute ONLY if root cause is
a training/evaluation provenance bug。

Allowed：

fix case argument propagation

fix output path case namespace

fix checkpoint case key

fix checkpoint reload path

fix render manifest case key

fix evaluator GT/render key

remove stale metric cache

ensure Adam loop actually executes

ensure optimizer.step is called。

Do NOT：

change representation

change optimizer hyperparameters

change loss

change carrier

change thresholds。

Output：

canonical_minimal_provenance_fix.md。

============================================================
十六、Rerun Exactly Three Canonical Jobs
============================================================

Execute ONLY after provenance fix
or if audit needs a clean controlled rerun。

Delete NOTHING from old outputs。

Rerun exactly：

K0
K1
K2

into NEW C4A output directories。

Use verified launcher。

Use the original frozen：

4096 Gaussians

O+C+V

Adam

16 TRAIN cameras

8 TEST cameras

loss

4000 max iterations

500 patience

seed20260714。

Every case receives
a unique output directory。

Record subprocess command
and PID。

Output：

c4a_canonical_rerun_manifest.csv。

============================================================
十七、Real Training Causality Gate
============================================================

For rerun K0/K1/K2：

require：

optimizer.step count >0

initial vs final total train loss
reduction >1%

at least one trainable tensor
final delta L2 >1e-6

optimizer state step >0

checkpoint reload error=0。

Output：

c4a_real_training_causality.csv。

Gate H5。

============================================================
十八、Fresh Independent C4 Re-evaluation
============================================================

Reload rerun checkpoints fresh。

Render8 TEST cameras。

Save arrays first。

Run independent evaluator。

Report：

K0:

PSNR
median tau Elog
median alpha Elog

K1 same

K2 same。

C4 threshold remains UNCHANGED：

PSNR >=28

median tau Elog <=0.25

median alpha-tau Elog <=0.25。

Gate H6。

============================================================
十九、Decision Rule
============================================================

CASE CANONICAL-PROVENANCE-BUG-FIXED:

a concrete provenance bug was found

minimal fix applied

H5 PASS

H6 PASS。

Conclusion：

previous C4 FAIL was invalid。

ALLOW：

resume24-job real oracle smoke。

------------------------------------------------------------

CASE REAL-CANONICAL-CARRIER-INSUFFICIENT:

H0-H5 PASS

training provenance is real

case keys are correct

fresh renders are correct

independent evaluator is correct

but H6 FAIL。

Conclusion：

the fixed4096-Gaussian
O+C+V canonical carrier
cannot reach the frozen canonical Gate。

Only NOW is carrier insufficiency accepted。

Do NOT run24 oracle jobs。

------------------------------------------------------------

CASE CANONICAL-PROVENANCE-STILL-UNRESOLVED:

H0-H5 cannot establish
real case-specific training/evaluation。

STOP。

============================================================
二十、Strict Gates
============================================================

H0:
C4A PROTOCOL LOCK。

H1:
CASE-SPECIFIC GT IDENTITY。

H2:
REAL CANONICAL TRAINING STATE

PASS iff H2a/H2b/H2c pass。

H3:
CHECKPOINT TO FRESH RENDER PROVENANCE

PASS iff H3a/H3b pass。

H4:
INDEPENDENT METRIC PROVENANCE

PASS iff H4a/H4b pass。

H5:
RERUN REAL TRAINING CAUSALITY。

H6:
FROZEN C4 CAPACITY GATE。

============================================================
二十一、Required Outputs
============================================================

c4a_protocol_lock.json
canonical_case_lock.csv

canonical_gt_path_audit.csv
canonical_gt_case_difference.csv

canonical_job_launch_trace.csv
canonical_case_reuse_source_search.csv

canonical_fit_history_audit.csv
canonical_fit_history_summary.csv

canonical_checkpoint_audit.csv
canonical_initial_final_parameter_delta.csv

canonical_checkpoint_crossload_render.csv

canonical_test_render_artifact_audit.csv
canonical_fresh_render_reproduction.csv

canonical_evaluator_key_trace.csv
canonical_evaluator_reuse_search.csv

independent_canonical_metrics.csv
canonical_metric_comparison.csv
canonical_metric_collision_diagnostic.csv

canonical_c4_root_cause.md
canonical_minimal_provenance_fix.md
if needed

c4a_canonical_rerun_manifest.csv
c4a_real_training_causality.csv

c4a_fresh_canonical_metrics.csv

stage4_0_R2A_C4A_report.md
stage4_0_R2A_C4A_summary.md
stage4_0_R2A_C4A_log.txt

============================================================
二十二、最终终端只输出
============================================================

A. H0

B. K0 vs K1 GT RGB/tau/alpha
same-camera SHA equality fraction

C. K0 vs K2 GT RGB/tau/alpha
same-camera SHA equality fraction

D. K0 vs K1 GT mean/max RGB diff

E. K0 vs K2 GT mean/max RGB diff

F. H1

G. K0/K1/K2 launch case arguments

H. K0/K1/K2 output directories

I. shared output/checkpoint path yes/no

J. K0 history rows/last iter

K. K1 history rows/last iter

L. K2 history rows/last iter

M. K0 initial/final/best train loss

N. K1 initial/final/best train loss

O. K2 initial/final/best train loss

P. K0 O/C/V final parameter delta L2

Q. K1 O/C/V final parameter delta L2

R. K2 O/C/V final parameter delta L2

S. H2

T. K0/K1/K2 checkpoint SHA

U. K0 vs K1 checkpoint identical yes/no

V. K0 vs K2 checkpoint identical yes/no

W. K0/K1/K2 optimizer max step

X. expected existing TEST array count

Y. existing TEST array count

Z. K0 vs K1 RGB render hash equality fraction

AA. K0 vs K2 RGB render hash equality fraction

AB. fresh reproduction RGB/alpha max diff

AC. H3

AD. evaluator K0 resolved GT/render case keys

AE. evaluator K1 resolved GT/render case keys

AF. evaluator K2 resolved GT/render case keys

AG. H4

AH. old K0 metrics

AI. independent K0 metrics

AJ. old K1 metrics

AK. independent K1 metrics

AL. old K2 metrics

AM. independent K2 metrics

AN. exact metric collision source

AO. root cause classification

AP. exact root cause file/function/lines

AQ. minimal provenance fix applied yes/no

AR. changed files/lines

AS. canonical rerun executed yes/no

AT. K0 rerun optimizer step count

AU. K1 rerun optimizer step count

AV. K2 rerun optimizer step count

AW. K0 rerun initial/final loss

AX. K1 rerun initial/final loss

AY. K2 rerun initial/final loss

AZ. rerun checkpoint reload max error

BA. H5

BB. fresh K0 PSNR/tau/alpha Elog

BC. fresh K1 PSNR/tau/alpha Elog

BD. fresh K2 PSNR/tau/alpha Elog

BE. H6

BF. Final CASE

BG. previous C4 fail valid yes/no

BH. real canonical carrier sufficient yes/no

BI. allow24-job real oracle smoke yes/no

BJ. AttributeDeformGS hypothesis status

BK. KIOT status

BL. report path

BM. summary path

============================================================
二十三、README
============================================================

Update README。

Record：

Stage4.0-R2A-F2F3 formally closed
the O/C/V differentiable graph。

O/C/V all alter actual rasterizer renders，
receive finite nonzero gradients，
and pass finite-difference
directional derivative checks。

F2 PASS。

The next C4 canonical smoke Gate reported
the identical metric triple：

18.135546 /
0.497695 /
0.497695

for K0，K1 and K2。

K0/K1 equality is not itself suspicious，
because MAT0 and MAT1 are optically
identical at D0 when Js=1。

However K2 uses a different
wavy surface and tinted sigma_rgb
[0.6,1.2,2.0]，
so exact metric equality with K0/K1
requires provenance closure
before declaring carrier insufficiency。

Stage4.0-R2A-C4A audits
case-specific GT keys，
training histories，
optimizer parameter changes，
checkpoint identity，
fresh TEST render hashes，
checkpoint reload reproduction，
and evaluator case-key resolution。

An independent evaluator recomputes
canonical metrics directly from
fresh saved arrays。

Only if real case-specific training，
checkpointing，
fresh rendering and evaluation all pass
while the unchanged C4 threshold still fails
may the4096-Gaussian O+C+V carrier
be classified as insufficient。

No oracle release jobs run
before this closure。

============================================================
二十四、实际执行
============================================================

必须实际：

lock C4 artifacts

audit K0/K1/K2 GT identity

prove K0/K1 GT equality at D0

prove K2 GT differs

trace case launch arguments

trace output/checkpoint paths

audit actual fit histories

audit optimizer state

audit initial-to-final parameter deltas

compare checkpoint SHAs

audit existing TEST render arrays

fresh reload each checkpoint

freshly render all TEST cameras

compare fresh vs old renders

trace evaluator GT/render keys

independently recompute metrics

locate exact source of identical metric triple

classify root cause

apply minimal provenance fix only if needed

rerun exactly K0/K1/K2

audit optimizer-step causality

fresh render arrays first

independently compute C4

Final CASE

绝对不要：

run24 oracle jobs before H6 PASS

change canonical model

change4096 count

change loss

change optimizer

change C4 thresholds

generate synthetic metrics

return to KIOT。
PROMPT
```

# 我的真实评价

**这条线现在还不能杀。**

因为目前最有价值的结果反而是：

> **真实 O/C/V pipeline 已经完全可微。**

这个是硬证据。

现在 C4 的三个完全一样指标太像：

> 训练/evaluator plumbing bug（训练或评估管线连接错误）。

当然，我也提前说清楚：

> 如果 C4A 最终证明 K0/K1/K2 都真实训练了，参数明显更新，checkpoint 不同，fresh render 正确，独立 evaluator 正确，而 PSNR 还是 18、Elog 还是 0.5——

那我会**正式接受 `REAL-CANONICAL-CARRIER-INSUFFICIENT`**。

到时候不要再救 4096 carrier。

但在看到这些来源证据之前，**现在直接判载体能力不够，还是太早。**
