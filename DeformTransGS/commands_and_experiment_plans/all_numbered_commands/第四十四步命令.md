这次我可以很明确地说：

> **这是目前 DeformTransGS 整条实验线里，第一次真正完成“发现问题 → 排除指标错误 → 证明机制 → 证明方法空间存在”的闭环。**
>
> **现在可以正式进入方法设计。**

而且下一步我**不建议直接上 MLP**。

你的结果已经给出了一个非常明确的解析方法方向。

---

# 一、先说第四十三步到底证明了什么

现在证据链是：

### 1. P0 的面积稀释不是假象

same-key（完全相同评估集合）重跑：

```text
alpha max diff = 0
R median/p95/max diff = 0
```

原来的 S2 FAIL 已明确来自：

> `REFERENCE-KEY-SET MISMATCH`

所以 repaired S2：

> **PASS**

这一点很重要。

---

### 2. Full covariance transport 的中心响应破坏正式成立

严格 S4：

```text
A = True
B = True
closer_count = 6/6
```

所以：

> **S4 SUPPORTED 没有争议。**

尤其 stretch2：

[
P0=0.462776
]

[
P2=0.955487
]

因此：

[
\Delta R=0.492711
]

之前的 `0.483 → 0.804` 来自旧的另一套表；当前 frozen protocol（冻结协议）下真正结果是：

[
\boxed{0.463\rightarrow0.955}
]

这个结果比原先还强。

完整协方差传输几乎把：

[
q=0.5
]

的面积稀释信号完全推回：

[
R\approx1
]

PhysGaussian 本身明确使用形变梯度的一阶信息演化 Gaussian kernel，并将 Fixed Covariance 定义为只平移核、Rigid Covariance 定义为仅施加刚体变换；所以你现在 P0/P1/P2 的运动学比较具有非常直接的文献依据。([CVF开放获取][1])

---

### 3. `tau / Js` 确实不是正确 scalar rule

这一次不是猜。

P3 implementation（实现）已经 PASS：

[
xyz_{P3}=xyz_{P2}
]

[
\Sigma_{P3}=\Sigma_{P2}
]

并且：

[
\frac{\tau_{P3}}{\tau_{P2}}
===========================

\frac1{J_s}
]

所以 P3 跑坏不能再怪代码。

stretch2：

[
R_{P3}=2.734181
]

六状态 mean central error：

[
1.183019
]

比 P2 还差。

---

### 4. 失败原因也已经定位到 Gaussian kernel 语义

本地 rasterizer 的实际公式与你的审计一致：

[
\alpha
======

\min(0.99,\ o\exp(power))
]

[
\alpha<1/255
\Rightarrow skip
]

随后：

[
T_{\rm new}=T(1-\alpha)
]

且：

[
T_{\rm new}<10^{-4}
]

时提前结束。官方 3DGS CUDA 实现也是 opacity 乘 Gaussian 指数衰减后得到 alpha，再更新透射率。([GitHub][2])

所以对于单 Gaussian：

[
g=e^{power}
]

[
\psi(o,g)
=========

-\log(1-og)
]

你的实验已经证明：

> `tau/Js` 在 (g=1) 的 Gaussian 中心严格正确，但离开中心以后不再严格正确。

而且高 (\tau) Gaussian 特别明显：

```text
tau p90, q=0.5

g=1.00 → 0.500
g=0.80 → 0.752
g=0.50 → 0.829
g=0.20 → 0.860
g=0.05 → 0.870
```

这组数字太漂亮了。

它直接说明：

[
\boxed{
\text{kernel amplitude 降低}
\Rightarrow
\tau/Js\text{ 的实际响应重新向 }1\text{ 漂移}
}
]

---

# 二、最关键的是 K6 + K7

这两条组合起来才是真正的“可以做方法”。

K6：

> `SCALAR-RULE-WRONG`

best scalar 相比 P3：

[
97.59%
]

RMSE improvement。

也就是说：

> **不是 scalar opacity（标量不透明度）本身完全没能力。**

只是：

[
\tau'=\tau/J_s
]

这个规则错了。

K7：

> `CAPACITY-SUPPORTED`

5×5 patch（局部 5×5 Gaussian 区域）自由优化每个 Gaussian 一个 (\tau)，能够恢复目标。

所以现在我们可以明确排除：

> “必须立刻给每个 Gaussian 加纹理 opacity”
>
> “必须用 view-dependent opacity”
>
> “必须用一个复杂 MLP 输出空间 opacity field”

已有工作确实通过给单 Gaussian 增加空间变化的 alpha texture（透明度纹理）提高表达能力；VoD-3DGS 也通过额外表示让 opacity 随视角变化。([arXiv][3])

但你的实验目前证明：

> **当前 controlled deformation 问题里，一个 Gaussian 一个 scalar optical state 仍然具有局部容量。**

所以现在直接上 richer optical representation（更复杂光学表示）反而属于过度设计。

---

# 三、我认为方法应该叫“核感知标量光学状态传输”

英文暂时可以写：

> **Kernel-Aware Scalar Optical State Transport**

简称：

> **KOST**

但我现在更喜欢一个更具体的名字：

> **Kernel-Integrated Opacity Transport**

简称：

> **KIOT**

中文：

> **核积分不透明度传输**

为什么？

因为 R1 已经告诉我们：

> 不能只保证 Gaussian 中心正确。

应该考虑：

> **整个 Gaussian kernel 上的有效光学贡献。**

---

# 四、这里其实可以直接推导一个解析规则

这是我认为下一步最值得测的地方。

对于一个 Gaussian：

[
g=e^{-\frac12r^2}
]

opacity 为：

[
o
]

单像素有效光学深度：

[
\psi(o,g)
=========

-\log(1-og)
]

暂时忽略 CUDA 的 skip 和 clamp。

考虑整个二维 Gaussian kernel：

[
\Phi(o)
=======

\int_{\mathbb R^2}
-\log(1-og),dA
]

代入：

[
g=e^{-r^2/2}
]

极坐标：

[
\Phi(o)
=======

2\pi
\int_0^\infty
-\log(1-oe^{-r^2/2})
r,dr
]

令：

[
g=e^{-r^2/2}
]

得到：

[
\Phi(o)
=======

2\pi
\int_0^1
\frac{-\log(1-og)}{g},dg
]

而这个积分就是：

[
\boxed{
\Phi(o)=2\pi\operatorname{Li}_2(o)
}
]

其中：

[
\operatorname{Li}_2
]

叫**二重对数函数**，是一种特殊数学函数。

---

## 那么我们的物理目标是什么？

面积伸长：

[
q=\frac1{J_s}
]

希望 Gaussian 整体 kernel optical contribution（核光学贡献）缩小：

[
\Phi(o')
========

q\Phi(o)
]

因此：

[
\operatorname{Li}_2(o')
=======================

q\operatorname{Li}_2(o)
]

所以：

[
\boxed{
o'
==

\operatorname{Li}_2^{-1}
\left(
q\operatorname{Li}_2(o)
\right)
}
]

最后：

[
\tau'
=====

-\log(1-o')
]

这和：

[
\tau'=\tau/J_s
]

完全不同。

---

# 五、更妙的是：这个公式正好解释你的实验

### `tau/Js`

保证：

[
g=1
]

Gaussian 中心正确。

所以是：

> **center-exact（中心严格正确）**

### KIOT

保证：

[
\int_{\rm kernel}\psi',dA
=========================

q
\int_{\rm kernel}\psi,dA
]

所以是：

> **kernel-integrated exact（核积分意义正确）**

这正好对应你的实验发现：

> tau/Js 在中心对，但完整 kernel 上错。

---

# 六、但是我必须提醒你一个非常重要的文献风险

我刚刚专门查了。

2026 年 SIGGRAPH/TOG 的 **Gaussian Point Splatting** 已经在处理 Gaussian opacity 的积分时使用了 dilogarithm（二重对数函数）以及它的逆函数。它的目标是把 Gaussian 转成随机 opaque points（不透明随机点）时保持原始 Gaussian splatting 的预期 opacity，而不是处理非刚性形变或面积伸长下的 optical-state transport。([图形与可视化出版物][4])

所以：

> **绝对不能把“我们首次在 Gaussian 中推导 Li₂”写成创新点。**

这会撞。

但这反而帮了我们。

因为说明：

> Gaussian kernel 积分自然出现 (\operatorname{Li}_2) 不是我拍脑袋推公式。

Gaussian Point Splatting 已经从另一个问题独立走到了相同数学结构。([图形与可视化出版物][4])

你的潜在创新应该是：

> **将 kernel-integrated effective optical contribution 作为形变时需要传输的光学量，并通过局部面积变化 (J_s) 驱动其演化。**

即：

[
\boxed{
\Phi(o_i')
==========

\frac1{J_{s,i}}
\Phi(o_i)
}
]

不是：

> 我发明了 Li₂。

---

# 七、而且 CUDA 的 skip/clamp 还能进一步纳入公式

实际 renderer：

[
\alpha_{skip}=\frac1{255}
]

[
\alpha_{max}=0.99
]

所以我们甚至不用只测试：

[
2\pi Li_2(o)
]

可以直接定义：

[
\Phi_{\rm CUDA}(o)
==================

2\pi
\int
\frac{
-\log[
1-\min(0.99,og)
]
}{g}
,dg
]

只积分：

[
og\geq1/255
]

如果：

[
\frac1{255}<o\leq0.99
]

有：

[
\boxed{
\Phi_{\rm CUDA}(o)
==================

2\pi
\left[
Li_2(o)
-------

Li_2(1/255)
\right]
}
]

如果：

[
o>0.99
]

则：

[
\boxed{
\Phi_{\rm CUDA}(o)
==================

2\pi
\left[
Li_2(0.99)
----------

## Li_2(1/255)

\log(0.01)
\log\frac{o}{0.99}
\right]
}
]

注意：

[
-\log(0.01)>0
]

所以也可写：

[
Li_2(0.99)-Li_2(1/255)
+
[-\log(0.01)]
\log(o/0.99)
]

然后定义：

[
\boxed{
o'
==

\Phi_{\rm CUDA}^{-1}
\left(
q\Phi_{\rm CUDA}(o)
\right)
}
]

这就是一个真正：

> **renderer-aware（渲染器感知）**
>
> **kernel-aware（Gaussian 核感知）**
>
> **scalar（标量）**
>
> **training-free（无需训练）**

的 optical-state transport（光学状态传输）规则。

---

# 八、这和现有工作的边界也比较清楚

OMG 在 ICLR 2025 明确提出：3DGS 中 opacity 不应完全脱离材料属性，并基于 Beer–Lambert 思想让 material cross section（材料截面性质）参与 opacity 建模。([arXiv][5])

但 OMG 解决的是：

> inverse rendering（逆渲染）中 material–opacity 关联。

你的问题是：

> **当 (F\Sigma F^T) 让 Gaussian kernel 随材料形变时，如何根据局部面积伸长传输 primitive optical state（图元光学状态）。**

这是不同的问题。

Textured Gaussians 则说明标准单 Gaussian 的统一 opacity/color 表达存在空间表达限制，并通过 alpha/RGB texture 增加单 primitive 的空间容量。([arXiv][3])

但你的 K7 已说明：

> 在当前问题中 scalar state 仍然有容量。

所以论文叙事应该是：

> **我们不是因为 scalar representation 无能而增加表达维度，而是发现传统的物理厚度缩放规则与 Gaussian splatting 的 kernel-opacity 参数语义不匹配。**

我觉得这个叙事比“加 opacity MLP”强得多。

---

# 下一步：Stage 3.5A

名称我建议：

> **Renderer-Aware Kernel-Integrated Opacity Transport Gate**

中文：

> **渲染器感知核积分不透明度传输验证**

正式测试 KIOT。

这次先不学网络。

````bash
cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式科学状态
============================================================

Stage3.4C-R1 completed：

K0 PASS
K1 PASS
K2 SUPPORTED
K3 PASS
K4 PASS
K5 PASS
K6 SCALAR-RULE-WRONG
K7 CAPACITY-SUPPORTED。

Final CASE：

SCALAR-RULE-INSUFFICIENT-BUT-STATE-CAPABLE。

This Final CASE is ACCEPTED。

============================================================
核心已验证结论
============================================================

1.

Stage3.4C P0 baseline
exactly reproduces under the SAME frozen key set。

Original S2 FAIL：

REFERENCE-KEY-SET MISMATCH CONFIRMED。

Same-key：

alpha max diff =0

R median/p95/max diff =0。

Therefore：

S2 repaired PASS。

------------------------------------------------------------

2.

Strict S4：

A=True

B=True

closer_count=6/6。

Therefore：

CENTRAL CANCELLATION SUPPORTED。

For locked stretch_2.00：

P0 median R =
0.462776

P2 median R =
0.955487

P2-P0 =
0.492711。

The earlier 0.804/0.483 values
came from another older reference table
and are retired。

------------------------------------------------------------

3.

P3 implementation：

PASS。

P3 geometry is P2 geometry。

xyz identical。

Sigma identical。

tau_P3 / tau_P2 =
q =
1/Js。

However：

P3 stretch2 median R =
2.734181

P3 six area-changing-state
mean central error =
1.183019。

Therefore：

tau' = tau/Js
does NOT restore
the physical optical response。

------------------------------------------------------------

4.

Actual local rasterizer semantics：

power =
Gaussian conic quadratic exponent

alpha =

min(
    0.99,
    opacity * exp(power)
)

alpha <1/255：
skip

test_T =
T*(1-alpha)

test_T <1e-4：
terminate。

------------------------------------------------------------

5.

Define：

g = exp(power)

o =1-exp(-tau)

psi(o,g) =

-log(
    1-o*g
)

ignoring clamp/skip
for continuous analytic diagnosis。

Desired contribution scaling：

psi' =
q*psi

q=1/Js。

tau'=q*tau

implies：

o_p3 =
1-(1-o)^q。

This is exact when：

g=1。

But generally non-exact
for g<1。

Stage3.4C-R1 proved：

CENTER-EXACT KERNEL-NONEXACT。

Example：

tau p90
q=0.5：

g=1:
response=0.500000

g=.8:
0.752445

g=.5:
0.828586

g=.2:
0.859804

g=.05:
0.869759。

------------------------------------------------------------

6.

Required exact opacity：

o_required(g)

=

[
1-(1-o*g)^q
]
/
g。

This varies with g。

Required opacity is nonconstant。

Single-Gaussian CUDA results
exactly match the analytic kernel model：

median analytic-CUDA error=0

p95=0。

------------------------------------------------------------

7.

However：

best scalar opacity oracle
improves RMSE over tau/Js by：

0.975877

approximately 97.6%。

PATCH_FREE_TAU_ORACLE：

CAPACITY-SUPPORTED。

Therefore：

the current controlled carrier
does NOT support the conclusion
that scalar optical state itself is insufficient。

The supported conclusion is：

THE SCALAR UPDATE RULE IS WRONG，
BUT SCALAR STATE CAPACITY REMAINS。

============================================================
正式方法方向
============================================================

Method family：

KERNEL-AWARE SCALAR OPTICAL-STATE TRANSPORT。

Do NOT add：

spatial opacity texture

view-dependent opacity

MLP

richer optical state

at this stage。

First test an analytic：

KERNEL-INTEGRATED OPACITY TRANSPORT。

Candidate name：

KIOT

Kernel-Integrated Opacity Transport。

中文：

核积分不透明度传输。

============================================================
重要文献边界
============================================================

The appearance of the dilogarithm Li_2
in Gaussian kernel opacity integration
is NOT claimed as novel。

Gaussian Point Splatting
(SIGGRAPH / TOG 2026)
also uses the dilogarithm
and its inverse
for a DIFFERENT opacity-correction problem：

stochastic opaque-point sampling
that preserves original Gaussian-splat opacity。

Therefore：

DO NOT claim：

"we first derive Li_2 for Gaussian opacity."

Our candidate novelty question is：

Can the kernel-integrated
effective optical contribution
be treated as the transported optical state
under deformation-induced area stretch？

Specifically：

Phi(o_i') =
q_i Phi(o_i)

q_i =1/Js_i。

This use of the kernel integral
for deformation-aware optical-state transport
is the experimental question。

Do NOT make novelty claims
before final literature audit。

============================================================
本阶段名称
============================================================

Stage 3.5A:

Renderer-Aware Kernel-Integrated
Opacity Transport Gate

中文：

渲染器感知核积分
不透明度传输验证

============================================================
一、唯一科学问题
============================================================

Answer：

Does transporting
the kernel-integrated effective optical contribution

rather than the Gaussian-center optical depth

provide a correct scalar optical-state update
after full covariance transport？

Formally：

P2 geometry：

xyz' = phi(xyz)

Sigma' = F Sigma F^T。

Physical area factor：

q_i =1/Js_i。

Compare scalar rules：

B0 P2_FIXED

B1 TAU_JS

B2 OPACITY_LINEAR

B3 KIOT_CONTINUOUS

B4 KIOT_CUDA_AWARE

B5 FREE_TAU_ORACLE_DIAGNOSTIC。

============================================================
二、禁止事项
============================================================

禁止：

MLP

neural network

training a correction model

opacity texture

view-dependent opacity

new covariance rule

clone
split
prune
densification

changing metric

changing frozen evaluation keys

changing Q_tau

changing physical q=1/Js

result-tuned thresholds。

Only test：

analytic scalar optical-state transport。

============================================================
三、输出目录
============================================================

所有新增实验：

/data/wyh/DeformTransGS/experiments/
stage3_5A_kernel_integrated_opacity_transport/

所有新增代码：

/data/wyh/DeformTransGS/analysis/

禁止覆盖 Stage3.4C/R1。

============================================================
四、Protocol Lock
============================================================

Lock：

Stage3.4C frozen eval camera keys

Stage3.4C frozen eval cell keys

Stage3.4C carrier

Stage3.4C deformation inputs

P2 FULL_AFFINE_COV geometry

CURRENT oracle-validated optical metric

CURRENT exact ratio

CURRENT Q_tau target

CURRENT cameras

CURRENT renderer extension

CURRENT forward.cu。

Output：

stage3_5A_protocol_lock.json。

All required SHA
must match Stage3.4C/R1。

If mismatch：

PROTOCOL-FAIL。

============================================================
五、Renderer Constants
============================================================

Read ACTUAL local forward.cu。

Lock：

ALPHA_SKIP =
1/255

ALPHA_MAX =
0.99

T_TERMINATE =
1e-4。

Do not hardcode until source check。

Output：

kiot_renderer_constants.json。

============================================================
六、Opacity Semantics
============================================================

Canonical：

tau_i >0。

o_i =

1-exp(-tau_i)。

After scalar transport
solve opacity o_i'。

Then convert back：

tau_i' =

-log1p(-o_i')。

Renderer still receives：

opacity =
1-exp(-tau_i')。

Numeric identity：

renderer opacity
must equal o_i'

max diff <=1e-7。

Output：

opacity_tau_roundtrip_test.md。

============================================================
七、Continuous Kernel Integral
============================================================

For one normalized 2D Gaussian：

g(r) =
exp(-r^2/2)。

Effective optical-depth contribution：

psi(o,g) =

-log(1-o*g)。

Define continuous full-kernel integral：

Phi_cont(o)

=

integral_R2
psi(o,g(x))
dx。

Analytic：

Phi_cont(o)

=
2*pi*Li_2(o)。

IMPORTANT：

Li_2 appearance itself
is NOT claimed novel。

Implement：

li2_unit_interval(o)。

Prefer：

scipy.special.spence(1-o)

if scipy is available。

Verify semantics numerically：

Li_2(0)=0

Li_2(1)=pi^2/6。

If scipy unavailable：

implement a float64 high-accuracy fallback。

Record implementation。

Output：

li2_implementation_audit.md。

============================================================
八、Continuous KIOT
============================================================

Desired：

Phi_cont(o_new)

=
q*Phi_cont(o_old)。

Therefore：

Li_2(o_new)

=
q*Li_2(o_old)。

Solve：

o_new =
Li_2^{-1}(
    q*Li_2(o_old)
)。

Implement monotonic bisection。

Do not use an approximate neural inverse。

```python
def invert_monotonic_bisection(
    target: float,
    fn,
    lo: float,
    hi: float,
    n_iter: int = 100,
) -> float:
    f_lo = fn(lo)
    f_hi = fn(hi)

    if target < f_lo or target > f_hi:
        raise ValueError(
            f"target {target} outside "
            f"[{f_lo}, {f_hi}]"
        )

    for _ in range(n_iter):
        mid = 0.5*(lo+hi)
        value = fn(mid)

        if value < target:
            lo = mid
        else:
            hi = mid

    return 0.5*(lo+hi)
````

Vectorize safely。

Output policy：

B3_KIOT_CONTINUOUS。

============================================================
九、CUDA-Aware Kernel Integral
============================

Actual local rasterizer：

alpha =
min(
ALPHA_MAX,
o*g
)

and skips：

alpha < ALPHA_SKIP。

Define：

Phi_cuda(o)

=

2*pi
integral over g
of：

psi(
min(ALPHA_MAX,o*g)
)

/g

for accepted alpha：

min(ALPHA_MAX,o*g)

> =ALPHA_SKIP。

For：

o <= ALPHA_SKIP：

Phi_cuda(o)=0。

For：

ALPHA_SKIP < o <= ALPHA_MAX：

Phi_cuda(o)

=

2*pi
[
Li_2(o)
-------

Li_2(ALPHA_SKIP)
]。

For：

o > ALPHA_MAX：

Phi_cuda(o)

=

2*pi
[
Li_2(ALPHA_MAX)
---------------

Li_2(ALPHA_SKIP)

*

(
-log(1-ALPHA_MAX)
)
*
log(
o/ALPHA_MAX
)
]。

Implement this exact piecewise function。

Output：

phi_cuda_definition.md。

============================================================
十、Phi_cuda Numerical Integration Validation
===========================================

Independently integrate：

2*pi
integral over r

using high-resolution numerical quadrature。

Formal opacity test values：

1e-4

ALPHA_SKIP*0.9

ALPHA_SKIP

ALPHA_SKIP*1.1

0.01

0.05

0.1

0.25

0.5

0.75

0.9

0.99

0.995

0.999

0.9999。

Compare：

analytic piecewise Phi_cuda

vs numerical radial integration。

Output：

phi_cuda_numerical_validation.csv。

Gate：

relative error <=1e-6

for Phi>1e-10

absolute error <=1e-10 otherwise。

If FAIL：

STOP。

============================================================
十一、CUDA-Aware KIOT
==================

Desired：

Phi_cuda(o_new)

=
q*Phi_cuda(o_old)。

Solve monotonically：

o_new =
Phi_cuda^{-1}(
q*Phi_cuda(o_old)
)。

Use bisection。

For target=0：

o_new=0。

Output policy：

B4_KIOT_CUDA_AWARE。

============================================================
十二、Scalar Policies
==================

Formal policies：

B0_P2_FIXED：

P2 full-affine geometry

tau unchanged。

---

B1_TAU_JS：

P2 geometry

tau_new =
q*tau。

This is existing P3。

---

B2_OPACITY_LINEAR：

P2 geometry

o_new =
q*o。

tau_new =
-log1p(-o_new)。

This is the low-kernel-amplitude
first-order baseline。

Do NOT call method。

---

B3_KIOT_CONTINUOUS：

Phi_cont transport。

---

B4_KIOT_CUDA_AWARE：

Phi_cuda transport。

---

B5_FREE_TAU_ORACLE_DIAGNOSTIC：

NOT a method。

Use the existing patch/free-scalar
capacity result
as oracle upper-bound evidence。

Additionally，
if feasible without protocol change，
run one FULL-CARRIER free-tau oracle
as Section 24。

============================================================
十三、Analytic Limiting Cases
==========================

Verify for every representative tau：

q=1：

all B1/B2/B3/B4
return original opacity/tau。

For o→0：

B1 opacity ratio approaches q

B2 exact opacity ratio=q

B3 opacity ratio approaches q

B4 above skip region
reports actual behavior。

For B3：

verify：

Phi_cont(o_new)
/
Phi_cont(o_old)
===============

q。

For B4：

verify：

Phi_cuda(o_new)
/
Phi_cuda(o_old)
===============

q

when Phi_old>0。

Output：

kiot_limiting_case_tests.md。

============================================================
十四、R1 Isolated Scalar Oracle Reuse
==================================

Load：

isolated_scalar_opacity_oracle_fit.csv

from Stage3.4C-R1。

For each Gaussian-camera group：

canonical opacity o

q

best scalar opacity o_fit

P3 opacity

and required kernel sample data
if available。

Compute candidate opacity：

B2

B3

B4。

Using EXACT SAME isolated samples
from R1，

compute RMSE：

RMSE_P3

RMSE_OPACITY_LINEAR

RMSE_KIOT_CONT

RMSE_KIOT_CUDA

RMSE_BEST_SCALAR。

Output：

isolated_candidate_scalar_comparison.csv。

============================================================
十五、Oracle-Gain Recovery
=======================

For candidate X：

GAIN_RECOVERY_X =

(
RMSE_P3
-------

RMSE_X
)
/
(
RMSE_P3
-------

RMSE_BEST_SCALAR
+
1e-12
)。

Clip only for reporting：

do NOT clip before Gate calculation。

Report per group。

Output：

isolated_oracle_gain_recovery.csv。

Define：

ISOLATED ANALYTIC SCALAR SUPPORTED
for a candidate iff：

median gain recovery >=0.80

AND

> =75% groups
> gain recovery >=0.50

AND

candidate median RMSE
<
P3 median RMSE。

============================================================
十六、Carrier-Wide Policy Input Audit
==================================

For all 10 deformation states：

generate B0-B4
using EXACT SAME P2 geometry。

Assert：

xyz identical。

Sigma identical。

color identical。

Only tau/opacity differs。

Output：

kiot_policy_input_manifest.csv。

For area-preserving states：

shear_k020
shear_k040
twist_60

q=1。

Require B1/B2/B3/B4：

tau identical to B0

max diff <=1e-8。

============================================================
十七、Fresh Rendering
==================

Policies：

B0_P2_FIXED

B1_TAU_JS

B2_OPACITY_LINEAR

B3_KIOT_CONTINUOUS

B4_KIOT_CUDA_AWARE。

10 states。

3 frozen cameras。

One shared canonical alpha。

Fresh current renderer。

Save：

alpha/<policy>/<state>/<camera>.npy。

Output：

kiot_render_manifest.csv。

============================================================
十八、Frozen Metric Evaluation
===========================

Use EXACT Stage3.4C：

frozen camera keys

frozen cell keys

current metric

Q_tau。

No policy-specific rejection。

Output：

kiot_cell_camera_response.csv

kiot_cell_response.csv。

Q_tau must be identical
across all candidates：

max diff <=1e-12。

============================================================
十九、Central Response
===================

For every policy/state：

n

median R

Q median

central error

R p05
p25
p75
p95。

Output：

kiot_central_response.csv。

Formal area-changing states：

stretch_1.25
stretch_1.50
stretch_2.00
biaxial_1.50
cubic_l020
cubic_l0333。

Report mean central error。

============================================================
二十、Primary Restoration Comparison
=================================

Compare：

B0 P2 fixed

B1 tau/Js

B2 opacity-linear

B3 KIOT continuous

B4 KIOT CUDA-aware。

For six area-changing states：

report full state table。

Output：

kiot_restoration_comparison.csv。

For candidate X：

IMPROVEMENT_FROM_P2 =

(
mean_error_P2
-------------

mean_error_X
)
/
mean_error_P2。

Formal KERNEL-INTEGRATED RESTORATION SUPPORTED
for B3 or B4 iff：

A.

six-state mean central error <=0.10

B.

improvement from P2 >=0.60

C.

stretch2 central error <=0.08

D.

at least 5/6 states：

candidate central error
<
B0 P2 central error。

Do NOT modify thresholds。

============================================================
二十一、Compare B3 vs B4
====================

Output：

continuous_vs_cuda_kiot.csv。

Per state：

B3 median R

B4 median R

B3 central error

B4 central error

absolute response difference。

If B4 mean central error
is at least 10% lower than B3：

CUDA-AWARE BENEFIT SUPPORTED。

Otherwise：

continuous kernel integral
is sufficient under this carrier。

No threshold changes。

============================================================
二十二、Tail Metrics
================

For audit states：

stretch_2.00

cubic_l0333

shear_k040

twist_60。

Report：

median E_log

p90

p95

p99

factor2 fraction

factor5 fraction

factor10 fraction

raw MAE。

Output：

kiot_tail_severity.csv。

============================================================
二十三、Paired Tail Comparison
==========================

Compare：

B4 vs B0

B4 vs B1

B3 vs B0。

Same frozen cells。

Per-cell：

Delta Elog。

Bootstrap：

10000 resamples

seed 20260713。

Output：

kiot_paired_tail_comparison.csv。

Analytic transport must NOT be called
tail-improving unless：

in >=3/4 states：

p95 Elog <=0.75 baseline

factor2 <=0.75 baseline

paired median Delta CI upper <0。

Otherwise：

TAIL MIXED/WEAK。

============================================================
二十四、Full-Carrier Free-Tau Oracle Diagnostic
===========================================

This is OPTIONAL but strongly preferred。

Name：

B5_FULL_FREE_TAU_ORACLE。

Geometry：

P2 FULL_AFFINE_COV。

1681 Gaussian tau scalars free。

xyz fixed。

Sigma fixed。

color fixed。

No densification。

No pruning。

No Gaussian addition/removal。

Training target：

the CURRENT physical
sample-level optical target。

For every frozen cell-camera Q7 sample：

tau_target_k =
q_k * tau_can_k。

Render deformed white-pass alpha
with current differentiable renderer。

Sample predicted alpha
at frozen deformed material projections。

Convert：

tau_pred =
alpha_to_tau(
alpha_pred_sample
)。

Loss：

mean over valid frozen samples：

(
log(
tau_pred +1e-6
)
-

log(
tau_target +1e-6
)
)^2。

Optimize only：

1681 tau_raw or positive tau parameter。

Initialize：

B4 KIOT_CUDA tau。

Adam：

lr=5e-3

max 3000 steps

seed 20260713。

No regularizer。

Early stop：

relative loss reduction <1e-6
over 200 steps。

This is：

FULL-CARRIER SCALAR CAPACITY ORACLE。

NOT METHOD。

Save：

full_free_tau_oracle_curve.csv

full_free_tau_oracle_metrics.csv。

Compare：

B0
B1
B4
B5。

If B5 six-state mean central error <=0.05：

FULL SCALAR CAPACITY SUPPORTED。

If additionally tail p95
substantially improves：

record only。

============================================================
二十五、Method Decision Gates
=========================

T0:
PROTOCOL LOCK

PASS。

---

T1:
PHI CUDA IMPLEMENTATION

analytic/numerical validation PASS。

---

T2:
ISOLATED ANALYTIC SCALAR

For B3/B4：

SUPPORTED / NOT SUPPORTED。

---

T3:
FULL-CARRIER CENTRAL RESTORATION

B3/B4 candidate：

SUPPORTED / NOT SUPPORTED。

---

T4:
CUDA-AWARE BENEFIT

SUPPORTED / NOT SUPPORTED。

---

T5:
TAIL EFFECT

IMPROVEMENT
WORSENING
MIXED/WEAK。

---

T6:
FULL SCALAR CAPACITY

SUPPORTED
NOT SUPPORTED
NOT EVALUATED。

============================================================
二十六、Final CASE
==============

CASE ANALYTIC-KIOT-SUPPORTED:

T0 PASS
T1 PASS

and either B3 or B4：

T2 SUPPORTED
T3 SUPPORTED。

Conclusion：

The failure of tau/Js
is caused by transporting
Gaussian-center optical depth
instead of the effective optical contribution
distributed across the Gaussian kernel。

A kernel-integrated scalar transport：

Phi(o') =
(1/Js) Phi(o)

substantially restores
the physical central optical response
after full covariance transport。

If B4 wins：

the local rasterizer's skip/clamp semantics
provide additional measurable benefit。

ALLOW：

promote KIOT
to first candidate method。

Do NOT add MLP yet。

---

CASE ANALYTIC-RULE-PARTIAL-SCALAR-CAPACITY:

T2 or T3 NOT SUPPORTED

but

T6 SUPPORTED。

Conclusion：

scalar optical-state capacity exists，

but the global kernel-integral rule
does not fully explain
the scene-level scalar correction。

ALLOW NEXT：

small kernel-statistic residual model

or local overlap-aware scalar transport。

Do NOT use spatial opacity texture yet。

---

CASE SCALAR-CAPACITY-NOT-SUPPORTED:

T6 NOT SUPPORTED。

Reconsider richer optical representation。

---

CASE PROTOCOL-FAIL:

T0/T1 FAIL。

============================================================
二十七、Required Visualizations
===========================

Use matplotlib。

No seaborn。

No manually specified colors。

Each figure separate。

1.

phi_cont_vs_opacity.png

2.

phi_cuda_vs_opacity.png

3.

transported_opacity_vs_q.png

4.

isolated_rmse_by_scalar_rule.png

5.

isolated_oracle_gain_recovery.png

6.

central_response_scalar_policies.png

7.

central_error_scalar_policies.png

8.

stretch2_scalar_policy_distribution.png

9.

tail_p95_scalar_policies.png

10.

factor2_scalar_policies.png

If B5：

11.

full_free_tau_oracle_curve.png

============================================================
二十八、Required Files
==================

stage3_5A_protocol_lock.json
kiot_renderer_constants.json

opacity_tau_roundtrip_test.md

li2_implementation_audit.md
phi_cuda_definition.md
phi_cuda_numerical_validation.csv
kiot_limiting_case_tests.md

isolated_candidate_scalar_comparison.csv
isolated_oracle_gain_recovery.csv

kiot_policy_input_manifest.csv
kiot_render_manifest.csv

kiot_cell_camera_response.csv
kiot_cell_response.csv

kiot_central_response.csv
kiot_restoration_comparison.csv
continuous_vs_cuda_kiot.csv

kiot_tail_severity.csv
kiot_paired_tail_comparison.csv

if B5 executed：
full_free_tau_oracle_curve.csv
full_free_tau_oracle_metrics.csv

kernel_integrated_opacity_transport_report.md
stage3_5A_summary.md
stage3_5A_log.txt

============================================================
二十九、报告必须中文回答
============

A. 为什么 Stage3.4C-R1
允许进入 scalar method design。

B. 为什么不能直接增加
spatial/view-dependent opacity。

C. KIOT transport 的物理/renderer motivation。

D. Li_2 为什么出现。

E. 为什么 Li_2 本身
不能作为 novelty claim。

F. Phi_cont formula。

G. Phi_cuda piecewise formula。

H. Phi_cuda numeric validation error。

I. B1 tau/Js exact semantic。

J. B2 opacity-linear exact/asymptotic semantic。

K. B3 continuous KIOT semantic。

L. B4 CUDA-aware KIOT semantic。

M. isolated P3 median RMSE。

N. isolated opacity-linear RMSE。

O. isolated KIOT continuous RMSE。

P. isolated KIOT CUDA RMSE。

Q. best scalar oracle RMSE。

R. B3 gain recovery。

S. B4 gain recovery。

T. T2。

U. stretch1.25 all policy median R/Q。

V. stretch1.5 all policy median R/Q。

W. stretch2 all policy median R/Q。

X. biaxial1.5 all policy median R/Q。

Y. cubic020 all policy median R/Q。

Z. cubic0333 all policy median R/Q。

AA. six-state mean central errors。

AB. P2→B3 improvement。

AC. P2→B4 improvement。

AD. T3。

AE. B3 vs B4 benefit。

AF. T4。

AG. tail p95/factor2 comparison。

AH. T5。

AI. full free-tau oracle result。

AJ. T6。

AK. Final CASE。

AL. strongest scientific conclusion。

AM. KIOT 是否可升格为 candidate method。

AN. 下一步是否允许
在真实 transparent reconstruction carrier
上集成。

============================================================
三十、README
=========

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage3.4C-R1 repaired the S2 reference-key mismatch
and strictly confirmed the central-cancellation Gate
in all 6/6 area-changing states。

It also proved that
tau/Js is not merely implemented incorrectly。

Under the actual rasterizer semantics：

alpha =
opacity * Gaussian kernel amplitude

before transmittance compositing。

tau/Js is exact at the Gaussian center
but kernel-nonexact off center，
especially for high-opacity Gaussians。

Despite this，
the best scalar opacity oracle
recovers approximately 97.6% RMSE
relative to tau/Js，
and a free-tau patch oracle
supports local scalar-state capacity。

Therefore the next method hypothesis is：

transport the kernel-integrated
effective optical contribution
rather than Gaussian-center optical depth。

Stage3.5A tests：

Phi(o') =
(1/Js) Phi(o)

using continuous
and local-CUDA-aware Gaussian kernel integrals。

The dilogarithm itself
is not claimed as novel；
Gaussian Point Splatting 2026
also uses Li_2 for a different
Gaussian opacity-correction problem。

The research contribution under test is
deformation-aware transport
of kernel-integrated optical state。

============================================================
三十一、实际执行
========

必须实际：

lock Stage3.4C/R1 protocol

read renderer constants

verify opacity/tau roundtrip

implement Li2

implement Phi_cont

implement Phi_cuda

numerically validate Phi_cuda

implement inverse transport

build B0-B4 scalar policies

run limiting-case tests

reuse R1 isolated scalar groups

compare P3 / opacity-linear / KIOT / best scalar

compute oracle gain recovery

run carrier-wide input audit

fresh render B0-B4

frozen-key metric evaluation

central restoration comparison

B3 vs B4

tail paired comparison

optional full-carrier free-tau oracle

T0-T6

Final CASE

不要：

MLP

new optical representation

view-dependent opacity

spatial opacity texture

change covariance

change metric

change frozen keys

change q

modify T thresholds。

============================================================
三十二、最终终端总结
==========

最后只输出：

1. T0 protocol lock
2. Phi_cuda numerical validation max error
3. T1
4. P3 isolated median RMSE
5. opacity-linear isolated median RMSE
6. KIOT-cont isolated median RMSE
7. KIOT-CUDA isolated median RMSE
8. best-scalar isolated median RMSE
9. KIOT-cont median gain recovery
10. KIOT-CUDA median gain recovery
11. T2 isolated analytic scalar
12. stretch1.25 B0/B1/B2/B3/B4/Q
13. stretch1.5 B0/B1/B2/B3/B4/Q
14. stretch2 B0/B1/B2/B3/B4/Q
15. biaxial1.5 B0/B1/B2/B3/B4/Q
16. cubic020 B0/B1/B2/B3/B4/Q
17. cubic0333 B0/B1/B2/B3/B4/Q
18. six-state mean central errors
19. P2→KIOT-cont improvement
20. P2→KIOT-CUDA improvement
21. T3 central restoration
22. CUDA-aware benefit
23. T4
24. tail effect
25. T5
26. full free-tau capacity
27. T6
28. Final CASE
29. Strongest scientific conclusion
30. KIOT candidate method yes/no
31. Can integrate into real transparent carrier yes/no
32. kernel_integrated_opacity_transport_report.md path
33. stage3_5A_summary.md path

请直接实际运行。

最高优先级：

DO NOT ADD AN MLP YET。

THE EXPERIMENT HAS ALREADY SHOWN
THAT SCALAR STATE CAPACITY EXISTS。

TAU/JS IS THE WRONG SCALAR TRANSPORT RULE。

TEST TRANSPORT OF THE
KERNEL-INTEGRATED EFFECTIVE OPTICAL CONTRIBUTION。

PHI_CONT(O)=2*PI*LI2(O)。

ALSO TEST THE ACTUAL CUDA-AWARE
SKIP/CLAMP PIECEWISE INTEGRAL。

DO NOT CLAIM LI2 ITSELF AS NOVEL。

THE NOVELTY QUESTION IS
DEFORMATION-AWARE TRANSPORT OF
KERNEL-INTEGRATED OPTICAL STATE。

ONLY IF ANALYTIC KIOT FAILS
BUT FREE-TAU CAPACITY REMAINS，
MOVE TO A SMALL KERNEL-STATISTIC RESIDUAL MODEL。
PROMPT

```

# 我现在最真实的评价

这次我比第四十二步更乐观。

因为第四十二步只是：

> “有个奇怪现象。”

第四十三步已经把问题切得很清楚：

\[
\boxed{
\text{scalar state 有容量}
}
\]

但：

\[
\boxed{
\tau/J_s\text{ 传输规则错了}
}
\]

这两个结论的组合非常关键。

而且 OMG 已经从材料建模角度指出 3DGS opacity 与物理材料属性之间存在被忽略的依赖关系；标准 3DGS 的 alpha 又明确是 overall opacity 与二维 Gaussian 权重的乘积。:contentReference[oaicite:7]{index=7}

所以你的故事现在可以逐渐变成：

> **连续介质运动学告诉我们如何传输 Gaussian 几何，但并没有告诉我们如何传输 Gaussian splatting 参数语义下的光学状态。物理厚度直接缩放 optical depth，只在 Gaussian 中心严格成立；在整个 splat kernel 上，应该传输 kernel-integrated optical contribution。**

这已经是一个**方法问题**，不是继续找现象的问题。

现在别上网络。

**先跑 KIOT。**

要是 Stage 3.5A 真出现：

\[
P2:0.955
\rightarrow
KIOT:0.50\sim0.58
\]

而 `tau/Js` 还是 2.73——

那我会非常认真地告诉你：

> **这个方向已经有论文主线了。**
```

[1]: https://openaccess.thecvf.com/content/CVPR2024/papers/Xie_PhysGaussian_Physics-Integrated_3D_Gaussians_for_Generative_Dynamics_CVPR_2024_paper.pdf "PhysGaussian: Physics-Integrated 3D Gaussians for Generative Dynamics"
[2]: https://github.com/graphdeco-inria/diff-gaussian-rasterization/blob/main/cuda_rasterizer/forward.cu "diff-gaussian-rasterization/cuda_rasterizer/forward.cu at main · graphdeco-inria/diff-gaussian-rasterization · GitHub"
[3]: https://arxiv.org/abs/2411.18625 "[2411.18625] Textured Gaussians for Enhanced 3D Scene Appearance Modeling"
[4]: https://publications.graphics.tudelft.nl/papers/854?utm_source=chatgpt.com "Gaussian Point Splatting"
[5]: https://arxiv.org/abs/2502.10988 "OMG: Opacity Matters in Material Modeling with Gaussian Splatting"
