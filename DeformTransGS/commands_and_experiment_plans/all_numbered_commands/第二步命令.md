这次先别被 `CASE ATTRIBUTE-ORACLE-INSUFFICIENT` 吓到。

我认真看完后，结论非常明确：

> **Stage 4.0 的 Final CASE 判错了。**
>
> 不是实验结果不好，而是 **A3 Gate 本身存在数学上的逻辑冲突。**

而且除此之外，还有两个分析协议 bug。

---

# 一、最严重的问题：A3 几乎被 MAT0 设计成“必然失败”

原 A3 要求，对至少：

[
29/36
]

个 case，同时满足：

[
E_{\rm FULL}\le0.15
]

以及：

[
E_{\rm FULL}\le0.5E_0
]

问题来了。

MAT0 是什么？

> 固定厚度材料。

Stage 4.0 又得到：

[
\text{MAT0 geometry-only sufficient fraction}=1.0
]

也就是 12/12 MAT0 case：

> **只传输几何已经够了。**

那么理论上：

[
E_0\approx E_{\rm FULL}
]

此时要求：

[
E_{\rm FULL}\le0.5E_0
]

意味着 FULL 必须比一个已经“足够正确”的 geometry-only 再降低 50%。

这是矛盾的。

更致命的是：

MAT0 有：

[
12
]

个 case。

即使全部 MAT1 + MAT2：

[
24
]

个 case 都满足 A3，最高也只有：

[
24/36=66.7%
]

而 A3 要：

[
80%=29/36
]

所以：

[
\boxed{
\text{如果 MAT0 真符合预期，A3 从设计上就不可能 PASS}
}
]

这不是 oracle 不足。

这是：

> **A3-PROTOCOL-LOGIC-BUG**

因此：

```text
CASE ATTRIBUTE-ORACLE-INSUFFICIENT
```

当前必须撤销。

同样：

```text
new main hypothesis supported: NO
```

也必须撤销。

现在正确状态应该是：

> **ATTRIBUTE-DYNAMICS-PROVISIONALLY-SUPPORTED，等待协议修复。**

---

# 二、现在的原始结果反而非常漂亮

你看三个材料机制。

## MAT0：固定厚度

几何充分率：

[
1.0
]

属性必要性：

```text
O = false
C = false
V = false
```

分类：

```text
STATIC-OPTICAL-STATE-SUFFICIENT
```

意思是：

> 固定厚度材料形变以后，只要几何正确变化，静态光学属性已经够用。

---

## MAT1：质量守恒、中性色

几何充分率：

[
0
]

属性：

```text
O = true
C = false
V = false
```

12 个 case：

```text
R1_O = 12
```

也就是说：

> **12/12 全部只释放 scalar opacity（标量不透明度）就达到最小充分。**

这个结果其实相当干净。

它说明：

[
\boxed{
\text{质量守恒中性半透明材料需要动态标量光学状态}
}
]

而不需要：

* 动态颜色；
* 视角相关 opacity residual。

这恰恰支持我们最早的一个直觉。

只是现在我们没有先假定 (J_s) 是答案。

---

## MAT2：质量守恒、有色材料

几何充分率：

[
0
]

属性：

```text
O = true
C = true
V = true
```

12/12：

```text
R7_O_C_V_FULL
```

分类：

```text
MULTI-ATTRIBUTE-STATE-NECESSARY
```

这说明：

> 一旦材料具有通道相关吸收，仅动态一个 scalar opacity 很可能就不够。

可能需要：

* 光学强度；
* 通道/颜色状态；
* 视角相关状态。

这其实比 KIOT 主线有意思得多。

---

# 三、第二个 bug：MAT0 的“minimal sufficient = R7”明显语义错误

你的结果：

```text
MAT0 geometry-only sufficient = 1.0
```

但：

```text
MAT0 minimal sufficient histogram:
R7_O_C_V_FULL = 12
```

这两句话放在一起明显矛盾。

原因在原 protocol。

我们定义 minimal sufficient subset 时只搜索：

```text
R1
R2
R3
R4
R5
R6
R7
```

**没把 R0 放进去。**

所以即使：

> 什么动态属性都不需要，

算法也必须强行从 R1-R7 选一个。

这就导致 MAT0：

> geometry-only 明明足够，却被标成 FULL O+C+V。

正确逻辑应该是：

第一步：

[
E_0\le1.10E_{\rm FULL}
]

则：

```text
MINIMAL = R0_NONE
```

即：

> 无动态属性。

只有 R0 不充分，才进入 R1-R7 搜索。

所以当前：

```text
MAT0 R7=12
```

应该撤销。

大概率正确结果会是：

```text
MAT0 R0_NONE = 12
```

如果真是这样，你的第一张核心表将非常漂亮：

| 材料机制   | 最小动态属性 |
| ------ | ------ |
| 固定厚度   | NONE   |
| 质量守恒中性 | O      |
| 质量守恒有色 | O+C+V  |

这已经有很明显的科学故事了。

---

# 四、第三个 bug：association 全是 0 是统计分组错了

你现在输出：

```text
Js=0.000
logJs=0.000
detF=0.000
```

并不是证明：

> oracle 属性变化和形变无关。

原因是当前 Spearman correlation（斯皮尔曼秩相关）按：

```text
material / surface / deformation
```

分组。

例如：

```text
MAT0
S0_PLANAR
D1_STRETCH_X_1P25
```

在这一组里面：

[
F
]

是固定的。

所以：

[
\det F
]

对所有 4096 Gaussian 都完全相同。

singular values（奇异值）：

[
s_1,s_2,s_3
]

也完全相同。

Planar sheet 的 normal 还全部一样，因此：

[
J_s
]

也完全一样。

你拿：

> 一个常数序列

和：

[
\Delta O
]

做 Spearman。

相关系数数学上是：

> undefined，未定义。

程序很可能把 NaN：

```text
nan
```

填成了：

```text
0
```

所以当前 T/U/V：

> **全部没有科学意义。**

正确做法应该至少按：

```text
material + surface
```

跨 D1-D6 pooled analysis（合并分析）。

这样：

[
J_s
]

[
\det F
]

[
s_1,s_2,s_3
]

才有变化。

association 分析必须重算。

---

# 五、我的当前真实判断

我现在不是想鼓励你。

而是客观地说：

> **Stage 4.0 的原始结果比 Final CASE 好得多。**

现在最值得关注的不是：

[
R7=0.122754
]

而是这个结构：

```text
MAT0:
geometry sufficient
O/C/V none necessary

MAT1:
geometry insufficient
O necessary
O minimal 12/12

MAT2:
geometry insufficient
O/C/V necessary
FULL minimal 12/12
```

这几乎正是我们设计“三种材料机制”的目的。

如果协议修复后仍保持这个结构，新主线：

> **What Must Deform?**

就是真的有东西。

---

# 现在不要跑 Stage 4.1

先做：

# Stage 4.0-R1：Oracle Gate and Attribute Classification Protocol Repair

中文：

> **Oracle 上限与属性分类协议修复**

这次原则是：

> **优先复用已经完成的 288 个真实 oracle job。**

不重新跑 288 个训练。

先验证结果来源，再重新计算 Gate 和分类。

只有 full oracle 真正存在容量问题，才补跑 R7。

直接执行：

```bash
cd /data/wyh/DeformTransGS

CUDA_VISIBLE_DEVICES=2,3 codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
最高优先级：修正 Stage4.0 Protocol Logic
============================================================

Stage4.0 reported：

A0 PASS
A1 PASS
A2 PASS
A3 FAIL
A4 PASS

Final CASE：

ATTRIBUTE-ORACLE-INSUFFICIENT。

This Final CASE is NOT ACCEPTED。

The reported：

"new main hypothesis supported = NO"

is also NOT ACCEPTED。

============================================================
发现的 A3 逻辑错误
============================================================

Original A3 required：

for at least29/36 cases：

R7 E_OPT <=0.15

AND

R7 E_OPT <=0.50 * R0 E_OPT。

However the benchmark explicitly contains：

12 MAT0_NEUTRAL_FIXED_THICKNESS cases。

MAT0 is a fixed-thickness control。

Stage4.0 measured：

MAT0 geometry-only sufficient fraction =
1.000。

Thus for MAT0：

R0 approximately R7

is the EXPECTED successful result。

Requiring：

R7 <=0.5 R0

contradicts the intended
STATIC-OPTICAL-STATE-SUFFICIENT control。

There are12 MAT0 cases。

Even if all24 MAT1/MAT2 cases
passed the 50%-improvement condition，

maximum possible passing fraction
would be：

24/36 =
66.7%

which is below the original80% A3 Gate。

Therefore：

A3 FAIL is caused by a
PROTOCOL LOGIC BUG。

Formal label：

A3-IMPROVEMENT-CONDITION-INCOMPATIBLE-WITH-STATIC-CONTROL。

Do NOT change numerical thresholds
to force a result。

Remove the logically incompatible
improvement requirement
from the ORACLE CAPACITY Gate。

============================================================
第二个协议错误
============================================================

Stage4.0 reported：

MAT0 geometry-only sufficient fraction =
1.000

but minimal sufficient histogram：

R7_O_C_V_FULL =12。

The original minimal-subset search
excluded R0_GEOMETRY_ONLY
from candidate minimal groups。

Therefore a case requiring
NO dynamic attribute
was forced to select R1-R7。

This is a classification semantic bug。

Formal label：

MINIMAL-SUBSET-MISSING-NONE-STATE。

============================================================
第三个分析错误
============================================================

Stage4.0 association analysis grouped by：

material / surface / deformation。

Within a fixed global-affine deformation：

detF is constant

singular values of F are constant。

For S0 planar surface：

normal is constant

therefore Js is also constant
within the deformation group。

Spearman association against
a constant feature is undefined。

The Stage4.0 summary reported many：

0.000

associations。

These zeros must NOT be interpreted
as zero scientific association。

Trace whether NaN/undefined correlations
were replaced by zero。

Formal issue：

CONSTANT-FEATURE-SPEARMAN-GROUPING-BUG。

============================================================
Stage4.0 raw observations to preserve
============================================================

Do NOT modify or delete original outputs。

R0-R7 mean E_OPT：

R0_GEOMETRY_ONLY =
0.298901

R1_O =
0.198602

R2_C =
0.2975703333

R3_V =
0.2662996667

R4_O_C =
0.196606

R5_O_V =
0.1640046667

R6_C_V =
0.2353616667

R7_O_C_V_FULL =
0.122754。

Geometry-only sufficient fractions：

MAT0 =
1.000

MAT1 =
0.000

MAT2 =
0.000。

Original diagnostic necessity：

MAT0：
O false
C false
V false

MAT1：
O true
C false
V false

MAT2：
O true
C true
V true。

Original regime classifications：

MAT0：
STATIC-OPTICAL-STATE-SUFFICIENT

MAT1：
SCALAR-OPACITY-DYNAMIC-STATE-SUFFICIENT

MAT2：
MULTI-ATTRIBUTE-STATE-NECESSARY。

These remain DIAGNOSTIC ONLY
until Stage4.0-R1 closes the protocol。

============================================================
本阶段名称
============================================================

Stage 4.0-R1:

Oracle Capacity
and Attribute Classification Protocol Repair

中文：

Oracle 上限能力
与动态属性分类协议修复

============================================================
唯一科学目标
============================================================

Using the already-completed
Stage4.0 oracle artifacts：

1. verify all288 jobs were real optimization jobs

2. verify metrics were loaded
from saved rendered outputs/checkpoints

3. repair the logically invalid A3 Gate

4. introduce an explicit NONE dynamic-state class

5. recompute attribute necessity
only on oracle-valid cases

6. recompute scientifically valid
deformation-attribute associations

7. determine whether the new
AttributeDeformGS hypothesis
is actually supported。

Do NOT design a transport model。

============================================================
禁止事项
============================================================

禁止：

KIOT

new transport formula

MLP

new attribute family

change O/C/V definitions

change GT renderer

change benchmark surfaces

change materials

change deformations

change camera split

rerun all288 jobs by default

delete Stage4.0 outputs

manually edit metrics

change E_OPT weights

tune thresholds after seeing
the repaired classification。

============================================================
输出目录
============================================================

Create：

/data/wyh/DeformTransGS/experiments/
stage4_0_R1_oracle_attribute_protocol_repair/

All new code：

/data/wyh/DeformTransGS/attribute_study/analysis/

Do not overwrite Stage4.0。

============================================================
一、Protocol Lock
============================================================

Lock Stage4.0：

protocol lock

GT files

camera lock

canonical checkpoints

canonical metrics

288 oracle checkpoints

oracle_run_manifest.csv

attribute_release_metrics.csv

attribute_release_primary_error.csv

oracle_attribute_delta

oracle view diagnostic。

Record SHA256 / mtime / size。

Output：

stage4_0_R1_protocol_lock.json。

Gate B0。

============================================================
二、Oracle Job Provenance Audit
============================================================

Trace exact code that generated：

canonical metrics

oracle checkpoints

oracle test renders

attribute_release_metrics.csv

attribute_release_primary_error.csv。

Output：

oracle_provenance_trace.md。

Must answer：

A.

Were R1-R7 tensors actually optimized
with autograd and Adam？

B.

Did every release group expose
only its declared tensors？

C.

Were TEST cameras excluded
from optimization？

D.

Were release jobs initialized
from the SAME canonical checkpoint？

E.

Were metrics computed
from fresh rendered TEST outputs？

F.

Any synthetic metric generation？

G.

Any hardcoded target E_OPT values？

Search source for：

synthetic

fake

mock

target_metric

preset_metric

expected_metric

hardcoded_psnr

hardcoded_elog

surrogate_result。

Output：

oracle_source_search.txt。

Formal B1 PASS iff：

real optimization = YES

declared release semantics = YES

test leakage = NO

fresh render metrics = YES

synthetic metric generation = NO。

============================================================
三、288 Job Artifact Completeness
============================================================

Expected：

2 surfaces
x3 materials
x6 deformations
x8 releases
=
288。

For every job verify：

manifest row

checkpoint exists for R1-R7

R0 render/metric artifact exists

test metric row exists

finite E_OPT

surface/material/deformation/release key unique。

Output：

oracle_artifact_completeness.csv。

Gate B2 PASS iff：

288/288 complete

all E_OPT finite

no duplicate keys

no missing keys。

============================================================
四、Per-Case Full Oracle Table
============================================================

Create：

full_oracle_case_audit.csv。

For all36 cases report：

surface
material
deformation

R0 E_OPT
R7 E_OPT

R7/R0 ratio

R7 <=0.15

R0 <=1.10*R7

GT optical-change magnitude。

Define GT optical-change magnitude：

using TEST-camera GT only：

compare D0 GT tau_rgb
against deformed-state GT tau_rgb

on material-coordinate matched
surface samples / valid projected support。

Do NOT use image pixel correspondence
without material mapping。

Use the existing material anchor identity
or benchmark surface parameter identity。

Define：

GT_CHANGE =

median absolute log ratio
of deformed vs canonical
tau_rgb

over valid material samples
and test cameras。

Output the exact implementation
in：

gt_change_metric_definition.md。

This metric is diagnostic。

============================================================
五、Repaired Full Oracle Capacity Gate
============================================================

The ORACLE CAPACITY question is：

Can the FULL released attribute state
represent the deformed GT？

It must NOT require improvement over R0
on a static-control case。

Define:

ORACLE_VALID_CASE iff：

R7 E_OPT <=0.15。

No R0 improvement clause。

This uses the SAME absolute0.15
already frozen in original Stage4.0 A3。

No numerical threshold is changed。

Repaired A3：

A3R PASS iff：

at least29/36 cases
are ORACLE_VALID_CASE

AND

each material regime has
at least10/12 ORACLE_VALID_CASES。

This preserves the original
approximately80% capacity requirement
while preventing one material regime
from being completely unsupported。

Output：

repaired_oracle_capacity_gate.json。

Report：

overall valid count

MAT0 count

MAT1 count

MAT2 count。

============================================================
六、Conditional R7 Optimization Closure
============================================================

Execute ONLY if A3R initially FAILS。

Do NOT rerun R0-R6。

Identify cases with：

R7 E_OPT >0.15。

For those cases only：

audit original R7 optimization：

train-loss curve

last500 iteration slope

best train loss iteration

final iteration

early-stop reason

gradient norm statistics
if logged。

Output：

r7_failed_case_convergence_audit.csv。

Classify each failed case：

CONVERGED-HIGH-ERROR

or

OPTIMIZATION-NOT-CONVERGED。

A case is OPTIMIZATION-NOT-CONVERGED iff：

final500-iteration
relative train-loss reduction >=0.05

OR

best loss occurs in the final10%
of iterations

without patience convergence。

For OPTIMIZATION-NOT-CONVERGED cases ONLY：

rerun R7 from the SAME canonical checkpoint

same released attributes O+C+V

same train cameras

same loss

same optimizer family

but maximum4000 iterations

fixed patience600。

This is an ORACLE CAPACITY closure，
not release-group comparison。

Save separately：

r7_capacity_closure_manifest.csv。

Metrics from closure may replace R7
ONLY for oracle validity
and FULL reference calculations。

Record both old and closure values。

Do NOT rerun a converged-high-error case。

After closure：

recompute A3R。

If A3R still FAILS：

Final CASE =
ATTRIBUTE-ORACLE-CAPACITY-UNRESOLVED。

STOP。

============================================================
七、Explicit NONE Dynamic-State Classification
============================================================

Define dynamic attribute set：

NONE
O
C
V
O+C
O+V
C+V
O+C+V。

For every ORACLE_VALID case：

FIRST test：

R0_NONE is sufficient iff：

R0 E_OPT <=1.10 * R7 E_OPT。

If TRUE：

minimal sufficient group =

R0_NONE。

Do NOT use gap recovery
because E0-EFULL may approach zero。

If R0 is NOT sufficient：

search R1-R7。

For R1-R7：

sufficient iff：

E_R <=1.10 * E_FULL

AND

gap_recovery >=0.90。

Choose fewest dynamic attribute families。

Tie-break：

R1 O
R2 C
R3 V
R4 O+C
R5 O+V
R6 C+V
R7 O+C+V。

Output：

repaired_minimal_sufficient_attribute_by_case.csv。

============================================================
八、Repaired Attribute Necessity
============================================================

Only ORACLE_VALID cases
participate。

If R0_NONE is sufficient：

O/C/V are all NOT necessary。

Otherwise for attribute A：

BEST_WITHOUT_A =

lowest E_OPT among：

R0 and release groups
not containing A。

Delta_A =

E_BEST_WITHOUT_A -E_FULL。

CASE_NECESSARY iff：

E_BEST_WITHOUT_A
>=1.25 * E_FULL

AND

Delta_A >0.02。

Same numerical criteria
as original Stage4.0。

Output：

repaired_attribute_necessity_by_case.csv。

============================================================
九、Regime Necessity Bootstrap
============================================================

For each regime：

use ORACLE_VALID cases only。

Require at least10 valid cases
for regime classification。

For O/C/V：

bootstrap paired Delta_A

10000 resamples

seed20260714。

REGIME_NECESSARY iff：

>=75% valid regime cases
are CASE_NECESSARY

AND

95% CI lower >0。

Output：

repaired_attribute_necessity_bootstrap.csv。

============================================================
十、Repaired Geometry-Only Sufficiency
============================================================

For ORACLE_VALID cases：

geometry-only sufficient iff：

R0 <=1.10 *R7。

Report per regime：

count
fraction

with denominator =
oracle-valid regime cases。

Output：

repaired_geometry_only_sufficiency.csv。

============================================================
十一、Repaired Regime Classification
============================================================

Use the SAME conceptual classes
but include NONE explicitly。

STATIC-OPTICAL-STATE-SUFFICIENT iff：

geometry-only sufficient
in >=75% oracle-valid cases。

SCALAR-OPACITY-DYNAMIC-STATE-SUFFICIENT iff：

O REGIME_NECESSARY

V NOT REGIME_NECESSARY

and minimal sufficient groups
containing O but not V

occur in >=75%
non-static oracle-valid cases。

VIEW-DEPENDENT-OPTICAL-STATE-NECESSARY iff：

V REGIME_NECESSARY

and fewer than2 attributes
are regime necessary。

APPEARANCE-STATE-NECESSARY iff：

C REGIME_NECESSARY

and fewer than2 attributes
are regime necessary。

MULTI-ATTRIBUTE-STATE-NECESSARY iff：

at least2 of O/C/V
are REGIME_NECESSARY。

ATTRIBUTE-REGIME-MIXED otherwise。

Output：

repaired_regime_classification.json。

============================================================
十二、Minimal-State Histograms
============================================================

Report histogram including：

R0_NONE。

Output：

repaired_minimal_state_histogram.csv。

Expected keys may include：

R0_NONE
R1_O
R2_C
R3_V
R4_O_C
R5_O_V
R6_C_V
R7_O_C_V_FULL。

Do NOT omit zero-count groups
from the CSV schema。

============================================================
十三、Repair Association Analysis
============================================================

Retire Stage4.0 per-deformation
Spearman interpretation。

First trace whether undefined Spearman
was converted to zero。

Output：

stage4_association_bug_trace.md。

Then recompute TWO analyses。

------------------------------------------------------------
ANALYSIS A：
POOLED MATERIAL-SURFACE
------------------------------------------------------------

Group by：

material + surface。

Pool all：

D1-D6

and all Gaussians。

Features：

Js
logJs
detF
s1
s2
s3
normal_change_angle。

Targets：

Delta logit O

Delta C norm

Delta V norm。

Before Spearman：

report feature unique count

std。

If unique count <3
or std <1e-12：

association = NaN

reason =
CONSTANT_FEATURE。

Do NOT replace NaN with0。

Output：

pooled_attribute_association.csv。

------------------------------------------------------------
ANALYSIS B：
PER-GAUSSIAN ACROSS DEFORMATIONS
------------------------------------------------------------

For every Gaussian：

use D1-D6 six observations。

Compute Spearman only if
feature has >=3 unique values。

Then aggregate correlation
distribution across Gaussians：

median
p25
p75
p90

valid Gaussian count。

Output：

per_gaussian_deformation_association.csv。

No causal claim。

============================================================
十四、View-Level V Association Repair
============================================================

Using TEST-camera diagnostics only：

group by：

material + surface。

Pool deformation and view observations。

Features：

abs Delta n_dot_v

deformed abs n_dot_v

Js
logJs。

Target：

Delta V norm。

Apply the same
constant-feature handling。

Output：

view_dependent_attribute_association.csv。

============================================================
十五、Cross-Regime Scientific Gate
============================================================

FINAL CASE ATTRIBUTE-DYNAMICS-SUPPORTED iff：

B0 PASS
B1 PASS
B2 PASS
A3R PASS

AND

at least one regime
is NOT:

STATIC-OPTICAL-STATE-SUFFICIENT。

------------------------------------------------------------

FINAL CASE STATIC-STATE-SUFFICIENT iff：

A3R PASS

and all3 regimes are static sufficient。

------------------------------------------------------------

FINAL CASE ATTRIBUTE-ORACLE-CAPACITY-UNRESOLVED iff：

A3R FAIL
after allowed conditional R7 closure。

------------------------------------------------------------

FINAL CASE STAGE4-PROVENANCE-FAIL iff：

B0/B1/B2 fail。

============================================================
十六、Stage4.1 Permission
============================================================

Allow Stage4.1
deformation-invariant attribute analysis iff：

Final CASE =
ATTRIBUTE-DYNAMICS-SUPPORTED

AND

at least one regime has
a non-NONE minimal dynamic state
in >=75% oracle-valid cases。

Stage4.1 must study
the attribute family discovered here。

Do NOT automatically return to KIOT。

============================================================
十七、Required Outputs
============================================================

stage4_0_R1_protocol_lock.json

oracle_provenance_trace.md
oracle_source_search.txt
oracle_artifact_completeness.csv

full_oracle_case_audit.csv
gt_change_metric_definition.md
repaired_oracle_capacity_gate.json

if needed：
r7_failed_case_convergence_audit.csv
r7_capacity_closure_manifest.csv

repaired_minimal_sufficient_attribute_by_case.csv
repaired_attribute_necessity_by_case.csv
repaired_attribute_necessity_bootstrap.csv
repaired_geometry_only_sufficiency.csv
repaired_regime_classification.json
repaired_minimal_state_histogram.csv

stage4_association_bug_trace.md
pooled_attribute_association.csv
per_gaussian_deformation_association.csv
view_dependent_attribute_association.csv

stage4_0_R1_attribute_protocol_repair_report.md
stage4_0_R1_summary.md
stage4_0_R1_log.txt

============================================================
十八、最终报告必须中文回答
============================================================

A. 为什么原 A3 从逻辑上与 MAT0 控制冲突。

B. 原 A3 理论最大通过 case 数
under MAT0 expected behavior。

C. B0。

D. R1-R7 real optimization yes/no。

E. TEST camera leakage yes/no。

F. synthetic/hardcoded metric yes/no。

G. B1。

H. 288 artifact complete count。

I. finite E_OPT count。

J. duplicate/missing keys。

K. B2。

L. 36 cases R7<=0.15 count。

M. MAT0/MAT1/MAT2 oracle-valid count。

N. initial A3R。

O. R7 closure executed yes/no。

P. closure case count。

Q. final A3R。

R. MAT0 minimal state histogram
including R0_NONE。

S. MAT1 minimal state histogram。

T. MAT2 minimal state histogram。

U. MAT0 O/C/V necessary。

V. MAT1 O/C/V necessary。

W. MAT2 O/C/V necessary。

X. MAT0 geometry-only sufficient fraction。

Y. MAT1 fraction。

Z. MAT2 fraction。

AA. MAT0 classification。

AB. MAT1 classification。

AC. MAT2 classification。

AD. undefined Spearman previously
converted to zero yes/no。

AE. strongest valid pooled associations
for Delta O。

AF. strongest valid pooled associations
for Delta C。

AG. strongest valid pooled associations
for Delta V。

AH. strongest valid view-level
association for Delta V。

AI. Final CASE。

AJ. new main hypothesis supported yes/no。

AK. exact discovered dynamic-state pattern。

AL. allow Stage4.1 yes/no。

AM. KIOT status。

AN. report path。

AO. summary path。

============================================================
十九、README
============================================================

Update README。

Record explicitly：

Stage4.0 originally reported
ATTRIBUTE-ORACLE-INSUFFICIENT。

That Final CASE was caused by
a logical error in the original A3 Gate。

A3 required the FULL dynamic oracle
to reduce geometry-only error by at least50%
in80% of all36 cases。

However12/36 cases belong to
MAT0 fixed-thickness control，
for which geometry-only sufficiency
is the intended successful behavior。

If MAT0 behaves correctly，
R0 approximately equals R7，
so the50%-improvement requirement fails
by design for all12 MAT0 cases。

Even perfect success on all24
MAT1/MAT2 cases yields only66.7%，
below the80% Gate。

Therefore the old A3 and
ATTRIBUTE-ORACLE-INSUFFICIENT Final CASE
are retired pending Stage4.0-R1。

Stage4.0 also omitted R0_NONE
from the minimal dynamic-state search，
forcing static cases to select
a dynamic release group。

The original per-deformation Spearman
analysis further grouped constant
global-affine features，
making several correlations undefined；
those undefined values must not be
reported as zero correlation。

Stage4.0-R1 repairs protocol logic
without changing the GT benchmark，
O/C/V attributes，
E_OPT definition，
or the already frozen numerical
sufficiency/necessity thresholds。

============================================================
二十、实际执行
============================================================

必须实际：

lock Stage4.0 artifacts

trace optimizer and metric provenance

search source for synthetic/hardcoded metrics

verify all288 artifacts

build36-case full oracle audit

compute GT_CHANGE diagnostic

recompute absolute oracle validity

compute repaired A3R

conditionally audit/rerun
only non-converged invalid R7 cases

add R0_NONE minimal state

recompute sufficiency

recompute necessity

bootstrap regime necessity

reclassify MAT0/MAT1/MAT2

trace old Spearman zero bug

run pooled association

run per-Gaussian across-deformation association

run view-level V association

Final CASE

绝对不要：

rerun all288 jobs automatically

add attributes

change GT

change E_OPT

change0.15

change1.10

change1.25

change0.02

return to KIOT

design a predictor。
PROMPT
```

# 我的真实评价

这次我反而比 Stage 4.0 开始前**更看好新主线了**。

因为 raw result（原始结果）已经出现了一个非常符合科学直觉、但不是我们硬编码出来的分层：

[
\boxed{
\text{Fixed thickness}
\rightarrow
\text{no dynamic state}
}
]

[
\boxed{
\text{Mass-conserving neutral}
\rightarrow
\text{scalar opacity}
}
]

[
\boxed{
\text{Mass-conserving tinted}
\rightarrow
\text{multi-attribute}
}
]

这正是“**What Must Deform?**”应该回答的东西。

现在真正该做的是**修 Gate，不是换方向**。

而且请注意：这里不是“因为结果好看所以放宽 A3”。

原 A3 是数学上真的与 MAT0 control（控制组）矛盾：

[
24/36<29/36
]

**理论上最多 24 个 case 能满足，Gate 却要求 29 个。**

这个必须修。

先跑 Stage 4.0-R1。

如果修复后还是：

```text
MAT0 -> NONE
MAT1 -> O
MAT2 -> O+C+V
```

那我会非常明确地告诉你：

> **新主线正式立住了。**
