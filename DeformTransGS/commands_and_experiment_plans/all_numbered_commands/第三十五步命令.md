cd /data/wyh/DeformTransGS

codex exec - <<'PROMPT'
继续执行独立科研项目：

/data/wyh/DeformTransGS

============================================================
当前正式状态
============================================================

Stage 3.4B-R0 已确认一个 logging / analysis bug：

Stage 3.4B reproduction comparison
使用全局：

comp_diffs = []

并在 state loop 中持续 append。

没有 per-state reset。

因此日志中的：

1495
2990
4480
5975
...
14945

来自 cumulative list accumulation。

该 bug：

CONFIRMED。

但是：

修正 state isolation 后，
P0 FIXED_COV 仍无法复现 Stage 3.3.R4。

Per-state aligned comparison：

stretch_1.25:
median diff = 0.0764

stretch_1.50:
median diff = 0.0893

stretch_2.00:
median diff = 0.117

cubic_l010:
median diff = 0.00921

cubic_l020:
median diff = 0.0191

cubic_l0333:
median diff = 0.0314

shear_k020:
median diff = 0.0381

shear_k040:
median diff = 0.0620

twist_60:
median diff = 0.396

Exact reproduction threshold：

median <=1e-6
p95 <=1e-5
max <=1e-3

因此：

A6 FAIL。

同时：

R4 row counts approximately 1503

P0 approximately 1495。

Outer merge：

left_only = 10 total
right_only = 90 total。

但是：

even among aligned cells，
median difference remains 0.009-0.396。

因此：

ROW MISALIGNMENT ALONE
CANNOT EXPLAIN THE METRIC DIVERGENCE。

============================================================
正式 Case 修正
============================================================

Stage 3.4B-R0 summary 中：

Final CASE = ROW-ALIGNMENT-BUG

暂时撤回。

正式当前状态：

METRIC-DIVERGENCE

原因：

state accumulation bug
only explains cumulative n logging。

It does not explain
the aligned per-cell R differences。

同时：

A5 ALPHA IDENTITY
不能标记 PASS。

原因：

report explicitly states
no cached R4 alpha was available for direct comparison。

Therefore：

A5 = NOT EVALUATED

not PASS。

============================================================
本阶段名称
============================================================

Stage 3.4B-R1:

First Intermediate Divergence Trace

中文：

R4 与 P0 首个中间量分叉定位审计

============================================================
一、唯一目标
============================================================

对完全相同：

carrier
deformation state
camera
alpha maps

同时运行：

PATH_R4

和：

PATH_P0

逐级比较 material-cell metric
所有中间量。

找到：

THE FIRST INTERMEDIATE VARIABLE
THAT DIVERGES。

禁止：

猜 boundary bug

猜 bilinear bug

猜 camera bug

猜 deformation bug。

必须用数值逐级证明。

只有找到第一处分叉，
才能修复。

============================================================
二、禁止事项
============================================================

禁止运行：

P1 RIGID_COV

P2 FULL_AFFINE_COV

P3 ORACLE

禁止：

shape mechanism

footprint diagnostic

clone

split

training

method design

修改 TSGS。

当前只审计：

P0 vs original R4 metric path。

============================================================
三、输出目录
============================================================

所有新增输出：

/data/wyh/DeformTransGS/experiments/
stage3_4B_R1_first_divergence_trace/

禁止覆盖任何已有实验。

============================================================
四、锁定原始 R4 Source
============================================================

原 R4 main script：

/data/wyh/DeformTransGS/analysis/
stage3_3R4_exact_projection_recheck.py

SHA256：

df3f619804a92fdb4057192dc43dd748ea778adc52bc498ce80524c014b81119

首先重新计算 SHA256。

要求完全一致。

如果不一致：

立即停止。

输出：

r4_source_identity.json

必须定位原 R4 中实际函数：

1.
exact projection 调用位置

2.
bilinear sampler

3.
make_cell_quad

4.
cell bounds construction

5.
canonical sample world mapping

6.
deformed sample mapping

7.
alpha_to_tau

8.
sample validity

9.
per-camera tau aggregation

10.
R_camera computation

11.
cross-camera aggregation

12.
material-cell row append

输出：

r4_metric_function_map.md

每项记录：

source function
line range
关键代码原文。

============================================================
五、加载 R4 Source：禁止手工重写其逻辑
============================================================

优先尝试：

```python
import importlib.util
from pathlib import Path


R4_SCRIPT = Path(
    "/data/wyh/DeformTransGS/analysis/"
    "stage3_3R4_exact_projection_recheck.py"
)


def load_module_from_path(
    module_name: str,
    path: Path,
):
    spec = importlib.util.spec_from_file_location(
        module_name,
        path,
    )

    if spec is None or spec.loader is None:
        raise RuntimeError(
            f"Cannot load module from {path}"
        )

    module = importlib.util.module_from_spec(spec)

    spec.loader.exec_module(module)

    return module


r4_module = load_module_from_path(
    "stage3_3R4_locked",
    R4_SCRIPT,
)
如果 import 会直接执行 main：

不要修改原文件。

使用 AST 提取原 function definitions。

关键代码骨架：

import ast


def extract_function_module(
    source_path: Path,
    function_names: set[str],
    output_path: Path,
) -> None:
    source = source_path.read_text(
        encoding="utf-8"
    )

    tree = ast.parse(source)

    selected = []

    for node in tree.body:
        if isinstance(
            node,
            (
                ast.Import,
                ast.ImportFrom,
            ),
        ):
            selected.append(node)

        elif (
            isinstance(
                node,
                ast.FunctionDef,
            )
            and node.name in function_names
        ):
            selected.append(node)

    module = ast.Module(
        body=selected,
        type_ignores=[],
    )

    ast.fix_missing_locations(module)

    output_path.write_text(
        ast.unparse(module),
        encoding="utf-8",
    )

IMPORTANT：

如果 function 依赖 global constants，
必须显式锁定这些 constants。

禁止为了“方便”
重新实现一个 approximate R4 metric。

============================================================
六、Shared Render Input

正式 audit states：

STATE_A =
stretch_2.00

STATE_B =
cubic_l0333

STATE_C =
twist_60

原因：

stretch2:
uniform affine

cubic0333:
spatially varying

twist60:
current largest median divergence 0.396。

正式 cameras：

cam_000
cam_004
cam_008。

只使用 Stage3.3.R4 actual camera objects。

============================================================
七、Shared Alpha Maps

这是本阶段最关键隔离步骤。

对于：

canonical

stretch_2.00

cubic_l0333

twist_60

使用：

Stage3.3.R4 original render path

fresh render ONE TIME。

生成共享：

A_SHARED[state][camera]

保存：

shared_alpha/
<state>/<camera>.npy

记录：

shape
dtype
min
max
mean
SHA256。

IMPORTANT：

PATH_R4

和：

PATH_P0

必须读取完全相同
A_SHARED object / .npy。

禁止：

R4 path 自己 render 一份

P0 path 再 render 一份。

本阶段首先隔离：

METRIC PATH ONLY。

输出：

shared_alpha_manifest.csv

============================================================
八、共享 Camera Objects

PATH_R4
和 PATH_P0

必须接收同一个 Python camera object reference。

验证：

assert camera_r4 is camera_p0

在 trace wrapper 中：

同一个 camera object
依次传给两条 path。

记录：

id(camera)

image_width
image_height

FoVx
FoVy

world_view_transform SHA

full_proj_transform SHA

camera_center SHA。

输出：

shared_camera_identity.csv

============================================================
九、共享 Deformation Object

PATH_R4
和 PATH_P0

必须使用同一个 deformation callable object。

例如：

deformation = DEFORMATION_MAP[state]

assert deformation_r4 is deformation
assert deformation_p0 is deformation

禁止：

R4 使用 deform_xyz

P0 使用另写的 phi_state

即使公式看起来相同。

如果 R4 原代码没有 callable abstraction：

将 R4 原 deformation code
抽取为唯一 locked function。

必须通过 source hash。

输出：

shared_deformation_identity.csv

============================================================
十、建立统一 Trace 数据结构

新建：

analysis/r4_metric_trace.py

使用：

from dataclasses import dataclass

import numpy as np


@dataclass
class CellCameraTrace:
    path_name: str
    state: str
    cell_id: int
    camera_id: str

    iu: int
    iv: int

    u_center: float
    v_center: float

    u_low: float
    u_high: float
    v_low: float
    v_high: float

    quad_u: np.ndarray
    quad_v: np.ndarray

    x_can: np.ndarray
    x_def: np.ndarray

    p_can_x: np.ndarray
    p_can_y: np.ndarray

    p_def_x: np.ndarray
    p_def_y: np.ndarray

    A_can: np.ndarray
    A_def: np.ndarray

    tau_can: np.ndarray
    tau_def: np.ndarray

    projection_valid: np.ndarray
    bilinear_valid: np.ndarray
    finite_valid: np.ndarray
    final_sample_valid: np.ndarray

    valid_sample_count: int
    valid_sample_fraction: float

    tau_cell_can: float
    tau_cell_def: float

    R_camera: float

    boundary_distance_value: float
    boundary_pass: bool

Cross-camera：

@dataclass
class CellAggregateTrace:
    path_name: str
    state: str
    cell_id: int

    camera_ids: list[str]
    R_camera_values: np.ndarray
    valid_camera_mask: np.ndarray

    valid_camera_count: int

    R_cell: float

每个 ndarray
保存 float64 / bool。

============================================================
十一、选择 Trace Cells

对于每 audit state：

stretch_2.00
cubic_l0333
twist_60

首先从 Stage3.4B-R0：

aligned_cell_differences.csv

选择：

CELL_CENTER：

material cell center
距离：

u=0
v=0

最近的 cell。

CELL_MEDIAN_DIFF：

abs_diff
最接近该 state median diff 的 cell。

CELL_P95_DIFF：

abs_diff
最接近 p95 diff 的 cell。

CELL_MAX_DIFF：

最大 diff cell。

另外：

MISSING_R4_ONLY：

从 right_only 中
选择第一个 cell。

MISSING_P0_ONLY：

从 left_only 中
选择第一个 cell。

每 state 最多：

6 cells。

输出：

trace_cell_manifest.csv

字段：

state
trace_role
cell_id
u_center
v_center
old_abs_diff。

============================================================
十二、关键：两条 Metric Path 必须独立执行

定义：

trace_r4_cell_camera(...)

必须直接执行：

R4 原代码逻辑。

定义：

trace_p0_cell_camera(...)

执行当前 Stage3.4B P0 metric logic。

两者输入完全相同：

cell_id
state
camera object
A_SHARED canonical
A_SHARED deformed
deformation callable
material parameterization。

禁止一个 path
调用另一个 path 的中间输出。

目的是找到：

first divergence。

============================================================
十三、Stage-by-Stage Array Comparator

新建：

from __future__ import annotations

import numpy as np


def compare_numeric_array(
    name: str,
    a: np.ndarray,
    b: np.ndarray,
    atol: float,
) -> dict:
    a = np.asarray(a)
    b = np.asarray(b)

    result = {
        "variable": name,
        "shape_a": str(a.shape),
        "shape_b": str(b.shape),
        "shape_equal": a.shape == b.shape,
    }

    if a.shape != b.shape:
        result.update(
            {
                "finite_common": 0,
                "max_abs_diff": np.nan,
                "mean_abs_diff": np.nan,
                "equal": False,
            }
        )

        return result

    if a.dtype == np.bool_ or b.dtype == np.bool_:
        mismatch = a.astype(bool) != b.astype(bool)

        result.update(
            {
                "finite_common": int(a.size),
                "max_abs_diff": float(
                    mismatch.any()
                ),
                "mean_abs_diff": float(
                    mismatch.mean()
                ),
                "equal": not mismatch.any(),
            }
        )

        return result

    af = np.asarray(
        a,
        dtype=np.float64,
    )

    bf = np.asarray(
        b,
        dtype=np.float64,
    )

    finite = (
        np.isfinite(af)
        & np.isfinite(bf)
    )

    if not finite.any():
        result.update(
            {
                "finite_common": 0,
                "max_abs_diff": np.nan,
                "mean_abs_diff": np.nan,
                "equal": False,
            }
        )

        return result

    diff = np.abs(
        af[finite] - bf[finite]
    )

    result.update(
        {
            "finite_common": int(
                finite.sum()
            ),
            "max_abs_diff": float(
                diff.max()
            ),
            "mean_abs_diff": float(
                diff.mean()
            ),
            "equal": bool(
                diff.max() <= atol
            ),
        }
    )

    return result

正式 variable order：

VARIABLE_ORDER = [

"u_center",
"v_center",

"u_low",
"u_high",
"v_low",
"v_high",

"quad_u",
"quad_v",

"x_can",
"x_def",

"p_can_x",
"p_can_y",

"p_def_x",
"p_def_y",

"A_can",
"A_def",

"tau_can",
"tau_def",

"projection_valid",
"bilinear_valid",
"finite_valid",
"final_sample_valid",

"valid_sample_count",
"valid_sample_fraction",

"boundary_distance_value",
"boundary_pass",

"tau_cell_can",
"tau_cell_def",

"R_camera",

]

Tolerance：

coordinate/material values：
1e-10

world xyz：
1e-8

pixel coordinates：
1e-6

alpha：
1e-7

tau：
1e-7

scalar aggregate：
1e-7

bool：
exact。

============================================================
十四、First Divergence Algorithm

对于每：

state
cell
camera

按 VARIABLE_ORDER
逐项 compare。

第一项：

equal == False

定义为：

FIRST_DIVERGENCE。

伪代码：

first_divergence = None

for variable in VARIABLE_ORDER:
    result = compare_variable(
        r4_trace,
        p0_trace,
        variable,
    )

    rows.append(result)

    if (
        first_divergence is None
        and not result["equal"]
    ):
        first_divergence = variable

输出：

intermediate_variable_comparison.csv

字段：

state
trace_role
cell_id
camera_id
variable
shape_r4
shape_p0
max_abs_diff
mean_abs_diff
equal
is_first_divergence。

输出：

first_divergence_summary.csv

字段：

state
trace_role
cell_id
camera_id
first_divergence_variable
max_abs_diff。

============================================================
十五、Full Dataset First-Divergence Check

不能只看 18 个 trace cells。

根据 trace 找到疑似第一变量后：

对全部 aligned：

state + cell + camera

只比较：

FIRST_DIVERGENCE variable

以及它前一个 variable。

例如：

如果 trace 显示：

x_def

首次不同，

则 full dataset 比较：

x_can
x_def。

如果：

boundary_pass

首次不同，

full dataset 比较：

valid mask
boundary distance
boundary pass。

输出：

full_dataset_divergence_validation.csv

要求：

至少解释：

=95%

存在 R_cell difference >1e-5
的 cell。

定义：

EXPLANATION_COVERAGE =

number of differing R cells
with upstream first-divergence
/
number of differing R cells。

要求：

=0.95。

============================================================
十六、特别审计：Cell Quadrature

逐字符检查：

R4 make_cell_quad

vs P0 quadrature。

记录：

是否 midpoint

是否 endpoints

meshgrid indexing

flatten order。

关键诊断：

对同一 center cell：

输出前 20 个：

quad_u
quad_v

R4 vs P0。

输出：

quadrature_sequence_comparison.csv

注意：

即使 quadrature point set 相同，
flatten ordering 不同
通常不影响 mean，

但如果 valid mask
或 deformation array
发生 positional pairing，
ordering mismatch 可能产生错误。

因此比较：

SORTED point set equality

和：

ORDERED sequence equality。

============================================================
十七、特别审计：Material Sample Deformation

这是当前高优先级嫌疑。

对于完全相同：

quad_u
quad_v
x_can

比较：

R4 x_def

vs P0 x_def。

重点 states：

stretch_2.00
cubic_l0333
twist_60。

输出：

material_sample_deformation_comparison.csv

字段：

state
cell_id
camera_independent
max x_can diff
max x_def diff

R4 deformation function source
P0 deformation function source

same callable yes/no。

特别是 twist：

输出每 sample：

u
v
x_can
y_can
z_can

theta_R4
theta_P0

x_def_R4
x_def_P0。

如果 theta 不是显式中间变量，
在 trace-only debug 中根据各自 path
实际计算值记录。

禁止使用第三份重实现 theta
代替 path 内真实值。

============================================================
十八、特别审计：Boundary Mask

定位：

R4 boundary mask creation

P0 boundary mask creation。

比较共享 alpha 输入条件下：

binary mask SHA256

distance transform SHA256

pixel-wise mismatch fraction

max distance-transform diff。

输出：

boundary_mask_identity.csv

如果 mask 不同：

记录 threshold

dilation

erosion

distance_transform function

nearest/round pixel indexing。

============================================================
十九、特别审计：Bilinear Sampling

即使 unit test 都 PASS，
仍需比较真实 coordinates。

对 trace cells：

使用相同：

A_SHARED

p_can
p_def

分别调用：

R4 sampler

P0 sampler。

如果 coordinates 相同：

要求 sampled alpha
max diff <=1e-10。

输出：

bilinear_real_coordinate_comparison.csv

如果 sampler output 不同：

给出：

image index values

x0/x1/y0/y1

wx/wy

四邻域 alpha

R4 output

P0 output。

============================================================
二十、Missing Cell Validity Trace

对：

right_only R4-only cells

left_only P0-only cells

逐：

state
cell
camera

输出：

R4:

projection valid fraction
bilinear valid fraction
finite fraction
final sample fraction
boundary distance
boundary pass
R_camera

P0 同字段。

输出：

missing_cell_validity_trace.csv

必须明确：

为什么 R4 有 9 cells
P0 没有。

为什么 P0 有 1 cell
R4 没有。

不能只写：

slightly stricter filtering。

必须给具体 first rejected condition。

============================================================
二十一、修复规则

找到 FIRST_DIVERGENCE 后：

只能修复：

Stage3.4B P0 path。

禁止修改：

Stage3.3.R4 source

Stage3.3.R4 CSV

Gate threshold。

修复方式：

优先直接 import / call
R4 original implementation。

禁止再次人工“对齐公式”。

例如：

如果 cell deformation diverges：

P0 必须调用 R4 原 deformation
material-sample path。

如果 boundary diverges：

P0 必须调用 R4 原 boundary
validity path。

如果 sampler diverges：

直接复用 R4 sampler。

============================================================
二十二、修复后 Shared-Alpha Metric Reproduction

首先仍使用：

A_SHARED

不重新 render。

对全部 states：

运行 repaired P0 metric。

比较 R4 CSV。

输出：

shared_alpha_metric_reproduction.csv

Gate：

每 state：

row count exact match

(state,cell_id) key exact match

median diff <=1e-8

p95 <=1e-7

max <=1e-5。

为什么比旧 Gate 更严：

此时使用完全相同 alpha maps。

metric path 正确时
应接近数值 identical。

如果 FAIL：

继续 first-divergence trace。

禁止 fresh render。

============================================================
二十三、Fresh-Render End-to-End Reproduction

只有 shared-alpha reproduction PASS 后：

P0 fresh render。

使用 exact R4：

carrier

camera objects

renderer wrapper

P0 fixed covariance input。

重新生成 alpha。

然后运行同一 repaired metric。

比较 R4。

输出：

fresh_render_r4_reproduction.csv

正式 Gate：

row key exact match

median diff <=1e-6

p95 <=1e-5

max <=1e-3

MAE absolute difference <=0.005

cubic Spearman difference <=0.01。

============================================================
二十四、严格 Gates

B0:
R4 SOURCE LOCK

PASS。

B1:
SHARED INPUT LOCK

same alpha object

same camera object

same deformation callable。

B2:
FIRST DIVERGENCE FOUND

必须给出：

exact variable

exact source/function

numeric evidence。

B3:
DIVERGENCE EXPLANATION COVERAGE

=0.95。

B4:
SHARED-ALPHA METRIC REPRODUCTION

全部 PASS。

B5:
FRESH-RENDER END-TO-END REPRODUCTION

全部 PASS。

只有：

B0-B5 ALL PASS

才允许：

Stage3.4B P1/P2/P3。

============================================================
二十五、Final CASE

CASE QUADRATURE-DIVERGENCE

CASE SAMPLE-TRANSPORT-DIVERGENCE

CASE PROJECTION-DIVERGENCE

CASE BOUNDARY-DIVERGENCE

CASE BILINEAR-DIVERGENCE

CASE TAU-AGGREGATION-DIVERGENCE

CASE CAMERA-AGGREGATION-DIVERGENCE

CASE MULTIPLE-DIVERGENCES

根据 first divergence
实际选择。

如果修复后 B4/B5 PASS：

追加：

RESTORED。

例如：

SAMPLE-TRANSPORT-DIVERGENCE-RESTORED。

CASE UNRESOLVED

B2 或 B3 FAIL。

禁止 shape policy test。

============================================================
二十六、报告

必须输出：

r4_source_identity.json
r4_metric_function_map.md

shared_alpha_manifest.csv
shared_camera_identity.csv
shared_deformation_identity.csv

trace_cell_manifest.csv

intermediate_variable_comparison.csv
first_divergence_summary.csv
full_dataset_divergence_validation.csv

quadrature_sequence_comparison.csv
material_sample_deformation_comparison.csv
boundary_mask_identity.csv
bilinear_real_coordinate_comparison.csv
missing_cell_validity_trace.csv

shared_alpha_metric_reproduction.csv
fresh_render_r4_reproduction.csv

first_divergence_trace_report.md
stage3_4B_R1_summary.md
stage3_4B_R1_log.txt

============================================================
二十七、报告必须中文回答

A. 为什么 ROW-ALIGNMENT-BUG
不足以解释 reproduction failure。

B. A5 为什么不能标记 PASS。

C. R4 source SHA 是否一致。

D. shared alpha 是否为同一输入。

E. shared camera object 是否同一 object。

F. shared deformation callable 是否同一。

G. center cell first divergence。

H. median-diff cell first divergence。

I. p95 cell first divergence。

J. max-diff cell first divergence。

K. twist60 first divergence。

L. 最常见 first divergence variable。

M. explanation coverage。

N. R4/P0 quadrature ordered same yes/no。

O. quadrature point set same yes/no。

P. material x_can same yes/no。

Q. material x_def same yes/no。

R. twist theta/path 是否一致。

S. projection coordinates same yes/no。

T. boundary mask SHA same yes/no。

U. boundary distance same yes/no。

V. bilinear output same yes/no。

W. tau arrays same yes/no。

X. valid mask same yes/no。

Y. tau_cell_can same yes/no。

Z. tau_cell_def same yes/no。

AA. R_camera same yes/no。

AB. valid camera set same yes/no。

AC. missing R4-only cells first rejected condition。

AD. missing P0-only cell first rejected condition。

AE. exact source/function bug。

AF. 修复内容。

AG. shared-alpha reproduction median/p95/max。

AH. fresh-render stretch2 reproduction。

AI. fresh-render cubic0333 reproduction。

AJ. fresh-render shear k040 reproduction。

AK. fresh-render twist reproduction。

AL. B0-B5。

AM. Final CASE。

AN. 是否允许恢复 P1/P2/P3。

============================================================
二十八、README

更新：

/data/wyh/DeformTransGS/README.md

记录：

Stage3.4B-R0 confirmed a cumulative-list bug
in the reproduction logger。

This explained the artificial count sequence
1495,2990,4480,...。

However，
after correct per-state isolation，
aligned P0 cells still differed from R4
with median R differences of
0.009-0.396。

Therefore row alignment alone
did not explain the protocol divergence。

Stage3.4B-R1 performs a first-intermediate-divergence trace
using the SAME alpha maps，
SAME camera objects，
and SAME deformation callable
for the R4 and P0 metric paths。

The comparison proceeds from：

material coordinates
→ quadrature
→ world samples
→ deformed samples
→ projection
→ alpha sampling
→ optical depth
→ validity
→ camera response
→ cross-camera response。

No shape-transport policy test resumes
until exact R4 reproduction is restored。

============================================================
二十九、实际执行

必须实际：

lock R4 source

map actual R4 metric functions

generate ONE shared alpha dataset

use identical camera object references

use identical deformation callable

build R4 trace path

build P0 trace path

select trace cells

compare every intermediate variable

find FIRST divergence

validate divergence across full dataset

audit quadrature ordering

audit material sample deformation

audit boundary masks

audit real-coordinate bilinear sampling

trace missing cells

fix only P0

shared-alpha exact reproduction

fresh-render end-to-end reproduction

B0-B5

Final CASE

不要运行 P1。

不要运行 P2。

不要运行 P3。

不要研究 covariance mechanism。

不要 clone。

不要 split。

不要训练。

不要修改 TSGS。

============================================================
三十、最终终端总结

最后只输出：

R4 source lock PASS/FAIL
Shared alpha identical yes/no
Shared camera object identical yes/no
Shared deformation callable identical yes/no
Center first divergence
Median-cell first divergence
P95-cell first divergence
Max-cell first divergence
Twist first divergence
Most common first divergence
Explanation coverage
Quadrature ordered equal yes/no
Quadrature set equal yes/no
x_can equal yes/no
x_def equal yes/no
Projection equal yes/no
Boundary mask equal yes/no
Bilinear equal yes/no
Tau equal yes/no
Valid mask equal yes/no
Tau cell equal yes/no
R camera equal yes/no
Camera set equal yes/no
Exact bug source/function
Fix
Shared-alpha median/p95/max diff
Fresh stretch2 reproduction diff
Fresh cubic0333 reproduction diff
Fresh shear k040 reproduction diff
Fresh twist reproduction diff
B0
B1
B2
B3
B4
B5
Final CASE
Can resume P1/P2/P3 yes/no
first_divergence_trace_report.md path
stage3_4B_R1_summary.md path

请直接实际定位 FIRST DIVERGENCE。

禁止回答：

possible boundary issue

possible bilinear issue

possible camera issue。

必须输出：

THE FIRST VARIABLE THAT DIFFERS

以及：

THE EXACT SOURCE FUNCTION THAT CAUSED IT。

最高优先级：

SAME ALPHA。

SAME CAMERA OBJECT。

SAME DEFORMATION CALLABLE。

COMPARE INTERMEDIATES IN ORDER。

DO NOT RUN P1/P2/P3 UNTIL B5 PASS。
PROMPT