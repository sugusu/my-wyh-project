这次 **Stage 0R 重置方向正确**。基于你现在给出的日志，我的判断是：

> **旧 15k 结果已经正确降级为 pilot；新的 30k 官方 TSGS 轨迹正在建立。当前仍不能重新进入 Gate 1，必须先完成 30k 轨迹验收。**

官方 `run_translab.sh` 确实使用 30k 训练，并显式启用 `--delight --normal --eval --use_asg`，同时设置 `delight_iterations=15000` 和 `--nofix_position --nofix_scaling --nofix_rotation`。([GitHub][1])

不过你现在这份报告里有 **两个表述必须修正**。

## 第一处：“nofix”解释写反了

报告现在写：

```text
No nofix_* flags:
Old run didn't freeze position/scaling/rotation during later training.
```

这是错的。

`nofix_position` 的直观含义就是：

> **不要固定 position。**

官方 TransLab 显式传入：

```text
--nofix_position
--nofix_scaling
--nofix_rotation
```

因此是为了让 xyz、scaling、rotation 在 15k 后**继续更新**。([GitHub][1])

所以正确描述应该是：

```text
旧 pilot 没有启用 nofix_position / nofix_scaling / nofix_rotation。

如果旧训练继续超过 15k，
这些参数将按照默认 selective policy 被固定。

但旧实验本身在 15k 结束，
因此实际上没有进入完整的 post-15k 阶段。
```

这个区别很重要。

## 第二处：“vanilla 15k 3DGS”说得太重

报告写：

```text
The old baseline was essentially a vanilla 15k 3DGS run
```

我建议不要这么写。

因为你毕竟跑的是：

```text
longxiang-ai/TSGS
```

只是没有启用 TransLab 正式 recipe 的核心功能。

更准确：

> **The old baseline was a simplified 15k TSGS pilot without the full TransLab-specific training configuration.**

中文就是：

> **旧实验是一个未启用完整 TransLab 配置的简化版 15k TSGS pilot，而不是正式 TSGS TransLab baseline。**

这样更科学，也避免以后自己把历史实验说错。

---

现在不要再设计新特征。下面只做 **Stage 0R-B：30k 正式轨迹验收**。

继续执行 RecycleGS 项目。

# 一、当前主线状态

当前状态：

```text
旧 simplified 15k TSGS：
PILOT / ARCHIVED

Official TSGS scene_01 30k：
TRAINING / VALIDATION PENDING

Formal Gate 1：
NOT STARTED

Formal Stage 2：
NOT STARTED

Stage 3 reseed：
FORBIDDEN
```

当前最大目标：

> 验证 `/data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k` 确实是一条完整、配置正确、checkpoint 连续的官方 TSGS 30k TransLab 训练轨迹。

在完成该验收前，禁止重新运行：

```text
mask-risk Gate 1
prune
recovery
reseed
```

---

# 二、修正 Stage 0R 报告中的两处错误

修改：

```text
src/baseline/build_stage0r_rebase_report.py
```

以及：

```text
outputs/debug/stage0r/old_vs_official_config_report.md
outputs/debug/stage0r/stage0r_rebase_report.md
```

## 2.1 修正 nofix 语义

删除：

```text
Old run didn't freeze position/scaling/rotation during later training.
```

替换为：

```text
The old pilot did not enable nofix_position,
nofix_scaling, or nofix_rotation.

Under TSGS selective post-15k control, the absence of these
nofix flags would allow position, scaling, and rotation to be
fixed after the stage transition.

However, the old pilot terminated at iteration 15000 and therefore
did not execute the full post-15k training stage.
```

中文报告写：

```text
旧 pilot 未启用 nofix_position、nofix_scaling 和
nofix_rotation。

按照 TSGS 的 15k 后参数控制逻辑，如果训练继续超过15k，
位置、尺度和旋转将进入默认固定策略。

但旧 pilot 在 iteration 15000 已结束，因此实际上没有经历
完整的 15k 后训练阶段。
```

---

## 2.2 修正 vanilla 3DGS 描述

删除：

```text
The old baseline was essentially a vanilla 15k 3DGS run.
```

替换为：

```text
The old baseline was a simplified 15k TSGS pilot without the
full TransLab-specific training configuration.
```

禁止将旧实验称为：

```text
vanilla 3DGS baseline
```

因为其代码基础仍为 TSGS。

---

# 三、检查当前30k训练进程状态

执行：

```bash
cd /data/wyh/RecycleGS

ps -p 1987496 \
  -o pid,etime,%cpu,%mem,stat,cmd
```

检查日志最后100行：

```bash
tail -n 100 \
outputs/debug/stage0r/training_30k.log
```

检查错误：

```bash
grep -nEi \
"traceback|error|exception|nan|inf|cuda out of memory|killed" \
outputs/debug/stage0r/training_30k.log \
| tail -100
```

检查输出节点：

```bash
find \
/data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k \
-maxdepth 3 -type f \
| sort
```

生成：

```text
outputs/debug/stage0r/training_runtime_status.json
outputs/debug/stage0r/training_runtime_status_report.md
```

必须记录：

```text
PID exists
process state
elapsed time
last logged iteration
last finite loss
latest saved PLY iteration
latest saved checkpoint iteration
error count
NaN/Inf count
```

这一步只是状态记录。

不得根据中间训练指标提前进入 Gate 1。

---

# 四、训练进程结束后首先检查退出状态和日志完整性

创建：

```text
src/baseline/audit_official_training_completion.py
```

输入：

```text
/data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k
outputs/debug/stage0r/training_30k.log
```

必须验证：

```text
1. 日志到达 iteration 30000
2. 没有未处理 Traceback
3. 没有 CUDA OOM
4. 没有 NaN loss
5. 没有 Inf loss
6. iteration 30000 PLY 存在
7. cfg_args 存在
```

输出：

```text
outputs/debug/stage0r/
official_training_completion.json

outputs/debug/stage0r/
official_training_completion_report.md
```

结论只能是：

```text
TRAINING_COMPLETE_VALID
TRAINING_INCOMPLETE
TRAINING_FAILED
```

只有：

```text
TRAINING_COMPLETE_VALID
```

才能继续。

---

# 五、严格审计正式 cfg_args

创建：

```text
src/baseline/audit_official_model_cfg.py
```

读取：

```text
/data/wyh/RecycleGS/baselines/
tsgs_official_scene01_30k/cfg_args
```

同时读取：

```text
/data/wyh/repos/TSGS/run_translab.sh
```

检查：

```text
iterations = 30000
delight = True
normal = True
eval = True
use_asg = True
resolution = 2

sd_normal_until_iter = 30000
delight_iterations = 15000
normal_cos_threshold_iter = 3000
ncc_loss_from_iter = 7000

nofix_position = True
nofix_scaling = True
nofix_rotation = True

seed = 42
```

同时报告：

```text
nofix_opacity
nofix_param
sh_degree
asg_degree
densify_until_iter
```

不要猜默认值。

必须直接读取 `cfg_args`。

输出：

```text
outputs/debug/stage0r/
official_cfg_audit.json

outputs/debug/stage0r/
official_cfg_audit_report.md
```

判定：

```text
OFFICIAL_CFG_MATCH
OFFICIAL_CFG_MISMATCH
```

任何核心字段不一致则停止。

---

# 六、检查五个训练节点

要求存在：

```text
iteration_7000
iteration_10000
iteration_15000
iteration_20000
iteration_30000
```

检查：

```bash
for ITER in 7000 10000 15000 20000 30000
do
  PLY=/data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k/point_cloud/iteration_${ITER}/point_cloud.ply

  echo "===== iteration ${ITER} ====="

  if [ -f "$PLY" ]; then
    ls -lh "$PLY"
    sha256sum "$PLY"
  else
    echo "MISSING: $PLY"
  fi
done
```

训练 checkpoint 至少要求：

```text
7000
10000
15000
20000
```

自动定位：

```bash
find \
/data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k \
-maxdepth 2 -type f \
\( -name "chkpnt*.pth" -o -name "*checkpoint*.pth" \) \
| sort
```

创建：

```text
src/baseline/audit_training_trajectory_artifacts.py
```

输出表：

```text
iteration
PLY exists
PLY SHA256
Gaussian count
checkpoint exists
checkpoint SHA256
```

保存：

```text
outputs/debug/stage0r/
trajectory_artifact_audit.csv

outputs/debug/stage0r/
trajectory_artifact_audit_report.md
```

---

# 七、严格验证15k是 mid-run checkpoint

创建或修正：

```text
src/baseline/verify_midrun_checkpoint.py
```

检查：

```text
model root:
tsgs_official_scene01_30k

cfg_args iterations:
30000

target PLY:
point_cloud/iteration_15000/point_cloud.ply

target checkpoint:
iteration 15000 training checkpoint

later PLY:
iteration_20000 exists
iteration_30000 exists
```

必须确认：

```text
15k checkpoint
20k PLY
30k PLY

属于同一 model root。
```

报告：

```text
model_root
cfg_iterations
checkpoint_iteration
checkpoint_path
checkpoint_sha256

ply_15000_path
ply_15000_sha256

ply_20000_path
ply_30000_path

is_midrun
```

输出：

```text
outputs/debug/stage0r/
scene01_midrun_checkpoint_report.md
```

只有：

```text
VALID_15K_MIDRUN_CHECKPOINT = true
```

才能进行正式 RecycleGS intervention。

---

# 八、审计15k阶段切换前后参数策略

创建：

```text
src/baseline/audit_official_stage_transition.py
```

从正式 cfg_args 和本地：

```text
GaussianModel.selective_learning_rate_control()
```

推导：

```text
iteration 14999
iteration 15000
iteration 15001
iteration 20000
```

每个 optimizer group 输出：

```text
name
lr
trainable
freeze reason
nofix flag
```

重点组：

```text
xyz
knn_f
f_dc
f_rest
opacity
transparency
scaling
rotation
f_asg
```

报告必须明确：

```text
哪些参数在15001继续优化
哪些参数在15001被冻结
```

禁止使用历史报告中的硬编码名单。

正式名单必须由：

```text
official cfg_args
+
本地 commit 73d3f984...
gaussian_model.py
```

联合推导。

输出：

```text
outputs/debug/stage0r/
official_stage_transition.csv

outputs/debug/stage0r/
official_stage_transition_report.md
```

---

# 九、正式评价训练轨迹

创建：

```text
src/baseline/evaluate_official_tsgs_trajectory.py
```

评价：

```text
7000
10000
15000
20000
30000
```

使用同一个 official-compatible evaluator。

正式 evaluator 必须记录：

```text
exact PLY path
PLY SHA256

camera split
view names
num views

GT image source
resolution
background

dataset sh_degree
active_sh_degree after load
max_sh_degree

use_asg
specular model path and SHA
use_app
AppModel path and SHA
```

为 evaluator 生成：

```text
evaluator_identity_sha256
```

五个 iteration 必须使用相同：

```text
camera split
view names
GT definition
resolution
```

如果辅助模型必须按 iteration 加载，则记录对应权重路径。

---

# 十、轨迹指标

每个节点输出：

```text
PSNR
SSIM
LPIPS
Gaussian count
```

保存：

```text
outputs/baseline/
official_scene01_training_trajectory.csv

outputs/baseline/
official_scene01_training_trajectory.json
```

表格：

```text
Iteration | PSNR | SSIM | LPIPS | N Gaussians
7000
10000
15000
20000
30000
```

同时生成：

```text
outputs/baseline/
official_scene01_training_trajectory_report.md
```

报告必须特别回答：

```text
15k → 20k：
PSNR 如何变化？

20k → 30k：
PSNR 如何变化？

15k 是否是合理 intervention point？
```

注意：

> 不得要求 PSNR 单调增长。

只报告真实轨迹。

---

# 十一、正式几何评价

Stage 0R 不允许继续只使用：

```text
SPR
d_shift
```

作为正式 baseline 几何指标。

必须查找并复用当前项目已有 mesh extraction 或 TSGS 官方评价流程。

对：

```text
iteration 15000
iteration 30000
```

至少计算：

```text
Chamfer Distance
accuracy
completeness
F-score @ 0.5% object diameter
F-score @ 1.0% object diameter
```

如果 normal evaluation 稳定：

```text
normal angular error
```

输出：

```text
outputs/baseline/
official_scene01_15k_geometry.json

outputs/baseline/
official_scene01_30k_geometry.json
```

以及：

```text
outputs/baseline/
official_scene01_geometry_report.md
```

如果正式 mesh extraction 尚未跑通：

```text
FORMAL_GEOMETRY_PENDING
```

Stage 0R 最多输出：

```text
BASELINE_RENDER_READY_GEOMETRY_PENDING
```

不得声称完整 baseline ready。

---

# 十二、检查官方30k结果是否基本合理

不要直接拿旧 pilot：

```text
PSNR 28.60
```

作为必须超过的目标。

旧 pilot 的：

```text
eval=False
delight=False
GT split/评价语义
```

与正式 official evaluator 不同。

只允许以下比较：

```text
official 15k
vs
official 20k
vs
official 30k
```

以及：

```text
当前 TSGS 官方代码和当前 TransLab 数据
在同一 evaluator 下的结果
```

禁止混用：

```text
旧训练日志 PSNR
Stage 2A evaluator PSNR
SH debug PSNR
正式 official trajectory PSNR
```

---

# 十三、修正 Stage 0R 最终状态机

修改：

```text
src/baseline/build_stage0r_rebase_report.py
```

最终结论改为四种。

## A. OFFICIAL_BASELINE_READY

要求：

```text
training complete
cfg match
15k mid-run valid
trajectory evaluation complete
formal geometry evaluation complete
```

允许：

```text
进入正式 Gate 1
```

## B. BASELINE_RENDER_READY_GEOMETRY_PENDING

要求：

```text
training complete
cfg match
15k mid-run valid
render trajectory complete
geometry evaluation pending
```

处理：

```text
补正式 geometry
不进入 formal Gate 1
```

## C. OFFICIAL_BASELINE_REPRODUCTION_FAILED

包括：

```text
training failed
cfg mismatch
outputs missing
15k mid-run invalid
```

## D. TRAINING_IN_PROGRESS

当前训练尚未形成完整30k结果时使用。

不得在训练仍未完成时输出：

```text
OFFICIAL_BASELINE_READY
```

根据用户当前提供日志，现在应为：

```text
TRAINING_IN_PROGRESS
```

---

# 十四、训练完成后的唯一执行脚本

创建：

```text
scripts/stage0rb_validate_official_trajectory.sh
```

内容：

```bash
#!/usr/bin/env bash
set -euo pipefail

cd /data/wyh/RecycleGS

export CUDA_VISIBLE_DEVICES=2,3
export PYTHONPATH=/data/wyh/RecycleGS:/data/wyh/repos/TSGS:${PYTHONPATH:-}

MODEL=/data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k

echo "1/7 Audit training completion"
python3 src/baseline/audit_official_training_completion.py \
  --model-dir $MODEL \
  --log outputs/debug/stage0r/training_30k.log

echo "2/7 Audit official cfg"
python3 src/baseline/audit_official_model_cfg.py \
  --model-dir $MODEL

echo "3/7 Audit trajectory artifacts"
python3 src/baseline/audit_training_trajectory_artifacts.py \
  --model-dir $MODEL \
  --iterations 7000 10000 15000 20000 30000

echo "4/7 Verify 15k mid-run checkpoint"
python3 src/baseline/verify_midrun_checkpoint.py \
  --model-dir $MODEL \
  --iteration 15000

echo "5/7 Audit official stage transition"
python3 src/baseline/audit_official_stage_transition.py \
  --model-dir $MODEL

echo "6/7 Evaluate official trajectory and geometry"
python3 src/baseline/evaluate_official_tsgs_trajectory.py \
  --model-dir $MODEL \
  --iterations 7000 10000 15000 20000 30000

python3 src/baseline/evaluate_official_tsgs_geometry.py \
  --model-dir $MODEL \
  --iterations 15000 30000

echo "7/7 Build Stage 0R final report"
python3 src/baseline/build_stage0r_rebase_report.py

echo "Stage 0R-B validation completed."
```

设置权限：

```bash
chmod +x scripts/stage0rb_validate_official_trajectory.sh
```

---

# 十五、Stage 0R-B 完成后停止

必须生成：

```text
outputs/debug/stage0r/
official_training_completion_report.md

outputs/debug/stage0r/
official_cfg_audit_report.md

outputs/debug/stage0r/
trajectory_artifact_audit_report.md

outputs/debug/stage0r/
scene01_midrun_checkpoint_report.md

outputs/debug/stage0r/
official_stage_transition_report.md

outputs/baseline/
official_scene01_training_trajectory_report.md

outputs/baseline/
official_scene01_geometry_report.md

outputs/debug/stage0r/
stage0r_rebase_report.md
```

完成后停止。

禁止自动执行：

```text
candidate domain
mask-risk
Gate 1
prune
recovery
reseed
```

只有：

```text
Stage 0R = OFFICIAL_BASELINE_READY
```

才允许重新进入正式 Gate 1。

## 我现在对项目的判断

**这是一次必要且正确的回退。**

而且旧实验并没有白做，它已经帮你验证了：

```text
跨视角 mask 支持值得研究
↓
实现 bug 已基本排掉
↓
删除评价流程已经审计过
↓
真正要重做的是 baseline 数据轨迹
```

还有一点，报告里“old baseline essentially vanilla 3DGS”建议马上改掉。它是**简化 TSGS pilot**，不是 vanilla 3DGS。

当前状态就应该老老实实写：

> **TRAINING_IN_PROGRESS — formal Gate 1 has not restarted.**

30k 轨迹一旦验收正确，后面不用再走 Stage 1A 到 Stage 1H 那套“考古史”；直接把已经验证过的 `mask_risk_mean` pipeline 在正式 15k mid-run checkpoint 上重新跑最小 Gate 链。

[1]: https://raw.githubusercontent.com/longxiang-ai/TSGS/main/run_translab.sh "raw.githubusercontent.com"
