继续执行 RecycleGS 项目。

# 一、立即暂停当前 Stage 2B

当前：

```text
Stage 2B-AC fixed recovery:
baseline PSNR = 22.3939
schedule control 500 = 17.5376
delta = -4.8563 dB
```

不得解释为：

```text
500 recovery steps insufficient
```

也不得继续：

```text
增加 recovery steps
开启 delight 后继续当前 checkpoint
调整 appearance LR
进入 Stage 3
```

当前正式判断：

```text
CURRENT_BASELINE_TRAJECTORY_INVALID_FOR_FORMAL_RECYCLEGs_EXPERIMENT
```

原因：

1. 当前 TSGS baseline 是独立 `--iterations 15000` 训练结果；
2. 它不是完整 30000-step TSGS trajectory 的 iteration-15000 checkpoint；
3. 当前 baseline 未使用官方 TransLab 完整训练 flags；
4. custom Stage-2 freeze policy 与官方 TransLab recipe 不一致。

当前进入：

> Stage 0R：Official TSGS Baseline Rebase

---

# 二、备份现有结果

不要删除任何结果。

执行：

```bash
cd /data/wyh/RecycleGS

mkdir -p outputs/archive/pilot_simplified_tsgs

STAMP=$(date '+%Y%m%d_%H%M%S')

cp -r \
  outputs/reliability \
  outputs/archive/pilot_simplified_tsgs/reliability_${STAMP}

cp -r \
  outputs/prune_only \
  outputs/archive/pilot_simplified_tsgs/prune_only_${STAMP}
```

创建：

```text
outputs/archive/pilot_simplified_tsgs/STATUS.md
```

内容：

```markdown
# Pilot Results on Simplified 15k TSGS Configuration

These results were generated using a terminal 15k TSGS-like run rather than
the iteration-15000 checkpoint of the official 30k TransLab training recipe.

Valid pilot observations:

- cross-view mask risk correlates with Gaussian mesh distance;
- mask-risk pruning removes higher-error Gaussians than random;
- Stage 2A loading and pruning are real and correctly audited.

These results are exploratory only and must not be used as formal TSGS baseline
or final RecycleGS comparisons.

Formal experiments are rebased on the official TSGS 30k TransLab trajectory.
```

实时日志写：

```markdown
## Stage 0R Baseline Rebase

发现当前正式 baseline 是独立15k简化TSGS训练，而不是官方30k训练轨迹中的15k中间checkpoint。

已有 mask-risk / prune-only 结果保留为 pilot evidence。

暂停 Stage 2B 和 Stage 3。

开始按照官方 TSGS TransLab 配方重新建立30k baseline，并从同一训练轨迹的15k checkpoint执行 RecycleGS。
```

---

# 三、锁定本地 TSGS 版本

本地仓库为最终 source of truth。

执行：

```bash
cd /data/wyh/repos/TSGS

git rev-parse HEAD
git status --short
git branch --show-current
```

保存：

```bash
mkdir -p /data/wyh/RecycleGS/outputs/debug/stage0r

{
  echo "commit=$(git rev-parse HEAD)"
  echo "branch=$(git branch --show-current)"
  echo "--- git status ---"
  git status --short
} > /data/wyh/RecycleGS/outputs/debug/stage0r/tsgs_git_state.txt
```

如果 TSGS 原仓库已经被直接修改：

```text
git status --short 非空
```

不要直接训练。

先生成 diff：

```bash
git diff > \
/data/wyh/RecycleGS/outputs/debug/stage0r/tsgs_local_changes.patch
```

检查修改是否仅为：

```text
pytorch3d compatibility stub
编译环境适配
debug logging
```

如果修改涉及：

```text
train.py training loss
selective_learning_rate_control
renderer
GaussianModel
densification
```

则必须先记录并评估。

不得静默使用修改版 TSGS 作为 official baseline。

---

# 四、审计旧 baseline 配置

创建：

```text
src/baseline/audit_old_vs_official_tsgs_config.py
```

如果目录不存在：

```bash
mkdir -p src/baseline
```

读取：

```text
/data/wyh/RecycleGS/baselines/tsgs_scene01_full/cfg_args
/data/wyh/RecycleGS/baselines/tsgs_scene03_full/cfg_args
```

并读取本地：

```text
/data/wyh/repos/TSGS/run_translab.sh
```

比较以下字段：

```text
iterations
delight
normal
eval
use_asg
resolution
sd_normal_until_iter
delight_iterations
normal_cos_threshold_iter
ncc_loss_from_iter
nofix_position
nofix_opacity
nofix_param
nofix_scaling
nofix_rotation
seed
sh_degree
asg_degree
densify_until_iter
```

输出：

```text
outputs/debug/stage0r/old_vs_official_config.csv
outputs/debug/stage0r/old_vs_official_config_report.md
```

报告必须明确：

```text
OLD_BASELINE_MATCHES_OFFICIAL_TRANSLAB_RECIPE = true/false
```

根据当前已知信息，预计为：

```text
false
```

但必须由本地 cfg_args 和本地 run_translab.sh 实际验证。

---

# 五、建立官方 RecycleGS Baseline 配置

创建：

```text
configs/baseline/tsgs_translab_official_locked.yaml
```

配置必须从：

```text
/data/wyh/repos/TSGS/run_translab.sh
```

自动解析或人工逐项复制。

当前官方 recipe 预期包括：

```yaml
iterations: 30000

delight: true
normal: true
eval: true
use_asg: true

resolution: 2

sd_normal_until_iter: 30000
delight_iterations: 15000
normal_cos_threshold_iter: 3000
ncc_loss_from_iter: 7000

nofix_position: true
nofix_scaling: true
nofix_rotation: true

seed: 42
```

注意：

```text
不要设置 nofix_opacity
```

因此 opacity 在15k后应被冻结。

注意：

```text
不要设置 nofix_param
```

应保持官方脚本一致。

生成配置后计算：

```text
SHA256
```

保存：

```text
outputs/debug/stage0r/official_baseline_config_sha256.txt
```

后续 scene_01 和 scene_03 必须使用相同配置。

---

# 六、记录真实15k后参数策略

创建：

```text
src/baseline/audit_official_translab_stage2_policy.py
```

根据：

```text
run_translab.sh flags
+
GaussianModel.selective_learning_rate_control()
```

推导 iteration=15001 的 optimizer groups。

必须输出：

```text
group
trainable_after_15000
lr_at_15001
reason
```

对于官方 TransLab recipe，预期：

```text
xyz          TRAINABLE    because nofix_position=true
scaling      TRAINABLE    because nofix_scaling=true
rotation     TRAINABLE    because nofix_rotation=true

transparency TRAINABLE    default nofix_param_list
f_dc         TRAINABLE    default nofix_param_list
f_rest       TRAINABLE    default nofix_param_list
f_asg        TRAINABLE    default nofix_param_list

opacity      FROZEN       nofix_opacity=false
knn_f        FROZEN       not in retained list
```

必须以本地实际代码为准。

输出：

```text
outputs/debug/stage0r/official_stage2_parameter_policy.csv
outputs/debug/stage0r/official_stage2_parameter_policy_report.md
```

从此禁止使用：

```text
freeze xyz/scaling/rotation after 15k
```

作为“官方 TSGS Stage 2 policy”的描述。

---

# 七、重新训练 scene_01 正式 TSGS 30k baseline

新模型目录：

```text
/data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k
```

不得覆盖：

```text
tsgs_scene01_full
```

首先删除可能存在的不完整新目录：

```bash
rm -rf \
/data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k_smoke
```

## 7.1 200-step smoke

执行：

```bash
cd /data/wyh/repos/TSGS

export CUDA_VISIBLE_DEVICES=2,3

python3 train.py \
  -s /data/wyh/RecycleGS/data/translab_full/scene_01 \
  -m /data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k_smoke \
  --delight \
  --normal \
  --eval \
  --use_asg \
  --resolution 2 \
  --iterations 200 \
  --sd_normal_until_iter 30000 \
  --delight_iterations 15000 \
  --normal_cos_threshold_iter 3000 \
  --ncc_loss_from_iter 7000 \
  --nofix_position \
  --nofix_scaling \
  --nofix_rotation \
  --seed 42 \
  --save_iterations 200
```

必须验证：

```text
loss finite
normal loaded
delight image loaded
ASG enabled
Gaussian PLY generated
```

生成：

```text
outputs/debug/stage0r/scene01_official_smoke_report.md
```

---

# 八、正式30k训练命令

Smoke PASS 后执行：

```bash
cd /data/wyh/repos/TSGS

export CUDA_VISIBLE_DEVICES=2,3

python3 train.py \
  -s /data/wyh/RecycleGS/data/translab_full/scene_01 \
  -m /data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k \
  --delight \
  --normal \
  --eval \
  --use_asg \
  --resolution 2 \
  --iterations 30000 \
  --sd_normal_until_iter 30000 \
  --delight_iterations 15000 \
  --normal_cos_threshold_iter 3000 \
  --ncc_loss_from_iter 7000 \
  --nofix_position \
  --nofix_scaling \
  --nofix_rotation \
  --seed 42 \
  --test_iterations 7000 10000 15000 20000 30000 \
  --save_iterations 7000 10000 15000 20000 30000 \
  --checkpoint_iterations 7000 10000 15000 20000
```

如果本地 `train.py` 的 list 参数语法与上述不同：

```text
以 argparse help 为准。
```

先执行：

```bash
python3 train.py --help | \
grep -E "test_iterations|save_iterations|checkpoint_iterations"
```

不要因为 CLI 语法错误修改训练代码。

---

# 九、15k checkpoint 必须来自同一30k run

正式 intervention checkpoint 必须是：

```text
/data/wyh/RecycleGS/baselines/
tsgs_official_scene01_30k/
point_cloud/iteration_15000/point_cloud.ply
```

对应 training checkpoint：

```text
chkpnt15000.pth
```

或本地 TSGS 实际命名。

必须验证：

```text
model cfg_args iterations = 30000
checkpoint iteration = 15000
same model directory contains 20k/30k outputs
```

创建：

```text
src/baseline/verify_midrun_checkpoint.py
```

检查：

```text
cfg iterations == 30000
checkpoint iteration == 15000
PLY iteration == 15000
model root identical
training config SHA identical
```

输出：

```text
outputs/debug/stage0r/scene01_midrun_checkpoint_report.md
```

只有：

```text
VALID_15K_MIDRUN_CHECKPOINT = true
```

才能继续 RecycleGS。

---

# 十、评价官方 baseline 的15k与30k

创建或复用统一 TSGS 官方 render evaluator。

评价：

```text
official 15k
official 30k
```

必须使用 TSGS 当前官方 render pipeline：

```text
active SH = max SH
AppModel exact official loading
SpecularModel/ASG exact official loading
same eval split
same resolution
```

输出：

```text
outputs/baseline/official_scene01_15k_metrics.json
outputs/baseline/official_scene01_30k_metrics.json
outputs/baseline/official_scene01_training_trajectory.csv
```

trajectory 至少：

```text
iteration
PSNR
SSIM
LPIPS
Gaussian count
```

节点：

```text
7000
10000
15000
20000
30000
```

同时运行官方 mesh / geometry evaluation。

正式 Stage 0 至少保存：

```text
Chamfer
F-score@0.5%
F-score@1%
accuracy
completeness
```

不要再只使用 SPR proxy 作为 baseline geometry。

---

# 十一、不要直接复用旧 Stage 1 数组

旧结果目录：

```text
outputs/reliability/scene_01/
```

来自旧 simplified baseline。

正式结果使用：

```text
outputs/formal/reliability/scene_01/
```

正式 prune 使用：

```text
outputs/formal/prune_only/scene_01/
```

禁止混用旧：

```text
candidate_indices.npy
mask_risk_mean.npy
removed_indices.npy
geometry_errors.npz
```

原因：

```text
Gaussian数量、位置、opacity、scale和视角依赖外观均可能改变。
```

Feature extraction 代码可以复用。

结果数组不能复用。

---

# 十二、正确 baseline 上只重跑最小 Gate 链

不要重新走 Stage 1A～1G 全部历史 debug。

在 official scene_01 15k checkpoint 上直接执行当前已经验证好的最终 feature pipeline：

```text
candidate domain
→ mask_risk_mean
→ mask_risk_boundary
→ mask_risk_cv
→ continuous GT geometry evaluation
```

当前正式主特征固定为：

```text
mask_risk_mean
```

同时报告：

```text
mask_risk_boundary
mask_risk_cv
```

不再重新测试：

```text
E_support_v2
PCA normal
mask variance
```

作为主特征。

输出：

```text
outputs/formal/reliability/scene_01/gate1_metrics.json
outputs/formal/reliability/scene_01/gate1_report.md
```

scene_01 quick gate：

```text
mask_risk_mean signed rho > 0.15
bootstrap CI lower > 0
T10/B10 > 1.30
coverage >= 30%
```

如果 scene_01 FAIL：

```text
暂停。
```

说明旧 pilot signal 不迁移到真实 TSGS baseline。

不得继续 prune。

---

# 十三、scene_01 Gate 通过后重跑 Stage 2A

仅比较：

```text
random
low_opacity
low_contribution
mask_risk
oracle
```

第一轮仍使用当前 Type-A 高置信度 pool 设计。

但必须重新生成：

```text
eligible pool
K
removed indices
```

从 official 15k checkpoint 生成。

不得复用旧 K=90。

立即评价：

```text
removed mean d_center_norm
PSNR
SSIM
LPIPS
Chamfer
F-score
accuracy
completeness
```

Stage 2A quick gate：

```text
mask-risk removed geometry error > random
mask-risk removed geometry error > low-opacity
mask-risk immediate render damage <= random
geometry not worse than random
```

PASS 后才进入500步恢复。

---

# 十四、500步恢复不再使用“自创Stage-2冻结策略”

这是重点。

Stage 2B recovery 必须定义为：

> 从 official 30k run 的 chkpnt15000.pth 恢复，并继续执行 TSGS 原生 iteration 15001～15500 训练逻辑。

Schedule control：

```text
restore official chkpnt15000
→ no prune
→ native TSGS loop 15001～15500
```

Random：

```text
restore same chkpnt15000
→ optimizer-aware prune random indices
→ native TSGS loop 15001～15500
```

Mask-risk：

```text
restore same chkpnt15000
→ optimizer-aware prune mask-risk indices
→ native TSGS loop 15001～15500
```

除了：

```text
是否prune
prune谁
```

其他训练逻辑必须完全相同。

---

# 十五、创建 Native Intervention Hook

不要继续维护一份手写简化版 TSGS training loop。

创建：

```text
src/integration/tsgs_recycle_hook.py
```

接口：

```python
def apply_recycle_intervention(
    gaussians,
    optimizer,
    method,
    removed_indices_path=None,
):
    """
    Called exactly once after restoring iteration-15000 checkpoint
    and before iteration 15001.
    """
```

方法：

```text
schedule_control:
    no operation

random:
    optimizer-aware prune saved random indices

mask_risk:
    optimizer-aware prune saved mask-risk indices
```

然后创建：

```text
src/integration/run_tsgs_native_recovery.py
```

原则：

```text
复制本地 TSGS train.py 到 RecycleGS integration wrapper，
或通过最小 hook 调用官方 training()。

除 intervention hook 和 output path 外，
训练循环必须与当前本地 TSGS train.py逐行等价。
```

必须保留：

```text
selective_learning_rate_control
oneupSHdegree
delight stage behavior
original camera sampling
ASG logic
original render arguments
scale loss
single-view normal loss
SD normal loss
alpha loss
transparency loss
multi-view geo/NCC loss
optimizer step
specular optimizer
AppModel optimizer
```

禁止再次写：

```text
只用 L1 + SSIM 的简化 recovery loop
```

---

# 十六、Native Equivalence Test

在剪枝前，必须先证明 wrapper 等价。

运行：

```text
official native resume:
chkpnt15000 → 15500

RecycleGS wrapper schedule_control:
chkpnt15000 → 15500
```

使用：

```text
same seed
same cfg_args
same checkpoint
same output isolation
```

比较：

```text
PSNR
SSIM
LPIPS
Gaussian count
parameter fingerprints
```

由于 GPU/随机顺序，允许极小浮点差异。

工程要求：

```text
PSNR difference <= 0.01 dB
SSIM difference <= 1e-4
Gaussian count identical
opacity mean difference <= 1e-6
```

输出：

```text
outputs/debug/stage0r/native_wrapper_equivalence_report.md
```

只有：

```text
NATIVE_WRAPPER_EQUIVALENCE = PASS
```

才运行 random / mask-risk recovery。

---

# 十七、scene_03 暂时不要训练

当前先只做：

```text
scene_01 official 30k baseline
→ official 15k Gate 1
→ Stage 2A immediate prune
→ native-wrapper equivalence
→ random/mask recovery
```

只有 scene_01 最小链重新通过后，才训练：

```text
tsgs_official_scene03_30k
```

使用完全相同 official config。

然后只重跑：

```text
Gate 1
Stage 2A
Stage 2B
```

不再重走历史 debug。

---

# 十八、Stage 0R 最终报告

创建：

```text
src/baseline/build_stage0r_rebase_report.py
```

输出：

```text
outputs/debug/stage0r/stage0r_rebase_report.md
```

必须回答：

1. 旧 baseline 与官方 TransLab recipe 有哪些配置差异；
2. 旧15k为什么不是正式 mid-run checkpoint；
3. 官方15k来自哪条30k训练轨迹；
4. 15k和30k渲染指标；
5. 15k和30k正式几何指标；
6. official Stage-2 哪些参数继续优化；
7. native wrapper 是否与官方 resume 等价；
8. 是否允许重新进入 Gate 1。

最终结论只允许：

```text
A. OFFICIAL_BASELINE_READY
B. OFFICIAL_BASELINE_REPRODUCTION_FAILED
C. NATIVE_WRAPPER_NOT_EQUIVALENT
```

只有：

```text
OFFICIAL_BASELINE_READY
```

才重新执行正式 Gate 1。

---

# 十九、一键 Stage 0R 脚本

创建：

```text
scripts/stage0r_rebase_official_tsgs.sh
```

内容：

```bash
#!/usr/bin/env bash
set -euo pipefail

cd /data/wyh/RecycleGS

export CUDA_VISIBLE_DEVICES=2,3
export PYTHONPATH=/data/wyh/RecycleGS:/data/wyh/repos/TSGS:${PYTHONPATH:-}

mkdir -p outputs/debug/stage0r

echo "1/7 Audit old baseline config"
python3 src/baseline/audit_old_vs_official_tsgs_config.py

echo "2/7 Audit official TransLab Stage-2 policy"
python3 src/baseline/audit_official_translab_stage2_policy.py

echo "3/7 Verify local TSGS git state"
test -f outputs/debug/stage0r/tsgs_git_state.txt

echo "4/7 Run official scene_01 smoke"
bash scripts/stage0r_scene01_official_smoke.sh

echo "5/7 Train official scene_01 30k baseline"
bash scripts/stage0r_scene01_official_30k.sh

echo "6/7 Verify 15k mid-run checkpoint and evaluate trajectory"
python3 src/baseline/verify_midrun_checkpoint.py \
  --model-dir \
  /data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k \
  --iteration 15000

python3 src/baseline/evaluate_official_tsgs_trajectory.py \
  --model-dir \
  /data/wyh/RecycleGS/baselines/tsgs_official_scene01_30k \
  --iterations 7000 10000 15000 20000 30000

echo "7/7 Build Stage 0R report"
python3 src/baseline/build_stage0r_rebase_report.py

echo "Stage 0R official baseline rebase completed."
```

完成后停止。

禁止自动执行：

```text
Stage 1 prune
Stage 2 recovery
Stage 3 reseed
```

直到：

```text
stage0r_rebase_report.md
```

结论为：

```text
OFFICIAL_BASELINE_READY
```
