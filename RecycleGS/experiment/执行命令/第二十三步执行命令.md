继续执行 RecycleGS 项目。

# 一、当前正式结论

Stage 2B-AB 已确认：

```text
所有现有 recovery_500 目录均为旧 buggy recovery artifact。

recovery_500_fixed 不存在。
```

旧 recovery 中：

```text
selective_learning_rate_control 未调用

baseline raw opacity mean = 0.806394
buggy recovery raw opacity mean = -2.922602

baseline effective opacity ≈ 0.691
buggy recovery effective opacity ≈ 0.051
```

因此旧 recovery 的 PSNR collapse：

```text
22.39 → 8.62
```

根因已经确认：

> opacity 等本应在 iteration > 15000 后冻结的参数，在 custom recovery 中仍然继续使用非零 learning rate 优化，导致 opacity collapse。

所有旧：

```text
outputs/prune_only/*/ratio_005/*/recovery_500/
```

均标记：

```text
INVALID_BUGGY_RECOVERY
```

不得继续用于任何 Gate 或论文实验。

当前进入：

> Stage 2B-AC：Generate First Valid Fixed-Policy Recovery Artifact

---

# 二、纠正 selective policy 的参数语义

必须按照本地：

```text
/data/wyh/repos/TSGS/scene/gaussian_model.py
```

的真实实现。

默认：

```text
nofix_param = false
```

时：

```python
nofix_param_list = [
    "transparency",
    "f_dc",
    "f_rest",
    "f_asg",
]
```

iteration > 15000 时：

```python
if param_group["name"] not in nofix_param_list:
    param_group["lr"] = 0.0
```

因此当前 scene_01 默认训练策略应为：

```text
继续优化：
- transparency
- f_dc
- f_rest
- f_asg（仅当存在）

冻结：
- xyz
- knn_f
- opacity
- scaling
- rotation
```

注意：

```text
f_rest 不冻结
transparency 不冻结
```

禁止使用 Stage 2B-AB 报告中错误的：

```text
freeze f_rest
freeze transparency
freeze f_asg
```

作为恢复策略。

---

# 三、保存本地 TSGS policy 证据

执行：

```bash
cd /data/wyh/RecycleGS

mkdir -p outputs/debug/stage2bac

sed -n '2075,2137p' \
  /data/wyh/repos/TSGS/scene/gaussian_model.py \
  > outputs/debug/stage2bac/local_selective_lr_control.txt

sed -n '1814,1826p' \
  /data/wyh/repos/TSGS/train.py \
  > outputs/debug/stage2bac/local_train_policy_call.txt
```

创建：

```text
outputs/debug/stage2bac/policy_manifest.json
```

内容根据实际源码自动生成：

```json
{
  "freeze_iter": 15000,
  "trainable_after_freeze": [
    "transparency",
    "f_dc",
    "f_rest"
  ],
  "conditionally_trainable": [
    "f_asg"
  ],
  "frozen_after_freeze": [
    "xyz",
    "knn_f",
    "opacity",
    "scaling",
    "rotation"
  ]
}
```

如果原始 `cfg_args` 中任意 `nofix_*` 为 true，根据真实配置修改。

不得硬编码与 cfg_args 冲突的 freeze list。

---

# 四、彻底隔离旧 buggy artifact

不要删除旧结果。

创建：

```bash
mkdir -p outputs/archive/stage2b_buggy_artifacts
```

生成：

```text
outputs/archive/stage2b_buggy_artifacts/INVALID_ARTIFACTS.md
```

内容：

```markdown
# INVALID Stage 2B Recovery Artifacts

The following `recovery_500` artifacts were generated before
`selective_learning_rate_control()` was added to
`train_pruned_recovery.py`.

They exhibit opacity collapse and must not be used for evaluation:

- scene_01 schedule_control
- scene_01 random
- scene_01 low_opacity
- scene_01 low_contribution
- scene_01 mask_risk
- scene_03 schedule_control
- scene_03 random
- scene_03 low_opacity
- scene_03 low_contribution
- scene_03 mask_risk

Observed failure:

effective opacity approximately 0.69 -> 0.05.

Status:

INVALID_BUGGY_RECOVERY
```

所有 evaluation / Gate 脚本增加保护：

```python
if recovery_dir.name == "recovery_500":
    raise RuntimeError(
        "Legacy buggy recovery artifact is forbidden. "
        "Use recovery_500_fixed."
    )
```

仅 debug 脚本允许通过：

```text
--allow-invalid-legacy-artifact
```

加载旧结果。

---

# 五、修正 train_pruned_recovery.py

目标：

```text
src/prune/train_pruned_recovery.py
```

恢复过程必须：

```text
1. load original iteration-15000 checkpoint
2. GaussianModel.restore(...)
3. apply optimizer-aware prune if required
4. enter global iteration 15001
5. call selective_learning_rate_control every iteration
6. backward
7. optimizer step
8. no densification
9. no opacity reset
10. no additional prune
```

训练循环结构必须与官方 TSGS 顺序一致：

```python
for global_iteration in range(15001, 15501):

    gaussians.selective_learning_rate_control(
        global_iteration,
        15000,
        nofix_position=opt.nofix_position,
        nofix_opacity=opt.nofix_opacity,
        nofix_param=opt.nofix_param,
        nofix_scaling=opt.nofix_scaling,
        nofix_rotation=opt.nofix_rotation,
    )

    if global_iteration % 1000 == 0:
        gaussians.oneupSHdegree()

    # official-compatible camera sampling
    # official-compatible GT image selection
    # official-compatible rendering
    # official-compatible losses

    loss.backward()

    gaussians.optimizer.step()
    gaussians.optimizer.zero_grad(set_to_none=True)
```

如果当前 custom recovery 使用 AppModel：

由于 scene_01：

```text
exposure_compensation = false
gaussians.use_app = false
AppModel weights = all zeros
```

可以继续创建 AppModel 以保持官方代码路径兼容。

但不得声称 AppModel 是 recovery 的有效优化模块。

优先保持与 TSGS 官方 loop 一致，不要为了“简化”再次改写 loss/render 流程。

---

# 六、训练前执行 Freeze Assertion

创建：

```text
src/prune/assert_stage2_policy.py
```

加载原始 15k checkpoint。

执行：

```python
gaussians.selective_learning_rate_control(
    15001,
    15000,
    nofix_position=opt.nofix_position,
    nofix_opacity=opt.nofix_opacity,
    nofix_param=opt.nofix_param,
    nofix_scaling=opt.nofix_scaling,
    nofix_rotation=opt.nofix_rotation,
)
```

输出所有 optimizer group：

```text
name
lr
```

scene_01 必须检查：

```text
xyz       == 0
knn_f     == 0
opacity   == 0
scaling   == 0
rotation  == 0

f_dc          > 0
f_rest        > 0
transparency  > 0
```

如果存在：

```text
f_asg
```

按 cfg_args 判断。

任何 freeze assertion 失败：

```text
exit 1
```

不得开始500步。

输出：

```text
outputs/debug/stage2bac/freeze_assertion.json
outputs/debug/stage2bac/freeze_assertion_report.md
```

---

# 七、只重新运行 scene_01 schedule_control

当前只允许运行：

```text
scene_01
schedule_control
500 steps
```

禁止：

```text
random
mask_risk
low_opacity
low_contribution
scene_03
```

输出新目录：

```text
outputs/prune_only/scene_01/ratio_005/
schedule_control/recovery_500_fixed/
```

不得覆盖：

```text
recovery_500/
```

---

# 八、固定恢复状态来源

起点必须为：

```text
/data/wyh/RecycleGS/baselines/
tsgs_scene01_full/
iteration-15000 training checkpoint
```

禁止从旧：

```text
recovery_500/point_cloud.ply
```

继续训练。

禁止从：

```text
point_cloud.ply
```

重新初始化 optimizer。

必须：

```text
GaussianModel.restore()
```

恢复原15k：

```text
Gaussian parameters
optimizer state
per-Gaussian buffers
active_sh_degree
spatial_lr_scale
```

---

# 九、训练中监控 opacity collapse

在：

```text
15000
15001
15002
15005
15010
15020
15100
15250
15500
```

保存参数统计。

输出：

```text
recovery_500_fixed/parameter_trace.csv
```

列：

```text
iteration
N

xyz_mean
xyz_max_delta_from_15000

opacity_raw_mean
opacity_raw_std
opacity_effective_mean
opacity_max_delta_from_15000

scaling_max_delta_from_15000
rotation_max_delta_from_15000

f_dc_mean
f_dc_max_delta_from_15000

f_rest_mean
f_rest_max_delta_from_15000

transparency_raw_mean
transparency_effective_mean
```

冻结参数要求：

```text
xyz max delta <= 1e-7
opacity max delta <= 1e-7
scaling max delta <= 1e-7
rotation max delta <= 1e-7
```

特别要求：

```text
opacity_effective_mean
```

必须保持接近 15k 起点。

禁止再次出现：

```text
0.69 -> 0.05
```

如果：

```text
opacity max delta > 1e-6
```

立即：

```text
POLICY_FAILURE
exit 1
```

---

# 十、使用正式 SH 配置

正式 evaluator 不允许：

```text
active_sh_degree = 0
```

评价模型时：

```python
gaussians = GaussianModel(
    dataset.sh_degree,
    dataset.asg_degree,
)

gaussians.load_ply(exact_ply_path)
```

然后保留：

```text
gaussians.active_sh_degree
```

由 `load_ply()` 设置。

当前 scene_01：

```text
dataset.sh_degree = 3
```

预期：

```text
active_sh_degree = 3
max_sh_degree = 3
```

正式 Gate evaluator 必须断言：

```python
assert gaussians.active_sh_degree == gaussians.max_sh_degree
```

SH=0 仅作为 debug matrix。

---

# 十一、统一 baseline 和 recovery evaluator

当前日志存在：

```text
baseline PSNR = 22.3939
```

以及 SH audit：

```text
baseline SH3 PSNR = 23.6824
```

这两个数仍然不一致。

因此创建：

```text
src/evaluation/evaluate_stage2bac_exact.py
```

只允许一个 evaluator。

评价：

```text
A. baseline 15000 PLY
B. fixed schedule-control 15500 PLY
```

必须：

```text
same exact view names
same camera split
same GT image source
same resolution
same background
same pipeline parameters
same active SH degree=3
same AppModel disabled/effectively inactive
same SpecularModel disabled
```

输出：

```text
outputs/debug/stage2bac/exact_evaluator_identity.json
```

包含：

```text
view_names
num_views
image_source
resolution
background
sh_degree
active_sh_degree
white_background
use_app
use_asg
T_threshold
observe_T_threshold
```

计算 SHA256：

```text
evaluator_identity_sha256
```

A 与 B 必须完全相同。

---

# 十二、Baseline Exact Evaluation

使用：

```text
/data/wyh/RecycleGS/baselines/
tsgs_scene01_full/
point_cloud/iteration_15000/point_cloud.ply
```

输出：

```text
outputs/debug/stage2bac/
baseline_exact_metrics.json
```

保存：

```text
psnr
ssim
lpips
num_views
active_sh_degree
PLY SHA
evaluator SHA
```

从此 Stage 2B-AC 使用该 baseline。

不得再混用：

```text
22.3939
23.6824
```

最终报告必须明确哪一个是 exact evaluator 的正式数值。

---

# 十三、Fixed Schedule-Control Evaluation

500步完成后评价：

```text
outputs/prune_only/scene_01/ratio_005/
schedule_control/recovery_500_fixed/
point_cloud/iteration_15500/point_cloud.ply
```

输出：

```text
recovery_500_fixed/render_metrics_exact.json
```

必须保存：

```text
PSNR
SSIM
LPIPS
active_sh_degree
PLY SHA
evaluator SHA
```

---

# 十四、Stage 2B-AC Gate

定义：

```text
P0 = baseline exact evaluator PSNR
P500 = fixed schedule-control exact evaluator PSNR
```

必须检查：

## C1 Fixed Artifact Exists

```text
recovery_500_fixed exists
```

## C2 Correct Policy

```text
selective_learning_rate_control called 500 times
```

## C3 Frozen Geometry

```text
xyz stable
opacity stable
scaling stable
rotation stable
```

## C4 No Opacity Collapse

```text
abs(
opacity_effective_mean_15500
-
opacity_effective_mean_15000
) <= 1e-6
```

由于 opacity 冻结，应近似完全相同。

## C5 Count Stable

```text
118557 -> 118557
```

## C6 Unified Exact Evaluator

```text
baseline evaluator SHA
==
recovery evaluator SHA
```

## C7 Rendering Stability

工程 Gate：

```text
P500 >= P0 - 0.50 dB
```

同时报告：

```text
delta_psnr
delta_ssim
delta_lpips
```

---

# 十五、判断规则

## PASS

满足：

```text
C1-C7 全部通过
```

结论：

> Fixed-policy schedule-control recovery is stable.

下一步只运行：

```text
scene_01 random recovery_500_fixed
scene_01 mask_risk recovery_500_fixed
```

---

## POLICY_PASS_RENDER_FAIL

满足：

```text
C1-C6 PASS
C7 FAIL
```

即：

```text
opacity/geometry 确实冻结
evaluator 完全一致
但 appearance refinement 500步仍下降 > 0.5 dB
```

此时：

```text
不要运行 random/mask_risk
```

下一步运行：

```text
native TSGS 15000 -> 15500 resume control
```

用于判断 custom loop 是否仍与官方训练不等价。

---

## POLICY_FAILURE

满足任意：

```text
opacity发生变化
xyz发生变化
scaling发生变化
rotation发生变化
```

停止。

修复 recovery policy。

---

# 十六、创建一键脚本

创建：

```text
scripts/stage2bac_generate_first_fixed_recovery.sh
```

内容：

```bash
#!/usr/bin/env bash
set -euo pipefail

cd /data/wyh/RecycleGS

export CUDA_VISIBLE_DEVICES=2,3
export PYTHONPATH=/data/wyh/RecycleGS:/data/wyh/repos/TSGS:${PYTHONPATH:-}

SCENE=configs/stage1/reliability_scene01.yaml
RECOVERY=configs/stage2/recovery_500_locked.yaml

mkdir -p outputs/debug/stage2bac

echo "1/5 Assert official Stage-2 policy"
python3 src/prune/assert_stage2_policy.py \
  --scene-config $SCENE \
  --recovery-config $RECOVERY

echo "2/5 Evaluate exact baseline"
python3 src/evaluation/evaluate_stage2bac_exact.py \
  --scene-config $SCENE \
  --model baseline

echo "3/5 Run first valid fixed schedule-control recovery"
python3 src/prune/train_pruned_recovery.py \
  --scene-config $SCENE \
  --recovery-config $RECOVERY \
  --method schedule_control \
  --output-dir \
  outputs/prune_only/scene_01/ratio_005/\
schedule_control/recovery_500_fixed \
  --seed 0

echo "4/5 Evaluate fixed schedule-control"
python3 src/evaluation/evaluate_stage2bac_exact.py \
  --scene-config $SCENE \
  --model fixed_schedule_control

echo "5/5 Build Stage 2B-AC gate"
python3 src/prune/evaluate_stage2bac_gate.py \
  --scene-config $SCENE \
  --recovery-config $RECOVERY

echo "Stage 2B-AC completed."
```

执行：

```bash
chmod +x \
scripts/stage2bac_generate_first_fixed_recovery.sh

bash \
scripts/stage2bac_generate_first_fixed_recovery.sh
```

---

# 十七、必须生成

```text
outputs/debug/stage2bac/
policy_manifest.json

outputs/debug/stage2bac/
freeze_assertion_report.md

outputs/debug/stage2bac/
exact_evaluator_identity.json

outputs/debug/stage2bac/
baseline_exact_metrics.json

outputs/prune_only/scene_01/ratio_005/
schedule_control/recovery_500_fixed/
parameter_trace.csv

outputs/prune_only/scene_01/ratio_005/
schedule_control/recovery_500_fixed/
render_metrics_exact.json

outputs/debug/stage2bac/
stage2bac_gate_report.md
```

完成后停止。

禁止自动运行：

```text
random fixed recovery
mask-risk fixed recovery
scene_03
Stage 3
reseed
```

下一步严格依据：

```text
stage2bac_gate_report.md
```

只接受：

```text
PASS
POLICY_PASS_RENDER_FAIL
POLICY_FAILURE
```
