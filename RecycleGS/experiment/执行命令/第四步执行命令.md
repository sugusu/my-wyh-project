你现在继续执行 RecycleGS 项目。注意：最大主线已经确认，主实验底座必须是 **TSGS**，不是 vanilla 3DGS。

当前进度：

* Step1 环境初始化完成；
* vanilla 3DGS 尝试失败，但这条线不是主线；
* 本机没有现成 TSGS 代码或 checkpoint；
* 现在需要下载 TSGS 官方代码并训练 TSGS baseline；
* 目标是得到真正的 TSGS Gaussian checkpoint，用于后续 RecycleGS reliability / prune / recycle。

---

# 0. 固定要求

工作根目录：

```bash
/data/wyh/
```

RecycleGS 项目目录：

```bash
/data/wyh/RecycleGS/
```

TransLab scene_01 数据路径：

```bash
/data/wyh/f4splat/data/translab/scene_01/
```

实时中文日志：

```bash
/data/wyh/RecycleGS/realtime_log.md
```

GPU 限制：

```bash
export CUDA_VISIBLE_DEVICES=2,3
```

注意：

* 只允许使用物理 GPU 2 和 3；
* 程序内部看到的 `cuda:0` 对应物理 GPU 2；
* 不要再尝试 vanilla 3DGS；
* 不要把 COLMAP 的 `sparse/points3D.ply` 当作 Gaussian checkpoint；
* 不要删除已有文件。

---

# 1. 进入项目并恢复日志函数

````bash
cd /data/wyh/RecycleGS
export CUDA_VISIBLE_DEVICES=2,3

log() {
  echo "" | tee -a /data/wyh/RecycleGS/realtime_log.md
  echo "### $(date '+%Y-%m-%d %H:%M:%S') $1" | tee -a /data/wyh/RecycleGS/realtime_log.md
}

run_cmd() {
  log "执行命令：$*"
  echo '```bash' >> /data/wyh/RecycleGS/realtime_log.md
  echo "$*" >> /data/wyh/RecycleGS/realtime_log.md
  echo '```' >> /data/wyh/RecycleGS/realtime_log.md
  echo "" >> /data/wyh/RecycleGS/realtime_log.md
  echo '```text' >> /data/wyh/RecycleGS/realtime_log.md
  eval "$*" 2>&1 | tee -a /data/wyh/RecycleGS/realtime_log.md
  echo '```' >> /data/wyh/RecycleGS/realtime_log.md
}

log "开始 Step4：获取 TSGS 官方代码并训练 TSGS baseline。"
````

---

# 2. 创建代码仓库目录

```bash
run_cmd "mkdir -p /data/wyh/repos"
cd /data/wyh/repos
```

---

# 3. 克隆 TSGS 官方仓库

先直接克隆：

```bash
run_cmd "git clone --recursive https://github.com/longxiang-ai/TSGS.git"
```

如果 GitHub 直连失败，改用镜像：

```bash
run_cmd "git clone --recursive https://gh-proxy.com/https://github.com/longxiang-ai/TSGS.git"
```

如果仓库已存在，则进入并更新：

```bash
cd /data/wyh/repos/TSGS
run_cmd "git status || true"
run_cmd "git submodule update --init --recursive || true"
```

如果克隆失败，停止并生成：

```bash
mkdir -p /data/wyh/RecycleGS/outputs/debug
cat > /data/wyh/RecycleGS/outputs/debug/tsgs_clone_failure.md <<'EOF'
# TSGS 克隆失败报告

TSGS 官方仓库克隆失败。

当前不能进入 RecycleGS 主实验，因为缺少 TSGS 代码和 TSGS Gaussian checkpoint。

需要手动上传 TSGS 源码到：
/data/wyh/repos/TSGS
EOF
```

然后停止。

---

# 4. 检查 TSGS 仓库结构

```bash
cd /data/wyh/repos/TSGS

run_cmd "pwd"
run_cmd "ls -lah"
run_cmd "find . -maxdepth 2 -type f | sort | head -200"
run_cmd "find . -maxdepth 3 -type d | sort | head -200"
```

重点确认是否存在：

```text
train.py
render.py
README.md
requirements.txt
scene/
gaussian_renderer/
arguments/
submodules/
```

如果文件结构不同，不要自己猜。先读 README 和帮助命令。

---

# 5. 读取 TSGS README 和训练参数

```bash
run_cmd "sed -n '1,260p' README.md || true"
run_cmd "python3 train.py -h || true"
run_cmd "python3 train.py --help || true"
```

把读取到的训练方式整理到：

```bash
mkdir -p /data/wyh/RecycleGS/outputs/debug

cat > /data/wyh/RecycleGS/outputs/debug/tsgs_train_command_notes.md <<'EOF'
# TSGS 训练命令解析记录

待 Codex 根据 README.md 和 train.py --help 填写：

- 数据路径参数：
- 输出路径参数：
- iteration 参数：
- normal 参数：
- de-lighting / delight 参数：
- mask 参数：
- save_iterations 参数：
- checkpoint_iterations 参数：
- 需要软链接的目录：
EOF
```

然后根据 README 和 help 输出补全这个文件。

---

# 6. 安装依赖

先安装通用依赖：

```bash
run_cmd "python3 -m pip install --user tqdm plyfile opencv-python-headless imageio matplotlib scikit-learn pyyaml trimesh ninja -i https://pypi.tuna.tsinghua.edu.cn/simple"
```

如果有 `requirements.txt`：

```bash
run_cmd "python3 -m pip install --user -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple || true"
```

如果仓库里有 3DGS 子模块：

```bash
run_cmd "find . -maxdepth 4 -type d \\( -iname 'diff-gaussian-rasterization' -o -iname 'simple-knn' \\) | sort"
```

如果存在：

```bash
run_cmd "MAX_JOBS=4 python3 -m pip install --user submodules/diff-gaussian-rasterization --no-build-isolation || true"
run_cmd "MAX_JOBS=4 python3 -m pip install --user submodules/simple-knn --no-build-isolation || true"
```

如果子模块目录名或位置不同，根据实际路径安装。

如果出现 `glm/glm.hpp` 缺失，执行：

```bash
run_cmd "find . -maxdepth 5 -type d -iname 'glm' | sort"
```

如果 `third_party/glm` 缺失，尝试：

```bash
run_cmd "git submodule update --init --recursive"
```

如果 GitHub 子模块无法拉取，尝试镜像方式或记录失败，不要继续训练。

如果出现 `Python.h` 缺失，记录错误。不要使用 sudo。生成安装失败报告，说明缺少 `python3-dev`，当前用户无 sudo 权限。

---

# 7. 检查 Python / Torch / CUDA

```bash
run_cmd "python3 --version"

run_cmd "python3 - <<'PY'
import torch
print('torch:', torch.__version__)
print('cuda available:', torch.cuda.is_available())
print('visible cuda count:', torch.cuda.device_count())
if torch.cuda.is_available():
    print('visible cuda 0:', torch.cuda.get_device_name(0))
PY"
```

---

# 8. 检查 TransLab scene_01 数据

```bash
run_cmd "find /data/wyh/f4splat/data/translab/scene_01 -maxdepth 2 -type d | sort"
run_cmd "find /data/wyh/f4splat/data/translab/scene_01/images -maxdepth 1 -type f | sort"
run_cmd "find /data/wyh/f4splat/data/translab/scene_01/transparent_masks -maxdepth 1 -type f | sort"
run_cmd "find /data/wyh/f4splat/data/translab/scene_01/normals -maxdepth 1 -type f | sort"
run_cmd "find /data/wyh/f4splat/data/translab/scene_01/delights -maxdepth 1 -type f | sort"
run_cmd "find /data/wyh/f4splat/data/translab/scene_01/sparse -maxdepth 2 -type f | sort"
```

如果 TSGS 需要特定目录名，不要移动原文件，只建软链接：

```bash
cd /data/wyh/f4splat/data/translab/scene_01

if [ ! -d normal ] && [ -d normals ]; then
  ln -s normals normal
fi

if [ ! -d transparent_mask ] && [ -d transparent_masks ]; then
  ln -s transparent_masks transparent_mask
fi

if [ ! -d delight ] && [ -d delights ]; then
  ln -s delights delight
fi
```

然后记录：

```bash
cd /data/wyh/RecycleGS
run_cmd "ls -lah /data/wyh/f4splat/data/translab/scene_01"
```

---

# 9. TSGS smoke test

根据 README 和 `train.py --help` 确认真实参数后，再运行 smoke test。

输出目录：

```bash
/data/wyh/RecycleGS/baselines/tsgs_scene01_smoke/
```

优先尝试类似命令：

```bash
cd /data/wyh/repos/TSGS
export CUDA_VISIBLE_DEVICES=2,3

run_cmd "CUDA_VISIBLE_DEVICES=2,3 python3 train.py \
  -s /data/wyh/f4splat/data/translab/scene_01 \
  -m /data/wyh/RecycleGS/baselines/tsgs_scene01_smoke \
  --iterations 200 \
  --save_iterations 200 \
  --checkpoint_iterations 200"
```

注意：

* 如果 TSGS 参数不是 `-s` / `-m`，必须使用 README / help 中的真实参数；
* 如果 TSGS 需要 `--use_normal`、`--use_delight`、`--use_mask` 等参数，只有在 help 中确认存在后才能添加；
* 不要凭空添加不存在的参数。

smoke test 完成后检查：

```bash
run_cmd "find /data/wyh/RecycleGS/baselines/tsgs_scene01_smoke -maxdepth 5 -type f | sort | head -200"
run_cmd "find /data/wyh/RecycleGS/baselines/tsgs_scene01_smoke -path '*point_cloud*point_cloud.ply' -type f | sort"
```

如果没有生成 `point_cloud.ply`，停止并写：

```bash
cat > /data/wyh/RecycleGS/outputs/debug/tsgs_smoke_failure.md <<'EOF'
# TSGS Smoke Test 失败

TSGS smoke test 没有生成 point_cloud.ply。

不能进入正式训练。

请检查：
- 训练命令参数是否正确；
- 数据格式是否符合 TSGS 要求；
- CUDA 子模块是否安装成功；
- normal / delight / mask 路径是否正确；
- 日志中的具体报错。
EOF
```

---

# 10. 正式训练 TSGS baseline

如果 smoke test 成功，再正式训练。

输出目录：

```bash
/data/wyh/RecycleGS/baselines/tsgs_scene01/
```

先训练到 15000 iteration：

```bash
cd /data/wyh/repos/TSGS
export CUDA_VISIBLE_DEVICES=2,3

run_cmd "CUDA_VISIBLE_DEVICES=2,3 python3 train.py \
  -s /data/wyh/f4splat/data/translab/scene_01 \
  -m /data/wyh/RecycleGS/baselines/tsgs_scene01 \
  --iterations 15000 \
  --test_iterations 7000 15000 \
  --save_iterations 7000 15000 \
  --checkpoint_iterations 7000 15000"
```

同样，如果实际参数不同，使用 TSGS 真实参数。

训练结束后检查：

```bash
run_cmd "find /data/wyh/RecycleGS/baselines/tsgs_scene01 -maxdepth 5 -type f | sort | head -300"
run_cmd "find /data/wyh/RecycleGS/baselines/tsgs_scene01 -path '*point_cloud*point_cloud.ply' -type f | sort"
```

---

# 11. 验证输出是否是真正 Gaussian PLY

```bash
run_cmd "python3 - <<'PY'
from plyfile import PlyData
from pathlib import Path

root = Path('/data/wyh/RecycleGS/baselines/tsgs_scene01')
candidates = list(root.rglob('point_cloud.ply'))
print('找到 point_cloud.ply 数量:', len(candidates))

required = [
    'opacity',
    'scale_0', 'scale_1', 'scale_2',
    'rot_0', 'rot_1', 'rot_2', 'rot_3',
    'f_dc_0', 'f_dc_1', 'f_dc_2'
]

for p in candidates:
    print('\\nPLY:', p)
    ply = PlyData.read(str(p))
    names = ply['vertex'].data.dtype.names
    print('字段:', names)
    ok = all(x in names for x in required)
    print('is_gaussian_ply =', ok)
PY"
```

必须看到：

```text
is_gaussian_ply = True
```

---

# 12. 更新 RecycleGS 配置

如果存在：

```bash
/data/wyh/RecycleGS/baselines/tsgs_scene01/point_cloud/iteration_15000/point_cloud.ply
```

则更新：

```bash
/data/wyh/RecycleGS/configs/scene_01.yaml
```

加入：

```yaml
baseline_type: TSGS
baseline_model_dir: /data/wyh/RecycleGS/baselines/tsgs_scene01
baseline_gaussian_checkpoint: /data/wyh/RecycleGS/baselines/tsgs_scene01/point_cloud/iteration_15000/point_cloud.ply
```

如果训练到 30000，则 checkpoint 改为：

```yaml
baseline_gaussian_checkpoint: /data/wyh/RecycleGS/baselines/tsgs_scene01/point_cloud/iteration_30000/point_cloud.ply
```

---

# 13. 生成 TSGS baseline 报告

创建：

```bash
/data/wyh/RecycleGS/baselines/tsgs_scene01_training_report.md
```

中文写明：

1. TSGS 仓库路径；
2. 数据路径；
3. 使用的训练命令；
4. 是否完成 smoke test；
5. 是否完成正式训练；
6. 训练到多少 iteration；
7. 最终 Gaussian checkpoint 路径；
8. PLY 字段是否满足 Gaussian checkpoint；
9. 是否可以进入 RecycleGS Stage 1；
10. 如果失败，失败原因是什么。

---

# 14. 阶段结束条件

成功条件：

```text
/data/wyh/RecycleGS/baselines/tsgs_scene01/point_cloud/iteration_15000/point_cloud.ply 存在
```

并且字段检查：

```text
is_gaussian_ply = True
```

成功后在日志末尾写：

```markdown
## TSGS baseline 训练结论

已成功生成 TSGS Gaussian checkpoint。

下一阶段可以进入 RecycleGS Stage 1：
- 读取 TSGS Gaussian PLY；
- 计算 mesh distance；
- 计算 mask support / normal conflict / depth conflict；
- 评估 reliability score 是否能识别错误 Gaussian。
```

失败后写：

```markdown
## TSGS baseline 训练失败结论

当前仍没有 TSGS Gaussian checkpoint，不能进入 RecycleGS 主实验。

下一步需要：
- 修复 TSGS 安装；
- 或手动上传可运行的 TSGS 源码；
- 或提供已有 TSGS checkpoint。
```

现在开始执行。
