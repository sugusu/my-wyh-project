继续执行 RecycleGS 项目。

# 一、当前状态修正

当前 Stage 2B-AA 已基本排除：

```text
AppModel state mismatch
SpecularModel state mismatch
```

scene_01：

```text
exposure_compensation = false
use_app = false
use_asg = false
AppModel weights all zero
```

因此 AppModel 和 SpecularModel 暂不再作为根因调查重点。

但是 Stage 2B-AA 暴露两个新的关键矛盾：

```text
1. 审计报告使用 recovery_500 路径，
   可能读取的是 selective policy 修复前的旧 recovery artifact。

2. 当前 unified evaluator 使用 active_sh_degree=0，
   而 TSGS 官方 load_ply() 会将 active_sh_degree 设置为 max_sh_degree。
```

本阶段名称：

> Stage 2B-AB：Recovery Artifact Identity and SH-Degree Render Audit

当前禁止：

```text
重新训练500步
Stage 3
reseed
修改mask-risk
修改删除比例
继续调查AppModel
```

只做已有模型的严格加载与渲染审计。

---

# 二、首先区分旧 recovery 与 fixed recovery

创建：

```text
src/evaluation/audit_recovery_artifact_identity.py
```

对以下目录递归检查：

```text
outputs/prune_only/scene_01/ratio_005/
schedule_control/recovery_500/

outputs/prune_only/scene_01/ratio_005/
schedule_control/recovery_500_fixed/
```

如果 `recovery_500_fixed` 不存在，也必须明确输出。

每个目录记录：

```text
directory
exists

PLY path
PLY SHA256
PLY Gaussian count

training_log path
training start iteration
training end iteration

selective_learning_rate_control recorded
policy_fix_version
creation/modification timestamp

xyz frozen trace exists
opacity frozen trace exists
```

同时对源码记录：

```text
train_pruned_recovery.py SHA256
```

输出：

```text
outputs/debug/stage2bab/
recovery_artifact_identity.json

outputs/debug/stage2bab/
recovery_artifact_identity_report.md
```

报告必须明确给出：

```text
OLD_BUGGY_ARTIFACT
FIXED_POLICY_ARTIFACT
UNKNOWN_ARTIFACT
```

不能把不同 recovery 目录混用。

---

# 三、禁止默认选择 recovery_500

所有后续脚本必须显式接收：

```text
--recovery-dir
```

例如：

```bash
--recovery-dir \
outputs/prune_only/scene_01/ratio_005/\
schedule_control/recovery_500_fixed
```

不得：

```python
recovery_dir = ".../recovery_500"
```

写死。

如果 fixed artifact 不存在：

```text
FIXED_ARTIFACT_MISSING
```

本阶段仍先完成 baseline/旧模型 SH 审计。

不要自动重新训练。

---

# 四、建立严格 PLY 加载证明

创建：

```text
src/evaluation/debug_exact_ply_loader.py
```

输入：

```text
--scene-config
--ply
```

必须：

1. 创建 `GaussianModel(sh_degree=3)`；
2. 直接执行本地 TSGS `load_ply(exact_path)`；
3. 不使用 `Scene(... load_iteration=...)` 自动选择 PLY；
4. 打印 exact path；
5. 计算 SHA256；
6. 加载后计算 Gaussian 参数 fingerprint。

fingerprint：

```text
N
xyz_sum
xyz_mean
xyz_std

f_dc_sum
f_dc_mean
f_dc_std

f_rest_sum
f_rest_mean
f_rest_std

opacity_sum
opacity_mean
opacity_std

transparency_sum
scaling_sum
rotation_sum
```

保存：

```text
loaded_ply_path
loaded_ply_sha256
active_sh_degree_after_load
max_sh_degree
parameter_fingerprint
```

必须断言：

```python
actual_sha256 == requested_sha256
```

输出：

```text
outputs/debug/stage2bab/exact_ply_loads/<name>.json
```

---

# 五、检查 TSGS 官方 SH 行为

本地 source of truth：

```text
/data/wyh/repos/TSGS/scene/gaussian_model.py
```

确认：

```python
def __init__(...):
    self.active_sh_degree = 0
```

以及：

```python
def load_ply(...):
    ...
    self.active_sh_degree = self.max_sh_degree
```

生成：

```text
outputs/debug/stage2bab/local_sh_policy_report.md
```

报告：

```text
dataset.sh_degree
GaussianModel.max_sh_degree
active_sh_degree immediately after constructor
active_sh_degree after load_ply
```

对于当前 scene_01 预期：

```text
max_sh_degree = 3
active after constructor = 0
active after PLY load = 3
```

如果本地仓库与此不同，以本地代码为准。

---

# 六、删除 Unified Evaluator 中强制 SH=0 的行为

检查：

```text
src/evaluation/tsgs_unified_renderer.py
src/evaluation/unified_recovery_evaluator.py
src/evaluation/evaluate_recovery_500.py
```

搜索：

```bash
grep -RniE \
"active_sh_degree.*=.*0|sh_degree.*=.*0|force.*sh|override.*sh" \
src/evaluation src/recyclegs
```

禁止正式评价中存在：

```python
gaussians.active_sh_degree = 0
```

或：

```python
GaussianModel(0)
```

正式 evaluator 必须：

```text
创建 GaussianModel(dataset.sh_degree)
→ exact load_ply()
→ 保持 load_ply 设置的 active_sh_degree
```

不得再次覆盖。

新增 debug 参数：

```text
--override-active-sh-degree
```

但只能用于 Stage 2B-AB 诊断。

正式 Gate evaluator 不得使用该参数。

---

# 七、执行 PLY × SH Degree 评价矩阵

创建：

```text
src/evaluation/evaluate_ply_sh_degree_matrix.py
```

第一轮只运行 scene_01。

评价模型：

## Model A：Baseline

```text
/data/wyh/RecycleGS/baselines/tsgs_scene01_full/
point_cloud/iteration_15000/point_cloud.ply
```

## Model B：Old Buggy Recovery

如果存在：

```text
outputs/prune_only/scene_01/ratio_005/
schedule_control/recovery_500/
point_cloud/iteration_15500/point_cloud.ply
```

## Model C：Fixed Recovery

如果存在：

```text
outputs/prune_only/scene_01/ratio_005/
schedule_control/recovery_500_fixed/
point_cloud/iteration_15500/point_cloud.ply
```

对每个 PLY 分别使用：

```text
active_sh_degree = 0
active_sh_degree = 1
active_sh_degree = 2
active_sh_degree = 3
```

计算：

```text
PSNR
SSIM
LPIPS
num_views
```

所有组合必须使用：

```text
相同camera split
相同view names
相同GT image source
相同resolution
相同background
相同pipeline
AppModel disabled
SpecularModel disabled
```

输出：

```text
outputs/debug/stage2bab/
ply_sh_degree_matrix.json

outputs/debug/stage2bab/
ply_sh_degree_matrix.csv

outputs/debug/stage2bab/
ply_sh_degree_matrix_report.md
```

表格：

```text
Model | PLY SHA | SH0 | SH1 | SH2 | SH3
Baseline
Old Buggy Recovery
Fixed Recovery
```

PSNR 至少保留8位小数。

---

# 八、强制检查 baseline_no_app 与 recovery_no_app 是否真的加载不同 PLY

当前 Stage 2B-AA 报告：

```text
baseline_no_app = 23.6824
recovery_no_app = 23.6824
```

创建：

```text
src/evaluation/audit_bundle_debug_loader_identity.py
```

读取产生上述两个指标的：

```text
debug_recovery_with_app_model.py
unified_recovery_evaluator.py
```

分别记录：

```text
baseline requested PLY
baseline actual PLY
baseline SHA
baseline fingerprint

recovery requested PLY
recovery actual PLY
recovery SHA
recovery fingerprint
```

判断：

```text
如果 SHA 相同：
    LOADER_PATH_REUSE_BUG

如果 SHA 不同但 fingerprint 完全相同：
    INVESTIGATE_COPY_OR_WRONG_ARTIFACT

如果 SHA/fingerprint不同且PSNR相同：
    SAME_METRIC_BY_COINCIDENCE_OR_CHANGES_RENDER_NEUTRAL
```

必须明确结论。

输出：

```text
outputs/debug/stage2bab/
bundle_debug_loader_identity_report.md
```

---

# 九、审计 Frozen Parameter Difference

Stage 2B-AA 当前报告：

```text
XYZ max diff = 0.011307
opacity max diff = 22.357542
```

这与 fixed Stage-2 policy 矛盾。

创建：

```text
src/prune/audit_fixed_recovery_frozen_parameters.py
```

只比较：

```text
baseline iteration_15000 checkpoint state
vs
FIXED_POLICY_ARTIFACT iteration_15500 state
```

不要比较 old `recovery_500`。

对于：

```text
xyz
knn_f
opacity
scaling
rotation
```

报告：

```text
shape
mean_abs_diff
max_abs_diff
relative_l2
exact_equal_ratio
```

严格要求：

```text
max_abs_diff <= 1e-7
```

如果 fixed artifact 来自 GPU optimizer 且存在极小数值差异，可报告实际值，但不得放宽到 1e-3。

如果 fixed artifact 中：

```text
xyz max diff > 1e-6
或 opacity max diff > 1e-6
```

则：

```text
FIXED_POLICY_ARTIFACT_INVALID
```

说明：

```text
虽然代码现在已修复，
但当前15500文件不是使用修复代码生成的。
```

---

# 十、Stage 2B-AB 判断树

## Case A：SH mismatch confirmed

满足：

```text
Baseline SH3 ≈ 22～24 dB
Baseline SH0 ≈ 17～18 dB

Fixed Recovery SH3 ≈ baseline
Fixed Recovery SH0 ≈ 17～18 dB
```

结论：

```text
ROOT_CAUSE =
ACTIVE_SH_DEGREE_EVALUATION_MISMATCH
```

当前 Stage 2B FAIL 作废。

修复正式 evaluator：

```text
active_sh_degree = load_ply default / max_sh_degree
```

重新评价现有 fixed recovery。

不重新训练。

---

## Case B：Loader path reuse bug

满足：

```text
baseline/recovery requested path不同
actual SHA相同
```

结论：

```text
ROOT_CAUSE =
CUSTOM_EVALUATOR_PLY_PATH_REUSE
```

修复 exact PLY loader。

重新评价。

不重新训练。

---

## Case C：Audited old artifact

满足：

```text
Stage 2B-AA used recovery_500
且 fixed policy output is recovery_500_fixed
```

结论：

```text
ROOT_CAUSE =
STALE_BUGGY_RECOVERY_ARTIFACT_REUSED
```

旧 `recovery_500` 标记：

```text
INVALID
DO_NOT_USE
```

所有 Gate 只能读取：

```text
recovery_500_fixed
```

---

## Case D：Fixed artifact missing

满足：

```text
recovery_500_fixed does not exist
```

结论：

```text
FIX_CONFIRMED_IN_SMOKE_ONLY
FULL_FIXED_RECOVERY_NOT_YET_GENERATED
```

此时只重新运行：

```text
scene_01 schedule_control 500 steps
```

生成 fixed artifact。

不运行 random/mask-risk。

---

## Case E：SH正确、exact PLY正确、fixed frozen参数正确，但fixed recovery SH3仍下降4～5dB

只有此时才能结论：

```text
FULL_MATCHING_RECOVERY_DEGRADATION
```

下一步才运行：

```text
Native TSGS Resume Control
```

---

# 十一、修改正式 Evaluator Identity

正式 evaluator identity 必须新增：

```text
dataset_sh_degree
active_sh_degree_after_load
max_sh_degree
```

例如：

```json
{
  "dataset_sh_degree": 3,
  "active_sh_degree_after_load": 3,
  "max_sh_degree": 3
}
```

Gate 中要求：

```text
baseline active_sh_degree
==
immediate prune active_sh_degree
==
recovery active_sh_degree
```

不一致时：

```text
EVALUATION_INVALID
```

而不是 FAIL。

---

# 十二、当前 Stage 2B Final Gate 的处理

将当前：

```text
Verdict: FAIL
```

改标记：

```text
INVALID_PENDING_SH_AND_ARTIFACT_AUDIT
```

因为已有证据：

```text
1. schedule_control 与剪枝方法共同下降；
2. AppModel 被排除；
3. custom evaluator 曾使用 active_sh_degree=0；
4. TSGS official load_ply sets active_sh_degree=max_sh_degree；
5. Stage 2B-AA 报告明确读取 recovery_500 路径。
```

当前以下观察继续有效：

```text
mask-risk deletion quality > random
Gaussian count stable
mask-risk recovery score ≈ random under same evaluator
```

当前以下结论无效：

```text
recovery PSNR degraded by 4.86 dB
```

直到 SH 与 artifact identity 审计完成。

---

# 十三、一键运行脚本

创建：

```text
scripts/stage2bab_audit_sh_and_artifacts.sh
```

内容：

```bash
#!/usr/bin/env bash
set -euo pipefail

cd /data/wyh/RecycleGS

export CUDA_VISIBLE_DEVICES=2,3
export PYTHONPATH=/data/wyh/RecycleGS:/data/wyh/repos/TSGS:${PYTHONPATH:-}

SCENE=configs/stage1/reliability_scene01.yaml

mkdir -p outputs/debug/stage2bab

echo "1/6 Audit recovery artifact identities"
python3 src/evaluation/audit_recovery_artifact_identity.py \
  --scene-config $SCENE

echo "2/6 Audit local SH policy"
python3 src/evaluation/audit_local_tsgs_sh_policy.py \
  --scene-config $SCENE

echo "3/6 Audit exact PLY loader"
python3 src/evaluation/debug_exact_ply_loader.py \
  --scene-config $SCENE \
  --all-known-artifacts

echo "4/6 Evaluate PLY x SH-degree matrix"
python3 src/evaluation/evaluate_ply_sh_degree_matrix.py \
  --scene-config $SCENE \
  --degrees 0 1 2 3

echo "5/6 Audit debug loader identity and frozen parameters"
python3 src/evaluation/audit_bundle_debug_loader_identity.py \
  --scene-config $SCENE

python3 src/prune/audit_fixed_recovery_frozen_parameters.py \
  --scene-config $SCENE

echo "6/6 Build Stage 2B-AB diagnosis"
python3 src/prune/build_stage2bab_report.py \
  --scene-config $SCENE

echo "Stage 2B-AB audit complete."
```

执行：

```bash
chmod +x scripts/stage2bab_audit_sh_and_artifacts.sh

bash scripts/stage2bab_audit_sh_and_artifacts.sh
```

---

# 十四、必须生成

```text
outputs/debug/stage2bab/
recovery_artifact_identity_report.md

outputs/debug/stage2bab/
local_sh_policy_report.md

outputs/debug/stage2bab/
ply_sh_degree_matrix_report.md

outputs/debug/stage2bab/
bundle_debug_loader_identity_report.md

outputs/debug/stage2bab/
fixed_recovery_frozen_parameter_report.md

outputs/debug/stage2bab/
stage2bab_final_report.md
```

完成后停止。

禁止：

```text
Stage 3
reseed
重新运行random recovery
重新运行mask-risk recovery
继续调整LR
```

下一步严格依据：

```text
stage2bab_final_report.md
```

只接受：

```text
ACTIVE_SH_DEGREE_EVALUATION_MISMATCH
CUSTOM_EVALUATOR_PLY_PATH_REUSE
STALE_BUGGY_RECOVERY_ARTIFACT_REUSED
FIXED_RECOVERY_ARTIFACT_MISSING
FULL_MATCHING_RECOVERY_DEGRADATION
```
