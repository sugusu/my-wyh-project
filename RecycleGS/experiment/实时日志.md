# RecycleGS 执行进度总览

> 生成时间：2026-07-10 UTC

---

## 第一步：环境初始化 ✅

- 创建 `/data/wyh/RecycleGS/` 工程目录
- 设置 GPU 限制 `CUDA_VISIBLE_DEVICES=2,3`
- 创建实时中文日志 `realtime_log.md`
- 执行环境检查（Python、PyTorch、GPU、依赖）
- 扫描 `/data/wyh/` 已有文件结构
- 创建工程子目录（configs/ src/ scripts/ outputs/ logs/）
- 创建空代码文件（io_utils.py, gaussian_io.py 等 22 个）
- 生成 `configs/scene_01.yaml` 配置模板
- 生成 `outputs/debug/step1_environment_report.md` 环境报告

**关键发现：** Python3 可用（3.10.12），PyTorch 2.6.0+cu124，TransLab scene_01 数据位于 `/data/wyh/f4splat/data/translab/scene_01/`，缺失依赖（trimesh/pyyaml/sklearn/matplotlib/imageio）已在第三步补装。

---

## 第二步：尝试训练 vanilla 3DGS baseline ❌

- 搜索本机已有 3DGS 仓库 → 未找到
- 克隆官方 3DGS `https://github.com/graphdeco-inria/gaussian-splatting` → 通过 gh-proxy 镜像成功
- 安装 Python 依赖（trimesh/pyyaml/sklearn/matplotlib/imageio）→ 成功
- 编译 CUDA 子模块 `diff-gaussian-rasterization` → **失败**
  - 原因1：`glm/glm.hpp` 缺失（third_party/glm 子模块未初始化，GitHub 不可达）
  - 原因2：`Python.h` 缺失（python3-dev 未安装，无 sudo 权限无法 apt 安装）

**当前状态：** 阻塞，等待第三步纠正方向。

---

## 第三步：修正方向为 TSGS baseline ❌

- 全局搜索 `/data/` 下 TSGS 代码 → **未找到**
- 全局搜索 `/data/` 下 TSGS 输出 checkpoint → **未找到**
- 生成 `outputs/debug/tsgs_missing_report.md` 缺失报告

**当前状态：** 阻塞。TSGS 源码/checkpoint 均不存在，无法进入 RecycleGS 主实验。

---

## 整体进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| Step1 环境初始化 | ✅ 完成 | 工程目录、日志、配置、环境报告 |
| Step2 vanilla 3DGS 训练 | ❌ 放弃 | 主线定为 TSGS，不再进行 |
| Step3 TSGS 代码搜索 | ❌ 未找到 | 服务器上无 TSGS 代码/checkpoint |
| **Step4 TSGS 训练** | **✅ 完成** | **已生成正式 Gaussian checkpoint** |
| RecycleGS 主实验 | ⏳ 准备就绪 | 可进入 Stage 1 可靠性分析 |

---

## Step4 详细完成情况

| 子任务 | 状态 | 说明 |
|--------|------|------|
| TSGS 代码克隆 | ✅ | `github.com/longxiang-ai/TSGS` 通过 gh-proxy 镜像成功 |
| 子模块编译 | ✅ | `diff-first-surface-rasterization` + `simple-knn` |
| pytorch3d 依赖 | ✅ | stub 替代（仅 `quaternion_to_matrix`） |
| 数据补全 | ⚠️ | COLMAP 400 帧，仅 10 张独立图像，其余 symlink |
| Smoke test (200 iters) | ✅ | 成功生成 `point_cloud.ply` |
| 正式训练 (15000 iters) | ✅ | 647,830 Gaussians, 156 MB PLY |
| PLY 字段验证 | ✅ | `is_gaussian_ply = True` |
| 配置文件更新 | ✅ | `configs/scene_01.yaml` |
| 训练报告 | ✅ | `baselines/tsgs_scene01_training_report.md` |

## 关键产出

- **Gaussian checkpoint:** `/data/wyh/RecycleGS/baselines/tsgs_scene01/point_cloud/iteration_15000/point_cloud.ply`
- **TSGS 仓库:** `/data/wyh/repos/TSGS`
- **数据目录（临时）:** `/data/wyh/RecycleGS/data/translab/scene_01/`

---

## 完整 TransLab 数据集 ❌ → ✅

| 方式 | 结果 | 说明 |
|------|------|------|
| HF CLI (`hf download`) | ❌ 失败 | 标准 endpoint `Network is unreachable` |
| HF Python API (`snapshot_download`) | ❌ 失败 | `ConnectError: [Errno 101]` |
| `HF_ENDPOINT=https://hf-mirror.com` | ❌ 失败 | 库内部连接错误 |
| **`requests` 直连 `hf-mirror.com` API** | **✅ 成功** | 绕过 huggingface_hub 库，用多线程 + pagination 分批下载 |

**下载结果：** 911 + 809 + 400 = **1,720 个文件全部下载完成**

| 目录 | 文件数 | 大小 |
|------|--------|------|
| images/ | 400 | 1.6 MB |
| delights/ | 400 | 1.6 MB |
| masks/ | 400 | 0.5 MB |
| normals/ | 400 | 1.6 MB |
| transparent_masks/ | 400 | 1.6 MB |
| meshes/ | 2 | 12 KB |
| sparse/ | 7 | 32 KB |
| **合计** | **1,720** | **~8 MB** |

**路径：** `/data/wyh/RecycleGS/data/translab_full/scene_01/`

## Step5：完整数据 TSGS 正式训练 ✅

| 阶段 | 状态 | 说明 |
|------|------|------|
| 数据完整性检查 | ✅ | 1,720 文件全部真实 PNG，0 symlink |
| Smoke test (200 iters) | ✅ | PSNR 17.80 |
| **正式训练 (15000 iters)** | **✅** | **PSNR 28.60, SSIM 0.947, LPIPS 0.095** |
| PLY 字段验证 | ✅ | `is_gaussian_ply = True`，118,557 Gaussians，28 MB |
| 训练报告 | ✅ | `baselines/tsgs_scene01_full_training_report.md` |

**正式 Gaussian checkpoint:**
`/data/wyh/RecycleGS/baselines/tsgs_scene01_full/point_cloud/iteration_15000/point_cloud.ply`

## Stage 6：跨视角几何可靠性分析 (Gate 1A)

| 模块 | 状态 | 说明 |
|------|------|------|
| 坐标系检查 | ✅ | 中心距 34.8%，通过 |
| 冻结参考导出 | ✅ | 64 个视图 |
| Gaussian 基础属性 | ✅ | 位置/opacity/scale/normal |
| Mask 支持度 | ✅ | 64 视图投影 |
| 法线冲突 | ✅ | 跨视角 weighted median |
| 深度排序冲突 | ✅ | 仅惩罚深度参考后方 |
| 表面支持 | ✅ | kNN=24, kdTree |
| Scale 异常 | ✅ | 分位数归一化 |
| 贡献安全门 | ✅ | proxy 模式 |
| GT 误差 | ✅ | 500k 采样点 |
| 风险分数 | ✅ | 加权组合 |
| **Gate 1A** | **❌ FAIL** | 0/3 检查通过 |

## Stage 1B：Gate 1A 失败诊断 ✅

| 诊断模块 | 状态 | 关键发现 |
|----------|------|----------|
| 坐标对齐诊断 | ✅ | 中心距 34.8% > 20% mesh 直径（expected） |
| GT 标签诊断 | ✅ | 3 种阈值方案统计完成 |
| 可靠性特征诊断 | ✅ | **E_depth 100% 为零值（实现错误）**，E_normal/E_support Spearman 为负 |
| Mask 对齐检查 | ✅ | 8 视图可视化完成 |
| Normal convention 检查 | ✅ | 候选轴变换测试完成 |
| Depth order 检查 | ✅ | depth_ref 分位数/有效比例统计完成 |
| 分类可靠性评价 | ✅ | Background/Internal 分支分析完成 |
| 综合诊断报告 | ✅ | **根因：Category G（特征崩溃）** |

## Stage 1D：评价域与标签有效性诊断 ✅

| 诊断模块 | 关键发现 |
|----------|----------|
| Domain GT 分布 | 高误差 Gaussian **99.3% 在不确定域**，物体域仅 207 个 |
| 候选物体域构建 | mask≥0.20 扩展到 **2,318（2.0%）**，含全部 core object |
| 连续误差标签 v2 | surface proxy 全部为 0 — 候选 Gaussian 距 mesh < 2 normal scale |
| 连续可靠性评价 | E_normal Spearman=**0.137**（弱正值），E_support/E_scale 方向反 |
| 7k vs 15k 对比 | 7k 可用，Gaussian 更少（99k vs 118k），d_norm 更低 |

**最终结论：B — 15k checkpoint 过晚**
- TSGS 训练到 15k 时，候选物体域 Gaussian 已全部收敛到 mesh 表面
- 错误的 Gaussian 主要分布在不确定域（低可见性）
- 需要验证更早 checkpoint（7k）来获得有意义的错误分布
